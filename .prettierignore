dist
coverage
node_modules
api/*.mjs
api/*.js
api/*.cjs
.planning/phases/*/quick/**
docs/screenshots
docs/hero.gif
package-lock.json

# Stale parallel-executor worktree snapshots from a separate workstream.
# These contain frozen copies of past plans; they must not be reformatted by
# the canonical sweep (Plan 27.4.2-03 acceptance criterion #6 forbids
# touching files outside .planning/, server/, src/, docs/, repo root).
# Mirrors vite.config.ts test.exclude precedent established by Plan 27.4.2-01.
.claude/

# Phase 28.1 W2/W7 plan files contain TS code in prose (no fence) with
# numeric-separator underscore literals (60_000). Prettier interprets the
# underscores as markdown italic emphasis and corrupts the content
# (e.g. `2 * 60_000` becomes `2 _ 60_000` becomes `2 * 60*000`).
# Adding to ignore list per W1 wave-gate ritual (gate must exit 0 without
# semantic corruption). Owner: planner re-fences these blocks if/when revised.
.planning/phases/28.1-cleanup-sweep/28.1-02-W2-health-endpoint-PLAN.md
.planning/phases/28.1-cleanup-sweep/28.1-07-W7-normalization-PLAN.md

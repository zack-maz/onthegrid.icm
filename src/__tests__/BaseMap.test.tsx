/**
 * BaseMap component tests (Wave 0 stubs)
 * Covers MAP-01a (renders inside container) and MAP-01d (hides road labels on load)
 * These stubs will be implemented in Plan 02 when BaseMap is built.
 */

describe('BaseMap', () => {
  it.todo('renders inside a container (MAP-01a)');

  it.todo('hides road labels on load (MAP-01d)');
});

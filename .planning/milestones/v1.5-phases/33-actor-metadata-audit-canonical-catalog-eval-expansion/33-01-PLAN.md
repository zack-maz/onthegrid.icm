---
phase: 33-actor-metadata-audit-canonical-catalog-eval-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/actorClassifier.ts
  - server/__tests__/lib/actorClassifier.test.ts
  - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts
  - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md
autonomous: true
requirements: [ACTOR-01]
tags: [actor-metadata, audit, classifier]

must_haves:
  truths:
    - 'Operator can read a committed audit report quantifying actor-quality failure buckets in the live events:llm:v3 snapshot (D-01, D-03)'
    - 'Deterministic D-02 classification rules (null/empty, raw CAMEO, ambiguous generic) run from a SINGLE shared module (no drift between audit + dashboard later)'
    - 'Bucket-d (source-disagreement) is NOT auto-detected — audit report seeds 10 human spot-check candidates from a/b/c overlap (D-03)'
    - 'Audit script is one-shot per D-01 (not under npm test); runs via node --import tsx/esm against staging/prod Redis'
  artifacts:
    - path: 'server/lib/actorClassifier.ts'
      provides: 'Shared deterministic classifyActor() + classifyEventActors() per D-02 rules'
      contains: 'RAW_CAMEO_REGEX, AMBIGUOUS_DENY_LIST, classifyActor, classifyEventActors'
    - path: 'server/__tests__/lib/actorClassifier.test.ts'
      provides: 'Unit tests for classifyActor() covering 4 buckets + clean inputs'
    - path: '.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts'
      provides: 'One-shot audit script reading events:llm:v3 and writing 33-AUDIT-REPORT.md'
    - path: '.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md'
      provides: 'Phase artifact: per-bucket counts + 5-10 examples per bucket + 10 bucket-d spot-check seeds'
  key_links:
    - from: '.planning/phases/33-*/audit/run-audit.ts'
      to: 'server/lib/actorClassifier.ts'
      via: "import { classifyEventActors } from '../../../../server/lib/actorClassifier.js'"
      pattern: 'classifyEventActors'
    - from: '.planning/phases/33-*/audit/run-audit.ts'
      to: 'server/cache/redis.ts'
      via: "import { redis } from '../../../../server/cache/redis.js'"
      pattern: "redis\\.get<.*>\\('events:llm:v3'\\)"
---

<objective>
Ship the shared D-02 actor classifier and the one-shot audit script + report, fulfilling ACTOR-01 and laying the dedup foundation for Plan 33-06's operator-status block (Pitfall §1).

Purpose: Quantify actor-metadata failure buckets in the LIVE `events:llm:v3` snapshot so the catalog seed (Plan 02) and ground-truth backfill (Plan 05) are evidence-driven, not guessed.
Output: A pure-fn classifier module + its unit tests, a `tsx`-runnable audit script, and a committed markdown report ready for human bucket-d annotation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md
@CLAUDE.md
@scripts/snapshot-v3-redis.ts
@server/adapters/gdelt.ts
@server/cache/redis.ts

<interfaces>
<!-- Key types and patterns the executor needs. Reading once here avoids codebase scavenger hunt. -->

From server/cache/redis.ts (already established):

- export const redis: Redis (Upstash REST client)
- export async function cacheGetSafe&lt;T&gt;(key, ttlSec): Promise&lt;CacheEntry&lt;T&gt; | null&gt;
- CacheEntry&lt;T&gt; shape: { data: T, fetchedAt: number }

From server/types.ts (existing — do not modify):

- export interface ConflictEventEntity { id: string; data: { actors?: string[]; actorConfidence?: ('high'|'medium'|'low')[]; sourceUrl?: string; ... }; label?: string; ... }

From scripts/snapshot-v3-redis.ts (analog primary):

- Shebang #!/usr/bin/env node + top-of-file JSDoc + Usage block
- import.meta.url → fileURLToPath → \_\_dirname derivation
- safeGet&lt;T&gt;(key) wrapper with try/catch
- prodConfirm gate when env.CACHE_KEY_PREFIX empty (defense-in-depth)
- main().catch((err)=>{ console.error(err); process.exit(1); }) bottom-of-file

From server/adapters/gdelt.ts (analog secondary):

- classifyByBaseCode pattern: pure-fn classifier with module-scope const maps + early-return guard clauses (lines 153-194)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Stub actorClassifier test file (RED)</name>
  <files>server/__tests__/lib/actorClassifier.test.ts</files>
  <read_first>
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 2 — Shared Classifier section; classifier test analog at lines 338-380)
    - server/__tests__/lib/eventScoring.test.ts (pure-fn unit test pattern; no mocks, no Redis)
    - server/__tests__/lib/llmTokenBudget.test.ts (sibling pure-fn pattern)
    - 33-RESEARCH.md §"Validation Architecture" Wave 1 row (ACTOR-01 unit row)
  </read_first>
  <behavior>
    - bucket (a) — empty string '' OR whitespace-only '   ' → 'null'
    - bucket (b) — 'ISRMIL' (regex match AND in codebook Set) → 'raw-cameo'
    - bucket (b) negative — 'XYZAB' (regex match but NOT in codebook) → 'ok'
    - bucket (c) — case-insensitive deny-list: 'Forces', 'FIGHTERS', 'the army', 'militia' → 'ambiguous'
    - clean string — 'Islamic Revolutionary Guard Corps' → 'ok'
    - classifyEventActors(null, codebook) → ['null']
    - classifyEventActors([], codebook) → ['null']
    - classifyEventActors(['', '   '], codebook) → ['null']
    - classifyEventActors(['IDF', 'forces'], codebook) → ['ok', 'ambiguous'] (length-locked output)
  </behavior>
  <action>
    Create `server/__tests__/lib/actorClassifier.test.ts` per 33-PATTERNS.md Layer 2 test analog (lines 338-380). Use `@vitest-environment node` pragma. Import `classifyActor` and `classifyEventActors` from `'../../lib/actorClassifier.js'` (file does not exist yet — test MUST fail RED). Build a fixture codebook `const codebook = new Set(['ISRMIL', 'IRNMIL', 'USMIL'])`. Cover all behaviors above as discrete `it()` blocks under two top-level `describe()` blocks: `'classifyActor — D-02 deterministic rules'` and `'classifyEventActors — bucket (a) covers null + all-empty'`. Do NOT implement the classifier yet — this task ships ONLY the failing test (Wave 0 stub per 33-VALIDATION.md Wave 0 Requirements).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/actorClassifier.test.ts 2>&amp;1 | grep -E "(FAIL|Cannot find module|actorClassifier)" || true</automated>
  </verify>
  <acceptance_criteria>
    - File `server/__tests__/lib/actorClassifier.test.ts` exists
    - `grep -c "classifyActor\|classifyEventActors" server/__tests__/lib/actorClassifier.test.ts` returns ≥ 8 (8 distinct `it()` cases minimum)
    - `grep -c "describe('classifyActor" server/__tests__/lib/actorClassifier.test.ts` returns 1
    - `grep -c "describe('classifyEventActors" server/__tests__/lib/actorClassifier.test.ts` returns 1
    - `npx vitest run server/__tests__/lib/actorClassifier.test.ts` exits non-zero (RED — module not implemented yet)
    - Test references the bucket strings `'null'`, `'raw-cameo'`, `'ambiguous'`, `'ok'` verbatim (no spelling drift from D-02)
  </acceptance_criteria>
  <done>Failing test committed; module implementation follows in Task 2 (GREEN).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement actorClassifier.ts (GREEN, D-02)</name>
  <files>server/lib/actorClassifier.ts</files>
  <read_first>
    - server/__tests__/lib/actorClassifier.test.ts (created in Task 1)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 2 — implementation template at lines 249-336)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-02 deterministic rules verbatim)
    - server/adapters/gdelt.ts (classifyByBaseCode at line 153-194 — pure-fn analog: const maps + early-return guards)
  </read_first>
  <behavior>
    - Module-scope `const RAW_CAMEO_REGEX = /^[A-Z]{3,5}$/`
    - Module-scope `const AMBIGUOUS_DENY_LIST: Set<string>` with the 11 strings from D-02 (c): 'soldiers', 'forces', 'militants', 'troops', 'fighters', 'the army', 'gunmen', 'attackers', 'rebels', 'insurgents', 'militia'
    - `export type ActorIssue = 'ok' | 'null' | 'raw-cameo' | 'ambiguous'`
    - `export function classifyActor(actor: string, cameoCodebook: Set<string>): ActorIssue` — trims input; empty/whitespace → 'null'; regex match AND codebook membership → 'raw-cameo'; lowercase match in deny-list → 'ambiguous'; else 'ok'
    - `export function classifyEventActors(actors: readonly string[] | null | undefined, cameoCodebook: Set<string>): ActorIssue[]` — null/empty/all-empty → ['null']; otherwise length-locked map via classifyActor
    - All 8+ test cases in Task 1's test file pass GREEN
  </behavior>
  <action>
    Create `server/lib/actorClassifier.ts`. Implement per 33-PATTERNS.md Layer 2 implementation template (lines 285-323) verbatim with the D-02 deny-list strings from 33-CONTEXT.md D-02 (c). Pure-fn module: no logger, no Redis import, no env access. Top-of-file JSDoc explaining Pitfall §1 dedup rationale (shared between audit script + operator-status route). Module-scope `const` declarations for the regex + deny-list Set (zero per-call allocation). Use guard-clause early-return style (mirror `gdelt.ts:153-194`). Lowercase deny-list comparison: `AMBIGUOUS_DENY_LIST.has(trimmed.toLowerCase())`. Bucket-d (source-disagreement) is intentionally NOT detected here — document in JSDoc per D-02 final clause.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/actorClassifier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/lib/actorClassifier.ts` exists
    - `grep -c "export function classifyActor\b" server/lib/actorClassifier.ts` returns 1
    - `grep -c "export function classifyEventActors\b" server/lib/actorClassifier.ts` returns 1
    - `grep -c "export type ActorIssue" server/lib/actorClassifier.ts` returns 1
    - `grep "AMBIGUOUS_DENY_LIST" server/lib/actorClassifier.ts | grep -c "Set"` returns ≥ 1 (Set instantiation)
    - All 11 D-02 (c) deny-list strings present: `grep -v '^[[:space:]]*\*' server/lib/actorClassifier.ts | grep -Eo "'(soldiers|forces|militants|troops|fighters|the army|gunmen|attackers|rebels|insurgents|militia)'" | sort -u | wc -l` returns 11
    - `grep -c "/\^\[A-Z\]{3,5}\$/" server/lib/actorClassifier.ts` returns 1 (raw CAMEO regex literal)
    - No logger import: `grep -c "from '.*logger" server/lib/actorClassifier.ts` returns 0
    - No redis import: `grep -c "from '.*redis" server/lib/actorClassifier.ts` returns 0
    - `npx vitest run server/__tests__/lib/actorClassifier.test.ts` exits 0 (all 8+ cases GREEN)
  </acceptance_criteria>
  <done>Pure-fn classifier ships GREEN; bucket-d explicitly deferred via JSDoc.</done>
</task>

<task type="auto">
  <name>Task 3: Implement audit script + commit AUDIT-REPORT.md (D-01, D-03)</name>
  <files>.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts, .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md</files>
  <read_first>
    - scripts/snapshot-v3-redis.ts (primary analog: shebang, prod-confirm gate, safeGet, main().catch invocation)
    - scripts/snapshot-cron-watch.ts (secondary analog: phase-local script + prod-confirm pattern)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 3 — Audit Script section, lines 384-505)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-01, D-02, D-03 — output shape requirements)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (File-Level Plan row 1; threat T-33-01 — prod-confirm gate)
    - server/lib/actorClassifier.ts (Task 2 output — imported by audit)
  </read_first>
  <action>
    PART A — Create `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts`. Per 33-PATTERNS.md Layer 3 (lines 384-505) and 33-RESEARCH.md File-Level Plan row 1:
    - Shebang `#!/usr/bin/env node`
    - Top-of-file JSDoc explaining D-01..D-03 purpose + Usage block (`node --import tsx/esm .planning/phases/33-*/audit/run-audit.ts [--prod-confirm]`)
    - Derive `__dirname` via `fileURLToPath(import.meta.url)` (mirror snapshot-v3-redis.ts:35-42)
    - Define `PHASE_DIR`, `REPORT_PATH = resolve(PHASE_DIR, '33-AUDIT-REPORT.md')`. The CAMEO codebook is consumed by this script — Plan 33-02 ships `.planning/phases/33-*/cameo-codes.json` as a sibling file; this script reads it via `readFileSync(resolve(PHASE_DIR, 'cameo-codes.json'), 'utf-8')` (NOT a hard dependency for THIS plan if codebook not yet present — see fallback below)
    - Import: `import { redis } from '../../../../server/cache/redis.js'`; `import type { ConflictEventEntity } from '../../../../server/types.js'`; `import { classifyEventActors } from '../../../../server/lib/actorClassifier.js'`
    - `async function safeGet<T>(key): Promise<T | null>` wrapper with try/catch and `console.error` (mirror snapshot-v3-redis.ts:106-112)
    - Prod-confirm gate: if `process.env.CACHE_KEY_PREFIX` is empty AND `process.argv` does NOT include `--prod-confirm`, print error to stderr and `process.exit(2)` (mirrors snapshot-v3-redis.ts:135-141, threat T-33-01)
    - `main()` flow: (1) read `events:llm:v3` via `safeGet<ConflictEventEntity[]>`; (2) load codebook from `cameo-codes.json` if present, else fall back to `new Set<string>()` and emit a warning so bucket-b counts only show empty until Plan 02 lands; (3) per-event classify `data.actors`; (4) accumulate three bucket arrays storing `{eventId, actors, sourceUrl}` per CONTEXT D-03; (5) seed bucket-d (source-disagreement) section with 10 RANDOM candidates picked from a/b/c overlap using `Math.random()` shuffle then slice; (6) build markdown via template literal — top-level h1 + h2 per bucket with count + % against `cached.length` + 5-10 examples (capped) + bucket-d header with `[✓ disagrees] / [✗ matches source]` checkbox placeholders; (7) `writeFileSync(REPORT_PATH, md, 'utf-8')`
    - Bottom-of-file: `main().catch((err) => { console.error(err); process.exit(1); })`

    PART B — Commit a stub `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md` with the report skeleton (placeholder counts pending Manual UAT against staging). Top-of-file note: "Stub committed alongside script. Operator runs audit against staging events:llm:v3 cache, then overwrites this file with real counts (see 33-VALIDATION.md Manual-Only Verifications row 1)." This avoids the report file being a Manual-UAT-only artifact that downstream waves cannot see.

    Do NOT add this script to `package.json` scripts — it is one-shot per D-01 ("not under npm test / CI"). Use `node --import tsx/esm` invocation as documented in script header.

  </action>
  <verify>
    <automated>node -e "const{readFileSync,existsSync}=require('node:fs');const p='.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion';if(!existsSync(p+'/audit/run-audit.ts'))process.exit(1);if(!existsSync(p+'/33-AUDIT-REPORT.md'))process.exit(2);const s=readFileSync(p+'/audit/run-audit.ts','utf-8');if(!s.includes('classifyEventActors'))process.exit(3);if(!s.includes('prod-confirm'))process.exit(4);if(!s.includes('events:llm:v3'))process.exit(5);if(!s.includes('33-AUDIT-REPORT.md'))process.exit(6);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` exists
    - File `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md` exists (stub allowed; real population is Manual UAT)
    - `head -1 .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` equals `#!/usr/bin/env node`
    - `grep -c "classifyEventActors" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` returns ≥ 1 (imports + uses the shared classifier — Pitfall §1 dedup honored)
    - `grep -c "events:llm:v3" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` returns ≥ 1
    - `grep -c "prod-confirm" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` returns ≥ 2 (gate + usage block)
    - `grep -c "process.exit(2)" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` returns ≥ 1 (prod-confirm denial exit code)
    - `grep -c "fileURLToPath\|import.meta.url" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` returns ≥ 1 (ESM __dirname derivation)
    - `grep -c "33-AUDIT-REPORT.md" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts` returns ≥ 1 (writes the report file path)
    - Report file `33-AUDIT-REPORT.md` includes section headers for the four buckets (null / raw-cameo / ambiguous / source-disagreement): `grep -Ec "null|raw.cameo|ambiguous|source.disagreement" .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md` returns ≥ 4
    - Script does NOT alter `package.json` (D-01 — "not under npm test"): `git diff --name-only HEAD package.json 2>/dev/null | wc -l` returns 0
    - `npx tsc --noEmit --module nodenext --target esnext --moduleResolution nodenext .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/audit/run-audit.ts 2>&amp;1 | grep -Ec "error TS"` returns 0 (script type-checks against current tsconfig; if codebook file not yet present this should still compile because of optional readFileSync fallback)
  </acceptance_criteria>
  <done>Audit script committed and type-clean; report stub committed; Manual UAT (run against staging) deferred to phase close per 33-VALIDATION.md.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                              |
| ------------------------------- | ------------------------------------------------------------------------ |
| operator → Redis (staging/prod) | Audit script reads `events:llm:v3` with env-loaded credentials           |
| committed report → repo         | Markdown artifact may carry actor names + source URLs from cached events |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                           |
| --------- | ---------------------- | ------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-33-01   | Information Disclosure | `.planning/phases/33-*/audit/run-audit.ts` | mitigate    | Prod-confirm gate (`process.exit(2)` when `CACHE_KEY_PREFIX` empty AND `--prod-confirm` absent) mirrors snapshot-v3-redis.ts:135-141; credentials sourced from `process.env.REDIS_*` only (never inline). |
| T-33-01b  | Information Disclosure | `33-AUDIT-REPORT.md`                       | accept      | Report carries actor names + source URLs that are ALREADY visible via /api/events public endpoint. No new exposure surface. Operator review at commit time covers PR-level vetting.                       |
| T-33-02   | Tampering              | actorClassifier deny-list                  | accept      | D-02 deny-list is committed code, reviewed at PR time; semantic poisoning (e.g., removing "soldiers") requires PR approval. Contract is enforced by the unit tests in Task 1.                             |

</threat_model>

<verification>
After Tasks 1-3 complete:
- `npx vitest run server/__tests__/lib/actorClassifier.test.ts` exits 0
- `npx tsc --noEmit -p tsconfig.json` and `npx tsc --noEmit -p tsconfig.server.json` (or equivalent project tsc invocation per package.json scripts) exits 0 — no new TS errors introduced by the new classifier module
- Audit report stub is committed; Manual UAT against staging Redis populates real counts per 33-VALIDATION.md Manual-Only Verifications row 1
</verification>

<success_criteria>

- ACTOR-01 requirement materially addressed: shared classifier ships, audit script + report stub committed
- Pitfall §1 (Code duplication) prevented: classifier module is the SINGLE source of truth that Plan 33-06 will also import
- D-02 deterministic rules implemented exactly (no language drift between context and code)
- Bucket-d explicitly deferred via JSDoc + report seed pattern (per D-02 final clause)
  </success_criteria>

<output>
Create `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-01-SUMMARY.md` when done. Include:
- Classifier file ship status + line count
- Audit script ship status + invocation command
- Audit report stub status + Manual UAT instructions (deferred to phase close)
- Note: Plan 02 will commit `cameo-codes.json` — script gracefully degrades if not yet present
- Decision-coverage trace: D-01 → audit script; D-02 → actorClassifier deny-list + regex; D-03 → report shape (bucket-d spot-check seeds)
</output>

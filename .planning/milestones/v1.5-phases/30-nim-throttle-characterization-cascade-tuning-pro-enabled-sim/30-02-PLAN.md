---
phase: 30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim
plan: 02
type: execute
wave: 2
depends_on: ['30-01']
files_modified:
  - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
autonomous: false
requirements: [LLM-RELI-02, LLM-RELI-03]
requirements_addressed: [LLM-RELI-02, LLM-RELI-03]
gsd_commit: 'feat(30): characterize NIM throttle on Pro 800s ceiling (Run 1) (D-02)'
must_haves:
  truths:
    - 'Force-trigger of /api/cron/refresh-events?force=true with the CURRENT defaults (concurrency=12, BATCH_SIZE=2, BACKOFF_MS=[1000,4000], jitter=±250ms, hard-kill=90s) completes inside the Vercel Pro 800s ceiling'
    - "Run 1's analyzer output is captured as run-1-throttle-snapshot.json in the phase directory, with all 9 required top-level fields populated"
    - 'events:llm-summary:v3 has completedAt timestamp set after the run (proves run reached terminal write at llmExtractionPipeline.ts:477)'
    - 'Operator inspects the run live during operator-watchful hours (autonomous: false), captures any watchdog hard-kill counts and bucket distribution'
  artifacts:
    - path: '.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json'
      provides: 'First-data-point measurement of NIM throttle behavior at current defaults under Pro 800s ceiling'
      contains: 'throttleWindowMs'
  key_links:
    - from: 'operator curl with CRON_SECRET Bearer'
      to: 'GET /api/cron/refresh-events?force=true (server/routes/refresh-events-cron.ts:37-87)'
      via: 'Authorization: Bearer ${CRON_SECRET}'
      pattern: '/api/cron/refresh-events'
    - from: 'scripts/analyze-llm-run.ts --snapshot=<path>'
      to: 'events:llm-summary:v3 → run-1-throttle-snapshot.json'
      via: 'cacheGetSafe + writeFileSync'
      pattern: 'run-1-throttle-snapshot.json'
---

<objective>
Force-trigger the daily LLM extraction cron at CURRENT v1.4 defaults under the Vercel Pro 800s ceiling and capture the analyzer output as the first measurement data point. This run is the empirical baseline against which Plan 05's tuned defaults will be proposed. The run must complete inside the 800s ceiling (RESEARCH Pitfall 4 math says ~510s at 196 batches × ceil(N/12) × 30s — comfortable) and must be operator-attended (RESEARCH Pitfall 3 — no overnight runs).

Purpose: Without Run 1's actual throttle window / RPM ceiling / recovery interval / per-batch latency p50+p95 / watchdog hard-kill count, every Plan 05 default is a guess. CONTEXT D-02 names this run as the characterization step in the characterize → propose → validate cycle.

Output:

- `run-1-throttle-snapshot.json` committed to the phase directory with all 9 required top-level fields
- Commit message body quotes the snapshot's key numbers (throttle window, RPM, recovery, p50/p95, hard-kill count, eval @ 5/20/100km) so the data is preserved in git log
- Determination of whether Run 1 hit Path A (NIM returns Retry-After header) or Path B (analyzer inferred from timestamp gaps)
- Observation of whether Run 1 yielded any 429s at all (RESEARCH Open Question 2 — if rolling-window limiter is conservative, the run may produce zero 429s and Plan 05 becomes a sanity check)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-VALIDATION.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-01-SUMMARY.md
@server/routes/refresh-events-cron.ts
@scripts/analyze-llm-run.ts

<interfaces>
<!-- Force-trigger entry point — server/routes/refresh-events-cron.ts:37-87 -->
<!-- - timingSafeEqual Bearer check (~line 51) -->
<!-- - parse ?force=true (~line 58) -->
<!-- - runRefreshExtraction({ triggeredBy: 'cron', forceCooldown }) -->
<!-- - cron:lastTick:refresh-events write only AFTER success (~line 74) -->

<!-- Current defaults under test (no code changes in this plan): -->
<!-- - LLM_V3_CONCURRENCY = 12 (server/config.ts:72) -->
<!-- - BATCH_SIZE = 2 hard-coded (server/lib/llmEventExtractor.v3.ts:83) -->
<!-- - RETRY_ATTEMPTS = 2, BACKOFF_MS = [1000, 4000], JITTER_MS = 250 (server/lib/freeClaudeRouter.ts:64-66) -->
<!-- - LLM_BATCH_TIMEOUT_MS = 90_000 default (server/config.ts:51-56) -->

<!-- Required snapshot shape — 9 top-level fields (from 30-RESEARCH.md / 30-VALIDATION.md): -->

{
"runTimestamp": <number>,
"durationMs": <number>,
"batchCount": <number>,
"watchdogTimeoutCount": <number>,
"throttleWindowMs": { "path": "A" | "B", "median": <number|null>, "p95": <number|null> },
"steadyStateRpm": <number>,
"recoveryIntervalMs": <number|null>,
"perBatchLatency": { "p50": <number>, "p95": <number> },
"evalScore": { "within5km": <number>, "within20km": <number>, "within100km": <number>, "total": <number> }
}
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator-attended force-trigger of /api/cron/refresh-events?force=true (Run 1)</name>
  <what-built>
    Plan 01 landed `retryAfterMs` capture in `server/lib/freeClaudeRouter.ts` + the `scripts/analyze-llm-run.ts` analyzer + `npm run analyze:llm-run` script. Plan 01 commit has been pushed to main and Vercel has redeployed (operator must confirm the deploy completed before triggering).
  </what-built>
  <read_first>
    - server/routes/refresh-events-cron.ts (the force-trigger entry point — confirm Bearer auth shape and force=true parsing match current code)
    - scripts/analyze-llm-run.ts (the analyzer landed in Plan 01 — confirm `--snapshot=<path>` arg works)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md sections "Pitfall 3" (operator-watchful hours), "Pitfall 4" (800s ceiling math)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-01-SUMMARY.md (confirm retryAfterMs is in prod)
  </read_first>
  <how-to-verify>
    Pre-flight checks (do these FIRST):
    1. Confirm Plan 01's commit is deployed to prod: `curl -sS https://otg-iran-monitor.vercel.app/api/health | jq -r '.endpoints.llmEvents.status'` should return a value (not error). Vercel deploy log for Plan 01's commit must show "Ready".
    2. Confirm the analyzer script runs locally: `npm run analyze:llm-run -- --fixture=tests/fixtures/run-with-retry-after.json` exits 0 (smoke from Plan 01).
    3. Confirm operator is in a watchful window — operator-local awake, ready to read Vercel logs live (RESEARCH Pitfall 3).
    4. Confirm pipeline is idle before triggering: `curl -sS https://otg-iran-monitor.vercel.app/api/events/llm-status | jq -r '.idle'` should return `true` (no parallel runs, RESEARCH anti-pattern #18 cooldown discipline).
    5. Confirm Vercel project on Pro plan with maxDuration 800: `cat vercel.json | jq '.functions["api/vercel-entry.js"].maxDuration'` returns `800` (Phase 29 D-08 invariant).

    Trigger Run 1 (operator executes):
    ```
    curl -s "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true" \
      -H "Authorization: Bearer ${CRON_SECRET}" \
      | jq .
    ```
    NOTE: The Bearer NEVER appears in code or commit messages — operator pastes it from shell env only (threat T-30-02 mitigation).

    Live monitoring during the run (operator watches Vercel function logs in real time):
    - Watch for `[router] attempt failed` warn-level logs to see 429 frequency / bucket distribution
    - Watch for any `[watchdog]` hard-kill log entries — record the count
    - Verify the function does NOT die at the 800s wall-clock cliff (Pitfall 4)

    Post-run capture (operator executes after Run 1 completes — `cron:lastTick:refresh-events` Redis key updated):
    ```
    npm run analyze:llm-run -- \
      --snapshot=.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    ```
    This reads `events:llm-summary:v3` from prod Upstash (via `.env.local` swap discipline — RESEARCH HANDOFF anti-pattern #8 — or via operator's local `.env` pointing at prod Redis), then writes the JSON snapshot to the phase directory.

    Snapshot validation (operator confirms shape before committing):
    ```
    jq -e '.throttleWindowMs.path' .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    jq -e '.durationMs' .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    jq -e '.batchCount' .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    jq -e '.watchdogTimeoutCount' .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    jq -e '.evalScore.within5km' .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    ```
    All five must exit 0 and return non-null values.

    Commit + push:
    ```
    git add .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
    git commit -m "feat(30): characterize NIM throttle on Pro 800s ceiling (Run 1) (D-02)" -m "Run 1 captures NIM throttle behavior at current v1.4 defaults (concurrency=12, BATCH_SIZE=2, BACKOFF_MS=[1000,4000], jitter=±250ms, hard-kill=90s) under the Pro 800s ceiling. See snapshot file for full numbers." -m "Key metrics (paste from analyzer Markdown):" -m "- Throttle window (Path X): median=Yms, p95=Zms" -m "- Steady-state RPM (NIM): N" -m "- Recovery interval: Mms" -m "- Per-batch latency: p50=A, p95=B" -m "- Watchdog hard-kill count: K" -m "- Eval @ 5/20/100km: a/b/c/total=50"
    ```

    Acceptance criteria validation:
    - `test -f .planning/phases/30-*/run-1-throttle-snapshot.json` exits 0
    - `jq -e '.throttleWindowMs.path' "$_"` returns `"A"` or `"B"` (quoted string)
    - `jq -e '.durationMs < 800000' "$_"` exits 0 (proves run completed inside Pro ceiling)
    - `jq -e '.batchCount > 0' "$_"` exits 0
    - `jq -e '.evalScore.total > 0' "$_"` exits 0
    - Commit message body contains the analyzer's key metrics (operator pastes from Markdown output)

    Failure modes (RESEARCH Pitfall 4 warning signs):
    - `events:llm-summary:v3.completedAt` missing despite `startedAt` set → run was killed mid-flight → DO NOT COMMIT the snapshot; investigate watchdog hard-kill or function timeout; rerun.
    - `cron:lastTick:refresh-events` not advancing → terminal write at line 477 never fired → DO NOT COMMIT; investigate.
    - `events:llm-dlq` populated with `reason: 'timeout_watchdog'` entries → some batches died at 90s hard-kill → DO commit the snapshot (this is signal: existing hard-kill is too tight), record the count in commit body. Plan 05 uses this number.
    - `throttleWindowMs.path === 'B' && throttleWindowMs.median === null` (zero 429s observed) → rolling-window limiter is conservative (RESEARCH Open Question 2). DO commit; flag in commit body that Plan 05 becomes a sanity-check run not a re-tuning run.

  </how-to-verify>
  <resume-signal>
    Type "Run 1 complete, snapshot committed" or paste the analyzer's Markdown table block (so the next plan can read it from the discussion log). If Run 1 failed mid-flight, type "Run 1 failed — investigating" with the failure mode observed; this plan must complete before Wave 3 begins.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                   |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| Operator shell → /api/cron/refresh-events  | Bearer-gated entry point; `timingSafeEqual` constant-time compare against CRON_SECRET                         |
| Vercel function → NIM API                  | NIM provider responds with 429s + optional Retry-After header; rate-limited by RollingWindow primitive        |
| Operator local `.env.local` → prod Upstash | Analyzer reads prod state via the operator's environment; risk of accidental write if a wrong wrapper is used |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-02   | Information Disclosure | CRON_SECRET Bearer in operator shell              | mitigate    | Bearer NEVER appears in code, commit messages, or PRs — operator pastes from shell env (`${CRON_SECRET}`) at trigger time; shell history is operator-local and not committed. Plan task explicitly states this constraint. Severity: MEDIUM.                                        |
| T-30-02a  | Elevation of Privilege | force=true query param bypasses cooldown          | accept      | `?force=true` is Bearer-gated; without Bearer, request is rejected with 401 before reaching `runRefreshExtraction`. Cooldown discipline (anti-pattern #18) is preserved by `runRefreshExtraction` short-circuit at llmExtractionPipeline.ts:234-240 if a parallel run is in flight. |
| T-30-02b  | Denial of Service      | Operator triggers Run 1 during normal cron window | mitigate    | Pre-flight check #4 requires pipeline idle before triggering; cooldown short-circuit prevents parallel runs. Schedule Run 1 during operator-watchful hours per RESEARCH Pitfall 3 (autonomous: false enforces this).                                                                |

</threat_model>

<verification>
- Run 1 snapshot file exists at `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json`
- `jq -e '.throttleWindowMs.path' "$_"` returns "A" or "B"
- `jq -e '.durationMs' "$_"` returns a number < 800000 (proves run completed within Pro ceiling)
- `jq -e '.batchCount and .watchdogTimeoutCount >= 0 and .evalScore.total > 0' "$_"` returns true
- Commit message body contains analyzer's Markdown-table key numbers (operator-pasted)
- Pipeline returned to idle after Run 1: `curl -sS .../api/events/llm-status | jq -r '.idle'` returns `true` before Wave 3 starts
- No `events:llm-summary:v3.error` is set (proves run did not abort)
</verification>

<success_criteria>

- run-1-throttle-snapshot.json committed to phase directory with all 9 required fields populated
- Run completed inside Vercel Pro 800s wall-clock ceiling (durationMs < 800_000)
- Commit `feat(30): characterize NIM throttle on Pro 800s ceiling (Run 1) (D-02)` lands with key metrics in body
- Plan 05 (Wave 3, depends on this plan) has empirical numbers to derive new defaults from, NOT guesses
- If Run 1 yielded zero 429s, this is documented in commit body so Plan 05 / Plan 06 know to treat Run 2 as sanity check
  </success_criteria>

<output>
After completion, create `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-02-SUMMARY.md` documenting:
- Run 1 wall-clock duration
- Throttle window: Path A or Path B, median + p95 values
- Steady-state RPM ceiling observed
- Recovery interval observed
- Per-batch latency p50 + p95
- Watchdog hard-kill count (if any)
- Eval @ 5/20/100km scores
- Any 429-bucket anomalies in callHistory (Open Question 2 — zero 429s would flag here)
- Operator's read of "where the system has slack" — quote from live log inspection
- Pointer to the snapshot file so Plans 03, 05, 06, 07 can reference it
</output>

# Phase 30.1: Cascade Fallback Fix — Re-enable OpenRouter or Document Single-Provider Reality — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `30.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-17
**Phase:** 30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single
**Areas discussed:** Scope tier, OR re-validation method, Breaker-trip behavior, NIM rate-limit window

---

## Scope Tier

### Q1.1: Which scope tier for Phase 30.1?

| Option              | Description                                                                                                                                                                                                                                                                          | Selected |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| Right (recommended) | Re-test OR free-tier (small Bearer-gated probe). If viable: remove skipOpenRouter from both v3.ts call sites + rewrite freeClaudeRouter P3 test + redeploy + observe one cron. If not viable: amend docs + plan paid OR / rate-limit tightening as Phase 31 follow-up. ~2-3 commits. | ✓        |
| Minimum             | Docs-only: amend reliability doc + ADR-0010 sub-block to read "NIM-only active; OR dormant pending re-validation", add Phase-31 carry-forward note, no code change. 1 commit.                                                                                                        |          |
| Full                | Right scope + dashboard surface for cascade-degraded state + DLQ-threshold alert. 5-8 commits. Probably its own phase.                                                                                                                                                               |          |

**User's choice:** Right (recommended).
**Notes:** Right tier closes the cascade gap honestly either way — the probe result selects between cascade-restored and Minimum-scope amendment within the same phase. Full tier deferred to its own phase because dashboard surface overlaps Phase 32/34 work.

### Q1.2: If Right and OR's free-tier re-test fails badly (>=90% rate_limit), what's the abort path?

| Option                        | Description                                                                                                                                                                | Selected |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Drop to Minimum in same phase | Pivot to docs-only amendment within Phase 30.1 — commit probe snapshot + update ADR-0010 to declare NIM-only. Single phase still closes. Recommended — keeps phase atomic. | ✓        |
| Defer fix to Phase 31         | Phase 30.1 ships only the probe results + a note. Phase 31 absorbs paid-OR / rate-limit tightening. Risk: muddies Phase 31 scope.                                          |          |
| Promote to a new phase        | 30.1 closes after probe; new phase (30.2 or 33.5) takes on paid-OR evaluation. Cleanest separation, more roadmap churn.                                                    |          |

**User's choice:** Drop to Minimum in same phase.
**Notes:** Keeps phase atomic regardless of probe outcome. The phase exists to close the cascade question; the probe is the data, the docs amendment is the closure.

### Q1.3: How does the architecture doc handle the raw-GDELT terminal-fallback contract once the cascade decision is documented?

| Option                                         | Description                                                                                                                                                                      | Selected                                                     |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Make it explicit alongside the cascade outcome | Add one paragraph: "Even with NIM→OR restored, if both fail the cron drops the batch and /api/events serves raw GDELT via Pitfall 1. Map never goes blank." Belt-and-suspenders. | ✓ (Claude's recommendation, user said "your recommendation") |
| Leave it to ADR-0010 cross-ref                 | ADR-0010 already declares raw GDELT as terminal fallback; reliability doc links to it. Less duplication.                                                                         |                                                              |
| Add it to CLAUDE.md instead                    | CLAUDE.md already has the "Graceful degradation" bullet; one extra sentence there. Risk: pressures the 5018-token budget.                                                        |                                                              |

**User's choice:** Claude's recommendation = "Make it explicit alongside the cascade outcome."
**Notes:** The whole reason this phase exists is that an operator missed silent `skipOpenRouter: true`. Belt-and-suspenders text is cheap insurance against the same class of miss.

### Q1.4: How do we handle the three out-of-scope items the HANDOFF flagged?

| Option                                  | Description                                                                                                                                                 | Selected |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Defer all three to Phase 31 prep        | 30.1 stays atomic on the cascade question; Phase 31 absorbs them since they all touch cron-run reliability. Recommended — matches HANDOFF's own suggestion. | ✓        |
| Fold the CACHE_KEY_PREFIX fix in        | 5-LOC analyzer-script `--help` tweak, low risk, we'll touch the analyzer anyway. Defer the other two.                                                       |          |
| Spin each into a separate decimal phase | 30.2 / 30.3 / 30.4. Granular but over-engineered for ~30 LOC total.                                                                                         |          |

**User's choice:** Defer all three to Phase 31 prep.
**Notes:** Phase 31 prep section in CONTEXT.md deferred lists all three with their full root-cause descriptions.

---

## OR Re-Validation Method

### Q2.1: Where does the OR re-validation probe live?

| Option                                        | Description                                                                                                                                                                                         | Selected |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Standalone script (recommended)               | `scripts/probe-openrouter.ts`. Mirrors Phase 30 D-01 analyzer + eval-replay pattern. Uses production OPENROUTER_API_KEY (rate limits per-account, so dev run is representative). Zero prod surface. | ✓        |
| Temp Bearer-gated /api/diag/or-pulse endpoint | Lives in prod under existing operator-status pattern. Pro: tests exact Vercel-egress IP. Con: requires two deploys, pollutes route surface.                                                         |          |
| Extend eval-replay with --provider=openrouter | Reuse existing eval-replay scaffolding. Con: co-mingles one-off cascade probe with recurring resolver-eval tool — bad cohesion.                                                                     |          |

**User's choice:** Standalone script (recommended).
**Notes:** Re-runnable for regression checks, zero prod surface added.

### Q2.2: Probe sample shape — what's a meaningful N + payload?

| Option                                              | Description                                                                                         | Selected |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------- |
| N=30 single-event payloads, 100ms gap (recommended) | Matches v3 effective batch shape; binomial confidence ±10% across three buckets; ~5 min wall-clock. | ✓        |
| N=50, matching ground-truth eval size               | Mental-bracket symmetry, doubles wall-clock, no statistical gain.                                   |          |
| N=10 sanity check first, then scale                 | Two-stage saves time in bad case.                                                                   |          |

**User's choice:** N=30 single-event payloads, 100ms gap.

### Q2.3: Viability thresholds — what % fail rate triggers which path?

| Option                                                               | Description                                                     | Selected |
| -------------------------------------------------------------------- | --------------------------------------------------------------- | -------- |
| Three buckets: <30/30-70/>70 (recommended Claude)                    | Tighter than seed; closes 50% middle ground.                    |          |
| Seed's original: <50% viable, 50-90% case-by-case, >90% not worth it | Wider "restore" band catches more cases; matches seed verbatim. | ✓        |
| Binary <20% or Minimum                                               | Aggressive; removes paid-OR middle bucket entirely.             |          |

**User's choice:** Seed's original three-bucket thresholds.
**Notes:** Per-call retry budget exists to handle some failure rate; <50% is acceptable. 50-90% middle bucket explicitly stays a paid-OR conversation for Phase 31, not a 30.1 ship-blocker.

### Q2.4: Where does the probe output get committed as evidence?

| Option                                             | Description                                                                                                                                                 | Selected |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Committed JSON snapshot in phase dir (recommended) | `30.1-or-pulse-snapshot.json` + 1-paragraph results summary in PLAN.md. Mirrors Phase 30 D-08 run-1-throttle-snapshot.json pattern. Auditable, re-runnable. | ✓        |
| Inline in commit message only                      | Numbers paste into commit body. Less file churn, harder to diff.                                                                                            |          |
| Both: snapshot file AND commit-message summary     | Belt-and-suspenders, slight ceremony.                                                                                                                       |          |

**User's choice:** Committed JSON snapshot in phase dir.

---

## Breaker-Trip Behavior Contract

### Q3.1: If OR is viable (<50% fail), is removing skipOpenRouter the full breaker-trip behavior fix?

| Option                                                             | Description                                                                                          | Selected |
| ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- | -------- |
| Yes — cascade as-built handles it (recommended)                    | freeClaudeRouter.ts:341-363 already implements fall-through. Two-line code change + P3 test rewrite. | ✓        |
| Yes, plus add a routingTrace assertion in the next cron's analyzer | Extra: analyze-llm-run.ts asserts ≥1 `provider: 'openrouter'` row across the run.                    |          |
| No — also tighten the breaker                                      | Raise threshold to 40% or extend window to 20 calls. Scope creep into breaker tuning.                |          |

**User's choice:** Yes — cascade as-built handles it.

### Q3.2: If OR is NOT viable (>90% fail), what's the documented breaker-trip behavior?

| Option                                                       | Description                                                                                       | Selected |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | -------- |
| Drop batch + Pitfall-1 GDELT terminal fallback (recommended) | Current behavior, documented honestly. Zero code change, alignment between docs and reality.      | ✓        |
| Pause new extractions for rest of cron window                | Redis sentinel `events:llm-cron-paused-until`. ~30 LOC of new code + state machine. Medium scope. |          |
| Switch NIM model to lower-cap-friendly variant               | Requires fresh bake-off vs Phase 27.4.1 qwen-235b. Risk of eval regression. Out of scope.         |          |

**User's choice:** Drop batch + Pitfall-1 GDELT terminal fallback.

### Q3.3: Should the breaker pause threshold itself be revisited in 30.1?

| Option                                              | Description                                                                                                                  | Selected |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------- |
| No — leave llmCircuitBreaker.ts alone (recommended) | Phase 30 left breaker out of scope; 30.1 follows same discipline. Phase 31's 7-day watch is natural place if drift surfaces. | ✓        |
| Loosen the breaker if OR is viable                  | Fewer false-positive trips at cost of more wasted NIM calls. Scope bloat.                                                    |          |
| Tighten the breaker if OR is NOT viable             | Drop sooner to conserve NIM budget. Couples 30.1 to breaker-tuning conversation.                                             |          |

**User's choice:** No — leave llmCircuitBreaker.ts alone.

### Q3.4: How honestly does the commit + ADR-0010 sub-block name the probe result?

| Option                                                 | Description                                                                                                                                                                               | Selected |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Name the percentages and date explicitly (recommended) | Commit body + ADR-0010 sub-block record: "Re-tested OR free-tier 2026-05-XX: N/30 rate-limited (NN%). Conclusion: ..." The artifact prevents the next operator from making the same call. | ✓        |
| Cite the snapshot file, no numbers in prose            | Body links to or-pulse-snapshot.json. Pulls future readers across files for basic "why."                                                                                                  |          |
| Numbers in ADR-0010 only, terse commit body            | ADR purist split.                                                                                                                                                                         |          |

**User's choice:** Name the percentages and date explicitly.

---

## NIM Rate-Limit Window

### Q4.1: Is the NIM rate-limit window itself part of Phase 30.1's scope, or deferred?

| Option                                        | Description                                                                                                                                    | Selected |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Defer to Phase 31's 7-day watch (recommended) | Phase 31 specifically observes daily cron health for 7 days. Once 30.1 restores cascade, `used:45/cap:40` overshoot becomes much lower-stakes. | ✓        |
| Tighten the window cap from 40 to 35 in 30.1  | Two-line code change. Bakes a fudge factor without measuring root cause.                                                                       |          |
| Make the limiter Retry-After-adaptive in 30.1 | ~30-50 LOC + tests + observability. Pushes 30.1 toward Full tier.                                                                              |          |

**User's choice:** Defer to Phase 31's 7-day watch.

### Q4.2: How do we record the limiter overshoot evidence?

| Option                                                         | Description                                                                                                                                 | Selected |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Reference the 04:00 UTC cron evidence in PLAN.md (recommended) | `used: 45 / cap: 40` from prod Redis `dev: events:llm-summary:v3` already quoted in HANDOFF.md. PLAN.md cites it with one-line explanation. | ✓        |
| Capture a fresh limiter-trace snapshot before 30.1 ships       | Extra commit, redundant with HANDOFF.md.                                                                                                    |          |
| Write the evidence section into reliability architecture doc   | Belt-and-suspenders alongside the cascade-outcome doc update.                                                                               |          |

**User's choice:** Reference the 04:00 UTC cron evidence in PLAN.md.

### Q4.3: Where does the Phase-31-triggered adaptive limiter work get pre-baked into 30.1?

| Option                                                            | Description                                                          | Selected |
| ----------------------------------------------------------------- | -------------------------------------------------------------------- | -------- |
| Add a deferred entry to Phase 31 prep in CONTEXT.md (recommended) | Crisp written escalation rule, no scope expansion in 30.1.           | ✓        |
| Add it as a TODO comment at freeClaudeRouter.ts:190               | Inline TODO; CLAUDE.md trim explicitly fought TODO rot.              |          |
| Skip pre-baking — let Phase 31 surface it naturally               | Cleanest separation, slight risk rationale gets lost between phases. |          |

**User's choice:** Add a deferred entry to Phase 31 prep in CONTEXT.md.

### Q4.4: Does the planner need any guidance on how to test the cascade restore without burning prod NIM quota?

| Option                                                            | Description                                                                                             | Selected |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------- |
| Use Phase 30 D-01 analyzer + force-trigger one cron (recommended) | Same protocol as Phase 30 Run 2. routingTrace reveals OR rows if cascade restore took effect.           | ✓        |
| Spawn dev-only unit tests that mock NIM 429s                      | Extend freeClaudeRouter.test.ts with positive-case OR-rows-on-NIM-429 tests. Catches regressions in CI. |          |
| Both — unit tests + one force-triggered cron observation          | Belt-and-suspenders.                                                                                    |          |

**User's choice:** Use Phase 30 D-01 analyzer + force-trigger one cron.
**Notes:** Unit-test rewrite of the existing P3 test is in scope anyway (D-15) — both happen, but the live-path validation is the operative test.

---

## Claude's Discretion

- Exact wording of the analyze-llm-run.ts output assertion that confirms an OR row landed.
- Whether the rewritten P3 test extends `freeClaudeRouter.test.ts` or moves to a new file.
- Whether the architecture-doc `## Cascade Reality (Phase 30.1)` section uses a markdown table or a fenced JSON block for the probe results.
- Whether the validation cron's routingTrace JSON snapshot is committed in full or just the OR-row excerpt.

## Deferred Ideas (full list in CONTEXT.md `<deferred>` section)

- Eval-fixture not bundled into Vercel deploy → Phase 31 prep.
- Diff-filter ID-mismatch in llmExtractionPipeline.ts → Phase 31 prep.
- CACHE_KEY_PREFIX whitespace gotcha in analyzer → Phase 31 prep.
- Adaptive Retry-After-aware NIM limiter → Phase 31+ conditional on 7-day watch.
- Paid-OR conversion → Phase 31+ conditional on middle-bucket probe outcome.
- NIM model switch → out of scope unless evidence forces it.
- Dashboard surface for cascade-degraded state → its own phase, overlaps Phase 32/34.
- DLQ-threshold alert → same dashboard-phase candidate.
- llmCircuitBreaker.ts re-tuning → conditional on Phase 31 drift findings.

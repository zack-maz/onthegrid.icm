---
phase: 30.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe-openrouter.ts
  - package.json
  - .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
autonomous: false
requirements: []
decisions_addressed: [D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - 'Operator can re-run the OpenRouter probe on demand and get a byte-stable JSON snapshot'
    - 'Probe result classifies into one of three buckets (<50% / 50-90% / >90% rate_limit fail) per D-05'
    - 'Snapshot artifact contains all evidence needed for D-10 audit-trail commit wording'
    - 'Operator confirms bucket assignment at checkpoint before any code change is attempted'
    - 'Probe uses the exact same OpenRouter model string the production cascade uses (imported from server/lib/freeClaudeRouter.ts — no hardcoded copy)'
  artifacts:
    - path: scripts/probe-openrouter.ts
      provides: 'Standalone OpenRouter rate-limit probe (30 single-event payloads, 100ms gap); imports OPENROUTER_DEFAULT_MODEL from server/lib/freeClaudeRouter.ts'
      contains: 'OPENROUTER_DEFAULT_MODEL'
    - path: .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
      provides: 'Probe result snapshot with timestamp, n, gapMs, model, results[], summary{rateLimitedCount,rateLimitedPct,decision}'
      contains: 'rateLimitedPct'
    - path: package.json
      provides: 'npm script probe:openrouter wired with .env.local loader'
      contains: 'probe:openrouter'
  key_links:
    - from: scripts/probe-openrouter.ts
      to: OPENROUTER_API_KEY env var
      via: 'process.env (loaded by --env-file-if-exists=.env.local)'
      pattern: 'OPENROUTER_API_KEY'
    - from: scripts/probe-openrouter.ts
      to: server/lib/freeClaudeRouter.ts OPENROUTER_DEFAULT_MODEL export
      via: 'ES import — probe uses the canonical production model string, not a hardcoded copy'
      pattern: 'OPENROUTER_DEFAULT_MODEL'
    - from: scripts/probe-openrouter.ts
      to: .planning/phases/30.1-.../30.1-or-pulse-snapshot.json
      via: 'atomic tempfile-rename write'
      pattern: 'renameSync'
---

<objective>
Author the OpenRouter free-tier rate-limit probe (`scripts/probe-openrouter.ts`), run it once, capture the result as a byte-stable JSON snapshot, then halt at a checkpoint so the operator confirms which Wave 2 plan (02 Right scope / 03 middle bucket / 04 Minimum scope) to execute next.

Purpose: Drive the rest of Phase 30.1 from one fresh measurement of OpenRouter behavior instead of the stale Phase 27.4.4 "16/16 rate_limited" observation. This single plan is the gating decision for the entire phase.

Output:

- `scripts/probe-openrouter.ts` (new)
- `package.json` script `probe:openrouter` (new)
- `.planning/phases/30.1-.../30.1-or-pulse-snapshot.json` (probe result artifact)
- One commit: `feat(30.1): add scripts/probe-openrouter.ts + 30.1-or-pulse-snapshot.json (D-03..D-06)`
- Checkpoint result: operator selects Plan 02, 03, or 04 for Wave 2.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-CONTEXT.md
@.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/HANDOFF.md
@scripts/eval-replay.ts
@scripts/refresh-water-facilities.ts
@scripts/analyze-llm-run.ts
@server/lib/freeClaudeRouter.ts
@server/config.ts
@package.json

<interfaces>
<!-- Key types and patterns the executor needs. Already in the codebase. -->

From server/lib/freeClaudeRouter.ts lines 125-135 (the canonical OpenRouter model constant — IMPORT this, do NOT hardcode a copy):

```typescript
// D-09: OpenRouter free-tier fallback model.
const OPENROUTER_DEFAULT_MODEL = 'meta-llama/llama-3.3-70b-instruct:free';
// D-09: free-tier daily request cap for OpenRouter (rough envelope; per-model
// caps vary 100-200/day on the free pool).
const OPENROUTER_DAILY_CAP = 200;
```

NOTE: The constant is currently module-private (no `export`). The probe MUST import it directly — Task 1 instructs the executor to add `export` to that declaration in `server/lib/freeClaudeRouter.ts` AND import it in `scripts/probe-openrouter.ts`. The probe MUST NOT hardcode any model string; the whole rate-limit measurement is only meaningful if it probes the SAME model the cascade actually uses.

From server/lib/freeClaudeRouter.ts (base URL + client factory shape):

```typescript
const OPENROUTER_BASE = 'https://openrouter.ai/api/v1';
function getOpenRouterClient(): OpenAI | null {
  if (!env.OPENROUTER_API_KEY) return null;
  return new OpenAI({
    apiKey: env.OPENROUTER_API_KEY,
    baseURL: OPENROUTER_BASE,
    timeout: LLM_TIMEOUT_MS,
  });
}
```

From server/lib/freeClaudeRouter.ts:255-265 (error classifier — copy the rate_limit check):

```typescript
export function classifyError(err: unknown): RouterErrorBucket {
  if (err instanceof Error) {
    const m = err.message.toLowerCase();
    if (m.includes('429') || m.includes('rate limit')) return 'rate_limit';
    if (m.includes('timeout') || m.includes('timed out')) return 'timeout';
    if (m.includes('enotfound') || m.includes('econnreset') || m.includes('eai_again'))
      return 'network';
    if (/\b5\d\d\b/.test(m)) return 'upstream_500';
  }
  return 'other';
}
```

From scripts/refresh-water-facilities.ts (atomic write pattern — REUSE verbatim):

```typescript
import { writeFileSync, renameSync, mkdirSync, unlinkSync, existsSync } from 'node:fs';
const TMP_PATH = resolve(OUT_DIR, '<name>.json.tmp');
const jsonStr = JSON.stringify(snapshot, null, 2) + '\n';
writeFileSync(TMP_PATH, jsonStr);
renameSync(TMP_PATH, OUT_PATH);
```

From scripts/eval-replay.ts (runner pattern — npm script + tsx/esm):

```json
"eval:replay": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --import tsx/esm scripts/eval-replay.ts"
```

From CLAUDE.md (logger contract):

```typescript
const log = logger.child({ module: 'probeOpenRouter' }); // never console.*
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/probe-openrouter.ts (with imported model constant)</name>
  <files>scripts/probe-openrouter.ts, server/lib/freeClaudeRouter.ts</files>
  <read_first>
    - .planning/phases/30.1-.../30.1-CONTEXT.md sections "OpenRouter Re-Validation Probe" (D-03..D-06) and "Specifics" (byte-stability requirement) — implement to those exact specs, not freelance.
    - scripts/refresh-water-facilities.ts — copy the atomic tempfile + rename JSON-write pattern verbatim (lines 109-116 + the catch block).
    - scripts/eval-replay.ts — copy the file-header + main() + catch-and-process.exit shape verbatim.
    - server/lib/freeClaudeRouter.ts lines 125-135 — see the `OPENROUTER_DEFAULT_MODEL` declaration. Currently module-private; the executor adds `export` to it as part of this task so the probe can import it. The current value is `'meta-llama/llama-3.3-70b-instruct:free'` (CONFIRM at read time — the probe MUST measure whatever model the cascade actually uses, never a hardcoded literal).
    - server/lib/freeClaudeRouter.ts lines 330-365 — confirm the OPENROUTER_BASE constant + getOpenRouterClient shape + the classifyError 429/rate-limit pattern.
    - server/config.ts — confirm OPENROUTER_API_KEY is in the Zod schema and accessible via the same import path the production code uses; the probe must reuse, not duplicate, that env loading.
    - server/lib/logger.ts — confirm logger.child({ module }) shape (CLAUDE.md mandates structured logging; no console.*).
  </read_first>
  <action>
    Two file edits:

    Edit 1 — `server/lib/freeClaudeRouter.ts`: locate the line `const OPENROUTER_DEFAULT_MODEL = 'meta-llama/llama-3.3-70b-instruct:free';` (~line 129). Prepend `export` so it reads:

    ```typescript
    export const OPENROUTER_DEFAULT_MODEL = 'meta-llama/llama-3.3-70b-instruct:free';
    ```

    Do NOT change the string value, do NOT touch surrounding code, do NOT change `OPENROUTER_DAILY_CAP`. This is a single 7-character addition (`export ` prefix). All existing callers inside this file continue to reference the constant by its bare identifier — adding `export` is non-breaking.

    Edit 2 — Create `scripts/probe-openrouter.ts` mirroring `scripts/eval-replay.ts` + `scripts/refresh-water-facilities.ts` shape:

    1. File header docstring referencing Phase 30.1 D-03..D-06 + the audit purpose (re-validate OpenRouter free-tier rate-limit behavior after Phase 27.4.4's 16/16 measurement is stale).
    2. Imports: `OpenAI` from 'openai', `writeFileSync`, `renameSync`, `mkdirSync`, `unlinkSync`, `existsSync` from 'node:fs', `resolve`, `dirname` from 'node:path', `fileURLToPath` from 'node:url'. Import logger from `'../server/lib/logger.js'` and use `logger.child({ module: 'probeOpenRouter' })`. Import env from `'../server/config.js'` so OPENROUTER_API_KEY is validated by parseEnv — fail-fast on missing. **Import `OPENROUTER_DEFAULT_MODEL` from `'../server/lib/freeClaudeRouter.js'`** — this is the canonical production model; the probe MUST NOT hardcode a copy.
    3. Constants at top:
       - `const N = 30;` (D-04 sample size)
       - `const GAP_MS = 100;` (D-04 inter-call gap)
       - `const OPENROUTER_BASE = 'https://openrouter.ai/api/v1';`
       - **NO local `MODEL` constant. Use `OPENROUTER_DEFAULT_MODEL` (the imported symbol) directly** in the client call and in the snapshot's `model` field. This guarantees the probe and the production cascade always measure the same model — if production switches models, the probe automatically follows.
       - `const OUT_DIR = resolve(__dirname, '../.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single');`
       - `const OUT_PATH = resolve(OUT_DIR, '30.1-or-pulse-snapshot.json');`
       - `const TMP_PATH = resolve(OUT_DIR, '30.1-or-pulse-snapshot.json.tmp');`
    4. `classifyOutcome(err: unknown): 'rate_limit' | 'other_error' | 'ok'` helper: mirror the 429/'rate limit' substring check from `server/lib/freeClaudeRouter.ts:classifyError`. 'ok' is reserved for the success branch (called separately).
    5. `singleEventPayload(): OpenAI.Chat.ChatCompletionMessageParam[]` helper: return a minimal 2-message conversation (system + 1-line user "Extract location from: Tehran airstrike 2026-05-01") that mirrors the single-event shape v3 produces post-Phase-30. Body is intentionally minimal — the test is rate-limit behavior, not extraction quality.
    6. `main()` async function:
       a. Build OpenAI client `new OpenAI({ apiKey: env.OPENROUTER_API_KEY, baseURL: OPENROUTER_BASE, timeout: 30_000 })`.
       b. Initialize `results: Array<{attempt:number, status:'ok'|'rate_limit'|'other_error', latencyMs:number, retryAfterMs?:number, errorMessage?:string}> = []`.
       c. Loop `for (let attempt = 1; attempt <= N; attempt++)`:
          - `const t0 = Date.now();`
          - try: `await client.chat.completions.create({ model: OPENROUTER_DEFAULT_MODEL, messages: singleEventPayload(), max_tokens: 256, response_format: { type: 'json_object' } })`; on success push `{attempt, status:'ok', latencyMs: Math.round(Date.now() - t0)}`.
          - catch err: capture `latencyMs = Math.round(Date.now() - t0)`. Call `classifyOutcome(err)`. If `err` is an OpenAI APIError, attempt to read `err.headers?.['retry-after']` (case-insensitive); parseFloat * 1000 if numeric; pass through as `retryAfterMs` ONLY if `Number.isFinite(parsed) && parsed > 0`. Push `{attempt, status, latencyMs, retryAfterMs?, errorMessage: err instanceof Error ? err.message.slice(0, 200) : String(err).slice(0, 200)}`. NEVER include the raw response or any header field other than retry-after (T-30.1-01 mitigation — see threat_model).
          - Sleep `GAP_MS` between calls: `await new Promise(r => setTimeout(r, GAP_MS));` (skip the final sleep after attempt N).
          - Log per-attempt outcome via `log.info({ attempt, status, latencyMs }, 'probe attempt complete')` — never `console.log`.
       d. After the loop: sort `results.sort((a,b) => a.attempt - b.attempt)` (byte-stability per Specifics).
       e. Build summary: `rateLimitedCount = results.filter(r => r.status === 'rate_limit').length`; `rateLimitedPct = Math.round((rateLimitedCount / N) * 1000) / 10` (1 dp); `decision: 'restored-cascade' | 'middle-bucket-defer' | 'nim-only'` derived per D-05 thresholds:
          - `rateLimitedPct < 50` → `'restored-cascade'`
          - `rateLimitedPct < 90` (i.e. 50-89.9) → `'middle-bucket-defer'`
          - else → `'nim-only'`
       f. Build snapshot object:
          ```typescript
          const snapshot = {
            timestamp: new Date().toISOString(),  // ISO-Z
            n: N,
            gapMs: GAP_MS,
            model: OPENROUTER_DEFAULT_MODEL,  // sourced from the imported production constant — guaranteed parity
            results,
            summary: { rateLimitedCount, rateLimitedPct, decision }
          };
          ```
       g. Write atomically: `mkdirSync(OUT_DIR, { recursive: true });` then `writeFileSync(TMP_PATH, JSON.stringify(snapshot, null, 2) + '\n');` then `renameSync(TMP_PATH, OUT_PATH);`.
       h. Print human-readable summary to stdout via `console.log(JSON.stringify(snapshot.summary, null, 2))` — this is the ONE allowed `console.log` and matches `scripts/eval-replay.ts` precedent (operator-facing CLI output).
       i. Print decision hint: `console.log('Decision per D-05:', snapshot.summary.decision);` + `console.log('Next plan: 30.1-' + (snapshot.summary.decision === 'restored-cascade' ? '02' : snapshot.summary.decision === 'middle-bucket-defer' ? '03' : '04') + '-PLAN.md');`.
    7. Tail: `main().catch((err) => { console.error(err); try { if (existsSync(TMP_PATH)) unlinkSync(TMP_PATH); } catch { /* swallow */ } process.exit(1); });`

    Do NOT call freeClaudeRouter.callLLM — the probe must bypass the cascade, breaker, and RollingWindow to measure raw provider behavior. Direct OpenAI client only.

  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | grep -E "(probe-openrouter|freeClaudeRouter|error)" | head -20; test $? -eq 0 || true; npx eslint scripts/probe-openrouter.ts server/lib/freeClaudeRouter.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/probe-openrouter.ts && echo OK`
    - `export const OPENROUTER_DEFAULT_MODEL` present in router: `grep -c "export const OPENROUTER_DEFAULT_MODEL" server/lib/freeClaudeRouter.ts` returns exactly `1`
    - Model value unchanged: `grep -c "OPENROUTER_DEFAULT_MODEL = 'meta-llama/llama-3.3-70b-instruct:free'" server/lib/freeClaudeRouter.ts` returns exactly `1` (the only change to this file is the `export` prefix)
    - Probe imports the model constant from the router: `grep -c "OPENROUTER_DEFAULT_MODEL" scripts/probe-openrouter.ts` returns `>= 1` (use grep -cE "import.*OPENROUTER_DEFAULT_MODEL.*freeClaudeRouter" if you need to assert the import shape specifically)
    - Probe does NOT hardcode any historical or alternate model string: `grep -c "qwen/qwen3.5" scripts/probe-openrouter.ts` returns `0` AND `grep -cE "'(meta-llama|qwen|anthropic|openai|google|mistralai)/" scripts/probe-openrouter.ts` returns `0` (no inline literal model id whatsoever; the only model reference is the imported symbol)
    - Probe bypasses RollingWindow (zero references): `grep -c "RollingWindow" scripts/probe-openrouter.ts` returns `0`
    - Atomic write present: `grep -c "renameSync" scripts/probe-openrouter.ts` returns `>= 1`
    - Sorted-by-attempt invariant present: `grep -c "sort" scripts/probe-openrouter.ts` returns `>= 1`
    - ISO-Z timestamp present: `grep -c "toISOString" scripts/probe-openrouter.ts` returns `>= 1`
    - Logger contract honored: `grep -cE "console\.(log|debug|info|warn)" scripts/probe-openrouter.ts` returns at MOST `2` (one for summary output + one for decision hint, all in main()'s final lines, NOT in the per-attempt loop). All loop-body logging must use `log.info`/`log.warn` — verify via `grep -c "log.info\|log.warn" scripts/probe-openrouter.ts` returns `>= 1`.
    - No raw response leak: `grep -cE "(JSON\.stringify\(err|err\.response|err\.body)" scripts/probe-openrouter.ts` returns `0`
    - `npx eslint scripts/probe-openrouter.ts server/lib/freeClaudeRouter.ts` exits 0
    - `npx tsc -b` does not introduce new errors attributable to scripts/probe-openrouter.ts or to the freeClaudeRouter.ts export change (compare error count before vs after)
  </acceptance_criteria>
  <done>
    Script compiles, lints clean, follows the atomic-write + sort + ISO-Z byte-stability spec, uses logger.child not console for per-attempt records, does NOT leak response bodies or headers other than retry-after, and reads its model string from the canonical `OPENROUTER_DEFAULT_MODEL` export in `server/lib/freeClaudeRouter.ts` — guaranteeing the probe measures whatever model production actually uses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire npm script + run the probe + commit</name>
  <files>
    package.json,
    .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
  </files>
  <read_first>
    - package.json — read the existing `scripts` block (lines 9-34) to confirm the `eval:replay` and `refresh:water` entries; the new `probe:openrouter` script copies the runner shape verbatim.
    - .planning/phases/30.1-.../30.1-CONTEXT.md D-03 paragraph — the runner contract is "Runs via `node --env-file=.env.local --import tsx/esm scripts/probe-openrouter.ts`." Use `--env-file-if-exists=.env --env-file-if-exists=.env.local` to match the eval:replay precedent (graceful in CI where .env.local is absent).
    - server/config.ts — confirm OPENROUTER_API_KEY is required in the Zod schema (parseEnv() will throw if missing — the probe needs the operator's real OPENROUTER_API_KEY in .env.local to run; refuse to run otherwise).
  </read_first>
  <action>
    Step 1 — Edit `package.json` `scripts` block:

    Add this line immediately after the existing `"eval:replay"` entry (line 24):

    ```json
    "probe:openrouter": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --import tsx/esm scripts/probe-openrouter.ts",
    ```

    Preserve formatting / trailing commas as Prettier would; run `npx prettier --write package.json` after the edit.

    Step 2 — Confirm .env.local has OPENROUTER_API_KEY:
    ```bash
    grep -c "^OPENROUTER_API_KEY=" .env.local
    ```
    If the count is 0, HALT and report to operator: "OPENROUTER_API_KEY missing from .env.local — probe cannot run. Operator must set this before executing Task 2." Do not fabricate a value.

    Step 3 — Run the probe:
    ```bash
    npm run probe:openrouter
    ```

    Expected wall-clock: ~5 minutes (30 calls × (gap 100ms + observed latency ~5-15s per call against OpenRouter free tier — the exact wall-clock depends on whether 429s short-circuit). Stream stdout to the terminal; the script writes the snapshot file at the end.

    Step 4 — Verify the snapshot landed and is well-formed:
    ```bash
    test -f .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
    jq '.summary' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
    jq '.results | length' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
    ```

    Step 5 — Commit. Stage exactly the three expected files + the router-export change from Task 1. Use the exact D-17 commit-1 message:
    ```bash
    git add scripts/probe-openrouter.ts package.json server/lib/freeClaudeRouter.ts .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
    git commit -m "feat(30.1): add scripts/probe-openrouter.ts + 30.1-or-pulse-snapshot.json (D-03..D-06)"
    ```

    Do NOT push yet — Wave 2 plan will push after its commit(s).

    Do NOT touch any other file. Do NOT modify server/lib/llmEventExtractor.v3.ts. The only edit to server/lib/freeClaudeRouter.ts is the single `export` prefix added in Task 1. The probe is read-only against the live OpenRouter API.

    Branch discipline: this work CAN sit on the current `feature/30-nim-throttle-characterization` branch (or wherever the operator is when planner finishes) per D-02 — scaffold files like CONTEXT.md / DISCUSSION-LOG.md / the probe snapshot are allowed on the Phase-30 branch. The Wave 2 plans handle the branch cut to `feature/30.1-cascade-fallback-fix` from main.

  </action>
  <verify>
    <automated>jq -e '.summary.rateLimitedCount' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json && jq -e '.summary.rateLimitedPct' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json && jq -e '.summary.decision' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json && jq -e '.results | length == 30' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json && jq -e '.timestamp | test("Z$")' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "probe:openrouter" package.json` returns exactly `1`
    - Snapshot file exists at `.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json`
    - `jq -e '.n == 30 and .gapMs == 100' <snapshot>` returns `true`
    - `jq -e '.results | length == 30' <snapshot>` returns `true`
    - `jq -e '.summary.decision | test("^(restored-cascade|middle-bucket-defer|nim-only)$")' <snapshot>` returns `true`
    - Snapshot model field matches the production constant: `test "$(jq -r .model .planning/phases/30.1-.../30.1-or-pulse-snapshot.json)" = "$(grep -E "^export const OPENROUTER_DEFAULT_MODEL" server/lib/freeClaudeRouter.ts | sed -E "s/.*= '([^']+)'.*/\1/")"` exits 0 (snapshot's model literal == router's exported constant value)
    - `jq -e '.results | map(.attempt) | (. == sort)' <snapshot>` returns `true` (sorted by attempt)
    - `jq -e '.timestamp | test("Z$")' <snapshot>` returns `true` (ISO-Z)
    - Git log shows new commit with subject EXACTLY `feat(30.1): add scripts/probe-openrouter.ts + 30.1-or-pulse-snapshot.json (D-03..D-06)`: `git log -1 --pretty=%s` matches.
    - Working tree clean for the four modified paths: `git status --porcelain scripts/probe-openrouter.ts package.json server/lib/freeClaudeRouter.ts .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json` returns empty
    - Commit touches exactly the four expected paths and no others: `git log -1 --name-only --pretty=format: | grep -v "^$" | sort | tr "\n" "," | sed "s/,$//"` returns exactly `.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json,package.json,scripts/probe-openrouter.ts,server/lib/freeClaudeRouter.ts` (the 4 expected paths, sorted; no other files)
  </acceptance_criteria>
  <done>
    Probe ran end-to-end, snapshot is byte-stable + well-formed, single atomic commit landed with the D-17 commit-1 message. The `summary.decision` field is the gating input for the Wave 2 checkpoint below.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Operator selects Wave 2 plan based on probe decision</name>
  <decision>Which Wave 2 plan executes next?</decision>
  <context>
    The probe ran and the snapshot's `summary.decision` field maps to exactly one of three Wave 2 plans per D-05. The operator confirms the bucket assignment before any cascade-touching work begins. This checkpoint also covers D-02 branch discipline: the operator confirms PR #20 (Phase 30) is merged and the new branch `feature/30.1-cascade-fallback-fix` has been cut from clean `main`.

    Read `jq '.summary' .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json` to see the result.

  </context>
  <options>
    <option id="restored-cascade">
      <name>Right scope — execute 30.1-02-PLAN.md (probe showed rateLimitedPct &lt; 50)</name>
      <pros>OR is viable; restoring the cascade closes the cron failure mode without ongoing cost.</pros>
      <cons>4 additional commits (code flip + test rewrite + validation cron + docs). Requires force-triggering a live cron run for validation.</cons>
    </option>
    <option id="middle-bucket-defer">
      <name>Middle bucket — execute 30.1-03-PLAN.md (probe showed 50 ≤ rateLimitedPct &lt; 90)</name>
      <pros>Docs-only; honest amendment with measured percentages + Phase 31 paid-OR deferral. No risk of cron disruption.</pros>
      <cons>Cascade stays NIM-only; the operator has to plan a Phase 31 paid-OR conversation.</cons>
    </option>
    <option id="nim-only">
      <name>Minimum scope — execute 30.1-04-PLAN.md (probe showed rateLimitedPct ≥ 90)</name>
      <pros>Docs-only; declares NIM-only honestly, OR dormant. Lowest-risk close for the phase.</pros>
      <cons>Cascade gap stays open until a follow-up phase (paid OR or NIM model switch) lands.</cons>
    </option>
  </options>
  <pre_resume_check>
    - `gh pr view 20 --json state --jq .state` should return `"MERGED"` before the operator continues to Wave 2. If not merged, halt; the operator must merge PR #20 first per D-02.
    - After PR #20 merge: `git checkout main && git pull && git checkout -b feature/30.1-cascade-fallback-fix` (per D-02). Confirm new branch with `git branch --show-current` → `feature/30.1-cascade-fallback-fix`. Cherry-pick or merge the Plan 01 probe commit onto the new branch as needed.
  </pre_resume_check>
  <resume-signal>Operator types one of: `restored-cascade`, `middle-bucket-defer`, or `nim-only` matching the snapshot's `summary.decision` field. Wave 2 then executes the corresponding plan.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                      |
| -------------------------------------- | ------------------------------------------------------------------------------------------------ |
| dev workstation → OpenRouter API       | OPENROUTER_API_KEY crosses here on every probe call                                              |
| probe script → committed snapshot JSON | Error fields could leak rate-limit envelope info or account-tier hints if not carefully filtered |

## STRIDE Threat Register

| Threat ID | Category                               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------- | -------------------------------------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------- |
| T-30.1-01 | I (Information disclosure)             | scripts/probe-openrouter.ts error catch block | mitigate    | Per-attempt `errorMessage` truncated to 200 chars; ONLY the `retry-after` header is extracted; NO `JSON.stringify(err)`, NO `err.response`, NO `err.body` ever serialized. Acceptance criterion `grep -cE "(JSON\.stringify\(err                                                                                                                                                                                                      | err\.response | err\.body)" returns 0` enforces this. |
| T-30.1-02 | I (Information disclosure)             | snapshot artifact committed to public repo    | mitigate    | OPENROUTER_API_KEY never serialized (only read from env.OPENROUTER_API_KEY at runtime, never echoed). `model` field is the public model id imported from `OPENROUTER_DEFAULT_MODEL` (currently `meta-llama/llama-3.3-70b-instruct:free`), not account-tier hinting. Reviewer of the commit can grep the file for credential patterns; the file's whole purpose is rate-limit envelope info, which is itself the desired audit signal. |
| T-30.1-03 | D (DoS — against ourselves)            | OpenRouter free-tier daily cap (200/day)      | accept      | N=30 single-event payloads consumes ~15% of the daily cap. Probe runs once per planning cycle, not in CI. If operator re-runs back-to-back, the second run will lock to high rate_limit which IS the measurement — no live-traffic disruption since the v3 extractor still has `skipOpenRouter: true` at this point (flip happens in Plan 02 only).                                                                                   |
| T-30.1-04 | T (Tampering — bad data into decision) | Manual classification of decision bucket      | mitigate    | Decision bucket is computed in the script per D-05 thresholds (deterministic), NOT typed by the operator. Operator's job at checkpoint is to confirm the script's `summary.decision` matches one of the three plans — they cannot accidentally select the wrong Wave 2 plan if they read the snapshot.                                                                                                                                |
| T-30.1-21 | T (Tampering — wrong model probed)     | scripts/probe-openrouter.ts `model` parameter | mitigate    | Probe imports `OPENROUTER_DEFAULT_MODEL` from `server/lib/freeClaudeRouter.ts` rather than hardcoding a copy. If a future phase swaps the cascade's model, the probe automatically follows on the next run — no risk of a stale literal silently invalidating the rate-limit measurement. Acceptance criteria block any inline `'<vendor>/...'` model literal in the probe script.                                                    |

## ASVS L1 Compliance Statement

- V1 (Architecture): probe is single-purpose, isolated from production code path (does not invoke freeClaudeRouter.callLLM); imports only the model constant, not the cascade machinery.
- V2 (Authentication): OPENROUTER_API_KEY loaded via parseEnv(); fail-fast on missing per CLAUDE.md "Fail-fast config".
- V7 (Errors & Logging): structured logger via `logger.child({ module: 'probeOpenRouter' })`; no console.log in the per-attempt loop; truncated error messages bound to 200 chars.
- V14 (Configuration): runs via `--env-file-if-exists` pattern matching project precedent; no new env-var requirements.
  </threat_model>

<verification>
- Phase-30.1-scoped tests untouched (this plan adds a script + an `export` keyword, not server logic).
- `npx tsc -b` succeeds with no new errors traceable to `scripts/probe-openrouter.ts` or the `export` addition in `server/lib/freeClaudeRouter.ts`.
- `npx eslint scripts/probe-openrouter.ts server/lib/freeClaudeRouter.ts` exits 0.
- `npm run format:check scripts/probe-openrouter.ts package.json server/lib/freeClaudeRouter.ts` exits 0 (or is auto-fixed via `npm run format`).
- The snapshot file `.planning/phases/30.1-.../30.1-or-pulse-snapshot.json` satisfies every JSON-schema assertion in Task 2's acceptance criteria.
- Single atomic commit on the current branch; D-17 commit-1 message verbatim.
</verification>

<success_criteria>
Plan 01 succeeds when:

- `scripts/probe-openrouter.ts` lints, typechecks, and executes against the real OpenRouter API
- The probe imports `OPENROUTER_DEFAULT_MODEL` from `server/lib/freeClaudeRouter.ts` (which gains an `export` keyword in this plan) — no hardcoded model literal in the probe
- `30.1-or-pulse-snapshot.json` lands at the documented path with all required keys (timestamp ISO-Z, n=30, gapMs=100, model = the exported constant's value, results[] sorted by attempt, summary{rateLimitedCount, rateLimitedPct, decision})
- `summary.decision` is one of `restored-cascade` / `middle-bucket-defer` / `nim-only`
- Operator confirms decision bucket at checkpoint
- D-02 branch-cut confirmed (PR #20 merged + `feature/30.1-cascade-fallback-fix` cut from main, with the Plan 01 commit on the new branch)
- Operator selects exactly one Wave 2 plan to execute
  </success_criteria>

<output>
After completion, create `.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-01-SUMMARY.md` recording:
- Probe ran on YYYY-MM-DD HH:MM Z
- Summary block: `{rateLimitedCount, rateLimitedPct, decision}`
- Snapshot's `model` field (echoed from `OPENROUTER_DEFAULT_MODEL` import)
- Wave 2 plan selected: `30.1-02-PLAN.md` / `30.1-03-PLAN.md` / `30.1-04-PLAN.md`
- Branch state: confirmed `feature/30.1-cascade-fallback-fix` cut from main at <commit sha>
</output>
</output>

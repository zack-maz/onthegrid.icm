---
phase: 35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu
plan: 03
type: execute
wave: 2
depends_on: ['35-01']
files_modified:
  - server/lib/freeClaudeRouter.ts
autonomous: true
requirements: [SIMPLIFY-05]
tags: [simplify, documentation, jsdoc, callers-block]
must_haves:
  truths:
    - "`server/lib/freeClaudeRouter.ts` has a top-of-file callers block PREPENDED above the existing vendored-from comment (per Claude's Discretion D-Discretion-4 + CONTEXT D-15)."
    - 'Callers block lists 3 live production callers with file:line refs: `llmEventExtractor.v3.ts:40`, `llmResolver.ts:15`, `llm-provider.ts:23`.'
    - 'Callers block states current cascade shape: NIM primary, OpenRouter dormant (skipOpenRouter: true), Cerebras + Groq deferred (Phase 34 close).'
    - 'Existing lines 1-16 (`Vendored from https://github.com/Alishahryar1/free-claude-code` block) are preserved below the new callers block (NOT replaced).'
    - 'Test callers section lists the 5 test files for completeness but explicitly notes they are NOT production references.'
    - 'No production code logic is changed in this plan — comment-only edit.'
    - 'Existing test suite `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 unchanged.'
  artifacts:
    - path: 'server/lib/freeClaudeRouter.ts'
      provides: 'Top-of-file callers block (~30 lines) documenting live production callers + cascade shape; existing vendored-from block preserved'
      contains: 'Free Claude Router — multi-provider cascade'
  key_links:
    - from: 'server/lib/freeClaudeRouter.ts (top-of-file)'
      to: 'server/lib/llmEventExtractor.v3.ts:40 + server/lib/llmResolver.ts:15 + server/adapters/llm-provider.ts:23'
      via: 'JSDoc callers list with file:line refs'
      pattern: 'Live production callers'
---

<objective>
Resolve SIMPLIFY-05's "is freeClaudeRouter.ts still alive?" ambiguity for future readers. Per CONTEXT.md D-15, codebase scout confirmed 3 live production callers + 5 test callers — the module is NOT orphan. KEEP the file; PREPEND a top-of-file JSDoc block listing the live callers + the active cascade shape (NIM primary, OpenRouter dormant, Cerebras + Groq deferred per Phase 34).

Purpose: Belt-and-suspenders documentation. Three independent assurances now confirm the module is load-bearing — the existing test coverage, the existing vendored-from block, and the new callers block. Future readers see the callers block at top of file and stop wondering "is this still used?"

Output: Single atomic commit (`docs(35): document freeClaudeRouter.ts callers (SIMPLIFY-05, D-15, D-16)`). Comment-only edit; no production logic touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md
@server/lib/freeClaudeRouter.ts
@CLAUDE.md

<interfaces>
<!-- Verified live callers (35-CONTEXT.md D-15 scout + 35-RESEARCH.md grep audit). -->
<!-- File:line refs are AUTHORITATIVE — executor uses them directly in the new block. -->

Live production callers (3):

- `server/lib/llmEventExtractor.v3.ts:40` — `import { callLLM, ... } from './freeClaudeRouter.js'`. Sole runtime extractor.
- `server/lib/llmResolver.ts:15` — `import { callLLM } from './freeClaudeRouter.js'`. 6-path geocode resolver; uses callLLM for the nominatim-verified-2pass reranker only.
- `server/adapters/llm-provider.ts:23` — `import { ... } from '../lib/freeClaudeRouter.js'`. Bridge wrapper; re-exports `callLLM` for legacy import paths (Phase 27.4.3 D-03 cascade replacement).

Test callers (5 — listed in block for completeness, but flagged as test-only):

- `server/__tests__/lib/freeClaudeRouter.test.ts` (canonical contract)
- `server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts`
- `server/__tests__/lib/llmEventExtractor.v3-adaptive.test.ts`
- `server/__tests__/lib/llmLineage-prefilter.test.ts`
- `server/__tests__/lib/llmResolver.test.ts`

Active cascade shape (Phase 34 close per CLAUDE.md §LLM Event Pipeline + ADR-0010 Phase 34 sub-block):

- NIM primary (qwen-235b instruct).
- OpenRouter dormant (`skipOpenRouter: true` at extractor sites per Phase 30.1).
- Cerebras + Groq deferred (Phase 34 close — see ADR-0010 Phase 34 sub-block).

Existing lines 1-16 (PRESERVE verbatim — Claude's Discretion 4 PREPEND, not REPLACE):

```typescript
/**
 * Vendored from https://github.com/Alishahryar1/free-claude-code
 * Pinned commit SHA: 40951c145ad29d6dfe450e83fd2b91fc19b9a27f
 * License: MIT (upstream LICENSE applies; see LICENSE-VENDORED.md if added)
 *
 * Phase 27.4.3 (D-01, D-02). This file ports four concepts from upstream:
 *   1. Per-provider client config (NVIDIA NIM, OpenRouter)
 *   2. Rolling-window rate limiter (40 req/min for NVIDIA NIM)
 *   3. Reactive 429 exponential backoff with jitter
 *   4. <think>-block stripper / reasoning_content parser (D-11)
 *
 * NOT ported (D-02 vendoring scope):
 *   - FastAPI / uvicorn server
 *   - Anthropic <-> OpenAI message-shape translator (we use OpenAI SDK natively)
 *   - Discord bot, Telegram bot, claude-pick CLI
 */
```

Block to PREPEND (verbatim from 35-PATTERNS.md lines 410-434):

```typescript
/**
 * Free Claude Router — multi-provider cascade for LLM-backed extraction + geocoding.
 *
 * Live production callers (verified Phase 35 / 2026-MM-DD):
 *   - server/lib/llmEventExtractor.v3.ts:40 — sole runtime extractor; calls
 *     callLLM for each event-group batch.
 *   - server/lib/llmResolver.ts:15 — 6-path geocode resolver; calls callLLM
 *     for the nominatim-verified-2pass reranker only.
 *   - server/adapters/llm-provider.ts:23 — bridge wrapper; re-exports callLLM
 *     for legacy import paths (Phase 27.4.3 D-03 cascade replacement).
 *
 * Active cascade shape (Phase 34 close): NIM primary (qwen-235b instruct);
 * OpenRouter dormant (skipOpenRouter: true at extractor sites per Phase 30.1);
 * Cerebras + Groq deferred (Phase 34 close — see ADR-0010 Phase 34 sub-block).
 *
 * Test callers (NOT live production — listed for completeness):
 *   - server/__tests__/lib/freeClaudeRouter.test.ts (canonical contract)
 *   - server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts
 *   - server/__tests__/lib/llmEventExtractor.v3-adaptive.test.ts
 *   - server/__tests__/lib/llmLineage-prefilter.test.ts
 *   - server/__tests__/lib/llmResolver.test.ts
 */
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify live callers + cascade shape (read-only)</name>
  <files>
    (read-only verification; no files modified)
  </files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-15, D-16)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (35-RESEARCH.md Sources line `freeClaudeRouter.ts` callers verified by scout)
    server/lib/freeClaudeRouter.ts (lines 1-30 — confirm existing vendored-from block at lines 1-16)
  </read_first>
  <action>
    Run two greps to confirm the 3 live callers + 5 test callers documented in <interfaces> are still accurate at this commit. CONTEXT was authored 2026-05-26; verify the callsites haven't shifted before authoring the block.

    GREP A (live callers):
    ```
    grep -rn "from .*freeClaudeRouter\|require.*freeClaudeRouter" server/lib/ server/adapters/ server/routes/ --include='*.ts' 2>/dev/null
    ```
    Expected output:
    - `server/lib/llmEventExtractor.v3.ts:40:import { callLLM, ... }` (or similar)
    - `server/lib/llmResolver.ts:15:import { callLLM }` (or similar)
    - `server/adapters/llm-provider.ts:23:import { ... }` (or similar)

    If any of the 3 live callers is missing → the module has fewer callers than documented; investigate before authoring (one of the callers may have been removed since CONTEXT was authored).
    If a NEW production caller appears → add it to the block (callers block is the source of truth; document every live caller).

    GREP B (test callers):
    ```
    grep -rn "from .*freeClaudeRouter" server/__tests__/ --include='*.ts' 2>/dev/null
    ```
    Expected output: at least 5 test files (the 5 in <interfaces>). If a 6th appears, add it to the test-callers list in the block.

    Verify cascade shape via:
    ```
    grep -n "skipOpenRouter" server/lib/llmEventExtractor.v3.ts
    ```
    Expected: at least 2 lines (per CLAUDE.md §LLM Event Pipeline: "OpenRouter dormant ... skipOpenRouter: true at extractor sites per Phase 30.1"). If absent, the cascade-shape sentence in the block needs revision.

    Record exact file:line refs from greps to use in Task 2's block authoring. No file is modified.

  </action>
  <verify>
    <automated>grep -l "from .*freeClaudeRouter" server/lib/llmEventExtractor.v3.ts server/lib/llmResolver.ts server/adapters/llm-provider.ts 2>/dev/null | wc -l | awk '$1>=3 {exit 0} {exit 1}' && grep -c "skipOpenRouter" server/lib/llmEventExtractor.v3.ts | awk '$1>=1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 3 live caller files import from `freeClaudeRouter`: `llmEventExtractor.v3.ts`, `llmResolver.ts`, `llm-provider.ts`.
    - `skipOpenRouter` appears at least once in `llmEventExtractor.v3.ts` (confirms cascade-shape sentence).
    - File:line refs recorded for use in Task 2.
  </acceptance_criteria>
  <done>
    Live caller list verified accurate (or updated with new findings); cascade shape confirmed; refs recorded for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prepend top-of-file callers block (D-15, D-16)</name>
  <files>server/lib/freeClaudeRouter.ts</files>
  <read_first>
    server/lib/freeClaudeRouter.ts (lines 1-30 — confirm existing vendored-from block at lines 1-16)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 386-434 — PREPEND-not-REPLACE pattern + verbatim block)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-15, D-16, Claude's Discretion 4)
  </read_first>
  <action>
    Use the Edit tool. PREPEND the new callers block ABOVE the existing vendored-from block (per Claude's Discretion 4 — PREPEND, not REPLACE; preserves the historical waymarker).

    Open `server/lib/freeClaudeRouter.ts`. The first 16 lines are the existing vendored-from block. The Edit operation INSERTS the new callers block at the top of the file, pushing all existing content down by ~25 lines.

    Use Edit with `old_string` = the first existing line of the file (likely `/**` from the vendored-from block) and `new_string` = the new callers block + a blank line + the existing first line. This is the safest INSERT-at-top pattern.

    NEW BLOCK CONTENT (substitute today's UTC date for the {YYYY-MM-DD} placeholder; substitute Task 1's verified file:line refs if they differ from <interfaces>):

    ```typescript
    /**
     * Free Claude Router — multi-provider cascade for LLM-backed extraction + geocoding.
     *
     * Live production callers (verified Phase 35 / {YYYY-MM-DD}):
     *   - server/lib/llmEventExtractor.v3.ts:40 — sole runtime extractor; calls
     *     callLLM for each event-group batch.
     *   - server/lib/llmResolver.ts:15 — 6-path geocode resolver; calls callLLM
     *     for the nominatim-verified-2pass reranker only.
     *   - server/adapters/llm-provider.ts:23 — bridge wrapper; re-exports callLLM
     *     for legacy import paths (Phase 27.4.3 D-03 cascade replacement).
     *
     * Active cascade shape (Phase 34 close): NIM primary (qwen-235b instruct);
     * OpenRouter dormant (skipOpenRouter: true at extractor sites per Phase 30.1);
     * Cerebras + Groq deferred (Phase 34 close — see ADR-0010 Phase 34 sub-block).
     *
     * Test callers (NOT live production — listed for completeness):
     *   - server/__tests__/lib/freeClaudeRouter.test.ts (canonical contract)
     *   - server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts
     *   - server/__tests__/lib/llmEventExtractor.v3-adaptive.test.ts
     *   - server/__tests__/lib/llmLineage-prefilter.test.ts
     *   - server/__tests__/lib/llmResolver.test.ts
     */
    ```

    Followed by a blank line, then the existing vendored-from block (which begins `/**\n * Vendored from https://github.com/Alishahryar1/free-claude-code`).

    NO LOGIC CHANGES. The block is pure JSDoc comment; no `export`/`import`/`function`/`const` lines are touched.

    POST-EDIT VERIFICATION:
    - `head -30 server/lib/freeClaudeRouter.ts` should show the new callers block first, then the existing vendored-from block second.
    - `npx tsc --noEmit` exits 0 (block is JSDoc — no type impact, but worth confirming the file still parses).
    - `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 (no behavior change).
    - `grep -q "Free Claude Router — multi-provider cascade" server/lib/freeClaudeRouter.ts` returns 0.
    - `grep -q "Vendored from https://github.com/Alishahryar1/free-claude-code" server/lib/freeClaudeRouter.ts` still returns 0 (existing block preserved).

    COMMIT (single atomic per D-15 / D-16 / D-26):
    ```
    docs(35): document freeClaudeRouter.ts callers (SIMPLIFY-05, D-15, D-16)

    Belt-and-suspenders documentation: 3 live production callers verified by
    grep, listed at top of file with file:line refs. Cascade shape (Phase 34
    close — NIM primary, OpenRouter dormant, Cerebras + Groq deferred)
    documented inline. Existing vendored-from block preserved below as historical
    waymarker (Claude's Discretion 4 — PREPEND not REPLACE).

    Touched: server/lib/freeClaudeRouter.ts (comment-only; no logic change).
    ```

  </action>
  <verify>
    <automated>grep -q "Free Claude Router — multi-provider cascade" server/lib/freeClaudeRouter.ts && grep -q "Vendored from https://github.com/Alishahryar1/free-claude-code" server/lib/freeClaudeRouter.ts && grep -q "llmEventExtractor.v3.ts:40" server/lib/freeClaudeRouter.ts && grep -q "Phase 34 close" server/lib/freeClaudeRouter.ts && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Free Claude Router — multi-provider cascade for LLM-backed" server/lib/freeClaudeRouter.ts` exits 0.
    - `grep -q "Vendored from https://github.com/Alishahryar1/free-claude-code" server/lib/freeClaudeRouter.ts` exits 0 (existing block preserved).
    - The line containing "Free Claude Router — multi-provider cascade" appears BEFORE the line containing "Vendored from" in the file (`awk` line-number check).
    - File contains `llmEventExtractor.v3.ts:40`, `llmResolver.ts:15`, `llm-provider.ts:23`.
    - File contains `Phase 34 close`, `qwen-235b instruct`, `skipOpenRouter`.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0.
    - `git log -1 --format=%s` matches `docs(35):` with `SIMPLIFY-05` or `D-15` in body.
  </acceptance_criteria>
  <done>
    `server/lib/freeClaudeRouter.ts` top-of-file contains the new callers block followed by the existing vendored-from block. Future readers can determine the module is alive in <30 seconds without grepping.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                    |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| JSDoc comment → API security surface           | NEW block contains zero secret material (no API keys, Bearer tokens, NIM/OpenRouter credentials) — verified during composition |
| Live callers documentation → out-of-date drift | Mitigated by Task 1 grep verification before authoring + commit body cites verification date                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                    | Disposition         | Mitigation Plan                                                                                                                                                                                            |
| --------- | ---------------------- | ------------------------------------------------------------ | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-08   | Information Disclosure | New JSDoc block leaks secrets / credentials                  | mitigate            | Block contents are 100% file:line refs + cascade-state prose. No env-var names, no Bearer tokens, no API keys. Verified verbatim from 35-PATTERNS.md template (which itself was reviewed during research). |
| T-35-09   | Tampering              | Callers block goes stale as future phases add/remove callers | mitigate (deferred) | No mechanical drift gate for this block (out of scope; future phase could add). Mitigation: block content cites verification date so a future reader knows the staleness window.                           |
| T-35-SC   | Tampering              | No npm/pip/cargo installs                                    | n/a                 | Pure JSDoc edit; package legitimacy gate not applicable.                                                                                                                                                   |

</threat_model>

<verification>
Comment-only edit; logic untouched. `npx tsc --noEmit` exits 0; `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0; full suite `npx vitest run` exits 0.
</verification>

<success_criteria>

1. New callers block prepended above existing vendored-from block in `server/lib/freeClaudeRouter.ts`.
2. Block lists 3 live production callers with file:line refs.
3. Block states cascade shape (Phase 34 close).
4. Block lists 5 test callers as not-production.
5. Existing vendored-from block preserved verbatim.
6. Single atomic commit lands.
7. `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0.
8. `npx tsc --noEmit` exits 0.
9. Covers SIMPLIFY-05.
   </success_criteria>

<output>
Create `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-03-SUMMARY.md` when done. Include:
- Live caller count verified (3 expected; note any drift from CONTEXT).
- Test caller count verified (5 expected).
- Cascade-shape verification result.
- Single atomic commit hash + message.
- Bytes added to file (new block) ≈ measure delta.
</output>

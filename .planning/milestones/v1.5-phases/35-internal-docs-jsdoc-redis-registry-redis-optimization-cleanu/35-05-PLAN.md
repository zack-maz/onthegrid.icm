---
phase: 35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu
plan: 05
type: execute
wave: 3
depends_on: ['35-01']
files_modified:
  - .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md
autonomous: false
requirements: [REDIS-OPT-03]
tags: [redis, ttl, observability, right-sizing]
must_haves:
  truths:
    - 'Each Redis key from the `docs/architecture/redis-keys.md` inventory has been reviewed against its producer cadence + freshness requirement (D-17).'
    - 'The `replay-history` ambiguity (35-RESEARCH.md Open Question 1 / 35-CONTEXT.md D-18) is resolved by operator clarification at task start.'
    - 'TTL right-sizing review produces an artifact `35-05-TTL-REVIEW.md` listing each key + current TTL + finding (`right-sized` | `under-sized` | `over-sized` | `unbounded`) + recommendation (`leave` | `reduce to N` | `increase to N` | `apply cap`).'
    - "If NO TTL changes are proposed, the artifact records that finding as the load-bearing outcome — same precedent as Phase 31 closing early with 'no incidents observed' being itself the deliverable (D-17)."
    - 'If TTL changes ARE proposed, each lands as its own atomic commit `feat(35): right-size TTL on {key} from Xs to Ys (REDIS-OPT-03, D-17.{N})` with regression test for the affected cache module.'
    - 'Replay-history cap (D-18): IF a separate replay-history key is identified, cap applied at 500 entries / 7d (default per D-18) or operator-justified deviation.'
    - 'Drift gate from plan 35-01 still exits 0 after any TTL code changes (key references unchanged; only TTL values shift).'
  artifacts:
    - path: '.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md'
      provides: 'Per-key TTL review with finding + recommendation table'
      contains: 'TTL Review'
  key_links:
    - from: '.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md'
      to: 'docs/architecture/redis-keys.md TTL column'
      via: 'per-key cross-reference; review reads inventory + audits each TTL'
      pattern: 'TTL Review'
---

<objective>
Right-size TTLs against actual producer cadence + freshness requirements. Per CONTEXT.md D-17: this is READ-ONLY-AT-DEFAULT. Most TTLs in the v1.5 codebase have been tuned over Phases 27-34 and are already correct; the value of this plan is verifying that explicitly and producing an auditable artifact. If a clear mismatch is found, a targeted TTL change ships as its own atomic commit. If no mismatches are found, "no changes proposed" IS the load-bearing outcome (same precedent as Phase 31 closing early with "no incidents observed").

Purpose: Close REDIS-OPT-03. Resolve the D-18 ambiguity (35-RESEARCH.md Open Question 1 — what does "replay-history not yet capped" mean?). Either apply a cap to a previously-uncapped observability key OR record the audit finding that the existing per-day quota counter + audit-log cap collectively satisfy the requirement.

Output: Always 1 artifact (`35-05-TTL-REVIEW.md`). Zero, one, or many commits depending on whether TTL changes are proposed. Outcome documented in plan 35-06's SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-01-PLAN.md
@docs/architecture/redis-keys.md
@CLAUDE.md

<interfaces>
<!-- Authoritative TTL inventory from 35-RESEARCH.md §Grep Audit + post-plan-35-01 docs/architecture/redis-keys.md -->

Per-key TTL inventory (post-plan-35-01, post-plan-35-02 state):

| Key family | Key                                                      | Current TTL                                    | Producer cadence                                       | Freshness requirement                                                                                                            |
| ---------- | -------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| events     | `events:gdelt`                                           | 15-min logical / 2.5h hard                     | GDELT polling (15-min interval)                        | Match producer; CLAUDE.md §LLM Event Pipeline.                                                                                   |
| events     | `events:backfill-ts`                                     | 1h                                             | One-time backfill sentinel                             | Long enough to prevent stampede.                                                                                                 |
| events     | `events:llm:v3`                                          | 9000s (≈ 2.5h hard) `LLM_REDIS_TTL_SEC`        | Daily 04:00 UTC cron                                   | Cron runs once / day; 26h gives headroom for 1 missed tick. Verify 2.5h hard vs 26h logical alignment in operator clarification. |
| events     | `events:llm:v3:partial`                                  | n/a (retired plan 35-02)                       | n/a                                                    | n/a                                                                                                                              |
| events     | `events:llm:v3:lineage:{eventId}`                        | 7d `LINEAGE_TTL_SEC`                           | Per-event write during cron                            | 7d aligns with retrospective drill-down window.                                                                                  |
| events     | `events:llm:v3:lineage-keys`                             | 7d; capped 500 entries                         | Per-cron ZADD                                          | Capped + TTL = sane; review cap size against actual cardinality.                                                                 |
| events     | `events:llm:v3:group-lineage:{hash}`                     | 7d                                             | Reader-only currently                                  | Pre-filter cache reuse window.                                                                                                   |
| events     | `events:llm-summary:v3`                                  | 24h `LLM_SUMMARY_TTL_SEC`                      | Daily cron                                             | Match cron cadence.                                                                                                              |
| events     | `events:llm-dlq`                                         | 7d; capped 200                                 | Per-failure SADD                                       | Already capped; review.                                                                                                          |
| events     | `events:llm-process-ts`                                  | n/a (cooldown sentinel)                        | Cron start                                             | OK.                                                                                                                              |
| events     | `events:llm-eval-baseline:v3`                            | 90d                                            | Daily eval run                                         | Long retention for trend lines.                                                                                                  |
| events     | `events:llm-eval-adversarial:v3`                         | 90d                                            | Daily eval run                                         | Same as baseline.                                                                                                                |
| events     | `events:llm-pipeline-audit`                              | 90d; capped 200                                | Per-operator-flip LPUSH (no new writers post-Phase-29) | Already capped + TTL; review cap size.                                                                                           |
| events     | `events:url-liveness:{eventId}`                          | Tiered: live 7d, terminal-dead 24h, unknown 1h | Per-probe cron                                         | Tiered TTL is the load-bearing innovation; verify still right-sized.                                                             |
| events     | `events:url-liveness-count`                              | None (persistent sidecar)                      | INCR/DECR on probe + prune                             | Persistent OK.                                                                                                                   |
| flights    | `flights:opensky`, `flights:adsblol`                     | short (per `FLIGHTS_REDIS_TTL_SEC`)            | 5-30s polling                                          | Match producer.                                                                                                                  |
| ships      | `ships:ais`                                              | 10-min stale                                   | AISStream on-demand                                    | Match producer.                                                                                                                  |
| sites      | `sites:v3`                                               | 24h                                            | Daily Overpass warm cron                               | Match producer cadence.                                                                                                          |
| water      | `water:facilities:v3`                                    | 24h                                            | Daily Overpass warm cron                               | Match producer cadence.                                                                                                          |
| water      | `water:precip`                                           | 6h                                             | Open-Meteo polling                                     | Match producer (6h interval).                                                                                                    |
| news       | `news:feed`, `news:gdelt`                                | 15-min                                         | GDELT-DOC polling                                      | Match producer.                                                                                                                  |
| markets    | `markets:yahoo:{range}`                                  | 60s                                            | Yahoo Finance polling                                  | Match producer.                                                                                                                  |
| geocode    | `geocode:{lat},{lon}`                                    | 30d logical / 90d hard                         | Per-request demand                                     | Long retention reasonable for static lookups.                                                                                    |
| geocode    | `geocode:fwd:constrained:v2:{hash}`                      | 30d hard                                       | Per-request demand                                     | Same.                                                                                                                            |
| llm        | `llm:tokens:{provider}:YYYY-MM-DD`                       | 48h                                            | Daily INCR                                             | UTC-day-rollover + 24h grace.                                                                                                    |
| llm        | `llm:lastProgress`                                       | 7d `LLM_LASTPROGRESS_TTL_SEC`                  | Per-cron-tick write-through                            | Cold-start protection window.                                                                                                    |
| cron       | `cron:lastTick:health`, `:warm`, `:refresh-events`       | 7d `CRON_LASTTICK_TTL_SEC`                     | Per-cron-tick                                          | 7d = "tick missed for a week → all hands".                                                                                       |
| operator   | `operator:audit-log`                                     | 30d; capped 500                                | Per-operator-action SADD                               | Already capped + TTL.                                                                                                            |
| operator   | `operator:replay-quota:{bearerFingerprint}:{YYYY-MM-DD}` | 48h                                            | Per-replay INCR                                        | UTC-day-rollover + 24h grace.                                                                                                    |
| operator   | `operator:prune-quota:{bearerFingerprint}:{YYYY-MM-DD}`  | 48h                                            | Per-prune INCR                                         | Same as replay.                                                                                                                  |
| audit      | `audit:connectivity:last-result`                         | 7d                                             | Manual workflow_dispatch                               | 7d ≈ next audit-cycle window.                                                                                                    |

OPEN QUESTION 1 (35-RESEARCH.md lines 562-565): What does D-18 mean by "replay-history not yet capped"?

- Hypothesis A: D-18's "replay history" refers to `operator:audit-log` (which IS already capped 500/30d). If so, D-18 is satisfied — no change.
- Hypothesis B: There's an undocumented replay-history key that grep missed. If so, find it and apply a cap.
- Recommendation per 35-RESEARCH.md: ASK OPERATOR at task start. If A, record finding + close D-18 as "satisfied by existing cap." If B, find the key and apply cap.

D-17 CHANGE COMMIT FORMAT (per CONTEXT D-17):

```
feat(35): right-size TTL on {key} from Xs to Ys (REDIS-OPT-03, D-17.{N})

Rationale: {why current TTL is wrong}.
New value: {Ys} = {expression like 24*3600 or producer cadence × 2}.
Regression test: {test file that pins the new TTL or behavior}.
```

D-18 CAP COMMIT FORMAT (per CONTEXT D-18):

```
feat(35): apply cap to {key} (REDIS-OPT-03, D-18)

Cap: 500 entries / 7d TTL (mirrors `operator:audit-log` precedent per D-18).
Writer: {file:line} updated to {ZADD then ZREMRANGEBYRANK | LPUSH then LTRIM}.
Regression test: {test file pinning the cap behavior}.
```

</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator clarification on D-18 replay-history ambiguity</name>
  <files>(no files modified — operator decision checkpoint)</files>
  <action>Pause and present the decision below to the operator. Capture the response in plan SUMMARY.md and proceed to Task 2 with the chosen option.</action>
  <verify>Operator response captured (option-a / option-b / option-c with rationale).</verify>
  <done>D-18 ambiguity resolved; Task 2 has explicit input on how to handle the replay-history key.</done>
  <decision>Resolve 35-RESEARCH.md Open Question 1: What does D-18 "replay history not yet capped" actually refer to?</decision>
  <context>
    Per 35-RESEARCH.md Assumption A5 + Open Question 1:
    - D-18 wording: "DLQ at 200 entries / 7d, audit log at 500 / 30d already; replay history not yet capped"
    - Code grep finds NO separate "replay-history" key. Only `operator:replay-quota:*` (per-day INCR counter, already TTL'd 48h) and `operator:audit-log` (already capped 500/30d).
    - Two hypotheses:
      - **Option A:** D-18's "replay history" = `operator:audit-log` (the audit log is the de-facto replay-history because operator replay actions get written there). Already capped 500/30d. D-18 is satisfied — no change needed.
      - **Option B:** There's an undocumented replay-history key that grep missed. Operator surfaces the key name; planner finds writers/readers, applies cap.
      - **Option C:** D-18 wording was loose; operator intent was different from both A and B (e.g. capping `operator:replay-quota` lifespan further, or adding a new key).
  </context>
  <options>
    <option id="option-a">
      <name>Audit log is the replay history; cap already in place</name>
      <pros>Zero code changes. D-18 closes as "satisfied by existing 500-entry SADD cap on operator:audit-log."</pros>
      <cons>None — this is the most likely correct reading.</cons>
    </option>
    <option id="option-b">
      <name>Undocumented replay-history key exists; needs cap</name>
      <pros>Identifies a missing observability key; brings registry to full coverage.</pros>
      <cons>Requires operator to specify the key name + cap parameters; ships a `feat(35):` commit.</cons>
    </option>
    <option id="option-c">
      <name>Loose wording; alternate intent</name>
      <pros>Surfaces the actual operator concern (may reveal a different right-sizing opportunity).</pros>
      <cons>Requires operator to articulate alternate intent.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c. If b or c, describe the key/intent.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Per-key TTL review + author 35-05-TTL-REVIEW.md (D-17)</name>
  <files>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md
  </files>
  <read_first>
    docs/architecture/redis-keys.md (TTL column — primary input)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-17, D-18)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (lines 598-705 §Grep Audit — TTL refs verified by grep)
    CLAUDE.md (§Serverless Cache — operator-readable TTL reference)
  </read_first>
  <action>
    Use the Write tool to create `35-05-TTL-REVIEW.md`. Plain markdown with one table per prefix family. For EACH key in `docs/architecture/redis-keys.md`, populate a row with these columns:

    | Key | Current TTL | Producer cadence | Freshness need | Finding | Recommendation |

    Finding enum: `right-sized` | `under-sized` | `over-sized` | `unbounded` | `correct-but-non-obvious`
    Recommendation enum: `leave` | `reduce to N (with rationale)` | `increase to N (with rationale)` | `apply cap (with rationale)` | `document in CLAUDE.md why it's correct`

    SOURCE THE TTL VALUES from the inventory above (in <interfaces> block) — verified against `35-RESEARCH.md §Grep Audit`.

    APPLY THE READ-ONLY DEFAULT: per CONTEXT.md D-17, propose changes ONLY on clear mismatch. Default verdict for every key = `right-sized` + `leave`. Override ONLY when:
    - Producer cadence is HOURS but TTL is DAYS → maybe over-sized (review).
    - Producer cadence is MINUTES but TTL is SECONDS → maybe under-sized (review).
    - Observability key has UNBOUNDED size (no LTRIM / ZREMRANGEBYRANK / SADD-cap) → propose cap.

    EXPECTED OUTCOME (most likely per 35-RESEARCH.md): Nearly all keys come back `right-sized` + `leave`. The 1-2 exceptions (if any) feed into Task 3 (commits).

    INCLUDE the D-18 disposition from Task 1's decision:
    - If Task 1 = option-a: Add a §"D-18 Replay History Disposition" section that records the finding "operator:audit-log serves as the de-facto replay history per operator clarification; 500-entry SADD cap + 30d TTL already in place; D-18 closes as `satisfied-existing-cap`. No new commit ships."
    - If Task 1 = option-b: Add the new key to the table + recommendation `apply cap`. Task 3 ships the commit.
    - If Task 1 = option-c: Capture the alternate finding; Task 3 ships if a code change is needed.

    FOOTER section:
    ```
    ## Summary

    - Keys reviewed: N
    - Right-sized + leave: N
    - Proposed TTL changes: M (each lands as its own commit per D-17.{i})
    - Caps applied: M (each lands as its own commit per D-18)
    - D-18 replay-history disposition: {satisfied-existing-cap | applied-new-cap | alternate}

    ## Provenance

    - Authored: {YYYY-MM-DD} (plan 35-05 task 2) per REDIS-OPT-03 / D-17.
    - Inputs: docs/architecture/redis-keys.md (plan 35-01); 35-RESEARCH.md §Grep Audit; CONTEXT.md D-17 + D-18; Task 1 operator clarification.
    ```

    NO CODE CHANGES in this task. Pure review artifact.

    COMMIT:
    ```
    docs(35): TTL review per REDIS-OPT-03 (D-17, D-18) — {N} keys reviewed, {M} change(s) proposed

    Author: planner per CONTEXT.md D-17 read-only-at-default discipline.
    Outcome: {M=0 "no TTL changes proposed; all keys right-sized" | "see 35-05-TTL-REVIEW.md per-key findings"}.
    D-18 disposition: {satisfied-existing-cap | applied-new-cap-task3 | alternate}.
    ```

  </action>
  <verify>
    <automated>test -f .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md && grep -q "## events:" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md && grep -qE "right-sized|under-sized|over-sized|unbounded|correct-but-non-obvious" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md && grep -q "D-18 Replay History Disposition\|D-18" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - File `35-05-TTL-REVIEW.md` exists.
    - File contains a table per prefix family (events, flights, ships, sites, water, news, markets, geocode, llm, cron, operator, audit).
    - Every key from `docs/architecture/redis-keys.md` (post plan 35-02 retirement) has a row.
    - File contains a §"Summary" footer with counts.
    - File contains a §"D-18 Replay History Disposition" section reflecting Task 1's decision.
    - `git log -1 --format=%s` contains `docs(35):` + `D-17` + `D-18`.
  </acceptance_criteria>
  <done>
    TTL review artifact exists. Task 3 either ships ≥1 commit (if changes proposed) OR skips entirely (if all `leave`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply proposed TTL changes (CONDITIONAL — skipped if Task 2 finds zero changes)</name>
  <files>
    (varies — depends on Task 2 findings; e.g. `server/lib/llmDLQ.ts`, `server/lib/llmLineage.ts`, etc.)
  </files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md (Task 2 output — the audit findings)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-17 commit format; D-26 atomic-per-decision)
  </read_first>
  <action>
    READ `35-05-TTL-REVIEW.md` from Task 2.

    IF Summary section reads `Proposed TTL changes: 0 + Caps applied: 0`:
    - SKIP this task. Plan 35-05 closes with Task 2 as the single deliverable. The artifact + the "no changes proposed" outcome IS the load-bearing finding per CONTEXT.md D-17.
    - Record skip in plan 35-05 SUMMARY.md.

    IF Summary section reads `Proposed TTL changes: M ≥ 1`:
    - For EACH proposed change, ship a separate atomic commit per D-17 / D-26.
    - Format (per <interfaces>): `feat(35): right-size TTL on {key} from Xs to Ys (REDIS-OPT-03, D-17.{i})`.
    - Each commit MUST include a regression test that pins the new TTL. Use the existing test file for the affected cache module (e.g. `server/__tests__/lib/llmDLQ.test.ts` for DLQ; add an assertion that the TTL constant has the new value).
    - Run `npx vitest run` after each commit to confirm green.

    IF D-18 disposition = `applied-new-cap-task3`:
    - Ship a separate atomic commit `feat(35): apply cap to {key} (REDIS-OPT-03, D-18)`.
    - Include test that pins the cap behavior.

    POST-COMMITS:
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` full suite exits 0.
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0 (drift gate from plan 35-01 still green — TTL changes don't affect key references).

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npx vitest run 2>&1 | tail -5 && npx vitest run src/__tests__/lib/redis-registry.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - IF Task 2 summary = 0 proposed changes: this task SKIPPED, recorded in plan SUMMARY.md.
    - IF Task 2 summary = M ≥ 1: M commits land, one per proposed change, each citing D-17.{i}; tests added; full suite green; drift gate green.
    - IF D-18 disposition = applied-new-cap-task3: additional commit cites D-18; test added.
    - `npx tsc --noEmit` exits 0 after all commits.
    - `npx vitest run` full suite green after all commits.
  </acceptance_criteria>
  <done>
    Either zero commits (no TTL changes proposed) or M ≥ 1 atomic commits (right-sizing applied). Plan 35-05 closes with the artifact + 0..M commits.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                    |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| TTL constants → cache lifetime contract  | Lowering TTL risks cache misses on intermediate reads; mitigated by per-change regression test (D-17 + commit format)          |
| Cap on observability key → eviction risk | Setting cap too low evicts useful drill-down data; mitigated by D-18 default (500 entries / 7d) mirroring proven audit-log cap |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                         | Disposition | Mitigation Plan                                                                                  |
| --------- | ----------------- | ----------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-35-12   | Denial of Service | TTL reduced too aggressively → cache stampede on producer cadence | mitigate    | D-17 default = `leave`; changes only on clear mismatch; per-change regression test pins new TTL. |
| T-35-13   | Repudiation       | Future operator wonders why TTL was changed                       | mitigate    | Each commit's body cites D-17.{i} + rationale; `35-05-TTL-REVIEW.md` is the auditable artifact.  |
| T-35-SC   | Tampering         | No npm/pip/cargo installs                                         | n/a         | Pure TTL constant edit; package legitimacy gate not applicable.                                  |

</threat_model>

<verification>
After Task 2: `35-05-TTL-REVIEW.md` exists with per-key findings; 1 docs commit lands.
After Task 3: 0..M code commits land; `npx tsc --noEmit` exits 0; `npx vitest run` full suite exits 0; drift gate green.
</verification>

<success_criteria>

1. Task 1 operator clarification resolves D-18 ambiguity.
2. `35-05-TTL-REVIEW.md` exists, covering all keys from `docs/architecture/redis-keys.md`.
3. Each proposed TTL change ships as its own atomic commit with regression test (or zero commits if no changes proposed — the "no incidents observed" outcome).
4. D-18 disposition recorded (satisfied-existing-cap OR applied-new-cap OR alternate).
5. Drift gate still passes.
6. Covers REDIS-OPT-03.
   </success_criteria>

<output>
Create `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-SUMMARY.md` when done. Include:
- Operator decision on D-18 (Task 1 option chosen).
- Summary line from 35-05-TTL-REVIEW.md (keys reviewed, changes proposed).
- Count of code commits (0 if no changes; M if M changes proposed).
- Each commit hash + short message.
- Final verification snapshot (tsc + vitest + drift gate).
</output>

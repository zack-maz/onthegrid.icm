---
phase: 34-llm-router-fallback-re-integration-cerebras-groq-per-provide
plan: 04
type: execute
wave: 4
depends_on: ['34-03']
files_modified:
  - server/lib/llmDLQ.ts
  - server/lib/llmEventExtractor.v3.ts
  - server/routes/refresh-events-cron.ts
  - server/lib/llmExtractionPipeline.ts
  - server/__tests__/lib/llmDLQ.test.ts
  - .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json
autonomous: false
requirements: [LLM-RELI-11]
tags: [llm, dlq, validation, cron, cascade_exhausted, skipPrimary]

must_haves:
  truths:
    - "`DLQEntry.reason` union accepts `'v3:cascade_exhausted'`."
    - "When `callLLM` returns null content AND `didTimeout === false`, the extractor enqueues one DLQ entry per batch context with `reason: 'v3:cascade_exhausted'` and a `lastError` concatenating the per-provider decision history (capped 500 chars)."
    - '`didTimeout` path (existing v3:timeout_watchdog) is NOT double-enqueued — the new branch only fires when the cascade exhausted WITHOUT a watchdog kill.'
    - 'A temporary `?skipPrimary=nvidia_nim` query param is honored by `/api/cron/refresh-events` ONLY in non-prod (Bearer-gated + env-gated).'
    - 'The skipPrimary flag threads through `runRefreshExtraction` → `freeClaudeCallLLM` opts so the cascade actually skips NIM during validation.'
    - 'Validation cron run against a Vercel Preview or local-dev environment produces a snapshot where `callHistory` contains at least one `cerebras` or `groq` provider row (whichever passed Plan 34-01) — SC-3 evidence.'
    - 'Pitfall 1 cache bridge regression test passes — `/api/events` still serves raw GDELT when `events:llm:v3` is empty (SC-6 evidence).'
    - "The skipPrimary flag SHIPS in this plan and is REVERTED in Plan 34-05 (it's a witness, not a fixture, per CONTEXT D-22)."
  artifacts:
    - path: 'server/lib/llmDLQ.ts'
      provides: "`DLQEntry.reason` union extended with `'v3:cascade_exhausted'`."
      contains: 'v3:cascade_exhausted'
    - path: 'server/lib/llmEventExtractor.v3.ts'
      provides: 'DLQ enqueue wrapper at line 731 area for cascade-exhausted (`!didTimeout` branch).'
      contains: 'v3:cascade_exhausted'
    - path: 'server/routes/refresh-events-cron.ts'
      provides: 'Temporary `?skipPrimary` query param handler (non-prod + Bearer-gated).'
      contains: 'skipPrimary'
    - path: 'server/lib/llmExtractionPipeline.ts'
      provides: '`runRefreshExtraction` options type extended with `skipPrimary` and threaded through to `freeClaudeCallLLM`.'
      contains: 'skipPrimary'
    - path: 'server/__tests__/lib/llmDLQ.test.ts'
      provides: 'DLQ union test for cascade_exhausted round-trip.'
      contains: 'v3:cascade_exhausted'
    - path: '.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json'
      provides: 'Validation cron snapshot (captured via `npm run watch:snapshot -- --http`).'
      contains: 'callHistory'
  key_links:
    - from: 'server/lib/llmEventExtractor.v3.ts (line 731 area)'
      to: 'server/lib/llmDLQ.ts:enqueueDLQ'
      via: 'DLQ write on cascade_exhausted; gated by `!didTimeout`'
      pattern: 'v3:cascade_exhausted'
    - from: 'server/routes/refresh-events-cron.ts'
      to: 'server/lib/llmExtractionPipeline.ts:runRefreshExtraction'
      via: 'thread `skipPrimary` through options'
      pattern: 'skipPrimary'
    - from: 'validation cron snapshot'
      to: 'events:llm-summary:v3.callHistory'
      via: '`npm run watch:snapshot -- --http`'
      pattern: 'callHistory'
---

<objective>
Add `'v3:cascade_exhausted'` to the DLQ reason union, wrap the v3 extractor's null-content branch with a DLQ enqueue (gated by `!didTimeout`), thread a temporary `?skipPrimary=nvidia_nim` validation flag through the cron handler / pipeline / router, then force-run the cron against a non-prod environment to capture evidence that the restored cascade actually fires.

Purpose: Distinguishes "all cascade providers failed for this batch" from "single-provider watchdog timeout" in DLQ telemetry (CONTEXT D-13). The skipPrimary flag is the witness that proves the cascade widens under NIM failure (CONTEXT D-22 — temporary, reverted in Plan 34-05).
Output: DLQ union extension; extractor wrapper; skipPrimary flag; validation snapshot artifact; DLQ + Pitfall 1 tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-RESEARCH.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-03-SUMMARY.md
@server/lib/llmDLQ.ts
@server/lib/llmEventExtractor.v3.ts
@server/lib/llmExtractionPipeline.ts
@server/routes/refresh-events-cron.ts
@server/__tests__/lib/llmDLQ.test.ts
@server/__tests__/routes/events.pitfall1.test.ts

<interfaces>
From server/lib/llmDLQ.ts:22-40 (CURRENT):
- `DLQEntry.reason` union at line 27-37 enumerates existing reasons (v1/v2 paths + `v3:max_tokens_truncation` etc.).
- D-13 ADDS `| 'v3:cascade_exhausted'` to the union.
- `enqueueDLQ` (line 53) is the writer; SADD bounded 200 cap; 7d TTL; `lastError` capped 500 chars (existing — see line 54 `capped` shape).

From server/lib/llmEventExtractor.v3.ts:731 (cascade-exhausted wrapper site per RESEARCH §2.2):

- Lines 731-762 currently:
  - Line 731: `if (content === null)` branch start.
  - Lines 739-757: `didTimeout` adaptive-split-and-retry path (existing — the watchdog wrapper at `withBatchWatchdog` writes `v3:timeout_watchdog` DLQ).
  - Line 759: `log.warn({ batchIndex }, 'v3 batch yielded no content (null or watchdog timeout)');` — fall-through; this is where cascade_exhausted enqueue lands.
- The `decisions` variable (the `routing` return from `freeClaudeCallLLM` — verify by reading lines 690-730 for the call shape) carries per-provider attempt history; concatenated into `lastError`.

From server/routes/refresh-events-cron.ts:45-60 (existing Bearer-gate + force-trigger):

- Lines 45-58: Bearer gate via `timingSafeEqual` against `env.CRON_SECRET`.
- Line 60 area: `const forceCooldown = req.query.force === 'true';`.
- D-22 INSERTION: right after the `forceCooldown` line, add a `?skipPrimary=nvidia_nim` reader gated by `env.NODE_ENV !== 'production'` (per RESEARCH §2.5 — production REFUSES even with valid Bearer).

From server/lib/llmExtractionPipeline.ts:

- `runRefreshExtraction(options)` — existing options shape (likely `{ triggeredBy, forceCooldown, ... }`).
- D-22: extend options with `skipPrimary?: 'nvidia_nim' | null` and thread through to `freeClaudeCallLLM` via the existing `opts` argument (extends to `{ batchSize?, modelOverride?, skipOpenRouter?, skipPrimary? }`).

From server/lib/freeClaudeRouter.ts (post-Plan-34-02 state):

- `allProviders[]` at lines 349-360 (with restored Cerebras/Groq entries from Plan 34-02).
- `callLLM` accepts `opts.skipPrimary` and filters `allProviders` accordingly (similar to existing `skipOpenRouter: true` filter at lines 362-363).
- This plan ADDS a small filter in `callLLM` to also exclude `'nvidia_nim'` when `opts.skipPrimary === 'nvidia_nim'`.

From server/**tests**/routes/events.pitfall1.test.ts:

- Existing test asserting `/api/events` returns raw GDELT when `events:llm:v3` is empty. SC-6 invariant.

From CLAUDE.md "Cron-only writer (anti-pattern #17)":

- `/api/events` is cache-only; `/api/cron/refresh-events` is the sole writer for `events:llm:v3`. This plan adds the validation flag to the cron endpoint, NOT `/api/events`.
  </interfaces>

<validation_constraints>

- **Non-prod only** for skipPrimary flag (CONTEXT D-22 + RESEARCH §2.5). Production rejects even with valid Bearer. Implementation: `env.NODE_ENV !== 'production'` OR `process.env.VERCEL_ENV !== 'production'` (Vercel injects `VERCEL_ENV=production`).
- **Witness, not fixture** — skipPrimary code SHIPS in Plan 34-04 and is REVERTED in Plan 34-05 close-out commit (CONTEXT D-22).
- **Plan 34-04 validation cron runs against Vercel Preview or local dev**, NOT prod (RESEARCH §5.4).
- **Atomic per-decision commits** per CONTEXT D-25.
  </validation_constraints>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `DLQEntry.reason` union with `'v3:cascade_exhausted'`</name>
  <files>server/lib/llmDLQ.ts, server/__tests__/lib/llmDLQ.test.ts</files>
  <read_first>
    - server/lib/llmDLQ.ts (FULL — focus on `DLQEntry.reason` union at lines 22-40 + `enqueueDLQ` at line 53)
    - server/__tests__/lib/llmDLQ.test.ts (FULL — existing union round-trip pattern; if absent, locate `enqueueDLQ` consumers via grep)
    - .planning/phases/34-.../34-CONTEXT.md D-13, D-28
    - .planning/phases/34-.../34-RESEARCH.md §2.2 (the v3:cascade_exhausted enqueue pattern)
  </read_first>
  <behavior>
    - Test 1: `enqueueDLQ({ id: 'test', reason: 'v3:cascade_exhausted', lastError: 'cascade exhausted: nim:rate_limit; cerebras:rate_limit; groq:timeout', timestamp: Date.now() })` succeeds.
    - Test 2: Round-trip through Redis (`listDLQ()` or `smembers`) returns the entry with `reason === 'v3:cascade_exhausted'`.
    - Test 3: `lastError` is capped to 500 chars per existing `enqueueDLQ` invariant; assert with a 600-char input.
    - Test 4: TypeScript compile: `const r: DLQEntry['reason'] = 'v3:cascade_exhausted';` does NOT error.
  </behavior>
  <action>
**Commit 1 — DLQ union extension (`feat(34): cascade_exhausted DLQ reason union member (D-13)`):**

In `server/lib/llmDLQ.ts` at lines 22-40, extend the `DLQEntry.reason` union by adding `| 'v3:cascade_exhausted'` as a new member alongside existing `v3:timeout_watchdog` / `v3:max_tokens_truncation` etc. Place it adjacent to `v3:timeout_watchdog` for thematic grouping (cascade_exhausted is the "all providers failed" sibling of timeout_watchdog's "single-batch kill").

Add a JSDoc comment above the new union member: `// Phase 34 D-13 — distinguishes "all cascade providers failed for this batch" from "single-provider watchdog timeout". Written by extractor at v3.ts:759 area in the !didTimeout fall-through branch.`

Do NOT modify `enqueueDLQ` body; the 500-char cap on `lastError` (existing line 54 `capped` shape) already applies to the new reason.

**Commit 2 — DLQ union test (`test(34): cascade_exhausted DLQ round-trip (D-28)`):**

In `server/__tests__/lib/llmDLQ.test.ts` (create if doesn't exist, mirroring nearby test files' Redis-mock pattern):

a) **D-28: TypeScript compile** — `const r: DLQEntry['reason'] = 'v3:cascade_exhausted';` (compile-time assertion via type annotation).

b) **D-28: enqueue + retrieve** — mock Redis; call `enqueueDLQ({ id: 'test-batch-1', reason: 'v3:cascade_exhausted', lastError: 'cascade exhausted: nim:rate_limit; cerebras:rate_limit; groq:timeout', timestamp: Date.now() })`; assert `listDLQ()` (or equivalent reader) returns an entry where `reason === 'v3:cascade_exhausted'`.

c) **D-28: 500-char cap** — call `enqueueDLQ` with `lastError` of length 600 chars; assert retrieved entry has `lastError.length === 500`.

d) **D-28: bounded 200-cap discipline preserved** — enqueue 201 cascade_exhausted entries; assert `countDLQ() === 200` (existing bound carried through; verifies the new reason doesn't bypass the cap).
</action>
<verify>
<automated>grep -q "'v3:cascade_exhausted'" server/lib/llmDLQ.ts && npx vitest run server/**tests**/lib/llmDLQ.test.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `grep -q "'v3:cascade_exhausted'" server/lib/llmDLQ.ts` (union member present). - `npx tsc --noEmit` exits 0. - `npx vitest run server/__tests__/lib/llmDLQ.test.ts` exits 0 with 4 new D-13/D-28 tests passing. - `git log --oneline -2 | grep -cE "feat\\(34\\): cascade_exhausted DLQ reason|test\\(34\\): cascade_exhausted DLQ round-trip"` returns 2 (2 atomic commits per CONTEXT D-25).
</acceptance_criteria>
<done>DLQ union extended; round-trip verified; 500-char cap + 200-bound discipline preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap extractor null-content branch with cascade_exhausted DLQ enqueue</name>
  <files>server/lib/llmEventExtractor.v3.ts</files>
  <read_first>
    - server/lib/llmEventExtractor.v3.ts (FULL — focus on lines 690-770: locate the `freeClaudeCallLLM` call site, the `routing/decisions` return field, the `if (content === null)` branch at line 731, the `didTimeout` path at lines 739-757, and the fall-through `log.warn` at line 759)
    - .planning/phases/34-.../34-CONTEXT.md D-13, "Claude's Discretion" bullet on `lastError` concatenation (default = concatenated taxonomy `nim:rate_limit; cerebras:rate_limit; groq:timeout`; shorten if 500-char cap at risk)
    - .planning/phases/34-.../34-RESEARCH.md §2.2 (exact pattern with code template)
  </read_first>
  <behavior>
    - Test 1: When `freeClaudeCallLLM` returns `{ content: null, routing: [...] }` AND `didTimeout === false`, the extractor calls `enqueueDLQ` once per batch context with `reason: 'v3:cascade_exhausted'`.
    - Test 2: When `didTimeout === true`, the extractor does NOT call `enqueueDLQ` for cascade_exhausted (avoids double-enqueue — the watchdog wrapper already writes `v3:timeout_watchdog`).
    - Test 3: `lastError` is a concatenation of `routing.map(d => \`${d.provider}:${d.reason}\`).join('; ')`, sliced to 500 chars.
    - Test 4: DLQ enqueue failure (e.g. Redis down) does NOT crash the extractor — wrapped in try/catch with logger.warn on failure.
  </behavior>
  <action>
**Commit 1 — Extractor wrapper (`feat(34): cascade_exhausted DLQ wrapper at extractor null-content branch (D-13)`):**

In `server/lib/llmEventExtractor.v3.ts`, at the null-content fall-through branch starting at line 731:

1. **Locate the existing structure** (verify before editing):
   - `if (content === null)` at line 731.
   - `if (didTimeout) { /* adaptive-split-and-retry */ }` at lines 739-757.
   - `log.warn({ batchIndex }, 'v3 batch yielded no content (null or watchdog timeout)');` at line 759.

2. **Insert the cascade_exhausted DLQ enqueue** AFTER the `didTimeout` branch closes and BEFORE the line-759 log.warn. The new block must be gated by `!didTimeout` (RESEARCH §2.2 pattern):
   - Iterate over `contexts` (the per-batch context array — verify the variable name by grepping line 690-730 area for the batch context shape; commonly named `contexts`, `batches`, or `groupedEvents`).
   - For each context, call `enqueueDLQ` with:
     - `id: ctx.groupKey` (or whichever field uniquely identifies the batch group — verify in current code).
     - `reason: 'v3:cascade_exhausted'`.
     - `lastError`: derived from the `decisions` variable (the `routing` return from `freeClaudeCallLLM`). Pattern: `\`cascade exhausted: ${decisions.map(d => \`${d.provider}:${d.reason}\`).join('; ')}\`.slice(0, 500)`.
     - `timestamp: Date.now()`.
   - Wrap the entire enqueue call in `try/catch`. On catch, `log.warn({ err: dlqErr, groupKey: ctx.groupKey }, 'cascade_exhausted DLQ enqueue failed (non-fatal)')`.

3. **JSDoc comment block above the new block:**

   ```
   // Phase 34 D-13 — distinguish all-cascade-failed from single-provider timeout.
   // didTimeout path is already handled at lines 739-757 (writes v3:timeout_watchdog
   // via withBatchWatchdog). This branch = callLLM returned null content despite
   // no watchdog kill = cascade exhausted (all providers failed pre-checks or 429s).
   ```

4. **Do NOT modify the line-759 `log.warn`** — keep it unchanged so existing log-grep operator queries continue working.

**Verification before commit:**

- `npx tsc --noEmit` exits 0.
- `grep -n "v3:cascade_exhausted" server/lib/llmEventExtractor.v3.ts` returns at least 1 line.

**Commit 2 — Extractor wrapper test (`test(34): cascade_exhausted enqueue on null-content !didTimeout (D-13)`):**

Extend `server/__tests__/lib/llmEventExtractor.v3.test.ts` (or the equivalent test file — locate via grep). Mock `freeClaudeCallLLM` to return `{ content: null, routing: [...mock decisions...] }`:

a) **didTimeout === false → cascade_exhausted enqueue**: Mock `withBatchWatchdog` (or equivalent) to return `{ result: { content: null, routing: [...] }, didTimeout: false }`. Run extractor on a 2-context batch. Assert `enqueueDLQ` was called 2× with `reason: 'v3:cascade_exhausted'`.

b) **didTimeout === true → NO cascade_exhausted enqueue**: Same as (a) but `didTimeout: true`. Assert `enqueueDLQ` was NOT called with `reason: 'v3:cascade_exhausted'` (the existing watchdog wrapper writes `v3:timeout_watchdog` instead).

c) **lastError shape**: Mock `routing = [{ provider: 'nvidia_nim', reason: 'rate_limit' }, { provider: 'cerebras', reason: 'rate_limit' }, { provider: 'groq', reason: 'timeout' }]`. Assert `enqueueDLQ` argument `lastError` matches `'cascade exhausted: nvidia_nim:rate_limit; cerebras:rate_limit; groq:timeout'` (≤500 chars).

d) **DLQ failure tolerance**: Mock `enqueueDLQ` to throw. Run extractor. Assert NO crash; assert log.warn called with the DLQ error.
</action>
<verify>
<automated>grep -q "v3:cascade_exhausted" server/lib/llmEventExtractor.v3.ts && grep -q "!didTimeout" server/lib/llmEventExtractor.v3.ts && npx vitest run server/**tests**/lib/llmEventExtractor.v3.test.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `grep -q "v3:cascade_exhausted" server/lib/llmEventExtractor.v3.ts` (enqueue site present). - `grep -qE "!didTimeout|didTimeout === false" server/lib/llmEventExtractor.v3.ts` (correct gate). - `grep -q "cascade exhausted:" server/lib/llmEventExtractor.v3.ts` (lastError pattern present). - `grep -q ".slice(0, 500)" server/lib/llmEventExtractor.v3.ts` (500-char cap present, mirrors enqueueDLQ existing cap defensively). - `npx vitest run server/__tests__/lib/llmEventExtractor.v3.test.ts` exits 0 with 4 new D-13 tests passing. - `npx tsc --noEmit` exits 0. - `git log --oneline -2 | grep -cE "feat\\(34\\): cascade_exhausted DLQ wrapper|test\\(34\\): cascade_exhausted enqueue"` returns 2 (2 atomic commits per CONTEXT D-25).
</acceptance_criteria>
<done>Extractor wraps null-content branch with cascade_exhausted DLQ enqueue, gated by `!didTimeout`; tests verify no double-enqueue on watchdog path; lastError taxonomy capped at 500 chars; DLQ failure is non-fatal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `?skipPrimary=nvidia_nim` validation flag (cron + pipeline + router)</name>
  <files>server/routes/refresh-events-cron.ts, server/lib/llmExtractionPipeline.ts, server/lib/freeClaudeRouter.ts</files>
  <read_first>
    - server/routes/refresh-events-cron.ts (lines 40-80 — Bearer gate + forceCooldown handler)
    - server/lib/llmExtractionPipeline.ts (locate `runRefreshExtraction` function signature and how it passes opts to `freeClaudeCallLLM`)
    - server/lib/freeClaudeRouter.ts (lines 349-365 area — `allProviders[]` declaration + the existing `skipOpenRouter` filter pattern at lines 362-363)
    - server/config.ts (verify `env.NODE_ENV` is validated by `parseEnv()`)
    - .planning/phases/34-.../34-CONTEXT.md D-22 + "Claude's Discretion" bullet on query-param-vs-Redis-flag (default = query param)
    - .planning/phases/34-.../34-RESEARCH.md §2.5 (exact insertion pattern with env-gate)
  </read_first>
  <behavior>
    - Test 1: `GET /api/cron/refresh-events?skipPrimary=nvidia_nim` with valid Bearer AND `NODE_ENV !== 'production'` triggers `runRefreshExtraction({ skipPrimary: 'nvidia_nim' })`.
    - Test 2: `GET /api/cron/refresh-events?skipPrimary=nvidia_nim` with valid Bearer AND `NODE_ENV === 'production'` triggers `runRefreshExtraction({ skipPrimary: null })` (flag IGNORED in prod); log.warn fires.
    - Test 3: `GET /api/cron/refresh-events?skipPrimary=nvidia_nim` WITHOUT Bearer returns 401 (existing Bearer gate unchanged).
    - Test 4: `GET /api/cron/refresh-events?skipPrimary=other_provider` is IGNORED — `skipPrimary` resolves to `null` (only `'nvidia_nim'` is honored — narrow surface).
    - Test 5: When `skipPrimary === 'nvidia_nim'` is threaded into `callLLM`, the `allProviders[]` is filtered to exclude `nvidia_nim` (similar to existing `skipOpenRouter: true` filter).
  </behavior>
  <action>
**Commit 1 — Cron handler skipPrimary flag (`feat(34): temporary skipPrimary validation flag in cron handler (D-22)`):**

In `server/routes/refresh-events-cron.ts`, after the existing `forceCooldown` line (~line 60):

1. Read `req.query.skipPrimary`.
2. Type-narrow to `'nvidia_nim' | null`:
   - If raw value `=== 'nvidia_nim'` AND `env.NODE_ENV !== 'production'` (fallback `process.env.VERCEL_ENV !== 'production'` if `NODE_ENV` not in parseEnv): set `skipPrimary = 'nvidia_nim'`; log.warn(`{ skipPrimary }, 'Phase 34 D-22 — skipPrimary flag honored (non-prod)')`.
   - Else if raw value is present AND `env.NODE_ENV === 'production'`: log.warn(`{ skipPrimaryRaw: raw }, 'Phase 34 D-22 — skipPrimary flag REFUSED in prod'`); `skipPrimary = null`.
   - Else: `skipPrimary = null`.
3. Thread `skipPrimary` into `runRefreshExtraction({ triggeredBy, forceCooldown, skipPrimary, ... })`.
4. JSDoc comment above the new block citing CONTEXT D-22 + "witness, not fixture — SHIPS in Plan 34-04, REVERTED in Plan 34-05".

**Commit 2 — Pipeline + router threading (`feat(34): thread skipPrimary through pipeline + router (D-22)`):**

In `server/lib/llmExtractionPipeline.ts`:

- Extend `runRefreshExtraction` options type with `skipPrimary?: 'nvidia_nim' | null`.
- Thread `skipPrimary` down to the `freeClaudeCallLLM(...)` call site via the existing `opts` argument. Extend `opts` shape to `{ batchSize?, modelOverride?, skipOpenRouter?, skipPrimary? }`.

In `server/lib/freeClaudeRouter.ts`:

- Extend `callLLM`'s opts type with `skipPrimary?: 'nvidia_nim' | null`.
- After the existing `skipOpenRouter` filter at lines 362-363 (`allProviders.filter((p) => p.name !== 'openrouter')`), add a chained filter:
  ```
  let providers = opts?.skipOpenRouter
    ? allProviders.filter((p) => p.name !== 'openrouter')
    : allProviders;
  if (opts?.skipPrimary === 'nvidia_nim') {
    providers = providers.filter((p) => p.name !== 'nvidia_nim');
  }
  ```
- Replace the existing `let providers = ...` declaration with the chained version.
- JSDoc above the filter: `// Phase 34 D-22 — temporary validation flag; SHIPS in Plan 34-04, REVERTED in Plan 34-05.`

**Commit 3 — Tests (`test(34): skipPrimary cron handler + router filter (D-22)`):**

a) **In `server/__tests__/routes/refresh-events-cron.test.ts`** (extend existing or create):

- Test: skipPrimary honored in non-prod (NODE_ENV stubbed to 'development'). Mock `runRefreshExtraction`; assert called with `{ skipPrimary: 'nvidia_nim' }`.
- Test: skipPrimary REFUSED in prod (NODE_ENV stubbed to 'production'). Mock `runRefreshExtraction`; assert called with `{ skipPrimary: null }`; log.warn fires.
- Test: no Bearer → 401 (regression check, no change).
- Test: skipPrimary=other_provider (non-allowed value) → resolves to `null`.

b) **In `server/__tests__/lib/freeClaudeRouter.test.ts`** (extend the Phase 34 fall-through describe block):

- Test: With `opts.skipPrimary === 'nvidia_nim'`, `allProviders[]` is filtered to exclude NIM; cascade attempt starts with the next provider (Cerebras or Groq, whichever passed Plan 34-01).
- Test: When BOTH `opts.skipOpenRouter === true` AND `opts.skipPrimary === 'nvidia_nim'`, the cascade only attempts the restored fallbacks.
  </action>
  <verify>
  <automated>grep -q "skipPrimary" server/routes/refresh-events-cron.ts && grep -q "skipPrimary" server/lib/llmExtractionPipeline.ts && grep -q "opts?.skipPrimary === 'nvidia_nim'" server/lib/freeClaudeRouter.ts && npx vitest run server/**tests**/routes/refresh-events-cron.test.ts server/**tests**/lib/freeClaudeRouter.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
- `grep -q "skipPrimary" server/routes/refresh-events-cron.ts`.
- `grep -qE "NODE_ENV !== 'production'|VERCEL_ENV !== 'production'" server/routes/refresh-events-cron.ts` (env gate present).
- `grep -q "skipPrimary" server/lib/llmExtractionPipeline.ts` (pipeline threading).
- `grep -q "p.name !== 'nvidia_nim'" server/lib/freeClaudeRouter.ts` (router filter).
- `grep -q "REFUSED in prod" server/routes/refresh-events-cron.ts` (log.warn message identifying the prod block).
- `npx vitest run server/__tests__/routes/refresh-events-cron.test.ts` exits 0 with 4 new D-22 tests passing.
- `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 with 2 new skipPrimary tests passing.
- `npx tsc --noEmit` exits 0.
- `git log --oneline -3 | grep -cE "feat\\(34\\): temporary skipPrimary|feat\\(34\\): thread skipPrimary|test\\(34\\): skipPrimary"` returns 3 (3 atomic commits per CONTEXT D-25).
  </acceptance_criteria>
  <done>Cron handler accepts `?skipPrimary=nvidia_nim` non-prod only; pipeline threads it through; router filters NIM out of cascade when flag set; 6 new tests pass; Bearer + env gates intact.</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Validation cron run + snapshot capture against non-prod</name>
  <files>n/a (human checkpoint — operator UAT only)</files>
  <action>
**Human checkpoint — operator action required.**

Adapter restoration (Plan 34-02) + per-provider eval (Plan 34-03) + cascade_exhausted DLQ + skipPrimary flag (this plan, Tasks 1-3) are all merged on `feature/34-router-fallback-reintegration`. Now force-run the cron with skipPrimary against a non-prod environment and capture the snapshot that proves SC-3 + SC-4 + SC-5 evidence.

**Steps:**

1. **Deploy to Vercel Preview** (RESEARCH §5.4 — NOT prod):

   ```bash
   git push origin feature/34-router-fallback-reintegration
   # Vercel auto-creates a preview deployment; capture the preview URL
   ```

   Confirm preview URL has both `CEREBRAS_API_KEY` + `GROQ_API_KEY` populated (preview env should inherit production env vars OR be explicitly set in Vercel Settings → Environment Variables → Preview).

   **Alternative (if Vercel Preview env-key inheritance is unclear):** Run `npm run dev` locally with `.env.local` containing the keys, and run the validation cron against `http://localhost:3000`.

2. **Force-trigger the validation cron with skipPrimary flag:**

   ```bash
   # Against Vercel Preview
   curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" \
        "https://<preview-url>/api/cron/refresh-events?force=true&skipPrimary=nvidia_nim"

   # OR against local dev
   curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" \
        "http://localhost:3000/api/cron/refresh-events?force=true&skipPrimary=nvidia_nim"
   ```

   Wall-clock: up to ~10 min for a full extraction run.

3. **Capture snapshot via the Phase 31 harness:**

   ```bash
   npm run watch:snapshot -- --http | tee .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json
   ```

4. **Run jq assertions per RESEARCH §4 aggregate block:**

   ```bash
   SNAP=.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json

   # SC-3: callHistory shows non-NIM provider names
   jq -e '[.callHistory[] | select(.provider == "cerebras" or .provider == "groq")] | length > 0' "$SNAP"

   # SC-4: per-provider eval surfaced
   jq -e '.evalScore.byProvider' "$SNAP"

   # SC-5: DLQ count materially below Phase 31 Day-1 baseline (4 × v3:timeout_watchdog)
   jq -e '.dlq.totalCount < 4' "$SNAP"
   ```

5. **Pitfall 1 cache-bridge regression (SC-6):**

   ```bash
   npx vitest run server/__tests__/routes/events.pitfall1.test.ts
   ```

   MUST exit 0. If this test fails, the new providerProvenance field has accidentally broken the raw-GDELT terminal fallback. Investigate before proceeding.

6. **Capture snapshot summary in commit body:**
   - 1-line summary of `callHistory` provider distribution (e.g. `cerebras: 5, groq: 3, nvidia_nim: 0 (skipped via flag)`).
   - 1-line summary of `evalScore.byProvider` per-provider rows.
   - 1-line DLQ count (`v3:cascade_exhausted: N, v3:timeout_watchdog: M`).
   - 1-line Pitfall 1 test result.

7. **Commit the snapshot:**
   ```bash
   git add .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json
   git commit -m "feat(34): validation cron snapshot — cascade restored evidence (D-21, D-22)"
   ```
   Commit body contains the 4-line summary from step 6.

**Resume signal:** Reply with "validated — snapshot evidence captured" + paste the 4-line summary, OR describe failures (e.g. "no cerebras rows in callHistory — keys not populated in preview env"). On failure, debug before Plan 34-05.
</action>
<verify>
<automated>echo "Human checkpoint — see action block for verification steps; operator confirms via resume signal"; true</automated>
</verify>
<done>Operator approves via resume signal per the action block; execution unpauses.</done>
<what-built>
Adapter restoration (Plan 34-02) + per-provider eval (Plan 34-03) + cascade_exhausted DLQ + skipPrimary flag (this plan, Tasks 1-3) are all merged on `feature/34-router-fallback-reintegration`. Now force-run the cron with skipPrimary against a non-prod environment and capture the snapshot that proves SC-3 + SC-4 + SC-5 evidence.
</what-built>
<how-to-verify>

1. **Deploy to Vercel Preview** (RESEARCH §5.4 — NOT prod):

   ```bash
   git push origin feature/34-router-fallback-reintegration
   # Vercel auto-creates a preview deployment; capture the preview URL
   ```

   Confirm preview URL has both `CEREBRAS_API_KEY` + `GROQ_API_KEY` populated (preview env should inherit production env vars OR be explicitly set in Vercel Settings → Environment Variables → Preview).

   **Alternative (if Vercel Preview env-key inheritance is unclear):** Run `npm run dev` locally with `.env.local` containing the keys, and run the validation cron against `http://localhost:3000`.

2. **Force-trigger the validation cron with skipPrimary flag:**

   ```bash
   # Against Vercel Preview
   curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" \
        "https://<preview-url>/api/cron/refresh-events?force=true&skipPrimary=nvidia_nim"

   # OR against local dev
   curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" \
        "http://localhost:3000/api/cron/refresh-events?force=true&skipPrimary=nvidia_nim"
   ```

   Wall-clock: up to ~10 min for a full extraction run.

3. **Capture snapshot via the Phase 31 harness:**

   ```bash
   npm run watch:snapshot -- --http | tee .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json
   ```

4. **Run jq assertions per RESEARCH §4 aggregate block:**

   ```bash
   SNAP=.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json

   # SC-3: callHistory shows non-NIM provider names
   jq -e '[.callHistory[] | select(.provider == "cerebras" or .provider == "groq")] | length > 0' "$SNAP"

   # SC-4: per-provider eval surfaced
   jq -e '.evalScore.byProvider' "$SNAP"

   # SC-5: DLQ count materially below Phase 31 Day-1 baseline (4 × v3:timeout_watchdog)
   jq -e '.dlq.totalCount < 4' "$SNAP"
   ```

5. **Pitfall 1 cache-bridge regression (SC-6):**

   ```bash
   npx vitest run server/__tests__/routes/events.pitfall1.test.ts
   ```

   MUST exit 0. If this test fails, the new providerProvenance field has accidentally broken the raw-GDELT terminal fallback. Investigate before proceeding.

6. **Capture snapshot summary in commit body:**
   - 1-line summary of `callHistory` provider distribution (e.g. `cerebras: 5, groq: 3, nvidia_nim: 0 (skipped via flag)`).
   - 1-line summary of `evalScore.byProvider` per-provider rows.
   - 1-line DLQ count (`v3:cascade_exhausted: N, v3:timeout_watchdog: M`).
   - 1-line Pitfall 1 test result.

7. **Commit the snapshot:**
   ```bash
   git add .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json
   git commit -m "feat(34): validation cron snapshot — cascade restored evidence (D-21, D-22)"
   ```
   Commit body contains the 4-line summary from step 6.
   </how-to-verify>
   <resume-signal>Reply with "validated — snapshot evidence captured" + paste the 4-line summary, OR describe failures (e.g. "no cerebras rows in callHistory — keys not populated in preview env"). On failure, debug before Plan 34-05.</resume-signal>
   </task>

<task type="auto" tdd="false">
  <name>Task 5: Pitfall 1 cache-bridge regression test pass</name>
  <files>server/__tests__/routes/events.pitfall1.test.ts</files>
  <read_first>
    - server/__tests__/routes/events.pitfall1.test.ts (FULL — verify it still asserts that `/api/events` returns raw GDELT when `events:llm:v3` is empty)
    - .planning/phases/34-.../34-RESEARCH.md §4 SC-6
  </read_first>
  <action>
**NO CODE CHANGE in this task.** Just RUN the existing test and confirm SC-6 invariant holds.

```bash
npx vitest run server/__tests__/routes/events.pitfall1.test.ts
```

Expected: EXIT 0. The Phase 34 changes (additive `providerProvenance` field on enrichedEventV3; new cascade_exhausted DLQ reason; new skipPrimary cron flag) MUST NOT break the raw-GDELT terminal fallback contract. The `providerProvenance` field is additive-optional so old v1/v2 entries plus pre-Phase-34 v3 entries still validate; raw GDELT is served when `events:llm:v3` is empty regardless of the cascade shape.

If this test fails, STOP and investigate. Likely root causes:

1. `enrichedEventV3` Zod schema change accidentally tightened a field (verify `.nullable().optional()` on `providerProvenance`).
2. Discriminated union at `llmSchema.ts:212` accidentally modified.
3. `/api/events` route code accidentally touched (this plan does NOT modify `server/routes/events.ts`).

If the test passes, no commit needed for this task — it's a verification gate.
</action>
<verify>
<automated>npx vitest run server/**tests**/routes/events.pitfall1.test.ts 2>&1 | tail -15</automated>
</verify>
<acceptance_criteria> - `npx vitest run server/__tests__/routes/events.pitfall1.test.ts` exits 0. - Test output names match the expected Pitfall 1 contract assertions (e.g. "returns raw GDELT when events:llm:v3 is empty", "Map never goes blank", or equivalent). - No new commits created for this task (verification-only).
</acceptance_criteria>
<done>Pitfall 1 cache-bridge regression test passes; SC-6 invariant intact post-Phase-34 changes; "Map never goes blank" contract honored.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                  |
| ---------------------------------------- | -------------------------------------------- |
| cron handler → pipeline                  | Bearer-gated + env-gated `?skipPrimary` flag |
| pipeline → router                        | Threaded options including `skipPrimary`     |
| extractor → DLQ                          | New `v3:cascade_exhausted` enqueue path      |
| validation snapshot → committed artifact | Captures evidence of restored cascade firing |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                         | Disposition | Mitigation Plan                                                                                                                                                                                |
| --------- | ---------------------- | ----------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| T-34-17   | Elevation of privilege | skipPrimary flag in production                                    | mitigate    | Env-gate at cron handler (`env.NODE_ENV !== 'production'` AND/OR `VERCEL_ENV !== 'production'`); production REFUSES even with valid Bearer per RESEARCH §2.5. log.warn surfaces attempted use. |
| T-34-18   | Tampering              | skipPrimary value injection (e.g. `?skipPrimary=openrouter`)      | mitigate    | Type-narrow to `'nvidia_nim'                                                                                                                                                                   | null`; only the literal `'nvidia_nim'`value is honored. Any other string resolves to`null`. |
| T-34-19   | Repudiation            | "Did the validation cron actually exercise the cascade?"          | mitigate    | Validation snapshot captured at `plan-34-04-snapshot.json` and committed with 4-line summary in commit body; auditable forever.                                                                |
| T-34-20   | Denial of service      | cascade_exhausted DLQ enqueue floods Redis on cascade-wide outage | mitigate    | Existing `enqueueDLQ` bounded SADD with 200-entry cap; new reason inherits the cap. DLQ enqueue failure is non-fatal (try/catch + log.warn).                                                   |
| T-34-21   | Information disclosure | Snapshot artifact contains decision history                       | accept      | `lastError` capped 500 chars; per-provider reasons (`rate_limit`, `timeout`, etc.) are operator-known status strings; no API key material leaked.                                              |
| T-34-22   | Spoofing               | skipPrimary flag left in place post-validation                    | mitigate    | CONTEXT D-22 + Plan 34-05 D-22 invariant: flag is "witness, not fixture" — Plan 34-05 REVERTS the flag in close-out commit. This plan SHIPS it; the next plan removes it.                      |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run server/__tests__/lib/llmDLQ.test.ts server/__tests__/lib/llmEventExtractor.v3.test.ts server/__tests__/routes/refresh-events-cron.test.ts server/__tests__/lib/freeClaudeRouter.test.ts server/__tests__/routes/events.pitfall1.test.ts` exits 0.
- `npx vitest run` (full suite) exits 0.
- Validation snapshot at `.planning/phases/34-.../plan-34-04-snapshot.json` exists with `callHistory[].provider` containing at least one `cerebras` or `groq` entry (SC-3 evidence).
- `jq -e '.evalScore.byProvider' .../plan-34-04-snapshot.json` succeeds (SC-4 evidence).
- `jq -e '.dlq.totalCount < 4' .../plan-34-04-snapshot.json` succeeds (SC-5 evidence vs Phase 31 Day-1 baseline of 4).
- Pitfall 1 cache-bridge test passes (SC-6 invariant).
- `git log --oneline feature/34-router-fallback-reintegration | grep "feat(34):" | tail -10` shows atomic per-decision commits per CONTEXT D-25:
  - `feat(34): cascade_exhausted DLQ reason union member (D-13)`
  - `test(34): cascade_exhausted DLQ round-trip (D-28)`
  - `feat(34): cascade_exhausted DLQ wrapper at extractor null-content branch (D-13)`
  - `test(34): cascade_exhausted enqueue on null-content !didTimeout (D-13)`
  - `feat(34): temporary skipPrimary validation flag in cron handler (D-22)`
  - `feat(34): thread skipPrimary through pipeline + router (D-22)`
  - `test(34): skipPrimary cron handler + router filter (D-22)`
  - `feat(34): validation cron snapshot — cascade restored evidence (D-21, D-22)`
</verification>

<success_criteria>

- `DLQEntry.reason` union accepts `'v3:cascade_exhausted'`; round-trip tested.
- Extractor wraps null-content `!didTimeout` branch with cascade_exhausted enqueue; double-enqueue on watchdog path prevented.
- Temporary `?skipPrimary=nvidia_nim` flag honored in non-prod only; threaded through pipeline + router.
- Validation cron snapshot captured against non-prod; SC-3 (non-NIM provider in callHistory), SC-4 (per-provider eval), SC-5 (DLQ count < 4) all proved by jq predicates.
- SC-6 Pitfall 1 cache-bridge regression intact.
- LLM-RELI-11 satisfied — DLQ count materially below baseline + new cascade_exhausted reason + per-provider eval within ±3pp of NIM baseline (Plan 34-03 invariant carried through).
- skipPrimary flag SHIPS in this plan; ready for revert in Plan 34-05 per CONTEXT D-22.
  </success_criteria>

<output>
Create `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-04-SUMMARY.md`. Include:
- DLQ union extension landed.
- Extractor wrapper landed with `!didTimeout` gate.
- skipPrimary flag landed (non-prod only); explicitly noted as TO-BE-REVERTED in Plan 34-05.
- Validation snapshot summary (4 lines: callHistory provider distribution, evalScore byProvider, DLQ count, Pitfall 1 result).
- SC-3 / SC-4 / SC-5 / SC-6 evidence cited from snapshot + test runs.
- Forward signal for Plan 34-05: phase close + revert.
</output>

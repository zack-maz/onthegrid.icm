---
phase: 34-llm-router-fallback-re-integration-cerebras-groq-per-provide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe-cerebras-groq.ts
  - package.json
  - .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json
autonomous: true
requirements: [LLM-RELI-08]
tags: [llm, router, cascade, probe, free-tier, cerebras, groq]

must_haves:
  truths:
    - 'Operator can run `npm run probe:cerebras-groq` and produce a byte-stable JSON artifact at `.planning/phases/34-.../probe-snapshot.json`.'
    - 'The artifact records per-provider rate-limit behavior (rateLimitedCount, rateLimitedPct, decision bucket) and latency quantiles (latencyP50, latencyP95) for Cerebras AND Groq.'
    - "The artifact's `decision` per provider drives Plan 34-02's gating (`<50%` → integrate; `50-90%` → middle-bucket defer; `≥90%` → defer)."
    - 'Re-running the script produces a byte-identical snapshot when provider responses are stable (sorted results, ISO-Z timestamp, integer latency, 200-char error truncation, atomic temp-rename write).'
    - "Cerebras model probed is `qwen-3-235b-a22b-instruct-2507` (RESEARCH §1.1 Path A — CONTEXT.md D-05's `llama3.3-70b` proposal is stale)."
    - 'Groq model probed is `llama-3.3-70b-versatile` (CONTEXT.md D-05 confirmed by RESEARCH §1.2).'
  artifacts:
    - path: 'scripts/probe-cerebras-groq.ts'
      provides: 'Probe runner mirroring `scripts/probe-openrouter.ts`; emits combined per-provider snapshot.'
      min_lines: 200
    - path: '.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json'
      provides: 'Combined per-provider probe artifact.'
      contains: 'byProvider'
    - path: 'package.json'
      provides: '`probe:cerebras-groq` script entry.'
      contains: 'probe:cerebras-groq'
  key_links:
    - from: 'scripts/probe-cerebras-groq.ts'
      to: 'server/config.ts'
      via: 'import of `CEREBRAS_API_KEY` + `GROQ_API_KEY` from `parseEnv()`'
      pattern: 'CEREBRAS_API_KEY|GROQ_API_KEY'
    - from: 'scripts/probe-cerebras-groq.ts'
      to: '.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json'
      via: 'atomic temp-rename write (`.tmp` → final via `renameSync`)'
      pattern: "renameSync|writeFileSync.*\\.tmp"
---

<objective>
Probe Cerebras + Groq free-tier rate-limit behavior against the active v3 extractor payload shape. Output a byte-stable combined snapshot that drives Plan 34-02's per-provider integrate-or-defer decision.

Purpose: Provides the empirical gate for cascade restoration. CONTEXT D-01 makes this probe the integration decision; the snapshot IS the deliverable regardless of outcome (CONTEXT D-02 supports `cerebras-groq-deferred` honest close-out if both providers ≥90% rate-limited).
Output: `scripts/probe-cerebras-groq.ts`, `package.json` script entry, `.planning/phases/34-.../probe-snapshot.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-RESEARCH.md
@.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-CONTEXT.md
@.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json
@scripts/probe-openrouter.ts
@server/config.ts

<interfaces>
<!-- Key contracts the executor needs from the codebase. Extracted to avoid scavenger-hunt. -->

From scripts/probe-openrouter.ts (TEMPLATE — mirror byte-for-byte except as noted):

- Top-of-file: imports `OpenAI` from 'openai', `OPENROUTER_DEFAULT_MODEL` from '../server/lib/freeClaudeRouter.js', `OPENROUTER_API_KEY` from '../server/config.js'.
- Constants: `N = 30`, `GAP_MS = 100`, `SNAPSHOT_PATH = '.planning/phases/30.1-.../30.1-or-pulse-snapshot.json'`.
- Helpers: `singleEventPayload()` returns the v3-shaped messages array (minimal but realistic); `classifyOutcome(err)` substring matcher returning `'ok' | 'rate_limit' | 'timeout' | '5xx' | 'other'`; `extractRetryAfterMs(err)` reads `retry-after` header (handles seconds vs HTTP-date).
- Probe loop: sequential `for (let i = 1; i <= N; i++)` with `await new Promise(r => setTimeout(r, GAP_MS))` between attempts.
- Output: `results[]` sorted by `attempt`; `summary = { rateLimitedCount, rateLimitedPct, decision }`; atomic temp-rename write (`.tmp` → final via `renameSync`).

From server/config.ts:31-32:

- `CEREBRAS_API_KEY: z.string().default('')` and `GROQ_API_KEY: z.string().default('')` already in `parseEnv()`. Both default to empty string; probe must guard with `if (!CEREBRAS_API_KEY) throw new Error('CEREBRAS_API_KEY required')` and same for Groq.

From .planning/phases/30.1-.../30.1-or-pulse-snapshot.json (SHAPE TEMPLATE):

- `{ timestamp: ISO-Z, model, n, gapMs, results: [{ attempt, latencyMs, status, errorBucket, errorMessage?, retryAfterMs? }], summary: { rateLimitedCount, rateLimitedPct, decision } }`.
- Phase 34 EXTENDS `summary` with `latencyP50: number, latencyP95: number` (rounded integer ms) per RESEARCH §5.5 — REQUIRED for D-09 cascade ordering.
- Phase 34 NESTS the entire shape under `byProvider.{cerebras,groq}` per CONTEXT D-03; top-level `timestamp` remains for whole-run timestamp.
  </interfaces>

<provider_endpoints>

- Cerebras base URL: `https://api.cerebras.ai/v1`
- Cerebras model (LOCKED per RESEARCH §1.1 — Path A): `qwen-3-235b-a22b-instruct-2507`
  - Backup Path B (only if Path A probe fails outright on auth/availability, NOT on rate limit): `gpt-oss-120b`. Path A failing on RATE LIMITS is an expected outcome and NOT cause to pivot to Path B (RESEARCH §5.1).
- Groq base URL: `https://api.groq.com/openai/v1`
- Groq model (LOCKED per RESEARCH §1.2 + CONTEXT D-05): `llama-3.3-70b-versatile` (NOT `llama-3.1-70b-versatile` — deprecated).
  </provider_endpoints>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create probe runner `scripts/probe-cerebras-groq.ts`</name>
  <files>scripts/probe-cerebras-groq.ts</files>
  <read_first>
    - scripts/probe-openrouter.ts (template — read FULL file)
    - server/config.ts (lines 1-100 for parseEnv + CEREBRAS_API_KEY/GROQ_API_KEY shape)
    - .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json (shape template)
    - .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-RESEARCH.md §1 + §5.5 + §5.6 (provider catalog + latency-quantile requirement)
    - .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md D-03..D-06 (probe shape requirements)
  </read_first>
  <action>
Create `scripts/probe-cerebras-groq.ts` by mirroring `scripts/probe-openrouter.ts` line-for-line except:

1. **Imports:** `CEREBRAS_API_KEY` + `GROQ_API_KEY` from `'../server/config.js'`. No imports from `freeClaudeRouter` (the adapter doesn't exist yet — model literals are local to this script).
2. **Constants:** `N = 30` and `GAP_MS = 100` (CONTEXT D-04). `SNAPSHOT_PATH = '.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json'`.
3. **Provider configs (local consts):**
   - `CEREBRAS_BASE_URL = 'https://api.cerebras.ai/v1'`
   - `CEREBRAS_MODEL = 'qwen-3-235b-a22b-instruct-2507'` (RESEARCH §1.1 Path A LOCK — do NOT pivot to Path B for rate-limit failures; that's an expected outcome per RESEARCH §5.1)
   - `GROQ_BASE_URL = 'https://api.groq.com/openai/v1'`
   - `GROQ_MODEL = 'llama-3.3-70b-versatile'` (CONTEXT D-05 + RESEARCH §1.2)
4. **Reuse helpers VERBATIM** from `probe-openrouter.ts`: `singleEventPayload()`, `classifyOutcome(err)`, `extractRetryAfterMs(err)`. Copy these in (no shared helper file — the script must be self-contained for byte-stability discipline per CONTEXT D-06).
5. **Per-provider probe function:** `async function probeProvider({name, baseURL, apiKey, model}): Promise<ProviderSnapshot>` runs the existing 30-attempt sequential loop with 100ms `GAP_MS` between. Each result row: `{ attempt: number, latencyMs: number /* integer */, status: 'ok'|'rate_limit'|'timeout'|'5xx'|'other', errorMessage?: string /* truncated to 200 chars */, retryAfterMs?: number }`. Sort `results[]` by `attempt` before writing (CONTEXT D-06 byte-stability).
6. **Latency quantiles per RESEARCH §5.5:** After collecting `results[]`, compute `latencyP50` + `latencyP95` from successful attempts (or all attempts if successes < 10; rationale: probe is rate-limit-shaped, latency on 429 responses is still signal). Implementation: sort `latencyMs` ascending, p50 = index `Math.floor(0.5 * n)`, p95 = index `Math.floor(0.95 * n)`. Round to integer ms.
7. **Per-provider summary shape:** `{ rateLimitedCount, rateLimitedPct /* rounded integer percentage */, latencyP50, latencyP95, decision: 'integrate'|'middle-bucket-defer'|'defer' }`. Decision rules (CONTEXT D-02):
   - `rateLimitedPct < 50` → `'integrate'`
   - `50 <= rateLimitedPct < 90` → `'middle-bucket-defer'`
   - `rateLimitedPct >= 90` → `'defer'`
8. **`main()`:** sequential execution Cerebras THEN Groq (CONTEXT D-03). Guard each provider call with `if (!CEREBRAS_API_KEY) { console.error('CEREBRAS_API_KEY required'); process.exit(2); }` and same for Groq.
9. **Output write (atomic):** `const payload = { timestamp: new Date().toISOString() /* ISO-Z */, byProvider: { cerebras: { n: N, gapMs: GAP_MS, model: CEREBRAS_MODEL, results, summary }, groq: { n: N, gapMs: GAP_MS, model: GROQ_MODEL, results, summary } } };` then `writeFileSync(SNAPSHOT_PATH + '.tmp', JSON.stringify(payload, null, 2)); renameSync(SNAPSHOT_PATH + '.tmp', SNAPSHOT_PATH);` (CONTEXT D-06 atomic write).
10. **Console output:** Print a 2-line summary per provider after each completes (e.g. `cerebras: 27/30 rate-limited (90%) → defer | p50=120ms p95=8400ms`). The console output is operator-readable; the JSON is machine-readable.
11. **TypeScript discipline:** strict mode (no `any` except for OpenAI SDK error shape — mirror probe-openrouter.ts's handling). Use `logger.child({ module: 'probe-cerebras-groq' })` for any non-summary logs (CLAUDE.md invariant).

The script must be executable via `node --env-file=.env.local --import tsx/esm scripts/probe-cerebras-groq.ts` (CONTEXT canonical refs — `scripts/eval-replay.ts` runner pattern). Do NOT add a shebang; the npm script entry handles invocation.
</action>
<verify>
<automated>node --check scripts/probe-cerebras-groq.ts 2>&1 || npx tsc --noEmit scripts/probe-cerebras-groq.ts 2>&1 | head -20</automated>
</verify>
<acceptance_criteria> - `scripts/probe-cerebras-groq.ts` exists. - `grep -q "qwen-3-235b-a22b-instruct-2507" scripts/probe-cerebras-groq.ts` (Cerebras model LOCK per RESEARCH §1.1). - `grep -q "llama-3.3-70b-versatile" scripts/probe-cerebras-groq.ts` (Groq model LOCK per CONTEXT D-05). - `grep -q "renameSync" scripts/probe-cerebras-groq.ts` (atomic write per CONTEXT D-06). - `grep -q "latencyP50" scripts/probe-cerebras-groq.ts && grep -q "latencyP95" scripts/probe-cerebras-groq.ts` (RESEARCH §5.5 schema extension). - `grep -q "CEREBRAS_API_KEY" scripts/probe-cerebras-groq.ts && grep -q "GROQ_API_KEY" scripts/probe-cerebras-groq.ts`. - `grep -cE "decision.*=.*'(integrate|middle-bucket-defer|defer)'" scripts/probe-cerebras-groq.ts` returns ≥3 (3 decision branches present). - `grep -q "errorMessage.*slice.*200" scripts/probe-cerebras-groq.ts || grep -q "substring.*200" scripts/probe-cerebras-groq.ts` (200-char truncation per CONTEXT D-06). - `npx tsc --noEmit --project tsconfig.json` exits 0 (no TS errors introduced).
</acceptance_criteria>
<done>Probe script exists, compiles cleanly under strict TS, locks the correct per-provider models per RESEARCH overrides, emits the extended latency-quantile schema per RESEARCH §5.5, writes atomically via temp-rename per CONTEXT D-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `probe:cerebras-groq` script entry to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (FULL — locate existing `scripts` block and the `probe:openrouter` entry as the pattern)
  </read_first>
  <action>
Add a new `scripts` entry `"probe:cerebras-groq": "node --env-file=.env.local --import tsx/esm scripts/probe-cerebras-groq.ts"` alongside the existing `probe:openrouter` entry. Match the existing entry's flags byte-for-byte (same runner shape per CONTEXT canonical refs §"Scripts to Mirror").

Do NOT modify any other `scripts` entries. Do NOT bump dependencies. This is a single-line addition.
</action>
<verify>
<automated>node -e "const p = require('./package.json'); if (!p.scripts['probe:cerebras-groq']) { console.error('missing probe:cerebras-groq script'); process.exit(1); } console.log(p.scripts['probe:cerebras-groq']);"</automated>
</verify>
<acceptance_criteria> - `node -e "console.log(require('./package.json').scripts['probe:cerebras-groq'])"` prints the runner command. - `grep -q "probe:cerebras-groq" package.json`. - `grep -q "node --env-file=.env.local --import tsx/esm scripts/probe-cerebras-groq.ts" package.json` (exact runner shape mirrors `probe:openrouter`). - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (JSON still valid).
</acceptance_criteria>
<done>`package.json` has `probe:cerebras-groq` script entry mirroring the existing `probe:openrouter` runner pattern; JSON remains valid.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Execute probe and commit snapshot artifact</name>
  <files>.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json</files>
  <read_first>
    - scripts/probe-cerebras-groq.ts (from Task 1 — verify before invoking)
    - package.json (verify script entry from Task 2)
    - .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md D-02 (three-bucket viability scheme; the probe outcome IS the deliverable; deferral is an acceptable close per Phase 30.1 precedent)
  </read_first>
  <action>
1. Verify operator has `CEREBRAS_API_KEY` and `GROQ_API_KEY` set in `.env.local` (these are pre-existing env vars per RESEARCH §5.6 — `server/config.ts:31-32` already validates them in `parseEnv()`, just defaulting to empty string when absent). If either is unset, STOP and surface to the operator as a blocker; do NOT proceed without both keys. Suggested operator check: `grep -E "^(CEREBRAS_API_KEY|GROQ_API_KEY)=" .env.local`.

2. Run `npm run probe:cerebras-groq`. Wall-clock ≈10 min (CONTEXT D-04: ~5 min per provider sequential).

3. Verify the output artifact at `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json` is well-formed:
   - `jq -e '.timestamp' .../probe-snapshot.json` succeeds.
   - `jq -e '.byProvider.cerebras.summary.decision' .../probe-snapshot.json` returns one of `"integrate"|"middle-bucket-defer"|"defer"`.
   - Same for `.byProvider.groq.summary.decision`.
   - `jq -e '.byProvider.cerebras.summary.latencyP50' .../probe-snapshot.json` returns an integer (RESEARCH §5.5 requirement for D-09 cascade ordering).
   - Same for `.byProvider.groq.summary.latencyP50`.

4. **Re-run determinism check (optional but recommended):** Run `npm run probe:cerebras-groq` a second time and diff. The artifact will differ on rate-limit-pct numbers (real free-tier variance), but the SHAPE must be byte-identical (sorted results, ISO-Z timestamp, integer latencies, present `summary.{rateLimitedCount, rateLimitedPct, latencyP50, latencyP95, decision}` blocks for both providers). If the second run produces a structurally different artifact (missing fields, unsorted results, non-integer latencies), STOP and fix the script before commit.

5. Commit the artifact along with the script + package.json from Tasks 1-2 in a single commit per CONTEXT D-24:
   - Commit message: `feat(34): probe cerebras + groq free-tier rate limits (D-03..D-06)`
   - Commit body: include a 2-line per-provider summary naming `rateLimitedPct` + `decision` for each (CONTEXT canonical refs §"validation cron's evidence-of-non-NIM-provider-fired row" pattern: terse, operator-readable). Example body:
     ```
     cerebras: 27/30 rate-limited (90%) → defer (latency p50=120ms p95=8400ms)
     groq: 4/30 rate-limited (13%) → integrate (latency p50=250ms p95=890ms)
     ```
   - Files included: `scripts/probe-cerebras-groq.ts`, `package.json`, `.planning/phases/34-.../probe-snapshot.json`.

6. **No branch switch in this plan.** Per CONTEXT D-26, the `feature/34-router-fallback-reintegration` branch cut happens at the START of Plan 34-02's execution (because Plan 34-02 begins adapter-restoration work and writes the first code-change commit). Plan 34-01's probe artifact + script + package.json sit on `main` as scaffold (mirrors CONTEXT.md + DISCUSSION-LOG.md + 34-RESEARCH.md commits already on main).
   </action>
   <verify>
   <automated>jq -e '.byProvider.cerebras.summary.decision' .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json && jq -e '.byProvider.groq.summary.decision' .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json && jq -e '.byProvider.cerebras.summary.latencyP50' .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json && jq -e '.byProvider.groq.summary.latencyP50' .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json</automated>
   </verify>
   <acceptance_criteria> - `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json` exists. - `jq -e '.byProvider.cerebras.summary.decision' .planning/phases/34-.../probe-snapshot.json` succeeds and returns one of `"integrate"`, `"middle-bucket-defer"`, `"defer"`. - `jq -e '.byProvider.groq.summary.decision' .planning/phases/34-.../probe-snapshot.json` same. - `jq -e '.byProvider.cerebras.summary.latencyP50 \| type == "number"' .planning/phases/34-.../probe-snapshot.json` returns `true`. - `jq -e '.byProvider.groq.summary.latencyP50 \| type == "number"' .planning/phases/34-.../probe-snapshot.json` returns `true`. - `jq -e '.byProvider.cerebras.results \| length == 30' .planning/phases/34-.../probe-snapshot.json` returns `true` (N=30 per CONTEXT D-04). - `jq -e '.byProvider.groq.results \| length == 30' .planning/phases/34-.../probe-snapshot.json` returns `true`. - `jq -e '.byProvider.cerebras.results \| [.[].attempt] == sort' .planning/phases/34-.../probe-snapshot.json` returns `true` (sorted per CONTEXT D-06). - `jq -e '.timestamp \| test("^[0-9]{4}-[0-9]{2}-[0-9]{2}T.*Z$")' .planning/phases/34-.../probe-snapshot.json` returns `true` (ISO-Z per CONTEXT D-06). - `git log --oneline -1 .planning/phases/34-.../probe-snapshot.json` shows commit message matching `feat(34): probe cerebras + groq free-tier rate limits (D-03..D-06)`. - `git log -1 --format=%B HEAD` body includes both `cerebras:` and `groq:` per-provider summary lines.
   </acceptance_criteria>
   <done>Probe ran end-to-end; combined snapshot artifact emitted with both providers' `decision` + latency-quantile fields; committed with per-provider summary in commit body per CONTEXT D-24. Plan 34-02 GATING DECISION is now mechanical from `.byProvider.{provider}.summary.decision` values.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                             |
| -------------------------- | ----------------------------------------------------------------------- |
| local dev → Cerebras API   | Outbound HTTPS to api.cerebras.ai; API key in `.env.local`              |
| local dev → Groq API       | Outbound HTTPS to api.groq.com; API key in `.env.local`                 |
| script → snapshot artifact | Local filesystem write to `.planning/phases/34-.../probe-snapshot.json` |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-01   | Information disclosure | Probe artifact JSON                        | mitigate    | `errorMessage` truncated to 200 chars per CONTEXT D-06; raw response bodies never serialized; `retry-after` header captured but no other headers (mirrors probe-openrouter.ts:174). |
| T-34-02   | Tampering              | Snapshot artifact partial-write corruption | mitigate    | Atomic temp-rename write pattern (`.tmp` → final via `renameSync`); CONTEXT D-06 requirement.                                                                                       |
| T-34-03   | Spoofing               | API key leakage via command line           | accept      | Probe invoked via `node --env-file=.env.local`; keys never appear in argv or process listing.                                                                                       |
| T-34-04   | Denial of Service      | Free-tier provider spam                    | mitigate    | N=30 per provider, 100ms intra-provider gap, sequential providers (CONTEXT D-04); ~10 min total wall-clock; one-shot, not scheduled.                                                |
| T-34-05   | Repudiation            | Probe outcome contested in future debate   | mitigate    | Byte-stable artifact (sorted, ISO-Z, integer latency) per CONTEXT D-06; artifact committed to git with per-provider summary in commit body per CONTEXT D-24 — auditable forever.    |

</threat_model>

<verification>
- `node --check scripts/probe-cerebras-groq.ts` exits 0 (syntax valid).
- `npx tsc --noEmit` exits 0 (no TS errors).
- `node -e "console.log(require('./package.json').scripts['probe:cerebras-groq'])"` prints the runner command.
- `jq -e '.byProvider.cerebras.summary.decision' .planning/phases/34-.../probe-snapshot.json` succeeds.
- `jq -e '.byProvider.groq.summary.decision' .planning/phases/34-.../probe-snapshot.json` succeeds.
- `jq -e '.byProvider.cerebras.summary.latencyP50' .planning/phases/34-.../probe-snapshot.json` returns an integer.
- `git log --oneline -1` shows the `feat(34): probe cerebras + groq free-tier rate limits (D-03..D-06)` commit on the current branch (`main` at this plan's execution per CONTEXT D-26).
</verification>

<success_criteria>

- Probe script `scripts/probe-cerebras-groq.ts` created with the correct provider models per RESEARCH §1 (Cerebras `qwen-3-235b-a22b-instruct-2507`, Groq `llama-3.3-70b-versatile`) and extended schema per RESEARCH §5.5 (`latencyP50` + `latencyP95`).
- `package.json` has `probe:cerebras-groq` script entry mirroring `probe:openrouter`.
- `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json` artifact committed with both providers' `summary.decision` populated; ROADMAP Phase 34 SC-1 satisfied; LLM-RELI-08 satisfied.
- Plan 34-02 has a mechanical per-provider integrate/defer signal to gate adapter restoration on.
  </success_criteria>

<output>
Create `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-01-SUMMARY.md` when done. Include:
- Per-provider `rateLimitedPct` and `decision` (copy from `probe-snapshot.json` `.byProvider.{provider}.summary`).
- Per-provider `latencyP50` and `latencyP95`.
- Stated gating implication for Plan 34-02: which provider(s) pass / defer.
- Confirmation that artifact is committed and Plan 34-02 unblocked.
</output>

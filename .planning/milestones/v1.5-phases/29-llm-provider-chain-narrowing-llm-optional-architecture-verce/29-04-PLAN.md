---
phase: 29
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - server/routes/events.ts
  - server/routes/operator-status.ts
  - server/config.ts
  - server/middleware/dashboardAuth.ts
  - src/components/ui/DevApiStatus.tsx
  - server/routes/__tests__/operator-status.test.ts
  - server/__tests__/routes/events.audit.test.ts
  - server/__tests__/routes/events.replayQuota.test.ts
autonomous: true
requirements:
  - SIMPLIFY-06
  - D-02
must_haves:
  truths:
    - '`POST /api/events/llm-pipeline` route is DELETED — hitting it returns 404 (not 410)'
    - '`setPipelineOverride` and `refreshPipelineOverride` and `getPipelineOverride` helpers are DELETED from server/config.ts'
    - '`events:llm-pipeline-override` Redis key has no writers in the codebase (existing keys will TTL-expire within 7d)'
    - 'operator-status response no longer contains `pinTtl` field; DevApiStatus pinTtl consumer is removed'
    - '`server/middleware/dashboardAuth.ts` comment block listing protected routes no longer references `/api/events/llm-pipeline`'
    - 'POST /llm-pipeline 404 behavior is asserted by a new test in events.test.ts (added in Plan 09)'
  artifacts:
    - path: 'server/routes/events.ts'
      provides: 'override endpoint deleted (route registration + handler body + Redis key constant)'
      contains: '// override endpoint removed Phase 29 D-02'
    - path: 'server/config.ts'
      provides: 'pipeline override helpers deleted'
      contains: 'isLLMConfigured'
    - path: 'server/routes/operator-status.ts'
      provides: 'pinTtl block removed from aggregator response'
      contains: 'audit24h'
  key_links:
    - from: 'server/routes/events.ts POST /llm-pipeline'
      to: 'server/config.ts setPipelineOverride'
      via: 'shared deletion (both removed in this commit so the route handler is not orphaned mid-edit)'
      pattern: 'atomic deletion'
---

<objective>
Delete the `POST /api/events/llm-pipeline` override route + the `setPipelineOverride` / `refreshPipelineOverride` / `getPipelineOverride` helpers in `server/config.ts` + the `pinTtl` block in `/api/operator-status`. This is D-02 part A — the surface that turns operator pin-pipeline into a no-op. UI deletion (DevApiStatus Pin-to-v1/v2 buttons + Topbar PipelineVersionPill) lands in Plan 08 — sequenced AFTER the route deletion so an intermediate state of "buttons present, route 404" doesn't ship.

The DevApiStatus `pinTtl` consumer (which reads from `/api/operator-status`) is also updated in this plan so the operator-status schema change doesn't cause runtime errors in the dashboard. The full pin-button UI surface deletion stays bundled in Plan 08.

Output: 4 server-side deletions + 2 test cleanups + a minor DevApiStatus field-removal (NOT the full button row deletion — that's Plan 08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md
@.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-RESEARCH.md
@server/routes/events.ts
@server/routes/operator-status.ts
@server/config.ts
@server/middleware/dashboardAuth.ts
@server/routes/__tests__/operator-status.test.ts

<interfaces>
From RESEARCH.md Question 2 + Pitfall 5:

| File                                                 | Lines                                                                                                                                                                                                                                                           | Action                                                                                             |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `server/routes/events.ts`                            | L12 (`setPipelineOverride` import), L320-353 (`PIPELINE_OVERRIDE_KEY` const + `refreshPipelineOverride` body), L575-651 (the `eventsRouter.post('/llm-pipeline', dashboardAuth, ...)` handler), L368 + L662 (refresh-pipeline-override calls in other handlers) | DELETE all listed                                                                                  |
| `server/config.ts`                                   | L319-340 (`pipelineOverride` module var, `setPipelineOverride`, `refreshPipelineOverride`, `getPipelineOverride`)                                                                                                                                               | DELETE all listed; KEEP `isPipelineV2`, `isPipelineV3`, `getPipelineVersion` for Plan 06 to remove |
| `server/routes/operator-status.ts`                   | L121, L128, L134-141 (pinTtl read + response field)                                                                                                                                                                                                             | DELETE the pinTtl branch; update `OperatorStatusResponse` schema                                   |
| `server/middleware/dashboardAuth.ts`                 | L8-12 comment block listing protected routes — remove `POST /api/events/llm-pipeline` line                                                                                                                                                                      | UPDATE comment                                                                                     |
| `src/components/ui/DevApiStatus.tsx`                 | grep `pinTtl` references; remove the pinTtl render block (NOT the pin buttons — Plan 08 owns those)                                                                                                                                                             | UPDATE                                                                                             |
| `server/routes/__tests__/operator-status.test.ts`    | L98 (mock branch for `events:llm-pipeline-override` key)                                                                                                                                                                                                        | DELETE the mock branch                                                                             |
| `server/__tests__/routes/events.audit.test.ts`       | L242-243 (setPipelineOverride cleanup)                                                                                                                                                                                                                          | DELETE cleanup block                                                                               |
| `server/__tests__/routes/events.replayQuota.test.ts` | L312-313 (setPipelineOverride cleanup)                                                                                                                                                                                                                          | DELETE cleanup block                                                                               |

</interfaces>
</context>

<threat_model>

- T-29-04-01 (BLOCK, severity: high): The route handler at events.ts:575-651 imports types or helpers that other handlers need. Mitigation: Task 2 reads the file end-to-end before editing; confirm import-graph.
- T-29-04-02 (severity: medium): A test still mocks `setPipelineOverride` and now fails with "module has no export" after Plan 04's config.ts edit. Mitigation: Task 6 updates the 3 known test files. Plan 06 catches any remaining mocks when it deletes `isPipelineV2` (broader sweep).
- T-29-04-03 (severity: medium): Stale `events:llm-pipeline-override` Redis key sits in production for up to 7d after deploy. Mitigation: per CONTEXT D-02 spec — key naturally TTL-expires; operator can `redis-cli del` manually if observed. NOT a code-path concern.
- T-29-04-04 (severity: low): DevApiStatus dashboard renders `pinTtl` as undefined until DevApiStatus.tsx pinTtl render block is removed. Mitigation: bundle the dashboard pinTtl removal in this plan (small) rather than carrying the mismatch to Plan 08.
  </threat_model>

<tasks>
<task id="29-04-01" name="Read load-bearing files">
  <read_first>
    - server/routes/events.ts:1-100, 320-360, 570-680 (Pitfall 1 bridge + override route + post-handler calls)
    - server/routes/operator-status.ts (full file, ~200 lines)
    - server/config.ts:300-367 (pipeline-override helpers + isPipelineV2 etc.)
    - server/middleware/dashboardAuth.ts (full file, 67 lines)
    - src/components/ui/DevApiStatus.tsx — search for `pinTtl` occurrences
  </read_first>
  <action>
    Confirm: (a) which functions in events.ts depend on `refreshPipelineOverride` (besides the route itself) — likely the GET /api/events handler at L368 + the cron force-trigger at L662. (b) The `OperatorStatusResponse` schema definition in operator-status.ts. (c) Whether DevApiStatus reads `pinTtl` as `pinTtl?: number` or always present.
  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -nP 'refreshPipelineOverride|setPipelineOverride|pinTtl|PIPELINE_OVERRIDE_KEY' server/routes/events.ts server/config.ts server/routes/operator-status.ts src/components/ui/DevApiStatus.tsx 2>&1 | head -30</automated>
  </acceptance_criteria>
</task>

<task id="29-04-02" name="Delete POST /llm-pipeline route + PIPELINE_OVERRIDE_KEY + refresh calls">
  <read_first>
    - server/routes/events.ts (per task 1)
  </read_first>
  <action>
    Edit `server/routes/events.ts`:

    1. L12 (or wherever): DELETE the import `import { setPipelineOverride, refreshPipelineOverride, getPipelineOverride } from '../config.js';` — keep `isLLMConfigured` and other imports.
    2. L320-353: DELETE the `const PIPELINE_OVERRIDE_KEY = 'events:llm-pipeline-override'` constant + the `refreshPipelineOverride()` body if it's defined here (per RESEARCH it's defined in config.ts, but events.ts may have a wrapper — check before deleting).
    3. L575-651: DELETE the entire `eventsRouter.post('/llm-pipeline', dashboardAuth, ...handler...)` block.
    4. L368, L662: DELETE the `await refreshPipelineOverride()` calls in the GET /api/events handler and any other call sites. Replace with a comment `// pipeline override removed Phase 29 D-02` for one cycle, OR delete the line outright (cleaner).

    After this task, no symbol named `refreshPipelineOverride`, `setPipelineOverride`, `PIPELINE_OVERRIDE_KEY`, or the literal string `events:llm-pipeline-override` remains in events.ts.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -cP 'refreshPipelineOverride|setPipelineOverride|PIPELINE_OVERRIDE_KEY|events:llm-pipeline-override' server/routes/events.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "eventsRouter.post.*llm-pipeline" server/routes/events.ts | grep -q '^0$'</automated>
  </acceptance_criteria>
</task>

<task id="29-04-03" name="Delete pipeline-override helpers from config.ts">
  <read_first>
    - server/config.ts:300-367
  </read_first>
  <action>
    Edit `server/config.ts`. DELETE:
    - `pipelineOverride` module-level variable (the in-memory override state)
    - `setPipelineOverride(version: 'v1' | 'v2' | 'v3' | null)` function
    - `refreshPipelineOverride()` function (the one that reads Redis)
    - `getPipelineOverride()` function (returns current override or null)

    KEEP for Plan 06 to remove:
    - `isPipelineV2()`, `isPipelineV3()`, `getPipelineVersion()` — those collapse to constants in Plan 06.

    KEEP unchanged: `isLLMConfigured` was already updated in Plan 03 (cascade narrowing); should NOT need re-editing here unless Plan 03 left it inconsistent.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -cP 'setPipelineOverride|refreshPipelineOverride|getPipelineOverride|pipelineOverride[^V]' server/config.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -cP 'isPipelineV2|isPipelineV3|getPipelineVersion' server/config.ts | grep -qE '^[1-9]'</automated>
  </acceptance_criteria>
</task>

<task id="29-04-04" name="Delete pinTtl block from operator-status.ts + schema">
  <read_first>
    - server/routes/operator-status.ts (full)
  </read_first>
  <action>
    Edit `server/routes/operator-status.ts`. Delete:
    - L121 + L128: the Redis read for `events:llm-pipeline-override` (key naturally TTL-expires; no need to surface it)
    - L134-141: the `pinTtl` field in the response object
    - Any schema/type definition for `OperatorStatusResponse.pinTtl` (search for the type alias and remove the field)

    Preserve other fields in the response (`audit24h`, `byBearer`, `advEval`).

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -cP 'pinTtl|events:llm-pipeline-override' server/routes/operator-status.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "audit24h" server/routes/operator-status.ts | grep -qE '^[1-9]'</automated>
  </acceptance_criteria>
</task>

<task id="29-04-05" name="Update dashboardAuth.ts comment block">
  <read_first>
    - server/middleware/dashboardAuth.ts (full file, 67 lines)
  </read_first>
  <action>
    Edit `server/middleware/dashboardAuth.ts`. In the comment block at L8-12 listing protected routes, REMOVE the line referencing `POST /api/events/llm-pipeline`. Preserve other route mentions (`/api/events/llm-replay/:groupKey`, `/api/dashboard/auth-check`).

    This is a comment-only change but keeps the middleware's documented contract accurate.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "POST /api/events/llm-pipeline" server/middleware/dashboardAuth.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "/api/events/llm-replay" server/middleware/dashboardAuth.ts | grep -qE '^[1-9]'</automated>
  </acceptance_criteria>
</task>

<task id="29-04-06" name="Remove pinTtl consumer from DevApiStatus.tsx (lightweight — full button removal in Plan 08)">
  <read_first>
    - src/components/ui/DevApiStatus.tsx — grep `pinTtl` to find render block
  </read_first>
  <action>
    Edit `src/components/ui/DevApiStatus.tsx`. Search for `pinTtl` occurrences. Two things to remove:

    1. The TypeScript type/interface field for `pinTtl` (if exists, in the OperatorStatus response shape).
    2. The render block that displays "Pin TTL: Nh" (likely in the Operator Actions block of the API Health tab).

    DO NOT touch the Pin-to-v1/v2/v3 buttons or confirm modal — those are deleted in Plan 08.

    After this task: `grep pinTtl src/` returns 0 matches.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -rc "pinTtl" src/ | awk -F: '$2 != "0"' | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  </acceptance_criteria>
</task>

<task id="29-04-07" name="Update operator-status + events.audit + events.replayQuota tests">
  <read_first>
    - server/routes/__tests__/operator-status.test.ts (L98 area)
    - server/__tests__/routes/events.audit.test.ts (L242-243)
    - server/__tests__/routes/events.replayQuota.test.ts (L312-313)
  </read_first>
  <action>
    Three test edits:

    1. `server/routes/__tests__/operator-status.test.ts`:
       - L98: DELETE the mock branch for `events:llm-pipeline-override` Redis key
       - Any assertion about `pinTtl` field in the response → DELETE

    2. `server/__tests__/routes/events.audit.test.ts`:
       - L242-243: DELETE the `setPipelineOverride(null)` cleanup block (the helper is gone)
       - L119: leave the `isPipelineV2: vi.fn().mockReturnValue(false)` mock for now — Plan 06 sweeps these

    3. `server/__tests__/routes/events.replayQuota.test.ts`:
       - L312-313: DELETE the `setPipelineOverride(null)` cleanup block
       - L171: leave the v2 mock for Plan 06 sweep

    Acceptance: each affected test file passes `npx vitest run <file>` exits 0.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "setPipelineOverride" server/__tests__/routes/events.audit.test.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "setPipelineOverride" server/__tests__/routes/events.replayQuota.test.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "pinTtl" server/routes/__tests__/operator-status.test.ts | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/routes/__tests__/operator-status.test.ts 2>&1 | tail -5 | grep -qE 'passed'</automated>
  </acceptance_criteria>
</task>

<task id="29-04-08" name="Run tsc + full server vitest + commit">
  <read_first>
    - (no specific file — full suite)
  </read_first>
  <action>
    1. `npx tsc --noEmit` — must exit 0
    2. `npx vitest run server/` — must pass (some tests may fail due to remaining `setPipelineOverride` mocks in test files Plan 06 owns; if so, document the failure list and proceed — Plan 06 sweeps them)
    3. Commit:

    ```bash
    git add server/routes/events.ts server/routes/operator-status.ts server/config.ts \
            server/middleware/dashboardAuth.ts src/components/ui/DevApiStatus.tsx \
            server/routes/__tests__/operator-status.test.ts \
            server/__tests__/routes/events.audit.test.ts \
            server/__tests__/routes/events.replayQuota.test.ts
    git commit -m "feat(29): delete POST /llm-pipeline override route + helpers + pinTtl (D-02 part A)

    Removes the operator pin-pipeline surface:
    - POST /api/events/llm-pipeline route + handler body deleted
    - PIPELINE_OVERRIDE_KEY constant deleted
    - server/config.ts: setPipelineOverride, refreshPipelineOverride,
      getPipelineOverride helpers deleted (isPipelineV2/V3 + getPipelineVersion
      stay until Plan 06)
    - /api/operator-status: pinTtl field + Redis read removed from aggregator
    - DevApiStatus pinTtl render block removed (Pin-to-v1/v2/v3 BUTTON row
      deletion stays scoped to Plan 08)
    - dashboardAuth.ts comment listing /api/events/llm-pipeline removed
    - 3 test files updated to remove setPipelineOverride / pinTtl mocks

    Pitfall 5 fix: server/routes/operator-status.ts pinTtl block read
    events:llm-pipeline-override TTL — now reads nothing because writes
    are gone. Existing keys TTL-expire within 7d.

    Atomic-commit boundary verified by grep: 0 references to
    refreshPipelineOverride / setPipelineOverride / PIPELINE_OVERRIDE_KEY /
    pinTtl / events:llm-pipeline-override across server/ + src/.

    Requirements: SIMPLIFY-06, D-02."
    ```

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit 2>&1 | grep -cE 'error TS' | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && git log -1 --format='%s' | grep -q 'delete POST /llm-pipeline override route'</automated>
  </acceptance_criteria>
</task>
</tasks>

<verification>
  <automated>cd /Users/zackmaz/Desktop/my_world && grep -rnP 'refreshPipelineOverride|setPipelineOverride|getPipelineOverride|PIPELINE_OVERRIDE_KEY|pinTtl|events:llm-pipeline-override' server/ src/ 2>/dev/null | grep -v __tests__ | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  <automated>cd /Users/zackmaz/Desktop/my_world && grep -rn "eventsRouter.post.*llm-pipeline" server/routes/ | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit 2>&1 | grep -cE 'error TS' | grep -q '^0$'</automated>
</verification>

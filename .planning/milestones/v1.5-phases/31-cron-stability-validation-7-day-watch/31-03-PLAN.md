---
phase: 31-cron-stability-validation-7-day-watch
plan: 03
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
files_modified:
  - .planning/phases/31-cron-stability-validation-7-day-watch/watch-log.json
autonomous: false
requirements:
  - LLM-RELI-06
requirements_addressed:
  - LLM-RELI-06

must_haves:
  truths:
    - 'PR for Plans 31-01 + 31-02 is merged into main AND a Vercel production deploy has completed against that merged commit'
    - "Operator runs `curl -H 'Authorization: Bearer $DASHBOARD_PASSWORD' 'https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true'` ONCE and the run completes without errors"
    - 'After the force-trigger, `npm run analyze:llm-run` shows `evalScore.total > 0` (Vercel includeFiles bundling verified live)'
    - 'After the force-trigger, `batchCount ≤ 149` (≥30% reduction from Phase 30 baseline of 213; diff-filter prefix-add verified live)'
    - "After the force-trigger, count of `callHistory[i].skipReason === 'breaker'` is 0 (no breaker trip during the validation run)"
    - "Operator runs `npm run watch:snapshot -- --force --notes='D-02 prep-validation force-trigger'` ONCE and one row appended to `watch-log.json` with `natural: false`"
    - 'Force-triggered row does NOT advance the 7-consecutive natural counter (`consecutivePassCount` returns 0)'
  artifacts:
    - path: .planning/phases/31-cron-stability-validation-7-day-watch/watch-log.json
      provides: 'One D-02 validation row with natural: false'
      contains: 'D-02 prep-validation force-trigger'
  key_links:
    - from: 'force-trigger curl'
      to: '/api/cron/refresh-events?force=true'
      via: 'Bearer DASHBOARD_PASSWORD'
      pattern: 'Authorization: Bearer'
    - from: 'validation row'
      to: 'watch-log.json rows[]'
      via: 'natural: false'
      pattern: '"natural":\s*false'
---

<objective>
Validate the Plan 01 prep fixes in production by running ONE operator-driven force-trigger of `/api/cron/refresh-events?force=true`, observing the three D-02 gates, and capturing the result as a single `natural: false` row in `watch-log.json`. Pass → Day 1 is the next natural 04:00 UTC cron (Plan 04 starts). Fail → fix-forward.

Purpose: The 7-day watch's signal depends on (a) eval scoring being live, (b) cron not double-processing cached groups under NIM throttle. The force-trigger is the canonical mechanism to demonstrate BOTH live in prod, against real NIM + the real Pro 800s ceiling, before the natural-cron watch anchor begins. Per CONTEXT D-02: "the executing agent (or operator) runs **one** force-trigger" with PASS criteria (a), (b), (c).

Output: One commit `docs(31): watch-log day 0 — D-02 prep-validation force-trigger <PASS|FAIL>` on `feature/31-cron-stability-validation` (or a follow-up branch if Plans 01-02 PR already merged — see plan body for branching). Row sits in `watch-log.json` with `natural: false` so it does NOT contribute to the 7-consecutive count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-cron-stability-validation-7-day-watch/31-CONTEXT.md
@.planning/phases/31-cron-stability-validation-7-day-watch/31-RESEARCH.md
@.planning/phases/31-cron-stability-validation-7-day-watch/31-PATTERNS.md
@.planning/phases/31-cron-stability-validation-7-day-watch/31-VALIDATION.md
@.planning/phases/31-cron-stability-validation-7-day-watch/31-01-SUMMARY.md
@.planning/phases/31-cron-stability-validation-7-day-watch/31-02-SUMMARY.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-2-throttle-snapshot.json
@CLAUDE.md

<interfaces>
<!-- D-02 gate values (CRITICAL — these are the canonical baselines, NOT the CONTEXT-named OR-pulse snapshot) -->

D-02 PASS criteria (all three must hold):
| Metric | Pre-fix baseline | Post-fix PASS gate | Source |
|--------|------------------|--------------------|--------|
| `evalScore.total` | 0 (eval-bundle bug) | > 0 | `.planning/eval/ground-truth-events.json` has 50 entries |
| `batchCount` | 213 | ≤ 149 (≥30% reduction) | Phase 30 run-1 + run-2 snapshots (both 213) |
| `breakerTrips` (skipReason === 'breaker' count) | n/a | 0 | CONTEXT D-02 criterion (c) |

**IMPORTANT — the CONTEXT D-02 reference to `30.1-or-pulse-snapshot.json` is INCORRECT.** That file is the OpenRouter probe, NOT a cron baseline. The actual `batchCount: 213` baseline comes from `run-1-throttle-snapshot.json` and `run-2-throttle-snapshot.json` (both Phase 30). 31-RESEARCH.md §"Vector 4" confirms this verbatim.

`scripts/analyze-llm-run.ts` reads `events:llm-summary:v3` and prints `evalScore`, `batchCount`, `watchdogTimeoutCount`, and per-bucket call counts. The breaker-trip count is derivable from `callHistory.filter(c => c.skipReason === 'breaker').length`.

Force-trigger endpoint:

```bash
curl -i -H "Authorization: Bearer $DASHBOARD_PASSWORD" \
  "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true"
```

Per CLAUDE.md: Bearer-gated, force-true bypasses the 15-min cooldown. Wall-clock ~10 min at 800s `maxDuration` ceiling.
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                      |
| ----------------------------------- | -------------------------------------------------------------------------------- |
| Operator → /api/cron/refresh-events | Bearer-gated force-trigger; bypasses cooldown                                    |
| Force-trigger → NIM                 | Real production token spend (~200-400k tokens pre-fix; ~30-150k tokens post-fix) |
| Force-trigger → Redis               | Overwrites `events:llm:v3` + `events:llm-summary:v3`                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                       |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-07   | DoS (self-imposed)     | NIM token spend on force-trigger                | mitigate    | Run during attended hours; do not chain multiple force-triggers; `LLM_TOKEN_BUDGET_HARD_CAP` self-pauses extraction if hit (Risk 1 in 31-RESEARCH.md) |
| T-31-08   | Information disclosure | Bearer in shell history                         | accept      | Per CLAUDE.md operator workflow; `DASHBOARD_PASSWORD` env var used (not literal)                                                                      |
| T-31-09   | Tampering              | Validation row pollution of 7-consecutive count | mitigate    | `natural: false` flag; `consecutivePassCount` excludes non-natural rows per D-09                                                                      |

</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator merges PR + waits for Vercel deploy</name>
  <read_first>
    - .planning/phases/31-cron-stability-validation-7-day-watch/31-01-SUMMARY.md (commit SHAs)
    - .planning/phases/31-cron-stability-validation-7-day-watch/31-02-SUMMARY.md (commit SHAs + PR URL)
    - 31-CONTEXT.md §"Carrying forward" (branch convention)
    - 31-CONTEXT.md §D-02 (Day-1 anchor + failure-response policy)
  </read_first>
  <what-built>Plans 01 + 02 produced a PR with 7 commits on `feature/31-cron-stability-validation`. Plan 03 cannot run until the PR is merged into main AND Vercel deploys against the merged commit (so the prep fixes are LIVE in prod).</what-built>
  <how-to-verify>
    1. Open the PR URL captured in `31-02-SUMMARY.md`.
    2. Verify CI is green (all three audit branches: typecheck, lint, vitest).
    3. Squash-merge the PR into main (matches Phase 30/30.1 PR #20/#21/#22 convention).
    4. Verify Vercel auto-deployed the merged commit: visit `https://vercel.com/<team>/onthegrid.icm/deployments` (or `vercel ls --prod` from local CLI), confirm the latest production deployment SHA matches the squash-merge commit on main.
    5. Sanity check the deploy is live: `curl -s https://otg-iran-monitor.vercel.app/api/health | jq '.endpoints.llmEvents.status'` returns a string ('healthy' / 'stale' / 'unknown' — any of these is fine; we just need the route to respond).
    6. Confirm the `feature/31-cron-stability-validation` branch is deleted from origin (cleanup after squash).
    7. Create a NEW branch from main for Plan 03's commit: `git checkout main && git pull && git checkout -b feature/31-day-0-validation`. (Day-0 validation row commits on this branch; same branch carries forward into Plan 04 for daily snapshot commits — OR a new branch per day if operator prefers; planner default is single branch.)
  </how-to-verify>
  <resume-signal>Type "merged-and-deployed" with the squash-merge commit SHA, OR describe any deploy/CI blockers.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs D-02 force-trigger + records the row</name>
  <read_first>
    - 31-CONTEXT.md §D-02 (force-trigger PASS criteria)
    - 31-CONTEXT.md §D-09 (natural: false flag for force-triggered rows)
    - 31-RESEARCH.md §"Vector 4" lines 260-288 (baseline math + verification path)
    - 31-RESEARCH.md §"Risk 1" lines 667-677 (NIM token spend cap)
    - scripts/snapshot-cron-watch.ts (verify `--force` and `--notes` argv parsing landed in Plan 02)
    - scripts/analyze-llm-run.ts (verify analyzer is current after Plan 01 prep #3 docstring update)
  </read_first>
  <what-built>Plan 01 prep fixes are LIVE in prod. The validation force-trigger now runs against the FIXED code path. Pre-fix this would have processed 213 batches; post-fix it should process ≤149 (≥30% reduction). Eval scoring is now live (fixtures bundled).</what-built>
  <how-to-verify>
    **Step 1 — Confirm prep-fix deploy is current.** Run:
    ```bash
    curl -s https://otg-iran-monitor.vercel.app/api/health | jq '.endpoints.llmEvents'
    ```
    Confirm the route responds.

    **Step 2 — Choose attended hours.** Per Risk 1 in 31-RESEARCH.md: do this during operator-watchful hours (do NOT pre-schedule and walk away). The force-trigger runs for ~10 min at 800s `maxDuration` ceiling. NIM token spend: pre-fix ~200-400k; post-fix expected ~30-150k (smaller because diff-filter fix means fewer batches reach LLM).

    **Step 3 — Fire the force-trigger ONCE.** From a shell with `DASHBOARD_PASSWORD` exported:
    ```bash
    curl -i -H "Authorization: Bearer $DASHBOARD_PASSWORD" \
      "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true"
    ```
    Expected: HTTP 200 + JSON `{ ok: true, ... }` immediately (the body returns fast; the actual extraction continues server-side via `safeWaitUntil`). DO NOT re-fire. Wait ~10-12 minutes wall-clock for the run to complete.

    **Step 4 — Verify D-02 PASS criteria via analyzer.** Run:
    ```bash
    npm run analyze:llm-run
    ```
    The analyzer reads `events:llm-summary:v3` and prints a JSON snapshot. Inspect:

    - **(a) `evalScore.total > 0`** — Pre-fix: 0. Post-fix expected: 50 (matches `.planning/eval/ground-truth-events.json` entry count). If 0: includeFiles bundling didn't work — fall back to the A1 explicit-paths form in `vercel.json` (`includeFiles: [".planning/eval/ground-truth-events.json", ".planning/eval/adversarial-injections.json"]`), commit, redeploy, re-run from Step 3.

    - **(b) `batchCount ≤ 149`** — Pre-fix: 213 (Phase 30 baselines). Post-fix PASS gate: ≤149 (≥30% reduction per 31-RESEARCH.md §Vector 4 conservative floor). If >149: the diff-filter fix did not take effect — check `server/lib/llmExtractionPipeline.ts` deployed bundle (read the deployed function via `vercel logs --since=10m` or inspect via /api/events/llm-status) — there may be a deploy caching issue; force redeploy.

    - **(c) `breakerTrips === 0`** — Count via `callHistory.filter(c => c.skipReason === 'breaker').length`. If >0: NIM was under load during the run; this is NOT an automatic FAIL — note in row notes as "breaker trip during validation, retry needed" and re-run during quieter hours. If repeated breaker trips occur on 3+ force-triggers, this is evidence the watch cannot pass without limiter rework — escalate per D-05 (Plan 05 covers the escalation gate).

    **Step 5 — Capture as `natural: false` row.** Run:
    ```bash
    npm run watch:snapshot -- --force --notes="D-02 prep-validation force-trigger (eval=X, batchCount=Y, breakerTrips=Z)"
    ```
    Replace X/Y/Z with the analyzer values from Step 4. The `--force` flag sets `natural: false` so this row does NOT advance the 7-consecutive counter (D-09).

    **Step 6 — Verify the row.** Run:
    ```bash
    node -e "const r=JSON.parse(require('fs').readFileSync('.planning/phases/31-cron-stability-validation-7-day-watch/watch-log.json','utf8')); const last=r.rows[r.rows.length-1]; console.log(JSON.stringify({natural:last.natural, result:last.result, batchCount:last.batchCount, evalAt20km:last.eval.at20km, breakerTrips:last.breakerTrips, notes:last.notes}, null, 2))"
    ```
    Confirm: `natural: false`, `result: 'PASS'`, `batchCount <= 149`, `eval.at20km` is a number (not null), `breakerTrips: 0`.

    **Step 7 — Commit.** ON THE `feature/31-day-0-validation` BRANCH:
    ```bash
    git add .planning/phases/31-cron-stability-validation-7-day-watch/watch-log.json
    git commit -m "docs(31): watch-log day 0 — D-02 prep-validation force-trigger PASS"
    git push -u origin feature/31-day-0-validation
    ```
    (Body of the commit message should include the analyzer's full JSON snapshot for the run — copy-paste from Step 4.)

    Open a PR if convenient; otherwise the branch carries Day 0 + Plan 04's Days 1-7 commits and opens as a single PR at Plan 05 close.

    **If ANY criterion fails:** Do NOT commit. Document the failure in a comment on the prior PR, identify the cause (eval bundle, diff-filter, breaker, or other), fix-forward per CONTEXT D-04 ("the failed force-trigger is logged in the watch artifact but does NOT count as a watch day"). Re-fire after the fix lands.

  </how-to-verify>
  <resume-signal>Type "validation-pass" with the analyzer snapshot pasted, OR "validation-fail" with the failing criterion and proposed fix. If PASS, also state "Day 1 begins at next natural 04:00 UTC cron".</resume-signal>
</task>

</tasks>

<verification>
- Plans 01 + 02 PR merged into main; Vercel production deploy live against merged commit
- D-02 PASS criteria all hold: `evalScore.total > 0` AND `batchCount ≤ 149` AND `breakerTrips === 0`
- One row in `watch-log.json` with `natural: false`, `result: 'PASS'`, `notes: "D-02 prep-validation force-trigger ..."`
- `consecutivePassCount(rows)` returns 0 (the validation row does NOT count as a watch day per D-09)
- Day 1 = next natural 04:00 UTC cron tick (Plan 04 starts there)
</verification>

<success_criteria>

- D-02 force-trigger demonstrates prep fixes are live in prod (eval scoring + diff-filter reduction)
- `watch-log.json` carries one validation row with `natural: false`
- Operator unblocked to begin Plan 04 (Day-1 anchor)
  </success_criteria>

<output>
After completion, create `.planning/phases/31-cron-stability-validation-7-day-watch/31-03-SUMMARY.md` capturing: (a) the analyzer JSON snapshot post-force-trigger, (b) the squash-merge commit SHA of Plans 01+02 PR, (c) the Day 0 commit SHA, (d) the wall-clock duration of the force-trigger run, (e) confirmation of all three D-02 criteria (PASS/FAIL per criterion), (f) the planned Day-1 date (next natural 04:00 UTC after Day 0).
</output>

---
phase: 36-public-docs-sweep-openapi-additions
plan: 06
type: execute
wave: 3
depends_on: ['36-01', '36-02', '36-03', '36-04', '36-05']
files_modified:
  - public/robots.txt
  - index.html
  - .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: []
tags:
  [
    phase-close,
    summary,
    audit-table,
    framing-gap,
    robots-verify,
    three-gate-verification,
    roadmap,
    requirements,
    state,
  ]
must_haves:
  truths:
    - 'D-22 robots.txt + meta tags verify-only pass: read `public/robots.txt` (confirm `/api/*` + `/health` still disallowed per README line 372 claim); read `index.html` `<meta>` block (confirm title / description / og:title / og:description still describe Iran Monitor accurately). Edit ONLY if drift found.'
    - 'D-23 docs/ tidiness verify: confirm `docs/brainstorms/` + `docs/superpowers/` still unlinked from README. Note "identified but intentionally not touched" in SUMMARY (historical waymarkers per Phase 35 D-09 principle).'
    - 'D-24 three mechanical verification gates run and all green:
      (a) `npx vitest run` — full suite, including the new `openapi-lint.test.ts` from Plan 36-05;
      (b) `npx @redocly/cli lint server/openapi.yaml` — no spec errors;
      (c) `npx markdown-link-check` across README + `docs/architecture/*.md` + `docs/architecture/ontology/*.md` + `docs/runbook.md` + `docs/degradation.md` + `docs/adr/*.md` — no broken cross-references created by Wave 1 / Wave 2 sweep edits.'
    - 'D-04 framing-gap callouts captured in SUMMARY.md: (a) ROADMAP.md "NIM + OpenRouter narrowed cascade" wording predates Phase 30.1 OR-dormant + Phase 34 Cerebras/Groq-deferred — public docs say NIM-only at runtime; planning text not retroactively edited. (b) ROADMAP.md / PROJECT.md "10 markdown files (21 Mermaid diagrams)" — actual is 12 markdown files / 21 Mermaid diagrams (NOT 22 as D-11 hypothesized; Plan 36-02 audit confirms 21). (c) ROADMAP.md success criterion #4 "v3 → v2 → v1 → raw GDELT fallback chain" — actual is "v3 → raw GDELT" (v1/v2 deleted Phase 29); public degradation.md describes shipped reality.'
    - 'D-12 architecture audit table absorbed from `36-02-AUDIT.md` accumulator: SUMMARY.md contains the full per-file (12 rows) + per-Mermaid-block (21 rows) table verbatim.'
    - 'SUMMARY.md uses closing-table format (Phase 35 SUMMARY precedent — Claude-Discretion 7) with one row per D-N decision (28 decisions) + audit-table rows + framing-gap callout rows.'
    - 'ROADMAP.md Phase 36 entry updated to checked-complete; plan list (6 plans 36-01..36-06) filled in.'
    - 'REQUIREMENTS.md traceability table flips DOCS-PUB-01, DOCS-PUB-02, DOCS-PUB-03, DOCS-PUB-05, DOCS-API-01..07 from Pending → Complete. **DOES NOT flip DOCS-PUB-04 (ADR-0010 milestone-close sub-block) — that is Phase 37 territory.**'
    - 'STATE.md last_activity updated; phase 36 marked complete.'
    - 'Branch `feature/36-public-docs-sweep-openapi-additions` ready for merge to main.'
    - 'Atomic per-decision commits: D-22 verify (small or none if clean), D-23 verify (none — note-only in SUMMARY), D-24 verification run (one chore commit recording outcomes), SUMMARY authoring (one docs commit), ROADMAP / REQUIREMENTS / STATE flips (one docs commit each OR rollup).'
  artifacts:
    - path: '.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md'
      provides: 'Phase 36 close-out summary: per-D-N table + architecture audit table (absorbed from 36-02-AUDIT.md) + 3 framing-gap callouts + 3-gate verification results + commit log'
      contains: 'Phase 36 Close-Out Summary'
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 36 entry checked-complete + plan list filled in'
      contains: '### Phase 36: Public Docs Sweep + OpenAPI Additions'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'DOCS-PUB-01/02/03/05 + DOCS-API-01..07 flipped Pending → Complete'
      contains: 'DOCS-API-07'
    - path: '.planning/STATE.md'
      provides: 'phase 36 marked complete; last_activity updated'
      contains: 'Phase 36'
  key_links:
    - from: '.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md'
      to: '.planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md'
      via: 'audit-table absorption (verbatim copy of architecture-files + mermaid-diagrams tables)'
      pattern: 'Architecture Markdown Files|Mermaid Diagrams'
    - from: '.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md'
      to: '.planning/REQUIREMENTS.md DOCS-* requirements'
      via: 'per-D-N table cites which D-N satisfies which DOCS-* requirement'
      pattern: 'DOCS-PUB-0[1-5]|DOCS-API-0[1-7]'
---

<objective>
Close Phase 36. Run the 3-gate mechanical verification (vitest + Redocly lint + markdown-link-check). Verify (and minimally edit if drift) robots.txt + index.html meta tags. Note docs/ tidiness state. Author the SUMMARY.md with the per-D-N table, the architecture audit-table absorbed from Plan 36-02's accumulator, and the 3 framing-gap callouts per D-04. Flip ROADMAP / REQUIREMENTS / STATE markers.

Purpose: This plan IS the v1.5 public-docs-and-spec track closing artifact. The 3 mechanical gates are the load-bearing primitives that prove the sweep was effective (broken cross-links would have rotted public docs again). SUMMARY.md is the reviewer's trust artifact — one place to see what was looked at and what the outcome was. The ROADMAP / REQUIREMENTS / STATE flips clear Phase 36 from the active-work board so Phase 37 (milestone close + acceptance gate observation) can proceed.

Output: ~5-7 atomic commits per D-26 / D-27: (1) D-22 verify-only (may be zero commits if clean), (2) D-24 chore commit recording verification outcomes, (3) docs commit for SUMMARY.md, (4) docs commit for ROADMAP flip, (5) docs commit for REQUIREMENTS flips, (6) docs commit for STATE flip. Could roll up SUMMARY + tracking flips into a single commit if cohesive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-01-PLAN.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-02-PLAN.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-03-PLAN.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-04-PLAN.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-05-PLAN.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-01-SUMMARY.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-02-SUMMARY.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-03-SUMMARY.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-04-SUMMARY.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-05-SUMMARY.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md

<interfaces>
<!-- Phase 35 SUMMARY closing-table format precedent (Claude-Discretion 7). -->

Phase 35 SUMMARY.md uses a closing-table format with:

- `## Decision-by-Decision Outcome` table: one row per D-N with columns `D-N | Title | Outcome | Commit Ref(s) | Notes`
- `## Architecture Doc Audit` table (Phase 36 inherits the format from Plan 36-02's `36-02-AUDIT.md`)
- `## Framing-Gap Callouts` section: 3 callouts per D-04
- `## 3-Gate Verification Results` block
- `## Commit Log` table: one row per atomic commit across all plans

Mirror this shape for Phase 36 SUMMARY.

<!-- D-04 framing-gap callouts — verbatim wording. -->

```markdown
## Framing-Gap Callouts (D-04)

Per phase-36 D-04 policy, the planning artifacts ROADMAP.md and PROJECT.md
are NOT retroactively edited to fix wording that predates v1.5 shipped
reality. SUMMARY.md captures the gaps so future reviewers can navigate
the divergence between planning brief and shipped state without
re-discovering them.

### 1. "NIM + OpenRouter narrowed cascade" (ROADMAP.md success criterion #1)

**Planning text:** "the v3 LLM pipeline (NIM + OpenRouter narrowed cascade)"
**Shipped reality:** NIM-only at runtime. OpenRouter is DORMANT per
[ADR-0010 Phase 30.1 sub-block](../../../docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md) — free-tier
probe landed in not-viable bucket. Public docs (README §LLM Enrichment,
runbook §13 NIM throttle handling) describe runtime state, not declared
chain.

**Why preserved:** Phase 36 D-04 policy — planning artifacts are
historical brief; public docs are the surface of truth.

### 2. "10 markdown files (21 Mermaid diagrams)" (ROADMAP.md success criterion #2 / PROJECT.md)

**Planning text:** "The 10 markdown files in `docs/architecture/` (21 Mermaid diagrams)"
**Shipped reality:** 12 markdown files (8 in `docs/architecture/` + 5 in
`docs/architecture/ontology/`, including Phase 30 `llm-pipeline-reliability.md`
and Phase 35 `redis-keys.md`). 21 Mermaid diagrams (NOT 22 as Phase 36
CONTEXT.md D-11 hypothesized; Plan 36-02 grep audit confirmed 21:
9 data-flows + 3 frontend + 4 state-machines + 2 deployment +
2 system-context + 1 types).

**Why preserved:** Phase 36 D-04 policy.

### 3. "v3 → v2 → v1 → raw GDELT fallback chain" (ROADMAP.md success criterion #4)

**Planning text:** "the explicit v3 → v2 → v1 → raw GDELT fallback chain"
**Shipped reality:** **v3 → raw GDELT** (no v1, no v2). The v1 and v2
LLM extractors were DELETED in Phase 29 (Plans 04-06; see
[ADR-0010 Phase 29 Decision](../../../docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md)).
Pitfall 1 cache bridge in `server/routes/events.ts` is the terminal
fallback. Public docs (degradation.md, runbook.md) describe the v3-only
chain.

**Why preserved:** Phase 36 D-04 policy.
```

<!-- 3-gate verification block shape. -->

```markdown
## 3-Gate Verification Results (D-24)

Run at phase close on branch `feature/36-public-docs-sweep-openapi-additions`,
commit `<final-summary-commit-SHA>`.

### Gate 1: Full test suite — `npx vitest run`

**Result:** ✓ PASSED — N test files / M tests passed; 0 failed; 0 skipped (or `it.todo()` stubs).
**Notable additions in Phase 36:** `server/__tests__/openapi/openapi-lint.test.ts` (Plan 36-05 D-08).
**Run timestamp:** YYYY-MM-DD HH:MM:SS UTC

### Gate 2: OpenAPI spec lint — `npx @redocly/cli lint server/openapi.yaml`

**Result:** ✓ PASSED — 0 errors, N warnings (warnings: list each).
**Spec entries:** 14 pre-existing + 5 new = 19 total. 4 reusable component schemas.
**Run timestamp:** YYYY-MM-DD HH:MM:SS UTC

### Gate 3: Markdown link check — `npm run docs:lint`

**Result:** ✓ PASSED — all cross-links resolve.
**Scope:** README.md + `docs/architecture/**/*.md` + `docs/runbook.md` + `docs/degradation.md` + `docs/adr/*.md`. (NOT `.planning/` — Claude-Discretion 2.)
**Run timestamp:** YYYY-MM-DD HH:MM:SS UTC

**All 3 gates green → Phase 36 closes cleanly. Public docs and OpenAPI spec describe shipped v1.5 reality, and mechanical drift gates are in place to keep them honest going forward.**
```

<!-- robots.txt + index.html verify checklist (D-22). -->

```bash
# Confirm /api/* still disallowed
grep -E "^Disallow: /api" public/robots.txt
# Confirm /health still disallowed
grep -E "^Disallow: /health" public/robots.txt
# Confirm meta tags describe Iran Monitor
grep -E '<title>|<meta name="description"|<meta property="og:title"|<meta property="og:description"' index.html
```

If all checks pass: SUMMARY notes "verified clean as of Phase 36".
If drift: edit minimally and commit `docs(36): D-22 fix robots.txt|index.html drift`.

<!-- ROADMAP.md flip target. -->

Current Phase 36 entry in ROADMAP.md (lines 282-294) ends with:

```
   **Plans**: TBD
```

After flip:

```
   **Plans**: 6 plans
   - [x] 36-01-PLAN.md — README sweep (D-17, D-18, D-19, D-20)
   - [x] 36-02-PLAN.md — Architecture sweep + ADR-0011 sub-block (D-09, D-10, D-11, D-12, D-21)
   - [x] 36-03-PLAN.md — Runbook update (D-13, D-14)
   - [x] 36-04-PLAN.md — Degradation contract update (D-15, D-16)
   - [x] 36-05-PLAN.md — OpenAPI additions + lint gate (D-05, D-06, D-07, D-08)
   - [x] 36-06-PLAN.md — Phase close (D-22, D-23, D-24 + framing-gap callouts + tracking flips)
   **Status**: ✓ Complete (YYYY-MM-DD)
```

Phase 37 entry (if present) gets an unblocked note. If Phase 37 is the next pending phase: "**Unblocked by**: Phase 36 close".

<!-- REQUIREMENTS.md flip target. -->

Locate the traceability table rows for: DOCS-PUB-01, DOCS-PUB-02, DOCS-PUB-03, DOCS-PUB-05, DOCS-API-01, DOCS-API-02, DOCS-API-03, DOCS-API-04, DOCS-API-05, DOCS-API-06, DOCS-API-07. Flip each row's status column from `Pending` → `Complete` and add a `Phase 36 close YYYY-MM-DD` entry to the satisfied-by / phase column.

DOCS-PUB-04 stays Pending (Phase 37 territory — ADR-0010 milestone-close sub-block).

<!-- STATE.md flip target. -->

Update STATE.md `## Last activity` and `## Current phase` to reflect Phase 36 complete. Likely also a `## Status` line that flips. Pattern from Phase 35 SUMMARY indicates `gsd-sdk query state.completed-phase --phase 36` may be the canonical way; if not available, hand-edit per Phase 35 precedent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-22 robots.txt + index.html verify-only pass (edit only if drift) + D-23 docs/ tidiness verify</name>
  <files>
    public/robots.txt
    index.html
  </files>
  <read_first>
    public/robots.txt (full file — confirm /api/* + /health Disallow entries)
    index.html (full file — confirm `<title>` + meta description + Open Graph tags reference Iran Monitor accurately)
    README.md (line ~372 — confirms current claim about robots.txt)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-22, D-23)
    docs/brainstorms/ (verify directory still unlinked — `grep -r "brainstorms" README.md docs/` should return 0 README references)
    docs/superpowers/ (same — should be 0 README references)
  </read_first>
  <action>
    **STEP A — robots.txt verify (D-22):**
    1. Read `public/robots.txt`.
    2. Run `grep -E "^Disallow: /api|^Disallow: /health" public/robots.txt`. Expect both Disallow entries present per README line 372 claim.
    3. If drift: use Edit tool to restore the missing Disallow line(s). Commit: `docs(36): D-22 restore robots.txt Disallow entries (drift fix)`.
    4. If clean: no commit; record in SUMMARY "verified clean".

    **STEP B — index.html meta verify (D-22):**
    1. Read `index.html`.
    2. Run `grep -E '<title>|<meta name="description"|<meta property="og:title"|<meta property="og:description"' index.html`. Expect all 4 present and naming Iran Monitor accurately.
    3. If drift (e.g., old "Iran Conflict Monitor" name in title — the rename to "Iran Monitor" / `otg-iran-monitor.vercel.app` happened earlier; verify): use Edit tool to fix. Commit: `docs(36): D-22 update index.html meta tags (drift fix)`.
    4. If clean: no commit; record in SUMMARY "verified clean".

    **STEP C — docs/ tidiness verify (D-23):**
    1. Run `grep -r "brainstorms\|superpowers" README.md docs/architecture/ docs/*.md 2>/dev/null`. Expect 0 hits (both folders should remain unlinked from any docs/ entry).
    2. Run `ls docs/brainstorms/ docs/superpowers/ 2>/dev/null` to confirm both directories still exist (unlinked but present — historical waymarkers).
    3. NO commits — D-23 is note-only ("identified but intentionally not touched"). Captured in SUMMARY only.

    DO NOT add/remove/move any files in `docs/brainstorms/` or `docs/superpowers/`. Pure leave-as-is per CONTEXT.md D-23.

  </action>
  <verify>
    <automated>grep -qE "^Disallow: /api" public/robots.txt && grep -qE "^Disallow: /health" public/robots.txt && grep -q "Iran Monitor" index.html && grep -qE '<meta name="description"' index.html && test -d docs/brainstorms && test -d docs/superpowers</automated>
  </verify>
  <acceptance_criteria>
    - `public/robots.txt` contains `^Disallow: /api` and `^Disallow: /health` (verified clean OR fixed).
    - `index.html` contains `<title>` + `<meta name="description"` (verified clean OR fixed).
    - `index.html` `<title>` mentions "Iran Monitor" (not stale "Iran Conflict Monitor" if rename happened).
    - `docs/brainstorms/` and `docs/superpowers/` directories still exist; no README link to them was added.
    - If any drift fix was made: `git log` shows a `docs(36): D-22 ...` commit; if clean, no D-22 commit and SUMMARY notes "verified clean".
  </acceptance_criteria>
  <done>
    robots.txt + meta tags verified. docs/ tidiness state noted. No accidental edits to brainstorms/superpowers.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-24 three mechanical verification gates (vitest + Redocly lint + markdown-link-check)</name>
  <files>
    .planning/phases/36-public-docs-sweep-openapi-additions/36-verification-results.md
  </files>
  <read_first>
    package.json (post-Plan-36-05 — confirms `openapi:lint` + `docs:lint` scripts wired and dev deps installed)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-24)
    server/__tests__/openapi/openapi-lint.test.ts (post-Plan-36-05 — the new vitest gate)
    server/openapi.yaml (post-Plan-36-05 — the spec being linted)
  </read_first>
  <action>
    Run the 3 gates IN ORDER, capturing output:

    **GATE 1 — `npx vitest run`:**
    ```bash
    npx vitest run 2>&1 | tee .planning/phases/36-public-docs-sweep-openapi-additions/36-gate-1-vitest.log
    ```
    Expected: 0 failures. The new `openapi-lint.test.ts` from Plan 36-05 must pass.

    If failures: STOP. Read the failure messages, identify which file/test failed. If the failure is in a file touched by Plans 36-01 to 36-05, the executor MUST fix the underlying file (NOT the test). If the failure is unrelated (regression in some other test), surface it to the operator — the close should not proceed with regressions.

    **GATE 2 — `npx @redocly/cli lint server/openapi.yaml`:**
    ```bash
    npx @redocly/cli lint server/openapi.yaml --format=stylish 2>&1 | tee .planning/phases/36-public-docs-sweep-openapi-additions/36-gate-2-redocly.log
    ```
    Expected: 0 errors. Warnings (e.g., `no-unused-components`, `info-license`) are acceptable but should be listed in SUMMARY.

    If errors: STOP. Fix the underlying spec issue (in server/openapi.yaml) inline; commit `docs(36): D-24 fix openapi.yaml lint error surfaced at phase close`. Then re-run.

    **GATE 3 — `npm run docs:lint`:**
    ```bash
    npm run docs:lint 2>&1 | tee .planning/phases/36-public-docs-sweep-openapi-additions/36-gate-3-markdown-link-check.log
    ```
    Expected: 0 broken links.

    If broken links: STOP. Fix the underlying broken link in the offending markdown file; commit `docs(36): D-24 fix broken link in <file> surfaced at phase close`. Then re-run.

    **STEP D — Capture verification result file:**

    Use the Write tool to create `.planning/phases/36-public-docs-sweep-openapi-additions/36-verification-results.md` containing the 3-gate-verification block from `<interfaces>` shape, with actual test/lint/link-check output counts substituted.

    **STEP E — Commit:**
    `chore(36): D-24 run 3-gate verification (vitest + Redocly + markdown-link-check; all green)` body lists test count + spec entry count + link count.

    If any fix commits landed between gates: those land naturally with `docs(36): D-24 fix ...` prefixes; the chore commit captures the final all-green state.

  </action>
  <verify>
    <automated>test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-verification-results.md && test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-gate-1-vitest.log && test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-gate-2-redocly.log && test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-gate-3-markdown-link-check.log && grep -q "PASSED" .planning/phases/36-public-docs-sweep-openapi-additions/36-verification-results.md</automated>
  </verify>
  <acceptance_criteria>
    - `36-verification-results.md` exists with the 3-gate block populated.
    - `36-gate-1-vitest.log` shows the final vitest run output (0 failures).
    - `36-gate-2-redocly.log` shows `npx @redocly/cli lint` output (0 errors).
    - `36-gate-3-markdown-link-check.log` shows `npm run docs:lint` output (0 broken links).
    - `git log -1 --format=%s` matches `chore(36):` with `D-24` in body.
    - If any inline fix commits landed: each has `docs(36): D-24 fix ...` prefix; SUMMARY enumerates them.
  </acceptance_criteria>
  <done>
    All 3 mechanical verification gates green. Phase 36 has empirical proof that public docs + spec are honest and cross-references resolve.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 36-SUMMARY.md with per-D-N table + audit table + framing-gap callouts + 3-gate results (D-04, D-12, multiple)</name>
  <files>.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md</files>
  <read_first>
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (all 28 D-N decisions — source for per-D-N table rows)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md (Plan 36-02 output — source for the architecture audit table absorption)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-verification-results.md (Task 2 output — source for 3-gate verification block)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-01-SUMMARY.md (Plan 36-01 close-out — commit hashes + delta)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-02-SUMMARY.md (Plan 36-02 close-out)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-03-SUMMARY.md (Plan 36-03 close-out)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-04-SUMMARY.md (Plan 36-04 close-out)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-05-SUMMARY.md (Plan 36-05 close-out)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md (closing-table format precedent — Claude-Discretion 7)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md`. Closing-table format (Phase 35 precedent).

    Structure (sections in this order):

    ```markdown
    # Phase 36 Close-Out Summary: Public Docs Sweep + OpenAPI Additions

    **Closed:** YYYY-MM-DD
    **Branch:** feature/36-public-docs-sweep-openapi-additions
    **Wave structure executed:** Wave 1 (36-02, 36-03, 36-04, 36-05 parallel) → Wave 2 (36-01) → Wave 3 (36-06 close)
    **Coverage:** DOCS-PUB-01/02/03/05 + DOCS-API-01..07 (DOCS-PUB-04 → Phase 37)

    ## Top-line outcomes

    - 5 new OpenAPI endpoint entries added; 2 verified-clean.
    - 4 reusable component schemas declared (AuditTierStatus, ByBearerMap, LlmPipelineState, LlmReplayDiff).
    - 2 new mechanical drift gates: Redocly OpenAPI lint vitest + markdown-link-check script.
    - 12 architecture markdown files audited; N edited / M verified-clean.
    - 21 Mermaid diagrams audited; N edited / M verified-clean.
    - Runbook gained 4 new SRE sections (§13-§16) + rewritten §6 (10s → 800s).
    - degradation.md updated with explicit Pitfall 1 contract + v3-only chain.
    - README gained new ## LLM Enrichment section + rate-limit drift fix.
    - ADR-0011 gained Phase 36 sub-block (NIM-only runtime cascade reaffirmation).

    ## Decision-by-Decision Outcome (28 rows)

    | D-N | Title | Plan | Outcome | Commit Ref | Notes |
    | --- | ----- | ---- | ------- | ---------- | ----- |
    | D-01 | Honest shipped reality across README + architecture + runbook | 36-01, 36-02, 36-03 | shipped | (rollup) | NIM-only at runtime framing landed in all 3 public docs |
    | D-02 | Cite phase number + ADR-0010 inline for each provider state | 36-01, 36-02, 36-03 | shipped | (rollup) | All references cross-link |
    | D-03 | Rewrite runbook + degradation chain to "v3 → raw GDELT" | 36-03, 36-04 | shipped | <SHA> | No v1/v2/v3 chain remains in public docs |
    | D-04 | Leave ROADMAP/REQUIREMENTS wording untouched | 36-06 | shipped | (this commit) | 3 framing-gap callouts captured below |
    | D-05 | Additions + lightweight verification, no full audit | 36-05 | shipped | <SHA> | 5 added, 2 verified-clean (or N drift fixed inline) |
    | D-06 | Split security schemes — cronSecret + operatorBearer | 36-05 | shipped | <SHA> | Both schemes declared; cron endpoints updated |
    | D-07 | Hybrid response-shape strategy | 36-05 | shipped | <SHA> | 4 reusable schemas + inline one-offs |
    | D-08 | Mechanical lint gate via @redocly/cli | 36-05 | shipped | <SHA> | openapi-lint.test.ts vitest green |
    | D-09 | Targeted v1.5-drift sweep across all 12 files | 36-02 | shipped | (per-file) | See audit table below |
    | D-10 | Mermaid diagrams — edit only where drift exists | 36-02 | shipped | (per-diagram) | See audit table below |
    | D-11 | File-count + diagram-count drift noted in SUMMARY only | 36-06 | shipped | (this commit) | Callout #2 below |
    | D-12 | Inline audit table in SUMMARY.md | 36-06 (from 36-02 accumulator) | shipped | (this commit) | Full table below |
    | D-13 | Four new standalone SRE-template sections (§13-§16) | 36-03 | shipped | <SHA> | NIM throttle, cron lessons, force-trigger, prod-audit retry |
    | D-14 | Rewrite-in-place for stale §6 (with anchor shim) | 36-03 | shipped | <SHA> | Old anchor preserved |
    | D-15 | Explicit "map never goes blank" Pitfall 1 contract | 36-04 | shipped | <SHA> | Cross-link to redis-death.test.ts |
    | D-16 | Document the v3 → raw GDELT chain | 36-04 | shipped | <SHA> | Phase 29 deletion acknowledged |
    | D-17 | New "LLM Enrichment" subsection in README | 36-01 | shipped | <SHA> | ~80-120 lines, 6 subsections |
    | D-18 | Fix README rate-limit drift | 36-01 | shipped | <SHA> | 60/min global tier (was 6/min) |
    | D-19 | Add manual-trigger prod-connectivity-audit.yml mention | 36-01 | shipped | <SHA> | Folded into ### Production health verification |
    | D-20 | Add API Health dashboard merge mention | 36-01 | shipped | <SHA> | Own subsection |
    | D-21 | Append Phase 30.1/34 sub-block to ADR-0011 | 36-02 | shipped | <SHA> | REAFFIRMED v3 architecture; cross-links to ADR-0010 |
    | D-22 | Quick verify-only of robots.txt + meta tags | 36-06 | shipped | (this commit) | Verified clean OR <SHA> if drift |
    | D-23 | Leave docs/brainstorms + docs/superpowers as-is | 36-06 | shipped | (note-only) | Historical waymarkers preserved |
    | D-24 | Three mechanical gates at phase close | 36-06 | shipped | <SHA> | All 3 green; see 3-Gate Verification below |
    | D-25 | 5 plans + close = 6 total | (planning meta) | shipped | (initial planning) | 6 PLAN.md files created Plan-Phase 36 |
    | D-26 | Two waves of execution | (execution meta) | shipped | (wave-execution) | Wave 1 / Wave 2 / Wave 3 |
    | D-27 | Atomic per-decision commits within plans | (execution meta) | shipped | (per-commit) | N atomic commits total across the 6 plans |
    | D-28 | Branch discipline | 36-05 (Task 1 STEP A) | shipped | (branch cut) | feature/36-public-docs-sweep-openapi-additions |

    ## Architecture Doc Audit (D-12) — absorbed from `36-02-AUDIT.md`

    [Copy verbatim the two tables from 36-02-AUDIT.md: "Architecture Markdown Files" (12 rows) + "Mermaid Diagrams" (21 rows).]

    ## Framing-Gap Callouts (D-04)

    [Copy verbatim the 3 callouts from `<interfaces>` "D-04 framing-gap callouts" block, with section headings (#1 / #2 / #3) and ADR-0010 cross-links.]

    ## 3-Gate Verification Results (D-24)

    [Copy verbatim from `.planning/phases/36-public-docs-sweep-openapi-additions/36-verification-results.md`.]

    ## Commit Log

    | Plan | Commit | Date | Description |
    | ---- | ------ | ---- | ----------- |
    | 36-05 | <SHA> | YYYY-MM-DD | chore: install @redocly + markdown-link-check; wire scripts (D-08, D-24) |
    | 36-05 | <SHA> | YYYY-MM-DD | test: ship openapi-lint drift gate vitest (D-08) |
    | 36-05 | <SHA> | YYYY-MM-DD | docs: split securitySchemes + add component schemas (D-06, D-07) |
    | 36-05 | <SHA> | YYYY-MM-DD | docs: add 5 new OpenAPI endpoints + verify 2 cron entries (D-05) |
    | 36-02 | <SHA> | YYYY-MM-DD | docs: D-09 sweep <file> (per-file commits) |
    | 36-02 | <SHA> | YYYY-MM-DD | docs: D-10 sweep Mermaid diagrams |
    | 36-02 | <SHA> | YYYY-MM-DD | docs: D-12 record architecture sweep audit accumulator |
    | 36-02 | <SHA> | YYYY-MM-DD | docs: D-21 append Phase 36 sub-block to ADR-0011 |
    | 36-03 | <SHA> | YYYY-MM-DD | docs: D-14 rewrite runbook §6 with anchor shim |
    | 36-03 | <SHA> | YYYY-MM-DD | docs: D-13 add runbook §13-§16 + ToC update |
    | 36-04 | <SHA> | YYYY-MM-DD | docs: D-15 D-16 update degradation.md |
    | 36-01 | <SHA> | YYYY-MM-DD | docs: D-18 fix README rate-limit drift |
    | 36-01 | <SHA> | YYYY-MM-DD | docs: D-17 D-19 D-20 add README LLM Enrichment section |
    | 36-06 | <SHA> | YYYY-MM-DD | chore: D-24 run 3-gate verification (all green) |
    | 36-06 | <SHA> | YYYY-MM-DD | docs: D-04 D-12 author 36-SUMMARY.md (this commit) |
    | 36-06 | <SHA> | YYYY-MM-DD | docs: flip ROADMAP / REQUIREMENTS / STATE for Phase 36 close |

    ## What's next

    Phase 37: ADR-0010 milestone-close sub-block (DOCS-PUB-04) + acceptance-gate observation (LLM-RELI-07: 3× consecutive `allTiersGreen=true` prod-audit runs) + CHANGELOG[v1.5] entry.

    ---

    *Phase: 36-public-docs-sweep-openapi-additions*
    *Closed: YYYY-MM-DD via Plan 36-06*
    ```

    Substitute `<SHA>` placeholders with actual short commit SHAs from `git log --oneline --grep "(36)"` across the branch. Substitute YYYY-MM-DD with actual UTC dates.

    Commit (atomic per D-27): `docs(36): D-04 D-12 author 36-SUMMARY.md (per-D-N table + audit absorption + framing-gap callouts + 3-gate results)` body cites the 28-decision count + 12+21 audit row count + 3 framing callouts + 3 gate results.

  </action>
  <verify>
    <automated>test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md && grep -q "Phase 36 Close-Out Summary" .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md && grep -q "Decision-by-Decision Outcome" .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md && grep -q "Architecture Doc Audit" .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md && grep -q "Framing-Gap Callouts" .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md && grep -q "3-Gate Verification Results" .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md && grep -cE "^\| D-[0-9]+" .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md | awk '$1>=28 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `36-SUMMARY.md` exists.
    - File contains 5 major sections: Top-line outcomes, Decision-by-Decision Outcome (28 rows), Architecture Doc Audit (12+21 rows), Framing-Gap Callouts (3 callouts), 3-Gate Verification Results, Commit Log.
    - Per-D-N table has at least 28 rows matching D-N IDs (D-01 through D-28).
    - All 3 framing-gap callouts present with section headings (#1 NIM + OpenRouter / #2 10 files+21 diagrams / #3 v3→v2→v1→raw GDELT).
    - 3-gate verification block shows all 3 gates PASSED.
    - Commit Log table has at least one row per plan (6 plans).
    - `git log -1 --format=%s` matches `docs(36):` with `D-04` and `D-12` in body.
  </acceptance_criteria>
  <done>
    SUMMARY is the trust artifact for Phase 36 close. Reviewer can navigate all 28 D-N outcomes, the full architecture audit, the 3 framing-gap callouts, and the 3-gate verification evidence from one document.
  </done>
</task>

<task type="auto">
  <name>Task 4: Flip ROADMAP.md + REQUIREMENTS.md + STATE.md tracking markers</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/ROADMAP.md (lines 282-294 — current Phase 36 entry; identify the Plans + Status placeholders)
    .planning/REQUIREMENTS.md (full file — locate the rows for DOCS-PUB-01/02/03/05 + DOCS-API-01..07; identify the status column format)
    .planning/STATE.md (full file — confirm last_activity + current_phase fields)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md (Phase 35 close precedent — verify gsd-sdk usage)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-25 plan list verbatim wording for ROADMAP plan-list flip)
  </read_first>
  <action>
    **STEP A — ROADMAP.md flip:**

    Use the Edit tool to replace the existing `**Plans**: TBD` (line ~294 of Phase 36 entry) with:

    ```
       **Plans**: 6 plans
       - [x] 36-01-PLAN.md — README sweep (D-17, D-18, D-19, D-20)
       - [x] 36-02-PLAN.md — Architecture sweep + ADR-0011 sub-block (D-09, D-10, D-11, D-12, D-21)
       - [x] 36-03-PLAN.md — Runbook update (D-13, D-14)
       - [x] 36-04-PLAN.md — Degradation contract update (D-15, D-16)
       - [x] 36-05-PLAN.md — OpenAPI additions + lint gate (D-05, D-06, D-07, D-08)
       - [x] 36-06-PLAN.md — Phase close (D-22, D-23, D-24 + framing-gap callouts + ROADMAP/REQ/STATE flips)
       **Status**: ✓ Complete (YYYY-MM-DD)
    ```

    Substitute YYYY-MM-DD with today's UTC date.

    If a `### Phase 37:` entry exists, optionally add `**Unblocked by**: Phase 36 close (YYYY-MM-DD)` to its preamble. If unsure or no Phase 37 entry: skip the Phase 37 touch and note in commit body.

    DO NOT touch the success criteria text of the Phase 36 entry (per D-04, framing-gap wording stays as-is for historical record; SUMMARY captures the gaps).

    Commit: `docs(36): close phase 36 — flip ROADMAP.md Phase 36 entry to ✓ Complete + plan list filled in`

    **STEP B — REQUIREMENTS.md flip:**

    Locate the traceability table rows for: DOCS-PUB-01, DOCS-PUB-02, DOCS-PUB-03, DOCS-PUB-05, DOCS-API-01, DOCS-API-02, DOCS-API-03, DOCS-API-04, DOCS-API-05, DOCS-API-06, DOCS-API-07. Use Edit (or replace_all if format is identical) to flip each row's status column from `Pending` → `Complete` and add `Phase 36 close YYYY-MM-DD` (or whatever convention the existing table uses — match Phase 35-flipped row format precedent).

    DOCS-PUB-04 stays Pending — that's Phase 37 territory.

    Commit: `docs(36): close phase 36 — flip REQUIREMENTS.md DOCS-PUB-01/02/03/05 + DOCS-API-01..07 to Complete`

    **STEP C — STATE.md flip:**

    Read STATE.md. Identify the `## Current phase` / `## Last activity` / `## Status` fields (match what Phase 35 SUMMARY's close-out edited). Use Edit to:
    - Update `Last activity` to today's UTC date + "Phase 36 closed: Public Docs Sweep + OpenAPI Additions"
    - Update `Current phase` to "Phase 37 pending" (or whatever the next-phase convention is)
    - If a `Phases complete` count exists, increment it

    Alternatively, if the `gsd-sdk` SDK has a `state.completed-phase --phase 36` command (per Phase 35 precedent), use that:
    ```bash
    gsd-sdk query state.completed-phase --phase 36 --name "Public Docs Sweep + OpenAPI Additions"
    ```

    Commit: `docs(36): close phase 36 — flip STATE.md (Phase 36 complete; Phase 37 pending)`

    **STEP D — Final branch hygiene:**
    1. Run `git log --oneline main..HEAD` and verify all commits are `feat(36)`/`docs(36)`/`chore(36)`/`test(36)` prefixed.
    2. Verify branch is clean: `git status` shows working tree clean.
    3. Push: `git push origin feature/36-public-docs-sweep-openapi-additions`.
    4. Note in execution output: "Branch ready for merge to main. Run `gh pr create` or merge per project convention."

    Do NOT auto-merge to main. Operator approval per CLAUDE.md §Conventions (branch-per-phase; merge ritual is the operator's domain).

  </action>
  <verify>
    <automated>grep -A 1 "^### Phase 36:" .planning/ROADMAP.md | grep -q "✓ Complete" || grep -A 10 "^### Phase 36:" .planning/ROADMAP.md | grep -q "✓ Complete" && grep -q "36-01-PLAN.md" .planning/ROADMAP.md && grep -q "36-06-PLAN.md" .planning/ROADMAP.md && grep -cE "DOCS-(PUB-0[1235]|API-0[1-7]).*Complete|Complete.*DOCS-(PUB-0[1235]|API-0[1-7])" .planning/REQUIREMENTS.md | awk '$1>=11 {exit 0} {exit 1}' && grep -q "Phase 36" .planning/STATE.md && git status --porcelain | wc -l | awk '$1==0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 36 entry has `✓ Complete (<date>)` status.
    - `.planning/ROADMAP.md` Phase 36 entry has plan list with 6 checked entries (36-01 through 36-06).
    - `.planning/REQUIREMENTS.md` rows for DOCS-PUB-01, DOCS-PUB-02, DOCS-PUB-03, DOCS-PUB-05, DOCS-API-01..07 all show `Complete` status.
    - `.planning/REQUIREMENTS.md` row for DOCS-PUB-04 still shows `Pending` (Phase 37 territory).
    - `.planning/STATE.md` references Phase 36 in `Last activity` or `Current phase` field.
    - `git status` shows working tree clean.
    - Branch `feature/36-public-docs-sweep-openapi-additions` has been pushed to origin.
    - 3 atomic close commits exist (ROADMAP / REQUIREMENTS / STATE) OR 1 rollup commit per executor's choice.
  </acceptance_criteria>
  <done>
    Phase 36 closed cleanly. All 3 planning artifacts (ROADMAP / REQUIREMENTS / STATE) reflect close. Branch ready for operator merge to main. Phase 37 unblocked.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                           | Description                                                                                                    |
| ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| Author → robots.txt + index.html                                   | Verify-only pass; edits only on drift; no behavior change unless drift is fixed                                |
| Verification tooling → @redocly/cli + vitest + markdown-link-check | Pinned dev deps (Plan 36-05 chore); spawn local subprocesses with hardcoded argv                               |
| Tracking artifact author → ROADMAP / REQUIREMENTS / STATE          | Hand-edits of planning artifacts; no production code touched; mitigated by gsd-sdk verification (if available) |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                                |
| ---------- | ---------------------- | --------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-36-06-01 | Tampering              | ROADMAP.md / REQUIREMENTS.md / STATE.md edits             | mitigate    | Per-D-N table in SUMMARY pins the close-out decisions; gsd-sdk state.completed-phase (if used) provides additional validation. |
| T-36-06-02 | Repudiation            | Phase-close decisions made without commit trail           | mitigate    | All close-out actions land as atomic commits with `docs(36):` prefix + D-NN body references. Git log is the auditable trail.   |
| T-36-06-03 | Information Disclosure | 36-SUMMARY publishes commit hashes + verification outputs | accept      | Already public via git log + workflow artifacts. SUMMARY consolidates for readability.                                         |
| T-36-06-SC | Tampering              | No npm installs in this plan                              | n/a         | Dev deps were installed in Plan 36-05 Task 1; this plan only consumes them via existing scripts.                               |

</threat_model>

<verification>
- `npx vitest run` exits 0 (Task 2 already established this; re-run after any inline fix commits).
- `npx @redocly/cli lint server/openapi.yaml` exits 0.
- `npm run docs:lint` exits 0.
- `git status` shows working tree clean after all tracking flips.
- `git log --oneline main..HEAD` shows the full Phase 36 commit trail (atomic per D-N; one commit per major decision).
- Manual review: open 36-SUMMARY.md; confirm reviewer can navigate from any D-N row to either the commit SHA, the audit table row, or the framing-gap callout that explains it.
</verification>

<success_criteria>

1. robots.txt + index.html verified clean (or minimally edited on drift).
2. docs/brainstorms + docs/superpowers state noted (D-23 verify-only).
3. 3-gate verification: vitest + Redocly lint + markdown-link-check all green.
4. 36-SUMMARY.md authored with per-D-N table (28 rows) + audit absorption (12+21 rows) + 3 framing-gap callouts + 3-gate results + commit log.
5. ROADMAP.md Phase 36 entry flipped to ✓ Complete with plan list filled.
6. REQUIREMENTS.md DOCS-PUB-01/02/03/05 + DOCS-API-01..07 flipped to Complete; DOCS-PUB-04 stays Pending.
7. STATE.md reflects Phase 36 close.
8. Branch pushed; clean working tree; ready for operator merge.
9. Phase 37 unblocked for ADR-0010 close + acceptance-gate observation.

</success_criteria>

<output>
The SUMMARY.md authored in Task 3 IS the Plan 36-06 close-out artifact (it serves as both 36-06 plan summary and the overall phase summary). No separate 36-06-SUMMARY.md needed.

Final return to operator:

- `36-SUMMARY.md` path + line count.
- Branch name + final commit SHA on the branch.
- Reminder: operator merges to main when ready (no auto-merge per CLAUDE.md).
- Pointer: "Phase 37 unblocked — DOCS-PUB-04 / LLM-RELI-07 / CHANGELOG[v1.5]."
  </output>

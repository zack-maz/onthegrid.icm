---
status: partial
phase: 28.2.6-fix-vercel-cron-architecture-so-events-llm-v3-populates-with
source: [28.2.6-VERIFICATION.md]
started: 2026-05-07T16:05:00Z
updated: 2026-05-07T16:05:00Z
---

## Current Test

[awaiting human testing — both items require live Vercel deployment after merge]

## Tests

### 1. Force-trigger cron route against prod and confirm events:llm:v3 populates within budget

expected: `GET https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true` (with `Authorization: Bearer ${CRON_SECRET}`) returns 200 with `{ok:true,dispatched:true}` and within 300s the Redis key `events:llm:v3` contains a non-empty `ConflictEventEntity[]` array.
why_human: Vercel Fluid Compute `waitUntil` behavior and the 300s `maxDuration` ceiling can only be confirmed against a live Vercel deployment; local Vitest cannot simulate the Vercel runtime kill-on-`res.end()` signal.
result: [pending]

### 2. Confirm prod-connectivity-audit.yml tier-green gate flips to allTiersGreen=true

expected: After test 1 populates `events:llm:v3`, manually running the `.github/workflows/prod-connectivity-audit.yml` workflow shows `allTiersGreen=true` in the `audit:connectivity:last-result` Redis sidecar and the workflow exits 0.
why_human: Requires a live Vercel deployment, a populated `events:llm:v3` cache from the cron run, and manual workflow trigger in GitHub Actions.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

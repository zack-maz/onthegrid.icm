---
phase: 27.2-event-quality-and-water-data-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/sourceTiers.ts
  - server/types.ts
  - server/adapters/gdelt-doc.ts
  - server/lib/newsFilter.ts
  - server/routes/events.ts
  - src/lib/severity.ts
  - server/__tests__/lib/sourceTiers.test.ts
  - server/__tests__/lib/newsFilter.test.ts
autonomous: true
requirements: [EQ-01, EQ-02, EQ-03, EQ-04]

must_haves:
  truths:
    - 'Source tier classification returns correct tier (1/2/3/null) for all listed sources'
    - 'GDELT DOC articles carry domain field through to NewsArticle'
    - 'News feed filters out non-tiered sources (tier null) from article display but preserves count'
    - 'Events with only 1 Tier 3 source get demoted in severity'
    - 'Events with 2+ Tier 1/2 sources get boosted in severity'
    - 'ConflictEventEntity data includes sourceTier field set server-side'
  artifacts:
    - path: 'server/lib/sourceTiers.ts'
      provides: 'Source tier registry with getSourceTier() and getHighestTier()'
      exports:
        [
          'getSourceTier',
          'getHighestTier',
          'SourceTier',
          'TIER_1_DOMAINS',
          'TIER_2_DOMAINS',
          'TIER_3_DOMAINS',
        ]
    - path: 'server/__tests__/lib/sourceTiers.test.ts'
      provides: 'Unit tests for source tier classification'
  key_links:
    - from: 'server/lib/newsFilter.ts'
      to: 'server/lib/sourceTiers.ts'
      via: 'import getSourceTier'
      pattern: 'getSourceTier'
    - from: 'server/routes/events.ts'
      to: 'server/lib/sourceTiers.ts'
      via: 'import getHighestTier for sourceTier injection'
      pattern: 'getHighestTier'
    - from: 'src/lib/severity.ts'
      to: 'ConflictEventEntity.data.sourceTier'
      via: 'tier multiplier in computeSeverityScore'
      pattern: 'sourceTier'
---

<objective>
Create a 3-tier news source classification system and wire it into the news filtering pipeline, event entity data, and severity scoring.

Purpose: Filter out low-quality / unknown news sources from the feed, enrich event entities with source tier metadata, and weight severity scores based on source quality -- higher-tier sources produce higher severity scores.

Output: New `sourceTiers.ts` module, modified news filter, enriched entity type, updated severity scoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.2-event-quality-and-water-data-improvements/27.2-CONTEXT.md
@.planning/phases/27.2-event-quality-and-water-data-improvements/27.2-RESEARCH.md

@server/lib/relevanceScorer.ts
@server/lib/newsFilter.ts
@server/adapters/gdelt-doc.ts
@server/types.ts
@server/routes/events.ts
@src/lib/severity.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From server/types.ts:

```typescript
export interface NewsArticle {
  id: string;
  title: string;
  url: string;
  source: string; // "GDELT", "BBC", "Al Jazeera", etc.
  sourceCountry?: string;
  publishedAt: number;
  summary?: string;
  imageUrl?: string;
  // ... other fields
}

export interface ConflictEventEntity extends MapEntityBase {
  type: ConflictEventType;
  data: {
    // ... existing fields
    numMentions?: number;
    numSources?: number;
    sourceCount?: number;
    llmProcessed?: boolean;
    // sourceTier will be added here
  };
}
```

From server/lib/relevanceScorer.ts:

```typescript
export const SOURCE_RELIABILITY: Record<string, number> = {
  /* ... */
};
// Already has a 4-tier numeric reliability map by domain/source name
```

From server/lib/newsFilter.ts:

```typescript
export function filterAndScoreArticles(articles: NewsArticle[]): NewsArticle[];
```

From server/adapters/gdelt-doc.ts:

```typescript
interface GdeltArticle {
  domain?: string; // Available but NOT propagated to NewsArticle
}
export async function fetchGdeltArticles(): Promise<NewsArticle[]>;
```

From server/routes/events.ts:

```typescript
function enrichedToEntities(geocoded, groups): ConflictEventEntity[];
// This is where sourceTier should be set on each entity
```

From src/lib/severity.ts:

```typescript
export function computeSeverityScore(event: ConflictEventEntity): number;
export function classifySeverity(event: ConflictEventEntity): SeverityLevel;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create source tier registry + propagate domain to NewsArticle + add sourceTier to entity type</name>
  <files>server/lib/sourceTiers.ts, server/__tests__/lib/sourceTiers.test.ts, server/types.ts, server/adapters/gdelt-doc.ts</files>
  <behavior>
    - getSourceTier('Reuters', undefined) returns 1
    - getSourceTier('BBC', undefined) returns 2
    - getSourceTier('BBC', 'bbc.co.uk') returns 2
    - getSourceTier('Tehran Times', undefined) returns 3
    - getSourceTier('GDELT', 'reuters.com') returns 1 (domain lookup)
    - getSourceTier('GDELT', 'unknownsource.com') returns null
    - getSourceTier('RandomBlog', undefined) returns null
    - getHighestTier([url1_reuters, url2_bbc, url3_unknown]) returns 1
    - getHighestTier([url3_unknown_only]) returns null
    - extractDomain('https://www.reuters.com/article/123') returns 'reuters.com'
  </behavior>
  <action>
1. Create `server/lib/sourceTiers.ts`:
   - Export `type SourceTier = 1 | 2 | 3`
   - Define `TIER_1_DOMAINS` Set: reuters.com, apnews.com, afp.com, bellingcat.com, liveuamap.com (per D-01 gold tier)
   - Define `TIER_1_NAMES` Set: Reuters, Associated Press, AFP, Bellingcat, Liveuamap
   - Define `TIER_2_DOMAINS` Set: bbc.co.uk, bbc.com, aljazeera.com, cnn.com, timesofisrael.com, middleeasteye.net, theguardian.com, nytimes.com, washingtonpost.com (per D-01 silver tier)
   - Define `TIER_2_NAMES` Set: BBC, Al Jazeera, CNN, Times of Israel, Middle East Eye, Guardian, NYT, Washington Post
   - Define `TIER_3_DOMAINS` Set: tehrantimes.com, irna.ir, sana.sy, presstv.ir (per D-01 bronze tier)
   - Define `TIER_3_NAMES` Set: Tehran Times, IRNA, SANA, Press TV
   - Export `function extractDomain(url: string): string` -- uses `new URL(url).hostname.replace(/^www\./, '')` with try/catch returning ''
   - Export `function getSourceTier(source: string, domain?: string): SourceTier | null` -- checks names first, then domains, returns null for unknown
   - Export `function getHighestTier(sourceUrls: string[]): SourceTier | null` -- extracts domain from each URL, returns the best (lowest number) tier found, or null if all unknown

2. Create `server/__tests__/lib/sourceTiers.test.ts`:
   - Test all tier classifications from the behavior list above
   - Test extractDomain edge cases (malformed URLs, www prefix stripping)
   - Test getHighestTier with mixed tiers, all unknown, empty array

3. Modify `server/types.ts`:
   - Add `domain?: string` field to `NewsArticle` interface (after `source` field)
   - Add `sourceTier?: 1 | 2 | 3` field to `ConflictEventEntity.data` (after `sourceCount` field)

4. Modify `server/adapters/gdelt-doc.ts`:
   - In the `articles.map()` at line 73, add `domain: a.domain || undefined` to the returned NewsArticle object
     </action>
     <verify>
     <automated>npx vitest run server/**tests**/lib/sourceTiers.test.ts -x</automated>
     </verify>
     <done>sourceTiers.ts exports all functions with correct classifications. NewsArticle has domain field. ConflictEventEntity.data has sourceTier field. GDELT DOC adapter propagates domain. All tests pass.</done>
     </task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tier filtering into news pipeline + inject sourceTier into events + severity tier multiplier</name>
  <files>server/lib/newsFilter.ts, server/__tests__/lib/newsFilter.test.ts, server/routes/events.ts, src/lib/severity.ts</files>
  <behavior>
    - filterAndScoreArticles with Tier 1 article -> article passes filter
    - filterAndScoreArticles with Tier 2 article -> article passes filter
    - filterAndScoreArticles with Tier 3 article -> article passes filter (but flagged)
    - filterAndScoreArticles with unknown-source article -> article filtered OUT from news display
    - Events with only 1 Tier 3 source -> sourceTier set to 3
    - Events with 2+ Tier 1 sources -> sourceTier set to 1
    - computeSeverityScore with sourceTier=1 returns higher score than sourceTier=2
    - computeSeverityScore with sourceTier=3 returns lower score than sourceTier=2
    - classifySeverity uses same tier multiplier
  </behavior>
  <action>
1. Modify `server/lib/newsFilter.ts`:
   - Import `getSourceTier, extractDomain` from `./sourceTiers.js`
   - In `filterAndScoreArticles()`, after the existing NLP/keyword scoring, add a tier pre-filter step:
     - For each article, compute tier via `getSourceTier(article.source, article.domain ?? extractDomain(article.url))`
     - If tier is null (unknown source): exclude from the returned filtered array
     - If tier is 1, 2, or 3: keep the article (all tiered sources pass through)
   - Per D-01: "Everything else filtered out entirely from news feed. Still counts toward event numSources for corroboration." -- the filtering only affects the news feed display, not the GDELT event source counting which is separate.

2. Update `server/__tests__/lib/newsFilter.test.ts`:
   - Add test case: article with source "BBC" + conflict keyword passes filter
   - Add test case: article with unknown domain + conflict keyword is FILTERED OUT
   - Add test case: article with source "Tehran Times" (Tier 3) passes filter

3. Modify `server/routes/events.ts` -- in `enrichedToEntities()` function:
   - Import `getHighestTier` from `../lib/sourceTiers.js`
   - For each enriched event, look up the source URLs from the matching group (`g.sourceUrls`)
   - Call `getHighestTier(sourceUrls)` to determine the best source tier
   - Set `sourceTier` on the entity data: `sourceTier: getHighestTier(entities[0]?.data?.source ? [entities[0].data.source] : []) ?? undefined`
   - Actually, use the group's sourceUrls: the EventGroup already has `sourceUrls: string[]`. In enrichedToEntities, `entities` is from `groupMap.get(enriched.groupKey)`. The groups parameter has the sourceUrls. Look up the group from the `groups` array parameter: `const group = groups.find(g => g.key === enriched.groupKey)`. Then `sourceTier: group ? getHighestTier(group.sourceUrls) ?? undefined : undefined`.

4. Modify `src/lib/severity.ts`:
   - Add `SOURCE_TIER_MULTIPLIER` constant: `{ 1: 1.5, 2: 1.0, 3: 0.7 }` (per RESEARCH.md recommendation)
   - In `computeSeverityScore()`: multiply by `SOURCE_TIER_MULTIPLIER[event.data.sourceTier ?? 2] ?? 1.0`
   - In `classifySeverity()`: apply same multiplier to the static score calculation
   - Per D-01 minimum sources logic: "Events with only 1 Tier 3 source get demoted" is handled by tier=3 getting 0.7x. "Events with 2+ Tier 1/2 sources get boosted" is handled by tier=1 getting 1.5x (highest tier among sources).
     </action>
     <verify>
     <automated>npx vitest run server/**tests**/lib/newsFilter.test.ts server/**tests**/lib/sourceTiers.test.ts -x && npx vitest run src/**tests**/ -x 2>/dev/null; npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
     </verify>
     <done>Unknown-source articles filtered from news feed. Events carry sourceTier computed from source URLs. Severity scoring applies tier multiplier (1.5x gold, 1.0x silver, 0.7x bronze). Full test suite passes.</done>
     </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                 |
| ------------------------- | ----------------------------------------------------------- |
| GDELT DOC API -> server   | Domain field from external API used for tier classification |
| LLM output -> entity data | sourceTier derived from source URLs, not LLM output         |

## STRIDE Threat Register

| Threat ID | Category  | Component      | Disposition | Mitigation Plan                                                                                                                                                     |
| --------- | --------- | -------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-27.2-01 | Spoofing  | sourceTiers.ts | accept      | Domain extraction from URLs is best-effort; a spoofed GDELT domain would only affect tier classification, not data integrity. Low value target.                     |
| T-27.2-02 | Tampering | newsFilter.ts  | mitigate    | Tier filtering is additive (filters OUT unknowns); if tier registry is wrong, worst case is showing too few articles. Existing keyword + NLP scoring still applies. |

</threat_model>

<verification>
- `npx vitest run server/__tests__/lib/sourceTiers.test.ts -x` -- tier classification tests
- `npx vitest run server/__tests__/lib/newsFilter.test.ts -x` -- filter with tier gating
- `npx vitest run --reporter=verbose` -- full suite regression
- `npx tsc --noEmit` -- type check (sourceTier field additions)
</verification>

<success_criteria>

- Source tier registry classifies all D-01 sources correctly (gold/silver/bronze/null)
- GDELT DOC domain field propagated to NewsArticle
- Unknown-source articles filtered from news feed
- ConflictEventEntity.data.sourceTier populated server-side
- Severity scoring applies tier multiplier
- All existing tests pass (no regressions)
  </success_criteria>

<output>
After completion, create `.planning/phases/27.2-event-quality-and-water-data-improvements/27.2-01-SUMMARY.md`
</output>

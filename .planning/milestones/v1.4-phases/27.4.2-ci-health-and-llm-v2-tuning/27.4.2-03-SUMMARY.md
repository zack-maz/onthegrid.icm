---
phase: 27.4.2-ci-health-and-llm-v2-tuning
plan: 03-prettier-mass-sweep
subsystem: infra
tags: [prettier, format, mass-sweep, ci-health, lint-staged]

# Dependency graph
requires:
  - phase: 27.4.2-02-npm-audit-fix
    provides: Clean dep-bump baseline (lockfile-only, no `package.json` churn) so the prettier diff is the only signal in this commit window — Wave-2 reviewers don't have to disentangle dep + cosmetic diffs
provides:
  - Prettier-canonical formatting across the entire repo (`npx prettier --check .` exits 0)
  - `.claude/` carve-out in `.prettierignore` mirroring Plan 01's `vite.config.ts` `test.exclude` precedent — locked agent-worktree snapshots will never pollute future format/lint sweeps
  - Pre-Wave-2 clean base: 16 of the 19 server drift files were `server/lib/llm*.ts` + `server/__tests__/lib/llm*.test.ts` — exactly the files Wave 2 (Plans 06-10) will touch. Tuning commits will start from canonical formatting and produce reviewable behavioral-only diffs
  - Inline `prefer-const` lint cleanup on `server/lib/llmExtractorWatchdog.ts` (Rule 3 deviation; behavior-preserving 1-line refactor `let` → `const` at declaration site)
affects:
  [
    27.4.2-04-lint-sweep-and-ci-gate,
    27.4.2-05-pre-commit-guard-verification,
    27.4.2-06-tuning-baseline-and-helpers,
    27.4.2-07-tuning-lever-1-poi,
    27.4.2-08-tuning-lever-2-twopass-thresholds,
    27.4.2-09-tuning-lever-3-system-prompt,
    27.4.2-10-tuning-lever-4-news-temporal-bellingcat,
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Cosmetic-isolation commit pattern: an entire `prettier --write .` sweep lands in a single `style(phase): prettier sweep — restore canonical formatting (N files)` commit, separated from any behavioral commit, so reviewers can rubber-stamp the cosmetic diff in one pass and focus their attention on the behavioral diffs that follow (D-05 verbatim).'
    - 'Pre-sweep `.prettierignore` audit: before running a repo-wide `prettier --write .`, audit `.prettierignore` against the actual top-level directory layout. Stale parallel-executor worktrees (`.claude/worktrees/`) were not in the original ignore list and would have polluted the sweep with 595 unwanted reformats. Pattern: when the canonical drift count from `prettier --check .` significantly exceeds the planned-for count, suspect ignore-list staleness BEFORE running `--write`.'
    - 'Plan-04-prep pattern: when the pre-commit lint-staged hook flags a pre-existing lint error on a file already in the cosmetic sweep set, fix it inline as a Rule-3 deviation (it directly blocks completion of the current task) and document the steal-from-Plan-04 scope shift in the SUMMARY. The receiving downstream plan (here Plan 04) reads the SUMMARY chain and adjusts its own scope downward by one finding. This is preferable to either (a) splitting Plan 03 into Plan 03 + Plan 03.5 lint-precursor, which would inflate the wave-1 plan count, or (b) `--no-verify` bypassing the hook, which violates the stated workflow.'

key-files:
  created:
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-03-SUMMARY.md
  modified:
    - .prettierignore (Rule-3 deviation: `.claude/` carve-out for locked agent worktrees)
    - server/lib/llmExtractorWatchdog.ts (Rule-3 deviation: 1-line `let` → `const` at declaration site to satisfy pre-commit eslint hook; the file was already in the prettier sweep set)
    - 31 files in `.planning/` (SUMMARY/HANDOFF/REVIEW markdown)
    - 19 files in `server/` (16 in `server/lib/llm*.ts` + `server/__tests__/lib/llm*.test.ts`; 3 in `server/lib/eventGrouping.ts`, `server/lib/meBounds.ts`, `server/lib/normalizeEventTypes.ts`, `server/routes/events.ts`, `server/__tests__/routes/events.test.ts`, `server/__tests__/lib/eventGrouping.test.ts`, `server/__tests__/adapters/llm-provider.test.ts`, `server/__tests__/adapters/nominatim-forward.test.ts`)
    - 2 files in `src/` (`src/__tests__/DetailPanel.test.tsx`, `src/components/ui/DevApiStatus.tsx`)
    - 2 files in `docs/` (`docs/adr/0009-two-key-split-for-llm-partial-progress-vs-terminal-reads.md`, `docs/adr/README.md`)
    - `PROJECT_STATUS.md` (root)

key-decisions:
  - 'D-05 applied verbatim: single mechanical `npm run format` (= `prettier --write .`) followed by single atomic commit. Cosmetic diff isolated from behavioral diffs.'
  - 'Rule-3 deviation #1: added `.claude/` to `.prettierignore` BEFORE running `prettier --write .`. Without this, the sweep would have reformatted 595 files inside locked agent-worktree snapshots (15 worktrees × ~40 files each), violating acceptance criterion #6. The .prettierignore augmentation was intentional and committed alongside the sweep — it is the canonical guard for all future sweeps in this repo, mirroring Plan 01s vite.config.ts test.exclude precedent for the same locked-worktree pollution.'
  - 'Rule-3 deviation #2: fixed pre-existing `prefer-const` lint error on `softWarnTimer` in `server/lib/llmExtractorWatchdog.ts`. Verified pre-existing via `git stash && eslint`. Required because the pre-commit `lint-staged` hook is a hard gate that ran ESLint over all 21 .ts/.tsx files in the sweep and refused to allow the commit until the error was resolved. Per the workflow rule "Never skip hooks (--no-verify) ... If a hook fails, investigate and fix the underlying issue", a 1-line behavior-preserving fix (declare `const` at assignment site) was the correct unblock. Naturally belongs to Plan 04 (lint-sweep-and-ci-gate); Plan 04 should adjust its scope to subtract this one finding.'
  - 'Pre-existing `npm run build` failure NOT auto-fixed in this plan. Same baseline carve-out as Plan 02: documented in `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md`, owned by Plan 04. Verified the failure is identical pre and post sweep (cosmetic prettier changes cannot affect Rollups module-resolution behavior on the unresolved `@deck.gl/react` and `@deck.gl/aggregation-layers` references in `vite.config.ts` `manualChunks`).'
  - 'Prettier idempotency edge case on `.planning/phases/27.4-llm-enrichment-improvements/27.4-REVIEW-FIX.md`: the file required a second `--write` pass before `--check` reported zero drift. Resolved with one targeted re-write; final repo-wide `--check` exits 0. Documented for downstream plans so they recognize the pattern (reformat + recheck + re-write any holdouts; do not assume single-pass idempotency on every file).'

patterns-established:
  - 'Pattern: pre-sweep ignore-list audit. When the canonical drift count from `prettier --check .` significantly exceeds the planned-for count (here 651 vs predicted 56), suspect an ignore-list gap before running `--write`. Inspect the top-of-list paths to identify the gap, augment `.prettierignore`, re-run `--check`, and only then proceed to `--write`. Saves ~595 unwanted reformats and a likely revert.'
  - 'Pattern: cosmetic + Rule-3 inline-unblock atomic commit. When a pre-commit hook surfaces a pre-existing issue (lint, test, etc.) that lives in a file already part of the cosmetic sweep, fix the issue inline as a documented Rule-3 deviation rather than splitting into a precursor plan. Document the steal-from-downstream-plan scope shift in the SUMMARY so the downstream plan reads the chain and adjusts. Two files in this commit (.prettierignore, llmExtractorWatchdog.ts) needed Rule-3 treatment; the cosmetic diff still dominates and the commit subject correctly reads `style(...)` because the behavioral surface change is two lines (one `let`/`const`, one `.prettierignore` line).'
  - 'Pattern: prettier-idempotency-edge-case re-write. Some markdown structures (mixed list nesting near code fences, certain HTML-in-md patterns, certain unicode runs) trip prettier into a non-fixed-point output on the first `--write` pass. Standard recipe: after `prettier --write .`, run `prettier --check .` and `prettier --write` any flagged file individually, then `prettier --check .` repo-wide one more time. Two passes is sufficient for all observed cases.'

requirements-completed: [D-05]

# Metrics
duration: 10min
completed: 2026-04-25
---

# Phase 27.4.2 Plan 03: prettier-mass-sweep Summary

**Single atomic `style(27.4.2)` commit reformatted 55 prettier-canonical drift files plus a `.claude/` carve-out in `.prettierignore` (Rule 3 deviation) — `npx prettier --check .` now exits 0 across the entire repo, Plan 01's `filters.test.ts` 49/49 still passes, and Wave 2 starts from a clean prettier baseline before tuning the same `server/lib/llm*.ts` files where 16 of the 19 server drift files lived.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-25T08:39:59Z
- **Completed:** 2026-04-25T08:49:54Z
- **Tasks:** 2 of 2 (Task 1: snapshot pre-sweep drift; Task 2: run `npm run format`, address pre-commit gates, commit atomically)
- **Files modified in commit:** 56 (1065 insertions, 818 deletions)
- **Files created:** 1 (this SUMMARY.md)

## Pre-Sweep Drift State

| Scope                        | Pre-sweep drift count | Expected (plan frontmatter)        |
| ---------------------------- | --------------------: | ---------------------------------- |
| Total flagged by `--check`   |                  ~651 | (not in plan; included `.claude/`) |
| `.claude/worktrees/agent-*/` |                   595 | 0 (locked-worktree pollution)      |
| **Canonical (post-ignore)**  |                **55** | **~56** (within ±2 acceptance)     |

### Canonical (55 files) by top-level directory

| Directory    | Count | Notes                                                                                                                                                                                                                                        |
| ------------ | ----: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.planning/` |    31 | SUMMARY/HANDOFF/REVIEW markdown across phases 27, 27.3.2, 27.4, 27.4.1                                                                                                                                                                       |
| `server/`    |    19 | **16 of 19 in `server/lib/llm*.ts` + `server/__tests__/lib/llm*.test.ts`** (the Pitfall 6 concentration); plus `server/lib/eventGrouping.ts`, `server/lib/meBounds.ts`, `server/lib/normalizeEventTypes.ts`, `server/routes/events.ts`, etc. |
| `src/`       |     2 | `src/__tests__/DetailPanel.test.tsx`, `src/components/ui/DevApiStatus.tsx`                                                                                                                                                                   |
| `docs/`      |     2 | `docs/adr/0009-two-key-split-for-llm-partial-progress-vs-terminal-reads.md`, `docs/adr/README.md`                                                                                                                                            |
| repo root    |     1 | `PROJECT_STATUS.md`                                                                                                                                                                                                                          |

### Canonical by extension

| Extension |  Count |
| --------- | -----: |
| `.md`     |     34 |
| `.ts`     |     19 |
| `.tsx`    |      2 |
| **Total** | **55** |

(Plan frontmatter said "(34 .md, 20 .ts, 2 .tsx)" — off by 1 .ts, well within ±2 acceptance tolerance #5.)

### server/lib/llm\*.ts + tests concentration (Pitfall 6 confirmation)

- `server/adapters/llm-provider.ts`
- `server/lib/llmEvalHarness.ts`
- `server/lib/llmEventExtractor.v2.ts`
- `server/lib/llmExtractorWatchdog.ts`
- `server/lib/llmResolver.ts`
- `server/lib/llmSchema.ts`
- `server/lib/llmTokenBudget.ts`
- `server/__tests__/adapters/llm-provider.test.ts`
- `server/__tests__/lib/llmEventExtractor.test.ts`
- `server/__tests__/lib/llmEventExtractor.v2.test.ts`
- `server/__tests__/lib/llmResolver.test.ts`
- `server/__tests__/lib/llmTokenBudget.test.ts`

That's **12 files in the LLM v2 surface area** alone, and another 4 in adjacent files Wave 2 will touch (`server/__tests__/routes/events.test.ts`, `server/lib/eventGrouping.ts`, `server/lib/normalizeEventTypes.ts`, `server/routes/events.ts`). Wave 2 (Plans 06-10) now starts from canonical formatting on every file it will edit — exactly the merge-conflict thrash D-05 was designed to prevent.

## Post-Sweep State

- `npx prettier --check .` reports **"All matched files use Prettier code style!"** (exit 0)
- Working tree clean except for the pre-existing untracked `.claude/` directory (now also ignored by `.prettierignore`)
- Single atomic commit `a25d0c3` with subject `style(27.4.2): prettier sweep — restore canonical formatting (56 files)`

## Task Commits

1. **Task 1: Snapshot pre-sweep drift state** — no commit (information gathering only; baseline saved to `/tmp/prettier-before.txt` and `/tmp/prettier-canonical-drift.txt`)
2. **Task 2: Run `npm run format` and commit atomically** — `a25d0c3` (style)

## Files Created/Modified

### Created

- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-03-SUMMARY.md` — this file

### Modified (in commit `a25d0c3`)

- `.prettierignore` — Rule-3 deviation: appended a `.claude/` line + 5 lines of explanatory comment. Mirrors Plan 01's vite.config.ts `test.exclude` precedent.
- `server/lib/llmExtractorWatchdog.ts` — Rule-3 deviation: 1-line `let softWarnTimer: ReturnType<typeof setTimeout> | undefined;` (line 79) deleted, declaration moved to assignment site as `const softWarnTimer: ReturnType<typeof setTimeout> = setTimeout(...)` (line 97). Plus prettier-driven cosmetic changes elsewhere in the same file.
- 53 other files reformatted by `prettier --write .` per the rules in `.prettierrc.json` (semi+singleQuote+trailingComma=all+printWidth=100+tabWidth=2+arrowParens=always+endOfLine=lf).

## Decisions Made

- **D-05 applied verbatim:** single mechanical `npm run format` pass, single atomic commit, cosmetic diff isolated from behavioral diffs. The plan's `npm run format` script is `prettier --write .` so no command translation was necessary.
- **`.claude/` carve-out (Rule-3 deviation #1):** the original `.prettierignore` only listed `dist`, `coverage`, `node_modules`, `api/*.{mjs,js,cjs}`, `.planning/phases/*/quick/**`, `docs/screenshots`, `docs/hero.gif`, `package-lock.json`. It did NOT list `.claude/`. The first `prettier --check .` flagged 651 files — 595 of them were inside `.claude/worktrees/agent-*/` locked snapshots from a separate workstream (the same parallel-executor pollution Plan 01 carved out from vitest discovery). Per acceptance criterion #6 ("No file outside `.planning/`, `server/`, `src/`, `docs/`, repo root was modified — no `.claude/`"), the carve-out had to land BEFORE `prettier --write .` to satisfy the criterion. Added with explanatory comment that links the precedent and the criterion.
- **`prefer-const` inline fix (Rule-3 deviation #2):** the pre-commit `lint-staged` hook ran ESLint over all 21 .ts/.tsx files in the staged sweep set. It flagged a pre-existing `prefer-const` error on `softWarnTimer` in `server/lib/llmExtractorWatchdog.ts:98`. Verified pre-existing on the base tree via `git stash && npx eslint server/lib/llmExtractorWatchdog.ts` (same error at line 100 pre-prettier — line shifted ~2 lines from quote/spacing changes). The pre-commit hook is a hard gate; per the workflow rule "Never skip hooks (--no-verify) ... investigate and fix the underlying issue", the 1-line `let` → `const` fix at declaration site was the correct unblock. Watchdog tests 4/4 still green post-fix. Naturally belongs to Plan 04 (lint-sweep-and-ci-gate); Plan 04 should subtract this finding from its lint-error inventory.
- **Pre-existing `npm run build` failure NOT auto-fixed.** Same baseline carve-out doctrine as Plan 02 — verified the build failure is identical pre and post sweep (cosmetic prettier changes cannot affect Rollup module resolution). Owned by Plan 04 per `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md`. Per the user prompt's explicit instruction ("Plan 04 will absorb it; do not touch it in this prettier sweep"), this was the right call.
- **Prettier idempotency holdout:** `27.4-REVIEW-FIX.md` was the lone file that did not stabilize on the first `--write` pass. Resolved with one targeted re-write; repo-wide `--check` then reported zero drift. Documented for downstream plans.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `.claude/` to `.prettierignore` to prevent worktree-snapshot pollution**

- **Found during:** Task 1 (pre-sweep drift snapshot revealed 651 files flagged, 595 of them in `.claude/worktrees/agent-*/` locked snapshots from a separate workstream)
- **Issue:** Without the carve-out, `prettier --write .` would have reformatted 595 files outside the allowed `.planning/`, `server/`, `src/`, `docs/`, repo-root scope, directly violating acceptance criterion #6.
- **Fix:** Appended `.claude/` to `.prettierignore` with a 5-line explanatory comment that links the criterion and Plan 01's vite.config.ts test.exclude precedent.
- **Files modified:** `.prettierignore`
- **Verification:** Re-ran `prettier --check .` post-edit; canonical drift count dropped from 651 to 55 (within ±2 of plan-predicted 56).
- **Committed in:** `a25d0c3` (Task 2 commit; .prettierignore was staged as part of `git add -u`)

**2. [Rule 3 - Blocking] Fixed pre-existing `prefer-const` lint error on `softWarnTimer`**

- **Found during:** Task 2 (first `git commit` invocation triggered pre-commit `lint-staged` hook, which ran `eslint --fix` over the 21 .ts/.tsx files in the staged sweep set and refused to allow the commit until the lint error was resolved)
- **Issue:** ESLint `prefer-const` error: `'softWarnTimer' is never reassigned. Use 'const' instead`. Verified pre-existing on the base tree via `git stash && eslint server/lib/llmExtractorWatchdog.ts` (same error at line 100 pre-prettier vs line 98 post-prettier — line shifted ~2 lines from prettier's quote/spacing changes only).
- **Fix:** Removed the `let softWarnTimer: ReturnType<typeof setTimeout> | undefined;` declaration on line 79, moved the type annotation to the assignment site on line 97 (`const softWarnTimer: ReturnType<typeof setTimeout> = setTimeout(...)`). Behavior-preserving: `softWarnTimer` is referenced only at line 135 for `clearTimeout` and was never reassigned post-init.
- **Files modified:** `server/lib/llmExtractorWatchdog.ts` (the file was already in the prettier sweep set; the lint fix is a 2-line subset of the same file's diff)
- **Verification:** `npx eslint server/lib/llmExtractorWatchdog.ts` exits 0 post-fix; `npx vitest run server/__tests__/lib/llmExtractorWatchdog.test.ts` reports 4/4 tests passing.
- **Committed in:** `a25d0c3` (Task 2 commit; folded into the same atomic commit since the file was already in the sweep set and the fix unblocks the same pre-commit hook the cosmetic diff was waiting on).

---

**Total deviations:** 2 auto-fixed (both Rule 3 — blocking).
**Impact on plan:** Both fixes were necessary to satisfy the plan's own acceptance criteria (#6 for the .prettierignore carve-out) and to unblock the pre-commit hook (for the prefer-const lint fix). Neither expanded the plan's stated scope (cosmetic restoration); both kept the plan's atomicity guarantee (single commit). The `prefer-const` fix is the only "scope steal" from a downstream plan — Plan 04 (lint-sweep-and-ci-gate) should subtract this one lint finding from its inventory.

### Documented as Deferred (not auto-fixed)

**Pre-existing `npm run build` failure** (vite.config.ts manualChunks references unresolved `@deck.gl/react` and `@deck.gl/aggregation-layers`) — out-of-scope per SCOPE BOUNDARY rule and per explicit user-prompt instruction ("Plan 04 will absorb it; do not touch it in this prettier sweep"). Already documented in `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md`. Verified via separate `git stash && npm run build` checks during Plan 02 that the failure predates this commit.

## Issues Encountered

- **Stale `.prettierignore` (gap on `.claude/`):** the original ignore list did not include the locked agent-worktree snapshots. Caught at Task 1 by inspecting the `prettier --check` output before running `--write`. Resolved with the Rule-3 deviation #1 above. Lesson encoded as Pattern: pre-sweep ignore-list audit.
- **Pre-commit hook surfaced pre-existing lint error:** the hook ran ESLint over the 21 staged .ts/.tsx files and flagged `prefer-const` on `server/lib/llmExtractorWatchdog.ts`. Resolved with Rule-3 deviation #2 above (1-line refactor, behavior-preserving, watchdog tests 4/4 green). Lesson encoded as Pattern: cosmetic + Rule-3 inline-unblock atomic commit.
- **Prettier idempotency edge case on `27.4-REVIEW-FIX.md`:** required a second `--write` pass before `--check` reported zero drift. Resolved with one targeted re-write. Lesson encoded as Pattern: prettier-idempotency-edge-case re-write.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- ✅ **Plan 04 (lint-sweep-and-ci-gate) ready:** the prettier baseline is now canonical. Plan 04 owns three things: (a) the pre-existing `npm run build` failure (still in `deferred-items.md`); (b) any remaining lint errors NOT including the `softWarnTimer` `prefer-const` already cleaned in this commit; (c) wiring `npm run format:check` and `npm audit --audit-level=high` into the CI gate. **Note:** Plan 04 should re-run `npx eslint .` to refresh its lint-error inventory before scoping its task list — one finding has been pre-paid here.
- ✅ **Plan 05 (pre-commit-guard-verification) ready:** husky + lint-staged + gitleaks all ran cleanly on this commit (lint-staged formatted .md/.json/.css with prettier and ts/tsx with eslint+prettier; gitleaks scanned 0 commits, 0 leaks). The pre-commit pipeline is functional and gated correctly before Plan 05 hardens it.
- ✅ **Wave 2 (Plans 06-10) ready:** the 16 files at the heart of the LLM v2 tuning surface (`server/lib/llm*.ts` + `server/__tests__/lib/llm*.test.ts`) are now canonical. Tuning commits will produce reviewable behavioral-only diffs without the prettier whitespace/quote/comma noise that was conflated with the post-Phase-27.4.1 work.

### Note for downstream plans

- **Plan 04 lint-error inventory shrank by one finding** — the `prefer-const` on `softWarnTimer` in `server/lib/llmExtractorWatchdog.ts` was inlined here under Rule 3. Plan 04 should re-run `npx eslint .` against the post-`a25d0c3` tree to get a fresh count.
- **`.prettierignore` now includes `.claude/`** — any future plan that introduces files inside `.claude/` (e.g., new agent worktrees from a parallel workstream) will not have those files reformatted by future `npm run format` runs. This is intentional and matches the locked-worktree-isolation doctrine established by Plan 01.
- **Two-pass prettier idempotency recipe:** for any plan that runs `prettier --write .` in the future, use the recipe from Pattern 3: `--write .` → `--check .` → `--write` any holdouts → `--check .` repo-wide one more time. One file in this sweep needed it; downstream plans should not assume single-pass idempotency.

## Self-Check: PASSED

- `.prettierignore` modified — FOUND in commit `a25d0c3` (with `.claude/` line and explanatory comment, head reads `.claude/`)
- `server/lib/llmExtractorWatchdog.ts` modified — FOUND in commit `a25d0c3` (let → const refactor at line 78-79 and 97; lint passes)
- 56 files staged + committed — VERIFIED (`git show --stat HEAD` reports `56 files changed, 1065 insertions(+), 818 deletions(-)`)
- Commit `a25d0c3` — FOUND in `git log` with subject `style(27.4.2): prettier sweep — restore canonical formatting (56 files)`
- `npx prettier --check .` exits 0 — VERIFIED (output: `All matched files use Prettier code style!`)
- `npx vitest run src/__tests__/filters.test.ts` — 49/49 passing post-commit (Plan 01 preserved)
- `npx vitest run server/__tests__/lib/llmExtractorWatchdog.test.ts` — 4/4 passing post-Rule-3-fix
- Acceptance criterion #6 — VERIFIED (categorization showed `planning=31 server=19 src=2 docs=2 root=1 prettierignore=1 other=0`)
- Pre-commit hooks ran clean post-Rule-3-fix — VERIFIED (lint-staged eslint+prettier passed; gitleaks 0 leaks)
- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-03-SUMMARY.md` — FOUND (this file)

---

_Phase: 27.4.2-ci-health-and-llm-v2-tuning_
_Plan: 03-prettier-mass-sweep_
_Completed: 2026-04-25_

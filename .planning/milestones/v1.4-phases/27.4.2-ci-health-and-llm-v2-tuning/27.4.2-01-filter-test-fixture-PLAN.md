---
phase: 27.4.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/filters.test.ts
autonomous: true
requirements_addressed: [D-03, D-17]
nyquist_compliant_intent: true
user_setup: []
tags: [ci-health, test-fixture, filter, vitest]

must_haves:
  truths:
    - 'src/__tests__/filters.test.ts vitest run reports 0 failures (down from 544)'
    - 'src/lib/filters.ts production contract is unchanged (enabledPrecisions remains non-nullable)'
    - "makeDefaults() returns a FilterState shape that includes enabledPrecisions=['exact','neighborhood','city','region']"
  artifacts:
    - path: 'src/__tests__/filters.test.ts'
      provides: 'FilterState test factory with enabledPrecisions backfilled per D-03'
      contains: "enabledPrecisions: ['exact', 'neighborhood', 'city', 'region']"
  key_links:
    - from: 'src/__tests__/filters.test.ts makeDefaults()'
      to: 'src/lib/filters.ts:147 entityPassesFilters precision branch'
      via: 'FilterState shape used by every test in this file'
      pattern: "filters\\.enabledPrecisions"
---

<objective>
Land the single-line fixture-factory extension that greens the entire `src/__tests__/filters.test.ts` suite (currently 544 cascading failures from one root cause: `filters.enabledPrecisions` is undefined in the test factory, and `src/lib/filters.ts:147` reads `filters.enabledPrecisions.length < 4` on every combined-filter assertion).

Purpose: Restore the test signal so subsequent CI gates (P2..P5) can be validated reliably, and so Wave 2 LLM tuning has a green-tests baseline to measure regressions against per D-02 strict CI gate.

Output: Modified `src/__tests__/filters.test.ts` with `enabledPrecisions` added to `makeDefaults()` and one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md
@src/__tests__/filters.test.ts
@src/lib/filters.ts
@src/stores/filterStore.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/stores/filterStore.ts (line ~46):

```typescript
export type PrecisionTier = 'exact' | 'neighborhood' | 'city' | 'region';
```

From src/lib/filters.ts (line ~147 — the crash site):

```typescript
// Crashes today with: Cannot read properties of undefined (reading 'length')
if (filters.enabledPrecisions.length < 4) { ... }
```

From src/**tests**/filters.test.ts:10 (the function to extend):

```typescript
function makeDefaults(): FilterState { return { ... } }  // missing enabledPrecisions
```

From src/**tests**/filters.test.ts:66 (the precedent for the array-of-string-literal field with `as` cast):

```typescript
enabledSiteTypes: ['nuclear', 'naval', 'oil', 'airbase', 'port'] as import('@/types/entities').SiteType[],
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add enabledPrecisions to makeDefaults() factory</name>
  <files>src/__tests__/filters.test.ts</files>
  <read_first>
    - src/__tests__/filters.test.ts (full file — see current makeDefaults at lines 10-95; identify insertion point)
    - src/lib/filters.ts (lines 140-160 — confirm the read site `filters.enabledPrecisions.length < 4`)
    - src/stores/filterStore.ts (lines 40-50 — confirm `PrecisionTier` type)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-03 lines 99-113 — verbatim decision)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md (lines 39-72 — exact insertion shape)
  </read_first>
  <behavior>
    - Test 1: every existing `expect(entityPassesFilters(...)).toBe(...)` assertion in src/__tests__/filters.test.ts now passes (was 544 failing)
    - Test 2: makeDefaults() return value satisfies the `FilterState` type (the file annotates it `: FilterState`)
    - Test 3: production code in src/lib/filters.ts is byte-identical to its pre-plan state (no `?.length`, no signature changes)
  </behavior>
  <action>
Add the following one-line property to the object literal returned by `makeDefaults()` in `src/__tests__/filters.test.ts`. Insert it AFTER the `setShowLowSeverity: () => {},` callback (around line 64 in the current file) and BEFORE the existing `// Site type toggles` comment at line 65, mirroring the `enabledSiteTypes` precedent shape (line 66) of `array-of-string-literal as ImportedType[]`:

```typescript
    // Phase 27.4.2 D-03 — backfill of Phase 27 precision filter shipped with
    // src/lib/filters.ts:147 reading filters.enabledPrecisions.length < 4.
    // Default value matches src/stores/filterStore.ts DEFAULTS.
    enabledPrecisions: ['exact', 'neighborhood', 'city', 'region'] as import('@/stores/filterStore').PrecisionTier[],
```

DO NOT:

- Modify `src/lib/filters.ts` (D-03 explicitly forbids softening the production contract via `?.length`).
- Backfill the other 25 missing fields proactively (Pitfall 1 / RESEARCH §6 — only `enabledPrecisions` is read by `entityPassesFilters`; adding other fields turns a 1-line cosmetic into a 25-line behavioral diff).
- Add `enabledPrecisions` to any other test file in this plan.

After the edit, run `npx vitest run src/__tests__/filters.test.ts` and confirm 0 failures (the previously-failing 544 assertions all derive from the same crash site).

Commit message:

```
fix(27.4.2): backfill enabledPrecisions in filters.test.ts makeDefaults

Greens 544 cascading failures from a single root cause — the test
fixture predates Phase 27's enabledPrecisions filter and crashes
at src/lib/filters.ts:147 (filters.enabledPrecisions.length < 4)
on every combined-filter assertion. Per CONTEXT D-03, fix the
fixture, not the production contract.
```

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/filters.test.ts 2>&1 | tail -20 | grep -E "(Test Files|Tests)" && npx vitest run src/__tests__/filters.test.ts --reporter=verbose 2>&1 | grep -c "FAIL" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/filters.test.ts` exits 0
    - `grep -c "enabledPrecisions: \\['exact'" src/__tests__/filters.test.ts` returns 1
    - `grep -c "enabledPrecisions" src/lib/filters.ts` is unchanged from baseline (no `?.length` introduced — confirm production contract preserved)
    - `git diff --stat src/lib/filters.ts` shows zero changes (production code untouched)
    - The exact insertion uses `as import('@/stores/filterStore').PrecisionTier[]` (matching the `enabledSiteTypes` precedent at line 66)
  </acceptance_criteria>
  <done>
    Filter test suite green; production contract preserved; single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                     |
| ---------------------------------- | --------------------------------------------------------------- |
| test-fixture → production-contract | Test code defaulting must NOT loosen production type signatures |

## STRIDE Threat Register

| Threat ID   | Category                                      | Component           | Disposition | Mitigation Plan                                                                                                                                                     |
| ----------- | --------------------------------------------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-27.4.2-02 | T (Tampering)                                 | src/lib/filters.ts  | mitigate    | Acceptance criterion #4: `git diff --stat src/lib/filters.ts` shows zero changes. The fix lives in `src/__tests__/filters.test.ts` only — never in production code. |
| T-27.4.2-01 | E (Elevation of Privilege via process bypass) | git pre-commit hook | mitigate    | Plan P5 verifies the husky hook prevents `--no-verify` drift; this plan is the test that the hook then guards.                                                      |

</threat_model>

<verification>
Run `npx vitest run src/__tests__/filters.test.ts` — must report 0 failures across all test files in the run. Run `git diff src/lib/filters.ts` — must show empty output (no production-code changes). Run `grep -n "enabledPrecisions" src/__tests__/filters.test.ts` — must show exactly one line in `makeDefaults()`.
</verification>

<success_criteria>

- 544 → 0 failures in `src/__tests__/filters.test.ts`
- `src/lib/filters.ts` unchanged byte-for-byte
- Single atomic commit `fix(27.4.2): backfill enabledPrecisions in filters.test.ts makeDefaults`
- VALIDATION.md Wave 0 row for `src/__tests__/filters.test.ts:10` checked
  </success_criteria>

<output>
After completion, create `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-01-SUMMARY.md` documenting:
- Pre/post failure count: 544 → 0
- File of insertion: `src/__tests__/filters.test.ts` makeDefaults() line N
- Commit SHA
- Confirmation that `src/lib/filters.ts` was NOT modified
- Any sibling test files now passing as a side effect (if any)
</output>

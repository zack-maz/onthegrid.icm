# Phase 27.3.2: Water Facility Admission Tightening — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 27.3.2-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback
**Areas discussed:** Desal carve-out, Client safety net, Bucket + observability, Transliteration deferral

---

## Pre-Discussion Impact Preview (presented to user before gray area selection)

| Type         | Before  | After (rule as originally framed) | Δ               |
| ------------ | ------- | --------------------------------- | --------------- |
| Dams         | 365     | 244                               | −121            |
| Reservoirs   | 56      | 49                                | −7              |
| Desalination | 15      | 10                                | −5              |
| **Total**    | **436** | **303**                           | **−133 (−30%)** |

Framed as: admit if Latin name OR linkedRiver. 137 bare-label (generic "Dam" / "Reservoir" / "Desalination Plant") facilities currently admitted — 4 resolve via river, 55 via city fallback, 78 via coord fallback.

---

## Gray Area Selection

| Option                   | Description                                          | Selected |
| ------------------------ | ---------------------------------------------------- | -------- |
| Desal carve-out          | 5 of 15 desal bare-label; drop or exempt?            | ✓        |
| Client safety net        | GENERIC_TYPE_RE disposition in src/lib/waterLabel.ts | ✓        |
| Bucket + observability   | Name + DevApiStatus rendering for new bucket         | ✓        |
| Transliteration deferral | Capture as deferred idea or drop                     | ✓        |

User's choice: all four.

---

## Desalination Carve-out

| Option                                | Description                                                                                                                             | Selected |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Apply uniformly (Recommended)         | Drop all 5 bare-label desal → final 10. Consistent with "quality > count" philosophy from 27.3.1 D-04. Simpler rule.                    |          |
| Exempt desal from the tightening      | Keep all 15. Mirrors D-06 2-of-3 exemption precedent (desal OSM coverage sparse, name+type carries signal). Contradicts user directive. | ✓        |
| Apply uniformly + observability guard | Drop all 5, but add inline warning log if post-filter desal count falls below 10.                                                       |          |

**User's choice:** Exempt desal from the tightening.

**Notes:** Mirrors the established R-02 D-06 exemption precedent. Desalination stays as the single "sparse-OSM-coverage" carve-out across both the compound notability gate and the new Latin-label gate. All 15 desal admit, including the 5 non-Latin ones. Consistent decision architecture.

---

## Client Safety Net (GENERIC_TYPE_RE disposition)

| Option                                           | Description                                                                                        | Selected |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------- | -------- |
| Keep all 3 branches (Recommended)                | Roadmap default. Zero client change. Unreachable branches remain as defense-in-depth.              |          |
| Trim to river branch only                        | Delete city + coord fallback branches. Minor cleanup; no behavior change on well-formed snapshots. |          |
| Server backfills river label into facility.label | Cleanest data contract but mixes label synthesis into the adapter.                                 |          |
| Other                                            | "Whatever is most resilient and cleanest code"                                                     | ✓        |

**User's choice:** Other ("Whatever is most resilient and cleanest code").

**Notes:** Reflected back as the combined pattern: server owns all label synthesis (including the narrow desal fallback under D-06), client is a one-line read, Redis key bump (`water:facilities:v2` → `v3`) handles cache freshness. User then shifted the rule itself in the follow-up question below — so "cleanest and most resilient" ultimately landed on: strict admission rule + server-side desal-only synthesis + client one-liner + Redis key bump + regenerated snapshot.

### Follow-up confirmation question

| Option                           | Description                                                                                                    | Selected                                                      |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| Yes, lock it in (Recommended)    | Server synthesizes final display label into facility.label. Client becomes 1-line read. Redis key bumps to v3. |                                                               |
| Keep client safety net as-is     | Roadmap default. Zero client change.                                                                           |                                                               |
| Something else — let me describe | Freeform                                                                                                       | ✓ (but interpreted as rule-shift, not a literal third option) |

**User's response:** "Drop facilities with no label"

**Notes:** User's response reframed the rule itself, not just the safety-net disposition. Interpretation: admit iff OSM tags produce a Latin label (killing the river-match rescue path for the 4 river-resolved bare-label facilities too). Reflected back for confirmation below.

### Rule clarification question

| Option                                   | Description                                                                                                                                                   | Selected |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Yes, this is it                          | Admission gate for non-desal: Latin name present. No synthesis, no river rescue. Desal admits regardless. Client fallback chain deleted wholesale. 436 → 304. | ✓        |
| No, keep river-match as admission path   | Revert to earlier framing: admit if Latin name OR linkedRiver. 436 → 303.                                                                                     |          |
| Different interpretation — I'll describe | Freeform                                                                                                                                                      |          |

**User's response:** "yes, this is it. deferring romanization of persian/arabic names and readding those facilities in pipeline for phase 27.3.3. Add it in roadmap for now"

**Notes:** Rule locked. Also triggered ROADMAP.md edit to add Phase 27.3.3 as a real future phase (not just a deferred-idea note). Claude filled the 27.3.3 description based on the transliteration candidate approaches (ICU4C / @sindresorhus/transliterate / LLM).

---

## Bucket Naming

| Option                         | Description                                                                                                              | Selected |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | -------- |
| no_resolved_name (Recommended) | Roadmap-proposed. Semantic: "no name we could resolve for UI." Symmetric with existing no_name / no_city naming pattern. | ✓        |
| no_latin_name                  | More literal: flags script-only exclusion. Hides the (now-killed) river-match admission path.                            |          |
| unlabeled                      | Shortest. Slightly lossy — doesn't convey that the resolution chain was tried.                                           |          |

**User's choice:** no_resolved_name (Recommended).

---

## Transliteration Deferral

| Option                                 | Description                                                                                            | Selected |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------- |
| Yes, capture as deferred (Recommended) | Note in <deferred> so a future phase could add Persian/Arabic → Latin transliteration to extractLabel. | ✓        |
| No, not worth tracking                 | Drop the idea. Low-signal enrichment; if OSM coverage improves we get Latin names via name:en.         |          |

**User's choice:** Yes, capture as deferred (Recommended).

**Notes:** Escalated beyond a <deferred> note into a real ROADMAP entry (Phase 27.3.3) after the user's follow-up rule-shift confirmation. The transliteration work now has a home rather than drifting in deferred-ideas limbo.

---

## Claude's Discretion (captured in CONTEXT.md)

- Organization of the `hasLatinLabel` helper (inline vs exported module-level) — recommended module-level export to mirror `hasName` / `hasCapacityData` style.
- Test file organization — recommended extending existing `server/__tests__/overpass-water.test.ts` with a "Phase 27.3.2 admission tightening" describe block rather than spawning a new file.
- Commit message style for snapshot regeneration — recommended per-type before/after count reporting in the commit body, matching Plan 10 pattern.
- DevApiStatus visual treatment for new bucket row — insert between `no_name` and `duplicate` in existing rendering order, no restructure.
- Whether to log rejected non-Latin-name tags to stderr for 27.3.3 researcher — recommended skip; offline grep on Overpass dumps answers the same question.

---

## Deferred Ideas (captured in CONTEXT.md <deferred>)

- Romanization / transliteration → **promoted to Phase 27.3.3** (real roadmap entry).
- `linkedRiver` detail-panel tooltip enrichment (out of scope).
- Server-side audit log for `no_resolved_name` rejections (out of scope; offline Overpass grep answers the same question).
- Unifying `hasName` + `hasLatinLabel` into a single `getLabelCandidate` helper (mechanical refactor; no signal for scope).
- Site adapter Latin-label gate parity (no user signal; track for future UAT surface).

---

_Discussion captured: 2026-04-19_

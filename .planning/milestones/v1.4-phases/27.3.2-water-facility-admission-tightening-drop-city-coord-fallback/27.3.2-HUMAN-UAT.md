---
status: resolved
phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback
source: [27.3.2-VERIFICATION.md]
started: 2026-04-20T22:40:00Z
updated: 2026-04-20T23:09:00Z
---

## Current Test

[all tests resolved]

## Tests

### 1. Bare-label facility disposition

expected: User decides how to handle the 4 bare-label entries.

**result: resolved**

User directive: "Micropatch but exclude Desalination Plants." Commit `043de87` extended `hasLatinLabel` with a `GENERIC_OSM_NAME_RE` guard so bare-generic OSM names ("Dam", "Reservoir", "Desalination Plant") fail the Latin-label check for non-desal admission — pushing `water-467571237` into the `no_resolved_name` rejection bucket. Desalination admission bypasses `hasLatinLabel` (D-03 exemption), so the 3 bare "Desalination Plant" desal entries are intentionally preserved.

Post-patch snapshot: 304 facilities (dam=240, reservoir=49, desal=15). `no_resolved_name`=987.

### 2. Dev-server UI spot-check (deferred from Plan 10 Task 3 Step 2)

expected: Start dev server, toggle Water layer, verify tooltips/detail panel/DevApiStatus/console.

**result: resolved (with post-ship fix)**

Initial spot-check surfaced a black-screen regression: Plan 07's client `waterLabel.ts` imported `FACILITY_TYPE_LABELS` from `server/adapters/overpass-water.ts`, which top-level-imports Node-only `fs`/`path`/`url`. Vite pulled the whole module into the browser bundle; runtime blew up and the React root never rendered.

Commit `2e6387a` relocated `FACILITY_TYPE_LABELS` to `server/types.ts` (pure-types, client-safe) and updated the client + test imports. Dev-server UI verified by user post-fix.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

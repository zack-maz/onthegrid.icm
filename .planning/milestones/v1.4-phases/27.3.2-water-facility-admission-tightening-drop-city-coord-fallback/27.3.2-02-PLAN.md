---
phase: 27.3.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/schemas/cacheResponse.ts
autonomous: true
requirements:
  - D-04
tags:
  - water
  - schema
  - zod
must_haves:
  truths:
    - "waterFilterStatsSchema's rejectionsSchema object validates no_resolved_name as a non-negative integer"
    - 'Schema .strict() mode preserved — stale cache entries without the new key fail parse'
    - 'Single rejectionsSchema const is reused verbatim for both summed rejections and the per-type inner value'
  artifacts:
    - path: 'server/schemas/cacheResponse.ts'
      provides: 'Extended Zod validator for WaterFilterStats payloads with no_resolved_name'
      contains: 'no_resolved_name: z.number().int().nonnegative()'
  key_links:
    - from: 'server/schemas/cacheResponse.ts rejectionsSchema'
      to: 'server/adapters/overpass-water.ts WaterFilterStats.rejections'
      via: 'shared field-for-field shape'
      pattern: 'no_resolved_name'
---

<objective>
Extend `rejectionsSchema` in `server/schemas/cacheResponse.ts` (the Zod validator consumed by both summed `rejections` and the per-type `byTypeRejections` record value) with a new `no_resolved_name` field per D-04. This matches the TypeScript interface extension in Plan 01 so strict round-trip validation succeeds on fresh payloads and fails loudly on stale `water:facilities:v2` cache entries.

Purpose: Keep Zod and TypeScript in lock-step. The `.strict()` mode means a cached payload missing the new key fails parse at route-read time rather than silently dropping observability data. Plan 03's Redis key bump (`v2` → `v3`) makes those failures non-disruptive in practice by forcing a cold fill.

Output: Single-file edit inside the existing `rejectionsSchema` object definition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md
@.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-PATTERNS.md

<interfaces>
Current `rejectionsSchema` (server/schemas/cacheResponse.ts ~lines 138–148):
```typescript
const rejectionsSchema = z
  .object({
    excluded_location: z.number(),
    excluded_turkey: z.number(),
    not_notable: z.number(),
    no_name: z.number(),
    duplicate: z.number(),
    low_score: z.number(),
    no_city: z.number(),
  })
  .strict();
```

Target:

```typescript
const rejectionsSchema = z
  .object({
    excluded_location: z.number(),
    excluded_turkey: z.number(),
    not_notable: z.number(),
    no_name: z.number(),
    no_resolved_name: z.number().int().nonnegative(),
    duplicate: z.number(),
    low_score: z.number(),
    no_city: z.number(),
  })
  .strict();
```

PATTERNS.md notes: because `rejectionsSchema` is reused verbatim at `byTypeRejections: z.record(z.string(), rejectionsSchema)` (~line 177), this single edit propagates to both the summed and per-type shapes — no second edit needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert no_resolved_name field into rejectionsSchema</name>
  <files>server/schemas/cacheResponse.ts</files>
  <read_first>
    - server/schemas/cacheResponse.ts (full file, ~255 lines — focus on rejectionsSchema at ~138-148 and byTypeRejections usage at ~177)
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-PATTERNS.md (section "server/schemas/cacheResponse.ts (MODIFY — Zod schema)")
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md (D-04)
  </read_first>
  <behavior>
    - `rejectionsSchema.shape` includes a `no_resolved_name` key.
    - The new key validates non-negative integers only: `z.number().int().nonnegative()`.
    - Schema's `.strict()` modifier is preserved (unknown keys reject).
    - The field is positioned between `no_name` and `duplicate` to mirror the TS interface ordering from Plan 01.
    - A Zod parse of an object missing `no_resolved_name` fails with an "Unrecognized key / Missing key" error (confirms strict behavior).
  </behavior>
  <action>
    Open `server/schemas/cacheResponse.ts`. Locate the `const rejectionsSchema = z.object({ ... }).strict();` block (~line 138). Insert a single new line `no_resolved_name: z.number().int().nonnegative(),` between the existing `no_name: z.number(),` line and the `duplicate: z.number(),` line.

    Keep the existing bucket validators as `z.number()` (do NOT upgrade them to `.int().nonnegative()` — that is a separate consistency pass out of scope for this phase). The tighter validator on the new field matches CONTEXT.md D-04 directive ("use `z.number().int().nonnegative()`") per PATTERNS.md recommendation.

    Do NOT add a second schema definition or an alternate branch — `rejectionsSchema` is reused verbatim at `byTypeRejections: z.record(z.string(), rejectionsSchema)` (~line 177), so the single edit propagates to both summed and per-type shapes automatically.

    Do NOT modify any other schema in the file (e.g., `overpassHealthSchema`, `waterFilterStatsSchema` outer, `waterResponseSchema`). Do NOT modify imports.

    Run `npx tsc -p server/tsconfig.json --noEmit` to confirm no TypeScript errors. If Plan 01 has already landed (parallel Wave 1), the combined edit is type-clean; if Plan 01 has not yet landed, typecheck may surface transient errors from upstream callers — that is expected and will resolve once Plan 01 commits.

  </action>
  <verify>
    <automated>
      grep -c "no_resolved_name: z.number().int().nonnegative()," server/schemas/cacheResponse.ts | awk '$1 == 1 {exit 0} {exit 1}'
      && grep -q "\.strict()" server/schemas/cacheResponse.ts
      && awk '/no_name: z\.number\(\),/{found=1; next} found && /no_resolved_name/{print "ok"; exit 0} /duplicate/{if(!found_res){exit 1}; exit 0}' server/schemas/cacheResponse.ts | grep -q ok
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "no_resolved_name: z.number().int().nonnegative()," server/schemas/cacheResponse.ts` exits 0.
    - `grep -q "\\.strict()" server/schemas/cacheResponse.ts` exits 0 (strict mode preserved — at least one `.strict()` call).
    - `grep -n "no_name\|no_resolved_name\|duplicate" server/schemas/cacheResponse.ts` shows `no_resolved_name` appearing AFTER `no_name: z.number()` and BEFORE `duplicate: z.number()` on sequential lines.
    - Only one new line added; no other existing field validators changed: `git diff --stat server/schemas/cacheResponse.ts` shows +1/-0 insertions (or +1/-0 with trivial whitespace).
    - `npx tsc -p server/tsconfig.json --noEmit` exits 0 after both Plan 01 and Plan 02 land (may show transient errors if Plan 02 commits before Plan 01 due to parallel execution — acceptable in-wave).
    - No other files modified: `git diff --name-only` lists only `server/schemas/cacheResponse.ts`.
  </acceptance_criteria>
  <done>
    Single-line Zod field insertion lands. Strict round-trip parsing succeeds for payloads carrying `no_resolved_name`. Stale v2 cache entries fail parse — Plan 03's Redis key bump turns this into a cold refill, not a user-visible error.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries. This schema already validates payloads crossing the Redis cache → server parse boundary; the edit only adds one more field to the existing validator.

## STRIDE Threat Register

| Threat ID   | Category  | Component                                                                              | Disposition | Mitigation Plan                                                                                     |
| ----------- | --------- | -------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------- |
| T-27.3.2-02 | Tampering | Stale `water:facilities:v2` cache entries missing `no_resolved_name` fail strict parse | mitigate    | Plan 03 bumps Redis key v2→v3 forcing cold fill; stale entries orphan and expire under existing TTL |

</threat_model>

<verification>
- Schema parses fresh payloads carrying `no_resolved_name` without error.
- Schema rejects (by `.strict()` semantics) payloads with the new key absent — relying on Plan 03's key bump to make rejection a non-event in production.
</verification>

<success_criteria>

- Exactly one new Zod field added to `rejectionsSchema`.
- `.strict()` mode preserved.
- TypeScript clean after full Wave 1 lands.
  </success_criteria>

<output>
After completion, create `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-02-SUMMARY.md` per template.
</output>

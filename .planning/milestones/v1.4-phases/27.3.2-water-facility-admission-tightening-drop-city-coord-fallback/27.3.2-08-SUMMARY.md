---
phase: 27.3.2
plan: 08
subsystem: water-label-client
tags:
  - water
  - client
  - tests
  - wave3
requirements:
  - D-10
  - D-17
dependency-graph:
  requires:
    - 27.3.2-07 (collapsed getWaterFacilityDisplayName one-liner — this plan's test suite asserts its shape)
    - 27.3.2-05 (extractLabel desal-synthesis branches — Test 3 encodes the synthesized format "Desalination Plant near Jeddah")
  provides:
    - '3-case regression lock on the collapsed getWaterFacilityDisplayName contract (trim + fallback + pass-through)'
    - 'Removal of the D-10 audit test case (dead per CONTEXT.md D-10: non-Latin-only admission now rejected via hasName tightening + Plan-05 desal synthesis)'
  affects:
    - 'None (tests only, no runtime path)'
tech-stack:
  added: []
  patterns:
    - 'Import FACILITY_TYPE_LABELS from server/adapters/overpass-water to assert fallback equivalence in Test 2 — single source of truth, no client-side duplicate'
    - 'Preserve stubStress + makeFacility Zustand-compatible scaffolding (matches 27.3.2-PATTERNS.md test pattern)'
key-files:
  created:
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-08-SUMMARY.md
  modified:
    - src/lib/__tests__/waterLabel.test.ts
decisions:
  - "Wrote the test file against the post-Plan-07 collapsed contract (per wave_context directive). Test 2 asserts that empty label returns FACILITY_TYPE_LABELS.reservoir — this only passes once Plan 07's collapse lands; in an un-collapsed state it fails (the 91-line fallback chain falls through to coordinate-based label for empty labels). Verified locally by temporarily installing the collapsed waterLabel.ts shape: all 3 tests pass cleanly."
  - "Did NOT preserve the D-10 audit case. Per plan acceptance criterion `! grep -q 'D-10 audit'` and CONTEXT.md D-10: the audit case exercised a path (non-Latin-only-name admission → bare-type label → GENERIC_TYPE_RE sentinel) that is gone post-27.3.2 (admission rejects non-Latin-only-name non-desal via hasLatinLabel; desal synthesizes via extractLabel branch 4/5). The test targets a behavior that no longer exists."
  - "Kept imports relative (`'../../../server/adapters/overpass-water'` and `'../../../server/types'`) rather than introducing path aliases — matches the 5 existing sibling files that already use this relative form in src/. No alias migration attempted."
  - 'Reused the existing stubStress + makeFacility helpers verbatim (field shapes already match the live WaterStressIndicators snake_case + WaterFacility shape in server/types.ts — verified at server/types.ts L161-206). No adjustments needed.'
metrics:
  duration: ~5 minutes
  tasks-completed: 1
  files-modified: 1
  completed-date: 2026-04-20
---

# Phase 27.3.2 Plan 08: Collapsed waterLabel Test Rewrite Summary

Replaced 10 obsolete test cases covering the deleted 3-branch fallback chain (river/city/coord) + deleted sentinels (NEAR_UNKNOWN_RE, GENERIC_TYPE_RE, WATER_TYPE_LABELS) + dead D-10 audit case with 3 focused unit tests that exercise the collapsed `getWaterFacilityDisplayName` one-liner contract. Final file: 50 lines (down from 127).

## What Changed

**Test file replaced: `src/lib/__tests__/waterLabel.test.ts`**

Pre-edit (127 lines, 10 `it` cases, describe title "Phase 27.3 Plan 05"):

1. `falls through to linkedRiver when label is the bare generic "Reservoir"` (deleted — linkedRiver fallback removed in Plan 07)
2. `falls through to linkedRiver when label is the bare generic "Dam"` (deleted — same)
3. `falls through to nearestCity when label is "Desalination Plant" and no river` (deleted — nearestCity fallback removed; desal synthesis now server-side per Plan 05)
4. `falls through to coordinate when label is "Dam" and no enrichment` (deleted — coord fallback removed in Plan 07)
5. `PRESERVES a substantive label like "Mosul Dam"` (deleted — superseded by new Test 1)
6. `PRESERVES "Hub Dam" as-is` (deleted — substring-sentinel distinction no longer exists post-collapse)
7. `is case-insensitive — "reservoir" (lowercase) is also a sentinel` (deleted — GENERIC_TYPE_RE gone)
8. `strips trailing whitespace before sentinel check` (deleted — GENERIC_TYPE_RE gone; trim semantics still verified implicitly in Test 1)
9. `Plan 04 regression guard — "Dam near Unknown" still triggers fallback` (deleted — NEAR_UNKNOWN_RE gone; source of sentinel strings removed across Phase 27.3 Plan 04 + Phase 27.3.1 Plan 10)
10. `D-10 audit: non-Latin-only OSM name path produces bare-type label, sentinel catches it` (deleted per CONTEXT.md D-10 — path no longer reachable after Plan-05 desal synthesis and hasLatinLabel admission gate)

Post-edit (50 lines, 3 `it` cases, describe title "Phase 27.3.2 — server-owned labels"):

1. `returns trimmed facility.label when present (Latin-named facility)` — asserts `'Mosul Dam'` → `'Mosul Dam'`
2. `returns FACILITY_TYPE_LABELS fallback when label is empty (defensive)` — asserts `label: ''` → `FACILITY_TYPE_LABELS.reservoir`
3. `passes synthesized desal label through unchanged` — asserts `'Desalination Plant near Jeddah'` → `'Desalination Plant near Jeddah'`

Preserved scaffolding: `stubStress: WaterStressIndicators` (8 fields, snake_case matching server/types.ts L163-172) + `makeFacility(overrides)` factory (8 required fields matching WaterFacility L174-183).

## Acceptance Criteria Verification

| Criterion                                                            | Result                                                                                                                                                                                                 |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `grep -cE "^\s*it\(" src/lib/__tests__/waterLabel.test.ts` returns 3 | PASS (3)                                                                                                                                                                                               |
| `grep -q "Phase 27.3.2"`                                             | PASS                                                                                                                                                                                                   |
| `grep -q "server-owned labels"`                                      | PASS                                                                                                                                                                                                   |
| `! grep -q "NEAR_UNKNOWN_RE\|GENERIC_TYPE_RE\|WATER_TYPE_LABELS"`    | PASS                                                                                                                                                                                                   |
| `! grep -q "D-10 audit"`                                             | PASS                                                                                                                                                                                                   |
| `grep -q "getWaterFacilityDisplayName"`                              | PASS                                                                                                                                                                                                   |
| `grep -q "FACILITY_TYPE_LABELS"`                                     | PASS                                                                                                                                                                                                   |
| `grep -q "Desalination Plant near Jeddah"`                           | PASS                                                                                                                                                                                                   |
| `grep -q "Mosul Dam"`                                                | PASS                                                                                                                                                                                                   |
| Post-edit line count in 40-60 range                                  | PASS (50)                                                                                                                                                                                              |
| `npx vitest run src/lib/__tests__/waterLabel.test.ts` green          | PASS against collapsed waterLabel.ts (verified locally by installing Plan-07 shape); FAILS against the 91-line pre-collapse file in this worktree (Test 2 — expected per wave-coordination, see below) |

## Wave 3 Coordination Note (expected runtime failure in isolation)

This plan's test file is authored against the post-Plan-07 collapsed contract (`facility.label?.trim() || FACILITY_TYPE_LABELS[facility.facilityType]`). Plan 07 runs concurrently in a sibling worktree and collapses `src/lib/waterLabel.ts` from 91 lines to ~20 lines.

In MY worktree (Wave 3 Plan 08), `src/lib/waterLabel.ts` is still the 91-line fallback chain at HEAD. Running `npx vitest run src/lib/__tests__/waterLabel.test.ts` against this un-collapsed file yields:

```
 Test Files  1 failed (1)
      Tests  1 failed | 2 passed (3)
 FAIL > returns FACILITY_TYPE_LABELS fallback when label is empty (defensive)
   expected 'Reservoir at 35.70°N, 51.40°E' to be 'Reservoir'
```

This is NOT a test-authoring bug — it is a dependency-on-Plan-07 constraint. Local verification confirms all 3 tests pass cleanly against the Plan-07 collapsed shape:

```bash
# Temporarily installed /tmp/collapsed-waterLabel.ts into src/lib/waterLabel.ts:
 Test Files  1 passed (1)
      Tests  3 passed (3)
# Then restored the 90-line original (Plan 07 owns that file in its worktree).
```

Once the orchestrator merges Plan 07 and Plan 08 worktrees together post-wave, the test file will run green against the collapsed runtime. No action required in Plan 08.

Per the plan's `wave_context` directive: "Write the test against the expected post-collapse contract." That is exactly what was done.

## Deviations from Plan

None. Plan executed exactly as written.

## Authentication Gates

None.

## Known Stubs

None. The test file uses fixture data only (stubStress, makeFacility); no placeholder rendering paths touched.

## Threat Flags

None. Test code with public-domain place names (Mosul, Jeddah). Matches the plan's threat register disposition (T-27.3.2-11 `accept`).

## Commits

- `da91968` — test(27.3.2-08): replace waterLabel tests with 3 focused cases per D-17

## Self-Check: PASSED

- `src/lib/__tests__/waterLabel.test.ts` exists at 50 lines with 3 `it` cases, correct describe title, and no references to deleted internals (grep-verified above).
- Commit `da91968` exists in git log (verified via `git rev-parse --short HEAD`).
- SUMMARY.md exists at `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-08-SUMMARY.md`.
- No modifications to STATE.md or ROADMAP.md (per parallel-executor contract).

---
phase: 27.3.2
plan: 01
subsystem: water-admission
tags:
  - water
  - types
  - rejections
  - wave1
requirements:
  - D-04
dependency-graph:
  requires:
    - 27.3.1 Plan 11 (water:facilities:v2 Redis envelope)
    - 27.3.1 Plan 10 (hasName gate + excluded_turkey bucket)
    - 27.3.1 Plan 05 (WaterFilterStats interface baseline)
  provides:
    - WaterFilterStats.rejections.no_resolved_name (typed counter)
    - WaterFilterStats.byTypeRejections[*].no_resolved_name (per-type counter)
    - Lock-step seed literals in fetchFacilityType + fetchWaterFacilities + buildEmptyFilterStats
  affects:
    - Plan 02 (Zod waterFilterStatsSchema extension — must add matching .strict() key)
    - Plan 04 (computeAdmissionDecision — new verdict `{ verdict: 'reject', bucket: 'no_resolved_name' }` now compiles)
    - Plan 08 (DevApiStatus WaterFiltersSection — will render the new bucket)
tech-stack:
  added: []
  patterns:
    - Rejection Bucket Lock-Step (27.3.2-PATTERNS.md §Shared Patterns)
    - AdmissionVerdict union auto-extension via `keyof WaterFilterStats['rejections']`
key-files:
  created: []
  modified:
    - server/adapters/overpass-water.ts
    - server/routes/water.ts
decisions:
  - "Added `no_resolved_name: 0` to `buildEmptyFilterStats` in server/routes/water.ts as Rule 3 auto-fix — the interface change triggered a TS2741 there that blocked server compilation. This helper is a fifth synchronized seed of the same `WaterFilterStats['rejections']` shape not listed in the plan; the Rejection Bucket Lock-Step pattern requires it to stay aligned."
  - "No new TypeScript errors introduced (22 pre-existing at base → 22 after; all unrelated to water admission). Pre-existing errors in `server/lib/llmEventExtractor.ts` + `server/routes/events.ts` logged in deferred-items.md, out of Plan 01 scope."
metrics:
  duration: "~10m"
  completed: "2026-04-20T21:19:18Z"
  tasks: 1
  files_modified: 2
---

# Phase 27.3.2 Plan 01: Water Facility Admission Tightening — `no_resolved_name` Type Foundation Summary

Extend `WaterFilterStats` with a `no_resolved_name` rejection bucket per D-04 so Wave 2's admission logic (Plan 04) can write `{ verdict: 'reject', bucket: 'no_resolved_name' }` without a TypeScript error and per-type + summed counters tally correctly at fetch time.

## What Shipped

Four lock-step edits inside `server/adapters/overpass-water.ts`:

1. **Interface `rejections` shape (line 594)** — `no_resolved_name: number;` inserted between `no_name` and `duplicate` with a 7-line JSDoc explaining the bucket semantics (Latin-label admission failure for non-desal; desal-exempt).
2. **Interface `byTypeRejections` inner shape (line 618)** — same field with a single-line JSDoc noting desal will always show 0 (D-03 exemption proof).
3. **`fetchFacilityType` `typeBucket` seed (line 883)** — `no_resolved_name: 0,` added in lock-step with the interface ordering, comment citing D-04.
4. **`fetchWaterFacilities` top-level `stats.rejections` seed (line 1007)** — same zero seed, same comment pattern.

One Rule 3 auto-fix in `server/routes/water.ts`:

5. **`buildEmptyFilterStats` `rejections` seed (line 85-86)** — fifth synchronized seed of the same `WaterFilterStats['rejections']` shape; the interface change produced a new TS2741 there that blocked server compilation. Added `no_resolved_name: 0` in lock-step with the same Phase 27.3.2 D-04 comment.

## Verification

- `grep -c "no_resolved_name" server/adapters/overpass-water.ts` = 4 (≥4 required) ✓
- `grep -c "no_resolved_name: number;" server/adapters/overpass-water.ts` = 2 (exactly 2 required) ✓
- `grep -c "no_resolved_name: 0," server/adapters/overpass-water.ts` = 2 (exactly 2 required) ✓
- Ordering invariant: every `no_resolved_name` line is directly after a `no_name` line and directly before a `duplicate` line at 585→594→595, 616→618→619, 881→883→884, 1005→1007→1008 ✓
- `npx tsc -p tsconfig.server.json --noEmit`: 22 errors before this plan, 22 errors after — **zero new errors introduced by this change**. Pre-existing errors all in `server/lib/llmEventExtractor.ts` + `server/routes/events.ts`, unrelated to water admission (see deferred-items.md).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking Issue] Extended `buildEmptyFilterStats` rejections seed in `server/routes/water.ts`**

- **Found during:** Task 1 verification (`tsc --noEmit` run)
- **Issue:** The WaterFilterStats interface change produced `TS2741: Property 'no_resolved_name' is missing` in `server/routes/water.ts:80` because `buildEmptyFilterStats` is a fifth, previously-unlisted seed of the `WaterFilterStats['rejections']` shape. The plan's PATTERNS.md §Rejection Bucket Lock-Step lists four sites (interface ×2 + seeds ×2) but `buildEmptyFilterStats` is a sixth seed not mentioned there — it was introduced in 27.3.1 Plan 11 as part of the error-without-cache fallback path.
- **Fix:** Added `no_resolved_name: 0,` between `no_name: 0,` and `duplicate: 0,` with the same Phase 27.3.2 D-04 lock-step comment.
- **Files modified:** `server/routes/water.ts` (+2 lines — field + comment)
- **Commit:** `747eb32` (same commit as the four primary edits)
- **Rationale:** Plan 01 acceptance criteria require `tsc --noEmit` to exit 0; letting a new error accumulate at the plan boundary would violate the lock-step invariant the plan itself mandates (PATTERNS.md §Shared Patterns "A PR that edits only a subset of these will either fail typecheck or fail cached-response parse"). Rule 3 (blocking issue directly caused by our own change) applies — fixed inline rather than escalated to checkpoint.

No authentication gates. No Rule 4 architectural changes. No Rule 2 missing-critical-functionality additions.

## Authentication Gates

None.

## Deferred Issues

22 pre-existing TypeScript errors at base commit `4b94565`, all in `server/lib/llmEventExtractor.ts` + `server/routes/events.ts`. Unrelated to water admission — likely fallout from a prior `noUncheckedIndexedAccess` enablement that missed these files. Logged to `deferred-items.md`. Not in Plan 01 scope; suitable for Phase 28 cleanup or a dedicated tech-debt ticket.

## Known Stubs

None. No empty arrays / placeholders / TODO comments added — this plan is a pure type-extension with all four seed literals correctly initialized to `0`.

## Self-Check: PASSED

- FOUND: `server/adapters/overpass-water.ts` (modified, 4 new lines of `no_resolved_name` content)
- FOUND: `server/routes/water.ts` (modified, 2 new lines for Rule 3 auto-fix)
- FOUND commit `747eb32`: `feat(27.3.2-01): add no_resolved_name rejection bucket to WaterFilterStats`
- Acceptance criteria greps all pass (see Verification section).
- Zero new TypeScript errors introduced (22 → 22 at plan boundary).

## Threat Flags

None. Plan 01 is a pure type/data-shape extension on an internal `WaterFilterStats` interface that never crosses a network or user trust boundary — T-27.3.2-01 mitigation (lock-step edit enforcement) is satisfied by the acceptance-criteria greps.

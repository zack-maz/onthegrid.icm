---
phase: 27.4.3-free-claude-code-routing
plan: 02a
subsystem: infra
tags: [llm, schema, observability, a9-mirror, progress-types, v3, zod, tdd-types]

# Dependency graph
requires:
  - phase: 27.4.3-free-claude-code-routing
    provides: 'Plan 01 — freeClaudeRouter.callLLM, isPipelineV3(), Provider union widened in breaker/DLQ/budget'
provides:
  - "enrichedEventV3 Zod branch + EnrichedEventV3 type (extends V2 with schemaVersion: 'v3')"
  - 'EVENT_EXTRACTION_SCHEMA_V3 alias to V2 (D-10 prompt-text-only wire contract for NVIDIA NIM / OpenRouter)'
  - "batchResponseV3 + BatchResponseV3 type (z.preprocess seeds schemaVersion: 'v3')"
  - 'enrichedEventAny widened to [v1, v2, v3] discriminated union for cache-read safety'
  - 'LLMPipelineProgress + LLMRunSummary widened (schemaVersion v3, callHistory provider 4-way, 6 v3 observability fields, recentEvents)'
  - 'RecentEnrichedEvent (server + client) extended with reasoningTrace?: string + lineageHash?: string per B-2'
  - 'A9 mirror: useLLMStatusPolling.ts LLMRunSummary + LLMStatus extended in same atomic commit (UI-SPEC §"Data freshness" wire contract)'
  - 'pipelineFlips type added on client (anticipates Plan 02b server-side instrumentation)'
affects: [27.4.3-02b, 27.4.3-03, 27.4.3-04, 27.4.3-05, 27.4.3-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Schema versioning by extension: zod .extend() preserves .strict() for byte-identical V2/V3 contracts'
    - 'Wire-contract alias: EVENT_EXTRACTION_SCHEMA_V3 = EVENT_EXTRACTION_SCHEMA_V2 surfaces same JSON Schema as prompt text under v3 (D-10 relaxation)'
    - 'A9 atomic mirror: server type extension + client hook type extension MUST land in single commit'
    - 'B-2 closure: RecentEnrichedEvent gains lineage fields (reasoningTrace, lineageHash) as optional on BOTH server + client surfaces, settling Plan 04 DrillDownRow type contract'
    - 'Field-name asymmetry preserved by inline note: server latencyHistogram → client latency, server-only samples ring buffer dropped on /llm-status route mapping'

key-files:
  created: []
  modified:
    - 'server/lib/llmSchema.ts (+62 lines: enrichedEventV3, EVENT_EXTRACTION_SCHEMA_V3, batchResponseV3, union widened)'
    - 'server/lib/llmProgress.ts (+264 lines: LLMPipelineProgress + LLMRunSummary v3 fields, RecentEnrichedEvent server-side, INITIAL_PROGRESS seeds, buildSummary threading)'
    - 'src/hooks/useLLMStatusPolling.ts (+165 lines: LLMRunSummary + LLMStatus mirror, RecentEnrichedEvent client-side, pipelineFlips)'
    - 'server/__tests__/lib/llmSchema.test.ts (+10 lines: Test 8c flipped to v3-admits, Test 8d added for v4-rejects forward defense)'

key-decisions:
  - 'enrichedEventV3 uses .extend() not full re-declaration: zod re-applies strict() on the resulting schema; surplus-key rejection still fires via the inner locationHierarchyV2 .strict() AND the outer .strict() preserved by extend.'
  - 'EVENT_EXTRACTION_SCHEMA_V3 is an alias to V2 (not a fork): D-10 only relaxes how the schema is delivered (prompt text vs. response_format.json_schema), not what the contract is. Aliasing keeps schema lock (D-23) at one site.'
  - 'callHistory.provider widened with INLINE 4-way union (not the breaker Provider re-export): keeps /llm-status JSON wire type 1:1 with the client-imported union — clients see the same string literals.'
  - 'pipelineFlips added on client interface (UI-SPEC line 321 contract) without server-side counterpart in this plan: Plan 02b/03 will wire the server-side instrumentation; client type is set NOW so the dashboard consumer (Plan 04) has the contract settled at type-check time.'
  - 'samples ring buffer is server-only on latencyHistogram: client receives latency (UI-SPEC name) without samples per I-9. The /llm-status route handler in Plan 02b performs the field rename + samples drop.'
  - 'recentEvents threads through both LLMPipelineProgress (live ring) and LLMRunSummary (persisted): cold-start dashboard reads see the same drill-down list as live polling, no edge case where the lineage UI goes blank on Vercel cold start.'

patterns-established:
  - 'Schema-version widening across all type sites in one commit: server schema, server progress, server summary, client hook, INITIAL_PROGRESS seed, buildSummary threading — covered atomically so /llm-status JSON is consistent end-to-end.'
  - 'Forward-defense test pattern: when widening a discriminated union to admit a new schemaVersion literal, ADD a test that asserts the next-bumped literal still rejects (Test 8d v4-rejects).'

requirements-completed: [] # Plan frontmatter `requirements: []` — no requirement IDs to mark.

# Metrics
duration: 7min
completed: 2026-04-26
---

# Phase 27.4.3 Plan 02a: v3 Schema + Progress Types + A9 Mirror Summary

**Atomic landing of v3 Zod schema + JSON Schema + batch envelope + LLMRunSummary observability fields (routingTrace/latencyHistogram/rateLimit/schemaFailures/errorTaxonomy/costShadow/recentEvents) + B-2 RecentEnrichedEvent.reasoningTrace+lineageHash + A9 client hook mirror in one commit (`dc9a4a9`), settling the v3 type contract before Plan 02b's instrumentation work begins.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-26T06:07:22Z
- **Completed:** 2026-04-26T06:14:50Z (approx)
- **Tasks:** 3 (all atomic in single commit per A9 invariant)
- **Files modified:** 4 (3 plan files + 1 test contract update)
- **Lines added:** +501

## Accomplishments

- **Task 1 (llmSchema.ts):** Added `enrichedEventV3`, `EnrichedEventV3` type, `EVENT_EXTRACTION_SCHEMA_V3` constant, `batchResponseV3` + `BatchResponseV3` type, widened `enrichedEventAny` to admit v3 — all v1/v2 exports byte-identical (D-21 rollback safety).
- **Task 2 (llmProgress.ts):** Widened `schemaVersion` union (v1|v2|v3) on both `LLMPipelineProgress` and `LLMRunSummary`. Widened `callHistory.provider` to 4-way inline union; added `routingReason` + 3 v3 skipReason values. Added 6 v3 observability fields (`routingTrace`, `latencyHistogram` w/ samples ring buffer, `rateLimit` w/ perModel breakout, `schemaFailures`, `errorTaxonomy`, `costShadow`). Added server-side `RecentEnrichedEvent` interface with B-2 fields (`reasoningTrace?`, `lineageHash?`). Wired `recentEvents` through both interfaces. Seeded INITIAL_PROGRESS (`routingTrace: undefined`, `costShadow: undefined`, etc.) so resetProgress() clears stale data. Threaded all new fields through `buildSummary()` so cold-start dashboard reads see the full v3 surface.
- **Task 3 (useLLMStatusPolling.ts — A9 mirror):** Same widenings on client `LLMRunSummary` + `LLMStatus`. Added the same 6 v3 fields with name-asymmetry handled (server `latencyHistogram` → client `latency`, samples ring dropped per I-9 inline note). Added `pipelineFlips` per UI-SPEC line 321 (anticipates Plan 02b server-side instrumentation). Extended client `RecentEnrichedEvent` with `reasoningTrace?` + `lineageHash?` — Plan 04 Task 3 DrillDownRow now type-safe under TS strict mode without `as any` or TS2339.

## Confirmation: Tasks 2 + 3 in same commit (A9 atomic invariant)

```
dc9a4a9 feat(27.4.3-02a): land v3 schema + progress types + A9 mirror atomically
 server/__tests__/lib/llmSchema.test.ts |  15 +-
 server/lib/llmProgress.ts              | 267 ++++++++++++++++++++++++++++++++-
 server/lib/llmSchema.ts                |  66 +++++++-
 src/hooks/useLLMStatusPolling.ts       | 172 +++++++++++++++++++--
 4 files changed, 501 insertions(+), 19 deletions(-)
```

All three plan tasks (server schema + server progress + client hook mirror) landed in commit `dc9a4a9` per the project A9 atomic invariant. No multi-commit drift between server-side type and client-side mirror is possible.

## Confirmation: B-2 RecentEnrichedEvent client + server reasoningTrace + lineageHash

- **Server side** (`server/lib/llmProgress.ts` lines 321-344):
  ```typescript
  export interface RecentEnrichedEvent {
    // ... 13 existing fields ...
    reasoningTrace?: string; // <think>...</think> from v3 LLM response
    lineageHash?: string; // sha256(prompt || model || eventId)
  }
  ```
- **Client side** (`src/hooks/useLLMStatusPolling.ts` lines 117-140):
  Same two fields, same comments documenting Plan 04 Task 3 consumer + A9 mirror invariant.
- **grep verification:** `grep -cE "reasoningTrace\?:|lineageHash\?:" server/lib/llmProgress.ts` returns 2; same for client file. B-2 closure satisfied on both surfaces.

## TS error count before/after on touched files

| File                               | Before | After |
| ---------------------------------- | -----: | ----: |
| `server/lib/llmSchema.ts`          |      0 |     0 |
| `server/lib/llmProgress.ts`        |      0 |     0 |
| `src/hooks/useLLMStatusPolling.ts` |      0 |     0 |

(8 pre-existing `useEntityLayers.ts` `depthTest` errors are documented in CLAUDE.md as deferred to Phase 27.4.3 — outside this plan's scope; the deck.gl v9 drift is unrelated to the type-foundation work.)

## Test verification

- `server/__tests__/lib/llmSchema.test.ts`: **23/23 green** (incl. updated Test 8c "v3 admits as v3 branch" + new Test 8d "v4 rejects" forward-defense).
- `server/__tests__/lib/llmProgress.test.ts`: **9/9 green**.
- LLM-adjacent broader test suite (8 test files: schema + progress + breaker + DLQ + budget + extractor v1 + extractor v2 + freeClaudeRouter): **99/99 green** in 5.35s.

## D-21 rollback safety preserved

`git diff dc9a4a9~1..dc9a4a9 -- server/lib/llmSchema.ts | grep -E "^-\s*export const enrichedEventV[12]\b|^-\s*export const EVENT_EXTRACTION_SCHEMA_V2\b|^-\s*export const batchResponseV2\b"` returns empty — no v1 or v2 exports were deleted.

## Files Created/Modified

- `server/lib/llmSchema.ts` — Added `enrichedEventV3`, `EnrichedEventV3` type, `EVENT_EXTRACTION_SCHEMA_V3` (alias to V2), `batchResponseV3`, `BatchResponseV3` type. Widened `enrichedEventAny` discriminated union to `[v1, v2, v3]`.
- `server/lib/llmProgress.ts` — Widened `schemaVersion` and `callHistory.provider` on both `LLMPipelineProgress` and `LLMRunSummary`. Added 6 v3 observability fields (mirrored on both interfaces). Added `RecentEnrichedEvent` interface with B-2 fields. Wired `recentEvents` through both interfaces. Updated `INITIAL_PROGRESS` and `buildSummary` to handle the new fields.
- `src/hooks/useLLMStatusPolling.ts` — A9 mirror: same widenings on client `LLMRunSummary` + `LLMStatus`. Added 7 v3 fields (6 server fields + `pipelineFlips`). Extended client `RecentEnrichedEvent` with B-2 fields. Field-name asymmetry handled via I-9 inline note (server `latencyHistogram` → client `latency`).
- `server/__tests__/lib/llmSchema.test.ts` — Test 8c flipped from "v3 rejects" to "v3 admits as v3 branch" (Rule 1 deviation: the test asserted old behavior that this plan's union widening directly contradicts). Added Test 8d "v4 rejects" for forward defense.

## Decisions Made

See frontmatter `key-decisions` for the full set. Summary highlights:

1. `enrichedEventV3 = enrichedEventV2.extend({schemaVersion: z.literal('v3')})` over full re-declaration — strict() preserves through `.extend()` in Zod v3 (verified via existing test 8c surplus-key rejection on the v3 branch); cleaner than 40-line copy-paste with one-character difference.
2. `EVENT_EXTRACTION_SCHEMA_V3` aliased to V2 — D-10 only relaxes wire delivery (prompt text vs. response_format.json_schema), not contract content. Aliasing keeps schema lock (D-23) at one site.
3. `callHistory.provider` widened with inline 4-way union — keeps /llm-status JSON wire type 1:1 with the client-imported union string literals.
4. `pipelineFlips` added on client interface only — UI-SPEC line 321 wire contract; server-side instrumentation lands in Plan 02b but the client type is set NOW so Plan 04's dashboard consumer is type-safe at check time.
5. `samples` ring buffer kept server-side only on `latencyHistogram` — UI-SPEC names the client field `latency` per I-9; /llm-status route in Plan 02b will perform the rename + samples drop.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Schema test 8c contradicted the plan's union widening**

- **Found during:** Task 1 verification (`npx vitest run server/__tests__/lib/llmSchema.test.ts`)
- **Issue:** Test 8c at `server/__tests__/lib/llmSchema.test.ts:150-155` asserted that a payload with `schemaVersion: 'v3'` REJECTS via `enrichedEventAny.safeParse`. After widening `enrichedEventAny` to `[v1, v2, v3]` per the plan's Task 1 action, v3 payloads parse successfully — the test would fail under the new contract.
- **Fix:** Updated Test 8c to assert the new contract: "v3 payload parses as the v3 branch" with `expect(result.success).toBe(true)` + `expect(result.data.schemaVersion).toBe('v3')`. Added Test 8d "v4 rejects (no matching discriminator)" for forward defense — when a future plan widens to v4, this test should also flip atomically.
- **Files modified:** `server/__tests__/lib/llmSchema.test.ts`
- **Verification:** 23/23 schema tests pass; the new Test 8c green confirms v3 admission, Test 8d green confirms v4 rejection.
- **Committed in:** `dc9a4a9` (atomic with Task 1's schema change — same commit as the contract flip)

**2. [Rule 3 - Blocking] Plan verify command pointed at non-existent `server/tsconfig.json`**

- **Found during:** Task 1 verification preflight
- **Issue:** Plan `<verify>` blocks for Task 1 + Task 2 specify `npx tsc --noEmit -p server/tsconfig.json`. That path does not exist in this repo — server-side TS is compiled via the root `tsconfig.json` project references (`tsconfig.app.json`, `tsconfig.node.json`, `tsconfig.server.json`).
- **Fix:** Used `npx tsc --noEmit -p tsconfig.server.json` (server) and `npx tsc --noEmit -p tsconfig.app.json` (client) per the actual project layout. No file changes needed — only the verification command path adjusted.
- **Files modified:** none (verification-command path correction only)
- **Verification:** Both commands return zero errors on the touched files; full server TS check is also zero errors.
- **Committed in:** N/A (no code change required — purely a verification path adjustment)

---

**Total deviations:** 2 auto-fixed (1 bug per Rule 1, 1 blocking per Rule 3)

**Impact on plan:** Both deviations were necessary for the plan to land cleanly. The Rule 1 fix is the test contract update that MUST accompany the union widening — without it the test suite would be red. The Rule 3 fix is a verification path correction that does not alter any code. No scope creep; no architectural change.

## Issues Encountered

- **Pre-existing app TS errors in `useEntityLayers.ts`:** 8 `depthTest`-related errors documented in CLAUDE.md ("remaining 8 are useEntityLayers.ts depthTest deck.gl v9 drift, deferred to Phase 27.4.3"). These are NOT caused by this plan's changes; explicitly outside scope per the deviation rules' SCOPE BOUNDARY clause. Verified via `git diff` that no `useEntityLayers.ts` content was touched.

## Note: extractor / routes / instrumentation / lineage helper are Plan 02b

Per the plan's `<output>` reminder: this plan is **type-only**. The actual code that POPULATES the new fields lives in Plan 02b:

- `routingTrace` writes — v3 extractor's per-batch routing decision capture
- `latencyHistogram.samples` ring buffer maintenance — Plan 02b instrumentation
- `rateLimit` snapshot — RollingWindow + daily-cap reads
- `schemaFailures` + `errorTaxonomy` increments — v3 extractor zod-fail / Cerebras-fail classification
- `costShadow` calculation — token-counter × Anthropic Sonnet rate
- `reasoningTrace` extraction (`<think>...</think>` strip) — Plan 02b lineage helper
- `lineageHash` write — Plan 02b lineage helper (sha256(prompt||model||eventId))
- `pipelineFlips` server-side wire — Plan 02b/03 v3 toggle endpoint instrumentation

The contract is now settled; Plan 02b can be a pure write — no backwards type-edit needed.

## User Setup Required

None — pure type-foundation work, no env vars or external services touched.

## Next Phase Readiness

- **Plan 02b** (Wave 2 sibling): all type contracts settled. Plan 02b's instrumentation can write `llmProgress.routingTrace`, `.latencyHistogram.samples`, `.rateLimit`, `.schemaFailures`, `.errorTaxonomy`, `.costShadow`, `.recentEvents[].reasoningTrace`, `.recentEvents[].lineageHash` directly under TS strict mode without `as any`.
- **Plan 03** (downstream): v3 cache write path can use `batchResponseV3` for batch parse + `enrichedEventAny` discriminated union for cache-read.
- **Plan 04 DrillDownRow extension** (downstream — was the B-2 motivator): client-side `RecentEnrichedEvent.reasoningTrace?` + `.lineageHash?` are now declared; component can render them without TS2339 or `as any`.

## Self-Check: PASSED

**Created/modified files exist:**

- FOUND: `server/lib/llmSchema.ts` (modified, +66 -4 lines)
- FOUND: `server/lib/llmProgress.ts` (modified, +267 -11 lines)
- FOUND: `src/hooks/useLLMStatusPolling.ts` (modified, +165 -7 lines)
- FOUND: `server/__tests__/lib/llmSchema.test.ts` (modified, +13 -3 lines)
- FOUND: `.planning/phases/27.4.3-free-claude-code-routing/27.4.3-02a-SUMMARY.md` (this file, created)

**Commit exists:**

- FOUND: `dc9a4a9` (feat(27.4.3-02a): land v3 schema + progress types + A9 mirror atomically)

**Acceptance criteria satisfied (compact summary):**

| Task   | Criterion                                                    |                 Result |
| ------ | ------------------------------------------------------------ | ---------------------: |
| 1      | enrichedEventV3 in llmSchema.ts                              |                    1 ✓ |
| 1      | EnrichedEventV3 in llmSchema.ts                              |               1 ✓ (≥1) |
| 1      | EVENT_EXTRACTION_SCHEMA_V3 in llmSchema.ts                   |               1 ✓ (≥1) |
| 1      | batchResponseV3 in llmSchema.ts                              |     2 ✓ (const + type) |
| 1      | Discriminated union [v1, v2, v3]                             |     ✓ (multiline form) |
| 1      | EVENT_EXTRACTION_SCHEMA_V2 preserved                         |               2 ✓ (≥1) |
| 1      | TS clean on llmSchema.ts                                     |             0 errors ✓ |
| 1      | Schema tests pass                                            |                23/23 ✓ |
| 2      | schemaVersion 3-way × 2 interfaces                           |               2 ✓ (≥2) |
| 2      | callHistory provider 4-way × 2 interfaces                    |               2 ✓ (≥2) |
| 2      | New skipReason values                                        |               6 ✓ (≥2) |
| 2      | 6 v3 fields × 2 interfaces                                   |             12 ✓ (≥12) |
| 2      | samples ring buffer typed                                    |               2 ✓ (≥1) |
| 2      | reasoningTrace + lineageHash on RecentEnrichedEvent (server) |                    2 ✓ |
| 2      | INITIAL_PROGRESS seeds new fields                            |               2 ✓ (≥2) |
| 2      | buildSummary threads new fields                              |               2 ✓ (≥2) |
| 2      | TS clean on llmProgress.ts                                   |             0 errors ✓ |
| 2      | Progress tests pass                                          |                  9/9 ✓ |
| 3      | schemaVersion 3-way × 2 client interfaces                    |               2 ✓ (≥2) |
| 3      | callHistory provider 4-way × 2 client interfaces             |               2 ✓ (≥2) |
| 3      | 7 v3 fields × 2 client interfaces                            |             14 ✓ (≥14) |
| 3      | reasoningTrace + lineageHash on RecentEnrichedEvent (client) |                    2 ✓ |
| 3      | I-9 inline note on naming asymmetry                          |               2 ✓ (≥1) |
| 3      | TS clean on useLLMStatusPolling.ts                           |             0 errors ✓ |
| atomic | All 3 tasks in same commit (A9)                              |          ✓ (`dc9a4a9`) |
| safety | v1/v2 schemas/types byte-identical                           | ✓ (no v1/v2 deletions) |

---

_Phase: 27.4.3-free-claude-code-routing_
_Completed: 2026-04-26_

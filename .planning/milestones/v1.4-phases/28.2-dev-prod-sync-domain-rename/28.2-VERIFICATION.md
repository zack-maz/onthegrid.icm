---
phase: 28.2-dev-prod-sync-domain-rename
verified: 2026-05-06T22:50:00Z
status: human_needed
score: 28/29 must-haves verified (1 deferred to operator checkpoint)
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: 'Operator: redeploy `onthegrid.icm` Vercel project so `DASHBOARD_PASSWORD=hello` env applies in prod (W1 D-03 follow-on)'
    expected: '`curl -sI https://otg-iran-monitor.vercel.app/api/health` returns 200; Bearer-gated routes accept the production password'
    why_human: 'Operator-only Vercel dashboard action — agent cannot trigger production deploy'
  - test: 'Operator: set GH Actions secrets PROD_DASHBOARD_PASSWORD + UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN'
    expected: "Workflow .github/workflows/prod-connectivity-audit.yml has all 3 secrets available so the smoke + rate-limit + sidecar-write steps execute (no 'Upstash credentials missing — skipping sidecar write' warnings)"
    why_human: 'GitHub repo settings page — agent cannot set secrets'
  - test: 'Operator: trigger .github/workflows/prod-connectivity-audit.yml via the GH Actions UI (workflow_dispatch)'
    expected: "Workflow run completes; both api-connectivity.test.ts (17 endpoint probes) and rate-limit.test.ts (2 companion checks) PASS against https://otg-iran-monitor.vercel.app; sidecar key audit:connectivity:last-result written to Upstash; JSON artifact uploaded with 30d retention; audit-result banner in merged API Health tab renders green: 'All 16 endpoints connecting (last verified <date> via prod audit).'"
    why_human: 'Manual workflow_dispatch + production credentials required; D-25 explicitly designates prod-URL run as the W6 phase-close gating evidence'
  - test: 'Operator: land closing commit `docs(28.2): close W6 prod connectivity audit — 16 of 16 GREEN`'
    expected: 'Commit lands on feature/28.2-dev-prod-sync-domain-rename branch; W6-AUDIT.md prod-pass column populated with PASS for all 16 endpoints; STATE.md updated to mark Phase 28.2 complete; ROADMAP.md progress counter updated'
    why_human: 'Commit must follow the operator-confirmed prod audit run; agent cannot fabricate the prod-URL evidence'
overrides_applied: 0
---

# Phase 28.2: Dev/Prod Sync + Domain Rename + Rate-Limiter Fold-In + Dashboard Merge Verification Report

**Phase Goal:** Re-establish dev↔prod parity after Phase 28.1 by (a) renaming the deployment domain to `otg-iran-monitor.vercel.app`, (b) folding Phase 999.1 Bearer-bypass into the global rate-limiter, (c) graduating three dev-only debug surfaces to Bearer-gated prod, (d) hardening operator-control endpoints with audit log + replay quota + adversarial sub-eval, (e) merging the DevApiStatus Overview tab into a unified API Health tab with diagnostic blocks + confirm modal + 429 alert + Operator Actions block, (f) shipping a three-pronged connectivity guarantee (smoke vitest + rate-limit vitest + scheduled GH workflow + audit-status route + audit banner).

**Verified:** 2026-05-06T22:50:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Codebase Gates

| Gate                     | Result              | Evidence                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------ | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npx tsc --noEmit`       | PASS                | Exit 0; no errors                                                                                                                                                                                                                                                                                                                                                                          |
| `npm run lint`           | PASS                | Exit 0; 0 errors, 16 warnings — all `react-refresh/only-export-components` (16/16 pre-existing per Phase 28.1 W7 audit). 0 `react-hooks/exhaustive-deps` warnings (matches CLAUDE.md "0 react-hooks/exhaustive-deps warnings" Phase 28.1 W7 D-27 contract)                                                                                                                                 |
| `npx vitest run`         | PASS                | 162 test files passed + 2 skipped (164 total); 2145 tests passed + 19 skipped (2164 total). The 19 skipped are the opt-in connectivity (17) + rate-limit (2) suites gated by `RUN_CONNECTIVITY_TEST=1` / `RUN_RATE_LIMIT_TEST=1` env flags — expected per Plan 06 design                                                                                                                   |
| `npx prettier --check .` | FLAG (non-blocking) | 2 files flagged: `server/lib/operatorAudit.ts` (1 line: 80-character cosmetic line-break preference) + `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-02-SUMMARY.md` (cosmetic list-item formatting in a planning artifact). Diff is purely stylistic — no functional impact, no spec violation. Recommend a final `npx prettier --write .` cleanup commit before merging to main |

**Note on prettier flags:** Both flagged files are introduced in this phase (commits `9549c3a` and `7ea84dd`). The single source-code line in `operatorAudit.ts` is a multi-line ternary that pretty-prefers wrapped form vs the executor's compact form; SUMMARY.md prose list formatting is a similar cosmetic question. Neither is load-bearing. Per the "phase verification gate" definition in `gates.md`, this is documented as a FLAG (informational) rather than a FAIL — the codebase compiles, tests pass, and lint is clean.

---

## Decision-Trail Coverage

| Decision | Subject                                                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Status                    |
| -------- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| **D-03** | Domain rename to `otg-iran-monitor.vercel.app`                                    | `scripts/load-test.js:18` BASE_URL + `scripts/load-test.spec.ts:16` PROD_URL + `CHANGELOG.md:78` + `docs/runbook.md:509` all contain the new URL. Repo-wide `grep -rn "irt-monitoring"` (excluding `.planning/phases/`, `.planning/milestones/`, `load-test-results/`, `.git/`, `node_modules/`, `dist/`, `dist-server/`, `.claude/worktrees/`) returns ZERO hits in editable code/docs (only narrative references in STATE.md historical entry and CLAUDE.md closeout section, which are correct) | PASS                      |
| **D-04** | Bearer-bypass on global rate-limiter                                              | `server/middleware/rateLimit.ts:1` imports `timingSafeEqual` from `node:crypto`; `:65` has the `if (prefix === 'ratelimit:public')` bypass branch with 5 grep-confirmed occurrences of `'ratelimit:public'` for scope-bounding (factory + bypass-scope check + 3 doc comments). 7-test matrix in `server/middleware/__tests__/rateLimit.test.ts` ALL PASS                                                                                                                                          | PASS                      |
| **D-05** | Per-field opt-in for graduations                                                  | Plan 04 SUMMARY records 4 atomic gate-swap commits (`ca5ea25`, `7bbbe69`, `1953616`) — one per surface; not bundled. Path-B vs Path-A decision documented per-field for OSM ID                                                                                                                                                                                                                                                                                                                     | PASS                      |
| **D-06** | Graduate 3 surfaces to Bearer-gated via `shouldRenderDashboard()`                 | `src/components/map/EntityTooltip.tsx:126` + `src/components/detail/EventDetail.tsx:57+166` + `src/components/detail/WaterFacilityDetail.tsx:118` all use `shouldRenderDashboard()`. Each file has the `import { shouldRenderDashboard } from '@/lib/dashboardAuth'` line. 19 render-parity tests across 5 test files PASS                                                                                                                                                                         | PASS                      |
| **D-07** | Lockdown — NotificationCard, MapDevExposer, notabilityScore stay dev-only         | `src/components/map/BaseMap.tsx:461` still uses `import.meta.env.DEV && <MapDevExposer />`; `src/components/notifications/NotificationCard.tsx` has no `shouldRenderDashboard` import (verified via static-source guard test). `WaterFacilityDetail.tsx:239` notabilityScore still gated by `import.meta.env.DEV` per Plan 04 audit. 5 lockdown regression tests PASS (NotificationCard + MapDevExposer = 4 + 1 cross-file bleed in WaterFacility)                                                 | PASS                      |
| **D-08** | Operator endpoint Bearer-gating + Pitfall 6 + audit log + replay quota            | `server/lib/operatorAudit.ts` (87 LOC, exports `appendOperatorAuditEntry` + `bearerFingerprint`, SADD bounded 500/30d). `server/lib/replayQuota.ts` exports `checkReplayQuota` (INCR with 48h TTL on first set). `server/routes/events.ts` lines 31, 36, 489, 538, 637 wire both helpers into `/llm-replay` (quota at top, audit before response) and `/llm-pipeline` (audit before response). Pitfall 6 dual-gate (NODE_ENV + Bearer + never-write-cache) preserved — 13 audit/quota tests PASS   | PASS                      |
| **D-09** | No new env-tunable surfaces                                                       | `git diff bcc9674..HEAD -- '*.ts' '*.tsx'` shows ZERO new `import.meta.env.VITE_*` introductions. The only new `process.env.*` references are: (a) `PROD_URL` already in `scripts/load-test.spec.ts` (renamed not added), (b) test-scope env-restoration scaffolding in audit/quota/route tests (not runtime config). `.env.example` UNCHANGED                                                                                                                                                     | PASS                      |
| **D-22** | Merge Overview into All APIs                                                      | `src/types/ui.ts:84` defines `DevApiStatusTab = 'apiHealth' \| 'water' \| 'sites' \| 'events'` — 'overview' and 'allApis' both removed. `src/stores/uiStore.ts:38` default `'apiHealth'`. `data-testid="polling-store-rows"` (line 1518) + `data-testid="llm-pipeline-section"` (line 1610) confirm fold-in nothing-lost contract. 9 tabMerge tests PASS                                                                                                                                           | PASS                      |
| **D-23** | Four diagnostic blocks (tier banner, quality metrics, retry button, sparkline)    | `tier-summary-banner` (DevApiStatus.tsx:1280), per-endpoint quality metrics (D-23 block 2 commit `4953fd3`), `Refresh now` retry button (line 1490, with `Refreshing...` in-flight state), recent-fetch sparkline (lines 1120-1166, 10-dot strip via `flex gap-1` + `w-1 h-1`). 18 diagnosticBlocks tests PASS                                                                                                                                                                                     | PASS                      |
| **D-24** | Three-pronged connectivity guarantee (smoke + companion + integration)            | `src/__tests__/api-connectivity.test.ts` (17 endpoint probes, opt-in via `RUN_CONNECTIVITY_TEST`); `src/__tests__/rate-limit.test.ts` (2 companion tests, opt-in via `RUN_RATE_LIMIT_TEST`); HealthBanner outage integration trace at `src/components/ui/__tests__/HealthBanner.outage.test.tsx` (2 tests PASS, confirms ungated mount per D-26)                                                                                                                                                   | PASS                      |
| **D-25** | Audit runs against deployed prod URL only                                         | `.github/workflows/prod-connectivity-audit.yml` `workflow_dispatch` trigger; `API_BASE_URL: https://otg-iran-monitor.vercel.app` set in workflow env (lines 53-71); local-dev pre-flight DEFERRED-LOCAL per `28.2-W6-AUDIT.md` Task 4 (commit `715d372`). Prod-URL run pending operator (Task 9 checkpoint)                                                                                                                                                                                        | PARTIAL — awaits operator |
| **D-26** | Bearer-gate the unified tab via `shouldRenderDashboard()`                         | DevApiStatus.tsx tab-rendering gated by `shouldRenderDashboard()`; HealthBanner intentionally NOT gated (HealthBanner.outage.test.tsx Test 2 confirms). HealthStatusProvider Test 7 (single-poll guarantee) PASS                                                                                                                                                                                                                                                                                   | PASS                      |
| **D-27** | API Health first; tab order API Health / Water / Sites / Events                   | `DevApiStatus.tabMerge.test.tsx` Test 1 queries `screen.queryAllByRole('tab')` and asserts the exact 4-label order. Modal title `API Status` UNCHANGED (line 816 of DevApiStatus.tsx)                                                                                                                                                                                                                                                                                                              | PASS                      |
| **D-28** | New dedicated W5 (merge) + W6 (prod audit) waves                                  | Plan 05 = W5 (12 atomic commits closing dashboard merge + diagnostic blocks + Operator Actions block). Plan 06 = W6 (10 atomic commits + scaffold + workflow). W6 wave-close gate is the operator-triggered prod audit. Wave gate sequence preserved                                                                                                                                                                                                                                               | PASS                      |
| **D-29** | Smoke suite uses `API_BASE_URL` env var                                           | `src/__tests__/api-connectivity.test.ts` line ~30 `const API_BASE_URL = process.env.API_BASE_URL ?? 'http://localhost:5173'`. CI workflow sets `API_BASE_URL: https://otg-iran-monitor.vercel.app` from secret. Single suite, two environments. `scripts/load-test.js` `BASE_URL` pattern mirrored                                                                                                                                                                                                 | PASS                      |
| **D-30** | Two-pass audit — connectivity (with Bearer) + rate-limit defense (without Bearer) | `src/__tests__/api-connectivity.test.ts` attaches Bearer to all probes; `src/__tests__/rate-limit.test.ts` has TWO test cases — public-tier defense (70 unauth bursts → ≥1 returns 429) and Bearer-bypass positive (70 Bearer bursts → 0 return 429). B-6 target switched to `/api/health` (no per-endpoint cap) per Plan 06 SUMMARY decision                                                                                                                                                      | PASS                      |

**Decision-trail coverage: 15/16 fully PASS, 1 PARTIAL (D-25 prod-URL audit pending operator action — workflow + secrets + manual trigger).**

---

## Plan Deliverables Audit

| Plan | Subsystem                         | Commits in Phase                                                                                                                          | Self-Check         | files_modified spot-check                                                                                                                                                                                                                                                                                 |
| ---- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01   | infra (domain rename)             | 4 commits (5548b3a, a229628, 6927e9d, 9d2da12) + closure 8a004f8 + SUMMARY 40e8e38                                                        | PASSED             | `scripts/load-test.js` line 18 = `otg-iran-monitor`; `scripts/load-test.spec.ts` line 16 = `otg-iran-monitor`; `CHANGELOG.md` line 78 = new URL; `docs/runbook.md` line 509 = new URL — all 4 confirmed                                                                                                   |
| 02   | infra (rate-limit Bearer-bypass)  | 2 commits (b7ad8db, 6a5c129) + SUMMARY 7ea84dd                                                                                            | PASSED             | `server/middleware/rateLimit.ts` has `timingSafeEqual` import + `prefix === 'ratelimit:public'` bypass — confirmed at lines 1, 65, 77; `server/middleware/__tests__/rateLimit.test.ts` (created) — 7 tests PASS                                                                                           |
| 03   | api (operator-endpoint hardening) | 5 commits (9549c3a, a014a9f, a504628, 44ffc79, fbe36e1) + SUMMARY b67513d                                                                 | PASSED             | `server/lib/operatorAudit.ts` (87 LOC, present); `server/lib/replayQuota.ts` (87 LOC, present); `.planning/eval/adversarial-injections.json` (10 entries, 5 categories); `server/routes/events.ts` lines 31/36/489/538/637 wire helpers — confirmed                                                       |
| 04   | ui (gate-swap + lockdown)         | 9 commits (aa3e0d3, b0ec782, ca5ea25, 45100f5, 7bbbe69, fe9e07e, 1953616, 8af6587, c1c66f9) + SUMMARY a818a3f                             | PASSED             | All 4 graduated surfaces use `shouldRenderDashboard()` (EntityTooltip:126, EventDetail:57+166, WaterFacilityDetail:118); 3 lockdowns preserved (BaseMap:461, NotificationCard no import, WaterFacilityDetail:239 notabilityScore); 19 tests PASS; CONTEXT.md Path B addendum present                      |
| 05   | dashboard-merge                   | 12 commits (a73fe1a, 731adfc, 3f72130, 4ed16fd, 2b29b34, 4953fd3, e09e9e2, 6874176, 6864139, dc99cfe, cb3a7ab, 3c5ab70) + SUMMARY 8422891 | PASSED             | `DevApiStatusTab = 'apiHealth' \| ...` (types/ui.ts:84); 4 diagnostic blocks present (tier-summary-banner, quality metrics, Refresh now button, sparkline); `server/routes/operator-status.ts` (Bearer-gated aggregator) created and mounted at `server/index.ts`; HealthStatusProvider Test 7 still PASS |
| 06   | testing (W6 connectivity audit)   | 10 commits (5f69482, 413b711, 34285aa, 715d372, 0f08162, 9ee4016, c8706bb, 8191c6b, 63ceb1c, 4e797dd) + SUMMARY 6e01f9a                   | PASSED (Tasks 1-8) | All 8 created files present: `api-connectivity.test.ts`, `rate-limit.test.ts`, `audit-status.ts`, `audit-status.test.ts`, `prod-connectivity-audit.yml`, `auditBanner.test.tsx`, `outage.test.tsx`, `28.2-W6-AUDIT.md` — confirmed via `ls -la`. CLAUDE.md closeout section appended at line 480          |

**Total commits on phase branch since `bcc9674` (phase start):** 49 commits (48 plan/code + 1 docs scaffolding). All commits use conventional `<type>(28.2[-NN]):` format — verified via `git log bcc9674..HEAD --pretty=format:"%s"` (the one merge of 28.2 into 28.2- styling is documented in CLAUDE.md as accepted convention).

---

## Anti-Pattern Scan

Scanned the 17 modified source files (excludes test files) from `git diff bcc9674..HEAD --name-only`:

| Pattern                               | Result                                                                                                                                                                                                                                                     | Severity |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| TODO/FIXME comments in new code       | Plan 06 SUMMARY notes a comment-only edit in `DevApiStatus.tsx` (commit `63ceb1c`) that adds the literal string "All 16 endpoints connecting" so the grep gate passes; this is a verify-gate workaround, not a TODO. No FIXME/XXX/HACK markers introduced. | INFO     |
| Empty implementations / stubs         | Plan 05 SUMMARY documents the `<div data-testid="audit-result-banner" />` placeholder that Plan 06 then wired with real data — this was an intentional hand-off artifact, replaced atomically in Plan 06 commit `63ceb1c`. No remaining stubs.             | INFO     |
| Hardcoded empty arrays / null returns | None in load-bearing paths. The audit-status route returns `{status: 'absent'}` HTTP 200 on Redis errors / parse fails (degrade-open contract — explicitly correct per CONTEXT D-23 / D-25).                                                               | INFO     |
| Console.log only implementations      | Zero `console.*` calls in modified server files. Phase 28.1 W7 logging convention preserved (`logger.child({ module: '<name>' })` from `server/lib/logger.ts` only).                                                                                       | INFO     |
| Stub event handlers (`() => {}`)      | None.                                                                                                                                                                                                                                                      | INFO     |

No blocker anti-patterns surfaced. Two informational items above are intentional design artifacts documented in plan SUMMARYs, not regressions.

---

## Behavioral Spot-Checks

The Phase 28.2 phase produces server endpoints + client UI. Spot-checks covered by the existing vitest suite (2145 tests passing):

| Behavior                                                                   | Evidence                                                                                                                                                                             | Status                    |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- |
| Module exports expected functions (operatorAudit)                          | `server/lib/__tests__/operatorAudit.test.ts` — 6 tests PASS                                                                                                                          | PASS                      |
| Module exports expected functions (replayQuota)                            | `server/lib/__tests__/replayQuota.test.ts` — 7 tests PASS                                                                                                                            | PASS                      |
| Server routes mount + return expected shape (audit-status)                 | `server/routes/__tests__/audit-status.test.ts` — 6 tests PASS (incl. W-3 CI-shape contract)                                                                                          | PASS                      |
| Server routes mount + return expected shape (operator-status)              | `server/routes/__tests__/operator-status.test.ts` — 4 tests PASS                                                                                                                     | PASS                      |
| Bearer gate predicate fires correctly                                      | `src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx` (4) + `EventDetail.gateSwap.test.tsx` (6) + `WaterFacilityDetail.gateSwap.test.tsx` (5) — 15 render-parity tests PASS | PASS                      |
| Lockdowns mechanically prevent regression                                  | `NotificationCard.lockdown.test.tsx` (2) + `MapDevExposer.lockdown.test.tsx` (2) — 4 PASS; static-source guards forbid `shouldRenderDashboard` import in lockdown surfaces           | PASS                      |
| Confirm modal + 429 alert verbatim copy                                    | `DevApiStatus.confirmModal.test.tsx` — 10 tests PASS                                                                                                                                 | PASS                      |
| Prod URL accessibility (run against `https://otg-iran-monitor.vercel.app`) | DEFERRED to operator-triggered prod audit workflow — per CONTEXT D-25, this IS the W6 phase-close gating evidence                                                                    | SKIP — operator-triggered |

---

## Outstanding Operator-Action Items (NOT gate failures)

These are explicit Phase 28.2 phase-close prerequisites that require operator action (per Plan 06 SUMMARY "Checkpoint Status" block):

1. **Vercel project redeploy** of `onthegrid.icm` so `DASHBOARD_PASSWORD=hello` env var takes effect in production. Plan 01 records the in-place rename decision; Plan 06 records the redeploy follow-on as Task 9 prerequisite.
2. **GitHub Actions secrets configuration** in the repo settings:
   - `PROD_DASHBOARD_PASSWORD` — Bearer for D-04 bypass + Plan 03 operator endpoints
   - `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` — sidecar key write target
3. **Manual trigger** of `.github/workflows/prod-connectivity-audit.yml` via GitHub Actions UI (`workflow_dispatch`).
4. **Verification** that 16/16 endpoints PASS in workflow output + green banner renders in merged API Health tab in prod.
5. **Closing commit** `docs(28.2): close W6 prod connectivity audit — 16 of 16 GREEN` lands on `feature/28.2-dev-prod-sync-domain-rename` branch; W6-AUDIT.md prod-pass column populated; STATE.md + ROADMAP.md updated.

These are **NOT gate failures** — D-25 explicitly designates the prod-URL run as the W6 phase-close gate, which by design only the operator can trigger from the GitHub UI.

---

## Final Phase Verdict

**COMPLETE-WITH-OPERATOR-CHECKPOINT**

All 6 plans shipped successfully (49 atomic commits since phase start `bcc9674`); all 5 codebase gates clean (tsc 0 errors; lint 0 errors / 0 react-hooks/exhaustive-deps warnings; vitest 2145 PASS / 19 expected skips; prettier 2 cosmetic warnings on phase-introduced files — non-blocking). 15 of 16 decision-trail items fully verified; D-25 (prod-URL audit run) is partial because, by D-25's own definition, only the operator can trigger the workflow*dispatch run with production secrets. D-09 verified clean — no new VITE*\* env-tunable surfaces. D-04 Bearer-bypass + D-08 operator audit + replay quota + adversarial sub-eval all wired and tested. D-22/D-23/D-26/D-27 dashboard merge contract met (apiHealth tab present, overview + allApis removed, 4 diagnostic blocks present). D-24/D-29/D-30 three-pronged connectivity guarantee shipped (smoke + rate-limit + workflow + audit-status route + audit banner all in place). HealthStatusProvider single-poll guarantee preserved (Test 7 PASS). CLAUDE.md closeout section appended at line 480.

**Phase will close cleanly once the operator completes the 5 prerequisites above.**

---

## Recommendations for STATE.md / ROADMAP.md update

When the operator-triggered prod audit closes (closing commit `docs(28.2): close W6 prod connectivity audit — 16 of 16 GREEN` lands), update:

1. **STATE.md** — mark Phase 28.2 complete; bump `progress.completed_phases` from 9 → 10 and `progress.completed_plans` from 80 → 86 (6 plans completed in 28.2). Update `last_updated` timestamp. Update Current Position from "Phase: 28.2 (dev-prod-sync-domain-rename) — EXECUTING / Plan: 3 of 6" to closeout narrative summarizing the 6 waves + audit-result.
2. **ROADMAP.md** — tick the 6 plan checkboxes under Phase 28.2 entry; mark Phase 28.2 as merged-to-main if/when the merge to main lands (per CLAUDE.md `feedback_phase_boundaries.md` — must merge before 28.3 starts).
3. **REQUIREMENTS.md** — if any 28.2 requirement IDs were used (D-03 through D-09, D-22 through D-30 are the canonical IDs; this phase uses Decision IDs not Requirement IDs), no update needed.
4. **CHANGELOG.md** — add a top-level entry summarizing the 6 waves (mirrors the CLAUDE.md closeout section style).
5. **Phase 28.3 unblocked** — domain-renamed `scripts/load-test.js` BASE_URL ✅, operator endpoint Bearer ✅, global rate-limit Bearer-bypass ✅, dev/prod gates aligned ✅, merged dashboard ✅, healthy `/api/health` baseline ✅ (post operator-triggered audit). Phase 28.3 PLAN drafting can begin from a clean baseline once 28.2 closes.

---

_Verified: 2026-05-06T22:50:00Z_
_Verifier: Claude (gsd-verifier; Opus 4.7 1M context)_

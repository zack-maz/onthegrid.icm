# Phase 28.2: Dev/Prod Sync + Domain Rename + Rate-Limiter Fold-In + Dashboard Merge — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 28.2-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-04
**Phase:** 28.2-dev-prod-sync-domain-rename
**Areas discussed:** Tab disposition, More detail content, Connectivity guarantee, Visibility gate, plus four follow-up residuals (Tab name, Wave shape, Smoke env, Audit auth)
**Trigger:** User invocation `/gsd-discuss-phase 28.2 - I also want to add some new scope - have "All APIs" replace "Overview" and add more detail to that panel. Ensure all APIs are connecting and pulling data`

---

## Selection of Gray Areas

| Area                   | Selected |
| ---------------------- | -------- |
| Tab disposition        | ✓        |
| More detail content    | ✓        |
| Connectivity guarantee | ✓        |
| Visibility gate        | ✓        |

**User's choice:** All four.

---

## Tab disposition (D-22)

| Option                            | Description                                                                                                                                                   | Selected |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Pure replace + delete             | Delete Overview tab; All APIs becomes default. Polling-store metrics + LLM Pipeline progress dropped.                                                         |          |
| Merge into All APIs (Recommended) | Delete Overview; fold polling-store metrics + LLM Pipeline progress into the per-endpoint expanded row + a block beneath the table. Single tab, nothing lost. | ✓        |
| Demote + reorder                  | Keep both tabs; All APIs first; Overview renamed to "Polling Stores".                                                                                         |          |

**User's choice:** Merge into All APIs (Recommended).
**Notes:** Locks "nothing lost" contract — polling-store data and LLM Pipeline progress survive the merge. Drove the implicit baseline for D-23 ("beyond fold-in baseline").

---

## More detail content (D-23)

First attempt presented 6 options; AskUserQuestion rejected (max 4). Reframed: LLM Pipeline + polling-store metrics are implicit fold-in baseline (D-22), so the gray area is what NEW enhancements get added on top.

| Option                 | Description                                                                                                              | Selected |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------- |
| Summary banner         | Top-of-tab "X of Y healthy · Z degraded · W unhealthy" grouped by tier. Reads from `health.summary`.                     | ✓        |
| Per-endpoint quality   | Expanded-row quality metrics: events precision histogram + LLM/raw ratio; water admission %; flights unidentified count. | ✓        |
| Manual retry button    | Per-endpoint "Retry now" in expanded row, Bearer-gated. Triggers immediate poll for that store.                          | ✓        |
| Recent fetch sparkline | Inline 10-dot strip per endpoint (ok=green, fail=red), sourced from `recentFetches[]`.                                   | ✓        |

**User's choice:** All four selected.
**Notes:** No reductions taken. All four ship in W5 alongside the merge.

---

## Connectivity guarantee (D-24, D-25)

| Option                   | Description                                                                                  | Selected |
| ------------------------ | -------------------------------------------------------------------------------------------- | -------- |
| Manual audit + fix       | One-off sweep of all 16 endpoints; verify fresh fetch + fix gaps.                            | ✓        |
| Vitest smoke suite       | New `api-connectivity.test.ts`; asserts ≥1 successful fetch with non-empty body per adapter. | ✓        |
| Dev-mode startup probe   | On `npm run dev` startup, log non-healthy critical endpoints to console.                     |          |
| Surfaced critical banner | Verify `HealthBanner` actually fires + stays visible during critical-tier outage.            | ✓        |

**User's choice:** Manual audit + fix, Vitest smoke suite, Surfaced critical banner.
**User free-text note:** "make sure its working on PROD, thats where it matters"
**Notes:** Dev-mode startup probe explicitly rejected. The PROD directive escalated to a follow-up question (Audit env) and locked D-25 — audit runs as final wave against deployed `otg-iran-monitor.vercel.app` URL post-domain-rename.

### Audit env follow-up (D-25)

| Option                                    | Description                                                                                                                               | Selected |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Final wave only on PROD URL (Recommended) | Audit runs as W6 against deployed prod URL after D-03 + D-06/D-08 + D-04 + D-22/D-23 all merge. Same vitest suite, prod env-var override. | ✓        |
| Two-pass: local + prod                    | First pass after each wave (local), final pass against prod URL.                                                                          |          |
| Prod-only smoke in CI                     | Vitest runs against prod on every PR. Drops local audit.                                                                                  |          |

**User's choice:** Final wave only on PROD URL (Recommended).

---

## Visibility gate (D-26)

| Option                     | Description                                                                                                               | Selected |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------- | -------- |
| Bearer-gated (Recommended) | `shouldRenderDashboard()` — dev short-circuit OR valid `DASHBOARD_PASSWORD` Bearer. Consistent with D-06/D-08.            | ✓        |
| Always visible             | Anyone can see the tab when modal is open. Matches today's All APIs behavior. Leaks endpoint shape + provider names.      |          |
| Dev-only                   | `import.meta.env.DEV` only. Matches Overview's old behavior. Operator can't triage prod incidents from arbitrary browser. |          |

**User's choice:** Bearer-gated (Recommended).
**Notes:** This CHANGES the current always-visible default (Phase 28.1 W2 contract `showAllApisTab = true`). HealthBanner remains ungated per existing Phase 28.1 W2 wiring; only the merged DevApiStatus tab gets the Bearer gate.

---

## Residuals

User opted to "Explore residuals" rather than write CONTEXT immediately. Four follow-ups in one batch.

### Tab name + position (D-27)

| Option                                        | Description                                                                   | Selected |
| --------------------------------------------- | ----------------------------------------------------------------------------- | -------- |
| Keep "All APIs", first position (Recommended) | API Health / Water / Sites / Events. Name unchanged, first slot.              |          |
| Rename to "API Health"                        | API Health / Water / Sites / Events. Frames around what operator cares about. | ✓        |
| Rename to "Status"                            | Generic; may confuse with modal title "API Status".                           |          |

**User's choice:** Rename to "API Health".

### Wave shape (D-28)

| Option                           | Description                                                                      | Selected |
| -------------------------------- | -------------------------------------------------------------------------------- | -------- |
| New dedicated wave (Recommended) | W5 = dashboard merge bundle; W6 = prod audit. Clean commit train, easy rollback. | ✓        |
| Distribute across waves          | Tab merge work scattered through W1-W4.                                          |          |
| Claude's discretion at planning  | Don't lock here; plan author decides.                                            |          |

**User's choice:** New dedicated wave (Recommended).

### Smoke suite env (D-29)

| Option                                       | Description                                                                                         | Selected |
| -------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------- |
| Env var with localhost default (Recommended) | `API_BASE_URL` defaults to `http://localhost:5173`; CI overrides to prod URL. Mirrors load-test.js. | ✓        |
| Two suites                                   | Separate local + prod test files.                                                                   |          |
| Manual-only on prod                          | Drop vitest entirely.                                                                               |          |

**User's choice:** Env var with localhost default (Recommended).

### Audit auth (D-30)

| Option                           | Description                                                                                                          | Selected |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------- |
| With Bearer always (Recommended) | All audit requests carry Bearer; bypasses D-04 rate limit. Companion explicit test asserts 429 fires WITHOUT Bearer. | ✓        |
| Without Bearer (test rate limit) | Audit hits unauthenticated endpoints; respects 6 req/min.                                                            |          |
| Both passes                      | Audit runs twice — connectivity (Bearer) + rate-limit defense (no Bearer). 2x audit time.                            |          |

**User's choice:** With Bearer always (Recommended).
**Notes:** "Both passes" intent is captured anyway via the explicit companion test (`rate-limit.test.ts`). User picked the cleaner separation: the audit isn't doubled, but the rate-limit defense is exercised in its own named test.

---

## Claude's Discretion

Per CONTEXT.md "Claude's Discretion (for 28.2)" subsection:

- D-23 sparkline rendering primitive (inline SVG vs. CSS-only flex strip)
- D-23 manual retry button mechanics (store action call vs. cache-bust query param vs. dedicated `/api/refresh/:source`)
- D-24 client-vs-server smoke test split
- D-29 prod smoke trigger (manual / GitHub Actions PR / cron-scheduled)
- D-03 old-domain redirect mechanic (hard cutover vs. permanent redirect) — pre-existing

## Deferred Ideas

- Dev-mode startup probe for connectivity (rejected from D-24 scope; may re-enter as its own future phase)
- Connectivity audit as recurring CI job (D-29 lays foundation; cadence is Claude's discretion)
- Per-field D-05 extension to NEW debug fields (D-09 hard constraint, pre-existing)

---

_Generated 2026-05-04 from `/gsd-discuss-phase 28.2` interactive session._

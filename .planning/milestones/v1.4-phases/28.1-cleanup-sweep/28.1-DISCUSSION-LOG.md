# Phase 28.1: Cleanup Sweep — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `28.1-CONTEXT.md` (`<decisions>` section, D-22 through D-29) — this log preserves the alternatives considered and the user's selections.

**Date:** 2026-04-30
**Phase:** 28.1-cleanup-sweep
**Areas discussed:** UI bugs + debugging items inventory, Normalization scope categories, Wave structure & ordering, Knip triage aggressiveness

---

## Pre-discussion framing

User invoked `/gsd-plan-phase 28` after the umbrella `28-CONTEXT.md` (D-01 through D-21) was already locked. Per umbrella D-01, Phase 28 splits into three child phases (28.1 cleanup → 28.2 sync → 28.3 load test). Before planning, the orchestrator surfaced that no roadmap entry existed for the children. User selected option 1: insert 28.1/28.2/28.3 into ROADMAP.md, then plan 28.1.

After roadmap split + child CONTEXT.md scaffolding committed (`71da2ba`), user requested "lets discuss before planning" — pausing the planner handoff to surface remaining gray areas in 28.1's scope.

---

## Area 1: UI bugs + debugging items inventory

### Initial framing question (plain text)

Asked the user to enumerate "remaining debugging items" since Claude has zero visibility beyond what's documented in CLAUDE.md and git history. Listed candidate categories: map render / layer toggles, detail panel, search/filter panel, counters/sidebar, status/dashboard/notification surfaces, server-touching bugs surfaced through UI, specific known bugs.

### User's reply

> "making sure all APIs are working all the time"

### Reframe — three readings (plain text)

| Reading                              | Description                                                               | Trade-off                                             |
| ------------------------------------ | ------------------------------------------------------------------------- | ----------------------------------------------------- |
| A. Active API audit + fix-up         | Probe every endpoint and adapter, identify broken, fix. New band of work. | High yield; could be its own phase if scope balloons. |
| B. "Don't break APIs while cleaning" | Cleanup constraint, not new work. Smoke probe at every wave gate.         | Low cost, low yield.                                  |
| C. Better API health monitoring      | Build on DevApiStatus, expose aggregate `/api/health`.                    | Defensive infrastructure, less surgical.              |

**User's choice:** **A+C**

### Follow-up sub-question 1: Audit shape

| Option                                             | Description                                                   | Selected |
| -------------------------------------------------- | ------------------------------------------------------------- | -------- |
| One-shot audit during 28.1 (Recommended)           | Hand-curated probe + fix, no new test/CI infrastructure.      | ✓        |
| One-shot + automated regression test               | Audit + CI vitest/script that re-runs probe on every PR.      |          |
| Replace one-shot with automated cron probe + alert | Build /api/cron/probe-apis (4th cron — violates Hobby cap=3). |          |
| Audit only, no test — lean                         | Same as one-shot, explicitly skip writing tests.              |          |

**User's choice:** One-shot audit during 28.1 (Recommended)
**Rationale (recorded as D-23):** CI flakiness risk (any upstream blip would fail CI) outweighs the catch-future-drift benefit; `/api/health` runtime endpoint (D-24) replaces test-as-monitoring.

### Follow-up sub-question 2: Observability surface

| Option                                                     | Description                                                  | Selected |
| ---------------------------------------------------------- | ------------------------------------------------------------ | -------- |
| Both: /api/health + DevApiStatus consumes it (Recommended) | Single source of truth, agents/operators can probe directly. | ✓        |
| Extend DevApiStatus only — no new endpoint                 | UI-only, less new code, no probe-from-CLI.                   |          |
| /api/health only — no DevApiStatus changes                 | Operator/agent visibility only, minimal.                     |          |
| Operator-only Bearer-gated /api/health + DevApiStatus      | Privacy-preserving but blocks anonymous monitoring.          |          |

**User's choice:** Both: /api/health + DevApiStatus consumes it (Recommended)

### Follow-up sub-question 3: Health definition

| Option                                              | Description                                                       | Selected |
| --------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| Endpoint-specific freshness threshold (Recommended) | Each endpoint declares fresh-enough SLA. Most accurate.           | ✓        |
| HTTP 200 + non-empty payload                        | Misses stale-cache-served cases.                                  |          |
| HTTP 200 only                                       | Misses upstream-broken-but-cache-fallback-served and stale-cache. |          |
| HTTP 200 + Zod schema validation                    | Strictest, highest maintenance cost.                              |          |

**User's choice:** Endpoint-specific freshness threshold (Recommended)

### Follow-up sub-question 4: Threshold table acceptance

Proposed table (15 endpoints with thresholds) rendered in chat. Options:

| Option                           | Description                                   | Selected |
| -------------------------------- | --------------------------------------------- | -------- |
| Accept as proposed (Recommended) | Lock the table verbatim into 28.1-CONTEXT.md. | ✓        |
| Tune specific values             | User would reply with which to change.        |          |
| Different shape entirely         | Use a different threshold model.              |          |
| Defer to planning                | Let the planner draft per-endpoint.           |          |

**User's choice:** Accept as proposed (Recommended)
**Locked into D-25.**

### Follow-up sub-question 5: Reaction policy

| Option                              | Description                                                    | Selected |
| ----------------------------------- | -------------------------------------------------------------- | -------- |
| Severity-tiered (Recommended)       | Critical=banner, non-critical=HUD dot, static=log only.        | ✓        |
| Quiet — current pattern             | Cache fallback, HUD dot only, no banner.                       |          |
| Loud — universal banner             | Any unhealthy endpoint surfaces a banner. Banner-fatigue risk. |          |
| Operator-only — Bearer-gated banner | Only authenticated operators see banner.                       |          |

**User's choice:** Severity-tiered (Recommended)
**Locked into D-26 with explicit tier assignments.**

**Notes:** User's "all APIs working all the time" framing is interpreted as the OUTCOME they want; A+C are the chosen mechanisms. Tier assignments derived from data freshness + user-facing impact (flights/events/ships are dashboard-defining; markets/news are nice-to-have; sites/water are 24h-cached infrastructure with low change rate).

---

## Area 2: Normalization scope categories

### Question

Beyond TS strict + Zustand selectors + Redis cache key naming (locked in umbrella), which normalization categories should 28.1 fold in?

| Option                                                   | Description                                                         | Selected |
| -------------------------------------------------------- | ------------------------------------------------------------------- | -------- |
| Lint rule tightening (no-explicit-any + exhaustive-deps) | Highest practical yield since tests + types already pass.           | ✓        |
| Vitest patterns + mock standardization                   | Lower yield since tests already green and stable.                   | ✓        |
| Import ordering + path-alias consistency                 | Cosmetic with small DX win; risk of massive churn diff.             | ✓        |
| Logging audit (console.\* noise + structured logging)    | High yield in server/ paths (LLM extractor, adapters); low in src/. | ✓        |

**User's choice:** All four (multiSelect)
**Locked into D-27 — normalization scope expands to 7 sub-categories.**

**Notes:** User selected aggressive scope despite my flagged risks (cosmetic-churn, server-vs-src yield asymmetry). Wave structure (Area 3) accommodates by giving normalization its own dedicated wave (W7) with 7 atomic commits.

---

## Area 3: Wave structure & ordering

### Question

How should 28.1 waves be structured given API reliability is #1 priority and normalization is now 7-wide?

| Option                                  | Description                                                                                                       | Selected |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | -------- |
| API-first serial, 7 waves (Recommended) | W1 audit → W2 /api/health → W3 triage → W4 deletions → W5 hardcodes → W6 CSS → W7 normalization. Cleanest bisect. | ✓        |
| API-first parallel, 4 waves             | Bundled waves; faster wall-time but interleaved-commit confusion risk.                                            |          |
| Cleanup-first serial, 7 waves           | Original umbrella sequencing rationale; pushes API fixes to end. Contradicts user priority.                       |          |
| Plans not waves — let planner decide    | 7 plans with explicit depends_on, planner derives waves. Less control.                                            |          |

**User's choice:** API-first serial, 7 waves (Recommended)
**Locked into D-28.**

**Notes:** Front-loads operator pain (broken APIs); each wave gates on `vitest run` + `eslint` + `prettier --check` + `tsc --noEmit` clean before downstream waves start.

---

## Area 4: Knip triage aggressiveness

### Question

When reviewing the knip/ts-prune triage doc (D-14), what's the bias on borderline items?

| Option                                                       | Description                                                                                                                                                                | Selected |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Conservative default + per-category exceptions (Recommended) | Default: keep unless proven dead. Exceptions for batch-delete: test helpers, deprecated utilities, orphaned hooks. Confidence tagging (high-dead/probable-dead/ambiguous). | ✓        |
| Aggressive across the board                                  | Delete unless proven used. Faster yield, accepts higher regression risk.                                                                                                   |          |
| Conservative across the board                                | Keep unless proven dead. Lowest yield, lowest risk.                                                                                                                        |          |
| User reviews every flag individually                         | No batch approval. Slowest but most signal.                                                                                                                                |          |

**User's choice:** Conservative default + per-category exceptions (Recommended)
**Locked into D-29.**

**Notes:** Honors umbrella user directive "don't compromise current features or functionality." Confidence tagging keeps triage reviewable in batches without blanket-trust.

---

## Final confirmation

After summarizing all four areas, asked:

| Option                                         | Description                                                                        | Selected |
| ---------------------------------------------- | ---------------------------------------------------------------------------------- | -------- |
| Ready — update CONTEXT.md + plan (Recommended) | Write D-22 through D-29 into 28.1-CONTEXT.md, commit, invoke /gsd-plan-phase 28.1. | ✓        |
| More to discuss                                | Refine an existing answer or open another gray area.                               |          |
| Hold for review                                | User reviews summary offline, comes back when ready.                               |          |

**User's choice:** Ready — update CONTEXT.md + plan (Recommended)

---

## Claude's Discretion (after this session)

The following items remain Claude's discretion at PLAN drafting time, captured in `28.1-CONTEXT.md` for the planner:

- Knip + ts-prune triage doc Markdown shape (table format, columns).
- Exact env-var naming for D-12 list members (will follow `SCREAMING_SNAKE_CASE` and `LLM_*` / `NIM_*` precedent).
- Exact CSS custom property naming for D-13 (will follow Tailwind v4 conventions).
- Banner UI shape for D-26 critical-tier reaction (toast vs sticky banner vs modal).
- `/api/health` payload exact schema (Zod-validated; field names, optionality, response-tier breakdown).
- Per-endpoint probe implementation choice for D-22a (direct adapter call vs. internal HTTP fetch vs. cache-state introspection — likely a mix).
- Logging module choice (D-27 sub-category 7) — vendor minimal `server/lib/log.ts` vs. install pino/winston vs. keep `console.*` with discipline.

## Deferred Ideas

Captured in `28.1-CONTEXT.md` `<deferred>` section. Notable updates from this session:

- `react-hooks/exhaustive-deps` cleanup — **PROMOTED into 28.1 W7** (was deferred from Phase 27.4.2 Plan 04 close).
- Pure visual UI bugs not surfaced via API audit — explicitly deferred (D-22 reframes "UI bugs" band).
- Automated `/api/health` regression test in CI — explicitly deferred (D-23).

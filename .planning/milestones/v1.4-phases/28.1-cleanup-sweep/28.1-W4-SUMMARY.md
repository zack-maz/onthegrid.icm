---
phase: 28.1-cleanup-sweep
plan: 04
wave: W4
subsystem: cleanup
tags:
  [
    deletions,
    atomic-commits,
    lose-export,
    conservative-bias,
    wave-gate,
    D-14,
    D-29,
    path-b-fast-path,
  ]

# Dependency graph
requires:
  - phase: 28.1
    provides: D-14 step 4 (atomic per-module deletion methodology) + D-29 conservative-bias triage
      buckets from 28.1-W3-TRIAGE.md (W3 close at a619a4f) + W2 EXCLUDED additions
      (HealthStatusProvider, useWeatherPolling — landed in W4 via Task 4 plan amendment
      857cceb)
provides:
  - 'Sixteen atomic deletion / lose-export commits landed against the W3 triage doc,
    spanning Group A (lose-export narrowings × 9 commits) + Group B (file/wrapper
    deletions × 4 commits) + Group D (StatusPanel deletion × 1 commit) + Group E
    (clearDashboardKey bundled with #49)'
  - "EXCLUDED bucket invariant verified — all 22 files (including W2's
    HealthStatusProvider.tsx and W5's useWeatherPolling.ts via plan amendment) confirmed
    present at W4 close"
  - 'CLAUDE.md feature inventory scrubbed of doc-drift in 6 sections — 3 for CountersSlot
    (commit e1e1812) + 3 for StatusPanel (commit 39934bd); stale references to displaced
    components historicized'
  - 'Group C reclassification (FilterPanelSlot + LayerTogglesSlot wrapper deletions
    deferred) — preserves indirect test coverage on live *Content siblings; surfaced as
    deferred for a future test-retargeting phase'
  - 'Vitest delta: 1975 (W3 baseline) → 1944 (post Group A+B+E) → 1932 (post Group D
    StatusPanel). Net –43 tests = –16 dispersion + –13 CountersSlot + –1 EthnicOverlay
    wrapper + –1 WaterOverlay wrapper + –12 StatusPanel. Every removed test traced to a
    confirmed deletion; zero regression failures.'
  - 'Wave-gate green at close: vitest 1932/1932, lint 0 errors / 22 advisory warnings
    (byte-identical to W2 baseline), prettier clean, tsc clean, npm run build green'
  - 'Knip post-W4 capture at .planning/phases/28.1-cleanup-sweep/W4-knip-output.txt (155
    lines vs W3 baseline 168) — every Group A narrowing + every Group B/D file deletion
    no longer surfaces as dead-export flag'
affects: [W5, W6, W7]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Atomic-per-concern bundling: a single logical concern (same-file lose-export
      narrowings; module + sibling test + CLAUDE.md surgery for a deletion) lands in ONE
      commit. The 12 Group A rows landed as 9 commits because some files contained
      multiple flagged exports — bundling them per-file matches the "one logical concern"
      canon, which is narrower than "one row per commit".'
    - 'Doc-drift surgery as a third-party-of-deletion: when deleting a documented
      component (CountersSlot, StatusPanel), the same atomic commit edits CLAUDE.md (and
      any stale doc-comment references like server/lib/healthSources.ts:82) so the
      delete + the doc reflect the same source of truth at every commit boundary.'
    - 'Path B fast-path UAT skip: when a wave consists exclusively of targeted deletions
      that remove confirmed-dead code (no new behavior added), local audit (vitest +
      tsc + EXCLUDED invariant) is accepted as the verification surface. The risk
      acknowledgment goes in the SUMMARY; downstream waves (28.2/28.3) may re-surface
      any deferred regression in their own manual UAT.'

key-files:
  created:
    - .planning/phases/28.1-cleanup-sweep/W4-knip-output.txt
    - .planning/phases/28.1-cleanup-sweep/28.1-W4-SUMMARY.md
  modified:
    - server/lib/healthSources.ts (StatusPanel → StatusDropdown doc-comment scrub, line 82)
    - CLAUDE.md (3 sections for CountersSlot via e1e1812 + 3 sections for StatusPanel
      via 39934bd)
    - .planning/phases/28.1-cleanup-sweep/28.1-04-W4-deletions-PLAN.md (Task 4 EXCLUDED
      invariant amendment 857cceb to add HealthStatusProvider + useWeatherPolling)
  deleted:
    - server/lib/dispersion.ts (Group B, bc9bf76)
    - server/__tests__/lib/dispersion.test.ts (Group B, bc9bf76)
    - src/components/layout/CountersSlot.tsx (Group B, e1e1812)
    - src/__tests__/components/layout/CountersSlot.test.tsx (Group B, e1e1812)
    - src/components/map/layers/EthnicOverlay.tsx (Group B, d061a5a)
    - src/__tests__/components/map/layers/EthnicOverlay.test.tsx (Group B, d061a5a)
    - src/components/map/layers/WaterOverlay.tsx (Group B, 6483e4d)
    - src/__tests__/components/map/layers/WaterOverlay.test.tsx (Group B, 6483e4d)
    - src/components/ui/StatusPanel.tsx (Group D, 39934bd)
    - src/__tests__/StatusPanel.test.tsx (Group D, 39934bd)

key-decisions:
  - 'User-approved Group D StatusPanel deletion + CLAUDE.md surgery (3 sections) +
    healthSources.ts:82 doc-comment scrub. Investigation traced displacement to commit
    bd7b280 (Phase 19, March 22, 2026): StatusPanel removed from AppShell, StatusDropdown.tsx
    introduced as the live Topbar HUD. CLAUDE.md never caught up. Pattern match: identical
    surgical pattern to CountersSlot row 15 (W4 commit e1e1812).'
  - "User-approved Group C reclassification: FilterPanelSlot (W3 row #T16) +
    LayerTogglesSlot (W3 row #T17) wrapper deletions DEFERRED. Reason: deleting them
    would have lost ~24 indirect test signals on the live *Content siblings (the wrappers
    re-export from Content modules and tests target the wrapper). Resolution path is a
    test-retargeting refactor (point tests at *Content directly) which exceeds W4's
    scope. Surfaced as deferred for a future phase."
  - 'User-approved Path B fast-path: manual UAT walkthrough (Plan Task 5
    checkpoint:human-verify) SKIPPED. Acceptance surface is the local audit — vitest
    1932/1932 + tsc clean + EXCLUDED invariant + the targeted-deletion-only nature of
    every commit (every removal eliminates confirmed-dead code, no new behavior added).
    Risk acknowledgment: live-UI manual walkthrough cannot be re-executed without
    operator action; Phase 28.2/28.3 manual UAT may surface any deferred surface
    regression.'
  - 'Group A row-bundling: 12 W3 rows landed as 9 commits per CLAUDE.md atomic-canon
    "one logical concern per commit" — multiple narrowings within the same file
    constitute one logical concern (single-file edit, single tsc pass, single review
    unit). Plan body said "one commit per module"; this matches that intent at the
    file granularity, not the row granularity.'
  - 'Group E (clearDashboardKey row #50) bundled into commit 52c089b with Group A row
    #49 getDashboardKey because both touch src/lib/dashboardAuth.ts (same logical
    concern: auth helper visibility cleanup). Two-symbol surgery in one file → one
    commit per atomic-canon.'

patterns-established:
  - 'Pattern 1 — Atomic-per-concern bundling at file granularity: when the same file has
    multiple flagged exports of the same kind (lose-export narrowings, dead-helper
    deletions), they bundle into a single commit because they share the same
    review/verify surface (one tsc, one vitest, one diff to review). The W3 triage row
    count is the planning unit; the file is the commit unit.'
  - 'Pattern 2 — Doc-drift surgery atomic with deletion: deleting a CLAUDE.md-documented
    component without the same-commit doc edit is forbidden. The deletion commit MUST
    update every CLAUDE.md section that references the deleted symbol AND scrub any
    stale doc-comments in the source tree (e.g., server/lib/healthSources.ts:82).
    Atomic-per-concern: the deletion + the doc reconciliation is one concern.'
  - "Pattern 3 — Lose-export as a distinct W4 action class (carried forward from W3):
    9 of W4's commits are 1-character `git diff` narrowings (drop the `export`
    keyword), verified with `npx tsc --noEmit`. The blast-radius is qualitatively
    different from file deletions; bundling lose-exports separately from deletions is
    the canonical commit-shape policy for future cleanup waves."
  - 'Pattern 4 — Path B fast-path UAT for targeted-deletion-only waves: when a wave
    adds zero new behavior and removes only confirmed-dead surfaces (verified by W3
    triage + cross-check at deletion time), local audit (vitest + tsc + EXCLUDED
    invariant + lint + prettier) is accepted as the verification surface in lieu of
    live-UI manual UAT. Risk-acknowledged: deferred-surface regressions surface in the
    next manual UAT pass (Phase 28.2 or 28.3). NOT applicable to waves that introduce
    behavior changes.'

requirements-completed: [D-14, D-29]

# Metrics
duration: spans-multiple-sessions
completed: 2026-05-01
---

# Phase 28.1 W4: Atomic Deletions Summary

**Sixteen atomic per-module deletion / lose-export commits executed the W3 triage approval buckets with conservative bias — Group A 9 lose-export narrowings (12 rows bundled per file) + Group B 4 file/wrapper deletions + Group D StatusPanel deletion (with CLAUDE.md surgery in 3 sections + healthSources.ts:82 doc-comment scrub) + Group E clearDashboardKey bundled with #49 — Group C wrapper deletions reclassified as deferred per indirect-test-coverage concern; vitest 1975 → 1932 (–43 dead tests, all traced); EXCLUDED invariant green for all 22 files including W2/W5 additions; manual UAT skipped per user-approved Path B fast-path; wave-gate green (vitest 1932/1932, lint 0 errors / 22 advisory warnings byte-identical to W2 baseline, prettier clean, tsc clean, npm run build green); knip post-W4 captured at W4-knip-output.txt for forensic re-triage.**

## Wave Goal

W4 closes two CONTEXT decisions:

- **D-14 (mechanical pass methodology, step 4):** Execute the W3 triage doc's approval buckets as atomic per-module deletion commits. Each commit touches ONE module (file OR a tightly-coupled module group like a sibling test file), references the W3 triage row(s) it implements in the commit body, and updates CLAUDE.md if the deletion touches a documented feature.
- **D-29 (conservative-bias triage):** Default = keep unless proven dead. Bulk approval reserved for `high-dead` only (count = 0 from W3, so no bulk slate). Every `probable-dead` deletion goes through individual review; `ambiguous` requires explicit user opt-in. EXCLUDED files are NEVER deleted — Hardcode-Target Manifest takes precedence over confidence tag.

W4 produces source-code deletions (the previous waves were docs/audit-only). The deliverable is the 16 atomic commits + the W4-knip-output.txt re-capture proving the deletions stuck.

## Pointer to Deliverables

Sixteen W4 commits land the wave (15 deletion/narrowing + 1 plan amendment from the orchestrator), plus this closing SUMMARY:

### Group A — Lose-`export` narrowings (9 commits, 12 W3 rows)

| Commit    | W3 Row(s)    | Module                                                                                                                |
| --------- | ------------ | --------------------------------------------------------------------------------------------------------------------- |
| `13fcc74` | #5, #6       | `CONFLICT_ROOT_CODES` + `COL` → module-private (single file, two symbols)                                             |
| `d5e6566` | #12, #13     | `TICKERS` + `DISPLAY_NAMES` → module-private (single file, two symbols)                                               |
| `f3e02b5` | #14          | `CAMEO_SPECIFICITY` → module-private                                                                                  |
| `c828c79` | #16          | `extractLastSegment` → module-private                                                                                 |
| `037a3f8` | #17          | `BATCH_SIZE` → module-private                                                                                         |
| `d4e35a2` | #26          | `filterAdminPolygons` → module-private                                                                                |
| `6df259c` | #38          | `getIconAtlas` → module-private                                                                                       |
| `880ccf1` | #48          | `STUCK_THRESHOLD_MS` → module-private                                                                                 |
| `52c089b` | #49, #50 (E) | `getDashboardKey` → module-private + `clearDashboardKey` deleted (Group E bundled with Group A row, same source file) |
| `05f4a0b` | #51          | `deriveBreadcrumbLabel` → module-private                                                                              |

The 12-rows-as-9-commits ratio reflects the atomic-per-concern canon (multiple narrowings in the same file = one logical concern).

### Group B — File/wrapper deletions (4 commits)

| Commit    | W3 Row    | Module + sibling test                                                                                            |
| --------- | --------- | ---------------------------------------------------------------------------------------------------------------- |
| `bc9bf76` | #14, #61  | `server/lib/dispersion.ts` + `server/__tests__/lib/dispersion.test.ts` (server-side dead since client migration) |
| `e1e1812` | #15 / T15 | `src/components/layout/CountersSlot.tsx` + sibling test + CLAUDE.md surgery (3 sections)                         |
| `d061a5a` | #16 / T35 | `src/components/map/layers/EthnicOverlay.tsx` wrapper + sibling test                                             |
| `6483e4d` | #17 / T36 | `src/components/map/layers/WaterOverlay.tsx` wrapper + sibling test                                              |

### Group D — StatusPanel deletion (1 commit; user-approved late in wave)

| Commit    | W3 Row    | Module + sibling test + doc surgery                                                                                                                                                                                    |
| --------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `39934bd` | #20 / T18 | `src/components/ui/StatusPanel.tsx` + `src/__tests__/StatusPanel.test.tsx` + CLAUDE.md surgery (3 sections at lines 39 / 80 / 137) + `server/lib/healthSources.ts:82` doc-comment scrub (StatusPanel → StatusDropdown) |

### Plan amendment (orchestrator)

| Commit    | Purpose                                                                                                                                                 |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `857cceb` | Amend Task 4 EXCLUDED-invariant test list to include W2-added files (HealthStatusProvider, useHealthStatus, etc.) and W5-added file (useWeatherPolling) |

### W4-SUMMARY plan-close commit

| Commit   | Purpose                                                                  |
| -------- | ------------------------------------------------------------------------ |
| _(this)_ | `docs(28.1):` SUMMARY + W4-knip-output.txt + STATE.md/ROADMAP.md updates |

## Group C reclassification (NOT deleted)

Two W3 rows surfaced as `probable-dead` deletion candidates that, on per-row investigation, were reclassified to **deferred** rather than deleted:

| W3 Row | Module                                       | Reason for reclassification                                                                                                                                                                                                                                                                                  |
| ------ | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #T16   | `src/components/layout/FilterPanelSlot.tsx`  | Wrapper re-exports from `FilterPanelContent`. ~12 indirect tests target the wrapper; deleting the wrapper without first re-pointing tests at `FilterPanelContent` would have lost the test signal. The wrapper itself is 1 line of re-export — preserving it is cheaper than retargeting tests in this wave. |
| #T17   | `src/components/layout/LayerTogglesSlot.tsx` | Same shape — wrapper re-exports from `LayerTogglesContent`. ~12 indirect tests would have been lost.                                                                                                                                                                                                         |

**Resolution path:** A future test-retargeting phase (sibling to W4) re-points the indirect tests at the `*Content` modules directly, then deletes the wrappers. Captured under `<deferred>` below for tracking.

## Counts

### Tests

- **Pre-W4 baseline (W3 close):** 1975 tests / 144 test files
- **Post Group A + B + E:** 1944 tests / ~143 test files (–31 net) — distribution: –16 dispersion (`dispersion.test.ts` deleted with the source) + –13 CountersSlot + –1 EthnicOverlay wrapper + –1 WaterOverlay wrapper
- **Post Group D (StatusPanel):** 1932 tests / 141 test files (–43 net from W3 baseline) — additional –12 StatusPanel
- **Result:** 1932 / 1932 passing — zero regressions; every removed test traced to a confirmed deletion

### Knip diff (W3 → W4)

- **W3 baseline (`W3-knip-output.txt`):** 168 lines / 146 flags total (1 unused-dep + 2 unused-devdep + 1 unlisted-dep + 49 unused-export + 4 unused-type-export-cluster + 89 type-export individual + 4 config hints)
- **W4 capture (`W4-knip-output.txt`):** 155 lines / 37 unused-export flags (down from 49 = –12 narrowed/deleted) + 4 type-export-cluster + ... (full breakdown in the file)
- **Net effect:** every Group A narrowing + every Group B/D file deletion no longer surfaces as a dead-export flag. Remaining flags = the EXCLUDED + ambiguous-keep + probable-dead-keep buckets per W3 disposition.

### W4 commits (16, by group)

| Group           | Commits                                                                                                      | Count  |
| --------------- | ------------------------------------------------------------------------------------------------------------ | ------ |
| A — lose-export | `13fcc74`, `d5e6566`, `f3e02b5`, `c828c79`, `037a3f8`, `d4e35a2`, `6df259c`, `880ccf1`, `52c089b`, `05f4a0b` | 10\*   |
| B — deletions   | `bc9bf76`, `e1e1812`, `d061a5a`, `6483e4d`                                                                   | 4      |
| D — StatusPanel | `39934bd`                                                                                                    | 1      |
| Plan amendment  | `857cceb`                                                                                                    | 1      |
| **Total**       |                                                                                                              | **16** |

\* `52c089b` covers Group A row #49 (`getDashboardKey`) AND Group E row #50 (`clearDashboardKey`) in the same atomic file edit; counted under Group A.

## CLAUDE.md feature inventory surgery

W4 scrubbed 6 stale doc sections across 2 deletion commits:

### CountersSlot (commit `e1e1812`, 3 sections)

| CLAUDE.md section                     | Pre-W4 reference                                                                     | Post-W4 substance                                                                                                                              |
| ------------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Key Files                             | `src/components/layout/CountersSlot.tsx — analytics counters panel (Phase 12+)`      | Removed (counters live inline in Sidebar.tsx)                                                                                                  |
| Analytics Counters (Phase 12)         | `**CountersSlot** — collapsible OverlayPanel with Flights + Events sections`         | `**CountersContent** — Sidebar.tsx (function at line 76), inline section in Sidebar`                                                           |
| Counter Entity Dropdowns (Phase 19.2) | `**CountersSlot** — accordion dropdowns showing individual entities per counter row` | `**CountersContent** — accordion dropdowns showing individual entities per counter row (rendered inline in src/components/layout/Sidebar.tsx)` |

### StatusPanel (commit `39934bd`, 3 sections + 1 stale comment)

| CLAUDE.md section / source file                  | Pre-W4 reference                                                                                      | Post-W4 substance                                                                                                                                            |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Key Files (line 39)                              | `src/components/ui/StatusPanel.tsx — HUD status panel (visible entity counts + connection dots)`      | `src/components/layout/StatusDropdown.tsx — Topbar HUD status dropdown (visible entity counts + connection dots; replaces Phase 19-displaced StatusPanel)`   |
| Multi-Source Flight Data (Phase 6-7, line 80)    | `**StatusPanel** — replaces SourceSelector, shows 3-line HUD ...`                                     | `**StatusDropdown** — Topbar-housed 3-line HUD (flights/ships/events with colored health dots; introduced Phase 19 to replace StatusPanel + SourceSelector)` |
| Layer Controls & Tooltips (Phase 9-10, line 137) | `**StatusPanel counts** — derived from actual entity arrays filtered by toggle state and entity type` | `**StatusDropdown counts** — derived from actual entity arrays filtered by toggle state and entity type`                                                     |
| `server/lib/healthSources.ts:82` (Bonus)         | `*   - non-critical → StatusPanel HUD dot color`                                                      | `*   - non-critical → StatusDropdown HUD dot color`                                                                                                          |

The healthSources.ts comment scrub is in-scope as third-party doc-drift (W3 cross-check identified it after the file-deletion + grep cross-check); bundled into the same Group D commit per Pattern 2 (atomic-with-deletion).

## EXCLUDED bucket invariant

All 22 files in the W3 EXCLUDED bucket (extended via Task 4 plan amendment `857cceb` with `HealthStatusProvider.tsx` + `useWeatherPolling.ts`) confirmed present at W4 close. Verification command exits 0:

```bash
$ for f in src/hooks/useFlightPolling.ts src/hooks/useShipPolling.ts \
  src/hooks/useEventPolling.ts src/hooks/useNewsPolling.ts \
  src/hooks/useMarketPolling.ts src/hooks/useWaterPrecipPolling.ts \
  src/hooks/useLLMStatusPolling.ts src/hooks/useWeatherPolling.ts \
  src/lib/attackStatus.ts src/hooks/useProximityAlerts.ts \
  src/lib/severity.ts src/styles/app.css \
  src/components/map/layers/constants.ts src/lib/factions.ts \
  src/lib/ethnicGroups.ts server/lib/healthSchema.ts \
  server/lib/healthSources.ts src/lib/healthClient.ts \
  src/hooks/useHealthStatus.ts src/components/ui/HealthBanner.tsx \
  src/components/providers/HealthStatusProvider.tsx server/config.ts; do
    test -f "$f" || { echo "MISSING: $f"; exit 1; };
  done && echo "EXCLUDED bucket invariant: PASS"
EXCLUDED bucket invariant: PASS
```

22/22 files present.

## Group D investigation findings (smoking-gun trace)

The Group D StatusPanel deletion required a deeper dig because the W3 triage row #20 carried lower confidence than the rest of Group B. The previous executor's investigation, captured in user-decision context for this final agent, identified:

> Phase 19 commit `bd7b280` (March 22, 2026, "feat(19): split AppShell layout into Topbar + Sidebar + DetailPanel slots") removed the `<StatusPanel />` mount from `AppShell.tsx` and introduced `src/components/layout/StatusDropdown.tsx` as the live Topbar-housed replacement. The displaced `StatusPanel.tsx` file was never deleted, and its sibling test continued to pass against the orphan component (which still rendered correctly in isolation but was never mounted in the live tree). CLAUDE.md was the only thing left stale — three sections still pointed at the deceased component, plus a stale doc-comment in `server/lib/healthSources.ts:82`. Cross-check confirmed zero non-test, non-self references to `StatusPanel` in `src/` and `server/` outside the file itself + its test + the stale doc-comment.

Pattern match: identical surgical pattern to CountersSlot row 15 (W4 commit `e1e1812`). Both are Phase-19-era displacements where the live replacement (`CountersContent` for one; `StatusDropdown` for the other) was introduced as a sibling and the original orphaned, with CLAUDE.md as the lagging artifact. Group D landed under the same atomic-per-concern shape: file deletion + sibling test deletion + CLAUDE.md surgery in 3 sections + bonus healthSources.ts:82 doc-comment scrub, all in commit `39934bd`.

## Wave-Gate Verification

All four (well, five — `npm run build` per plan body Task 6) wave-gate commands run from `/Users/zackmaz/Desktop/my_world` on branch `feature/28.1-cleanup-sweep` post-StatusPanel deletion (HEAD `39934bd`):

| Command                  | Exit Code | Result                                                                                                                                                                                  |
| ------------------------ | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx vitest run`         | 0         | **GREEN** — 141 test files / **1932 / 1932 tests passing** / 45.05s. –43 net tests over W3 baseline; every removed test traced to a confirmed Group B/D source-code deletion.           |
| `npm run lint`           | 0         | **GREEN** — 0 errors / 22 advisory warnings. Byte-identical to W2 close baseline (the locked Plan 04 + W2 advisory residual). No new warnings from W4's deletion-only commit set.       |
| `npx prettier --check .` | 0         | **GREEN** — "All matched files use Prettier code style!"                                                                                                                                |
| `npx tsc --noEmit`       | 0         | **GREEN** — clean exit, no type errors                                                                                                                                                  |
| `npm run build`          | 0         | **GREEN** — Vite frontend bundle + tsup server bundle both exit 0; vendor-deckgl 815 KB / vendor-maplibre 1048 KB / index 720 KB (warnings about chunk size are pre-existing, not new). |

All five exit 0. W4 closes the D-28 wave-gate ritual cleanly.

## Decisions Made

- **Group D StatusPanel landed as a separate late-wave commit** rather than batched with Group B. Reason: investigation of the displacement trace (Phase 19 commit `bd7b280`) was deferred until after the simpler Group B deletions completed, so the wave's checkpoint cycle could surface user-approval before the surgical delete + 3-section CLAUDE.md edit + doc-comment scrub landed. This is the canonical shape for any deletion that requires CLAUDE.md surgery touching multiple sections — investigate first, surface findings, get explicit approval, then atomic delete.
- **Group C wrapper deletions reclassified as deferred** (FilterPanelSlot + LayerTogglesSlot) rather than deleted. Reason: ~24 indirect tests target the wrappers; deleting them without first re-pointing tests at the live `*Content` siblings would have lost test signal. The wrappers themselves are 1-line re-exports — keeping them is cheaper than the test-retargeting refactor that would unlock the deletion. Captured for a future phase.
- **Path B fast-path UAT skip** (Plan Task 5 checkpoint:human-verify). Reason: every commit in W4 removes confirmed-dead code (no new behavior added). Local audit suffices: vitest 1932/1932 + tsc clean + EXCLUDED invariant + lint + prettier + npm run build all green. Risk-acknowledged: live-UI manual walkthrough cannot be re-executed without operator action; Phase 28.2/28.3 manual UAT may surface any deferred surface regression. User explicitly approved this path in the resume directive.
- **Atomic-per-concern bundling at file granularity** (Group A 12 W3 rows → 9 commits). Reason: when the same file has multiple flagged exports of the same kind (lose-export narrowings), they bundle into a single commit because they share the same review/verify surface (one tsc, one vitest, one diff to review). The W3 triage row count is the planning unit; the file is the commit unit.
- **Group E (`clearDashboardKey` row #50) bundled into commit `52c089b` with Group A row #49 (`getDashboardKey`)** because both touch `src/lib/dashboardAuth.ts` (same source file = same logical concern: auth helper visibility cleanup). Two-symbol surgery in one file → one commit per atomic-canon. Group E was therefore not a separate commit; it absorbed into Group A.
- **W2-added files added to EXCLUDED bucket Task 4 verification list via plan amendment `857cceb`** (`HealthStatusProvider.tsx`, `HealthBanner.tsx`, `useHealthStatus.ts`, `healthSchema.ts`, `healthSources.ts`, `healthClient.ts`) PLUS the W5-added file (`useWeatherPolling.ts`). Reason: the original W3 EXCLUDED list predated W2's freshly-introduced files; the amendment ensured the canonical `test -f` invariant covers the full Hardcode-Target Manifest as it stood at W4 execution time.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 3 - Blocking] Group A bundled per-file rather than per-row**

- **Found during:** Task 1 execution (lose-export sweep)
- **Issue:** The W4 plan body Task 1 instructions implied "one commit per row" for Group A, which would have produced 12 commits for files where multiple W3 rows shared a single source file (e.g., `dashboardAuth.ts` has #49 + #50; `factions.ts` has #5 + #6; `marketStore.ts` has #12 + #13). Each pair would have generated near-duplicate commits with the same review surface but different rows referenced.
- **Fix:** Bundled per-file per CLAUDE.md atomic-per-concern canon — multiple narrowings within the same file constitute one logical concern. 12 rows → 9 commits.
- **Files modified:** N/A (procedure-level decision)
- **Verification:** `npx tsc --noEmit` + `npx vitest run` after each file-bundled commit (no row-level granularity loss in the verification surface).
- **Committed in:** Multiple commits per the Group A table above (the "12 W3 rows / 9 commits" mismatch is the audit trail).

**2. [Rule 3 - Blocking] Group E (`clearDashboardKey` row #50) absorbed into commit `52c089b` with Group A row #49**

- **Found during:** Task 1 execution (when row #49's narrowing landed)
- **Issue:** Row #50 is in the same source file (`src/lib/dashboardAuth.ts`) as row #49. Splitting them into separate commits would have left an inconsistent state between commits (file edits can't be split mid-commit at the symbol level).
- **Fix:** Bundled both edits into commit `52c089b` (Group E classification preserved in the commit message body for triage cross-reference).
- **Files modified:** `src/lib/dashboardAuth.ts` (one file, two symbol changes: `getDashboardKey` narrowed; `clearDashboardKey` deleted).
- **Verification:** `npx tsc --noEmit` + `npx vitest run` post-commit (clean).
- **Committed in:** `52c089b` (Group A + E bundle).

**3. [Rule 3 - Blocking] Plan Task 5 manual UAT skipped per user-approved Path B fast-path**

- **Found during:** Task 5 checkpoint (after the 15 deletion commits + plan amendment landed and HEAD reached `857cceb`)
- **Issue:** The plan body's Task 5 specified a `checkpoint:human-verify` requiring a live-UI walkthrough of the entire CLAUDE.md feature inventory. User explicitly approved the Path B fast-path: accept local audit (vitest + tsc + EXCLUDED invariant + targeted-deletion-only nature of every commit) as the verification surface. This is documented as the user's deliberate operator decision, not a Claude-driven rule application.
- **Fix:** Treated Task 5 as auto-approved with the user's resume-signal serving as the approval; proceeded directly to Task 6 (wave-gate + SUMMARY) after landing the StatusPanel commit. Risk acknowledgment captured under "Known Limitations" below.
- **Files modified:** N/A (process-level decision)
- **Verification:** Full wave-gate green per the table above.
- **Committed in:** N/A (operator decision; documented here for forensic traceability).

**4. [Rule 2 - Missing critical] `server/lib/healthSources.ts:82` doc-comment scrub bundled into Group D commit `39934bd`**

- **Found during:** Group D pre-deletion cross-check (`grep -rn StatusPanel src/ server/`)
- **Issue:** A stale doc-comment at `server/lib/healthSources.ts:82` (`*   - non-critical → StatusPanel HUD dot color`) referenced the to-be-deleted component. Leaving it as a stale reference would have created a broken doc-link to a non-existent symbol, violating the atomic-with-deletion canon (Pattern 2).
- **Fix:** Updated the doc-comment to `*   - non-critical → StatusDropdown HUD dot color` in the same atomic Group D commit (`39934bd`).
- **Files modified:** `server/lib/healthSources.ts` (1 line, doc-comment only — no behavior change).
- **Verification:** `npx tsc --noEmit` clean (no type-level dependency on the doc-comment text); `npx vitest run` clean (server suite unaffected).
- **Committed in:** `39934bd` (Group D StatusPanel + CLAUDE.md surgery + healthSources.ts:82 scrub).

---

**Total deviations:** 4 auto-fixed (3 Rule-3 blocking, 1 Rule-2 missing-critical).

**Impact on plan:** All four deviations are procedure-level adjustments, not scope creep. (1) and (2) refine the commit-shape canon for cleanup waves with same-file flag clusters. (3) is the user-approved fast-path skip. (4) preserves the Pattern 2 atomic-with-deletion invariant. No additional source modifications outside the W3-triage-approved scope.

## Known Limitations

### Manual UAT walkthrough deferred (Path B)

- **What was skipped:** Live-UI walkthrough of the CLAUDE.md feature inventory (every documented feature toggled, every detail panel exercised, every search query tested).
- **Why:** User-approved Path B fast-path. Every commit in W4 removes confirmed-dead code (no new behavior added); local audit (vitest + tsc + EXCLUDED invariant + lint + prettier + npm run build) is the operator-accepted verification surface for this wave class.
- **What stands in for it:** Full wave-gate green per the table above (1932/1932 tests, lint clean, prettier clean, tsc clean, build green) PLUS the source-modification scope check (every modified file traces to a specific W3 triage row) PLUS the EXCLUDED invariant verification.
- **Risk acknowledgment:** Live-UI manual walkthrough cannot be re-executed without operator action. Any deferred surface regression — e.g., a deleted symbol that turns out to be referenced via a dynamic import path that knip + ts-prune both missed — would surface in Phase 28.2 or 28.3's manual UAT, not at W4 close. User explicitly approved this acceptance path on 2026-05-01.

### Group C wrapper deletions deferred

- **What was skipped:** Deletion of `src/components/layout/FilterPanelSlot.tsx` (W3 row #T16) and `src/components/layout/LayerTogglesSlot.tsx` (W3 row #T17).
- **Why:** Each wrapper re-exports from a sibling `*Content` module. ~12 indirect tests per wrapper target the wrapper file, not the `*Content` module. Deleting the wrappers without first re-pointing the tests at `*Content` would have lost ~24 test signals — net regression in test surface.
- **Resolution path:** A future test-retargeting phase (sibling to W4) re-points the indirect tests at the `*Content` modules directly, then deletes the wrappers in a follow-up cleanup pass. Captured under `<deferred>` below.

## Items Added to Umbrella `<deferred>`

| Item                                                                       | Owner Wave / Phase         | Rationale                                                                                                                                                                                        |
| -------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `FilterPanelSlot.tsx` + `LayerTogglesSlot.tsx` wrapper deletions (Group C) | Future test-retarget phase | Wrappers re-export from `*Content` siblings; ~24 indirect tests target the wrappers. Resolution requires test-retargeting refactor (point tests at `*Content` directly) before deletion is safe. |
| Manual UAT walkthrough of CLAUDE.md feature inventory                      | Phase 28.2 or 28.3         | Path B fast-path skip per user approval at W4. Will be exercised in the next wave that introduces behavior changes (W5 hardcode-generalization or W6 CSS-theme-migration manual UAT).            |

## Wave-Gate Verification (final)

```
$ npx vitest run
Test Files  141 passed (141)
     Tests  1932 passed (1932)
   Start at  21:48:55
   Duration  45.05s

$ npm run lint
✖ 22 problems (0 errors, 22 warnings)   ← byte-identical to W2 close baseline; zero
                                         drift from W4's deletion-only commit set

$ npx prettier --check .
Checking formatting...
All matched files use Prettier code style!

$ npx tsc --noEmit
(clean exit)

$ npm run build
✓ built in 4.07s    (Vite frontend)
ESM ⚡️ Build success in 100ms   (tsup server entry)
```

All five exit 0. W4 closes the D-28 wave-gate ritual cleanly.

## Issues Encountered

None beyond the 4 documented deviations. The wave executed with the conservative-bias triage outcome the W3 close predicted (zero high-dead bulk-approval slate; every probable-dead deletion got individual review; every ambiguous item kept by default).

## Threat Flags

None. W4 introduces no new HTTP surface, no new auth path, no new file-access pattern, and no schema change at any trust boundary. The plan's `<threat_model>` enumerates three threats — T-28.1-W4-01 (accidental regression → mitigated by per-deletion vitest + EXCLUDED invariant + lint + tsc + build), T-28.1-W4-02 (information disclosure → accepted, source code being deleted is already-public), T-28.1-W4-03 (DoS via broken build → mitigated, npm run build added to wave-gate per plan Task 6). All three mitigations executed:

- T-28.1-W4-01: Per-deletion vitest run (clean throughout); EXCLUDED-bucket verification (22/22 PASS); manual UAT skipped per user-approved Path B (acknowledged risk above); npm run build at wave close (green). Five-layer defense — four executed, one (manual UAT) deferred per user approval.
- T-28.1-W4-02: Source code deleted is already-public committed code; nothing new exposed. Accepted per plan disposition.
- T-28.1-W4-03: `npm run build` added to wave-gate; exits 0 at W4 close. Mitigation honored.

No new uncovered surface added.

## Self-Check: PASSED

- FOUND: `.planning/phases/28.1-cleanup-sweep/28.1-W4-SUMMARY.md` (this file)
- FOUND: `.planning/phases/28.1-cleanup-sweep/W4-knip-output.txt`
- FOUND: commit `13fcc74` (Group A row #5+#6 narrowing)
- FOUND: commit `d5e6566` (Group A row #12+#13 narrowing)
- FOUND: commit `f3e02b5` (Group A row #14 narrowing)
- FOUND: commit `c828c79` (Group A row #16 narrowing)
- FOUND: commit `037a3f8` (Group A row #17 narrowing)
- FOUND: commit `d4e35a2` (Group A row #26 narrowing)
- FOUND: commit `6df259c` (Group A row #38 narrowing)
- FOUND: commit `880ccf1` (Group A row #48 narrowing)
- FOUND: commit `52c089b` (Group A row #49 + Group E row #50 bundle)
- FOUND: commit `05f4a0b` (Group A row #51 narrowing)
- FOUND: commit `bc9bf76` (Group B `server/lib/dispersion.ts` + test deletion)
- FOUND: commit `e1e1812` (Group B `CountersSlot.tsx` + test + CLAUDE.md surgery 3 sections)
- FOUND: commit `d061a5a` (Group B `EthnicOverlay.tsx` wrapper + test deletion)
- FOUND: commit `6483e4d` (Group B `WaterOverlay.tsx` wrapper + test deletion)
- FOUND: commit `857cceb` (Plan amendment — Task 4 EXCLUDED invariant +HealthStatusProvider +useWeatherPolling)
- FOUND: commit `39934bd` (Group D `StatusPanel.tsx` + test + CLAUDE.md 3-section surgery + healthSources.ts:82 scrub)
- FOUND: 22/22 EXCLUDED-bucket files (verified via `test -f` loop, exit 0)

## Next Wave Readiness

**W4 closes green; W5 (hardcode generalization) may begin.**

W5 was already amended at `bd5bbce` (the orchestrator's W3 hand-off) to add `VITE_POLL_WEATHER_MS` to the D-12 env-var introduction set — W5 D-12 is **12 env vars across 11 files** (was 11 across 10). The amendment landed before W4 began; W5 may begin from W4's HEAD without further amendment.

W4's deletion record gives W5 a slightly smaller cleanup target (the 4 file/wrapper deletions remove modules that would otherwise have been re-checked in W5/W6 hardcode passes). The 22 EXCLUDED-bucket files (including the 6 W2-added health files + W5-added `useWeatherPolling.ts`) are confirmed present, so W5's hardcode-generalization sweep can target them directly.

W6 (CSS theme migration) and W7 (normalization) are unaffected by W4's deletions — they target z-index / @theme / Redis-key normalization in surfaces W4 did not touch.

---

_Phase: 28.1-cleanup-sweep_
_Wave: 4 — Atomic Deletions (D-14 step 4 / D-29 conservative-bias)_
_Completed: 2026-05-01_

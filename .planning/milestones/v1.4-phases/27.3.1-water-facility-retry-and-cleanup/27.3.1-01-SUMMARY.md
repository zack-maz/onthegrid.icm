---
phase: 27.3.1
plan: 01
requirement: R-01
status: complete
completed_at: 2026-04-19T02:48:00Z
gate_decision: pass
---

# Plan 01 — R-01 Post-Recovery Refresh Gate — COMPLETE

## Outcome

**Gate: PASS.** All three water facility types returned non-zero counts from the first
post-recovery `/api/water?refresh=true` hit. Wave 2 (calibration) unblocks.

## What was verified

- Overpass primary endpoint (`https://overpass-api.de/api/interpreter`) responsive at
  2026-04-19T02:42Z (outage started 2026-04-18 15:15 PT).
- Upstash Redis keys `water:facilities` and `water:precip` both cleared (DEL returned
  `{"result":1}` for both — caches were warm, so the refresh forced a real Overpass round-trip).
- `.dev-cache/water-facilities.json` deleted.
- `/api/water?refresh=true` returned HTTP 200, 1,878,667 bytes, 83.8s wall time across three
  Overpass queries (dams + reservoirs + desalination).

## Numbers captured

| Type         | Raw    | Admitted | Admit rate | vs target band |
| ------------ | ------ | -------- | ---------- | -------------- |
| Dams         | 8,699  | 4,588    | 52.7%      | ABOVE (~9.2×)  |
| Reservoirs   | 15,355 | 892      | 5.8%       | ABOVE (~1.8×)  |
| Desalination | 63     | 63       | 100.0%     | ABOVE (~2.5×)  |

Total on-map: 5,374 facilities.

Rejection buckets: excluded_location 2,423 · not_notable 12,942 · no_name 2,965 ·
duplicate 168 · low_score 0 · no_city 244.

Enrichment coverage (all facilities): withCapacity 2.8%, withCity 40.2%, withRiver 2.7%.

## Key signal for Plan 04

`low_score: 0` — `MIN_NOTABILITY_SCORE=15` is non-binding. Tuning the floor alone will not
move the counts. Plan 04 must tighten the compound gate (D-06) or remove the pure-name +
priority-country admission path; raising the score floor is a secondary lever at best.

## Blocking notes for Plan 04

Plan 04 (R-02 calibration) is unblocked but will need to tune hard — dams are 9× the target
ceiling. The detailed lever analysis (which gate to adjust for each facility type) lives in
`27.3.1-R01-REFRESH-LOG.md` § Implications for Plan 04.

## Evidence

- `27.3.1-R01-REFRESH-LOG.md` — full per-bucket numbers, score histogram, sample facilities,
  and Plan-04 implications. Committed in the same commit.

## Commit

- `651f631` docs(27.3.1): record R-01 refresh — dams=4588 reservoirs=892 desal=63 (gate=pass)

# Phase 27.3.1: Water Facility Retry and Cleanup — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `27.3.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-18
**Phase:** 27.3.1-water-facility-retry-and-cleanup
**Areas discussed:** Context lifecycle, Notability, Snapshot, Observability, Dam gate, Sites snapshot

---

## Context Lifecycle

| Option                           | Description                                                                                                                          | Selected |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| Update it in place (Recommended) | Revise R-02, R-03, and add observability + snapshot specifics. Keep R-01, R-05, R-06, R-07 as-is. Carry forward decisions from 27.3. | ✓        |
| View current version first       | Show existing CONTEXT.md before deciding.                                                                                            |          |
| Start fresh                      | Discard existing decisions and discuss from scratch (loses Package A context).                                                       |          |

**User's choice:** Update it in place.
**Notes:** Existing CONTEXT.md from 2026-04-18 already held R-01 through R-07 with 100–500 target bands. User's new direction narrows targets and adds observability + snapshot emphasis — an update is strictly additive/tightening, not a reset.

---

## Notability — "Officially Recognized with a Name"

| Option                                                                         | Description                                                                                               | Selected |
| ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | -------- |
| Wikidata OR wikipedia OR (name AND operator)                                   | Tighter than current. Likely ~400 not 892.                                                                |          |
| Name required AND (wikidata OR wikipedia OR priority country OR capacity data) | Middle ground. Any name is baseline; notability comes from wiki ref, conflict-zone location, or capacity. | ✓        |
| Name:en required (Latin script name)                                           | Strictest. Skews toward English-OSM-community-mapped facilities — may under-pull Iran/Iraq/Syria.         |          |
| Claude's discretion — tune to hit ~400 each                                    | Pick a gate, run refresh, read filterStats, iterate.                                                      |          |

**User's choice:** Name required AND (wikidata OR wikipedia OR priority country OR capacity data).
**Notes:** Captured as D-05 and D-06. Compound gate is the primary admission logic; `MIN_NOTABILITY_SCORE` demoted to a secondary coarse floor.

---

## Snapshot Format + Refresh Model

| Option                                                             | Description                                                                                                                              | Selected |
| ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Pre-enriched WaterFacility[] + manual refresh script (Recommended) | scripts/refresh-water-facilities.ts runs the full pipeline and writes src/data/water-facilities.json. Refreshes are intentional commits. | ✓        |
| Raw Overpass elements + runtime enrichment                         | Snapshot holds raw tags; enrichment runs each cold start. Slower first-request latency.                                                  |          |
| Pre-enriched + Vercel cron weekly refresh                          | Same pre-enriched JSON, but cron hits /api/water?refresh=true and writes back to Redis. More moving parts.                               |          |

**User's choice:** Pre-enriched + manual refresh script.
**Notes:** Captured as D-11 through D-15. Vercel cron deferred (D-14); the snapshot regeneration is intentional and reviewed via PR diff.

---

## Observability Additions (multi-select)

| Option                                                  | Description                                                                                 | Selected |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------- |
| Per-country admission counts                            | Break filteredCounts by nearest-centroid country. Surfaces imbalances instantly.            | ✓        |
| Overpass health: status, timing, mirror used, attempts  | Which mirror succeeded, HTTP status per attempt, duration. Makes outage debugging a glance. | ✓        |
| Snapshot metadata: generated-at, source, facility count | Shows whether response came from snapshot, Redis, or Overpass, with timestamp.              | ✓        |
| Admission reason breakdown per facility type            | Per-type rejection tallies (reservoirs: no_city=X low_score=Y vs dams: no_name=Z).          | ✓        |

**User's choice:** All four.
**Notes:** Captured as D-28 through D-31. Sites panel (D-19 from R-05) reuses these same extension points — D-32 sequences the water additions before the sites panel.

---

## Dam Gate Tightening

| Option                                                    | Description                                                                                                           | Selected |
| --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | -------- |
| Require hasName() for ALL dams, everywhere (Recommended)  | Unnamed dams drop regardless of country. Priority bonus still contributes to score but doesn't substitute for a name. | ✓        |
| Keep current rule — priority-country unnamed dams allowed | Unnamed conflict-zone dams still admit. Contradicts user directive.                                                   |          |
| Require hasName() for dams AND desalination               | Symmetric tightening; reservoirs already require name via REV-2.                                                      |          |

**User's choice:** Require hasName() for ALL dams, everywhere.
**Notes:** Captured as D-07. Combined with D-05 (every admitted facility must have hasName), desalination already benefits from the same rule — so the effect is equivalent to option 3 without needing a separate desal-specific branch.

---

## Sites Snapshot Parity (R-05 Scope)

| Option                                                                     | Description                                                       | Selected |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| Full parity: src/data/sites.json + refresh script + DevApiStatus telemetry | Sites get the same cold-start-safe pipeline + telemetry as water. | ✓        |
| Committed snapshot only, skip the telemetry parity                         | Add snapshot + refresh script, defer DevApiStatus panel.          |          |
| Defer sites entirely to Phase 27.3.2 or later                              | Focus 27.3.1 on water only.                                       |          |
| Claude's discretion — audit first, decide during planning                  | Planner reads sites adapter, picks scope.                         |          |

**User's choice:** Full parity.
**Notes:** Captured as D-16 through D-19. Water R-08 additions land first (extension points), then sites panel consumes them (D-32 sequencing).

---

## Claude's Discretion

- Exact DevApiStatus layout/typography for the expanded observability panel
- Implementation shape of `computeAdmissionDecision` (D-20) — explicit state machine vs guarded early-returns
- Whether the snapshot generation script shells out to the running server or runs the pipeline standalone
- Whether the sites snapshot bakes in basin stress or leaves it runtime

## Deferred Ideas

- Vercel cron auto-refresh for snapshots
- Circuit breaker on Overpass adapter
- ML-based facility notability ranking
- Snapshot staleness CI check
- Per-type DevApiStatus panels for flights/ships/events

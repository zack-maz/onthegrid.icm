---
status: complete
phase: 27.3.1-water-facility-retry-and-cleanup
source: [27.3.1-VERIFICATION.md]
started: 2026-04-19T23:55:00Z
updated: 2026-04-20T00:20:00Z
completed: 2026-04-20T00:10:00Z
resolved: 2026-04-20T00:20:00Z
results:
  total: 3
  passed: 3
  issues: 0
  issues_resolved: 1
  scope_gaps: 1
  pending: 0
---

## Current Test

[testing complete]

## Tests

### 1. DevApiStatus modal open/close, tab switching, Escape priority, backdrop behavior

expected: |
Open the running dev app. Click the dev-only "API ~/!" badge in the Topbar
right cluster (between ResetButton and NotificationBell). The DevApiStatus
modal opens centered at `z-index: var(--z-modal)`; backdrop darkens the rest
of the viewport; Overview tab active by default.

- Click **Water** tab: `WaterFiltersSection` shows populated `byCountry` rows
  (Iran, Iraq, Saudi Arabia, Egypt, UAE, Afghanistan — **NO Turkey row**).
- Click **Sites** tab: `SitesFiltersSection` shows `byType` populated
  (airbase=284, port=232, oil=99, naval=60, nuclear=45).
- Press **Escape**: the modal closes FIRST — before any nav-stack pop or
  detail-panel close.
- Click the **backdrop**: modal closes.
- Click inside the modal container: modal stays open.
- Modal body scrolls with `max-h-[85vh]` + `overflow-y-auto` when Water/Sites
  tabs render populated tables (no viewport overflow).
  result: pass
  reported: "yes but water filter options should only appear if water filter is on"
  severity: minor
  resolved_by: "Commit 9705893 — tabs now gated on useLayerStore water layer + useFilterStore showSites; 4 new regression tests added"

### 2. Redis envelope round-trip — live Upstash filterStats persistence

expected: |
With the dev server running against a real Upstash Redis instance:

1. Delete Redis keys: `water:facilities:v2` and `sites:v3`.
2. Curl `/api/water` and `/api/sites` once each:
   - Response: `filterStats.source === "snapshot"`
   - Response: `filterStats.rejections.no_name > 0`
   - Response: `filterStats.rejections.excluded_turkey > 0`
   - Response: `filterStats.byCountry.Iran` has non-zero type bucket
3. Curl `/api/water` and `/api/sites` a second time each:
   - Response: `filterStats.source === "redis"` (override applied)
   - Response: `filterStats.rejections.*` **identical** to first-hit values
   - Response: `byCountry` / `byType` tallies **identical** to first-hit values

This proves the `{facilities, filterStats}` and `{sites, filterStats}`
envelopes round-trip through Upstash without losing observability data.
result: pass
evidence: |
Deletion via Upstash pipeline: DEL water:facilities:v2 → 1, DEL sites:v3 → 1.
First hit (cache miss):
/api/water → source=snapshot, excluded_turkey=1662, no_name=17769, byCountry.Iran={dam:51, reservoir:6}
/api/sites → source=snapshot, excluded_turkey=156, byCountry.Iran={airbase:17, port:3, naval:3, oil:4, nuclear:3}
Second hit (cache hit):
Both → source=redis, rejections/byCountry/byType byte-identical to first hit.
Only generatedAt timestamp + source label differ — exactly the expected override fields.
Note: sites filterStats schema has no no_name bucket (only excluded_turkey, no_coords, no_type, duplicate) — no_name is water-only by design.
Cache override pathway verified end-to-end on both routes.

### 3. No raw generic labels in water facility detail panels

expected: |
With the map rendering water facilities, hover or click any facility whose
OSM name is non-Latin script only (Persian / Arabic / Georgian). Detail
panel displays a derived label like:

- "Dam on Tigris" (river fallback)
- "Dam near Mosul" (city fallback)
- "Dam at 36.12°N, 43.00°E" (coord fallback)

Confirm **no** facility shows a raw "Dam" / "Reservoir" / "Desalination
Plant" label. The 137 admitted facilities whose `label` field in
`src/data/water-facilities.json` equals `"Dam"` / `"Reservoir"` /
`"Desalination Plant"` must flow through client-side
`getWaterFacilityDisplayName` in `src/lib/waterLabel.ts` and render
sentinel-rewritten text.
result: pass
evidence: |
User confirmed no raw "Dam" / "Reservoir" / "Desalination Plant" labels
render on the map — the client-side sentinel rewrite in waterLabel.ts is
working as designed.
follow_up: |
User requested a future scope tightening: drop facilities that only
resolve to city or coord fallback, keeping only river-resolved or
original-named facilities. Logged as separate gap below — NOT a defect
in Phase 27.3.1 (existing truth met). Will roll into the next water
phase (27.3.2 or similar).

## Summary

total: 3
passed: 3
issues: 0
issues_resolved: 1
pending: 0
skipped: 0
blocked: 0
scope_gaps: 1

## Gaps

- truth: "DevApiStatus Water tab and Sites tab are only visible when their respective visualization layer toggles (showWater / showSites) are ON — observability should be scoped to active layers, not always-visible"
  status: resolved
  reason: "User reported: 'yes but water filter options should only appear if water filter is on' — the modal currently renders all 3 tabs (Overview / Water / Sites) unconditionally. Water tab should be hidden (or disabled with a muted state) when showWater toggle is OFF; same for Sites tab when showSites toggle is OFF. Overview tab stays unconditional."
  severity: minor
  test: 1
  resolved_by: "Commit 9705893 fix(27.3.1): gate DevApiStatus Water/Sites tabs on layer toggles — Water tab reads useLayerStore.activeLayers.has('water'); Sites tab reads useFilterStore.showSites; useEffect snaps activeTab back to overview when an active tab's toggle flips off. 4 new regression tests cover hide-when-off and redirect-to-overview. 19/19 DevApiStatus tests pass, 816/849 client, 811/811 server, no new TS errors."
  artifacts:
  - src/components/ui/DevApiStatus.tsx
  - src/**tests**/devApiStatus.test.tsx
    missing: []

- truth: "Water facility admission drops facilities whose label only resolves via city-fallback or coord-fallback — snapshot should contain only real-named facilities + river-resolved facilities, never generic 'Dam near X' or coord-at-lat/lng names"
  status: scope_change
  classification: feature_request
  reason: "User reported: 'yes... but I want to remove city and coord fallbacks and just drop those facilities' — Test 3's existing truth (no raw labels rendered) passed, but user wants stricter admission going forward. Forward-looking tightening of the hasName + label-resolution pipeline. Filter out any OSM element whose (name / name:en / operator / river-match) chain all return empty; drop to no_name bucket instead of admitting with a derived city/coord label."
  severity: minor
  test: 3
  scope_note: "NOT a defect in Phase 27.3.1 — current truth was explicitly met. Queue for a subsequent water phase (27.3.2 or similar). Will further reduce snapshot admission count below the current 436 by dropping the subset of bare-label facilities that resolve only to city/coord rather than to a river. Client-side GENERIC_TYPE_RE sentinel fallback stays in place as a safety net but becomes effectively unreachable."
  artifacts: []
  missing: []

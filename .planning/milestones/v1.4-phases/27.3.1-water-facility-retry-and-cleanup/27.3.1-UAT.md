---
status: resolved
phase: 27.3.1-water-facility-retry-and-cleanup
source:
  [
    27.3.1-01-SUMMARY.md,
    27.3.1-02-SUMMARY.md,
    27.3.1-03-SUMMARY.md,
    27.3.1-04-SUMMARY.md,
    27.3.1-05-SUMMARY.md,
    27.3.1-06-SUMMARY.md,
    27.3.1-07-SUMMARY.md,
    27.3.1-08-SUMMARY.md,
  ]
started: 2026-04-19T18:06:25Z
updated: 2026-04-19T23:00:00Z
gap_closure_plans:
  G1: 27.3.1-10-PLAN.md
  G2: 27.3.1-10-PLAN.md
  G3: 27.3.1-11-PLAN.md
  G4: 27.3.1-11-PLAN.md
  G5: 27.3.1-09-PLAN.md
  G6: 27.3.1-12-PLAN.md
  G7: 27.3.1-12-PLAN.md
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test

expected: |
Kill the running dev server (Ctrl+C), clear `.dev-cache/water-facilities.json`
(water snapshot) + Redis `water:facilities` + Redis `sites:facilities`, then
restart via `npm run dev`. Server boots without errors. First `/api/water`
request returns 602 facilities with `filterStats.source: "snapshot"`. First
`/api/sites` request returns 720 sites with `filterStats.source: "snapshot"`.
Map renders water + sites without hitting Overpass on the request path.
result: pass

### 2. No generic-type labels (R-03 / D-05)

expected: |
Pan the map across Iran, Turkey, Saudi Arabia, and the Gulf. Every water
facility icon you click shows a real OSM name in the detail panel — no "Dam",
"Reservoir", "Dam near Afghanistan", or "Reservoir near unknown" labels
appear anywhere. Unnamed facilities are simply not on the map.
result: issue
reported: "We're pulling a lot of unnamed dams like 'Dam near... XXX' Not needed. In general, we're also pulling too many dams and some reservoirs in Turkey - its not needed, priority should be middle east in conflict"
severity: major
subissues:

- "A: Generic-type fallback still reachable — 'Dam near <region>' labels appear on the map despite D-05 hasName requirement. Either hasName() is admitting non-name tags, or the label fallback chain is constructing generic labels post-admission."
- "B: Turkey over-representation — priority-country status is admitting too many Turkey dams/reservoirs. Scope should skew toward conflict-relevant Middle East countries (Iran, Iraq, Syria, Yemen, Lebanon, Israel, Gulf states). Turkey is priority-country today but low conflict relevance."

### 3. Calibrated facility counts (R-02)

expected: |
The Counters HUD + DevApiStatus Water Filters panel both agree on
approximately: dams ~515, reservoirs ~73, desalination ~15 (total ~602
water facilities). Counts reflect the frozen snapshot, not a live refresh.
Reservoirs at 73 is intentionally below the 300 floor per the quality
override you approved.
result: pass
note: "Map counts are correct. DevApiStatus Water Filters panel shows all zeros when source=redis (raw 0 → kept 0). Separately logged under Test 4 as Plan 03 Redis-write regression — snapshot stats aren't persisted to Redis, so post-first-hit responses lose the numbers."

### 4. DevApiStatus Water Filters — R-08 observability blocks

expected: |
Open DevApiStatus (dev-only panel). The Water Filters section shows, in
order: (a) Source + Generated provenance header ("Source: snapshot · Generated:
Xh ago"), (b) raw/kept summary, (c) per-type counts, (d) By Country table
(top 12 rows), (e) per-type Rejections breakdown, (f) Overpass Health rows
per facility-type query (mirror / status / durationMs / attempts / OK|FAIL).
All four R-08 additions visible.
result: issue
reported: "Whole panel state: Source: redis · Generated: 10h ago. 0 raw → 0 kept (0%). Total rejections: excl=0 nn=0 nname=0 dup=0 low=0 nocity=0. Enriched: cap=0 city=0 river=0. Scores: (empty)."
severity: major
root_cause_hypothesis: "filterStats is NOT persisted to Redis alongside the data array. On cold start the snapshot tier serves real stats (source=snapshot). That first response warms Redis with facilities only — the filterStats envelope is either stripped before Redis write or never stored. On every subsequent request, the route reads Redis, sees data but no stats, and falls through to `buildEmptyFilterStats` (Plan 03 Task 3 placeholder) which emits all-zero counts. The R-08 observability panel then renders those zeros instead of the snapshot's real per-country / per-type / rejections numbers. byCountry and Overpass Health blocks likely also render empty."

### 5. DevApiStatus Sites Filters — new panel (Plan 08)

expected: |
Scroll below the Water Filters section inside DevApiStatus. A new Sites
Filters section renders with 6 blocks: provenance header, raw/kept summary
(~876 raw → 720 kept), per-type counts (airbase ~284, port ~232, oil ~99,
naval ~60, nuclear ~45 — sorted desc), By Country top-12 table, Rejections
row (4 buckets: excluded_turkey / no_coords / no_type / duplicate — no
byTypeRejections), Overpass Health row. Sites panel matches water visual
rhythm. Water panel is unchanged.
result: issue
reported: "Sites Filters — Source: redis · Generated: 10h ago. 0 raw → 0 kept (0%). Rejections: turkey=0 nocoords=0 notype=0 dup=0."
severity: major
root_cause_hypothesis: "Identical shape to Test 4 issue — the sites route's Redis write path only caches the sites array, losing the filterStats envelope. Once Redis warms, the `buildEmptyFilterStats` equivalent for sites emits all zeros. The Sites panel component itself (Plan 08) renders correctly per structure — the data upstream is empty. Single root cause shared with water; one fix closes both."
note: "Panel structure (6 blocks, layout) appears correct from the pasted line — provenance header + raw/kept line are rendering. byCountry / byType / Overpass Health blocks likely show empty-state placeholders too."

### 6. Refresh script regenerates water snapshot

expected: |
From the repo root, run `npm run refresh:water`. The script connects to
Overpass, runs the full fetch+classify+compound-gate+enrichment pipeline,
writes `src/data/water-facilities.json` atomically (tempfile+rename), and
prints a per-type + rejection-bucket summary at the end. Exit code 0.
`git diff src/data/water-facilities.json` shows only coord/label/timestamp
drift — no structural change.
result: issue
reported: "Script hangs mid-run on reverse-geocoding phase. Error floods: '[Upstash Redis] Redis client was initialized without url or token. Failed to execute command.' User killed the process. Fetch phase completed successfully (602 admitted: dams=515 reservoirs=73 desal=15, matches Plan 04). Geocoding phase stalls because of Redis failure flood (139 unnamed facilities × multiple cache ops × 2s timeout each ≈ 9 min of hangs)."
severity: major
root_cause: "`package.json` `refresh:water` script is `tsx scripts/refresh-water-facilities.ts` — no --env-file-if-exists=.env flag. Server code (`server/cache/redis.ts`) imports Upstash client at module load; with no URL in process.env, every cacheGet/cacheSet fires the REDIS_OP_TIMEOUT_MS=2000 withTimeout Promise.race and fails. Fix: change to `node --env-file-if-exists=.env --import tsx/esm scripts/refresh-water-facilities.ts` (mirrors dev:server pattern) OR add a module-top `import 'dotenv/config'` in the script. Same bug in refresh:sites — single fix closes both."
evidence_data:
byCountry_from_log:
Turkey: "dam=151 reservoir=14 (165 = 27% of total)"
Iran: "dam=51 reservoir=6"
Iraq: "dam=46 reservoir=11"
Syria: "dam=23 reservoir=8"
Israel: "dam=3 reservoir=8"
Lebanon: "dam=7 reservoir=13"
Yemen: "dam=1 reservoir=0"
Saudi_Arabia: "dam=67 desal=1"
UAE: "dam=74 reservoir=3 desal=1"
Afghanistan: "dam=25 reservoir=6"
Cyprus: "dam=8 desal=4"
Turkmenistan: "desal=2 (Caspian, outside MENA conflict scope)"
conflict_core_total: "177 across Iran+Iraq+Syria+Lebanon+Israel+Yemen (less than Turkey alone)"
unnamed_admits: "139 of 602 (23%) — these are the 'Dam near X' labels driving Test 2 Issue A. Plan 02's hasName(tags) shortcut via wikidata/wikipedia means these facilities pass D-05 even with no actual name tag."

### 7. Refresh script regenerates sites snapshot

expected: |
From the repo root, run `npm run refresh:sites`. Script connects to Overpass,
runs `fetchSites()` (core + extended batches), writes `src/data/sites.json`
atomically, prints per-type + per-country summary. Exit code 0. Generated
snapshot still has ~720 sites across airbase/port/oil/naval/nuclear.
result: pass
note: "Script completed in 23s. 720 sites written (airbase=284, port=232, oil=99, naval=60, nuclear=45). Summary emitted with byType + byCountry top-10 + Overpass health. Env bug from Test 6 exists in this script too but is not exercised because sites has no reverse-geocoding phase. IMPORTANT OBSERVATION: sites adapter has `excluded_turkey: 156` rejection bucket — it ACTIVELY EXCLUDES Turkey. Water adapter does not. Plan 08's fix for Test 2 Issue B should copy this pattern (add excluded_turkey bucket to WaterFilterStats.rejections and apply isExcludedLocation to Turkey in overpass-water.ts)."

### 8. Snapshot served on cold Redis (R-07 tier)

expected: |
Clear Redis key `water:facilities` via the Upstash REST API, then reload the
app in a fresh browser tab. Water facilities still render immediately — no
blank map, no error banner. DevApiStatus Water Filters provenance shows
`Source: snapshot`. After the first page-load, a second reload shows
`Source: redis` (snapshot populated Redis on the first hit). Repeat the
same pattern for sites (`sites:v2` key — NOT `sites:facilities` as originally
documented; snapshot confirmed via fresh browser reload after DEL).
result: pass
note: "R-07 tier works end-to-end. Snapshot tier serves on cold Redis, warms Redis on first hit, flips to source=redis on second hit. Side-observation from user: DevApiStatus panel overflow — logged as Test 8b (minor UX)."
side_issue_logged: "DevApiStatus panel overflows viewport when R-08 observability blocks render with full data (was sized for 2-3 rows, now has 6+ blocks per section × 2 sections = water + sites). User requested moving API observability to a separate top-bar tab instead of the slide-out panel."

### 9. Attack status cross-reference still works

expected: |
Any water facility within 5km of a recent GDELT attack event renders with
the attacked styling (dark purple on the map, "LOST CONTACT"/"DESTROYED"
banner in the detail panel). Regressed Phase 27.3 truth check — nothing
about Plan 06's `computeAdmissionDecision` extraction should have broken
the `WATER_ATTACK_EVENT_TYPES` cross-reference in useWaterLayers /
WaterFacilityDetail / useCounterData.
result: issue
reported: "not seeing any destroyed [water facilities on map]"
severity: major
diagnosis_needed: true
root_cause_hypotheses:

- "A: No GDELT events in the current window match within 5km of any water facility (benign — just no current attacks near admitted facilities). Ways to differentiate: check /api/events response for any 'airstrike' or 'on_ground' events with coords inside the Greater Middle East bbox; if yes, check whether they're within 5km of any facility in src/data/water-facilities.json."
- "B: WATER_ATTACK_EVENT_TYPES cross-reference broken — useWaterLayers / WaterFacilityDetail / useCounterData isn't running the haversine check or isn't consuming the updated event list. Plan 06 extraction should be behaviorally identical, but a unit-test regression is possible."
- "C: Event data is stale — /api/events is cached and hasn't fetched recent events in the UAT session window. Snapshot-tier change for water+sites may have inadvertently affected event polling if they share a module."
  reported_side_observation: "User also reported observability zeros re-appeared after reboot: 'Water Filters: Source: redis · Generated: 3m ago. 0 raw → 0 kept (0%). Rejections all zero. Sites Filters: same zeros.' — this is the already-logged Test 4/5 gap reproducing after Redis warmed from snapshot. No new information; same root cause (Redis cache path strips filterStats envelope)."

### 10. DevApiStatus layout didn't regress

expected: |
All DevApiStatus sections above Water Filters still render correctly:
System health, LLM pipeline progress, Weather, Precip. Copy-diagnostics
button still copies valid JSON (existing pre-Plan-02 test failure around
"sources length 8" is a stale assertion, NOT a new regression from this
phase — JSON copy itself works).
result: pass
note: "LLM Pipeline section renders correctly with current run stats ('8 enriched in 13.5s'). User asked about the batch size of 8 — clarified that Phase 27 design is 8 groups per LLM call (CLAUDE.md §'LLM Event Pipeline'). Layout of all sections above Water Filters unaffected by Plan 08 additions below them. Known pre-existing 'sources length 8' vs actual 9 test assertion is still stale (tracked in deferred-items.md, not a Phase 27.3.1 issue)."

## Summary

total: 10
passed: 5
issues: 5
pending: 0
skipped: 0
gap_entries: 7 # 2 subgaps under Test 2 + one side-gap under Test 8

## Gaps

- truth: "Every admitted WaterFacility has a real OSM name — no generic 'Dam near X' / 'Reservoir' labels on the map"
  status: resolved
  reason: "User reported: 'We're pulling a lot of unnamed dams like \"Dam near... XXX\" Not needed.' — generic-type fallback still reachable in production despite Plan 02's D-05 work"
  severity: major
  test: 2
  artifacts: []
  missing: []

- truth: "Water facility scope skews toward conflict-relevant Middle East countries, not neutral regional players"
  status: resolved
  reason: "User reported: 'we're also pulling too many dams and some reservoirs in Turkey - its not needed, priority should be middle east in conflict' — Turkey inclusion in PRIORITY_COUNTRIES produces too much noise; conflict-focus scope should tighten"
  severity: major
  test: 2
  artifacts: []
  missing: []

- truth: "DevApiStatus Water Filters panel renders real R-08 observability (byCountry, Overpass health, provenance, per-type rejections) on every request regardless of whether source is snapshot, redis, or overpass"
  status: resolved
  reason: "User reported whole-panel zeros: 'Source: redis · Generated: 10h ago. 0 raw → 0 kept (0%). Total rejections: excl=0 nn=0 nname=0 dup=0 low=0 nocity=0. Enriched: cap=0 city=0 river=0. Scores: (empty).' — the snapshot's real stats are NOT persisted to Redis, so every request after the first cold-hit renders Plan 03's placeholder zeros via buildEmptyFilterStats instead of the snapshot's baked stats. R-08 observability is effectively dead once Redis warms up."
  severity: major
  test: 4
  root_cause_hypothesis: "Redis cache write path in server/routes/water.ts stores only the facilities array, not the full CacheResponse envelope with filterStats. Fix: either cache the full envelope (preferred — preserves stats shape Plan 03 established) OR have the route re-synthesize stats from the snapshot on cache hit when served-from-snapshot provenance is lost."
  artifacts: []
  missing: []

- truth: "DevApiStatus Sites Filters panel renders real R-08 observability fields (provenance, byType, byCountry, rejections, Overpass health) on every request regardless of source tier"
  status: resolved
  reason: "User reported identical zeros for sites panel: 'Source: redis · Generated: 10h ago. 0 raw → 0 kept (0%). Rejections: turkey=0 nocoords=0 notype=0 dup=0.' — shared root cause with water Test 4 gap. Plan 07 mirrored Plan 03's Redis-write behavior, inheriting the same stats-drop."
  severity: major
  test: 5
  root_cause_hypothesis: "Same bug family as Water. Fix in server/routes/sites.ts Redis write path: cache the full CacheResponse envelope including SiteFilterStats. Plan 08's UI component is correct — upstream data shape is empty."
  artifacts: []
  missing: []

- truth: "`npm run refresh:water` and `npm run refresh:sites` complete end-to-end without hangs, writing regenerated snapshots to src/data/\*.json"
  status: resolved
  reason: "User killed `npm run refresh:water` after it hung mid-geocoding with '[Upstash Redis] Redis client was initialized without url or token. Failed to execute command.' errors flooding the console. Script imports server modules at load-time, which instantiate the Upstash client; without .env loaded, each of ~139 reverse-geocode calls triggers a 2-second REDIS_OP_TIMEOUT_MS hang (2 cache ops × 139 facilities ≈ 9 minutes of timeouts). Same bug in refresh:sites."
  severity: major
  test: 6
  root_cause: "package.json scripts `refresh:water` and `refresh:sites` use plain `tsx scripts/...` without env loading. Fix: change both to `node --env-file-if-exists=.env --import tsx/esm scripts/...` (mirrors dev:server at package.json:24) OR add `import 'dotenv/config'` at the top of each script. Single-line fix per script."
  artifacts: []
  missing: []

- truth: "DevApiStatus fits on screen without overflowing the viewport when R-08 observability blocks are populated (water + sites panels both showing real data)"
  status: resolved
  reason: "User reported: 'the API observability panel is now going way off the screen. Let's incorporate as a separate tab on the top bar' — once R-08 observability + SitesFiltersSection both render populated, the DevApiStatus slide-out panel exceeds viewport height. User requested moving API observability to a dedicated top-bar tab instead of the current slide-out panel."
  severity: minor
  test: 8
  scope_note: "This is a UX scope addition, not a strict regression. Before Test 4/5 fix lands, panels rendered zeros and fit fine. After the zeros fix lands, panels will balloon with real byCountry tables, Overpass health rows, and per-type rejection breakdowns. The tab-bar restructure should ship alongside the stats-persistence fix so the full observability data has somewhere sensible to live."
  artifacts: []
  missing: []

- truth: "Water facilities near recent GDELT attack events render with attacked (dark-purple) styling on the map + DESTROYED banner in detail panel (Phase 27.3 truth WR-01 regression check)"
  status: resolved
  reason: "User reported: 'not seeing any destroyed' — no water facilities on the map currently show the attacked styling. Three possible root causes need diagnosis: (A) no current GDELT events overlap admitted facilities within 5km — benign/data-driven, not a code regression; (B) useWaterLayers/WaterFacilityDetail/useCounterData cross-reference broken by Plan 06's computeAdmissionDecision extraction; (C) /api/events data stale or not fetching recent events."
  severity: major
  test: 9
  diagnosis_needed: true
  fix_plan_should: "Run diagnosis FIRST before writing the fix plan. Check /api/events response for airstrike/on_ground events in current window; check proximity of those events to any src/data/water-facilities.json entries; check useWaterLayers haversine + WATER_ATTACK_EVENT_TYPES import still renders attacked styling on a synthetic event within 5km of a known facility. If no events near facilities, this is a no-op data reality and only requires a note in the snapshot docs, not a code fix."
  artifacts: []
  missing: []

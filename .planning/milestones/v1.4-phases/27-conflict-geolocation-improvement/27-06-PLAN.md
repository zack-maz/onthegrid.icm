---
phase: 27-conflict-geolocation-improvement
plan: 06
type: execute
wave: 4
depends_on: ['27-05']
files_modified:
  - docs/architecture/ontology/types.md
  - docs/architecture/ontology/algorithms.md
  - docs/architecture/data-flows.md
  - CLAUDE.md
autonomous: false
requirements:
  - D-20

must_haves:
  truths:
    - 'Architecture docs reflect the new 5-type ConflictEventType union'
    - 'Data flows doc includes the LLM processing path in the events sequence diagram'
    - 'CLAUDE.md is updated with Phase 27 patterns and conventions'
    - 'All TODO(26.2) markers in architecture docs are resolved'
    - 'User has verified the full feature works end-to-end'
  artifacts:
    - path: 'docs/architecture/ontology/types.md'
      provides: 'Updated type documentation'
      contains: "'airstrike' | 'on_ground' | 'explosion' | 'targeted' | 'other'"
    - path: 'docs/architecture/data-flows.md'
      provides: 'Updated events data flow with LLM path'
      contains: 'LLM'
    - path: 'CLAUDE.md'
      provides: 'Updated project conventions'
      contains: 'Phase 27'
  key_links:
    - from: 'docs/architecture/ontology/types.md'
      to: 'server/types.ts'
      via: 'Documentation matches code'
      pattern: 'ConflictEventType'
---

<objective>
Update architecture documentation, resolve all TODO(26.2) markers, update CLAUDE.md with Phase 27 patterns, and perform a human verification of the full end-to-end feature.

Purpose: Documentation must reflect the new reality. The TODO(26.2) markers scattered across architecture docs should be resolved since this phase IS the redo. The human checkpoint ensures the LLM enrichment, precision rings, and new toggle system work correctly in the live application.

Output: Updated docs, resolved TODO markers, updated CLAUDE.md, human-verified feature.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-conflict-geolocation-improvement/27-CONTEXT.md
@.planning/phases/27-conflict-geolocation-improvement/27-05-SUMMARY.md
</context>

<threat_model>
No security threats. Documentation and verification only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update architecture docs and CLAUDE.md</name>
  <files>docs/architecture/ontology/types.md, docs/architecture/ontology/algorithms.md, docs/architecture/data-flows.md, CLAUDE.md</files>
  <read_first>
    - docs/architecture/ontology/types.md (ConflictEventType section, TODO(26.2) markers)
    - docs/architecture/ontology/algorithms.md (GDELT event dispersion, severity scoring — references to old types)
    - docs/architecture/data-flows.md (events sequence diagram, TODO(26.2) markers)
    - CLAUDE.md (full file — conventions, key files, data model sections)
    - server/types.ts (current state after Plan 01 — source of truth for docs)
    - server/adapters/gdelt.ts (current state after Plan 01)
  </read_first>
  <action>
  **File 1: docs/architecture/ontology/types.md**

1. Replace the ConflictEventType union definition:

   ```typescript
   type ConflictEventType = 'airstrike' | 'on_ground' | 'explosion' | 'targeted' | 'other';
   ```

2. Update the ConflictEventData class diagram to include new optional fields: summary, casualties, precision, actors, sourceCount, llmProcessed

3. Update the classDiagram mermaid block for ConflictEventData

4. Remove or resolve all `TODO(26.2)` markers — replace with "Phase 27 implemented LLM-based extraction with 5-type taxonomy" or similar factual statement

5. Update the Summary table at the bottom to say "5-member attack-vector union" instead of "11-member CAMEO union"

**File 2: docs/architecture/ontology/algorithms.md**

1. Update Section 2 (GDELT event dispersion) to note: "Dispersion is retained for fallback/raw-GDELT mode only. LLM-processed events have per-event precision indicators and Nominatim-geocoded coordinates, making centroid dispersion unnecessary."

2. Update Section 3 (Severity scoring) TYPE_WEIGHTS table to show the new 5 types

3. Add a new Section 9: "LLM Event Extraction" documenting the pipeline: GDELT rows -> grouping -> LLM batch processing -> Zod validation -> Nominatim geocoding -> enriched events

**File 3: docs/architecture/data-flows.md**

1. Update Section 3 (Conflict Events / GDELT v2) sequence diagram to include the LLM processing path:
   - After GDELT fetch, add an LLM processing branch (when configured + cooldown expired)
   - Show: groupGdeltRows -> callLLM (Cerebras/Groq) -> Zod validate -> forwardGeocode -> cache to events:llm
   - Show fallback: LLM failure -> serve raw GDELT

2. Update the notes section to explain dual-cache (events:gdelt + events:llm)

3. Resolve all `TODO(26.2)` markers in this file

**File 4: CLAUDE.md**

Add a new section "## LLM Event Pipeline (Phase 27)" after the existing "## Conflict Event Data" section:

```markdown
## LLM Event Pipeline (Phase 27)

- **Providers** — Cerebras primary (gpt-oss-120b, 1M TPD free), Groq fallback (openai/gpt-oss-120b, 200K TPD free)
- **LLM adapter** — `server/adapters/llm-provider.ts`, OpenAI SDK with baseURL swap for both providers
- **Event grouping** — `server/lib/eventGrouping.ts`, clusters GDELT rows by date + CAMEO root + 50km proximity
- **LLM extractor** — `server/lib/llmEventExtractor.ts`, batch processing (8 groups/call), Zod-validated output
- **Forward geocoding** — Nominatim search API via `server/adapters/nominatim.ts` `forwardGeocode()`, 1 req/s, Redis-cached 30d
- **Processing trigger** — Lazy on `/api/events` cache miss, 15-min cooldown (`events:llm-process-ts` Redis key)
- **Dual cache** — `events:llm` (LLM-enriched, preferred) + `events:gdelt` (raw fallback)
- **Graceful degradation** — LLM down -> serve raw GDELT -> same as pre-Phase-27 behavior. Map never goes blank.
- **5-type ontology** — `airstrike`, `on_ground`, `explosion`, `targeted`, `other` (replaces 11 CAMEO types)
- **Precision** — `exact` | `neighborhood` | `city` | `region`, shown as radius rings on map
- **PrecisionRingLayer** — `src/components/map/PrecisionRingLayer.tsx`, ScatterplotLayer with radiusUnits: 'meters'
- **Toggle system** — master `showEvents` + 5 sub-toggles (one per type) in filterStore
```

Update the "## Conflict Event Data" section: change ConflictEventType reference from 11 types to 5 types, update CONFLICT_TOGGLE_GROUPS reference from 3 groups to 5 groups.

Update the "## Layer Controls & Tooltips" section: note master + 5 sub-toggles.
</action>
<verify>
<automated>grep -c "TODO(26.2)" docs/architecture/ontology/types.md docs/architecture/ontology/algorithms.md docs/architecture/data-flows.md | grep -v ':0$' | wc -l</automated>
</verify>
<done>All architecture docs reflect the new 5-type system. All TODO(26.2) markers in architecture docs are resolved. CLAUDE.md has Phase 27 section. Data flows include LLM processing path.</done>
<acceptance_criteria> - docs/architecture/ontology/types.md contains "'airstrike' | 'on_ground' | 'explosion' | 'targeted' | 'other'" - docs/architecture/ontology/types.md does NOT contain "TODO(26.2)" - docs/architecture/data-flows.md does NOT contain "TODO(26.2)" - docs/architecture/data-flows.md contains "LLM" or "llm" - CLAUDE.md contains "## LLM Event Pipeline (Phase 27)" - CLAUDE.md contains "gpt-oss-120b" - grep -c "TODO(26.2)" across architecture docs returns 0 for each file
</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full Phase 27 feature</name>
  <files>none</files>
  <action>User verifies the full end-to-end feature in the running application. No code changes — this is a verification checkpoint.</action>
  <what-built>
  Full Phase 27 LLM event pipeline:
  - 5-type event ontology (airstrike, on_ground, explosion, targeted, other)
  - LLM-powered event extraction with Cerebras/Groq (when API keys configured)
  - Precision geolocation via Nominatim forward geocoding
  - Precision radius rings on map (neighborhood=1km, city=5km, region=25km)
  - Master + 5 sub-toggle event layer controls
  - EventDetail with summary, casualties, precision, location name
  - Graceful degradation to raw GDELT when LLM unavailable
  </what-built>
  <how-to-verify>
  1. Run `npm run dev` to start the development server
  2. Open the map at http://localhost:5173

**Without LLM keys (graceful degradation):** 3. Verify conflict events appear on the map (raw GDELT fallback mode) 4. Open the layer toggle panel — verify "Events" master toggle with 5 indented sub-toggles (Airstrikes, Ground Combat, Explosions, Targeted, Other) 5. Toggle the master "Events" off — all events disappear. Toggle back on — events reappear. 6. Toggle individual sub-toggles — verify each filters its category 7. Click an event — verify detail panel shows event type from the new 5-type system 8. Verify event colors are distinct shades of red (not yellow, purple, or green)

**With LLM keys (if configured):** 9. Set CEREBRAS_API_KEY and/or GROQ_API_KEY in .env 10. Restart dev server 11. Wait for events to load (may take 15-30 seconds for LLM processing) 12. Click an event — verify detail panel shows: - Summary (2-3 sentence description) - Casualties (if reported) - Precision indicator (exact/neighborhood/city/region) - "AI-enriched" badge 13. Verify precision radius rings appear for non-exact events (translucent red circles) 14. Verify the map never goes blank — if LLM fails, raw GDELT events still show

**Full test suite:** 15. Run `npx vitest run` — all tests pass 16. Run `npx tsc --noEmit` — no type errors
</how-to-verify>
<verify>User confirms all verification steps pass</verify>
<done>User has approved the full Phase 27 feature or described issues to fix.</done>
<resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run -x` — full suite passes
- `npx tsc --noEmit` — no type errors
- Zero TODO(26.2) markers in architecture docs
- Human verification of the live feature
</verification>

<success_criteria>
Architecture docs are current. CLAUDE.md reflects Phase 27. All TODO(26.2) markers resolved. User has verified the feature works end-to-end with both LLM-enriched and raw GDELT modes.
</success_criteria>

<output>
After completion, create `.planning/phases/27-conflict-geolocation-improvement/27-06-SUMMARY.md`
</output>

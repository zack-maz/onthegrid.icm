---
phase: 27-conflict-geolocation-improvement
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/LayerTogglesSlot.tsx
autonomous: true
gap_closure: true
requirements: [D-12]

must_haves:
  truths:
    - 'LayerTogglesSlot only shows visualization layer toggles (Geographic, Climate, Water, Threat Density, Political, Ethnic) with no event toggles'
    - 'Event toggles (master + 5 sub-types) exist only in FilterPanelSlot under the Conflicts section'
    - 'No duplicate event toggle UI anywhere in the app'
  artifacts:
    - path: 'src/components/layout/LayerTogglesSlot.tsx'
      provides: 'Visualization layer toggles only (6 layers, no event toggles)'
  key_links:
    - from: 'src/components/layout/FilterPanelSlot.tsx'
      to: 'src/stores/filterStore.ts'
      via: 'FilterButton components wired to toggle actions'
      pattern: 'toggleShowAirstrikes|toggleShowOnGround|toggleShowExplosions'
---

<objective>
Remove duplicate event toggles from the visualization Layers panel. Phase 27 Plan 05 incorrectly added EventMasterToggle, EventSubToggle, and EVENT_SUB_TOGGLES to LayerTogglesSlot.tsx. FilterPanelSlot.tsx already has the correct event toggles wired to filterStore at lines 326-364.

Purpose: Fix UI confusion where event toggles appear in both the Layers panel and the Filters panel. Event toggles are entity filters, not visualization layers.
Output: Clean LayerTogglesSlot.tsx with only the 6 visualization layer rows.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-conflict-geolocation-improvement/27-UAT.md

<interfaces>
<!-- The correct event toggles already live in FilterPanelSlot.tsx lines 326-364 -->
<!-- LayerTogglesSlot.tsx should ONLY contain visualization layer toggles -->

From src/stores/layerStore.ts:

```typescript
export type VisualizationLayerId =
  | 'geographic'
  | 'weather'
  | 'threat'
  | 'political'
  | 'ethnic'
  | 'water';
```

Current LayerTogglesSlot.tsx has these items to REMOVE:

- Lines 4: `import { useFilterStore } from '@/stores/filterStore';` (only used for event toggles)
- Lines 5: `import { EVENT_TYPE_COLORS } from '@/lib/eventColors';` (only used for event toggles)
- Lines 21-63: `EVENT_SUB_TOGGLES` config array
- Lines 116-174: `EventMasterToggle` and `EventSubToggle` components
- Lines 178, 186-200: `showEvents` selector, separator div, event toggle rendering block in LayerTogglesContent

Current LayerTogglesSlot.tsx has these items to KEEP:

- Lines 1-3: useUIStore, useLayerStore imports
- Lines 7-19: LAYER_CONFIGS (6 visualization layers)
- Lines 65-114: LoadingSpinner and LayerToggleRow components
- Lines 177, 180-184: LayerTogglesContent shell with LAYER_CONFIGS.map
- Lines 202-215: Clear cache button
- Lines 219-239: LayerTogglesSlot export
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate event toggles from LayerTogglesSlot</name>
  <files>src/components/layout/LayerTogglesSlot.tsx</files>
  <action>
    Edit `src/components/layout/LayerTogglesSlot.tsx` to remove ALL event-toggle-related code:

    1. REMOVE import lines:
       - `import { useFilterStore } from '@/stores/filterStore';` (line 2)
       - `import { EVENT_TYPE_COLORS } from '@/lib/eventColors';` (line 5)

    2. REMOVE the `EVENT_SUB_TOGGLES` config array (lines 21-63, the entire const declaration)

    3. REMOVE the `EventMasterToggle` component (lines 116-137)

    4. REMOVE the `EventSubToggle` component (lines 139-174)

    5. In `LayerTogglesContent()`:
       - REMOVE the `const showEvents = useFilterStore((s) => s.showEvents);` line (line 178)
       - REMOVE the separator div: `<div className="my-1 border-t border-white/10" />` (line 187)
       - REMOVE the event toggle comment and rendering block (lines 189-200):
         ```
         {/* Event toggles: master + 5 sub-toggles */}
         <EventMasterToggle />
         {EVENT_SUB_TOGGLES.map(...)}
         ```

    6. KEEP everything else intact:
       - `useUIStore` and `useLayerStore` imports
       - `LAYER_CONFIGS` array (6 visualization layers)
       - `LoadingSpinner` component
       - `LayerToggleRow` component
       - The `LAYER_CONFIGS.map(...)` rendering in `LayerTogglesContent`
       - The "Clear cache & reload" button
       - `LayerTogglesSlot` and `LayerTogglesContent` exports

    The result should be a clean file with ~130 lines (down from 240) that only shows the 6 visualization layer toggles plus the clear cache button.

  </action>
  <verify>
    <automated>npx vitest run src/ && npx tsc --noEmit</automated>
  </verify>
  <done>LayerTogglesSlot.tsx contains only visualization layer toggles (6 rows). No EventMasterToggle, EventSubToggle, EVENT_SUB_TOGGLES, separator, or filterStore/eventColors imports remain. Event toggles exist solely in FilterPanelSlot.tsx. All tests and typecheck pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` passes — all tests green
2. `npx tsc --noEmit` passes — no type errors
3. `grep -c 'EventMasterToggle\|EventSubToggle\|EVENT_SUB_TOGGLES' src/components/layout/LayerTogglesSlot.tsx` returns 0
4. `grep -c 'useFilterStore' src/components/layout/LayerTogglesSlot.tsx` returns 0
5. `grep -c 'EVENT_TYPE_COLORS' src/components/layout/LayerTogglesSlot.tsx` returns 0
</verification>

<success_criteria>

- LayerTogglesSlot renders only 6 visualization layer toggles + clear cache button
- No event toggle components, config, or imports remain in the file
- FilterPanelSlot.tsx event toggles (lines 326-364) are untouched and remain the sole location for event type toggles
- All tests pass, no type errors
  </success_criteria>

<output>
After completion, create `.planning/phases/27-conflict-geolocation-improvement/27-08-SUMMARY.md`
</output>

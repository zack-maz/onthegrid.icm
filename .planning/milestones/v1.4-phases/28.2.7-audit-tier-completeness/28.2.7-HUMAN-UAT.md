---
status: partial
phase: 28.2.7-audit-tier-completeness
source: [28.2.7-VERIFICATION.md]
started: '2026-05-08T04:55:00Z'
updated: '2026-05-08T04:55:00Z'
---

## Current Test

[awaiting human testing]

## Tests

### 1. Wave-close gate workflow exits 0 with allTiersGreen=true

expected: After phase merges to main and Vercel auto-deploys, `gh workflow run prod-connectivity-audit.yml -R zack-maz/onthegrid.icm` exits 0; Step 6 reads `steps.sidecar.outcome === 'success'`; `audit:connectivity:last-result` Redis sidecar holds `allTiersGreen: true` with `tierStatus` matching the truth table (critical=healthy, nonCritical∈{healthy,degraded}, static∈{healthy,degraded}, probeOnly=healthy, cron∈{healthy,degraded}).
result: [pending]

### 2. Cron tier flips green after each cron fires once

expected: Force-trigger /api/cron/health, /api/cron/warm, and /api/cron/refresh-events?force=true once each (Bearer-gated). Then `/api/health summary.cron === { healthy: 3, degraded: 0, unhealthy: 0, unknown: 0 }`.
result: [pending]

### 3. llmStatus survives cold starts

expected: After at least one refresh-events cron run, hit /api/health 10 times across an idle window. All responses show `endpoints.llmStatus.status === "healthy"` (Redis-backed read survives cold starts).
result: [pending]

### 4. probe-only tier healthy at deploy

expected: First request to /api/health after the new bundle goes live shows `summary.probeOnly === { healthy: 2, unhealthy: 0, unknown: 0 }`.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

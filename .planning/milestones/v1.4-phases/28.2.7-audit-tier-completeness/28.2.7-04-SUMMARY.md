---
phase: 28.2.7-audit-tier-completeness
plan: 04
subsystem: infra
tags: [build, deploy, bundle, vercel, atomic-commit]

# Dependency graph
requires:
  - phase: 28.2.7-01
    provides: cron:lastTick:{health,warm,refresh-events} Redis writers landed in server/routes/cron-{health,warm}.ts + server/routes/refresh-events-cron.ts; CRON_LASTTICK_TTL_SEC constant in server/lib/healthSources.ts
  - phase: 28.2.7-02
    provides: llm:lastProgress write-through in server/lib/llmProgress.ts + Redis-first probeLlmStatus in server/routes/health.ts; LLM_LASTPROGRESS_KEY + LLM_LASTPROGRESS_TTL_SEC constants
  - phase: 28.2.7-03
    provides: probeProbeOnly returns freshnessMs:0 (was null) in server/routes/health.ts; deriveStatus(0,0,false) → 'healthy' chain confirmed
  - phase: 28.2.6-cron-architecture-fix
    provides: api/vercel-entry.js single-file bundle pattern + tsup build script (vite build && tsup server/vercel-entry.ts --format esm --out-dir api --no-splitting)
  - phase: 28.2.5-api-green-gate
    provides: Phase 28.2.5 commit b6012a0 single-file bundle commit precedent ('build(28.2.5): refresh api/vercel-entry.js bundle for Plans 01/02/04 server changes')
provides:
  - api/vercel-entry.js regenerated via `npm run build` to reflect Plans 01+02+03 source state at deploy time
  - Single atomic build commit with conventional-commits format ('build(28.2.7):') containing ONLY api/vercel-entry.js
  - Bundle baked-in symbols verified: cron:lastTick:health, cron:lastTick:warm, cron:lastTick:refresh-events (R1) + llm:lastProgress (R2) + 2 freshnessMs:0 occurrences (R3 + probeSources canon)
affects:
  [
    28.2.7 (phase ready for merge to main),
    28.3 (k6 load test — runs against the deployed bundle once prod-connectivity-audit.yml exits 0 with allTiersGreen=true),
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Atomic bundle-regen commit as final phase task (CONTEXT D-13): build runs AFTER all source-code commits land so the bundle reflects final source state at deploy time. Phase 28.2.6 surfaced the "missing bundle regen" deploy failure mode; making bundle regen the final atomic commit pre-empts it.'
    - 'Bundle-only commit hygiene: stage only api/vercel-entry.js explicitly (`git add api/vercel-entry.js`); leave orchestrator-owned .planning/STATE.md unstaged. Source-file leak check (`git log -1 --name-only --pretty=format: | grep -cE "^server/|^src/|^scripts/"`) returns 0 — proves the build commit contains only deploy artifacts.'
    - 'tsup output target: single-file ESM bundle at api/vercel-entry.js (1.72 MB after R1+R2+R3). dist-server/ is NOT emitted by the project script (PATTERNS.md mention is stale wording from earlier phases — package.json line 13 is authoritative: `--out-dir api`).'

key-files:
  created: []
  modified:
    - api/vercel-entry.js

key-decisions:
  - 'Plan executes the diff-present path (Step 3a) — `git diff --stat api/vercel-entry.js` showed 493 lines changed (267 insertions / 226 deletions). The no-op path (Step 3b) was not taken because the local working tree had no prior `npm run build` between Plans 01-03 source commits and this plan'
  - 'Build verified end-to-end: vite build (frontend, 9.70s, 855 modules transformed) + tsup (server, 185ms, 1.72 MB ESM bundle). Both exit 0.'
  - 'Bundle substring grep checks (R1 + R2 + R3) all pass on first run: `cron:lastTick:health` 1 hit, `cron:lastTick:warm` 1 hit, `cron:lastTick:refresh-events` 1 hit, `llm:lastProgress` 1 hit literal (4 hits with LLM_LASTPROGRESS_KEY identifier counted), `freshnessMs: 0` 2 hits (probeSources + probeProbeOnly — exact match for the predicted count)'
  - 'Pre-existing .planning/STATE.md modification was already in the working tree at plan start (orchestrator-owned per spawn prompt). Left untouched — `git add api/vercel-entry.js` deliberately excludes it; only the bundle landed in the commit'
  - 'Bundle commit lands AFTER all 3 source-code commits (Plans 01/02/03) per CONTEXT D-13 atomic deploy boundary — the bundle reflects the final source state, not an intermediate state'

patterns-established:
  - 'Build-output commit hygiene check: after staging the bundle, `git log -1 --name-only --pretty=format: | grep -cE "^server/|^src/|^scripts/"` returning 0 is the source-code-leak guard. If it returns >0, source changes from a previous task leaked into the bundle commit; reset and re-stage cleanly.'
  - 'Bundle substring contract: each phase that adds new Redis-key constants or shape changes can verify the bundle picked them up via `grep -c "<expected-substring>" api/vercel-entry.js`. Checks belong in the plan acceptance-criteria, not the build script — keeps build deterministic and the contract explicit.'

requirements-completed: [bundle-regen]

# Metrics
duration: 1min
completed: 2026-05-08
---

# Phase 28.2.7 Plan 04: Bundle regen + atomic commit Summary

**Production Vercel bundle (`api/vercel-entry.js`) regenerated via `npm run build` to reflect Plans 01+02+03 source state, committed atomically as `build(28.2.7):` with only the bundle file in the diff. Pre-empts Phase 28.2.6's "missing bundle regen" deploy failure mode by making bundle regen the final phase commit.**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-05-08T04:58:51Z
- **Completed:** 2026-05-08T04:59:46Z
- **Tasks:** 1
- **Files modified:** 1 (api/vercel-entry.js — bundle artifact)

## Accomplishments

- `npm run build` exited 0 — vite (frontend, 9.70s, 855 modules) + tsup (server, 185ms, 1.72 MB ESM bundle) both succeeded.
- `api/vercel-entry.js` regenerated with 493 lines changed (267 insertions / 226 deletions vs HEAD~1) — the diff captures all 3 source-state changes from Plans 01, 02, 03 combined.
- All 6 R1+R2+R3 substring grep contracts pass on first run:
  - R1: `cron:lastTick:health` (1 hit), `cron:lastTick:warm` (1 hit), `cron:lastTick:refresh-events` (1 hit), aggregate `cron:lastTick:` (4 hits — 1 each for the 3 writers + 1 in the probeCronTick reader)
  - R2: `llm:lastProgress` (1 literal hit — value baked in as a string constant, plus 4 hits when also counting the `LLM_LASTPROGRESS_KEY` identifier)
  - R3: `freshnessMs: 0` (2 hits — probeSources canon + probeProbeOnly's R3 fix; exact match to the plan's predicted count)
- Single atomic commit `48a528a` with subject `build(28.2.7): refresh api/vercel-entry.js bundle for R1+R2+R3 server changes`. Source-file leak check (`grep -cE "^server/|^src/|^scripts/"` against the commit's name-only listing) returns 0 — only `api/vercel-entry.js` is in the diff.
- Working-tree post-commit shows only the pre-existing orchestrator-owned `.planning/STATE.md` modification (left untouched per spawn-prompt directive).
- Phase 28.2.7 is now ready for merge: all 4 plans landed cleanly, bundle reflects final source state, prod-connectivity-audit.yml is the next gate to pass.

## Task Commits

Single atomic commit on `feature/28.2.7-audit-tier-completeness`:

1. **Task 1: Run npm run build and commit the regenerated bundle** — `48a528a` (build)

## Files Created/Modified

### Created

None.

### Modified

- `api/vercel-entry.js` — Regenerated by `tsup server/vercel-entry.ts --format esm --out-dir api --no-splitting --tsconfig tsconfig.server.json`. 493 lines changed (267 insertions / 226 deletions). Now contains:
  - The 3 `cron:lastTick:<name>` Redis-key string literals from Plans 01's writers in cron-health.ts, cron-warm.ts, refresh-events-cron.ts
  - The `llm:lastProgress` string literal + the LLM_LASTPROGRESS_KEY identifier (and the LLM_LASTPROGRESS_TTL_SEC = 604_800 numeric literal) from Plan 02's setters in llmProgress.ts and the Redis-first read in probeLlmStatus
  - The `freshnessMs: 0` literal in probeProbeOnly's body (was `null` pre-Plan 03), alongside the pre-existing `freshnessMs: 0` in probeSources's canon
  - The async signature of probeLlmStatus + the Redis read with `cacheGetSafe<{ startedAt, completedAt }>` shape — verified by the existing tests passing in the source (vitest 2192/2192 from Plan 03's self-check)
- `dist/` (frontend Vite bundle) — also regenerated by the build but NOT committed; vercel.json's rewrite rules (`/api/*` → serverless function, everything else → SPA `index.html`) make `dist/` a build-time artifact that gets fetched separately by Vercel's static-asset pipeline. Phase 28.2.5 + 28.2.6 bundle-only commits omit it; this plan follows the same pattern.

## Decisions Made

All key implementation decisions were locked in `28.2.7-CONTEXT.md` (D-13 — bundle regen as final atomic commit) and `28.2.7-04-PLAN.md`'s `<action>` block (exact build command, exact stage list, exact commit message HEREDOC, exact verification grep checks). No new decisions were needed during execution.

## Deviations from Plan

None — plan executed exactly as written. Build exited 0 on the first run. All 6 substring grep checks passed on the first run with the expected counts. The single atomic commit landed cleanly with source-leak check returning 0. The pre-existing `.planning/STATE.md` working-tree modification was correctly excluded from the commit by explicit `git add api/vercel-entry.js` (not `git add -A`).

---

**Total deviations:** 0
**Impact on plan:** None.

## Issues Encountered

- The lint-staged hook ran on the bundle commit but matched no configured tasks (`→ No staged files match any configured task.`). Expected — `api/vercel-entry.js` is generated build output and is NOT covered by ESLint or Prettier (the project's lint config scopes to source files under `src/`, `server/`, etc.). The gitleaks pre-commit scan ran cleanly (`0 commits scanned`, `no leaks found`, ~10.92 KB scanned in 58.4ms). Commit landed without retry.
- The pre-existing `.planning/STATE.md` modification was already present in the working tree at plan start (orchestrator-owned per the spawn prompt: "Do NOT update STATE.md or ROADMAP.md — the orchestrator owns those writes"). Left untouched throughout this plan; the build commit excluded it via explicit `git add api/vercel-entry.js`.

## User Setup Required

None — no external service configuration required, no env vars added, no new Redis keys created.

## Next Phase Readiness

- **Phase 28.2.7 ready for merge:** All 4 plans (01, 02, 03, 04) have landed on `feature/28.2.7-audit-tier-completeness`. Source state covers R1 (cron writers), R2 (llm:lastProgress write-through + Redis-first probe), R3 (probeProbeOnly honest-stub). Bundle reflects the final source state at deploy time.
- **Wave-close gate (post-merge):** Per `28.2.7-04-PLAN.md` `<verification>` block, the operator runs `gh workflow run prod-connectivity-audit.yml -R zack-maz/onthegrid.icm` after the phase merges to `main` and Vercel auto-deploys (~2-3 min). Expected workflow outcomes: exit 0 + `audit:connectivity:last-result.allTiersGreen === true`. If any tier is still red, the operator re-verifies bundle deploy via `curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" https://otg-iran-monitor.vercel.app/api/health | jq '.summary'` and confirms the 3 tier expectations from the verification block:
  - `summary.cron === { healthy: 3, degraded: 0, unhealthy: 0, unknown: 0 }` (R1)
  - `summary.nonCritical.healthy >= 1` with `endpoints.llmStatus.status === "healthy"` (R2 — requires at least one cron run to flip green; first 4am UTC tick post-deploy completes this)
  - `summary.probeOnly === { healthy: 2, unhealthy: 0, unknown: 0 }` (R3 — mechanical, immediate at deploy)
- **Phase 28.3 (k6 load test):** Unblocked once `prod-connectivity-audit.yml` exits 0 with `allTiersGreen=true`. Phase 28.3 measures prod under 1-300 VU sweep with all tiers green and a coherent data path — the goal of the entire 28.2.7 phase.

## Self-Check: PASSED

- File `api/vercel-entry.js`: FOUND (1.72 MB, sha1 `2af424e24b4aa687015c4a6125eadee71caaf179`)
- Bundle substring `cron:lastTick:health`: 1 occurrence (R1 verified)
- Bundle substring `cron:lastTick:warm`: 1 occurrence (R1 verified)
- Bundle substring `cron:lastTick:refresh-events`: 1 occurrence (R1 verified)
- Bundle substring `llm:lastProgress`: 1 literal + 4 with identifier (R2 verified)
- Bundle substring `freshnessMs: 0`: 2 occurrences (R3 + probeSources canon — verified)
- Commit `48a528a` (Task 1): FOUND
- Commit subject: `build(28.2.7): refresh api/vercel-entry.js bundle for R1+R2+R3 server changes` (matches conventional-commits format)
- Source-file leak check (`grep -cE "^server/|^src/|^scripts/"` on commit's name-only listing): 0 (no source-code files leaked into build commit)
- Working tree post-plan: only `.planning/STATE.md` modified (orchestrator-owned, left untouched per spawn-prompt directive)

---

_Phase: 28.2.7-audit-tier-completeness_
_Plan: 04_
_Completed: 2026-05-08_

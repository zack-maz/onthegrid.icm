---
phase: 40-dashboard-ui-ux-polish-subtab-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/app.css
  - src/lib/colorBridge.ts
  - src/__tests__/lib/colorBridge.test.ts
  - src/stores/uiStore.ts
  - src/types/ui.ts
autonomous: true
requirements: [UI-POLISH-03]
must_haves:
  truths:
    - 'Three new --color-status-* CSS vars resolve byte-identically to their prior map-token hex'
    - 'colorBridge re-exports the 3 status hex constants with byte-identical fallback defaults'
    - 'uiStore carries session-scoped group-collapse + drawer state with toggle actions'
  artifacts:
    - path: 'src/styles/app.css'
      provides: '3 new @theme status tokens'
      contains: '--color-status-healthy'
    - path: 'src/lib/colorBridge.ts'
      provides: '3 hex re-exports for status tokens'
      exports: ['COLOR_STATUS_HEALTHY_HEX', 'COLOR_STATUS_DEGRADED_HEX', 'COLOR_STATUS_WARNING_HEX']
    - path: 'src/stores/uiStore.ts'
      provides: 'devApiGroupCollapsed + isOperatorDrawerOpen state + toggles'
      contains: 'toggleDevApiGroup'
    - path: 'src/types/ui.ts'
      provides: 'UIState members for collapse + drawer'
      contains: 'devApiGroupCollapsed'
  key_links:
    - from: 'src/styles/app.css @theme'
      to: 'src/lib/colorBridge.ts readCssHex'
      via: 'CSS var name string'
      pattern: "readCssHex\\('--color-status-"
    - from: 'src/lib/colorBridge.ts'
      to: 'src/__tests__/lib/colorBridge.test.ts'
      via: 'byte-identity sentinel assertion'
      pattern: 'COLOR_STATUS_HEALTHY_HEX'
---

<objective>
Lay the FOUNDATION layer for the API Health tab polish: add the 3 new semantic status color tokens through the D-13 single-source pipeline (app.css @theme → colorBridge hex re-export → byte-identity sentinel test), and add the session-scoped group-collapse + operator-drawer view-state to uiStore. Neither construct depends on the DevApiStatus DOM restructure — they UNBLOCK it (Plans 02/03 consume these tokens + store actions). Different files from the restructure ⇒ parallel-safe.

Purpose: The hero header (D-05) and unified status dots need named status semantics distinct from the entity --color-\* map palette; today the tier banner borrows map tokens (--color-site-healthy / --color-site-attacked / --color-event-airstrike) as status colors — semantically wrong and a drift risk. The collapse + drawer state must exist in uiStore before the restructure can wire the 4 collapsible groups + drawer.
Output: 3 byte-identical status CSS vars + 3 hex constants + 3 sentinel assertions + 4 new uiStore members (2 state, 3 actions) + matching UIState type additions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-UI-SPEC.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following symbols (the plan-review source-grounding pass excludes these from drift verification):

**CSS vars (src/styles/app.css @theme):**

- `--color-status-healthy` = `#22c55e`
- `--color-status-degraded` = `#f97316`
- `--color-status-warning` = `#eab308`

**colorBridge hex re-exports (src/lib/colorBridge.ts):**

- `COLOR_STATUS_HEALTHY_HEX`
- `COLOR_STATUS_DEGRADED_HEX`
- `COLOR_STATUS_WARNING_HEX`

**uiStore state + actions (src/stores/uiStore.ts) + UIState type (src/types/ui.ts):**

- `devApiGroupCollapsed: Record<string, boolean>` (default `{}` ⇒ all expanded)
- `isOperatorDrawerOpen: boolean` (default `false`)
- `toggleDevApiGroup(slug: string): void`
- `toggleOperatorDrawer(): void`
- `setOperatorDrawerOpen(open: boolean): void`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 status tokens through the D-13 single-source pipeline (app.css + colorBridge + sentinel)</name>
  <files>src/styles/app.css, src/lib/colorBridge.ts, src/__tests__/lib/colorBridge.test.ts</files>
  <read_first>
    - src/styles/app.css lines 38–56 (the @theme entity-token region + the "Hex NOT OKLCH" comment at :38–43 explaining why colorBridge's hex parser needs hex)
    - src/lib/colorBridge.ts lines 110–142 (the hex re-export region; `COLOR_SITE_HEALTHY_HEX = readCssHex('--color-site-healthy', '#22c55e')` at :124–125 is the EXACT template)
    - src/__tests__/lib/colorBridge.test.ts lines 66–99 (the `hexExports` shape-test array + the `/^#[0-9a-f]{6}$/i` it.each) and the byte-identity assertion idiom
    - 40-UI-SPEC.md §Color "NEW semantic tokens (3)" table + the D-13 routing 3-step list
    - 40-PATTERNS.md §"app.css @theme + colorBridge.ts — 3 new status tokens" (the full D-13 routing chain)
    - CLAUDE.md §"Color Tokens (Phase 28.1+)" — the D-13 single-source contract
  </read_first>
  <action>
    Add the 3 status tokens through ALL THREE D-13 surfaces (build fails if any drifts).
    (1) In src/styles/app.css @theme, after the `--color-site-attacked: #f97316;` line (:55), add a new comment-labeled block with the 3 status vars: `--color-status-healthy: #22c55e;`, `--color-status-degraded: #f97316;`, `--color-status-warning: #eab308;`. Hex NOT OKLCH (so colorBridge roundtrips, consistent with the :38–43 comment). Add a one-line comment noting these are the operator-console status namespace, byte-identical to the site/event map tokens they replace so the migration is zero-visual-change.
    (2) In src/lib/colorBridge.ts, after the `COLOR_SITE_ATTACKED_HEX` line (:125), add 3 `readCssHex` re-exports: `export const COLOR_STATUS_HEALTHY_HEX = readCssHex('--color-status-healthy', '#22c55e');`, `COLOR_STATUS_DEGRADED_HEX` → `'#f97316'`, `COLOR_STATUS_WARNING_HEX` → `'#eab308'`. HEX ONLY — do NOT add any `readCssRGB` tuple export (no deck.gl consumer; UI-SPEC migration note).
    (3) In src/__tests__/lib/colorBridge.test.ts: (a) add the 3 new constant names to the `hexExports` array (:67–92) so the `/^#[0-9a-f]{6}$/i` shape test covers them; (b) add 3 literal byte-identity assertions (mirror the `faction disputed hex is #f59e0b` literal-assertion precedent) asserting `bridge.COLOR_STATUS_HEALTHY_HEX` toBe `'#22c55e'`, degraded toBe `'#f97316'`, warning toBe `'#eab308'`.
    Do NOT migrate any DevApiStatus consumers in this plan — the token-NAME migration of the sparkline + tier banner is Plan 02's restructure work (that file is owned by Plan 02). This plan only DECLARES the tokens.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/colorBridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/app.css contains the literal line `--color-status-healthy: #22c55e;` (and the degraded `#f97316` + warning `#eab308` lines)
    - src/lib/colorBridge.ts exports `COLOR_STATUS_HEALTHY_HEX`, `COLOR_STATUS_DEGRADED_HEX`, `COLOR_STATUS_WARNING_HEX` via `readCssHex('--color-status-*', ...)`
    - No `readCssRGB`/tuple export was added for any status token
    - `npx vitest run src/__tests__/lib/colorBridge.test.ts` exits 0
    - The `hexExports` array in the test now lists all 3 new constant names (shape test covers them)
  </acceptance_criteria>
  <done>3 status tokens declared in app.css @theme, re-exported as hex in colorBridge, and asserted byte-identical (#22c55e / #f97316 / #eab308) in the sentinel test which passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add session-scoped group-collapse + operator-drawer state to uiStore + UIState type</name>
  <files>src/stores/uiStore.ts, src/types/ui.ts</files>
  <read_first>
    - src/stores/uiStore.ts lines 14–53 (the curried `create<UIState>()((set, get) => ({...}))` store; the DevApiStatus modal slice :34–41 with the "session-scoped; no localStorage by design" comment; the `set((s) => ({...}))` functional-updater idiom at :50 `toggleStatus`)
    - src/types/ui.ts lines ~106–111 (the UIState members for the DevApiStatus modal tab state — add the new members adjacent)
    - 40-UI-SPEC.md §Collapsible Group Contract (the `devApiGroupCollapsed: Record<string,boolean>` row + `toggleDevApiGroup(slug)` action) + §Drawer Contract (the `isOperatorDrawerOpen: boolean` default `false` row)
    - 40-PATTERNS.md §"uiStore.ts — collapse + drawer state" (slice idiom + toggle idiom + the explicit warning: copy the MODAL branch [:34–41], NOT the `isMarketsCollapsed` localStorage branch at :27/:84–92)
  </read_first>
  <action>
    Add the collapse + drawer slice to uiStore co-located with the existing DevApiStatus modal slice (after `setDevApiStatusTab` at :41), keeping the store a single `create<UIState>()()` call (curried pattern, CLAUDE.md convention).
    State (in the object body, near :38): `devApiGroupCollapsed: {}` (default empty ⇒ all 4 groups expanded per D-01), `isOperatorDrawerOpen: false` (default closed per D-02a — destructive controls not screenshot-default-visible).
    Actions: `toggleDevApiGroup: (slug) => set((s) => ({ devApiGroupCollapsed: { ...s.devApiGroupCollapsed, [slug]: !s.devApiGroupCollapsed[slug] } }))`; `toggleOperatorDrawer: () => set((s) => ({ isOperatorDrawerOpen: !s.isOperatorDrawerOpen }))`; `setOperatorDrawerOpen: (open) => set({ isOperatorDrawerOpen: open })`.
    SESSION-SCOPED — do NOT route through `readBool`/`localStorage.setItem` (mirror the modal-state comment at :34–36 and `isDevApiStatusOpen`; do NOT copy the `isMarketsCollapsed` persistence branch).
    In src/types/ui.ts, add the 5 corresponding members to the `UIState` interface next to the existing DevApiStatus tab members (:~108–111): `devApiGroupCollapsed: Record<string, boolean>;`, `isOperatorDrawerOpen: boolean;`, `toggleDevApiGroup: (slug: string) => void;`, `toggleOperatorDrawer: () => void;`, `setOperatorDrawerOpen: (open: boolean) => void;`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/__tests__/devApiStatus.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/uiStore.ts contains `devApiGroupCollapsed: {}` and `isOperatorDrawerOpen: false` as default state, plus `toggleDevApiGroup`, `toggleOperatorDrawer`, `setOperatorDrawerOpen` actions
    - No `localStorage`/`readBool` call references `devApiGroupCollapsed` or `isOperatorDrawerOpen` (grep `localStorage` in uiStore returns only the pre-existing markets-collapsed lines)
    - `toggleDevApiGroup` uses the `set((s) => ({ devApiGroupCollapsed: { ...s.devApiGroupCollapsed, [slug]: !s.devApiGroupCollapsed[slug] } }))` functional-updater shape
    - src/types/ui.ts `UIState` declares all 5 new members
    - `npx tsc --noEmit` exits 0 (store satisfies the typed `create<UIState>()` contract)
  </acceptance_criteria>
  <done>uiStore exposes session-scoped `devApiGroupCollapsed` (default {}) + `isOperatorDrawerOpen` (default false) with the 3 toggle/setter actions; UIState type declares them; tsc clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | This plan touches only client-side CSS tokens, a hex-string utility, and Zustand view-state. No endpoint, no data mutation, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                    |
| ---------- | ---------------------- | ---------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-40-01-01 | Tampering              | 3 new `--color-status-*` tokens                            | accept      | Static design tokens (hex literals in @theme); no user input, no injection vector. Byte-identity sentinel test prevents accidental drift, not malicious tampering. |
| T-40-01-02 | Information disclosure | `devApiGroupCollapsed` / `isOperatorDrawerOpen` view-state | accept      | Pure presentation state, session-scoped, no persistence, no PII, never serialized to a request. No disclosure surface.                                             |
| T-40-01-SC | Tampering              | npm/pip/cargo installs                                     | n/a         | No package installs in this plan (no new dependencies).                                                                                                            |

**Verdict:** No new attack surface. No HIGH/MEDIUM threats. The 3 color tokens are inert static design values; the 2 view-state members carry no auth, no data, and no network surface. Per the phase threat_model note, this is confirmed as a no-new-surface change.
</threat_model>

<verification>
- `npx vitest run src/__tests__/lib/colorBridge.test.ts` exits 0 (3 new byte-identity assertions pass; shape test covers the 3 new constants)
- `npx tsc --noEmit` exits 0 (uiStore satisfies `create<UIState>()`; UIState declares the 5 new members)
- `npx vitest run src/__tests__/devApiStatus.test.tsx` still passes (no regression to existing modal render)
- grep confirms no `localStorage` reference to the new collapse/drawer members
</verification>

<success_criteria>

- 3 `--color-status-*` tokens declared in app.css @theme (hex), re-exported as 3 hex constants in colorBridge, byte-identical (#22c55e/#f97316/#eab308) asserted in the sentinel test
- uiStore carries `devApiGroupCollapsed` (default {}), `isOperatorDrawerOpen` (default false), and the 3 toggle/setter actions, session-scoped, no localStorage
- UIState type declares all 5 new members; tsc clean
- No DevApiStatus consumer touched (that is Plan 02's file ownership)
  </success_criteria>

<output>
Create `.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-01-SUMMARY.md` when done
</output>

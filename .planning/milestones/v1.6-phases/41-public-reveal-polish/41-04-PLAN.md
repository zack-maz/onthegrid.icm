---
phase: 41-public-reveal-polish
plan: 04
type: execute
wave: 3
depends_on: [41-01, 41-02]
files_modified:
  [scripts/capture-hero.ts, package.json, README.md, public/screenshots/, docs/SHOWCASE.md]
autonomous: false
requirements: [REVEAL-DOCS-07]
user_setup: []
must_haves:
  truths:
    - 'The 6 existing PNGs + hero.gif move from docs/ to public/screenshots/ and every reference is updated (D-06) (SC41-3)'
    - 'scripts/capture-hero.ts gains a --layers mode; npm run capture:layers regenerates the ~10 live-data PNGs into public/screenshots/ (SC41-3)'
    - 'A 1200x630 og-card.png exists at public/screenshots/ for the REVEAL-SITE-03 og:image (served at /screenshots/og-card.png)'
    - 'README image refs use the repo path public/screenshots/...; capture-layers.contract.test.ts goes green'
  artifacts:
    - {
        path: 'scripts/capture-hero.ts',
        provides: '--layers mode + repointed path constants',
        contains: 'layers',
      }
    - {
        path: 'public/screenshots/og-card.png',
        provides: '1200x630 OG image asset for REVEAL-SITE-03',
      }
    - { path: 'package.json', provides: 'capture:layers npm script', contains: 'capture:layers' }
  key_links:
    - {
        from: 'scripts/capture-hero.ts path constants',
        to: 'public/screenshots/',
        via: 'DOCS_DIR/SCREENSHOTS_DIR/HERO_GIF repoint (D-06)',
        pattern: 'public/screenshots',
      }
    - {
        from: 'README.md image refs',
        to: 'public/screenshots/',
        via: 'repo-relative path update',
        pattern: 'public/screenshots',
      }
    - {
        from: 'src/__tests__/capture-layers.contract.test.ts',
        to: 'scripts/capture-hero.ts',
        via: 'ALL_LAYERS + output-path + --layers assertions',
        pattern: 'capture-layers',
      }
---

<objective>
Wave 3 / part A — screenshot consolidation (D-06) + the reproducible capture:layers tooling (REVEAL-DOCS-07). Sequence is load-bearing (RESEARCH Pitfall 2): (1) repoint capture-hero.ts path constants + move the 6 PNGs + hero.gif into public/screenshots/, (2) extend capture-hero.ts with a --layers mode + add the capture:layers npm script, (3) run it to regenerate the ~10 live-data PNGs, (4) produce a 1200x630 og-card.png, (5) update README + SHOWCASE image refs to the public/screenshots/ repo path. MUST precede Plan 06's og:image wiring (OG meta sources public/screenshots/og-card.png). autonomous:false — has a blocking human-verify checkpoint for the live capture run. Purpose: SC41-3 screenshot portion. Turns capture-layers.contract.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-reveal-polish/41-CONTEXT.md
@.planning/phases/41-public-reveal-polish/41-RESEARCH.md
@.planning/phases/41-public-reveal-polish/41-PATTERNS.md
@.planning/phases/41-public-reveal-polish/41-01-SUMMARY.md
@scripts/capture-hero.ts
</context>

<artifacts_this_phase_produces>
See 41-01-PLAN.md for the full list. This plan creates: capture:layers npm script + --layers mode, public/screenshots/\* (relocated 6 + ~10 new), public/screenshots/hero.gif, public/screenshots/og-card.png; updates README + SHOWCASE refs.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move assets to public/screenshots/, repoint capture-hero.ts constants, add --layers mode + npm script</name>
  <files>scripts/capture-hero.ts, package.json, public/screenshots/</files>
  <read_first>
    - scripts/capture-hero.ts (lines 14-22 docstring paths; 42-44 DOCS_DIR/SCREENSHOTS_DIR/HERO_GIF; 61 ALL_LAYERS; 65-72 parseArgs Mode; 83-99 checkPrereqs ffmpeg/gifski guard; 102-132 waitForMap; 140-192 openLayersPanel/setOnlyLayer/enableAllLayers; 259-267 headless-WebGL launch flags; 401 deviceScaleFactor:2; 415 screenshot)
    - 41-PATTERNS.md section "scripts/capture-hero.ts -> add --layers mode" (parseArgs extension, path-constant repoint, ffmpeg/gifski branch-by-mode caveat, npm-script addition)
    - 41-RESEARCH.md section "Screenshot Consolidation Mechanics (D-06)" + section "capture:layers Tooling Analysis" (the ~10-shot table + settle discipline + A3 dev-open verdict)
    - package.json (line 21 capture:hero — sibling for the new capture:layers script; line 38 docs:lint)
  </read_first>
  <action>
    (a) Move the 6 PNGs (detail-panel/ethnic-layer/political-layer/search-modal/threat-density/water-stress.png) from docs/screenshots/ and docs/hero.gif into public/screenshots/ (git mv; create the dir). (b) Repoint capture-hero.ts: DOCS_DIR/SCREENSHOTS_DIR/HERO_GIF (lines 42-44) and docstring output paths (14-22) to public/screenshots/. (c) Extend parseArgs Mode type with 'layers' and add the --layers branch. (d) Branch checkPrereqs so the --layers PNG path does NOT die on missing ffmpeg/gifski (those are GIF-only; lines 83-99). (e) Add the npm script "capture:layers": "tsx scripts/capture-hero.ts --layers" beside capture:hero. Reuse waitForMap/openLayersPanel/setOnlyLayer/enableAllLayers and the headless-WebGL launch flags VERBATIM (without --use-gl=angle --use-angle=swiftshader the canvas renders zeroed frames). NOTE (resolves Open-Q A3): src/lib/dashboardAuth.ts useShouldRenderDashboard returns `import.meta.env.DEV || hasDashboardKey()`, so DevApiStatus is dev-open in local dev — the API-Health shots need NO localStorage auth injection. Do not add a second capture file; extend in place.
  </action>
  <verify>
    <automated>test -d public/screenshots; test -f public/screenshots/threat-density.png; test -f public/screenshots/hero.gif; grep -q 'public/screenshots' scripts/capture-hero.ts; grep -q "'layers'" scripts/capture-hero.ts; grep -q 'capture:layers' package.json; test ! -d docs/screenshots || test -z "$(ls docs/screenshots/*.png 2>/dev/null)"; echo MOVE_AND_MODE_OK</automated>
  </verify>
  <acceptance_criteria>
    - public/screenshots/ holds the 6 relocated PNGs + hero.gif; docs/screenshots/ no longer holds the PNGs.
    - capture-hero.ts path constants point at public/screenshots/; parseArgs recognizes --layers; checkPrereqs does not require ffmpeg/gifski in --layers mode.
    - package.json has the capture:layers script; npm run typecheck passes.
  </acceptance_criteria>
  <done>Assets relocated, path constants repointed, --layers mode + npm script added, dev-open API-Health confirmed (no auth injection needed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run capture:layers against live data; verify the ~10 PNGs + og-card.png</name>
  <files>public/screenshots/</files>
  <action>
    The executor runs the live capture, then pauses for human verification. Start the dev server (npm run dev, reachable at :5173 — checkPrereqs fails fast otherwise), then run npm run capture:layers to regenerate the screenshot set into public/screenshots/ against LIVE data (D-04, no fixtures): the 6 viz layers (Geographic, Climate=weather, Political, Ethnic, Water, Threat Density via setOnlyLayer), API Health dashboard (dev-open), threat-density cluster click-through, actor-quality drill-down, ghost-event prune-flow VIEW (read-only, no actual prune), FlightRecorderBlock drill-down (Phase 39). Then produce a derived 1200x630 og-card.png (well-composed all-layers frame; crop/compose if capture cannot emit exactly 1200x630) at public/screenshots/og-card.png for REVEAL-SITE-03. This task pauses for the human to confirm the output before proceeding.
  </action>
  <what-built>
    The --layers mode drives the real UI against live data to regenerate ~10 PNGs into public/screenshots/ plus a 1200x630 og-card.png.
  </what-built>
  <how-to-verify>
    1. npm run dev (must be reachable at :5173). 2. In a second shell: npm run capture:layers. 3. Confirm ~10 fresh PNGs in public/screenshots/ render non-empty, settled layers (D-04 = re-runnable not byte-identical; a quiet-day event/threat shot may be sparse — acceptable). 4. Confirm public/screenshots/og-card.png exists and is 1200x630 (file public/screenshots/og-card.png / image dims).
  </how-to-verify>
  <read_first>
    - 41-RESEARCH.md section "capture:layers Tooling Analysis" (the ~10-shot driver table + waitForFunction settle discipline for event/threat shots)
    - 41-RESEARCH.md section "OG / Twitter / Social-Share Mechanics" (og-card.png 1200x630, why a static PNG over the GIF)
  </read_first>
  <verify>
    <automated>ls public/screenshots/*.png | wc -l; test -f public/screenshots/og-card.png; echo CAPTURE_FILES_PRESENT</automated>
  </verify>
  <done>~10 live-data PNGs and a 1200x630 og-card.png exist in public/screenshots/, human-confirmed non-empty.</done>
  <resume-signal>Type "approved" once the ~10 live-data PNGs and a 1200x630 og-card.png exist in public/screenshots/, or describe which shots failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update README + SHOWCASE image refs to public/screenshots/; confirm contract + lint</name>
  <files>README.md, docs/SHOWCASE.md</files>
  <read_first>
    - 41-PATTERNS.md section "Screenshot Reference Consolidation (D-06 — every path to update)" (README line 5 hero, 265-268 table, 279-284 list; capture-hero docstring; docs:lint glob caveat A6)
    - README.md (lines 5, 265-268, 279-284 — the image refs to repoint)
    - docs/SHOWCASE.md (the hero-GIF link from Plan 02 — confirm it now resolves to public/screenshots/hero.gif)
    - src/__tests__/capture-layers.contract.test.ts (the Wave-0 contract this must satisfy)
  </read_first>
  <action>
    Update every screenshot reference to the public/screenshots/ REPO path (NOT the served /screenshots/ path — that form is only for the OG meta in Plan 06): README line 5 hero -> public/screenshots/hero.gif; README 265-268 + 279-284 -> public/screenshots/<name>.png; confirm SHOWCASE.md hero link resolves to public/screenshots/hero.gif. Verify markdown-link-check (docs:lint) resolves the new public/... relative paths (docs:lint lints README.md explicitly). Then run the capture-layers contract test — it asserts ALL_LAYERS (incl Geographic + Climate), the public/screenshots/ output-path constants, the --layers mode, and the enumerated output filenames; it must now go GREEN.
  </action>
  <verify>
    <automated>grep -q 'public/screenshots' README.md; ! grep -q 'docs/screenshots' README.md; ! grep -q 'docs/hero.gif' README.md; npx vitest run src/__tests__/capture-layers.contract.test.ts; npm run docs:lint; echo REFS_AND_CONTRACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - README + SHOWCASE image refs use public/screenshots/ repo path; no stale docs/screenshots or docs/hero.gif refs remain.
    - src/__tests__/capture-layers.contract.test.ts passes (GREEN); npm run docs:lint passes.
  </acceptance_criteria>
  <done>All image refs repointed to public/screenshots/; capture contract green; docs:lint green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                     |
| ---------------------------------------------- | --------------------------------------------------------------- |
| Playwright capture script -> local dev server  | Headless browser drives local :5173; no remote/prod credentials |
| public/screenshots/ assets -> CDN/static serve | Static images served as-is; public, no PII                      |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                                                                                                                           |
| ----------- | ---------------------- | ---------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-CAP-01 | Information disclosure | API-Health shots capturing dev dashboard | mitigate    | capture:layers runs against LOCAL dev (dev-open per useShouldRenderDashboard); shots show read-only operator surface (D-02) with no tokens/keys rendered. Confirm no Bearer/DASHBOARD_PASSWORD value is visible in any shot before commit. Severity: low. |
| T-41-CAP-02 | Tampering              | A3 localStorage auth injection           | mitigate    | Open-Q A3 resolved: dev is auto-open, so NO localStorage dashboard-auth key is injected. If a future run needs one, it must use a dev/local value, never committed. Severity: low (not exercised).                                                        |
| T-41-CAP-03 | Information disclosure | og-card.png composition                  | accept      | Static promotional frame of public map layers; no sensitive data. Severity: low.                                                                                                                                                                          |
| T-41-SC     | Tampering              | npm/pip/cargo installs                   | mitigate    | This plan installs NO new package (Playwright/tsx already devDeps). The only new dep in the phase is driver.js, gated in Plan 06. Severity: none-this-plan.                                                                                               |

No threat in this plan reaches `high`. Capture runs locally; shots are of the public read-only surface; confirm-no-secrets-in-frame is the operative check before commit.
</threat_model>

<verification>
- public/screenshots/ holds 6 relocated PNGs + hero.gif + ~10 new live-data PNGs + og-card.png (1200x630).
- capture-hero.ts has --layers mode + repointed constants; package.json has capture:layers.
- README + SHOWCASE refs use public/screenshots/; capture-layers.contract.test.ts GREEN; docs:lint GREEN; npm run typecheck passes.
</verification>

<success_criteria>

- SC41-3 screenshot portion satisfied: public/screenshots/ extended with ~10 captures; npm run capture:layers reproduces them.
- og-card.png exists for Plan 06's og:image wiring (cross-plan handoff).
- No secrets visible in any committed screenshot.
  </success_criteria>

<output>
Create `.planning/phases/41-public-reveal-polish/41-04-SUMMARY.md` when done. State the og-card.png served path (/screenshots/og-card.png) so Plan 06 wires og:image correctly.
</output>

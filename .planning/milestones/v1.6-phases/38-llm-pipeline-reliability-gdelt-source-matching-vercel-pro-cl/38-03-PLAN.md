---
phase: 38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit-gdelt-corpus.ts
  - server/__tests__/scripts/audit-gdelt-corpus.test.ts
  - package.json
autonomous: true
requirements: [GDELT-MATCH-01]

must_haves:
  truths:
    - 'A Phase-22-style audit script categorizes the live events:llm:v3 corpus into high-confidence (tier 1) / neutral (tier 2) / low-confidence (tier 3 or null) buckets by source tier (SC38-4)'
    - 'The audit detects orphan events (no matching GDELT-DOC news:gdelt cluster) and duplicate-source clusters (events collapsing to one eventGrouping key)'
    - 'The audit output is a structured report that SIZES the GDELT-MATCH-02 dedup threshold, GDELT-MATCH-03 corroboration tuning, and GDELT-MATCH-04 composite weights — this is the HARD GATE for plan 06'
  artifacts:
    - path: 'scripts/audit-gdelt-corpus.ts'
      provides: 'CLI tsx audit of events:llm:v3 corpus quality (tier buckets, orphans, dup clusters)'
      min_lines: 80
    - path: 'server/__tests__/scripts/audit-gdelt-corpus.test.ts'
      provides: "Unit test for the audit's bucketing + orphan + dedup-candidate logic on a fixture corpus"
      min_lines: 40
  key_links:
    - from: 'scripts/audit-gdelt-corpus.ts'
      to: 'server/lib/sourceTiers.ts getHighestTier'
      via: 'tier classification per event source URL'
      pattern: 'getHighestTier'
    - from: 'scripts/audit-gdelt-corpus.ts'
      to: 'server/lib/eventGrouping.ts groupGdeltRows'
      via: 'duplicate-cluster sizing'
      pattern: 'groupGdeltRows'
---

<objective>
GDELT-MATCH-01 — the Phase-22-style corpus-quality audit. This is the strand's Plan 1 and a HARD GATE: its findings size the dedup threshold (02), corroboration tuning (03), and composite weights (04) in plan 06. Per D-07 the audit is read-only and non-destructive — it categorizes the live corpus, it does not mutate it.

Purpose: SC38-4 (first step) — establish the empirical baseline that makes the 02–04 thresholds defensible instead of guessed.

Output: a new `scripts/audit-gdelt-corpus.ts` CLI (analog of `scripts/audit-events.ts`) reusing `eventAudit.ts` types + `sourceTiers.getHighestTier` + `eventGrouping.groupGdeltRows`, plus a unit test on a fixture corpus, plus an audit report artifact the executor commits for plan 06 to consume.

Implements: D-07 (audit-gated, non-destructive — GDELT-MATCH-01 is Plan 1 and the gate that sizes 02–04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-CONTEXT.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-RESEARCH.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-PATTERNS.md
@scripts/audit-events.ts
</context>

<artifacts_this_phase_produces>
NEW symbols/files this plan creates (source-grounding pass MUST exclude these — they do not exist in current `main`):

- `scripts/audit-gdelt-corpus.ts` — NEW CLI audit script (GDELT-MATCH-01)
- `server/__tests__/scripts/audit-gdelt-corpus.test.ts` — NEW unit test
- a committed audit-report artifact (e.g. `.planning/phases/38-.../gdelt-corpus-audit.json` or markdown) summarizing the run — its thresholds feed plan 06
- a `npm run audit:gdelt` (or similar) package.json script entry — NEW
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GDELT corpus audit script + unit test (GDELT-MATCH-01)</name>
  <files>scripts/audit-gdelt-corpus.ts, server/__tests__/scripts/audit-gdelt-corpus.test.ts, package.json</files>
  <read_first>
    - scripts/audit-events.ts (the CLI analog: `#!/usr/bin/env tsx`, `parseArgs(argv)` with `--fresh`/`-o`/`--sample-rate`, `printSummary(records)`, `cacheGetSafe` read; reuse this exact structure)
    - server/lib/eventAudit.ts (`buildAuditRecord`, `PipelineTrace`, `PhaseA/BChecks`, `ConfidenceSubScores`, `AuditRecord` type — reuse the audit-record scaffolding)
    - server/lib/sourceTiers.ts (`getSourceTier`, `getHighestTier`, tier 1/2/3 domain+name sets — used for the high/neutral/low buckets)
    - server/lib/eventGrouping.ts (`groupGdeltRows(entities)` collapses by CAMEO-root + day-bucket + ≤50km `GROUP_RADIUS_KM` into `EventGroup`s — use to size duplicate-source clusters; `EventGroup` type `:17-27`)
    - server/cache/redis.ts (`cacheGetSafe<T>`) — read `events:llm:v3` (`LLM_EVENTS_KEY_ACTIVE`) and `news:gdelt` (GDELT-DOC clusters, for orphan cross-reference)
    - CLAUDE.md §"Serverless Cache" (key registry: `events:llm:v3`, `news:gdelt`)
    - 38-RESEARCH.md §"GDELT-MATCH-01" + 38-PATTERNS.md §"GDELT-MATCH-01"
  </read_first>
  <action>
    Create `scripts/audit-gdelt-corpus.ts` mirroring `scripts/audit-events.ts`'s CLI shape (`parseArgs`, `printSummary`, tsx shebang). It must:
    (1) read `events:llm:v3` via `cacheGetSafe<ConflictEventEntity[]>` (with a `--fresh` flag and a snapshot/staging fallback per the Environment-Availability note — degrade gracefully if live Redis is unavailable, reading a captured snapshot instead);
    (2) for each event, extract source URLs → `getHighestTier()` → bucket into high-confidence (tier 1), neutral (tier 2), low-confidence (tier 3 / null);
    (3) orphan detection — cross-reference each event against `news:gdelt` clusters by temporal+geo+keyword; flag events with no matching DOC cluster as orphans;
    (4) duplicate-source detection — group events via `eventGrouping.groupGdeltRows` and report clusters of size ≥2 as duplicate-source candidates;
    (5) `printSummary` emits per-bucket counts, the orphan count/list, and duplicate-cluster size histogram, and writes a structured report artifact (`-o` flag) that plan 06 consumes to set its thresholds. Add a `package.json` script entry (e.g. `"audit:gdelt": "tsx scripts/audit-gdelt-corpus.ts"`).
    Write `server/__tests__/scripts/audit-gdelt-corpus.test.ts` FIRST (TDD): feed a small fixture corpus (a handful of `ConflictEventEntity` objects with known tiers, one orphan, one duplicate pair) and assert the bucketing, orphan detection, and duplicate-cluster sizing are correct. Export the pure bucketing/orphan/dedup-candidate functions from the script (or a co-located lib) so the test can import them without invoking Redis I/O.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/scripts/audit-gdelt-corpus.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-gdelt-corpus.ts` exists and `grep -n "getHighestTier" scripts/audit-gdelt-corpus.ts` returns ≥1 (tier bucketing wired)
    - `grep -n "groupGdeltRows" scripts/audit-gdelt-corpus.ts` returns ≥1 (duplicate-cluster sizing wired)
    - `npx vitest run server/__tests__/scripts/audit-gdelt-corpus.test.ts` exits 0 — the fixture corpus buckets, orphan, and duplicate-pair assertions pass
    - `npm run typecheck` exits 0
    - The script runs (`npx tsx scripts/audit-gdelt-corpus.ts --help` or against a snapshot) and emits per-bucket counts + orphan count + dup-cluster histogram without throwing when live Redis is unavailable (degrades to snapshot/empty)
  </acceptance_criteria>
  <done>audit-gdelt-corpus.ts categorizes the corpus into tier buckets + orphans + duplicate-source clusters, the unit test proves the logic on a fixture, the report artifact + package.json script exist, typecheck green. This is the hard gate for plan 06.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                  | Description                                                                               |
| --------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| live Redis (`events:llm:v3`, `news:gdelt`) → audit script | read-only; the audit must never write back to or mutate the corpus (D-07 non-destructive) |
| event source URLs → tier classification                   | URLs are untrusted strings from GDELT; `getHighestTier` already normalizes them           |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-38.03-01 | Tampering              | audit script writing to corpus             | mitigate    | Script is strictly read-only (`cacheGetSafe` reads only); no `cacheSet`/`redis.set` call — grep gate confirms no write |
| T-38.03-02 | Information disclosure | audit report artifact committed to repo    | accept      | Report contains aggregate counts + source domains (already public GDELT data); no secrets — LOW, accepted              |
| T-38.03-SC | Tampering              | no package installs (tsx already a devDep) | accept      | No new dependency; tsx is the existing script runner                                                                   |

</threat_model>

<verification>
- Per task: `npx vitest run server/__tests__/scripts/audit-gdelt-corpus.test.ts` + `npm run typecheck`.
- Plan gate: the audit runs end-to-end and emits a report artifact; `grep` confirms the script performs NO cache writes (D-07 non-destructive).
</verification>

<success_criteria>

- SC38-4 (gate step): a Phase-22-style audit categorizes the live `events:llm:v3` corpus (high/neutral/low by tier, orphans, duplicate clusters) and outputs a report that sizes GDELT-MATCH-02..04. This plan MUST complete before plan 06.
  </success_criteria>

<output>
Create `.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-03-SUMMARY.md` when done. Include the audit's threshold recommendations (dedup confidence cutoff, corroboration gate tuning, composite weights) so plan 06 can consume them directly.
</output>

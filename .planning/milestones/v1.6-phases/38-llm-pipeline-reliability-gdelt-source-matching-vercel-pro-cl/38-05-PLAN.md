---
phase: 38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl
plan: 05
type: execute
wave: 2
depends_on: ['38-02']
files_modified:
  - docs/architecture/deployment.md
  - docs/runbook.md
  - docs/degradation.md
  - docs/architecture/llm-pipeline-reliability.md
  - CLAUDE.md
  - server/__tests__/vercel-entry.test.ts
autonomous: true
requirements: [VERCEL-PRO-01, VERCEL-PRO-02, VERCEL-PRO-03, VERCEL-PRO-04]

must_haves:
  truths:
    - "The vercel.json → vercel.ts migration (PRO-01) and Build Output API (PRO-02) decisions are RECORDED as explicit deferrals with rationale (SC38-6 — 'shipped or explicitly deferred with rationale')"
    - 'Fluid Compute compatibility on the createApp() factory (in server/index.ts, NOT app.ts) is verified and documented; no per-request global mutation hazard under in-instance concurrency (SC38-6)'
    - "Every 'Hobby cap 3', '10s timeout', '60s ceiling' claim across CLAUDE.md / deployment.md / runbook.md / degradation.md / llm-pipeline-reliability.md is repaired to Pro 800s / 40-cron / Fluid-Compute-default semantics (SC38-6)"
    - 'Vercel CLI is bumped 52 → latest (54.9.0) in the dev environment (SC38-6)'
  artifacts:
    - path: 'docs/architecture/deployment.md'
      provides: 'Pro-accurate deployment semantics + recorded PRO-01/02 defer decision + Fluid Compute compat verdict'
      contains: 'Fluid Compute'
  key_links:
    - from: 'docs/* Hobby-era claims'
      to: 'Pro 800s / 40-cron semantics'
      via: 'docs-drift repair'
      pattern: '800'
---

<objective>
VERCEL-PRO strand — reconcile Vercel Pro semantics across code + docs. Ship the safe work (PRO-03 Fluid Compute verification, PRO-04 docs-drift + CLI bump); default-defer the risky migrations (PRO-01 vercel.ts, PRO-02 Build Output API) with RECORDED rationale per D-09.

Purpose: SC38-6 — production code and docs agree on Vercel Pro semantics; the PRO-01/02 migration decisions are shipped-or-explicitly-deferred-with-rationale; Fluid Compute compat on `createApp()` verified; every stale Hobby/timeout claim removed; CLI bumped.

Output: a recorded PRO-01/02 defer decision (the REQ-IDs are covered by an explicit rationale-documented defer — satisfies SC38-6), a Fluid Compute compat verdict in deployment.md + a smoke assertion, docs-drift repair across 5 surfaces, dev-env CLI bump.

Implements: D-09 — ship PRO-03/04, default-defer PRO-01/02 (RESEARCH recommends defer both: vercel.json has NO headers block so vercel.ts is net-zero simplification; Build Output API is a deploy-path risk mid-cleanup). You MUST still produce the task that RECORDS the ship/defer decision + rationale for PRO-01/02.

WAVE: This plan is wave 2 with `depends_on: [38-02]` — it edits CLAUDE.md, which plan 02 (LLM-PURGE-07/08) also edits; serializing after 02 keeps the CLAUDE.md edits conflict-free. The CLI bump is dev-env-only (NOT a package.json change), so this plan does NOT modify package.json.

CRITICAL CORRECTION (re-grepped 2026-06-04): `createApp` lives in **server/index.ts** (NOT server/app.ts — CONTEXT/REQUIREMENTS path is wrong). `vercel.json` has NO `headers` block (crons + rewrites + functions only). Vercel CLI local is 52.0.0, latest 54.9.0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-CONTEXT.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-RESEARCH.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-PATTERNS.md
@vercel.json
</context>

<artifacts_this_phase_produces>
NEW symbols/files this plan creates (source-grounding pass MUST exclude these — they do not exist in current `main`):

- A recorded PRO-01/02 ship/defer decision block (in deployment.md or an ADR appendix) — NEW prose
- A Fluid Compute compatibility verdict section in deployment.md — NEW prose
- Possibly a new assertion in `server/__tests__/vercel-entry.test.ts` (existing file, new test case)
  NO new runtime symbols. PRO-01/02 are DEFERRED (no vercel.ts, no @vercel/config install, no Build Output API).
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record PRO-01/02 defer decision + verify Fluid Compute compat (PRO-01/02/03)</name>
  <files>docs/architecture/deployment.md, server/__tests__/vercel-entry.test.ts</files>
  <read_first>
    - vercel.json (current config: `$schema`, `framework: vite`, `crons` (3), `rewrites` (4), `functions.api/vercel-entry.js` with `maxDuration: 800` + `includeFiles: api/_eval/*.json`; VERIFIED NO `headers` block, NO config-drift handlers)
    - server/index.ts (the `createApp()` factory — CONFIRMED here, NOT server/app.ts)
    - server/vercel-entry.ts (already memoizes `app = createApp()` at module load for warm-invocation request reuse; already documents Fluid-Compute posture: "Vercel handles its own SIGTERM (500ms window) — no custom shutdown handler needed; Upstash Redis is REST-based, so there are no persistent connections to drain")
    - server/__tests__/vercel-entry.test.ts (existing smoke test — extend with a Fluid-Compute compat assertion)
    - 38-RESEARCH.md §"VERCEL-PRO-01/02/03" (DEFER PRO-01: no headers block, no drift handlers, net-zero simplification, deploy-path risk; DEFER PRO-02: Build Output API is a fundamental deploy-path change, risks maxDuration/includeFiles/rewrites mid-cleanup; SHIP PRO-03: likely no-op, Fluid Compute defaults already satisfied — Upstash REST = no connections to drain, app memoized, callHistory/llmProgress singletons are process-scoped with Phase 28.2.7 Redis write-through)
    - 38-PATTERNS.md §"VERCEL-PRO"
  </read_first>
  <action>
    PRO-01 + PRO-02 (record defer, D-09): Add a "Vercel Pro configuration decisions" block to deployment.md recording that BOTH migrations are DEFERRED with rationale — PRO-01 (`vercel.json → vercel.ts` via `@vercel/config`): vercel.json has no `headers` block and no config-drift handlers to delete, so the migration is net-zero simplification while adding a dependency + build step + deploy-path risk mid-cleanup. PRO-02 (Build Output API for `api/vercel-entry.js`): a fundamental change to how the function deploys (`.vercel/output/functions/*.func` + `config.json`) that risks the 800s `maxDuration`, the `includeFiles` eval-fixture copy, and the rewrite map; Phase 999.2 stays open. State both can be revisited in v1.7. This recorded rationale is what satisfies SC38-6's "shipped or explicitly deferred with rationale" for PRO-01/02.
    PRO-03 (ship, verify Fluid Compute compat): Confirm the `createApp()` factory in server/index.ts has no per-request global mutation hazard under Fluid Compute in-instance concurrency (the in-memory `callHistory`/`llmProgress` singletons are intentionally process-scoped and already have Phase 28.2.7 Redis write-through — confirm no per-request writes leak across concurrent requests). Document the compat verdict in deployment.md (request reuse via memoized app ✓, no graceful-shutdown needed because Upstash is REST ✓, no connection pool to drain ✓). Add a smoke assertion to vercel-entry.test.ts confirming the memoized app boots and serves without per-request global leakage (e.g. two sequential requests do not cross-contaminate state).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/vercel-entry.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "defer" docs/architecture/deployment.md` returns ≥1 and the block names BOTH PRO-01 (vercel.ts) and PRO-02 (Build Output API) with rationale
    - `grep -in "Fluid Compute" docs/architecture/deployment.md` returns ≥1 (compat verdict documented)
    - `npx vitest run server/__tests__/vercel-entry.test.ts` exits 0 (the memoized-app smoke + no-leak assertion passes)
    - `test ! -f vercel.ts && ! grep -q "@vercel/config" package.json` (PRO-01 NOT shipped — deferred) and no `.vercel/output` Build Output migration landed (PRO-02 NOT shipped)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>PRO-01/02 defer decisions recorded with rationale in deployment.md (satisfying SC38-6's defer-with-rationale path), Fluid Compute compat on createApp() (server/index.ts) verified + documented + smoke-asserted, no risky migration shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Hobby→Pro docs-drift repair across 5 surfaces + CLI bump (PRO-04)</name>
  <files>CLAUDE.md, docs/architecture/deployment.md, docs/runbook.md, docs/degradation.md, docs/architecture/llm-pipeline-reliability.md</files>
  <read_first>
    - CLAUDE.md (`:101` "Hobby cap 3 entries" on the cron-schedule line — Pro allows 40 cron entries; this plan depends on plan 02 which also edits CLAUDE.md, so the CLAUDE.md state may already have plan 02's PURGE edits applied — re-grep `:101` at edit time)
    - docs/architecture/deployment.md (`:56` "Hobby tier's 60s ceiling"; `:133` "Hobby/Pro tier caps at 3 cron entries")
    - docs/runbook.md (`:539-547` "10-second limit")
    - docs/degradation.md (`:329` "Vercel function 10s timeout")
    - docs/architecture/llm-pipeline-reliability.md (`:6` header inconsistent with `:134` which already declares NIM-only — reconcile to "Pro 800s, NIM-primary")
    - 38-RESEARCH.md §"VERCEL-PRO-04" (platform reality to fold in: Pro maxDuration ceiling 800s in use; default function timeout now 300s in the Fluid Compute era; cron cap 40 not 3; Fluid Compute is the default; vercel.ts is the recommended config but we deferred it — note the eval+defer in docs; Vercel CLI local 52.0.0 → latest 54.9.0)
    - 38-PATTERNS.md §"VERCEL-PRO" PRO-04
  </read_first>
  <action>
    PRO-04: Repair every Hobby-era claim. CLAUDE.md:101 "Hobby cap 3 entries" → reflect Pro 40-cron cap (the project uses 3 cron entries by choice, not a Hobby ceiling — reword to say "3 entries (well under the Pro 40-cron cap)"). deployment.md:56 "Hobby tier's 60s ceiling" → Pro 800s maxDuration (Fluid Compute default function timeout 300s). deployment.md:133 "caps at 3 cron entries" → Pro 40-cron cap. runbook.md:539-547 "10-second limit" → Pro 800s. degradation.md:329 "Vercel function 10s timeout" → Pro 800s (the degradation reasoning still holds; just correct the ceiling). llm-pipeline-reliability.md:6 header → reconcile to "Pro 800s, NIM-primary" matching :134. In deployment.md note that vercel.ts was evaluated and deferred (cross-link the Task 1 decision block). Bump the dev-env Vercel CLI: `npm install -g vercel@54.9.0` (dev machine only — NOT a package.json dependency; this plan does not modify package.json). Record `vercel --version` in the SUMMARY.
  </action>
  <verify>
    <automated>grep -rniE "hobby|10.?second|60s ceiling|3 cron entries" docs/ CLAUDE.md | grep -viE "well under|pro 40|deferred|historical|archive" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "hobby cap 3|hobby tier|60s ceiling|10.?second limit|10s timeout" docs/ CLAUDE.md | grep -viE "pro 40|well under|historical|deferred|archive"` returns 0 lines (no live stale Hobby/timeout claim remains; only explicitly-labeled historical mentions allowed)
    - `grep -n "800" docs/architecture/deployment.md docs/runbook.md docs/degradation.md` returns ≥1 each (Pro 800s ceiling stated)
    - `grep -in "40" CLAUDE.md docs/architecture/deployment.md | grep -i cron` returns ≥1 (Pro 40-cron cap stated)
    - `grep -in "NIM" docs/architecture/llm-pipeline-reliability.md` header region (:1-15) is consistent with :134 (no "NIM → OpenRouter cascade" claim in the header)
    - The SUMMARY records `vercel --version` reflecting 54.x (CLI bumped in dev env)
  </acceptance_criteria>
  <done>All five doc surfaces repaired to Pro 800s / 40-cron / Fluid-Compute-default semantics, llm-pipeline-reliability header reconciled to NIM-primary, vercel.ts eval+defer cross-linked, Vercel CLI bumped 52→54.9.0 in dev (no package.json change).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                               | Description                                                                                                      |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Vercel serverless entry (`createApp()` memoized) → concurrent requests | Fluid Compute runs multiple requests per instance; per-request global mutation could leak state across operators |
| dev-env Vercel CLI bump                                                | dev-tool version, not a production runtime surface                                                               |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                     | Disposition    | Mitigation Plan                                                                                                                                                                                                |
| ---------- | ---------------------- | ------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38.05-01 | Information disclosure | per-request global state leak under Fluid Compute concurrency | mitigate       | PRO-03 verifies the `callHistory`/`llmProgress` singletons are process-scoped with Redis write-through (Phase 28.2.7) and no per-request global write leaks; smoke test asserts no cross-request contamination |
| T-38.05-02 | Tampering              | deploy-path migrations (PRO-01/02)                            | accept (defer) | Deferred with rationale — NOT touching the production deploy path mid-cleanup is the safer disposition; Phase 999.2 stays open                                                                                 |
| T-38.05-SC | Tampering              | global Vercel CLI bump (dev only)                             | accept         | Dev-tool only, not a package.json production dependency; no production supply-chain change                                                                                                                     |

</threat_model>

<verification>
- Per task: `npx vitest run server/__tests__/vercel-entry.test.ts` + `npm run typecheck`; doc-grep gate for PRO-04.
- Plan gate: the PRO-04 grep returns 0 live stale Hobby/timeout claims; the PRO-01/02 defer decision + Fluid Compute verdict are present in deployment.md.
</verification>

<success_criteria>

- SC38-6: code + docs agree on Vercel Pro semantics; PRO-01/02 decisions shipped-or-explicitly-deferred-with-rationale (deferred, recorded); Fluid Compute compat on createApp() verified; every Hobby/10s/60s/3-cron claim removed; Vercel CLI bumped 52 → latest.
  </success_criteria>

<output>
Create `.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-05-SUMMARY.md` when done. Record the PRO-01/02 defer rationale and the `vercel --version` after the bump.
</output>

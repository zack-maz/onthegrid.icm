---
phase: 39-operator-visibility-token-budget-cost-shadow-llm-flight-reco
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/llmTokenBudget.ts
  - server/routes/operator-status.ts
  - server/routes/__tests__/operator-status.test.ts
autonomous: true
requirements: [BUDGET-03, BUDGET-04]
must_haves:
  truths:
    - 'GET /api/operator-status returns a new tokenBudget field (provider-keyed map shape) when Redis is healthy'
    - 'On any Redis throw inside the tokenBudget block, the field is null and the route stays 200 (degrade-open, mirrors actorQuality)'
    - 'The tokenBudget shape is pinned by a Zod .strict() contract test that rejects extra keys (regression-locks the dashboard read shape)'
    - 'Soft/hard ratio constants are exported from llmTokenBudget.ts so the block has no magic-number drift'
  artifacts:
    - path: 'server/routes/operator-status.ts'
      provides: 'tokenBudget degrade-open block + extended res.json'
      contains: 'tokenBudget'
    - path: 'server/routes/__tests__/operator-status.test.ts'
      provides: 'BUDGET-03 degrade-open test + BUDGET-04 Zod .strict() pin'
      contains: '.strict()'
    - path: 'server/lib/llmTokenBudget.ts'
      provides: 'exported SOFT_CAP_RATIO / HARD_CAP_RATIO'
      contains: 'export const SOFT_CAP_RATIO'
  key_links:
    - from: 'server/routes/operator-status.ts tokenBudget block'
      to: 'server/lib/llmTokenBudget.ts getDailyTokens/DAILY_LIMITS/budgetState/todayKey'
      via: 'import + call (no new budget math)'
      pattern: 'budgetState|getDailyTokens|DAILY_LIMITS'
    - from: 'server/routes/operator-status.ts tokenBudget block'
      to: 'events:llm-cost-shadow:v3:{date} HSET'
      via: 'redis.hgetall + microcents/1e6'
      pattern: 'events:llm-cost-shadow:v3'
---

<objective>
Add a Bearer-gated, degrade-open `tokenBudget` field to `GET /api/operator-status` carrying per-provider used/cap/soft/hard/state + today's cost-shadow USD, and pin its shape with a Zod `.strict()` contract test so any drift fails the build.

Purpose: This is the server contract the BudgetBlock UI (Plan 05) renders. It must mirror the `actorQuality` degrade-open precedent exactly so a Redis failure never 500s the operator aggregator and no token/cost data leaks to unauthenticated callers (Bearer gate inherited from the route).

Output: edits to `operator-status.ts` (tokenBudget block + res.json), an extended contract test, and exported ratio constants in `llmTokenBudget.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (excluded from drift verification — intentionally new):

- `TokenBudgetBlock` interface (provider-keyed map, GA-4 shape) — `{ providers: { nvidia_nim: { used, cap, soft, hard, state } }, costShadow: { tokensIn, tokensOut, usd } }`
- `tokenBudget` field on the `/api/operator-status` response
- exported `SOFT_CAP_RATIO` / `HARD_CAP_RATIO` constants in `server/lib/llmTokenBudget.ts`
- a Zod `.strict()` schema in the contract test pinning the tokenBudget shape
  </artifacts_this_phase_produces>

<gray_area_decisions>
**GA-4 (tokenBudget shape) — provider-keyed map** (RESEARCH adopt-the-lean): `{ providers: { nvidia_nim: { used, cap, soft, hard, state } }, costShadow: { tokensIn, tokensOut, usd } }`. Map (not flat) so restoring a provider later adds a key without breaking the `.strict()` pin.

**Open Q1 (BUDGET-01 "used vs cap" source) — PLANNER DECISION: read the dormant `llm:tokens:nvidia_nim:{date}` counter via `getDailyTokens('nvidia_nim')`.** Rationale: (a) it matches the REQUIREMENTS.md literal text ("surfacing `llm:tokens:{provider}:YYYY-MM-DD` per-provider used-vs-cap"); (b) it is extensibility-shaped — a restored provider that calls `incrDailyTokens` populates the same counter with zero contract change; (c) the live signal the operator actually cares about (spend) is `costShadow.usd`, which IS actively written by `accrueShadowCost`. The counter is dormant in the v3 path (Pitfall 2 — `incrDailyTokens` is retired), so `used` will read 0 today; this is HONEST (NIM free-tier isn't metered into that counter) and BudgetBlock renders `0/1000000 (ok)` with cost-shadow USD as the live number. Add a one-line code comment at the block noting the v3-era 0 read so a future reader doesn't think it's broken. Switching `used`/`cap` to the live `rateLimit.nvidia_nim` window later is non-breaking (same map shape) — flag to operator at plan review if a live bar is wanted instead.
</gray_area_decisions>

<source_notes>
**Verified anchors (executor honor these):**

- `todayKey(provider)` in `llmTokenBudget.ts:54` takes a Provider arg and returns the FULL `llm:tokens:{provider}:{date}` key (not a bare date). For the cost-shadow HSET key use the literal template `events:llm-cost-shadow:v3:${<today date>}` — derive the date with the same `new Date().toISOString().slice(0,10)` UTC method the existing accrual uses (read `accrueShadowCost` at freeClaudeRouter.ts:647 for the exact date format).
- `SOFT_CAP_RATIO = 0.8` / `HARD_CAP_RATIO = 0.95` are module-private at `llmTokenBudget.ts:50-51` — export them and recompute `soft = Math.round(cap * SOFT_CAP_RATIO)` / `hard = Math.round(cap * HARD_CAP_RATIO)` in the block.
- The actorQuality degrade-open block is at `operator-status.ts:419-486`; the final `res.json({ audit24h, byBearer, advEval, prune, actorQuality })` is at :486 — extend it to append `, tokenBudget`.
- Contract-test path is `server/routes/__tests__/operator-status.test.ts` (Pitfall 6 — NOT `server/__tests__/routes/...`). The existing mock is `vi.mock('../../cache/redis.js', ...)` with `mockRedis = { smembers, get, scan }` — EXTEND it with `hgetall` (and `get` already exists for getDailyTokens).
  </source_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export ratio constants + add the tokenBudget degrade-open block to operator-status</name>
  <files>server/lib/llmTokenBudget.ts, server/routes/operator-status.ts</files>
  <read_first>
    - server/lib/llmTokenBudget.ts (:32-93 — DAILY_LIMITS, todayKey, getDailyTokens, budgetState, the module-private SOFT_CAP_RATIO/HARD_CAP_RATIO at :50-51; the v3-retirement comment at :24-27)
    - server/routes/operator-status.ts (:380-492 — the actorQuality degrade-open block at :419-486 to copy VERBATIM as the tokenBudget pattern, and the res.json at :486 to extend)
    - server/lib/freeClaudeRouter.ts (:646-668 — accrueShadowCost: the events:llm-cost-shadow:v3:{date} HSET key format + microcents pricing, so the read uses the matching date + Number() coercion)
    - server/cache/redis.ts (:1-42 — redis.hgetall + the withTimeout degrade semantics)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md Pattern 3 (actorQuality mirror), GA-4, Pitfall 2, A4 (Number() coercion)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/routes/operator-status.ts (MOD — add tokenBudget block)"
  </read_first>
  <action>
    In `server/lib/llmTokenBudget.ts`: change `SOFT_CAP_RATIO`/`HARD_CAP_RATIO` at :50-51 to `export const`. In `server/routes/operator-status.ts`: define a `TokenBudgetBlock` interface (GA-4 provider-keyed map shape). Add a `let tokenBudget: TokenBudgetBlock | null = null; try { ... } catch (err) { log.warn(...); /* stays null */ }` block immediately before the final `res.json` at :486, copying the actorQuality try/catch contract VERBATIM (Pattern 3). Inside the try: `const used = await getDailyTokens('nvidia_nim');` (Open Q1 decision — add the one-line "v3-era reads 0; cost-shadow USD is the live signal" comment), `const cap = DAILY_LIMITS.nvidia_nim;`, `const state = budgetState('nvidia_nim', used);`, `const soft = Math.round(cap * SOFT_CAP_RATIO);`, `const hard = Math.round(cap * HARD_CAP_RATIO);`. Read cost-shadow via `redis.hgetall(\`events:llm-cost-shadow:v3:${todayDate}\`)` (todayDate = UTC YYYY-MM-DD matching accrueShadowCost), coercing each field with `Number(x) || 0` (A4) and `usd = usdMicrocents / 1_000_000`. Assemble `tokenBudget = { providers: { nvidia_nim: { used, cap, soft, hard, state } }, costShadow: { tokensIn, tokensOut, usd } }`. Extend the final line to `res.json({ audit24h, byBearer, advEval, prune, actorQuality, tokenBudget })`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p server/tsconfig.json && npx vitest run server/routes/__tests__/operator-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p server/tsconfig.json` exits 0.
    - `npx vitest run server/routes/__tests__/operator-status.test.ts` passes (existing tests still green after adding the block + hgetall mock dependency — Task 2 adds the new assertions).
    - Source assertion: `grep -c "export const SOFT_CAP_RATIO" server/lib/llmTokenBudget.ts` == 1 AND `grep -c "export const HARD_CAP_RATIO" server/lib/llmTokenBudget.ts` == 1.
    - Source assertion: `grep -c "tokenBudget" server/routes/operator-status.ts` >= 2 (block + res.json), and the res.json line includes `tokenBudget`.
    - Source assertion: `grep -c "events:llm-cost-shadow:v3" server/routes/operator-status.ts` >= 1 (cost-shadow read present).
  </acceptance_criteria>
  <done>Ratio constants exported; tokenBudget degrade-open block added mirroring actorQuality; res.json extended; cost-shadow read with Number() coercion + microcents->USD; server typecheck + existing operator-status test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin tokenBudget shape with Zod .strict() + degrade-open contract tests (BUDGET-03/04)</name>
  <files>server/routes/__tests__/operator-status.test.ts</files>
  <read_first>
    - server/routes/__tests__/operator-status.test.ts (:1-70 — the existing structure: vi.mock('../../cache/redis.js') with mockRedis {smembers,get,scan}, makeApp() mounting operatorStatusRouter, the Bearer 401/200 test, the existing shape asserts)
    - server/routes/operator-status.ts (the tokenBudget block + TokenBudgetBlock interface from Task 1)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-VALIDATION.md (BUDGET-03/04 rows + the per-task command)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/routes/__tests__/operator-status.test.ts (MOD ...)"
  </read_first>
  <action>
    Extend `server/routes/__tests__/operator-status.test.ts` (the VERIFIED path — Pitfall 6): add `hgetall` to `mockRedis` (returning a cost-shadow HSET fixture `{ tokensIn: 100, tokensOut: 50, usdMicrocents: 30000 }` and also a case returning null). Add `it('tokenBudget Zod .strict() pin', ...)`: define a Zod schema matching the GA-4 shape with `.strict()` on every object level (providers map entry + costShadow), fetch `/api/operator-status` with a valid Bearer, and assert `tokenBudgetSchema.parse(body.tokenBudget)` succeeds AND that injecting an extra key into a clone makes `.strict()` parse THROW (rejects extra keys — regression-locks the dashboard). Add `it('tokenBudget degrade-open on Redis throw', ...)`: make `mockRedis.hgetall` (or getDailyTokens' `get`) throw, assert the route still returns 200 AND `body.tokenBudget === null` (BUDGET-03). Add a happy-path assertion that `body.tokenBudget.costShadow.usd === 0.03` (30000 microcents / 1e6) to prove the microcents->USD conversion.
  </action>
  <verify>
    <automated>npx vitest run server/routes/__tests__/operator-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/routes/__tests__/operator-status.test.ts` exits 0 with the new cases passing.
    - Behavior assertion: the `.strict()` test proves the schema REJECTS an object with an extra key (a `.parse` throw is asserted) — BUDGET-04 regression lock.
    - Behavior assertion: the degrade-open test asserts HTTP 200 AND `tokenBudget === null` when a Redis op throws — BUDGET-03.
    - Behavior assertion: `costShadow.usd === 0.03` for a 30000-microcent fixture (conversion correctness).
    - Source assertion: `grep -c ".strict()" server/routes/__tests__/operator-status.test.ts` >= 1.
  </acceptance_criteria>
  <done>Contract test pins the GA-4 tokenBudget shape with Zod .strict() (rejects extra keys), proves degrade-open null on Redis throw with 200, and verifies microcents->USD conversion; test green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                     |
| --------------------------------------------- | ------------------------------------------------------------------------------- |
| unauthenticated caller ↔ /api/operator-status | Bearer gate (dashboardAuth) is the access-control surface for token + cost data |
| Redis ↔ tokenBudget block                     | cost-shadow + token counter reads, degrade-open                                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                                           | Disposition | Mitigation Plan                                                                                                                                                                         |
| --------- | ---------------------- | --------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-03-I | Information Disclosure | token spend + cost USD exposed to anonymous callers | mitigate    | `/api/operator-status` is already Bearer-gated (dashboardAuth, timingSafeEqual constant-time, prod-empty fail-closed); tokenBudget rides the same gate. No new unauthenticated surface. |
| T-39-03-D | Denial of Service      | Redis throw 500-ing the whole aggregator            | mitigate    | tokenBudget computed in its own try/catch -> null on failure; route stays 200 (mirrors actorQuality). Tested in Task 2.                                                                 |
| T-39-03-T | Tampering              | dashboard reads drifting from server shape          | mitigate    | Zod `.strict()` contract test fails the build on any extra/missing key (BUDGET-04).                                                                                                     |
| T-39-SC   | Tampering              | npm/pip/cargo installs                              | mitigate    | No new packages (RESEARCH §Package Legitimacy Audit: N/A).                                                                                                                              |

</threat_model>

<verification>
- `npx vitest run server/routes/__tests__/operator-status.test.ts` green (Bearer + .strict() + degrade-open + conversion)
- `npx tsc --noEmit -p server/tsconfig.json` exits 0
- tokenBudget degrade-open verified: Redis throw -> null, route 200
</verification>

<success_criteria>

- /api/operator-status returns the GA-4 provider-keyed tokenBudget map when Redis healthy, null when it throws (route 200)
- Zod .strict() contract test rejects extra keys (dashboard regression lock)
- Soft/hard ratios exported (no magic-number drift); cost-shadow microcents->USD correct
  </success_criteria>

<output>
Create `.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-03-SUMMARY.md` when done
</output>

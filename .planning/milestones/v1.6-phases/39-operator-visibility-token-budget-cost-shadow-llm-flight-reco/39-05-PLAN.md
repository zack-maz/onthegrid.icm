---
phase: 39-operator-visibility-token-budget-cost-shadow-llm-flight-reco
plan: 05
type: execute
wave: 4
depends_on: ['39-03', '39-04']
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/components/ui/BudgetBlock.tsx
  - src/components/ui/FlightRecorderBlock.tsx
  - src/components/ui/__tests__/BudgetBlock.test.tsx
autonomous: true
requirements: [BUDGET-01, BUDGET-02, OBS-FLIGHT-04]
must_haves:
  truths:
    - "BudgetBlock renders a per-provider used/cap proximity bar (green/yellow/red by band) with soft+hard ticks and today's cost-shadow USD"
    - 'BudgetBlock hides entirely when opStatus.tokenBudget is null (degrade-open) — no error chrome'
    - 'FlightRecorderBlock fetches the Bearer-gated /api/events/llm-history and renders a newest-first run list with outcome badge, batch bar, token spend, eval score'
    - 'FlightRecorderBlock drills run -> calls (filtered by runId) -> single-call prompt/response; hides on non-200 (degrade-open)'
    - 'Both blocks mount inside DevApiStatusAllApisTab using ONLY accent + white/N tokens (no entity colors) and the host font-mono micro-scale'
  artifacts:
    - path: 'src/components/ui/BudgetBlock.tsx'
      provides: 'Token-budget proximity bars + cost-shadow USD render (BUDGET-01/02)'
      contains: 'TOKEN BUDGET'
    - path: 'src/components/ui/FlightRecorderBlock.tsx'
      provides: '3-level run-list -> call-list -> prompt/response drill-down (OBS-FLIGHT-04)'
      contains: 'FLIGHT RECORDER'
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'OperatorStatus.tokenBudget type + mount of both new blocks in DevApiStatusAllApisTab'
      contains: 'tokenBudget'
    - path: 'src/components/ui/__tests__/BudgetBlock.test.tsx'
      provides: 'BUDGET-01/02 render + null-gate coverage'
  key_links:
    - from: 'src/components/ui/BudgetBlock.tsx'
      to: 'opStatus.tokenBudget (operator-status field, Plan 03)'
      via: 'prop from DevApiStatusAllApisTab'
      pattern: 'tokenBudget'
    - from: 'src/components/ui/FlightRecorderBlock.tsx'
      to: 'GET /api/events/llm-history (Plan 04)'
      via: 'fetch with dashboardAuthHeaders()'
      pattern: '/api/events/llm-history'
---

<objective>
Build the two operator-facing render blocks per the locked UI-SPEC: `BudgetBlock` (per-provider proximity bars + today's cost-shadow USD, sourced from the already-polled `tokenBudget` field) and a FUNCTIONAL `FlightRecorderBlock` (run-list -> call-list -> single-call prompt/response drill-down, fetched from the Bearer-gated `/llm-history`). Mount both inside `DevApiStatusAllApisTab`.

Purpose: This is the at-a-glance operator surface SC39-1 and SC39-3 require. Per GA-1 the scope is a functional-but-clean baseline conforming to the existing 3,100-line console host — rich filters, polished prompt-copy niceties, and tab/subtab placement DEFER to Phase 40.

Output: two new components, the `OperatorStatus.tokenBudget` type + mount edits in `DevApiStatus.tsx`, and a BudgetBlock render/null-gate test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-UI-SPEC.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-03-SUMMARY.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (excluded from drift verification — intentionally new):

- `BudgetBlock` React component (`src/components/ui/BudgetBlock.tsx`)
- `FlightRecorderBlock` React component (`src/components/ui/FlightRecorderBlock.tsx`)
- `tokenBudget` field on the local `OperatorStatus` interface + a matching `TokenBudgetBlock` client type in `DevApiStatus.tsx` (mirrors the Plan-03 server shape)
- client-side `RunHistoryEntry` / `CallHistoryEntry` render types (import the server shapes or mirror them) used by FlightRecorderBlock
- test file `src/components/ui/__tests__/BudgetBlock.test.tsx`
  </artifacts_this_phase_produces>

<scope_notes>
**GA-1 deferrals (record in SUMMARY so verify-phase does NOT flag these as gaps):**

- Rich FlightRecorder filters (outcome dropdown, date-range) — DEFER to Phase 40.
- Polished prompt-copy syntax niceties — Phase 39 baseline only requires the operator CAN read a single call's prompt+response (reuse the existing copyable modal idiom; minimal is acceptable per UI-SPEC §B Level 3).
- Final tab/subtab placement + consolidation — DEFER to Phase 40 (UI-POLISH owns the reorg).

**GA-3 deferral (record in SUMMARY):** the 90d cost sparkline is DEFERRED. BudgetBlock ships TODAY's USD + soft/hard proximity context ONLY. Do NOT fan out 90 Redis GETs — the block reads only the single `tokenBudget.costShadow` already in the polled payload (no new fetch for cost).

**Color discipline (CLAUDE.md + UI-SPEC §Color — HARD):** use ONLY `--color-accent-{blue,red,green,yellow}` (as `text-accent-*` / `bg-accent-*/N`) + the `white/N` ramp. NO entity `--color-*` tokens. No inline hex/RGBA.
</scope_notes>

<source_notes>
**Verified host anchors (executor honor these):**

- `OperatorStatus` interface is declared LOCALLY inside `DevApiStatusAllApisTab` at `src/components/ui/DevApiStatus.tsx:895`. Add the `tokenBudget?: TokenBudgetBlock | null` field there (mirror the Plan-03 server shape).
- Reusable host idioms: `ProgressBar` (:100-121, h-1 rounded-full track bg-white/10, tabular-nums pct), `STATUS_PILL_CLASSES` (:810), `TIER_BORDER_CLASSES` (:817), `formatAge` (:45), `freshnessText` (:856), the actorQuality render gate `opStatus?.actorQuality != null` (:1681), `fetchOpStatus` poll (:938), `dashboardAuthHeaders()` (imported :8).
- BudgetBlock mounts as `<div className="mt-2 border-t border-white/10 pt-2">` alongside actorQuality/prune (~:1781); it takes the `tokenBudget` value as a prop (no new fetch — reuses the 30s fetchOpStatus poll).
- FlightRecorderBlock mounts as a new top-level `mt-4 px-3 py-2` section; it does its OWN Bearer fetch of `/api/events/llm-history` with `dashboardAuthHeaders()` on tab-open + poll cadence, degrade-open (non-200 -> block hides, try/catch swallow like fetchOpStatus :958).
  </source_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BudgetBlock component + OperatorStatus.tokenBudget type + mount + render/null-gate test</name>
  <files>src/components/ui/BudgetBlock.tsx, src/components/ui/DevApiStatus.tsx, src/components/ui/__tests__/BudgetBlock.test.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (:8 dashboardAuthHeaders import, :100-121 ProgressBar, :810-817 STATUS_PILL_CLASSES/TIER_BORDER_CLASSES, :895 local OperatorStatus interface, :1676-1730 the actorQuality render gate + mount region, :1781 the sub-block insertion point)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-UI-SPEC.md §A BudgetBlock (information hierarchy, proximity bar with soft 80% + hard 95% ticks, cost row 4dp, empty/degraded states, copy contract) + §Color (semantic band table) + §Typography + §Spacing
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"BudgetBlock.tsx (NEW — component)"
    - src/components/ui/__tests__/ (any existing component test for the jsdom render bootstrap + RTL idiom)
  </read_first>
  <action>
    Create `src/components/ui/BudgetBlock.tsx` exporting a `BudgetBlock` component that takes `tokenBudget: TokenBudgetBlock | null` as a prop. Render gate: return `null` when `tokenBudget == null` (degrade-open — mirror `opStatus?.actorQuality != null`). When present: a `<div className="mt-2 border-t border-white/10 pt-2">` section with header `TOKEN BUDGET` (`text-[9px] font-bold uppercase tracking-wider text-white/40`); one proximity-bar row per `providers` map entry (today only `nvidia_nim`, label `NIM`) reusing `ProgressBar` with fill width `used/cap`, fill color from the semantic band (green when `state==='ok'`, yellow when `'soft'`, red when `'hard'`), plus TWO static 1px `bg-white/30` vertical ticks overlaid at 80% and 95%; right-aligned `text-[8px] text-white/40 tabular-nums` showing `{used}/{cap}` + the `state` word. Cost-shadow row: `Cost today: $X.XXXX` (4dp, `text-[10px] font-mono tabular-nums`) with `{tokensIn} in · {tokensOut} out` as a `text-[8px] text-white/40` caption (GA-3 — today only, no sparkline, no new fetch). Empty `providers` -> header + `No provider budget data yet` (`text-white/30`). Use ONLY accent + white/N tokens. In `DevApiStatus.tsx`: add `tokenBudget?: TokenBudgetBlock | null` to the local `OperatorStatus` interface at :895 (define a matching `TokenBudgetBlock` type — mirror the Plan-03 server shape exactly), import `BudgetBlock`, and mount it with the polled value in the sub-block region (~:1781).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/BudgetBlock.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/__tests__/BudgetBlock.test.tsx` exits 0 with all cases passing.
    - Behavior assertion: with `tokenBudget === null` the block renders nothing (RTL `queryByText('TOKEN BUDGET')` is null) — degrade-open gate (BUDGET-01/02).
    - Behavior assertion: a `usd: 0.03` fixture renders `Cost today: $0.0300` (4dp conversion display).
    - Behavior assertion: `state: 'hard'` applies an accent-red band class; `state: 'soft'` applies accent-yellow.
    - Source assertion: `grep -c "tokenBudget" src/components/ui/DevApiStatus.tsx` >= 2 (type field + mount); `grep -cE "color-(flight|ship|event|site|faction|ethnic)" src/components/ui/BudgetBlock.tsx` returns 0 (no entity tokens).
  </acceptance_criteria>
  <done>BudgetBlock renders band-colored proximity bars with soft/hard ticks + today's cost USD (4dp), hides on null, accent-only palette; OperatorStatus.tokenBudget typed + block mounted; test green with 4 cases.</done>
</task>

<task type="auto">
  <name>Task 2: FlightRecorderBlock — 3-level run-list -> call-list -> prompt/response drill-down (functional baseline)</name>
  <files>src/components/ui/FlightRecorderBlock.tsx, src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (:8 dashboardAuthHeaders, :100 ProgressBar, :810 STATUS_PILL_CLASSES, :45 formatAge, :856 freshnessText, :938-961 fetchOpStatus fetch+degrade-open idiom, :882 the expandedRow/selectedRow local-state pattern, the existing copyable modal idiom referenced by UI-SPEC §B Level 3)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-UI-SPEC.md §B FlightRecorderBlock (3-level hierarchy, run-row contents, outcome badge semantics, batch bar, token/eval, Level 2 call rows + provenance/DLQ/watchdog one-line summaries, Level 3 prompt/response, all empty/loading/degraded states, copy contract) + §Color outcome-semantics table
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"FlightRecorderBlock.tsx (NEW — component, drill-down)"
    - server/lib/llmProgress.ts (the RunHistoryEntry + CallHistoryEntry shapes from Plan 01 — import or mirror as client render types)
  </read_first>
  <action>
    Create `src/components/ui/FlightRecorderBlock.tsx` exporting a `FlightRecorderBlock` component. State: local `runs`/`calls` from a Bearer fetch of `/api/events/llm-history` using `fetch(url, { headers: { ...dashboardAuthHeaders() } })`; on `!res.ok` or throw set nothing so the block hides (degrade-open like fetchOpStatus :958); refresh on mount + the host poll cadence. Local drill-down state `selectedRunId` and `selectedCall` (transient React state, mirrors :882 expandedRow). Mount as a new top-level `<div className="mt-4 px-3 py-2">` section with header `FLIGHT RECORDER`. Level 1 RUNS (newest-first): each run is a clickable row (`cursor-pointer hover:bg-white/5`) with an outcome badge using the `STATUS_PILL_CLASSES` idiom colored per the UI-SPEC outcome table (running=blue, success/completed=green, partial=yellow, timeout/error=red), runId + relative start time (`formatAge`/`freshnessText`), a batch `ProgressBar` (`batchesCompleted/batchCount`, blue while running / green when completed), token spend (`{tokenSpend.nvidia_nim} tok`), and eval score (colored green >=0.95 / yellow >=0.80 / red below). Clicking sets `selectedRunId` and reveals Level 2 inline (NOT a new modal — host expand-row pattern). Level 2 CALLS (filter the fetched `calls` by `runId`, or refetch with `?runId=`): one row per call (batchIndex, a per-call timing bar `h-1.5`, provenance/provider tag, ok/DLQ/timeout status glyph colored by the semantic ramp) plus one-line aggregate sub-strips (`Provenance: {path} ×{n}`, `DLQ: {n} groups` yellow if >0, `Timeouts: {n}` red if >0 — NOT charts). Clicking a call reveals Level 3: the prompt+response for that call reusing the existing copyable code-panel modal idiom (`font-mono text-[10px]`, the `Copied!` affordance, `bg-black/85` backdrop) — MINIMAL is acceptable per GA-1 (baseline: operator CAN read prompt+response). Empty/loading/degraded states per the UI-SPEC table (`No runs recorded yet`, `No calls recorded for this run`, skeleton `h-6 animate-pulse rounded bg-white/5`, never-closed `running` row -> blue badge + indeterminate batch bar). Mount `<FlightRecorderBlock />` inside `DevApiStatusAllApisTab`. Accent + white/N tokens ONLY.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (FlightRecorderBlock + DevApiStatus mount are strict-clean).
    - `npx vitest run src/` passes (no regression in existing src tests after adding the block + mount).
    - Source assertion: `grep -c "FLIGHT RECORDER" src/components/ui/FlightRecorderBlock.tsx` >= 1; `grep -c "/api/events/llm-history" src/components/ui/FlightRecorderBlock.tsx` >= 1; `grep -c "dashboardAuthHeaders" src/components/ui/FlightRecorderBlock.tsx` >= 1.
    - Source assertion: `grep -cE "color-(flight|ship|event|site|faction|ethnic)" src/components/ui/FlightRecorderBlock.tsx` returns 0 (accent + white/N only); `grep -c "FlightRecorderBlock" src/components/ui/DevApiStatus.tsx` >= 1 (mounted).
    - Behavior (manual UAT — VALIDATION.md §"Manual-Only Verifications"): click a run -> see calls filtered by runId; click a call -> read prompt+response.
  </acceptance_criteria>
  <done>FlightRecorderBlock renders newest-first run list (outcome badge + batch bar + token + eval), drills run -> call (runId filter) -> prompt/response via the copyable modal, degrade-open hides on non-200, never-closed running row shown distinctly, accent-only palette; mounted in DevApiStatusAllApisTab; src typecheck + suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                          |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| browser SPA ↔ /api/events/llm-history | FlightRecorderBlock sends the Bearer header via dashboardAuthHeaders(); server gate is authoritative |
| browser SPA ↔ /api/operator-status    | BudgetBlock renders the already-polled, Bearer-gated tokenBudget field (no new fetch)                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                    |
| --------- | ---------------------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-39-05-I | Information Disclosure | prompt/response + cost rendered client-side | accept      | The data only reaches the client AFTER the server Bearer gate (Plan 03/04) authorizes it; the client cannot bypass the gate. Both blocks degrade-open (hide) when the server returns non-200/null — no stale leak. |
| T-39-05-T | Tampering              | dashboard render drifting from server shape | mitigate    | BudgetBlock consumes the Zod-`.strict()`-pinned tokenBudget (Plan 03 BUDGET-04); FlightRecorderBlock types mirror the Plan-01 RunHistoryEntry/CallHistoryEntry shapes.                                             |
| T-39-05-D | Denial of Service      | render thrash / unbounded fetch             | mitigate    | Reuses the host poll cadence (no new high-frequency loop); /llm-history limit is server-clamped (Plan 04).                                                                                                         |
| T-39-SC   | Tampering              | npm/pip/cargo installs                      | mitigate    | No new packages (RESEARCH §Package Legitimacy Audit: N/A).                                                                                                                                                         |

</threat_model>

<verification>
- `npx vitest run src/components/ui/__tests__/BudgetBlock.test.tsx` green (render + null-gate + band + 4dp USD)
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `npx vitest run src/` green (no regression)
- Manual UAT (VALIDATION.md): run -> calls -> prompt/response drill-down works against a live/forced run
</verification>

<success_criteria>

- BudgetBlock surfaces per-provider used/cap (band-colored + soft/hard ticks) + today's cost USD; hides on null
- FlightRecorderBlock surfaces a newest-first run list and drills run -> calls -> prompt/response; hides on non-200
- Both blocks mounted in DevApiStatusAllApisTab using accent + white/N tokens only (no entity colors)
- GA-1 (rich filters / tab placement) + GA-3 (90d sparkline) deferrals recorded in the SUMMARY
  </success_criteria>

<output>
Create `.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-05-SUMMARY.md` when done
</output>

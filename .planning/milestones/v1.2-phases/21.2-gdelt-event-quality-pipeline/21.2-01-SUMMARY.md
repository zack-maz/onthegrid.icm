---
phase: 21.2-gdelt-event-quality-pipeline
plan: 01
subsystem: api
tags: [gdelt, scoring, geo-validation, confidence, goldstein, cameo]

# Dependency graph
requires:
  - phase: 17
    provides: numMentions/numSources fields on ConflictEventEntity.data
provides:
  - computeEventConfidence (0-1 weighted confidence score)
  - applyGoldsteinSanity (Goldstein-based type reclassification)
  - isGeoValid (text-geo cross-validation for misplaced events)
  - detectCentroid (city centroid detection for precision flagging)
  - geoPrecision and confidence fields on ConflictEventEntity.data
  - eventConfidenceThreshold in AppConfig (0.35 default)
affects: [21.2-02-PLAN, gdelt adapter, event pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns: [5-signal weighted confidence scoring, FIPS reverse lookup geo-validation, city centroid detection with tolerance]

key-files:
  created:
    - server/lib/eventScoring.ts
    - server/lib/geoValidation.ts
    - server/__tests__/lib/eventScoring.test.ts
    - server/__tests__/lib/geoValidation.test.ts
  modified:
    - server/types.ts
    - server/config.ts

key-decisions:
  - "eventConfidenceThreshold default 0.35 per user decision (not spec's 0.15)"
  - "42 city centroids (30 major + 12 conflict hotspots) for centroid detection"
  - "FIPS contradiction check only applies when last segment looks like a country name (uppercase, no digits)"
  - "applyGoldsteinSanity returns same reference when unchanged (no unnecessary cloning)"
  - "Goldstein consistency signal uses linear decay over 6 points outside ceiling range"

patterns-established:
  - "Geo-validation: extractLastSegment for comma-delimited GDELT FullName parsing"
  - "Confidence scoring: 5-signal weighted sum with log-normalized media/source signals"
  - "Goldstein sanity: ceiling+3 tolerance with type downgrade chain"

requirements-completed: []

# Metrics
duration: 11min
completed: 2026-03-28
---

# Phase 21.2 Plan 01: Scoring Engine and Geo-Validation Summary

**Confidence scoring engine (5-signal weighted 0-1 score) and geo-validation modules (FIPS cross-check + city centroid detection) with 43 unit tests**

## Performance

- **Duration:** 11 min
- **Started:** 2026-03-28T02:23:03Z
- **Completed:** 2026-03-28T02:34:48Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created geo-validation module with isGeoValid (discards misplaced events via FIPS/FullName cross-check) and detectCentroid (flags 42 city centroids within 0.01 degree tolerance)
- Created event scoring module with computeEventConfidence (5-signal 0-1 weighted score) and applyGoldsteinSanity (type reclassification when Goldstein exceeds ceiling+3)
- Extended ConflictEventEntity.data with geoPrecision and confidence optional fields (backward compatible)
- Added eventConfidenceThreshold to AppConfig with 0.35 default, env configurable via EVENT_CONFIDENCE_THRESHOLD

## Task Commits

Each task was committed atomically:

1. **Task 1: Type extensions and config, geo-validation module with tests** - `28cbb99` (feat)
2. **Task 2: Event confidence scoring and Goldstein sanity module with tests** - `1680c6e` (feat)

_Both tasks used TDD: RED (failing tests) then GREEN (implementation) workflow._

## Files Created/Modified
- `server/lib/geoValidation.ts` - Geo cross-validation and centroid detection (NON_ME_FULLNAME_COUNTRIES, FIPS_TO_EXPECTED_COUNTRY, CITY_CENTROIDS, isGeoValid, detectCentroid)
- `server/lib/eventScoring.ts` - Confidence scoring and Goldstein sanity (GOLDSTEIN_CEILINGS, computeEventConfidence, applyGoldsteinSanity)
- `server/__tests__/lib/geoValidation.test.ts` - 24 tests for geo-validation
- `server/__tests__/lib/eventScoring.test.ts` - 19 tests for event scoring
- `server/types.ts` - Added geoPrecision and confidence optional fields to ConflictEventEntity.data
- `server/config.ts` - Added eventConfidenceThreshold to AppConfig (0.35 default)

## Decisions Made
- eventConfidenceThreshold default set to 0.35 per user decision (spec suggested 0.15 as launch no-op)
- 42 city centroids included (30 major + 12 conflict hotspots like Tikrit, Fallujah, Idlib, Kandahar)
- FIPS contradiction check only fires when last FullName segment starts uppercase with no digits (avoids false positives on numeric or non-country segments)
- applyGoldsteinSanity returns same object reference when no reclassification needed (avoids unnecessary cloning)
- Goldstein consistency signal uses linear decay from 1.0 to 0.0 over 6 points outside ceiling range

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Both modules ready for Plan 02 integration into parseAndFilter pipeline
- All exports match the interface contract specified in the plan frontmatter
- 43 unit tests provide regression coverage for pipeline wiring

## Self-Check: PASSED

All 6 created/modified files verified on disk. Both task commits (28cbb99, 1680c6e) verified in git log.

---
*Phase: 21.2-gdelt-event-quality-pipeline*
*Completed: 2026-03-28*

---
phase: 21.3-multi-user-load-testing
verified: 2026-03-29T22:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 5/7
  gaps_closed:
    - "k6 load test executes against https://irt-monitoring.vercel.app (production), not localhost"
    - "Playwright runs 3 concurrent browser workers alongside k6, not 1 sequential worker"
  gaps_remaining: []
  regressions: []
---

# Phase 21.3: Multi-User Load Testing — Verification Report

**Phase Goal:** Validate that the production deployment handles multiple concurrent users without degradation — test rate limiting, Redis connection pooling, Vercel cold starts, and client-side polling under real multi-user load
**Verified:** 2026-03-29T22:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (Plan 03)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | k6 load test script simulates 100 VUs with realistic polling patterns matching the app's polling hooks | VERIFIED | `scripts/load-test.js` (297 lines): 6 scenarios (flights_polling 5s, ships_polling 30s, markets_polling 60s, slow_polling, static_fetch, health_monitor), ramping-vus executor 0->100 ramp, custom Counter/Trend metrics |
| 2 | Playwright browser test verifies React app stays functional under concurrent load | VERIFIED | `scripts/load-test.spec.ts` line 26: `test.describe.configure({ mode: 'parallel' })` added in Plan 03. `playwright-output.txt` line 2: "Running 3 tests using 3 workers". All 3 tests passed (844ms load, 11.2s status health, 2.7m stability with 6/6 checks). |
| 3 | k6 thresholds enforce p95 < 500ms and < 5% non-429 error rate | VERIFIED | `k6-output.txt` line 2508: `p(95)=153.62ms` passes `p(95)<500`. Per-endpoint: flights `p(95)=136.54ms` passes `p(95)<300`, ships `p(95)=145.31ms` passes `p(95)<6000`. Two documented-expected threshold crossings: health p95 (Redis roundtrip probe by design) and http_req_failed (rate limiter throttling 501 VU single-IP load). |
| 4 | Production handles 100 concurrent VUs without 5xx errors | VERIFIED | k6-output.txt line 21: "Load test targeting: https://irt-monitoring.vercel.app". All 9536 application checks passed (100%). `rate_limited: 831` were 429 responses (rate limiter). No 500/502/503 errors found in output. |
| 5 | Redis stays healthy (not degraded) throughout the load test | VERIFIED | k6-output.txt line 23: "Pre-test health: status=ok, redis=true, latencyMs=63". Line 2490: "Post-test health: status=ok, redis=true, latencyMs=68". Redis did not degrade under 501 VU load (5ms latency delta). |
| 6 | Edge cache hit ratio demonstrates scaling without multiplying Redis/serverless costs | VERIFIED | k6-output.txt line 2541: `cache_hits: 4265` (13.8/s). Line 2542: `cache_misses: 1334` (4.3/s). Hit ratio: 76.2%. Flights p95 136ms confirms cache-served responses dominating. Cold start avg 102ms documented in 03-SUMMARY. |
| 7 | Performance bottlenecks are identified and documented | VERIFIED | 02-SUMMARY documents: AISStream bimodal latency (18ms HIT vs 5s+ cold-cache WebSocket), GDELT DOC API intermittent timeouts (7/100 failures), health endpoint Redis roundtrip latency pattern. 03-SUMMARY confirms same findings hold in production execution. |

**Score: 7/7 truths verified**

---

## Required Artifacts

| Artifact | Expected | Actual Lines | Status | Details |
|----------|----------|--------------|--------|---------|
| `scripts/load-test.js` | k6 script, min 100 lines, production BASE_URL | 297 | VERIFIED | Line 18: `BASE_URL = __ENV.BASE_URL \|\| 'https://irt-monitoring.vercel.app'`; 6 scenarios, thresholds, custom metrics |
| `scripts/load-test.spec.ts` | Playwright spec, min 30 lines, parallel mode | 139 | VERIFIED | Line 26: `test.describe.configure({ mode: 'parallel' })`; 3 test cases, PROD_URL defaults to production |
| `load-test-results/k6-output.txt` | k6 results from production, contains production URL | ~2575 | VERIFIED | Line 21: "Load test targeting: https://irt-monitoring.vercel.app" |
| `load-test-results/playwright-output.txt` | Playwright results, contains "3 workers" | 15 | VERIFIED | Line 2: "Running 3 tests using 3 workers"; all 3 passed |
| `.planning/phases/21.3-multi-user-load-testing/21.3-02-SUMMARY.md` | Load test analysis, min 20 lines | 172 | VERIFIED | Per-endpoint latency table, Redis health comparison, bottleneck findings |
| `.planning/phases/21.3-multi-user-load-testing/21.3-03-SUMMARY.md` | Gap closure results, min 20 lines | 128 | VERIFIED | Production execution results, decisions on expected threshold failures |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `scripts/load-test.js` | `https://irt-monitoring.vercel.app/api/*` | k6 http.get() with default BASE_URL | WIRED | Line 18 default confirmed; k6-output.txt line 21: "Load test targeting: https://irt-monitoring.vercel.app"; 5601 requests made to production |
| `scripts/load-test.spec.ts` | `https://irt-monitoring.vercel.app` | Playwright page.goto() with 3 parallel workers | WIRED | Line 16 PROD_URL default confirmed; playwright-output.txt line 2: "3 workers"; all 3 tests passed |

---

## Re-verification: Gap Closure Evidence

### Gap 1 (CLOSED): k6 targeted localhost instead of production

**Previous finding:** k6-output.txt showed "Load test targeting: http://localhost:3001"

**Closure evidence:**
- `load-test-results/k6-output.txt` line 21: "Load test targeting: https://irt-monitoring.vercel.app"
- `scripts/load-test.js` line 18: `const BASE_URL = __ENV.BASE_URL || 'https://irt-monitoring.vercel.app'` — no override passed
- 5601 HTTP requests reached production; 9536 application checks passed at 100%; Redis confirmed stable pre/post

**Status: CLOSED**

### Gap 2 (CLOSED): Playwright ran with 1 sequential worker

**Previous finding:** playwright-output.txt showed "Running 3 tests using 1 worker"

**Closure evidence:**
- `scripts/load-test.spec.ts` line 26: `test.describe.configure({ mode: 'parallel' });` added in commit d611afb (Plan 03 Task 1)
- `load-test-results/playwright-output.txt` line 2: "Running 3 tests using 3 workers"
- All 3 tests passed: app-loaded (844ms), status-healthy (11.2s), stability 3-minute window (6/6 checks)

**Status: CLOSED**

---

## Requirements Coverage

No formal requirement IDs assigned to this phase (testing/validation phase). The `requirements` field is empty in all three plans' frontmatter. No entries in REQUIREMENTS.md map to phase 21.3. No orphaned requirements.

---

## Anti-Patterns Found

No blockers or warnings found in the test scripts or output files.

One informational observation:

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `load-test-results/k6-output.txt` | Two thresholds crossed (health p95 at 781ms vs 500ms; http_req_failed at 14.83% vs 1%) | INFO | Both documented as expected behavior in 03-SUMMARY key-decisions. Health endpoint performs live Redis roundtrip probe making 500ms threshold too tight by design. 14.83% failure rate is rate limiter correctly protecting server under 501 VU single-IP load. No threshold values were changed to mask these — they are accepted and documented. |

---

## Human Verification Required

None. All automated checks passed against actual production output files. Both gaps confirmed closed by file content inspection.

---

## Summary

Phase 21.3 achieved its goal. Production at https://irt-monitoring.vercel.app was validated under 501 peak concurrent VUs with the following confirmed outcomes:

- Zero 5xx errors; all 9536 application-level checks passed (100%)
- Data endpoint p95 latencies: flights 136ms, ships 145ms, overall 153ms — all well under thresholds
- Redis remained healthy throughout (ok/true, 63ms -> 68ms latency delta, no degradation)
- Edge cache hit ratio of 76.2% (4265 hits / 5599 total) confirms Vercel CDN absorbing repeated polling
- Rate limiter correctly throttled 831 requests (14.83%) under extreme single-IP 501 VU load
- Three concurrent Playwright browser workers confirmed app loads, map renders, status indicators show healthy state, and app remains stable over a 3-minute window during load
- Two k6 threshold crossings (health p95, http_req_failed rate) are intentional known behaviors, documented in 03-SUMMARY key-decisions

Both gaps from the initial verification — localhost targeting and single-worker Playwright — were fully closed by Plan 03 execution.

---

_Verified: 2026-03-29T22:00:00Z_
_Verifier: Claude (gsd-verifier)_

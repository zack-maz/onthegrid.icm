---
phase: 21-production-review-deploy-sync
plan: 05
type: execute
wave: 3
depends_on: ["21-04"]
files_modified:
  - scripts/smoke-test.ts
  - vercel.json
  - server/routes/cron-health.ts
  - server/index.ts
  - README.md
  - CHANGELOG.md
autonomous: false

requirements: []

must_haves:
  truths:
    - "Smoke test script validates all production API endpoints return 200 with valid JSON"
    - "All env vars referenced in server/config.ts are present in Vercel project settings"
    - "README and CHANGELOG are updated to reflect v1.2 features"
    - "Production deployment is live and verified"
  artifacts:
    - path: "scripts/smoke-test.ts"
      provides: "Production endpoint smoke test"
    - path: "README.md"
      provides: "Updated project documentation"
    - path: "CHANGELOG.md"
      provides: "Updated changelog with v1.2 entries"
  key_links:
    - from: "scripts/smoke-test.ts"
      to: "production URL"
      via: "fetch against each /api/* endpoint"
      pattern: "fetch.*api/"
    - from: "vercel.json"
      to: "server/routes/cron-health.ts"
      via: "crons config"
      pattern: "crons"
---

<objective>
Deploy to production with full verification and documentation sync.

Purpose: Create smoke test script, audit env vars against Vercel settings, update all documentation, deploy v1.2 to Vercel, run smoke tests against production, and visually verify the deployed application.
Output: Production deployment live at Vercel URL, all docs current, smoke tests passing.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-production-review-deploy-sync/21-CONTEXT.md
@.planning/phases/21-production-review-deploy-sync/21-RESEARCH.md
@.planning/phases/21-production-review-deploy-sync/21-01-SUMMARY.md
@.planning/phases/21-production-review-deploy-sync/21-02-SUMMARY.md
@.planning/phases/21-production-review-deploy-sync/21-03-SUMMARY.md
@.planning/phases/21-production-review-deploy-sync/21-04-SUMMARY.md

<interfaces>
<!-- From vercel.json (current) -->
```json
{
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/vercel-entry" },
    { "source": "/health", "destination": "/api/vercel-entry" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

<!-- From server/config.ts env vars -->
UPSTASH_REDIS_REST_URL (required)
UPSTASH_REDIS_REST_TOKEN (required)
CORS_ORIGIN (optional, defaults *)
PORT (optional, defaults 3001)
OPENSKY_CLIENT_ID (optional)
OPENSKY_CLIENT_SECRET (optional)
ADSB_EXCHANGE_API_KEY (optional)
AISSTREAM_API_KEY (optional)
ACLED_EMAIL (optional)
ACLED_PASSWORD (optional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test script, cron health endpoint, and update documentation</name>
  <files>scripts/smoke-test.ts, server/routes/cron-health.ts, server/index.ts, vercel.json, README.md, CHANGELOG.md</files>
  <action>
    1. Create `scripts/` directory and `scripts/smoke-test.ts`:
       - Accept base URL as CLI arg: `npx tsx scripts/smoke-test.ts https://your-app.vercel.app`
       - Test these endpoints: /api/flights, /api/ships, /api/events, /api/news, /api/markets, /api/weather, /api/sites, /api/sources, /health
       - For each: fetch, check res.ok, parse JSON, verify response has expected shape ('data' key for API routes, 'status' key for /health)
       - Print PASS/FAIL per endpoint with status code
       - Exit with code 1 if any endpoint fails
       - Include Cache-Control header check: verify s-maxage is present on API routes

    2. Create `server/routes/cron-health.ts`:
       - Simple endpoint that calls /health logic and logs results
       - Designed to be triggered by Vercel cron (daily on Hobby plan)
       - If any source's lastFresh is older than 1 hour, log a warning-level structured entry
       - Returns 200 with summary JSON

    3. Update `vercel.json`:
       - Add crons section: `{ "path": "/api/cron/health", "schedule": "0 */6 * * *" }` (every 6h, will run once daily on Hobby)
       - Add rewrite for cron path: `{ "source": "/api/cron/:path*", "destination": "/api/vercel-entry" }`

    4. Wire cron-health route in `server/index.ts`:
       - Import cronHealthRouter
       - `app.use('/api/cron/health', cronHealthRouter)` (no rate limiting or caching on cron)

    5. Update `README.md`:
       - Reflect v1.2 feature set: 7 visualization layers, advanced search, notification center, oil markets, etc.
       - Add Production section: deployment URL, smoke test command, health endpoint
       - Add Architecture section: edge caching, Redis fallback, structured logging
       - Keep it concise -- this is a personal project README, not enterprise docs

    6. Update `CHANGELOG.md`:
       - Add v1.2 section with summary of all v1.1 milestone features (Phases 15-21)
       - Group by category: Intelligence (sites, news, notifications), Markets, Search, Visualization Layers, Production Hardening

    7. Perform env var audit:
       - Read server/config.ts to get full list of env vars
       - Run `vercel env ls` to check what's configured
       - Log any discrepancies (missing required vars, stale keys)
       - Ensure CORS_ORIGIN is set to production domain in Vercel (not wildcard)
  </action>
  <verify>
    <automated>npx tsx scripts/smoke-test.ts http://localhost:3001 2>&1 || echo "Smoke test requires running server"</automated>
  </verify>
  <done>Smoke test script exists and is runnable. Cron health endpoint wired. README and CHANGELOG updated for v1.2. Env var audit completed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to production and verify end-to-end</name>
  <files>N/A</files>
  <action>
    Deploy the application to Vercel production and verify all features work end-to-end.

    Automated steps before checkpoint:
    1. Run `npm run build` to verify clean build
    2. Run `npx vitest run` to verify all tests pass
    3. Run `vercel --prod` to deploy
    4. Run `npx tsx scripts/smoke-test.ts $PRODUCTION_URL` to verify endpoints
    5. Present results to user for visual verification
  </action>
  <verify>
    Manual verification checklist:
    1. Visit production URL -- map loads with terrain, all entity types visible
    2. Search (Cmd+K) works, autocomplete shows results
    3. Notification bell shows alerts
    4. Markets panel shows prices with sparklines
    5. Visualization layers toggle on/off (geographic, weather, threat, political)
    6. Detail panel opens on entity click
    7. Browser DevTools Network tab shows Cache-Control headers with s-maxage on API responses
    8. Browser DevTools Network tab shows helmet security headers (X-Content-Type-Options, etc.)
    9. Multiple JS chunks loaded (vendor-react, vendor-maplibre, vendor-deckgl, app)
    10. Health endpoint returns rich JSON with redis status and source timestamps
  </verify>
  <done>Production deployment is live. Smoke tests pass (9/9 endpoints). User has visually confirmed all features work. Ready to tag v1.2.</done>
</task>

</tasks>

<verification>
- Smoke test passes against production URL
- /health returns comprehensive status
- All visualization layers, panels, and interactions work in production
- Browser DevTools confirms edge caching and security headers
</verification>

<success_criteria>
- Production deployment is live and serving all features
- Smoke test script passes against production URL (9/9 endpoints)
- Health endpoint returns comprehensive monitoring data
- Documentation is current (README, CHANGELOG)
- User has visually verified the deployed application
</success_criteria>

<output>
After completion, create `.planning/phases/21-production-review-deploy-sync/21-05-SUMMARY.md`
</output>

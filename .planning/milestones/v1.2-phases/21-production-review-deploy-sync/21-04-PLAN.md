---
phase: 21-production-review-deploy-sync
plan: 04
type: execute
wave: 2
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - server/**/*.ts
  - src/**/*.ts
  - src/**/*.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All console.log/console.error calls in server code replaced with structured logger"
    - "All API routes use cacheGetSafe/cacheSetSafe for Redis graceful degradation"
    - "No TypeScript strict-mode violations remain"
    - "Pre-existing ThreatHeatmapOverlay test failures are fixed"
    - "All 838+ tests pass"
  artifacts:
    - path: "server/routes/*.ts"
      provides: "Routes migrated to cacheGetSafe"
      contains: "cacheGetSafe"
    - path: "server/**/*.ts"
      provides: "Server code using structured logger"
      contains: "import.*log.*from.*logger"
  key_links:
    - from: "server/routes/*.ts"
      to: "server/cache/redis.ts"
      via: "cacheGetSafe import"
      pattern: "cacheGetSafe"
    - from: "server/**/*.ts"
      to: "server/lib/logger.ts"
      via: "structured log calls"
      pattern: "log\\("
---

<objective>
Comprehensive code polish pass across the entire codebase for correctness, maintainability, and performance.

Purpose: Per locked decision, this is an autonomous full-codebase audit. Migrate server routes to use Redis graceful degradation, replace all console.log/error with structured logging, fix pre-existing test failures, and address any correctness/performance issues discovered during the audit.
Output: Polished codebase with consistent patterns, all tests green, ready for production deployment.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-production-review-deploy-sync/21-CONTEXT.md
@.planning/phases/21-production-review-deploy-sync/21-RESEARCH.md
@.planning/phases/21-production-review-deploy-sync/21-01-SUMMARY.md
@.planning/phases/21-production-review-deploy-sync/21-02-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these -->
From server/lib/logger.ts (created in Plan 01):
```typescript
export interface LogEntry { level: 'info'|'warn'|'error'; message: string; method?: string; path?: string; status?: number; durationMs?: number; cacheHit?: boolean; timestamp?: string; }
export function log(entry: LogEntry): void;
```

From server/cache/redis.ts (updated in Plan 02):
```typescript
export async function cacheGetSafe<T>(key: string, logicalTtlMs: number): Promise<CacheResponse<T> | null>;
export async function cacheSetSafe<T>(key: string, data: T, redisTtlSec: number): Promise<void>;
```

From server/types.ts (updated in Plan 02):
```typescript
export interface CacheResponse<T> { data: T; stale: boolean; lastFresh: number; degraded?: boolean; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all server routes to cacheGetSafe and structured logging</name>
  <files>server/routes/flights.ts, server/routes/ships.ts, server/routes/events.ts, server/routes/news.ts, server/routes/markets.ts, server/routes/weather.ts, server/routes/sites.ts, server/routes/sources.ts, server/adapters/*.ts</files>
  <action>
    **Route migration (8 routes):**
    For each route file in `server/routes/`:
    1. Replace `import { cacheGet, cacheSet }` with `import { cacheGetSafe, cacheSetSafe }` from `../cache/redis.js`
    2. Replace all `cacheGet(` calls with `cacheGetSafe(`
    3. Replace all `cacheSet(` calls with `cacheSetSafe(`
    4. This gives all routes automatic in-memory fallback when Redis is down

    **Structured logging migration:**
    For ALL server files (routes, adapters, middleware, lib):
    1. Find every `console.log(` and `console.error(` call
    2. Replace with `log({ level: 'info'|'error', message: '...' })` from `server/lib/logger.js`
    3. Include contextual fields where available (e.g., in adapters: `message: 'GDELT fetch failed'` with relevant details)
    4. Exception: keep `console.log` in `server/index.ts` isMainModule block (startup message) -- this runs only in local dev, not serverless

    **Correctness scan:**
    Grep the full server directory for:
    - Unhandled promise rejections (async without try/catch in Express handlers)
    - Type assertion shortcuts (`as any`, `as unknown as T`) that could be typed properly
    - Unused imports or dead code
    - Missing error handling in adapter fetch calls
    Fix issues found inline.
  </action>
  <verify>
    <automated>npx vitest run server/</automated>
  </verify>
  <done>All 8 API routes use cacheGetSafe/cacheSetSafe. Zero console.log/console.error calls remain in server code (except local dev startup). Correctness issues addressed.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend polish and fix pre-existing test failures</name>
  <files>src/components/map/layers/ThreatHeatmapOverlay.tsx, src/components/map/layers/__tests__/ThreatHeatmapOverlay.test.tsx, src/**/*.ts, src/**/*.tsx</files>
  <action>
    **Fix pre-existing test failures:**
    1. Read `src/components/map/layers/__tests__/ThreatHeatmapOverlay.test.tsx` (or wherever the 6 failing tooltip tests are)
    2. Compare test assertions against actual component output
    3. Fix test expectations to match current component behavior (the component is correct, tests are stale)
    4. Verify all 6 previously-failing tests now pass

    **Frontend audit:**
    Scan `src/` directory for:
    - Unused imports (especially from refactored toggle system in Phase 20)
    - Dead code paths from removed features
    - Performance anti-patterns:
      - Object/array creation in render (should be useMemo)
      - Missing dependency arrays in useEffect/useMemo/useCallback
      - Inline function definitions in props that could cause unnecessary re-renders
    - TypeScript strictness: any remaining `as any` casts, missing return types on exported functions
    - Consistent error handling in hooks (try/catch around fetch calls)

    Fix issues found inline. Keep changes minimal and targeted -- this is polish, not refactoring.

    **Run full test suite** to confirm zero regressions.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All 838+ tests pass (zero failures). Pre-existing ThreatHeatmapOverlay tooltip tests fixed. No unused imports, dead code, or TypeScript strict-mode issues remain in frontend code.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- ALL tests pass (0 failures)
- `npm run build` -- clean build, no warnings
- `npm run typecheck` -- zero TypeScript errors
- `grep -r "console\.\(log\|error\)" server/ --include="*.ts" | grep -v __tests__ | grep -v "index.ts"` -- zero results (all migrated to structured logger)
</verification>

<success_criteria>
- Every server route uses cacheGetSafe for Redis graceful degradation
- Zero console.log/error in production server code
- All 838+ tests pass including the 6 previously-failing tests
- TypeScript strict mode clean across entire codebase
- No performance anti-patterns or dead code in frontend
</success_criteria>

<output>
After completion, create `.planning/phases/21-production-review-deploy-sync/21-04-SUMMARY.md`
</output>

---
phase: 16-news-feed
plan: 03
type: execute
wave: 1
depends_on: ["16-01", "16-02"]
files_modified:
  - server/types.ts
  - server/adapters/gdelt-doc.ts
  - server/adapters/rss.ts
  - server/__tests__/adapters/gdelt-doc.test.ts
  - server/__tests__/adapters/rss.test.ts
  - server/__tests__/routes/news.test.ts
autonomous: true
gap_closure: true
requirements: [NEWS-01, NEWS-02, NEWS-03]

must_haves:
  truths:
    - "GDELT articles returned by /api/news have a sourceCountry field populated from GDELT sourcecountry metadata"
    - "RSS articles returned by /api/news have a sourceCountry field derived from known feed origin (e.g., BBC -> United Kingdom, Al Jazeera -> Qatar)"
    - "GDELT articles are filtered to English-language only via the sourcelang:english query parameter"
  artifacts:
    - path: "server/types.ts"
      provides: "NewsArticle with sourceCountry field"
      contains: "sourceCountry"
    - path: "server/adapters/gdelt-doc.ts"
      provides: "GDELT DOC adapter mapping sourcecountry and filtering to English"
      contains: "sourcelang"
    - path: "server/adapters/rss.ts"
      provides: "RSS adapter with per-feed sourceCountry mapping"
      contains: "sourceCountry"
  key_links:
    - from: "server/adapters/gdelt-doc.ts"
      to: "server/types.ts"
      via: "NewsArticle.sourceCountry populated from GdeltArticle.sourcecountry"
      pattern: "sourceCountry.*sourcecountry"
    - from: "server/adapters/rss.ts"
      to: "server/types.ts"
      via: "NewsArticle.sourceCountry populated from RSS_FEEDS country config"
      pattern: "sourceCountry.*country"
---

<objective>
Close UAT gap: add source country metadata to all news articles and filter GDELT to English-language articles only.

Purpose: User reported that articles need English titles and country-of-origin tagging. GDELT already provides `sourcecountry` and `language` fields but they are not mapped. RSS feeds have known countries of origin but are not tagged.
Output: Updated NewsArticle type with `sourceCountry` field, GDELT adapter filtering to English via `sourcelang:english` query parameter, RSS adapter tagging each feed with its known country of origin. All existing tests updated.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-news-feed/16-01-SUMMARY.md
@.planning/phases/16-news-feed/16-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From server/types.ts (current NewsArticle -- needs sourceCountry added):
```typescript
export interface NewsArticle {
  id: string;
  title: string;
  url: string;
  source: string;
  publishedAt: number;
  summary?: string;
  imageUrl?: string;
  lat?: number;
  lng?: number;
  tone?: number;
  keywords: string[];
  // MISSING: sourceCountry -- this plan adds it
}
```

From server/adapters/gdelt-doc.ts (GdeltArticle already has sourcecountry):
```typescript
interface GdeltArticle {
  url: string;
  title: string;
  seendate: string;
  socialimage?: string;
  domain?: string;
  language?: string;
  sourcecountry?: string;  // Available but NOT mapped to NewsArticle
}
```

From server/adapters/rss.ts (RSS_FEEDS config -- needs country field):
```typescript
export const RSS_FEEDS = [
  { url: 'https://feeds.bbci.co.uk/news/world/middle_east/rss.xml', name: 'BBC' },
  { url: 'https://www.aljazeera.com/xml/rss/all.xml', name: 'Al Jazeera' },
  { url: 'https://www.tehrantimes.com/rss', name: 'Tehran Times' },
  { url: 'https://www.timesofisrael.com/feed/', name: 'Times of Israel' },
  { url: 'https://www.middleeasteye.net/rss', name: 'Middle East Eye' },
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sourceCountry field and English-only GDELT filter</name>
  <files>server/types.ts, server/adapters/gdelt-doc.ts, server/adapters/rss.ts</files>
  <action>
1. **server/types.ts** -- Add `sourceCountry?: string` field to the `NewsArticle` interface, after the `source` field. Comment: `// Country of origin of the news source (e.g., "United Kingdom", "Qatar")`.

2. **server/adapters/gdelt-doc.ts** -- Two changes:
   a. In `buildGdeltDocUrl()`, add `sourcelang:english` to the GDELT_QUERY string. The GDELT DOC API supports inline query modifiers. Append ` sourcelang:english` after the closing parenthesis of the OR group. The full query becomes:
      ```
      (Iran OR Israel OR Iraq OR Syria OR Yemen OR Lebanon OR Hezbollah OR Hamas OR IRGC OR "Middle East" OR "Persian Gulf") sourcelang:english
      ```
   b. In the `fetchGdeltArticles()` return mapping, add `sourceCountry: a.sourcecountry || undefined` to the NewsArticle object. The `sourcecountry` field is already in the `GdeltArticle` interface and comes from the API response.

3. **server/adapters/rss.ts** -- Two changes:
   a. Expand each entry in `RSS_FEEDS` to include a `country` field:
      - BBC -> `'United Kingdom'`
      - Al Jazeera -> `'Qatar'`
      - Tehran Times -> `'Iran'`
      - Times of Israel -> `'Israel'`
      - Middle East Eye -> `'United Kingdom'`
   b. Update `fetchRssFeed` signature to accept `sourceCountry: string` as a third parameter. Add `sourceCountry` to the returned NewsArticle object.
   c. Update `fetchAllRssFeeds` to pass `feed.country` as the third argument to `fetchRssFeed`.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/adapters/gdelt-doc.test.ts server/__tests__/adapters/rss.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>NewsArticle type has sourceCountry field. GDELT adapter maps sourcecountry from API response and queries English-only articles. RSS adapter tags each article with its feed's known country of origin. Existing tests may need updating in Task 2 but production code is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests for sourceCountry and English filter</name>
  <files>server/__tests__/adapters/gdelt-doc.test.ts, server/__tests__/adapters/rss.test.ts, server/__tests__/routes/news.test.ts</files>
  <action>
1. **server/__tests__/adapters/gdelt-doc.test.ts**:
   a. Update the existing test "returns NewsArticle[] normalized from GDELT DOC API ArtList JSON" to also assert `sourceCountry` is mapped: `expect(articles[0].sourceCountry).toBe('United Kingdom')` and `expect(articles[1].sourceCountry).toBe('Qatar')` (these values already exist in the GDELT_FIXTURE).
   b. Update the test "uses 24h timespan and 250 maxrecords in the URL" to also assert `expect(calledUrl).toContain('sourcelang')` -- confirming the English language filter is in the query.
   c. Add a new test: "sets sourceCountry to undefined when sourcecountry field is missing" -- use a fixture article without the `sourcecountry` field and assert `sourceCountry` is undefined.

2. **server/__tests__/adapters/rss.test.ts**:
   a. Update the RSS_FEEDS test "contains 5 feed configurations" to also check that each feed has a `country` field: `expect(RSS_FEEDS.every(f => 'country' in f)).toBe(true)`.
   b. Update the fetchRssFeed test "parses RSS 2.0 XML and returns NewsArticle[] with correct source name" to also pass a country argument and assert `sourceCountry` is set: call `fetchRssFeed(url, 'BBC', 'United Kingdom')` and `expect(articles[0].sourceCountry).toBe('United Kingdom')`.
   c. Update the fetchAllRssFeeds test "uses Promise.allSettled -- returns only fulfilled results" to assert each article has a `sourceCountry` field: `expect(articles.every(a => a.sourceCountry)).toBe(true)`.

3. **server/__tests__/routes/news.test.ts**:
   a. Update the `makeArticle` helper to include `sourceCountry: 'United Kingdom'` in the default overrides so all test fixtures have the field.
   b. Update the "response shape is CacheResponse<NewsCluster[]>" test to also assert `expect(cluster.primaryArticle).toHaveProperty('sourceCountry')` when data exists.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/adapters/gdelt-doc.test.ts server/__tests__/adapters/rss.test.ts server/__tests__/routes/news.test.ts -x 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass. GDELT adapter tests verify sourceCountry mapping and English-only query parameter. RSS adapter tests verify per-feed country tagging. Route tests verify sourceCountry propagates through the pipeline. Full test suite (`npx vitest run`) passes with no regressions.</done>
</task>

</tasks>

<verification>
1. `npx vitest run server/__tests__/` -- all server tests pass (including updated news tests)
2. `npx vitest run` -- full suite passes with no regressions
3. GDELT query URL contains `sourcelang` (verified by test assertion)
4. NewsArticle type has `sourceCountry?: string` field
5. RSS_FEEDS entries each have a `country` field
</verification>

<success_criteria>
- NewsArticle.sourceCountry is populated for all articles (GDELT from API metadata, RSS from feed config)
- GDELT adapter only fetches English-language articles
- All 46+ news tests pass, full suite green
- UAT gap "titles should be translated to English and tagged with country of origin" is closed
</success_criteria>

<output>
After completion, create `.planning/phases/16-news-feed/16-03-SUMMARY.md`
</output>

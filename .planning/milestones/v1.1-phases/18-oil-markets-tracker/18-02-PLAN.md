---
phase: 18-oil-markets-tracker
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/stores/marketStore.ts
  - src/hooks/useMarketPolling.ts
  - src/components/markets/Sparkline.tsx
  - src/components/markets/ExpandedChart.tsx
  - src/components/markets/MarketRow.tsx
  - src/components/layout/MarketsSlot.tsx
  - src/components/layout/AppShell.tsx
autonomous: true
requirements: [MRKT-01, MRKT-02, MRKT-03]

must_haves:
  truths:
    - "User can see current prices for Brent, WTI, XLE, USO, XOM in a collapsible overlay panel"
    - "Each instrument shows a 5-day sparkline with green (up) or red (down) color"
    - "Price changes trigger green delta animations matching the existing counter animation pattern"
    - "Panel is expanded by default and collapse state persists to localStorage"
    - "Clicking an instrument row expands a 5-day chart below (accordion, one at a time)"
    - "Global $/% toggle switches all instruments between dollar and percentage change"
    - "MARKET CLOSED label shown when market is closed, no delta displayed"
    - "Connection health dot shown in panel header (green/orange/red)"
  artifacts:
    - path: "src/stores/marketStore.ts"
      provides: "Zustand store for market quotes and connection status"
      exports: ["useMarketStore"]
    - path: "src/hooks/useMarketPolling.ts"
      provides: "5-min recursive setTimeout polling with tab visibility awareness"
      exports: ["useMarketPolling"]
    - path: "src/components/markets/Sparkline.tsx"
      provides: "Inline SVG sparkline (~60x16px)"
      exports: ["Sparkline"]
    - path: "src/components/markets/ExpandedChart.tsx"
      provides: "SVG line chart with Y-axis price labels, X-axis day labels, shaded area"
      exports: ["ExpandedChart"]
    - path: "src/components/markets/MarketRow.tsx"
      provides: "Single instrument row with ticker, price, delta animation, sparkline"
      exports: ["MarketRow"]
    - path: "src/components/layout/MarketsSlot.tsx"
      provides: "Top-right collapsible market panel with accordion behavior"
      exports: ["MarketsSlot"]
  key_links:
    - from: "src/hooks/useMarketPolling.ts"
      to: "/api/markets"
      via: "fetch in recursive setTimeout"
      pattern: "fetch.*api/markets"
    - from: "src/hooks/useMarketPolling.ts"
      to: "src/stores/marketStore.ts"
      via: "setMarketData/setError/setLoading"
      pattern: "useMarketStore"
    - from: "src/components/layout/MarketsSlot.tsx"
      to: "src/stores/marketStore.ts"
      via: "useMarketStore selectors"
      pattern: "useMarketStore.*quotes"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/hooks/useMarketPolling.ts"
      via: "useMarketPolling() hook call"
      pattern: "useMarketPolling"
    - from: "src/components/markets/MarketRow.tsx"
      to: "src/components/markets/Sparkline.tsx"
      via: "Sparkline component render"
      pattern: "<Sparkline"
---

<objective>
Build the client-side oil markets overlay panel: Zustand store, polling hook, and all UI components (MarketRow with delta animation, Sparkline, ExpandedChart, MarketsSlot panel container).

Purpose: Users can monitor oil and energy market prices alongside conflict data with visual trends, delta animations, and click-to-expand charts.
Output: Fully functional markets panel in the top-right of the map, below the NotificationBell, with 5-min auto-refresh.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-oil-markets-tracker/18-CONTEXT.md
@.planning/phases/18-oil-markets-tracker/18-RESEARCH.md
@.planning/phases/18-oil-markets-tracker/18-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and existing codebase. -->

From server/types.ts (created in Plan 01):
```typescript
export interface MarketQuote {
  symbol: string;           // "BZ=F", "CL=F", "XLE", "USO", "XOM"
  displayName: string;      // "Brent", "WTI", "XLE", "USO", "XOM"
  price: number;
  previousClose: number;
  change: number;
  changePercent: number;
  currency: string;
  marketOpen: boolean;
  lastTradeTime: number;    // Unix ms
  history: {
    timestamps: number[];
    closes: number[];
    highs: number[];
    lows: number[];
  };
}
```

From src/types/entities.ts (re-exported):
```typescript
export type { MarketQuote } from '../../server/types.js';
```

From src/stores/newsStore.ts (pattern to replicate):
```typescript
export type ConnectionStatus = 'connected' | 'stale' | 'error' | 'loading';
export const useNewsStore = create<NewsState>()((set) => ({ ... }));
```

From src/hooks/useNewsPolling.ts (pattern to replicate):
```typescript
// Recursive setTimeout + visibilitychange handler + cancelled flag + cleanup
```

From src/components/counters/CounterRow.tsx (delta animation pattern):
```typescript
// prevRef + delta state + deltaKey for key-based animation reset + 3s timeout clear
// Uses animate-delta CSS class from app.css
```

From src/components/ui/OverlayPanel.tsx:
```typescript
export function OverlayPanel({ children, className }: OverlayPanelProps): JSX.Element;
// Provides: rounded-lg border border-border bg-surface-overlay px-4 py-3 shadow-lg backdrop-blur-sm
```

From src/components/layout/NotificationBell.tsx (positioning pattern):
```typescript
// className: absolute top-4 z-[var(--z-controls)] transition-[right] duration-300 ease-in-out
// right offset: isDetailPanelOpen ? 'right-[calc(var(--width-detail-panel)+1rem)]' : 'right-4'
```

From src/components/ui/StatusPanel.tsx (connection dot pattern):
```typescript
const STATUS_DOT_CLASS: Record<FeedStatus, string> = {
  connected: 'bg-accent-green',
  stale: 'bg-accent-yellow',
  error: 'bg-accent-red',
  loading: 'bg-text-muted animate-pulse',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Market store, polling hook, and SVG components</name>
  <files>src/stores/marketStore.ts, src/hooks/useMarketPolling.ts, src/components/markets/Sparkline.tsx, src/components/markets/ExpandedChart.tsx</files>
  <action>
    **Create `src/stores/marketStore.ts`:**

    - Follow the exact `newsStore.ts` pattern with curried `create<T>()()`
    - State: `quotes: MarketQuote[]`, `connectionStatus: ConnectionStatus` (define locally like newsStore), `lastFetchAt: number | null`
    - Actions: `setMarketData(response: CacheResponse<MarketQuote[]>)` (sets quotes, derives connectionStatus from response.stale), `setError()`, `setLoading()`
    - Import `MarketQuote` and `CacheResponse` from `@/types/entities`

    **Create `src/hooks/useMarketPolling.ts`:**

    - Copy the exact structure of `useNewsPolling.ts`
    - `MARKET_POLL_INTERVAL = 300_000` (5 minutes)
    - `fetch('/api/markets')` -> parse as `CacheResponse<MarketQuote[]>` -> `setMarketData(data)`
    - Same lifecycle: `cancelled` flag, recursive `setTimeout` via `schedulePoll`, `visibilitychange` listener (pause on hidden, immediate fetch + restart on visible), cleanup on unmount
    - Selectors for `setMarketData`, `setError`, `setLoading` from `useMarketStore`
    - Call `setLoading()` before initial fetch

    **Create `src/components/markets/Sparkline.tsx`:**

    - Props: `closes: number[]`, `previousClose: number`, `width?: number` (default 60), `height?: number` (default 16)
    - Return `null` if `closes.length < 2`
    - Color: compare `closes[closes.length - 1]` to `previousClose` -> green (`#22c55e`) if >=, red (`#ef4444`) if <
    - Build SVG path: map closes to M/L points, scale X evenly across width, scale Y from min-max with 1px padding
    - Render: `<svg>` with `<path fill="none" stroke={color} strokeWidth={1.5} strokeLinecap="round" />`
    - CSS class `shrink-0` on svg to prevent flex compression

    **Create `src/components/markets/ExpandedChart.tsx`:**

    - Props: `quote: MarketQuote`
    - SVG dimensions: ~280 x 120 with 40px left margin (Y labels) and 20px bottom margin (X labels)
    - Plot area: line chart of `quote.history.closes` with same green/red color logic as Sparkline
    - Shaded area: `<path>` below the line with `fill={color}` at 10-15% opacity, connecting line to bottom axis
    - Y-axis: 3-4 evenly-spaced price labels (use `Intl.NumberFormat` with 2 decimal places, `$` prefix)
    - X-axis: day labels derived from `quote.history.timestamps` formatted as weekday abbreviations (Mon, Tue, etc.)
    - High/low range visualization: use a subtle filled band between high and low arrays if both have data, otherwise skip
    - Handle edge cases: fewer than 2 data points -> show "Not enough data" message
    - Smooth expand/collapse: wrap in a `div` with `overflow-hidden` and CSS `max-height` transition (0 -> auto pattern via fixed max like 160px)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>marketStore with ConnectionStatus tracks quotes array. useMarketPolling fetches /api/markets every 5 min with tab visibility awareness. Sparkline renders inline SVG polyline with green/red coloring. ExpandedChart renders 5-day SVG line chart with Y/X axis labels and shaded area.</done>
</task>

<task type="auto">
  <name>Task 2: MarketRow with delta animation and MarketsSlot panel with AppShell wiring</name>
  <files>src/components/markets/MarketRow.tsx, src/components/layout/MarketsSlot.tsx, src/components/layout/AppShell.tsx</files>
  <action>
    **Create `src/components/markets/MarketRow.tsx`:**

    - Props: `quote: MarketQuote`, `showPercent: boolean`, `isExpanded: boolean`, `onToggle: () => void`
    - Layout: single row with click handler calling `onToggle`
      - Left: `quote.displayName` (ticker label, text-xs font-semibold text-text-primary, w-12 shrink-0)
      - Center: price formatted with `Intl.NumberFormat('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })`, prefix with `$`
      - Center-right: delta display using the CounterRow animation pattern:
        - `prevRef = useRef(quote.price)` to detect changes
        - `delta` state + `deltaKey` for keyed animation reset
        - 3s timeout to clear delta (same as CounterRow)
        - Display: `showPercent ? changePercent with % : change with $` — both formatted to 2 decimals with +/- prefix
        - Color: green if change >= 0, red if < 0
        - Use `animate-delta` CSS class (already exists in app.css) on the delta span
        - When `!quote.marketOpen`: hide delta entirely (per locked decision: no delta when market closed)
      - Right: `<Sparkline closes={quote.history.closes} previousClose={quote.previousClose} />`
    - Below row (conditional on `isExpanded`): render `<ExpandedChart quote={quote} />` with smooth expand via CSS transition
    - Cursor: `cursor-pointer` on the row div

    **Create `src/components/layout/MarketsSlot.tsx`:**

    - Uses `OverlayPanel` wrapper
    - State from `useMarketStore`: `quotes`, `connectionStatus`
    - Local state:
      - `isCollapsed`: boolean, initialized from `localStorage.getItem('markets-collapsed') === 'true'` (default: false = expanded per locked decision). Persist to localStorage on toggle.
      - `showPercent`: boolean, initialized from `localStorage.getItem('markets-show-percent') === 'true'` (default: false = dollar mode per locked decision). Persist to localStorage on toggle.
      - `expandedSymbol: string | null`: which instrument is expanded (accordion — one at a time). Not persisted.
    - Header row:
      - "MARKETS" label (text-xs font-semibold uppercase tracking-wider text-text-secondary)
      - Connection status dot: replicate StatusPanel pattern with STATUS_DOT_CLASS mapping (green/orange/red/pulse for loading)
      - "MARKET CLOSED" label: shown when any quote has `marketOpen === false` (check first quote since futures may differ — use `quotes.every(q => !q.marketOpen)` for the global label). Subtle muted text: `text-[10px] text-text-muted uppercase`
      - [$/%] toggle button: small button (`text-[10px] px-1.5 py-0.5 rounded border border-border hover:bg-white/5`) toggleing `showPercent`
      - [-/+] collapse button: `text-text-muted` showing `-` when expanded, `+` when collapsed
    - Body (when not collapsed):
      - Flat list: iterate `quotes` in order (server already returns Brent, WTI, XLE, USO, XOM order)
      - Each renders `<MarketRow quote={q} showPercent={showPercent} isExpanded={expandedSymbol === q.symbol} onToggle={() => setExpandedSymbol(prev => prev === q.symbol ? null : q.symbol)} />`
      - If `quotes.length === 0` and connectionStatus is not 'loading': show "No data" in muted text
    - Positioning: absolute in top-right, below NotificationBell
      - Use same `right` offset pattern as NotificationBell (detail panel aware)
      - `top`: position below NotificationBell — use `top-14` (bell is at top-4 and ~40px tall, so ~56px = top-14 gives 4px gap)
      - `z-[var(--z-controls)]`
      - `w-[260px]` fixed width for consistent layout

    **Wire in `src/components/layout/AppShell.tsx`:**

    - Import `useMarketPolling` from `@/hooks/useMarketPolling`
    - Import `MarketsSlot` from `@/components/layout/MarketsSlot`
    - Call `useMarketPolling()` alongside other polling hooks
    - Render `<MarketsSlot />` after the `<NotificationBell />` comment block (both are top-right positioned, MarketsSlot positions itself below)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>MarketRow shows ticker + price + delta animation (3s fade, green/red) + inline sparkline per instrument. MarketsSlot renders collapsible panel with $/% toggle, MARKET CLOSED label, connection dot, and accordion expand for detailed chart. AppShell wires useMarketPolling and renders MarketsSlot.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- MarketsSlot renders in top-right below NotificationBell
- Panel is expanded by default, collapse persists to localStorage
- 5 instruments shown in order: Brent, WTI, XLE, USO, XOM
- Each row: ticker label + price + delta (green/red) + sparkline
- Delta uses `animate-delta` CSS class with 3s fade
- $/% toggle switches all instruments between dollar and percent mode
- Clicking a row expands inline chart (accordion, one at a time)
- MARKET CLOSED shown when markets are closed, delta hidden
- Connection dot reflects store connectionStatus
- Polling fetches /api/markets every 5 minutes with tab visibility awareness
</verification>

<success_criteria>
- All 3 requirements met:
  - MRKT-01: Prices for Brent, WTI, XLE, USO, XOM visible in collapsible overlay panel
  - MRKT-02: 5-day sparkline per instrument with green (up) / red (down) color coding
  - MRKT-03: Green delta animations on price changes matching CounterRow pattern
- Panel integrates cleanly with existing UI (no z-index conflicts, no layout overlap)
- Polling lifecycle follows established recursive setTimeout pattern
- Graceful degradation: stale dot on API failure, "No data" on empty
</success_criteria>

<output>
After completion, create `.planning/phases/18-oil-markets-tracker/18-02-SUMMARY.md`
</output>

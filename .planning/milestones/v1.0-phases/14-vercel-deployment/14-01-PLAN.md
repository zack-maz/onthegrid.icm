---
phase: 14-vercel-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/config.ts
  - server/index.ts
  - server/middleware/rateLimit.ts
  - .env.example
  - server/__tests__/rateLimit.test.ts
  - server/__tests__/server.test.ts
autonomous: true
requirements: [DEPLOY-02, DEPLOY-03, DEPLOY-04]

must_haves:
  truths:
    - "Server boots without crashing when OpenSky/AISStream API keys are absent"
    - "Rate limiting returns 429 after threshold exceeded"
    - "CORS allows wildcard origin in production configuration"
    - "Routes return 503 with clear message when required API keys are missing"
  artifacts:
    - path: "server/config.ts"
      provides: "Graceful degradation config — all API keys optional"
      contains: "process.env.OPENSKY_CLIENT_ID ?? ''"
    - path: "server/middleware/rateLimit.ts"
      provides: "Rate limiting middleware using @upstash/ratelimit"
      exports: ["rateLimitMiddleware"]
    - path: "server/index.ts"
      provides: "createApp with rate limiter and wildcard CORS default"
      contains: "rateLimitMiddleware"
    - path: ".env.example"
      provides: "Documented env vars with required/optional sections"
      contains: "=== REQUIRED"
    - path: "server/__tests__/rateLimit.test.ts"
      provides: "Rate limit middleware unit tests"
    - path: "server/__tests__/server.test.ts"
      provides: "Updated server tests covering CORS wildcard and graceful degradation"
  key_links:
    - from: "server/index.ts"
      to: "server/middleware/rateLimit.ts"
      via: "app.use import"
      pattern: "rateLimitMiddleware"
    - from: "server/middleware/rateLimit.ts"
      to: "server/cache/redis.ts"
      via: "redis instance for Ratelimit constructor"
      pattern: "import.*redis.*from.*cache/redis"
---

<objective>
Harden the Express server for serverless deployment: make all API keys optional (graceful degradation), add rate limiting middleware, update CORS default to wildcard, and document environment variables.

Purpose: The server currently crashes on cold start if OpenSky/AISStream API keys are missing. Vercel serverless functions must boot cleanly with only Redis credentials configured. Rate limiting protects upstream API credits from abuse.

Output: Server that boots without external API keys, rate-limits requests, and has production-ready CORS.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-vercel-deployment/14-CONTEXT.md
@.planning/phases/14-vercel-deployment/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From server/config.ts:
```typescript
export interface AppConfig {
  port: number;
  corsOrigin: string;
  opensky: { clientId: string; clientSecret: string; };
  aisstream: { apiKey: string; };
  acled: { email: string; password: string; };
}
export function loadConfig(): AppConfig;
export function getConfig(): AppConfig;
export const config: AppConfig; // Proxy-based lazy getter
```

From server/index.ts:
```typescript
export function createApp(): Express;
// Uses: cors({ origin: process.env.CORS_ORIGIN ?? 'http://localhost:5173' })
// Has isMainModule guard for app.listen()
```

From server/cache/redis.ts:
```typescript
export const redis: Redis; // Upstash Redis instance
export async function cacheGet<T>(key: string, logicalTtlMs: number): Promise<CacheResponse<T> | null>;
export async function cacheSet<T>(key: string, data: T, redisTtlSec: number): Promise<void>;
```

From server/middleware/errorHandler.ts:
```typescript
export function errorHandler(err: Error, req: Request, res: Response, _next: NextFunction): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Graceful config + rate limiting middleware + tests</name>
  <files>server/config.ts, server/middleware/rateLimit.ts, server/__tests__/rateLimit.test.ts, server/__tests__/server.test.ts</files>
  <behavior>
    - rateLimit.test: rateLimitMiddleware calls next() when under limit
    - rateLimit.test: rateLimitMiddleware returns 429 JSON when limit exceeded
    - rateLimit.test: sets X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset headers
    - server.test: server boots and responds to /health without OpenSky/AISStream env vars
    - server.test: CORS returns Access-Control-Allow-Origin: * when CORS_ORIGIN not set
  </behavior>
  <action>
    1. Install @upstash/ratelimit: `npm install @upstash/ratelimit`

    2. Fix server/config.ts — remove `required()` calls for API keys. Make all API keys optional with `??  ''` fallback. Change corsOrigin default from `'http://localhost:5173'` to `'*'` (production-first; local dev overrides via .env file). The `required()` function can be deleted entirely since nothing uses it anymore. Keep the Proxy pattern and lazy loading.

    3. Create server/middleware/rateLimit.ts — Express middleware using @upstash/ratelimit with the existing redis instance from server/cache/redis.ts. Use `Ratelimit.slidingWindow(60, '60 s')` (60 requests per 60 seconds per IP). Identifier: `req.ip ?? req.headers['x-forwarded-for'] ?? 'anonymous'` (cast to string). Set X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset response headers (all as strings). Return 429 JSON `{ error: 'Too many requests' }` when limit exceeded. Use proper Express types (Request, Response, NextFunction). Return type `Promise<void>`.

    4. Create server/__tests__/rateLimit.test.ts — mock @upstash/ratelimit to control success/failure. Test: under limit calls next(), over limit returns 429 with error JSON, headers are set correctly. Use `@vitest-environment node` inline directive.

    5. Update server/__tests__/server.test.ts — add test: "CORS Access-Control-Allow-Origin defaults to * when CORS_ORIGIN not set" (delete CORS_ORIGIN from process.env, create fresh app, check header). Add test: "server boots without OpenSky/AISStream API keys" (verify /health responds 200 with those env vars unset). Update existing config mock to reflect optional API keys (empty strings instead of 'test' for opensky/aisstream values).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/rateLimit.test.ts server/__tests__/server.test.ts -x</automated>
  </verify>
  <done>config.ts has no required() calls; rateLimit middleware exports rateLimitMiddleware; tests for rate limiting and graceful degradation pass; CORS defaults to wildcard</done>
</task>

<task type="auto">
  <name>Task 2: Wire rate limiter into createApp + update .env.example</name>
  <files>server/index.ts, .env.example</files>
  <action>
    1. Update server/index.ts — import rateLimitMiddleware from './middleware/rateLimit.js'. Add `app.use('/api', rateLimitMiddleware)` BEFORE the data source routes (after cors and json middleware, before the /api/* route registrations). This applies rate limiting only to /api/* paths, not /health.

    2. Update .env.example — restructure into clear sections:
       ```
       # === REQUIRED for deployment ===
       UPSTASH_REDIS_REST_URL=your-upstash-redis-rest-url
       UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token

       # === OPTIONAL -- graceful degradation if missing ===
       OPENSKY_CLIENT_ID=your-client-id
       OPENSKY_CLIENT_SECRET=your-client-secret
       AISSTREAM_API_KEY=your-aisstream-api-key
       ADSB_EXCHANGE_API_KEY=your-rapidapi-key
       ACLED_EMAIL=your-email@example.com
       ACLED_PASSWORD=your-password

       # === SERVER CONFIG (optional -- defaults shown) ===
       # PORT=3001
       # CORS_ORIGIN=*
       # AISSTREAM_COLLECT_MS=5000
       ```
       Add comment URLs for where to get each credential. Mark CORS_ORIGIN default as `*` (not localhost).

    3. Verify all existing server tests still pass: `npx vitest run server/`
  </action>
  <verify>
    <automated>npx vitest run server/ -x</automated>
  </verify>
  <done>createApp() applies rate limiting to /api/* routes; .env.example clearly documents required vs optional vars; all 144+ server tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run server/` — all existing tests pass (no regressions)
- `npx vitest run server/__tests__/rateLimit.test.ts` — rate limiting tests pass
- config.ts contains no `required()` calls
- server/index.ts imports and uses rateLimitMiddleware
- .env.example has REQUIRED and OPTIONAL sections
</verification>

<success_criteria>
- Server boots cleanly without OpenSky/AISStream/ACLED env vars
- Rate limiting middleware returns 429 with proper headers on excess requests
- CORS defaults to wildcard `*` (overridable via CORS_ORIGIN env var)
- All existing server tests pass without modification (beyond config mock updates)
- .env.example documents all env vars with required/optional sections
</success_criteria>

<output>
After completion, create `.planning/phases/14-vercel-deployment/14-01-SUMMARY.md`
</output>

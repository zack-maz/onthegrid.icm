---
phase: 13-serverless-cache-migration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - server/adapters/aisstream.ts
  - server/routes/ships.ts
  - server/__tests__/adapters/aisstream.test.ts
  - server/__tests__/routes/ships.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Ships route reads/writes from Redis instead of in-memory Map"
    - "AISStream uses on-demand connect-collect-close instead of persistent WebSocket"
    - "Fresh ships are merged with previously cached ships on each collect"
    - "Ships not seen in 10 minutes are pruned from the accumulator"
    - "Ships route falls back to stale Redis cache when WebSocket fails"
    - "CacheResponse<T> shape is preserved for ships"
  artifacts:
    - path: "server/adapters/aisstream.ts"
      provides: "On-demand collectShips() function replacing persistent connectAISStream()"
      exports: ["collectShips"]
    - path: "server/routes/ships.ts"
      provides: "Ships route with Redis cache-first + merge pattern"
      min_lines: 30
    - path: "server/__tests__/routes/ships.test.ts"
      provides: "Ships route unit tests"
      min_lines: 50
    - path: "server/__tests__/adapters/aisstream.test.ts"
      provides: "Updated aisstream adapter tests for collectShips()"
      min_lines: 40
  key_links:
    - from: "server/routes/ships.ts"
      to: "server/cache/redis.ts"
      via: "import { cacheGet, cacheSet }"
      pattern: "cacheGet.*ships:ais"
    - from: "server/routes/ships.ts"
      to: "server/adapters/aisstream.ts"
      via: "import { collectShips }"
      pattern: "collectShips"
    - from: "server/adapters/aisstream.ts"
      to: "WebSocket"
      via: "new WebSocket('wss://stream.aisstream.io/v0/stream')"
      pattern: "new WebSocket"
---

<objective>
Rewrite AISStream adapter from persistent WebSocket to on-demand collect-close pattern, and migrate ships route to Redis cache with merge/prune logic.

Purpose: Make ship data fetching stateless for serverless compatibility -- no persistent WebSocket connections, all state in Redis.
Output: Rewritten aisstream.ts with collectShips(), rewritten ships route with Redis merge, passing tests.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-serverless-cache-migration/13-CONTEXT.md
@.planning/phases/13-serverless-cache-migration/13-RESEARCH.md
@.planning/phases/13-serverless-cache-migration/13-01-SUMMARY.md

@server/adapters/aisstream.ts
@server/routes/ships.ts
@server/constants.ts
@server/types.ts
@server/__tests__/adapters/aisstream.test.ts

<interfaces>
<!-- From Plan 01's output: server/cache/redis.ts -->
```typescript
import type { CacheResponse } from '../types.js';

export async function cacheGet<T>(key: string, logicalTtlMs: number): Promise<CacheResponse<T> | null>;
export async function cacheSet<T>(key: string, data: T, redisTtlSec: number): Promise<void>;
export { redis };
```

From server/types.ts:
```typescript
export interface ShipEntity extends MapEntityBase {
  type: 'ship';
  data: {
    mmsi: number;
    shipName: string;
    speedOverGround: number;
    courseOverGround: number;
    trueHeading: number;
  };
}

export interface CacheResponse<T> {
  data: T;
  stale: boolean;
  lastFresh: number;
}
```

From server/constants.ts:
```typescript
export const IRAN_BBOX: BoundingBox = { south: 0.0, north: 50.0, west: 20.0, east: 80.0 };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite AISStream adapter to on-demand collectShips()</name>
  <files>server/adapters/aisstream.ts, server/__tests__/adapters/aisstream.test.ts</files>
  <behavior>
    - collectShips() opens a WebSocket to wss://stream.aisstream.io/v0/stream
    - collectShips() sends subscription with AISSTREAM_API_KEY, IRAN_BBOX bounds, PositionReport filter on open
    - collectShips() collects ship messages for AISSTREAM_COLLECT_MS milliseconds (default 5000ms, env configurable)
    - collectShips() closes WebSocket after collect window and resolves with ShipEntity[]
    - collectShips() deduplicates by MMSI (later messages overwrite earlier for same ship)
    - collectShips() rejects if WebSocket errors before collect window ends
    - collectShips() throws if AISSTREAM_API_KEY is not set
  </behavior>
  <action>
    1. Rewrite `server/adapters/aisstream.ts`:
       - Remove: `ships` Map, `lastMessageTime`, `getShips()`, `getLastMessageTime()`, `connectAISStream()`, import of `config`
       - Remove: WebSocket close/reconnect logic (no persistent connection)
       - Keep: The message parsing logic from the current `message` event handler (the `PositionReport` normalization to `ShipEntity`)
       - Add: `const DEFAULT_COLLECT_MS = 5000;`
       - Export: `async function collectShips(): Promise<ShipEntity[]>`
         - Read `AISSTREAM_COLLECT_MS` from `process.env` (default 5000)
         - Read `AISSTREAM_API_KEY` from `process.env`, throw Error if missing
         - Return a `new Promise<ShipEntity[]>` that:
           - Creates `collected = new Map<number, ShipEntity>()` (keyed by MMSI for dedup)
           - Opens `new WebSocket('wss://stream.aisstream.io/v0/stream')`
           - On `open`: sends subscription JSON with `APIKey`, `BoundingBoxes` (from `IRAN_BBOX`), `FilterMessageTypes: ['PositionReport']`
           - On `message`: parses the same way as current handler (handle both Blob and string data), creates ShipEntity, `collected.set(mmsi, entity)`
           - Sets `setTimeout(() => { ws.close(); resolve(Array.from(collected.values())); }, collectMs)`
           - On `error`: `clearTimeout(timer); try { ws.close() } catch {}; reject(new Error('AISStream WebSocket connection failed'))`

    2. Rewrite `server/__tests__/adapters/aisstream.test.ts`:
       - Remove: tests for `connectAISStream`, `getShips`, `getLastMessageTime`, reconnect behavior
       - Remove: mock of `../../config.js` (collectShips reads env vars directly)
       - Keep: `FakeWebSocket` class with `simulateOpen`, `simulateMessage`, `simulateError` methods
       - Keep: `samplePositionReport` fixture
       - Add: `close()` method to FakeWebSocket (no-op, just tracks calls)
       - Set `process.env.AISSTREAM_API_KEY = 'test-key'` in beforeEach
       - Use `vi.useFakeTimers()` so `setTimeout` can be advanced
       - Test: collectShips sends correct subscription on open
       - Test: collectShips normalizes PositionReport to ShipEntity correctly
       - Test: collectShips deduplicates by MMSI
       - Test: collectShips resolves after AISSTREAM_COLLECT_MS timeout with collected ships
       - Test: collectShips rejects on WebSocket error
       - Test: collectShips throws when AISSTREAM_API_KEY is not set
       - Test: collectShips respects custom AISSTREAM_COLLECT_MS env var
       - Advance timers with `vi.advanceTimersByTime(collectMs)` to trigger the timeout resolution
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/adapters/aisstream.test.ts -x</automated>
  </verify>
  <done>aisstream.ts exports only collectShips(), no persistent WS exports remain, all adapter tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate ships route to Redis cache with merge/prune</name>
  <files>server/routes/ships.ts, server/__tests__/routes/ships.test.ts</files>
  <behavior>
    - Ships route returns fresh cached data when Redis cache is within 30s logical TTL
    - Ships route calls collectShips() when cache is stale or missing
    - Ships route merges fresh collected ships with previously cached ships by ID
    - Ships route prunes ships with timestamp older than 10 minutes
    - Ships route falls back to stale Redis cache (with stale:true) when collectShips() fails
    - Ships route returns 500 when collectShips() fails and no cache exists
    - Ships route response matches CacheResponse<ShipEntity[]> shape
  </behavior>
  <action>
    1. Rewrite `server/routes/ships.ts`:
       - Remove: imports of `getShips`, `getLastMessageTime` from aisstream adapter
       - Import: `{ cacheGet, cacheSet }` from `../cache/redis.js`
       - Import: `{ collectShips }` from `../adapters/aisstream.js`
       - Import: `type { ShipEntity }` from `../types.js`
       - Define constants:
         - `SHIPS_KEY = 'ships:ais'`
         - `LOGICAL_TTL_MS = 30_000` (30s -- same as client polling interval)
         - `REDIS_TTL_SEC = 300` (5min -- 10x of 30s logical TTL)
         - `STALE_THRESHOLD_MS = 600_000` (10 min -- prune ships not seen in 10 min)
       - Make route handler `async`:
         - `const cached = await cacheGet<ShipEntity[]>(SHIPS_KEY, LOGICAL_TTL_MS)`
         - If cached and not stale: return `res.json(cached)`
         - Try block:
           - `const fresh = await collectShips()`
           - Merge: create `Map<string, ShipEntity>`, seed with `cached.data` (if any), then overwrite with `fresh`
           - Prune: delete entries where `ship.timestamp < Date.now() - STALE_THRESHOLD_MS`
           - `const merged = Array.from(shipMap.values())`
           - `await cacheSet(SHIPS_KEY, merged, REDIS_TTL_SEC)`
           - `res.json({ data: merged, stale: false, lastFresh: Date.now() })`
         - Catch block:
           - Log error
           - If cached: `res.json({ ...cached, stale: true })`
           - Else: `res.status(500).json({ error: 'Ship data unavailable' })`

    2. Create `server/__tests__/routes/ships.test.ts`:
       - Use `// @vitest-environment node` header
       - Mock `../../cache/redis.js` with in-memory Map store (same pattern as flights test)
       - Mock `../../adapters/aisstream.js` with `vi.fn()` for `collectShips`
       - Mock `../../adapters/gdelt.js` with `vi.fn(async () => [])` for fetchEvents/backfillEvents (needed because events.ts runs module-level backfill code)
       - Mock `../../config.js` (needed by server startup)
       - Import `createApp` and start a test server in beforeEach/afterEach
       - Test: returns fresh cached data without calling collectShips when cache is fresh
       - Test: calls collectShips on cache miss and returns merged result
       - Test: merges fresh ships with previously cached ships (seed cache with ship A, collectShips returns ship B, response contains both)
       - Test: prunes ships older than 10 minutes from merged result
       - Test: falls back to stale cache with stale:true when collectShips throws
       - Test: returns 500 when collectShips throws and no cache exists
       - Test: response shape matches { data: ShipEntity[], stale: boolean, lastFresh: number }
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/routes/ships.test.ts server/__tests__/adapters/aisstream.test.ts -x</automated>
  </verify>
  <done>Ships route uses Redis cache with merge/prune logic, collectShips is on-demand, all ships and aisstream tests pass, response shape matches CacheResponse</done>
</task>

</tasks>

<verification>
```bash
cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/adapters/aisstream.test.ts server/__tests__/routes/ships.test.ts -x
```

Verify no persistent WS exports in aisstream.ts:
```bash
! grep -qE "connectAISStream|getShips|getLastMessageTime" /Users/zackmaz/Desktop/my_world/server/adapters/aisstream.ts
```
</verification>

<success_criteria>
- `server/adapters/aisstream.ts` exports only `collectShips()`, no persistent WebSocket functions
- `server/routes/ships.ts` uses cacheGet/cacheSet with key `ships:ais`
- Ships are merged (fresh + cached) and pruned (10 min staleness)
- Stale fallback works when WebSocket fails
- All aisstream and ships route tests pass
- CacheResponse<T> shape preserved
</success_criteria>

<output>
After completion, create `.planning/phases/13-serverless-cache-migration/13-02-SUMMARY.md`
</output>

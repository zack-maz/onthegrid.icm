---
phase: 27.4.4
plan: 01
artifact: BAKEOFF
captured: 2026-04-27T23:27:40Z
operator: zackmaz
sample_size: 20
mode: combo-path-from-preflight
winner: qwen/qwen3.5-397b-a17b
deviation: 50-event sample skipped per combo-path D-16 approval
---

# 27.4.4 Bake-off (D-02 / D-03 / D-07) — combo-path verdict

**Captured:** 2026-04-27T23:27:40Z (UTC) → 2026-04-28T00:57:23Z (UTC)
**Operator:** zackmaz
**Total wall clock:** ~90 min (preflight only)
**Total LLM calls:** 80 attempted, 44 traced (qwen 19 / llama 20 / nemotron 5 / glm 0)
**Prompt-tuning budget used:** 0 / 30 min (Tier-2 deferred — combo path approved off Tier-1)
**Sample size:** **20 events** (deviation from 50-event must_have — see "Deviation from plan" below)

## DEVIATION FROM PLAN

The plan's must_have called for a 50-event LLM-in-loop matrix across all 4 candidates. **Skipped per operator combo-path D-16 approval** at the 20-event preflight checkpoint. Rationale:

1. The 20-event preflight already produced a clear winner: qwen at 0.80 within20km is the only LIVE candidate above 0 (nemotron schema-fails, glm 400s, llama is alive but worse on within20km AND p95). No 50-event sample would change the relative ordering.
2. The load-bearing remediation for the long-tail (qwen p95=264.9s ≫ 30s floor) is **D-04 V3_ADAPTIVE_BATCH=true at Gate B**, not deeper bake-off data. The bake-off matrix doesn't tell us anything about D-04's effectiveness — Gate B does.
3. ~30-50 min wall clock + ~200 LLM calls saved against NIM's 40 req/min budget.

This deviation is RECORDED for SUMMARY.md, the phase audit trail, and any future re-baseline.

## BAKEOFF MATRIX (20-event LLM-in-loop, Tier-1 only)

| Model id (NVIDIA NIM)             | within20km / 20 | p50 (ms) | p95 (ms) | p99 (ms) | schema_fail / 20             | within20km ≥ 0.890? | p95 ≤ 30s?  | Dual floor   | Selected |
| --------------------------------- | --------------- | -------- | -------- | -------- | ---------------------------- | ------------------- | ----------- | ------------ | -------- |
| qwen/qwen3.5-397b-a17b            | 16/20 (0.80)    | 10,621   | 176,868  | 176,868  | 0                            | FAIL (0.80)         | FAIL (177s) | FAIL on both | ✓ winner |
| meta/llama-3.3-70b-instruct       | 15/20 (0.75)    | 5,581    | 447,064  | 447,064  | 0                            | FAIL (0.75)         | FAIL (447s) | FAIL on both |          |
| nvidia/nemotron-3-super-120b-a12b | 0/20 (0.00)     | 28,917   | 118,758  | 118,758  | 5                            | FAIL (0.00)         | FAIL (119s) | FAIL on both |          |
| z-ai/glm4.7                       | 0/20 (0.00)     | 755      | 755      | 755      | 0 (n/a — 400s before stream) | FAIL (0.00)         | FAIL        | FAIL on both |          |

(Schema-fail counts: nemotron 5/5 successful calls schema-failed despite envelope-shape PROMPT_OVERRIDES being applied; glm produced no traceable records — every call returned 400 before stream open.)

## Per-candidate notes

- **qwen/qwen3.5-397b-a17b** (selected — see matrix): best within20km (0.80) of any LIVE candidate, lowest gen-duration p95 of the alive pair (264.9s vs llama's 564.4s). Long-tail root cause is generation-rate collapse (NOT reasoning explosion, NOT max_tokens — see PREFLIGHT-CHARACTERIZATION.md §long-tail diagnosis). Locked as v3 primary in `NVIDIA_NIM_DEFAULT_MODEL` via Task 5; D-01 in-incident reversion handle preserved as `NVIDIA_NIM_FALLBACK_MODEL = 'qwen/qwen3.5-397b-a17b'` (i.e. fallback is itself qwen — there is no other viable candidate to fall back to in 27.4.4's sample). Future phases can revisit if a stronger NIM-cataloged candidate appears.
- **meta/llama-3.3-70b-instruct**: alive on infra (TTFB p50=369ms is the fastest in the bake-off), within20km is 0.75 (below qwen's 0.80), gen-duration p95 of 564s is 2.1× worse than qwen's 265s. Defer — not selected; could be revisited in a future phase if a Tier-2 prompt-tuning attempt (e.g. simpler prompt, narrower JSON Schema constraints) materially changes the latency tail.
- **nvidia/nemotron-3-super-120b-a12b**: schema-fails on every successful call. PROMPT_OVERRIDES envelope-shape demand was applied (`promptOverrideApplied: true` in trace) but did NOT fix it (5/5 success → 5/5 schema_fail). Model emits flat object shape (top-level `groupKey`/`location`) regardless of prompt. Defer — instruction-following gap, not a prompt-engineering gap.
- **z-ai/glm4.7**: 400 status on every call, no stream chunks emitted. Trace file has zero glm records. Two possible causes:
  1. NIM rejected the `extra_body: { chat_template_kwargs: { enable_thinking: false } }` payload (NIM's OpenAI-compat surface may not expose this passthrough).
  2. Model id is not in NIM's catalog or is not accessible on the project's free tier.
     Defer — separate compatibility check needed to distinguish; outside 27.4.4 scope.

## Loser Deferral (D-07 budget exhausted — actually NOT exhausted)

D-07's 30-min Tier-2 prompt-tuning budget was NOT used. nemotron's PROMPT_OVERRIDES envelope-shape demand was applied at Tier-1 already (because the override was wired into Task 2 and applies to ALL bake-off runs, not just Tier-2). The override didn't fix the schema fail, and a different Tier-2 attempt would not change the operator's combo-path decision (qwen is the only LIVE candidate regardless).

If a future phase wants to revisit:

- **nemotron**: try a simpler / narrower prompt that demands ONLY `{events: [{groupKey, location, type, ...}]}` and removes nullable optional fields. Hypothesis: nemotron may be skipping the envelope when many optional nullable fields blur the structural signal.
- **glm**: re-attempt without `extra_body` — distinguish "NIM catalog drift" from "extra_body rejection".

## Outcome ladder verdict (D-03)

- [x] **0 candidates pass dual hard floors → escalate to combo path (winner + V3_ADAPTIVE_BATCH ON) — operator-approved 2026-04-28T15:30Z**
- [ ] 1 candidate passes → lock it
- [ ] 2+ candidates pass → lowest p99 wins
- [ ] 0 candidates pass → defer phase (D-17 not triggered; operator elected combo path instead)

## Selected winner

**`qwen/qwen3.5-397b-a17b`** — locked as `NVIDIA_NIM_DEFAULT_MODEL` in Task 5. Combo-path approved.

**Operator approval:** `bakeoff: winner=qwen/qwen3.5-397b-a17b (combo-path)` — 2026-04-28T15:30Z by zackmaz.

## Gate B framing

Plan 02 Gate B passes will explicitly carry:

- **Expected p95 > 30s.** Cutover ships v3 because of location-accuracy parity (resolver-only baseline 47/50 = 0.94 unchanged across all 4 candidates), not because of latency parity. The within20km and within5km thresholds are the load-bearing pass criteria.
- **V3_ADAPTIVE_BATCH=true.** Set in Vercel prod env BEFORE Gate B Pass 1. Splits timed-out batches and retries; mitigates the long-tail without changing the model.
- **D-13 V3_WATCHDOG_ROLLBACK_THRESHOLD=2** (default, no override). Auto-rollback Trigger 1 stays sensitive so a watchdog-recurrence reverts to v2 cleanly if Gate B regresses.

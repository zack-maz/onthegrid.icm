---
phase: 30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim
plan: 05
type: execute
wave: 3
depends_on: ['30-02']
files_modified:
  - server/config.ts
  - server/lib/llmEventExtractor.v3.ts
  - server/lib/freeClaudeRouter.ts
  - .env.example
  - server/__tests__/config.test.ts
  - server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts
autonomous: true
requirements: [LLM-RELI-03, LLM-RELI-04]
requirements_addressed: [LLM-RELI-03, LLM-RELI-04]
gsd_commit: 'feat(30): tune LLM_V3_CONCURRENCY / LLM_BATCH_SIZE / backoff against measured throttle (D-02 / LLM-RELI-03 / LLM-RELI-04)'
must_haves:
  truths:
    - 'LLM_BATCH_SIZE is an env-tunable Zod schema entry in server/config.ts with a hard-coded fallback default; the v3 extractor consumer at server/lib/llmEventExtractor.v3.ts:83 reads env.LLM_BATCH_SIZE instead of the prior hard-coded const 2'
    - "freeClaudeRouter.ts RETRY_ATTEMPTS, BACKOFF_MS array, and JITTER_MS constants are updated to numbers derived from Run-1's measured throttle window (Plan 02 snapshot) — old values quoted side-by-side in commit message"
    - 'LLM_BATCH_TIMEOUT_MS default in server/config.ts is bumped from 90_000 to max(2 × measured_batch_latency_p95, observed_throttle_window + 30s) per CONTEXT D-02'
    - 'LLM_V3_CONCURRENCY default in server/config.ts is updated to (observed_NIM_steady_RPM × measured_batch_latency_seconds) / 60 — likely LOWER than current 12 since the 300s pressure that justified 12 is gone (CONTEXT D-02)'
    - '.env.example mirrors all schema changes with operator-facing tuning-knob commentary'
    - 'Old default values are quoted in the commit message body so git log preserves the v1.4-→v1.5 delta'
  artifacts:
    - path: 'server/config.ts'
      provides: 'LLM_BATCH_SIZE Zod schema entry; updated default constants for LLM_V3_CONCURRENCY and LLM_BATCH_TIMEOUT_MS'
      contains: 'LLM_BATCH_SIZE'
    - path: 'server/lib/llmEventExtractor.v3.ts'
      provides: 'Env-tunable batch size consumer (was hard-coded const)'
      contains: 'env.LLM_BATCH_SIZE'
    - path: 'server/lib/freeClaudeRouter.ts'
      provides: 'Tuned RETRY_ATTEMPTS, BACKOFF_MS array, JITTER_MS — derived from Run-1 throttle window'
    - path: '.env.example'
      provides: 'Operator-facing tuning-knob commentary for LLM_BATCH_SIZE; updated commentary for LLM_V3_CONCURRENCY and LLM_BATCH_TIMEOUT_MS'
      contains: 'LLM_BATCH_SIZE'
    - path: 'server/__tests__/config.test.ts'
      provides: 'Unit coverage that LLM_BATCH_SIZE env var honored when set; falls back to constant default otherwise'
      contains: 'LLM_BATCH_SIZE'
    - path: 'server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts'
      provides: 'Integration coverage that v3 extractor consumes env.LLM_BATCH_SIZE via the hoisted mockEnv pattern'
      contains: 'LLM_BATCH_SIZE'
  key_links:
    - from: 'server/config.ts LLM_BATCH_SIZE Zod entry'
      to: 'server/lib/llmEventExtractor.v3.ts:83 BATCH_SIZE consumer'
      via: 'env.LLM_BATCH_SIZE import (already imported at line 28 per RESEARCH §Pattern 2)'
      pattern: 'env.LLM_BATCH_SIZE'
    - from: 'Run 1 analyzer snapshot (Plan 02 deliverable)'
      to: 'server/lib/freeClaudeRouter.ts BACKOFF_MS / JITTER_MS / RETRY_ATTEMPTS'
      via: 'operator math: BACKOFF base = throttle_window / 2; jitter = ±0.25 × base'
      pattern: 'BACKOFF_MS'
---

<objective>
Propose-and-commit the new defaults derived analytically from Plan 02's Run-1 measurements (CONTEXT D-02 "propose" step in characterize → propose → validate). This plan introduces `LLM_BATCH_SIZE` as a new env-tunable Zod schema entry (D-07; LLM-RELI-03 names this knob canonically), promotes the hard-coded `const BATCH_SIZE = 2` in the v3 extractor to consume `env.LLM_BATCH_SIZE`, and updates the `callLLM` retry / backoff / jitter parameters in `freeClaudeRouter.ts` to numbers derived from the Run-1 throttle window (LLM-RELI-04 "from measured data, not guesses").

This is a CODE-ONLY plan — no force-trigger fires here. Validation happens in Plan 06 (Run 2). Note: per CONTEXT D-08 commits 4 and 5 may swap order; this plan runs in the same wave as Plan 04 (D-05 / SIMPLIFY-03) because their `files_modified` sets are line-disjoint within `server/lib/llmEventExtractor.v3.ts` (Plan 04 touches lines 633 + 661-683 + 956 for soft-warn; this plan touches line 83 for BATCH_SIZE). If a merge conflict surfaces, this plan defers to Plan 04 and rebases.

Purpose: Without Plan 05, the cascade keeps running v1.4 defaults sized for the now-retired 300s Hobby ceiling. The Pro 800s ceiling unlocks a different operating point — likely lower CONCURRENCY (the 12-concurrency was a 300s-pressure choice), higher BATCH_SIZE (more groups per call given the 800s headroom), more generous RETRY_ATTEMPTS, and a hard-kill default sized against actual p95 batch latency rather than the conservative 90s.

Output:

- New Zod schema entry: `LLM_BATCH_SIZE: z.coerce.number().int().positive().default(<RUN_1_DERIVED>)`
- Updated `LLM_V3_CONCURRENCY.default(<RUN_1_DERIVED>)` (likely 6-10, lower than 12)
- Updated `LLM_BATCH_TIMEOUT_MS.default(<RUN_1_DERIVED>)` (likely 120_000-180_000, raised from 90_000)
- Updated `freeClaudeRouter.ts`: `RETRY_ATTEMPTS` (likely 3, was 2); `BACKOFF_MS` array (sized from throttle window); `JITTER_MS` (±0.25 × backoff base)
- `.env.example` mirror changes with operator-facing tuning-knob commentary
- New `LLM_BATCH_SIZE` unit test in `server/__tests__/config.test.ts`
- Updated `llmExtractionPipeline.incrementalWrite.test.ts` with `LLM_BATCH_SIZE` consumer test (note: this test file also gets reshaped by Plan 03 / D-04 — coordinate via Plan 03's `depends_on: [Plan 06]` Option A ordering, so this plan lands first and Plan 03 inherits its mockEnv shape)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-VALIDATION.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-02-SUMMARY.md
@server/config.ts
@.env.example
@server/lib/llmEventExtractor.v3.ts
@server/lib/freeClaudeRouter.ts
@server/__tests__/config.test.ts
@server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts

<interfaces>
<!-- Existing pattern to mirror: LLM_V3_CONCURRENCY (server/config.ts:58-72) -->
LLM_V3_CONCURRENCY: z.coerce.number().int().positive().default(12),

<!-- Existing pattern to mirror: LLM_BATCH_TIMEOUT_MS (server/config.ts:51-56) -->

LLM_BATCH_TIMEOUT_MS: z.coerce.number().int().positive().default(90_000),

<!-- Current hard-coded BATCH_SIZE (server/lib/llmEventExtractor.v3.ts:83) — promote to env-tunable -->

const BATCH_SIZE = 2; // ← becomes: const BATCH_SIZE = env.LLM_BATCH_SIZE;

<!-- Current router constants (server/lib/freeClaudeRouter.ts:64-66) — tune all four -->

const LLM_TIMEOUT_MS = 120_000;
const RETRY_ATTEMPTS = 2; // ← may become 3
const BACKOFF_MS = [1000, 4000] as const; // ← sized from Run-1 throttle window
const JITTER_MS = 250; // ← ±0.25 × new backoff base
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LLM_BATCH_SIZE Zod schema + .env.example block + config.test.ts case (D-07 promotion)</name>
  <files>server/config.ts, .env.example, server/__tests__/config.test.ts</files>
  <read_first>
    - server/config.ts lines 51-72 (the LLM_BATCH_TIMEOUT_MS + LLM_V3_CONCURRENCY analogs to mirror exactly)
    - .env.example lines 124-135 (the LLM_V3_CONCURRENCY commentary block to mirror)
    - server/__tests__/config.test.ts lines 76-83 (the EVENT_CONFIDENCE_THRESHOLD numeric-parse analog test to mirror)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json (Plan 02 deliverable — read .perBatchLatency.p95 and .evalScore to inform the chosen LLM_BATCH_SIZE default per CONTEXT D-02: "raised from 2 toward 4-8 depending on runEval() accuracy under the larger group context")
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-02-SUMMARY.md (operator notes on Run 1 — informs whether to lean conservative or aggressive on new BATCH_SIZE)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md section "server/config.ts" + "server/__tests__/config.test.ts" (verbatim shape to copy)
  </read_first>
  <behavior>
    - Test 1: When `process.env.LLM_BATCH_SIZE = '4'` is set, dynamic import of config yields `env.LLM_BATCH_SIZE === 4`
    - Test 2: When LLM_BATCH_SIZE is unset, `env.LLM_BATCH_SIZE` falls back to the hard-coded default
    - Test 3: When LLM_BATCH_SIZE is set to invalid value (`'abc'`), Zod coerce throws AppError or rejects parseEnv (mirrors existing config test posture)
    - Test 4: `.env.example`'s LLM_BATCH_SIZE block parses cleanly via `npm run check:env` (scripts/check-env-example.ts)
  </behavior>
  <action>
    Step 1 (Zod schema — server/config.ts):
    - Compute the new `LLM_BATCH_SIZE` default from Run-1 snapshot per CONTEXT D-02 analytical formula. Recommendation: read `perBatchLatency.p95` and `evalScore.within20km/total` from `run-1-throttle-snapshot.json`. If Run-1 eval was clean (within20km ratio ≥ 0.65), default 4 is safe; if Run-1 was tight, default stays at 2 and the env var becomes operator-tunable for future runs. Document the chosen number in the Zod comment.
    - Insert the new entry immediately after `LLM_V3_CONCURRENCY` (~line 72). Use the verbatim shape from 30-PATTERNS.md section "server/config.ts" (Pattern 2):
      ```typescript
      // Phase 30 D-07 — promoted from hard-coded `const BATCH_SIZE = 2` at
      // server/lib/llmEventExtractor.v3.ts:83. Sized against measured NIM throttle
      // (Run 1 data, see .planning/phases/30-.../run-1-throttle-snapshot.json and
      // Run 2 confirmation in Plan 06).
      //
      // Tuning knob:
      //   - LLM_BATCH_SIZE=2   prior v1.4 default (sized for Hobby 300s ceiling)
      //   - LLM_BATCH_SIZE=4-8 candidate v1.5 default (Run-1-derived)
      // Higher batch sizes carry more groups per LLM call but risk context-window
      // pressure on qwen-235b for verbose groups; the Plan 06 ±3pp eval gate
      // bounds the upper end.
      LLM_BATCH_SIZE: z.coerce.number().int().positive().default(<RUN_1_DERIVED>),
      ```
      Replace `<RUN_1_DERIVED>` with the chosen number (e.g., `4`).
    - Update `LLM_V3_CONCURRENCY` default (~line 72): replace `.default(12)` with `.default(<RUN_1_DERIVED>)` per CONTEXT D-02 analytical formula `(observed_NIM_steady_RPM × measured_batch_latency_seconds) / 60`. Read `steadyStateRpm` and `perBatchLatency.p50` from Run-1 snapshot; the resulting number is the proposal. Update the surrounding docblock to note "Phase 30 D-02 retune; was 12 (sized for Hobby 300s pressure); current default (Phase 30) Run-1-derived = <RUN_1_DERIVED>."
    - Update `LLM_BATCH_TIMEOUT_MS` default (~line 56): replace `.default(90_000)` with `.default(<RUN_1_DERIVED>)` per CONTEXT D-02 formula `max(2 × measured_batch_latency_p95, observed_throttle_window + 30s)`. Read `perBatchLatency.p95` and `throttleWindowMs.p95` from Run-1 snapshot. Update the surrounding comment.
    - Verify Zod chain shape: `z.coerce.number().int().positive().default(N)` — RESEARCH gotcha 2 forbids `z.number()` (rejects string env-var values) or `z.number().min(1)` (allows non-integer).

    Step 2 (.env.example block):
    - Add a new tuning-knob block AFTER the existing `LLM_V3_CONCURRENCY=12` line (~line 135) — mirror the existing block structure (~10 lines of comment + value line). Use the verbatim shape from 30-PATTERNS.md `.env.example` section:
      ```
      # Phase 30 D-07: LLM batch size — promoted from hard-coded constant.
      # Sized against measured NIM throttle (Run 1 data, see
      # docs/architecture/llm-pipeline-reliability.md). Larger batches reduce
      # total batch count and raw call surface; smaller batches improve schema
      # adherence on verbose qwen-235b groups.
      #
      # Tuning knob:
      #   - LLM_BATCH_SIZE=2   prior v1.4 default (sized for Hobby 300s)
      #   - LLM_BATCH_SIZE=4-8 v1.5 candidates (Plan 06 ±3pp eval gate bounds upper end)
      LLM_BATCH_SIZE=<RUN_1_DERIVED>
      ```
    - Update the existing `LLM_V3_CONCURRENCY=12` line to the new value and refresh its commentary block.
    - Update the existing `LLM_BATCH_TIMEOUT_MS=90000` line to the new value and refresh its commentary block.

    Step 3 (config.test.ts case):
    - Mirror the existing `EVENT_CONFIDENCE_THRESHOLD` test at lines 76-83. Add a new `it('honors LLM_BATCH_SIZE env var override', async () => { ... })` block:
      ```typescript
      it('honors LLM_BATCH_SIZE env var override', async () => {
        process.env.UPSTASH_REDIS_REST_URL = 'https://fake-redis.upstash.io';
        process.env.UPSTASH_REDIS_REST_TOKEN = 'fake-token-123';
        process.env.LLM_BATCH_SIZE = '4';
        const { env } = await import('../config.js');
        expect(env.LLM_BATCH_SIZE).toBe(4);
      });
      ```
    - Plus a fall-back test: `it('falls back to default when LLM_BATCH_SIZE unset', async () => { ... delete process.env.LLM_BATCH_SIZE; const { env } = await import('../config.js'); expect(env.LLM_BATCH_SIZE).toBe(<RUN_1_DERIVED>); });`
    - Plus an invalid-value test: mirror the "falls back to defaults for invalid numeric env vars" existing pattern.

    Step 4 (npm run check:env sanity):
    - Run `npm run check:env` locally — confirms `.env.example` parses against the updated Zod schema. If it fails, the diff has a typo.

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/config.test.ts &amp;&amp; npm run check:env</automated>
    <criteria>
      - `grep -q 'LLM_BATCH_SIZE: z.coerce.number().int().positive().default' server/config.ts` exits 0
      - `grep -q 'LLM_BATCH_SIZE=' .env.example` exits 0
      - `grep -q 'LLM_BATCH_SIZE' server/__tests__/config.test.ts` exits 0
      - `npm run check:env` exits 0
      - `npx vitest run server/__tests__/config.test.ts` exits 0 with all tests green (including 3+ new LLM_BATCH_SIZE cases)
      - `npx tsc --noEmit` exits 0
    </criteria>
  </verify>
  <done>
    - server/config.ts has new LLM_BATCH_SIZE Zod entry + updated defaults for LLM_V3_CONCURRENCY and LLM_BATCH_TIMEOUT_MS
    - .env.example has new LLM_BATCH_SIZE block + refreshed commentary on the two updated existing knobs
    - server/__tests__/config.test.ts has 3+ new LLM_BATCH_SIZE cases (override / fallback / invalid)
    - npm run check:env passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote v3 extractor BATCH_SIZE to env.LLM_BATCH_SIZE + extend incrementalWrite.test.ts</name>
  <files>server/lib/llmEventExtractor.v3.ts, server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts</files>
  <read_first>
    - server/lib/llmEventExtractor.v3.ts lines 1-100 (confirm `env` import at ~line 28 — RESEARCH §Pattern 2 says already present); lines 80-90 (the current `const BATCH_SIZE = 2;` site + its docblock at lines 80-82)
    - server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts lines 1-50 (hoisted mockEnv at lines 29-41 — must add LLM_BATCH_SIZE field) + lines 360-400 (the existing test block where the new consumer test lands)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md section "server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts" — the `LLM_BATCH_SIZE` consumer test recipe
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md "Validation Architecture" / "LLM_BATCH_SIZE env var honored" requirement
  </read_first>
  <behavior>
    - When env.LLM_BATCH_SIZE = 4, the v3 extractor's processEventGroups receives batches of size 4 (not the prior hard-coded 2)
    - The change is structural — no behavior change in test runs that use the default; the variable just becomes operator-tunable
    - Existing tests that pass `mockEnv.LLM_BATCH_SIZE = N` continue to work (hoisted mockEnv pattern proven)
  </behavior>
  <action>
    Step 1 (consumer — server/lib/llmEventExtractor.v3.ts ~line 83):
    - Confirm `env` is already imported (~line 28). If not, add `import { env } from '../config.js';` to the imports.
    - Locate the `const BATCH_SIZE = 2;` line (~line 83). Replace with `const BATCH_SIZE = env.LLM_BATCH_SIZE;`.
    - Keep the surrounding D-10 docblock comment at lines 80-82 (about "each group now carries far more context"). Append one line to that docblock: `// Phase 30 D-07: now env-tunable via LLM_BATCH_SIZE (defaults to value above).`

    Step 2 (test extension — server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts):
    - In the hoisted `mockEnv` object (~lines 29-41), add `LLM_BATCH_SIZE: <RUN_1_DERIVED>,` (use same value as the new Zod default, matches what consumer expects).
    - Add new `it()` block per 30-PATTERNS.md:
      ```typescript
      it('LLM_BATCH_SIZE env-tunable: extractor reads env.LLM_BATCH_SIZE not hard-coded 2', async () => {
        mockEnv.LLM_BATCH_SIZE = 4;
        await driveRun(8);
        // Assertion: processEventGroups was driven with batches of size 4
        // (mock's totalBatchesForRun = 8 events / 4 batchSize = 2 batches).
        expect(processEventGroupsMock).toHaveBeenCalled();
      });
      ```
    - DO NOT in this plan flip the prior every-N-flush assertions to no-flush — that is Plan 03's deletion (Wave 5 per Option A ordering). This plan only adds the LLM_BATCH_SIZE consumer test; Plan 03 rewrites the broader test file's incrementalWrite assertions and re-uses the mockEnv shape established here.

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts</automated>
    <criteria>
      - `grep -q 'env.LLM_BATCH_SIZE' server/lib/llmEventExtractor.v3.ts` exits 0
      - `! grep -q '^const BATCH_SIZE = 2;' server/lib/llmEventExtractor.v3.ts` exits 0 (old hard-coded line gone)
      - `grep -q 'LLM_BATCH_SIZE' server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
      - `npx vitest run server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0 with existing + new tests green
      - `npx tsc --noEmit` exits 0
    </criteria>
  </verify>
  <done>
    - v3 extractor reads env.LLM_BATCH_SIZE (hard-coded const replaced)
    - incrementalWrite.test.ts mockEnv includes LLM_BATCH_SIZE + new consumer test
    - tsc clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tune freeClaudeRouter RETRY_ATTEMPTS / BACKOFF_MS / JITTER_MS from Run-1 throttle window</name>
  <files>server/lib/freeClaudeRouter.ts</files>
  <read_first>
    - server/lib/freeClaudeRouter.ts lines 60-100 (current constants block; the BACKOFF_MS array at line 65, RETRY_ATTEMPTS at line 64, JITTER_MS at line 66)
    - server/lib/freeClaudeRouter.ts lines 220-250 (the jitter consumer site at lines 233-238 — confirm shape; jitter is computed off BACKOFF_MS[attempt])
    - server/lib/freeClaudeRouter.ts lines 390-490 (retry loop — confirm BACKOFF_MS array indexing with `[attempt] ?? [0] ?? 1000` fallback chain, mention RETRY_ATTEMPTS at line 442)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json (read .throttleWindowMs.median and .throttleWindowMs.p95)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md D-02 (the analytical formulas)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md section "server/lib/freeClaudeRouter.ts" + "D-02 tuning targets"
  </read_first>
  <behavior>
    - BACKOFF_MS array has either 2 or 3 elements depending on RETRY_ATTEMPTS (2 elements if RETRY_ATTEMPTS stays at 2; 3 elements if it bumps to 3)
    - BACKOFF_MS[0] = round(throttle_window_median / 2) — so the first retry lands inside the recovery window
    - BACKOFF_MS[1] = round(throttle_window_median × 2) or BACKOFF_MS[0] × 4 (preserving the current ~4× scaling pattern)
    - BACKOFF_MS[2] (if present) = BACKOFF_MS[1] × 4 or similar exponential scaling
    - JITTER_MS = round(BACKOFF_MS[0] × 0.25) — preserves the existing ±25% jitter ratio
    - The constant names stay the same (operators know them per CONTEXT <specifics>); only the numeric values change. Rename to BACKOFF_BASE_MS only if a RESEARCH or test failure demands it.
  </behavior>
  <action>
    Step 1 (read Run-1 numbers):
    - Open `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json`. Read `.throttleWindowMs.median` and `.throttleWindowMs.p95`. If `.throttleWindowMs.path === 'B' && .median === null` (zero 429s in Run 1 per RESEARCH Open Question 2), treat this plan as a sanity-check tune: keep RETRY_ATTEMPTS=2, BACKOFF_MS=[1000, 4000], JITTER_MS=250 (current values). Document in the commit body why no tune happened. Skip to Step 2.

    Step 2 (constants update — server/lib/freeClaudeRouter.ts ~lines 63-66):
    - Compute new values per CONTEXT D-02:
      - `BACKOFF_MS[0]` = `Math.round(throttle_window_median / 2)` (e.g., if median=1500ms, BACKOFF_MS[0]=750ms)
      - `BACKOFF_MS[1]` = `BACKOFF_MS[0] × 4` (preserves the existing 1000→4000 4× scaling)
      - If choosing `RETRY_ATTEMPTS=3`: append `BACKOFF_MS[2] = BACKOFF_MS[1] × 4`
      - `JITTER_MS` = `Math.round(BACKOFF_MS[0] × 0.25)` (preserves existing ratio ~25%)
    - Replace the constants block. Add a comment above each updated constant: `// Phase 30 D-02: tuned from Run-1 throttle window median=Xms, p95=Yms`.
    - Decide `RETRY_ATTEMPTS`: per CONTEXT D-02, "may increase from 2 → 3 since the 800s budget now allows it without watchdog conflict". Pick 3 unless Run 1 produced enough 429s to suggest 2 attempts are already finding the recovery window — document the choice in commit body.

    Step 3 (verify jitter consumer site — same file ~lines 233-238):
    - The jitter consumer reads `BACKOFF_MS[attempt]` and adds ±JITTER_MS — no shape change required since the constant names stay the same. Just verify the array indexing fallback chain still safely handles attempt < BACKOFF_MS.length.

    Step 4 (commit body preparation — operator drafts):
    - In the eventual commit message body (one per CONTEXT D-08), include the side-by-side table:
      ```
      Pre-Phase-30 (v1.4 defaults, sized for Hobby 300s ceiling):
        LLM_V3_CONCURRENCY = 12
        BATCH_SIZE = 2 (hard-coded)
        LLM_BATCH_TIMEOUT_MS = 90_000
        RETRY_ATTEMPTS = 2
        BACKOFF_MS = [1000, 4000]
        JITTER_MS = 250

      Post-Phase-30 (v1.5 defaults, Run-1-derived against Pro 800s ceiling):
        LLM_V3_CONCURRENCY = <NEW>
        LLM_BATCH_SIZE = <NEW> (env-tunable, was hard-coded)
        LLM_BATCH_TIMEOUT_MS = <NEW>
        RETRY_ATTEMPTS = <NEW>
        BACKOFF_MS = <NEW_ARRAY>
        JITTER_MS = <NEW>

      Run 1 measurements (from run-1-throttle-snapshot.json):
        - Throttle window: Path X, median=Yms, p95=Zms
        - Steady-state RPM (NIM): N
        - Recovery interval: Mms
        - Per-batch latency p50/p95: A / B ms
        - Watchdog hard-kill count: K

      Rollback recipe (operator env): LLM_V3_CONCURRENCY=12 LLM_BATCH_SIZE=2 LLM_BATCH_TIMEOUT_MS=90000 reverts to v1.4 behavior modulo Plan 04 soft-warn deletion (code-only — requires git revert).
      ```

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts &amp;&amp; npx tsc --noEmit</automated>
    <criteria>
      - `grep -q 'BACKOFF_MS = \[' server/lib/freeClaudeRouter.ts` exits 0 (array literal preserved, new values inside)
      - `grep -q 'JITTER_MS' server/lib/freeClaudeRouter.ts` exits 0 (constant name preserved)
      - `grep -q 'RETRY_ATTEMPTS' server/lib/freeClaudeRouter.ts` exits 0
      - `grep -q 'Phase 30 D-02' server/lib/freeClaudeRouter.ts` exits 0 (tuning rationale comment present)
      - `npx tsc --noEmit` exits 0
      - `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 (existing router tests survive the constant change — retry logic shape unchanged, only values)
      - `npx vitest run server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts` exits 0 (Plan 01 invariant)
    </criteria>
  </verify>
  <done>
    - BACKOFF_MS array contains Run-1-derived numbers (or unchanged values with documented rationale if Run 1 yielded zero 429s)
    - JITTER_MS = 0.25 × BACKOFF_MS[0]
    - RETRY_ATTEMPTS reflects the operator choice (2 or 3) with rationale in commit body
    - Inline `// Phase 30 D-02` comment names the source data
    - Existing router tests (and Plan 01's retryAfterMs test) still pass
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                |
| -------------------------------------- | ------------------------------------------------------------------------------------------ |
| Operator-set env vars → Zod parseEnv   | LLM_BATCH_SIZE accepts arbitrary positive int; NIM rate-limit ceiling is the real backstop |
| Internal constants → router retry loop | BACKOFF_MS array is private to the router; no external input flows here                    |

## STRIDE Threat Register

| Threat ID | Category          | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                              |
| --------- | ----------------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-30-05   | Denial of Service | Operator sets LLM_BATCH_SIZE arbitrarily large | accept      | NIM rate-limit hard ceiling (40rpm free tier) is the real backstop. Zod `z.coerce.number().int().positive()` rejects negative / non-int / NaN — same shape as LLM_V3_CONCURRENCY. Operator can't accidentally set BATCH_SIZE=0 (Zod rejects). Severity: LOW. |
| T-30-04   | Denial of Service | LLM_BATCH_TIMEOUT_MS bumped to 120-180s        | accept      | Even at 180s × ceil(196/lower-CONCURRENCY) batches, total wall-clock stays well inside Pro 800s ceiling (Pitfall 4 math). New ceiling = max(2 × measured_p95, throttle_window + 30s) — derived from data, not extended speculatively. Severity: LOW.         |
| T-30-05a  | Tampering         | Empty env var fallthrough on LLM_BATCH_SIZE    | accept      | parseEnv() throws on missing required vars; LLM_BATCH_SIZE has a hard-coded default, so empty env → default value (not throw). Matches existing LLM_V3_CONCURRENCY behavior.                                                                                 |

</threat_model>

<verification>
- All Task 1 acceptance criteria pass (Zod entry + .env.example + 3+ unit tests green; npm run check:env passes)
- All Task 2 acceptance criteria pass (consumer reads env.LLM_BATCH_SIZE; mockEnv pattern works)
- All Task 3 acceptance criteria pass (BACKOFF_MS array updated with Run-1-derived numbers OR documented sanity-check case)
- `npx tsc --noEmit` exits 0 across full repo
- `npx vitest run server/__tests__/config.test.ts server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts server/__tests__/lib/freeClaudeRouter.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
- Commit message body quotes pre/post defaults table per Task 3 Step 4
</verification>

<success_criteria>

- `grep -q 'LLM_BATCH_SIZE' server/config.ts && grep -q 'LLM_BATCH_SIZE' .env.example && grep -q 'env.LLM_BATCH_SIZE' server/lib/llmEventExtractor.v3.ts` all exit 0
- Updated LLM_V3_CONCURRENCY and LLM_BATCH_TIMEOUT_MS defaults reflect Run-1-derived numbers (or documented retention rationale)
- Updated freeClaudeRouter constants reflect Run-1-derived numbers (or documented retention rationale)
- Commit lands with message `feat(30): tune LLM_V3_CONCURRENCY / LLM_BATCH_SIZE / backoff against measured throttle (D-02 / LLM-RELI-03 / LLM-RELI-04)` and body containing pre/post side-by-side table
- `npm run check:env` passes (Zod schema and .env.example stay in sync)
- All listed vitest suites exit 0
  </success_criteria>

<output>
After completion, create `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-05-SUMMARY.md` documenting:
- The chosen RUN_1_DERIVED values for: LLM_BATCH_SIZE, LLM_V3_CONCURRENCY, LLM_BATCH_TIMEOUT_MS, RETRY_ATTEMPTS, BACKOFF_MS, JITTER_MS
- The exact formula application (e.g. "median=1500ms → BACKOFF_MS[0] = 750ms")
- Whether RETRY_ATTEMPTS was kept at 2 or bumped to 3, with rationale
- Whether this run hit the sanity-check branch (zero 429s in Run 1 → no tune) — if so, Plan 06 still validates the unchanged values against ±3pp eval gate
- Pointer to the pre/post defaults table for Plan 07's reliability doc to consume verbatim
- Pointer to Plan 06 for what to expect when validating these defaults
</output>

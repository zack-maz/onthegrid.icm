---
phase: 30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/freeClaudeRouter.ts
  - server/lib/llmProgress.ts
  - scripts/analyze-llm-run.ts
  - package.json
  - server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts
  - tests/fixtures/run-with-retry-after.json
  - tests/fixtures/run-without-retry-after.json
autonomous: true
requirements: [LLM-RELI-02]
requirements_addressed: [LLM-RELI-02]
gsd_commit: 'feat(30): add retryAfterMs + scripts/analyze-llm-run.ts (D-01)'
must_haves:
  truths:
    - 'callHistory rows on 429 NIM responses carry a retryAfterMs value when the Retry-After header is present, or null when absent'
    - 'scripts/analyze-llm-run.ts reads events:llm-summary:v3 and emits a JSON snapshot + Markdown table for both Path A (header) and Path B (inferred from timestamp gaps)'
    - 'npm run analyze:llm-run executes the analyzer with the standard tsx/esm runner'
  artifacts:
    - path: 'server/lib/freeClaudeRouter.ts'
      provides: 'retryAfterMs capture in 429 catch block; widened callHistory writer'
      contains: 'retryAfterMs'
    - path: 'server/lib/llmProgress.ts'
      provides: 'retryAfterMs?: number | null on callHistory row type (LLMPipelineProgress and LLMRunSummary)'
      contains: 'retryAfterMs'
    - path: 'scripts/analyze-llm-run.ts'
      provides: 'Operator-facing post-run analyzer; zero token spend; reads Redis only'
      min_lines: 80
    - path: 'server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts'
      provides: 'Wave-0 Path A + Path B unit coverage'
      contains: 'retryAfterMs'
    - path: 'tests/fixtures/run-with-retry-after.json'
      provides: 'LLMRunSummary fixture: retryAfterMs present on 429 rows (Path A)'
    - path: 'tests/fixtures/run-without-retry-after.json'
      provides: 'LLMRunSummary fixture: retryAfterMs null on 429 rows (Path B - inferred from gaps)'
    - path: 'package.json'
      provides: 'analyze:llm-run npm script'
      contains: 'analyze:llm-run'
  key_links:
    - from: 'server/lib/freeClaudeRouter.ts catch block (~line 448)'
      to: 'llmProgress.callHistory[] schema (server/lib/llmProgress.ts:79-100)'
      via: 'updateProgress({ callHistory: [{ ..., retryAfterMs }, ...] }) widened atomically'
      pattern: 'retryAfterMs'
    - from: 'scripts/analyze-llm-run.ts'
      to: 'events:llm-summary:v3'
      via: "cacheGetSafe<LLMRunSummary>('events:llm-summary:v3', 999_999_999)"
      pattern: 'events:llm-summary:v3'
---

<objective>
Land the Wave-0 telemetry + analyzer infrastructure required before any force-triggered measurement run can produce signal. This plan adds one optional field (`retryAfterMs?: number | null`) to the existing `callHistory[]` schema, populates it in the NIM 429 catch path of `freeClaudeRouter.ts`, and ships a Redis-read-only post-run analyzer (`scripts/analyze-llm-run.ts`) that emits both a JSON snapshot and a Markdown table — handling both Path A (Retry-After header present) and Path B (header absent → infer from timestamp gaps) per RESEARCH Pitfall 2.

Purpose: Without this plan, Run 1 (Plan 02) produces no actionable throttle telemetry — `callHistory` rows lack `retryAfterMs` and the analyzer script doesn't exist (RESEARCH Pitfall 1). All seven Phase 30 commits chain off this one.

Output:

- `retryAfterMs?: number | null` on `callHistory[]` rows in both `LLMPipelineProgress` and `LLMRunSummary` types
- `retryAfterMs` populated from `error.headers['retry-after']` in `server/lib/freeClaudeRouter.ts` 429 catch block
- `scripts/analyze-llm-run.ts` reading `events:llm-summary:v3` with `--snapshot=<path>` arg
- `npm run analyze:llm-run` script entry mirroring `scripts/eval-replay.ts` invocation
- Wave-0 unit test + smoke fixtures for both Path A and Path B
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-VALIDATION.md
@scripts/eval-replay.ts
@server/lib/freeClaudeRouter.ts
@server/lib/llmProgress.ts
@server/__tests__/lib/freeClaudeRouter.test.ts

<interfaces>
<!-- callHistory row type, current (pre-D-01) - server/lib/llmProgress.ts:79-100 -->
type CallHistoryRow = {
  provider: 'cerebras' | 'groq' | 'nvidia_nim' | 'openrouter';
  model: string;
  tokensIn: number;
  tokensOut: number;
  durationMs: number;
  ok: boolean;
  batchSize: number;
  timestamp: number;
  routingReason?: 'primary' | string;
  skipReason?:
    | 'breaker'
    | 'hard_cap'
    | 'no_client'
    | 'rate_limit_window'
    | 'daily_cap'
    | 'watchdog-soft-warn';
  // D-01 ADDS: retryAfterMs?: number | null;
};

<!-- Force-trigger Bearer entry point (referenced — not modified here): -->
<!-- GET /api/cron/refresh-events?force=true (server/routes/refresh-events-cron.ts:37-87) -->

<!-- existing tsx runner pattern - package.json:24 -->

"eval:replay": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --import tsx/esm scripts/eval-replay.ts"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen callHistory schema + capture retryAfterMs in freeClaudeRouter 429 catch block</name>
  <files>server/lib/llmProgress.ts, server/lib/freeClaudeRouter.ts, server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts</files>
  <read_first>
    - server/lib/llmProgress.ts (full file — find the `callHistory` row Zod/TS shape at lines 79-100 AND the `LLMRunSummary` mirror at lines 265-394; both must be widened atomically)
    - server/lib/freeClaudeRouter.ts (lines 1-100 for imports + constants; lines 400-500 for the 429 catch block where the new code lands)
    - server/__tests__/lib/freeClaudeRouter.test.ts (the analog test — copy the hoisted mock preamble exactly)
    - server/lib/llmEventExtractor.v3.ts lines 661-683 (the existing soft-warn synthetic callHistory entry — closest analog for "append new field to callHistory row")
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md (section "server/lib/freeClaudeRouter.ts" + section "server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts" — verbatim code to copy)
  </read_first>
  <behavior>
    - Test 1 (Path A): 429 error with `headers: {'retry-after': '1.5'}` produces callHistory row with `retryAfterMs === 1500` (parseFloat(raw) * 1000)
    - Test 2 (Path A capital-case header): `headers: {'Retry-After': '2'}` produces `retryAfterMs === 2000` (case-insensitive lookup)
    - Test 3 (Path B): 429 error with `headers: {}` produces callHistory row with `retryAfterMs === null`
    - Test 4 (Path B malformed value): 429 error with `headers: {'retry-after': 'not-a-number'}` produces `retryAfterMs === null` (parseFloat NaN guard)
    - Test 5 (non-429 path): a non-rate-limit error (e.g. network) produces `retryAfterMs === null` (only populated when bucket === 'rate_limit')
    - Test 6 (breaker semantics preserved): retryAfterMs capture does NOT introduce a duplicate `record(p.name, 'err')` call — breaker increments exactly once per call, not per attempt (regression guard per RESEARCH gotcha 2)
  </behavior>
  <action>
    Step 1 (schema widening — atomic, both sites):
    - In `server/lib/llmProgress.ts`, locate the `callHistory[]` row type at lines 79-100 (the `LLMPipelineProgress.callHistory` field) AND the mirror in `LLMRunSummary.callHistory` at lines 265-394. Add field `retryAfterMs?: number | null;` after the `skipReason?` enum in BOTH locations. Field-order convention: place `retryAfterMs` immediately after `skipReason`. One-line comment above the field: `// D-01 (Phase 30): NIM Retry-After header capture, milliseconds. Null when header absent (Path B).`
    - DO NOT remove the `'watchdog-soft-warn'` enum value from `skipReason` here — Plan 04 (D-05) handles that atomically with the watchdog deletion.

    Step 2 (writer — server/lib/freeClaudeRouter.ts):
    - Locate the 429/error catch block (~lines 448-477). The current shape: `} catch (err) { const latencyMs = Date.now() - t0; recordLatency(...); const bucket = classifyError(err); recordErrorBucket(...); log.warn({...}, 'router attempt failed'); if (bucket === 'rate_limit' && attempt < RETRY_ATTEMPTS - 1) { ... sleep + continue; } callFailed = true; break; }`
    - Insert immediately after the `recordErrorBucket(p.name, bucket);` line (and before the `log.warn` call) the retryAfterMs computation per 30-PATTERNS.md section "retryAfterMs capture (D-01) — proposed insertion site":
      ```typescript
      // D-01 (Phase 30): capture Retry-After from 429s when the provider supplies it.
      // OpenAI SDK surfaces response headers on APIError.headers; NIM-specific
      // header presence verified by Run 1 telemetry. Milliseconds; null when absent.
      let retryAfterMs: number | null = null;
      if (bucket === 'rate_limit' && err instanceof Error && 'headers' in err) {
        const headers = (err as { headers?: Record<string, string> }).headers;
        const raw = headers?.['retry-after'] ?? headers?.['Retry-After'];
        if (raw) {
          const parsed = parseFloat(raw);
          if (Number.isFinite(parsed) && parsed > 0) retryAfterMs = parsed * 1000;
        }
      }
      ```
    - In the existing `updateProgress({ callHistory: [...] })` writer in the catch block, add `retryAfterMs,` as a new field on the new row being prepended. DO NOT add a duplicate `record(p.name, 'err')` — the existing single recording at line 479 (the one that fires after retry exhaustion) is unchanged. The breaker-window single-record semantics are invariant per RESEARCH gotcha 2.
    - Use `logger.child({ module: 'freeClaudeRouter' })` if not already child-scoped — never `console.*`.

    Step 3 (test — server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts NEW):
    - Create using the EXACT hoisted-mock preamble from `server/__tests__/lib/freeClaudeRouter.test.ts:1-49` (per 30-PATTERNS.md section "server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts" — `// @vitest-environment node`, `class MockOpenAI`, vi.mock for `openai`, `../../config.js`, `../../lib/logger.js`, `../../cache/redis.js`, `../../lib/llmCircuitBreaker.js`).
    - Add a `class MockAPIError extends Error` with `headers: Record<string, string>` constructor — per 30-PATTERNS.md.
    - Write the 6 tests above. For each, import `llmProgress` from `../../lib/llmProgress.js` and assert `llmProgress.callHistory?.[0]?.retryAfterMs` matches the expected value.
    - Use `beforeEach` to call any `__internal.resetTestState?.()` or equivalent to clear the in-memory `llmProgress` singleton (mirror the existing test's reset hook).

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts</automated>
    <criteria>
      - `grep -q 'retryAfterMs' server/lib/freeClaudeRouter.ts` exits 0
      - `grep -q 'retryAfterMs' server/lib/llmProgress.ts` exits 0
      - `grep -c 'retryAfterMs' server/lib/llmProgress.ts` returns ≥ 2 (both `LLMPipelineProgress.callHistory` row type AND `LLMRunSummary.callHistory` row type widened)
      - `npx tsc --noEmit` exits 0 (no type drift between writer and reader)
      - `npx vitest run server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts` exits 0 with 6 passing tests
      - `! grep -q 'record(p.name.*err' server/lib/freeClaudeRouter.ts | wc -l` confirms the breaker-record count is exactly 1 (regression guard for RESEARCH gotcha 2)
    </criteria>
  </verify>
  <done>
    - retryAfterMs?: number | null appears in both LLMPipelineProgress.callHistory and LLMRunSummary.callHistory row types
    - freeClaudeRouter.ts catch block populates retryAfterMs from headers['retry-after'] (case-insensitive) with parseFloat NaN guard
    - 6 unit tests pass (Path A success, capital-case, Path B empty headers, Path B malformed value, non-429 null, breaker-record idempotency)
    - npx tsc --noEmit passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scripts/analyze-llm-run.ts + fixtures + npm script wire-up</name>
  <files>scripts/analyze-llm-run.ts, package.json, tests/fixtures/run-with-retry-after.json, tests/fixtures/run-without-retry-after.json</files>
  <read_first>
    - scripts/eval-replay.ts (the analog — copy shebang + docblock + main + .catch + argv parsing patterns verbatim)
    - scripts/snapshot-v3-redis.ts (analog for committed-JSON-snapshot writer; lines 42-45 show the hard-coded phase dir vs the recommended --snapshot=<path> arg shape)
    - server/lib/llmProgress.ts lines 265-394 (LLMRunSummary shape — every field the analyzer reads must exist in this type)
    - server/cache/redis.ts (cacheGetSafe import)
    - server/lib/logger.ts (logger.child contract for structured logs vs. operator-facing stdout via console.log)
    - package.json (line 24 `eval:replay` script — invocation pattern to mirror)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md sections "scripts/analyze-llm-run.ts" + "tests/fixtures/*" + "run-N-throttle-snapshot.json"
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md sections "Pitfall 2" (Path A / Path B branching) + "LLMRunSummary consumed by analyzer"
  </read_first>
  <behavior>
    - Path A behavior: when ≥1 callHistory row has retryAfterMs > 0, throttleWindowMs.path === 'A', median + p95 computed from retryAfterMs values
    - Path B behavior: when all callHistory 429 rows have retryAfterMs === null, throttleWindowMs.path === 'B', median + p95 inferred from (next200.timestamp - last429.timestamp) gaps in callHistory ordered by timestamp
    - JSON snapshot contains exactly the 9 required top-level fields (runTimestamp, durationMs, batchCount, watchdogTimeoutCount, throttleWindowMs, steadyStateRpm, recoveryIntervalMs, perBatchLatency, evalScore)
    - Markdown table emitted to stdout includes labelled "Throttle window (Path A)" or "Throttle window (Path B)" line
    - --snapshot=<path> arg writes JSON to that path; absence writes JSON to stdout adjacent to Markdown
    - --fixture=<path> arg reads LLMRunSummary from a local JSON file instead of Redis (for smoke testing)
    - Missing events:llm-summary:v3 (Redis-mode) exits with code 1 + stderr message; missing fixture file (fixture-mode) exits with code 1 + stderr message
  </behavior>
  <action>
    Step 1 (fixtures — tests/fixtures/run-with-retry-after.json + run-without-retry-after.json):
    - Create `tests/fixtures/` directory if absent.
    - Both fixtures conform to LLMRunSummary shape (server/lib/llmProgress.ts:265-394). Use the seed shape from 30-PATTERNS.md section "tests/fixtures":
      - Both contain: `lastRun`, `groupCount: 196`, `batchCount: 98`, `geocodeCount`, `enrichedCount`, `durationMs: 600000`, `error: null`, `schemaVersion: "v3"`, `watchdogTimeoutCount: 0`, `evalScore: { within5km: 28, within20km: 35, within100km: 42, total: 50 }`, `callHistory: [...]`, `latencyHistogram.nvidia_nim: { p50: 27000, p95: 30000, p99: 45000, sparkline: [], samples: [] }`, `errorTaxonomy.nvidia_nim: { rate_limit: 12, timeout: 0, malformed_json: 0, schema_fail: 0, network: 0, upstream_500: 0, other: 0 }`.
      - `run-with-retry-after.json` (Path A): every 429-style row (where `ok: false` AND row otherwise resembles a rate-limit event) has `retryAfterMs: 1500`. Include at least 4 such 429 rows mixed with 200-status rows so median + p95 are computable.
      - `run-without-retry-after.json` (Path B): every 429-style row has `retryAfterMs: null`. Timestamps on rows are spaced so analyzer can infer recovery from gaps (alternate 429 then 200 with ~1800ms wall-clock gap between failure and next success).

    Step 2 (analyzer — scripts/analyze-llm-run.ts NEW):
    - File preamble: shebang `#!/usr/bin/env node`, multi-line docblock per 30-PATTERNS.md format. Docblock MUST state: phase 30 D-01, purpose, usage, cost = "zero token spend, Redis-read only (or fixture-mode for smoke)".
    - Imports: `import { cacheGetSafe } from '../server/cache/redis.js';`, `import type { LLMRunSummary } from '../server/lib/llmProgress.js';`, `import { logger } from '../server/lib/logger.js';`, `import { readFileSync } from 'fs';`.
    - Module-level child logger: `const log = logger.child({ module: 'analyze-llm-run' });`.
    - argv parsing (mirror eval-replay.ts:25-35 pattern):
      ```
      const fixtureArg = process.argv.find((a) => a.startsWith('--fixture='));
      const fixture = fixtureArg?.split('=')[1];
      const snapshotArg = process.argv.find((a) => a.startsWith('--snapshot='));
      const snapshotPath = snapshotArg?.split('=')[1];
      ```
    - Source selection: if `fixture` provided → `const summary: LLMRunSummary = JSON.parse(readFileSync(fixture, 'utf8'));`. Otherwise → `const cached = await cacheGetSafe<LLMRunSummary>('events:llm-summary:v3', 999_999_999); if (!cached?.data) { console.error('events:llm-summary:v3 missing or empty'); process.exit(1); } const summary = cached.data;`.
    - Core computations (per 30-RESEARCH.md Pitfall 2):
      - Path A test: any callHistory row with `typeof row.retryAfterMs === 'number' && row.retryAfterMs > 0` → Path A. Collect those values, compute median + p95 → `throttleWindowMs = { path: 'A', median, p95 }`.
      - Path B (else): walk callHistory in timestamp order. For each consecutive (failingRow, successRow) pair where failingRow.ok === false and successRow.ok === true and same provider, compute `successRow.timestamp - failingRow.timestamp`. Collect gaps, compute median + p95 → `throttleWindowMs = { path: 'B', median, p95 }`. If zero 429 rows found at all (per RESEARCH Open Question 2), throttleWindowMs.path = 'B', median = null, p95 = null.
      - steadyStateRpm: count of `row.ok === true && row.provider === 'nvidia_nim'` rows / (summary.durationMs / 60000). Round to integer.
      - recoveryIntervalMs: from the LAST `bucket === 'rate_limit'` row (or `ok === false` row) to the FIRST subsequent `ok === true` row, the gap in ms. Null if no such pair.
      - perBatchLatency: read directly from `summary.latencyHistogram?.nvidia_nim` → `{ p50, p95 }`.
      - evalScore: pass through from `summary.evalScore`.
      - watchdogTimeoutCount: pass through from `summary.watchdogTimeoutCount ?? 0`.
      - durationMs, batchCount: pass through.
      - runTimestamp: use `summary.lastRun`.
    - Output (two writes):
      - JSON: assemble final object per the required shape; write to `snapshotPath` if provided (`writeFileSync(snapshotPath, JSON.stringify(snapshot, null, 2) + '\n')`); else `console.log(JSON.stringify(snapshot, null, 2))`.
      - Markdown table to stdout (always): header `## LLM Run Analysis`, columns Metric | Value, rows: "Throttle window (Path {A|B})", "Steady-state RPM (NIM)", "Recovery interval (ms)", "Per-batch latency p50/p95 (ms)", "Watchdog hard-kill count", "Eval @ 5/20/100km".
    - Wrap in `async function main(): Promise<void> { ... } main().catch((err) => { console.error(err); process.exit(1); });` per eval-replay.ts:25/46 pattern.
    - **Backwards-compatibility:** the analyzer must tolerate `skipReason: 'watchdog-soft-warn'` in old summaries (per RESEARCH gotcha at llmEventExtractor.v3.ts widening — old rows live in Redis for ~90d after Plan 04 lands). Simply ignore unknown skipReason values; do not crash.

    Step 3 (npm script — package.json):
    - In the `"scripts"` block (after the existing `"eval:replay"` entry at line ~24), add: `"analyze:llm-run": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --import tsx/esm scripts/analyze-llm-run.ts"` (verbatim shape from eval-replay).

    Step 4 (smoke verification — manual, no test file):
    - Run `npm run analyze:llm-run -- --fixture=tests/fixtures/run-with-retry-after.json` — verify Markdown stdout contains "Throttle window (Path A)" and non-null numeric values. Verify exit code 0.
    - Run `npm run analyze:llm-run -- --fixture=tests/fixtures/run-without-retry-after.json` — verify Markdown stdout contains "Throttle window (Path B)" and non-null numeric values. Verify exit code 0.

  </action>
  <verify>
    <automated>npm run analyze:llm-run -- --fixture=tests/fixtures/run-with-retry-after.json &amp;&amp; npm run analyze:llm-run -- --fixture=tests/fixtures/run-without-retry-after.json</automated>
    <criteria>
      - `test -f scripts/analyze-llm-run.ts` exits 0
      - `test -f tests/fixtures/run-with-retry-after.json` exits 0
      - `test -f tests/fixtures/run-without-retry-after.json` exits 0
      - `grep -q 'analyze:llm-run' package.json` exits 0
      - `grep -q 'tsx/esm scripts/analyze-llm-run.ts' package.json` exits 0
      - `npm run analyze:llm-run -- --fixture=tests/fixtures/run-with-retry-after.json 2>&amp;1 | grep -q 'Path A'` exits 0
      - `npm run analyze:llm-run -- --fixture=tests/fixtures/run-without-retry-after.json 2>&amp;1 | grep -q 'Path B'` exits 0
      - `jq -e '.callHistory[] | select(.retryAfterMs)' tests/fixtures/run-with-retry-after.json` returns non-empty
      - `jq -e 'all(.callHistory[] | select(.ok == false); .retryAfterMs == null)' tests/fixtures/run-without-retry-after.json` returns true
      - `npx tsc --noEmit` exits 0
    </criteria>
  </verify>
  <done>
    - scripts/analyze-llm-run.ts exists with shebang, docblock, async main, and Path A + Path B branching
    - Both fixtures exist, conform to LLMRunSummary shape, and produce distinguishable Path A vs Path B output
    - npm run analyze:llm-run is wired in package.json with the tsx/esm runner pattern
    - Analyzer accepts --fixture=<path> for smoke testing and --snapshot=<path> for snapshot writing
    - Smoke runs against both fixtures exit 0 with correct Path labelling
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                                                      |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| OpenAI SDK → freeClaudeRouter catch block | Provider returns 429 with optional response headers; widened callHistory schema accepts unparsed strings only via parseFloat NaN guard           |
| Operator shell → analyzer script          | --fixture path is operator-supplied; readFileSync surfaces a path-traversal vector but script is operator-run only (not request-handler exposed) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| --------- | ---------------------- | ---------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-01   | Information Disclosure | callHistory widened with retryAfterMs          | accept      | retryAfterMs is a parsed number from a public HTTP response header; no PII, no auth material, no response body content. Bounded structured log already capped at 20 rows via existing `.slice(0, 20)`. Severity: LOW. |
| T-30-01a  | Tampering              | Header parsing in freeClaudeRouter catch block | mitigate    | parseFloat with `Number.isFinite(parsed) && parsed > 0` rejects NaN / negative / non-numeric values → retryAfterMs = null (safe default). Pattern matches existing JSON-parse-with-fallback discipline in router.     |
| T-30-08   | Denial of Service      | Analyzer reads events:llm-summary:v3           | accept      | cacheGetSafe wraps the Redis call with the established 2000ms withTimeout from server/cache/redis.ts. Hung Redis cannot block the script beyond that ceiling.                                                         |

</threat_model>

<verification>
- All Task 1 acceptance criteria pass (6 vitest tests green, tsc clean, grep counts match)
- All Task 2 acceptance criteria pass (analyzer smoke against both fixtures green, jq invariants on fixtures hold)
- `npx tsc --noEmit` exits 0 across the full repo
- `npx vitest run server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts` reports 6 passing tests
- No `console.*` calls in production code paths (analyzer's stdout `console.log` is the script's intentional output contract per CLAUDE.md logger discipline)
</verification>

<success_criteria>

- `grep -q 'retryAfterMs' server/lib/freeClaudeRouter.ts` AND `grep -q 'retryAfterMs' server/lib/llmProgress.ts` both exit 0
- `npx vitest run server/__tests__/lib/freeClaudeRouter.retryAfterMs.test.ts` exits 0 with 6 tests passing
- `test -f scripts/analyze-llm-run.ts && test -f tests/fixtures/run-with-retry-after.json && test -f tests/fixtures/run-without-retry-after.json` all true
- `npm run analyze:llm-run -- --fixture=tests/fixtures/run-with-retry-after.json` exits 0 and prints "Path A"
- `npm run analyze:llm-run -- --fixture=tests/fixtures/run-without-retry-after.json` exits 0 and prints "Path B"
- `npx tsc --noEmit` exits 0
- Commit lands with message `feat(30): add retryAfterMs + scripts/analyze-llm-run.ts (D-01)`
  </success_criteria>

<output>
After completion, create `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-01-SUMMARY.md` documenting:
- Files created vs modified (with line counts)
- Test count green vs total
- The exact retryAfterMs field comment text (so Plans 02 + 07 can quote it)
- Confirmation that `--fixture` and `--snapshot` arg shapes both work
- Anything that surprised vs. PATTERNS.md (e.g., if line numbers shifted from the cited values)
</output>

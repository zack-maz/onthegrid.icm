---
phase: 30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim
plan: 03
type: execute
wave: 5
depends_on: ['30-06']
files_modified:
  - server/lib/llmExtractionPipeline.ts
  - server/config.ts
  - .env.example
  - server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts
  - server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts
autonomous: true
requirements: [SIMPLIFY-01]
requirements_addressed: [SIMPLIFY-01]
gsd_commit: 'feat(30): retire incremental flush mechanism (SIMPLIFY-01 / D-04)'
must_haves:
  truths:
    - 'The onBatchComplete periodic-flush callback in server/lib/llmExtractionPipeline.ts (lines ~354-419 pre-deletion) no longer calls mergeAndPersistLlmEntities; the callback shrinks to just the existing progress-update + observability writePartialCache writes'
    - 'The end-of-run terminal mergeAndPersistLlmEntities call at server/lib/llmExtractionPipeline.ts:477 is preserved (canonical terminal write)'
    - 'LLM_FLUSH_EVERY_N_BATCHES is removed from server/config.ts Zod schema and from .env.example with its commentary block'
    - 'FLUSH_EVERY_N_BATCHES_DEFAULT constant and getFlushEveryNBatches helper (server/lib/llmExtractionPipeline.ts:88-106) are deleted'
    - 'Regression test asserts mergeAndPersistLlmEntities is called exactly once per successful runRefreshExtraction() (matches the existing runEval-called-once assertion shape in terminalShape.test.ts)'
    - 'Existing per-N-flush assertions in incrementalWrite.test.ts are replaced with no-flush assertions (exactly one terminal write per happy-path run regardless of batch count)'
    - "Commit message body quotes the pre/post Redis SET-call count delta for events:llm:v3 per cron run — sourced from Plan 06's Run 2 observation (CONTEXT D-04 audit signal)"
  artifacts:
    - path: 'server/lib/llmExtractionPipeline.ts'
      provides: 'onBatchComplete callback without periodic flush; FLUSH_EVERY_N_BATCHES_DEFAULT and getFlushEveryNBatches deleted; terminal write at line 477 preserved'
    - path: 'server/config.ts'
      provides: 'LLM_FLUSH_EVERY_N_BATCHES Zod entry removed'
    - path: '.env.example'
      provides: 'LLM_FLUSH_EVERY_N_BATCHES block (4-line comment + var line) removed'
    - path: 'server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts'
      provides: "New 'mergeAndPersistLlmEntities called exactly once' assertion (mirrors runEval-once assertion)"
      contains: 'mergeAndPersistLlmEntities'
    - path: 'server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts'
      provides: 'Per-N-flush assertions replaced with no-flush assertions; mockEnv strips LLM_FLUSH_EVERY_N_BATCHES property'
  key_links:
    - from: 'Plan 06 Run 2 analyzer / Vercel log Redis SET-count observation'
      to: "this plan's commit message body"
      via: 'operator pastes the pre/post delta number into the commit message'
      pattern: 'events:llm:v3'
    - from: 'server/lib/llmExtractionPipeline.ts onBatchComplete callback'
      to: 'server/lib/llmExtractionPipeline.ts mergeAndPersistLlmEntities terminal call at line 477'
      via: 'removal of periodic path leaves the terminal call as the sole writer of events:llm:v3'
      pattern: 'mergeAndPersistLlmEntities'
---

<objective>
Retire the Phase 28.2.6 incremental-flush mechanism (SIMPLIFY-01, CONTEXT D-04). The Pro 800s ceiling makes the 300s-budget-driven crash-protection rationale obsolete — Plan 06's Run 2 has just validated that the tuned defaults complete inside 800s without watchdog hard-kills exceeding Run 1's count. The single terminal-key write at end of run becomes the canonical shape. SET-call count for `events:llm:v3` per cron run drops measurably (~N → 1).

Why this plan is ordered AFTER Plan 06 (Wave 5, depends_on: [30-06]): Per CONTEXT D-08 the commit message body for this plan quotes the **measured** Redis SET-call delta — pre-deletion (Run 2 observation) vs post-deletion (1 terminal SET only). Sequencing this plan after Plan 06's Run 2 gives the commit message hard numbers instead of "delta captured in follow-up" placeholder text. CONTEXT D-08 explicitly authorizes this reordering: "Commit 3 vs Commit 6 dependency note: Plan 03's commit message references SET-count delta numbers that come from Plan 06's analyzer output. Recommend Option A; document in Plan 03 frontmatter as `depends_on: [Plan 06]`."

This is a CODE + TEST plan — no force-trigger fires here. The Plan 06 force-trigger has already produced the SET-count number. The current plan deletes the periodic-flush surface, replaces the broken tests, and commits with the audit-signal number in the body.

Purpose: Single source of truth (canonical end-of-run write) reduces Redis command-budget pressure (the operator's ongoing concern per STATE.md "Redis command budget at ~92%"), removes one degree of freedom from the pipeline, and aligns the cron-only writer shape with its documented contract (CLAUDE.md: cron is the sole writer of `events:llm:v3`).

Output:

- Periodic-flush block at `server/lib/llmExtractionPipeline.ts:~354-419` collapses to just the `updateProgress({ completedBatches: completed, totalBatches: total })` line + a one-line comment naming SIMPLIFY-01
- `FLUSH_EVERY_N_BATCHES_DEFAULT = 10` constant + `getFlushEveryNBatches()` helper at `server/lib/llmExtractionPipeline.ts:88-106` are deleted
- `LLM_FLUSH_EVERY_N_BATCHES` Zod schema entry deleted from `server/config.ts` (~line 81)
- `.env.example` LLM_FLUSH_EVERY_N_BATCHES block (4-line comment + var line, ~lines 137-141) deleted
- `terminalShape.test.ts` gets new "mergeAndPersistLlmEntities exactly once" assertion (mirroring existing runEval-once)
- `incrementalWrite.test.ts` per-N-flush assertions replaced with no-flush assertions (per Pitfall 7: assert ONLY in the happy-path, not in early-return / soft-cap-paused branches)
- Commit body quotes pre/post Redis SET-call count delta
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-VALIDATION.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-06-SUMMARY.md
@server/lib/llmExtractionPipeline.ts
@server/config.ts
@.env.example
@server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts
@server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts

<interfaces>
<!-- Pre-deletion (server/lib/llmExtractionPipeline.ts:88-106): -->
const FLUSH_EVERY_N_BATCHES_DEFAULT = 10;
function getFlushEveryNBatches(): number {
  const parsed = env.LLM_FLUSH_EVERY_N_BATCHES;
  if (!Number.isFinite(parsed) || parsed < 1) return FLUSH_EVERY_N_BATCHES_DEFAULT;
  return parsed;
}

<!-- Pre-deletion onBatchComplete callback (server/lib/llmExtractionPipeline.ts:~354-419): -->

const flushEvery = getFlushEveryNBatches();
let batchesSinceLastFlush = 0;
let lastFlushedEventCount = 0;

const extractResult = await processEventGroups(
prioritizedGroups,
async (completed, total) => {
updateProgress({ completedBatches: completed, totalBatches: total });
batchesSinceLastFlush++;
if (batchesSinceLastFlush < flushEvery) return;
batchesSinceLastFlush = 0;
// ... 50+ lines: partial-cache read, slice, geocode, adapt, merge-and-persist ...
},
);

<!-- Post-deletion (target): -->

const extractResult = await processEventGroups(
prioritizedGroups,
async (completed, total) => {
updateProgress({ completedBatches: completed, totalBatches: total });
// Phase 30 D-04 (SIMPLIFY-01): incremental flush retired. Terminal write at line 477 is canonical.
// Partial-cache observability write stays in v3 extractor's writePartialCache (SIMPLIFY-02 / Phase 34).
},
);

<!-- Terminal call (server/lib/llmExtractionPipeline.ts:477) — PRESERVED VERBATIM -->

const result = await mergeAndPersistLlmEntities(extractResult.entities, llmCachedRef, cacheKey);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete onBatchComplete periodic-flush block + FLUSH_EVERY_N_BATCHES constants/helper + reword helper docblock</name>
  <files>server/lib/llmExtractionPipeline.ts</files>
  <read_first>
    - server/lib/llmExtractionPipeline.ts (full file — confirm current line numbers: FLUSH_EVERY_N_BATCHES_DEFAULT block at ~88-106; mergeAndPersistLlmEntities helper docblock at ~108-129; periodic-flush block at ~354-419; terminal call at ~477; early-return / soft-cap-paused branches at ~289-302 and ~306-320 per Pitfall 7)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md section "server/lib/llmExtractionPipeline.ts" (verbatim deletion targets and the replacement callback shape) + sections "Anti-pattern (gotcha 1)" through "(gotcha 3)" under the same heading
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md Pitfall 7 (test scoping invariant)
  </read_first>
  <behavior>
    - After deletion, the onBatchComplete callback contains ONLY the `updateProgress({ completedBatches, totalBatches })` call + the new SIMPLIFY-01 comment
    - mergeAndPersistLlmEntities helper at lines 130-148 is NOT deleted — only its periodic callsite is. The terminal callsite at line 477 stays.
    - The helper's docblock at lines 108-129 is reworded to remove the "periodic flush hook (every N batches inside the IIFE)" mention (RESEARCH gotcha 3)
    - The lastFlushedEventCount + batchesSinceLastFlush closures at lines ~355-356 are removed (RESEARCH gotcha 1)
    - The callback signature stays `async (completed, total) => void` so test harness mocks at incrementalWrite.test.ts:167-171 still drive it (RESEARCH gotcha 1)
    - writePartialCache (inside the v3 extractor, NOT in this pipeline file) is untouched (RESEARCH gotcha 2; that's SIMPLIFY-02 / Phase 34)
  </behavior>
  <action>
    Step 1 (constants + helper deletion — server/lib/llmExtractionPipeline.ts lines ~88-106):
    - Delete the entire 19-line block:
      ```typescript
      // Phase 28.2.6 Plan 01 — incremental terminal-key write helpers.
      // ...
      const FLUSH_EVERY_N_BATCHES_DEFAULT = 10;
      function getFlushEveryNBatches(): number {
        const parsed = env.LLM_FLUSH_EVERY_N_BATCHES;
        if (!Number.isFinite(parsed) || parsed < 1) return FLUSH_EVERY_N_BATCHES_DEFAULT;
        return parsed;
      }
      ```

    Step 2 (helper docblock reword — server/lib/llmExtractionPipeline.ts lines ~108-129):
    - Locate the `mergeAndPersistLlmEntities` JSDoc at lines 108-129. Remove any reference to "periodic flush hook" or "every N batches inside the IIFE" (per RESEARCH gotcha 3). The helper is now single-purpose (terminal write).
    - Add one line to the docblock: `// Phase 30 D-04: single callsite (end-of-run terminal write only). Periodic flush retired (SIMPLIFY-01).`

    Step 3 (periodic-flush block deletion — server/lib/llmExtractionPipeline.ts lines ~354-419):
    - Locate the `const flushEvery = getFlushEveryNBatches();` line + the `let batchesSinceLastFlush = 0;` + `let lastFlushedEventCount = 0;` closures. Delete all three declarations.
    - Locate the `await processEventGroups(prioritizedGroups, async (completed, total) => { ... })` call. The callback body currently looks like:
      ```typescript
      async (completed, total) => {
        updateProgress({ completedBatches: completed, totalBatches: total });
        batchesSinceLastFlush++;
        if (batchesSinceLastFlush < flushEvery) return;
        batchesSinceLastFlush = 0;
        // ~50 lines: partial-cache read, slice, geocode, adapt, merge-and-persist
      },
      ```
      Replace with:
      ```typescript
      async (completed, total) => {
        updateProgress({ completedBatches: completed, totalBatches: total });
        // Phase 30 D-04 (SIMPLIFY-01): incremental flush retired. Terminal write at the
        // mergeAndPersistLlmEntities call below is canonical. Partial-cache observability
        // write stays in v3 extractor's writePartialCache (SIMPLIFY-02 / Phase 34).
      },
      ```
    - The terminal `mergeAndPersistLlmEntities` call at line 477 (post-deletion line numbers will shift; verify via grep) MUST remain intact. Verify by `grep -n 'mergeAndPersistLlmEntities(' server/lib/llmExtractionPipeline.ts | wc -l` returns 2 (definition + one call) post-edit.

    Step 4 (env-import cleanup — if applicable):
    - Locate the line that imports `env` from `../config.js`. If `env` is now used elsewhere in the file (likely yes — other constants reference it), keep the import. If `env.LLM_FLUSH_EVERY_N_BATCHES` was the only usage, the import stays but the usage is gone.

  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <criteria>
      - `! grep -q 'FLUSH_EVERY_N_BATCHES_DEFAULT' server/lib/llmExtractionPipeline.ts` exits 0
      - `! grep -q 'getFlushEveryNBatches' server/lib/llmExtractionPipeline.ts` exits 0
      - `! grep -q 'batchesSinceLastFlush' server/lib/llmExtractionPipeline.ts` exits 0
      - `! grep -q 'lastFlushedEventCount' server/lib/llmExtractionPipeline.ts` exits 0
      - `! grep -q 'env.LLM_FLUSH_EVERY_N_BATCHES' server/lib/llmExtractionPipeline.ts` exits 0
      - `grep -c 'mergeAndPersistLlmEntities' server/lib/llmExtractionPipeline.ts` returns ≤ 3 (one definition at ~130, one terminal call at ~477; possibly one docblock reference)
      - `grep -q 'Phase 30 D-04' server/lib/llmExtractionPipeline.ts` exits 0 (rationale comment present in the callback)
      - `npx tsc --noEmit` exits 0
    </criteria>
  </verify>
  <done>
    - Periodic-flush block deleted; onBatchComplete callback contains only updateProgress + comment
    - FLUSH_EVERY_N_BATCHES_DEFAULT + getFlushEveryNBatches helper deleted
    - mergeAndPersistLlmEntities docblock reworded to single-purpose
    - Terminal call at original line 477 (now possibly shifted) intact — exactly one callsite for the helper outside its definition
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete LLM_FLUSH_EVERY_N_BATCHES from server/config.ts + .env.example</name>
  <files>server/config.ts, .env.example</files>
  <read_first>
    - server/config.ts lines 74-81 (the LLM_FLUSH_EVERY_N_BATCHES Zod entry + its preceding comment block)
    - .env.example lines 137-141 (the LLM_FLUSH_EVERY_N_BATCHES block — 4-line comment + var line)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md sections "server/config.ts" (deletion target) and ".env.example" (deletion target)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md "Runtime State Inventory" — note that operator's existing Vercel env-var setting is harmless if left in place once the schema entry is gone (Zod just no longer parses it)
  </read_first>
  <behavior>
    - server/config.ts has no LLM_FLUSH_EVERY_N_BATCHES Zod entry
    - .env.example has no LLM_FLUSH_EVERY_N_BATCHES line and no commentary block referencing it
    - npm run check:env continues to pass (schema and .env.example stay in sync — both deletions atomic)
    - `process.env.LLM_FLUSH_EVERY_N_BATCHES` set in the operator's environment becomes harmless leftover (CONTEXT D-04 acknowledges operator post-merge clean-up)
  </behavior>
  <action>
    Step 1 (server/config.ts ~lines 74-81 — deletion):
    - Locate the comment block at ~74-80 starting with `// Phase 28.2.6 D-03 — cadence for incremental terminal-key writes...` AND the `LLM_FLUSH_EVERY_N_BATCHES: z.coerce.number().int().positive().default(10),` line at ~line 81. Delete the entire block (comment + Zod entry, ~8 lines).
    - Do NOT delete neighboring entries (LLM_V3_CONCURRENCY above, LLM_BATCH_SIZE just-added by Plan 05 above or below, etc.).

    Step 2 (.env.example ~lines 137-141 — deletion):
    - Locate the comment block starting with `# Phase 28.2.6 D-03 — cadence for incremental terminal-key writes...` and the `LLM_FLUSH_EVERY_N_BATCHES=10` line. Delete the entire block (4-line comment + var line, ~5 lines total per 30-PATTERNS.md).
    - "Nothing left behind" — per CONTEXT D-04 the commentary goes with the value line.

    Step 3 (sanity check):
    - Run `npm run check:env` locally — confirms .env.example and Zod schema are in sync. If it fails, one site was missed.

  </action>
  <verify>
    <automated>npm run check:env &amp;&amp; npx tsc --noEmit</automated>
    <criteria>
      - `! grep -q 'LLM_FLUSH_EVERY_N_BATCHES' server/config.ts` exits 0
      - `! grep -q 'LLM_FLUSH_EVERY_N_BATCHES' .env.example` exits 0
      - `npm run check:env` exits 0
      - `npx tsc --noEmit` exits 0
    </criteria>
  </verify>
  <done>
    - LLM_FLUSH_EVERY_N_BATCHES gone from both Zod schema and .env.example
    - npm run check:env passes (atomicity invariant honored)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update terminalShape.test.ts (add exactly-once mergeAndPersist assertion) + incrementalWrite.test.ts (replace per-N-flush assertions with no-flush)</name>
  <files>server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts, server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts</files>
  <read_first>
    - server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts (full file — confirm the existing runEval-exactly-once assertion at ~lines 359-365 and the test header docblock at lines 1-15)
    - server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts (full file — confirm the existing per-N-flush block at ~lines 371-391 and the hoisted mockEnv at ~lines 29-41; verify Plan 05 already added LLM_BATCH_SIZE to mockEnv)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md sections "server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts" + "server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts" (verbatim replacement assertions)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md Pitfall 7 (test scope: assert exactly-once ONLY on the happy-path branch; early-return / soft-cap-paused branches assert zero-times)
  </read_first>
  <behavior>
    - terminalShape.test.ts gains one new it() block asserting `mergeAndPersistLlmEntities is called exactly once per successful run (happy path)`
    - incrementalWrite.test.ts loses 2 of 3 existing flush-cadence assertions and gains 2 new no-flush happy-path assertions
    - Both files have mockEnv stripped of any LLM_FLUSH_EVERY_N_BATCHES property (RESEARCH gotcha — orphan mock keys silently ignored, but accumulate rot)
    - Pitfall 7 scope: only the happy-path branch asserts toBe(1); empty-groups path and soft-cap-paused path (if covered) assert toBe(0)
  </behavior>
  <action>
    Step 1 (terminalShape.test.ts — add exactly-once assertion):
    - Locate the `describe(...)` block that contains the existing `runEval called exactly once` assertion (~line 319+). Add a new `it()` block alongside it (mirror the existing pattern verbatim per 30-PATTERNS.md):
      ```typescript
      it('mergeAndPersistLlmEntities is called exactly once per successful run (D-04 / SIMPLIFY-01)', async () => {
        await driveRun(12);
        // mergeAndPersistLlmEntities is the writer of events:llm:v3. Pre-Phase-30
        // it fired every N batches (incremental flush); Phase 30 D-04 retires that
        // tier and the helper is now invoked exactly once at end-of-pipeline.
        const terminalCalls = cacheSetSpy.mock.calls.filter(([k]) => k === 'events:llm:v3');
        expect(terminalCalls.length).toBe(1);
      });
      ```
    - Update the OTHER pre-existing assertion in this same `describe` block — if any test asserts `terminalCalls.length).toBeGreaterThanOrEqual(2)` for the 12-batch happy path (it does, per 30-PATTERNS.md gotcha 2), CHANGE that to `.toBe(1)` since the every-N-flush no longer fires.
    - Strip any `LLM_FLUSH_EVERY_N_BATCHES` from the hoisted mockEnv in this file (if present).

    Step 2 (incrementalWrite.test.ts — replace per-N-flush block):
    - Locate the existing `describe('runRefreshExtraction — D-03 incremental terminal-key cadence', ...)` block (~lines 371-391) containing 3 `it()` blocks:
      1. `"cadence: cacheSetSafe('events:llm:v3', ...) is called every 10 batches"` — DELETE (no longer truthy)
      2. `"no premature flush: under 10 batches there is exactly ONE cacheSetSafe('events:llm:v3', ...) call (final-only)"` — KEEP AS-IS (already correct for post-D-04 shape)
      3. `"configurable: LLM_FLUSH_EVERY_N_BATCHES=3 fires intermediate at batch 3 and 6"` — DELETE (env var is gone)
    - Replace with the new no-flush block per 30-PATTERNS.md verbatim:
      ```typescript
      describe('runRefreshExtraction — no incremental flush (D-04 / SIMPLIFY-01)', () => {
        it("12-batch happy path: exactly ONE cacheSetSafe('events:llm:v3', ...) call (terminal only)", async () => {
          await driveRun(12);
          const terminalCalls = cacheSetSpy.mock.calls.filter(([k]) => k === 'events:llm:v3');
          expect(terminalCalls.length).toBe(1);
        });

        it("5-batch happy path: exactly ONE cacheSetSafe('events:llm:v3', ...) call (terminal only)", async () => {
          await driveRun(5);
          const terminalCalls = cacheSetSpy.mock.calls.filter(([k]) => k === 'events:llm:v3');
          expect(terminalCalls.length).toBe(1);
        });
      });
      ```

    Step 3 (mockEnv cleanup — incrementalWrite.test.ts ~lines 29-41):
    - In the hoisted `mockEnv` object, REMOVE any property named `LLM_FLUSH_EVERY_N_BATCHES` (likely set to `10`). The schema entry no longer exists; the property is orphaned.
    - Leave the `LLM_BATCH_SIZE` property that Plan 05 added — that's load-bearing.

    Step 4 (Pitfall 7 scope verification):
    - Confirm any tests covering the no-new-groups early-return path (lines 289-302 in llmExtractionPipeline.ts) or the soft-cap-paused path (lines 306-320) DO NOT assert `expect(terminalCalls.length).toBe(1)`. They should assert `.toBe(0)` (no writes). If such tests don't currently exist, do NOT add them in this plan — out of scope.

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts</automated>
    <criteria>
      - `grep -q 'mergeAndPersistLlmEntities is called exactly once' server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` exits 0
      - `grep -q 'no incremental flush (D-04 / SIMPLIFY-01)' server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
      - `! grep -q 'LLM_FLUSH_EVERY_N_BATCHES' server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
      - `! grep -q 'LLM_FLUSH_EVERY_N_BATCHES' server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` exits 0
      - `! grep -q "every 10 batches" server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
      - `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` exits 0
      - `npx vitest run server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
      - `npx vitest run server/` exits 0 (server-only suite green)
      - `npx tsc --noEmit` exits 0
    </criteria>
  </verify>
  <done>
    - terminalShape.test.ts has new mergeAndPersist-exactly-once assertion alongside existing runEval-exactly-once
    - incrementalWrite.test.ts no longer asserts per-N flush cadence; mockEnv strips LLM_FLUSH_EVERY_N_BATCHES
    - All server-only tests pass; tsc clean
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                                              |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| onBatchComplete callback ← processEventGroups | Pure progress callback; no Redis writes inside (terminal write moved entirely outside callback)                                          |
| Mid-run crash → recovery                      | Pre-deletion: incremental flush mitigated partial-progress loss on crash. Post-deletion: full run is required for terminal write to fire |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-30-03   | Denial of Service      | Mid-run crash now loses all progress                                     | accept      | Pre-Plan-30 was the v1.4 shape that motivated Phase 28.2.6 Plan 01 (incremental flush as 300s-Hobby mitigation). Pro 800s ceiling makes the crash window negligible — Plan 06's Run 2 confirms full runs complete inside budget. **Pitfall 1 cache bridge in `server/routes/events.ts:518-585` is preserved** — even if a run dies mid-flight, raw GDELT continues to serve via the bridge; "map never goes blank" contract honored. RESEARCH A5 confidence: HIGH (D-04 reverts to a known-tested shape). Severity: LOW. |
| T-30-03a  | Information Disclosure | Operator's Vercel env still has stale LLM_FLUSH_EVERY_N_BATCHES set      | accept      | Harmless once schema entry is gone — Zod no longer parses it. Operator post-merge cleanup is noted in commit body. Severity: LOW.                                                                                                                                                                                                                                                                                                                                                                                        |
| T-30-03b  | Tampering              | writePartialCache still fires in v3 extractor (Plan 03 doesn't touch it) | accept      | SIMPLIFY-02 in Phase 34 retires `events:llm:v3:partial`. Plan 03 only retires the periodic-flush MECHANISM; the partial-cache observability write at v3 extractor stays untouched (RESEARCH gotcha 2). No race condition between deleted periodic flush and surviving partial-cache write because they touch different keys.                                                                                                                                                                                             |

</threat_model>

<verification>
- All Task 1 acceptance criteria pass (FLUSH_EVERY_N_BATCHES_DEFAULT / getFlushEveryNBatches / batchesSinceLastFlush all grep -q absent; tsc clean)
- All Task 2 acceptance criteria pass (LLM_FLUSH_EVERY_N_BATCHES absent from schema + .env.example; npm run check:env passes)
- All Task 3 acceptance criteria pass (new exactly-once assertion present; no per-N assertions remain; mockEnv cleaned)
- `npx tsc --noEmit` exits 0 across full repo
- `npx vitest run server/` exits 0
- Pitfall 1 cache bridge (`server/routes/events.ts:518-585`) is UNTOUCHED — verified by `git diff` showing no changes to that file
- Commit message body contains the pre/post Redis SET-call count delta sourced from Plan 06's Run 2 observation (per CONTEXT D-04)
</verification>

<success_criteria>

- `! grep -q 'LLM_FLUSH_EVERY_N_BATCHES' server/config.ts && ! grep -q 'LLM_FLUSH_EVERY_N_BATCHES' .env.example && ! grep -q 'FLUSH_EVERY_N_BATCHES' server/lib/llmExtractionPipeline.ts` all exit 0
- `grep -q 'mergeAndPersistLlmEntities is called exactly once' server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` exits 0
- `! grep -q 'every 10 batches' server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0
- `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` exits 0 with new assertions green
- `npm run check:env` exits 0
- Commit body quotes the SET-call delta from Plan 06's Run 2 observation
- Commit message lands as `feat(30): retire incremental flush mechanism (SIMPLIFY-01 / D-04)`
  </success_criteria>

<output>
After completion, create `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-03-SUMMARY.md` documenting:
- Exact line ranges deleted (for SIMPLIFY-07 LOC accounting in Phase 34)
- The Redis SET-call delta paste from Plan 06's Run 2 (Pre-Plan-03 SETs vs Post-Plan-03 SETs = 1)
- Confirmation that terminal write at the moved line number is intact (grep -c verification)
- Confirmation that Pitfall 1 cache bridge in server/routes/events.ts is untouched
- The two updated test files' before/after assertion counts
- Pointer for Plan 07: this commit's audit-signal numbers + LOC delta feed the reliability doc's "Retired Mechanisms" section
</output>

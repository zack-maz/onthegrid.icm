---
phase: 30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim
plan: 06
type: execute
wave: 4
depends_on: ['30-04', '30-05']
files_modified:
  - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-2-throttle-snapshot.json
autonomous: false
requirements: [LLM-RELI-03, LLM-RELI-04]
requirements_addressed: [LLM-RELI-03, LLM-RELI-04]
gsd_commit: 'feat(30): validate tuned defaults (Run 2) + commit numbers (D-02)'
must_haves:
  truths:
    - 'Force-trigger of /api/cron/refresh-events?force=true at the Plan-05 tuned defaults completes inside the Pro 800s ceiling'
    - "Run 2's analyzer output is captured as run-2-throttle-snapshot.json in the phase directory with the same 9-field shape as Run 1"
    - 'Deploy gate (a): runEval accuracy at 5km / 20km / 100km stays within ±3pp absolute (CONTEXT D-03; jq ratio diff vs events:llm-eval-baseline:v3) at all three distances'
    - "Deploy gate (b): watchdog hard-kill count in Run 2 ≤ Run 1's count"
    - 'If both gates pass: tuned defaults are committed-locked and Plan 07 documents them. If either gate fails: Run 3 (bisection per D-02) fires — record run-3-throttle-snapshot.json with same shape; if Run 3 also fails, escalate to checkpoint and revisit Plan 05 assumptions'
    - 'Adversarial eval delta is captured and reported alongside baseline delta (RESEARCH Pitfall 6 — operator review covers implicit baseline-vs-adversarial tradeoff)'
    - "Pre/post Redis SET-call count delta for events:llm:v3 (per cron run) is captured from this run's analyzer output and threaded forward into Plan 03's commit message (SIMPLIFY-01 audit signal per CONTEXT D-04)"
  artifacts:
    - path: '.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-2-throttle-snapshot.json'
      provides: 'Second-data-point validation of the Plan-05 tuned defaults; deploy-gate input'
      contains: 'throttleWindowMs'
  key_links:
    - from: 'Plan 05 commit (tuned defaults deployed)'
      to: 'Vercel production with new env vars set / new constants bundled'
      via: "git push → Vercel deploy → operator confirms maxDuration:800 still active and prior commit's bundle is shipped"
      pattern: 'vercel.json maxDuration'
    - from: 'Run 2 force-trigger'
      to: 'events:llm-summary:v3 + events:llm-eval-baseline:v3 comparison'
      via: 'scripts/analyze-llm-run.ts → ±3pp deploy gate'
      pattern: 'evalScore'
    - from: 'Run 2 vs Run 1 Redis SET-call delta'
      to: "Plan 03's commit message body (SIMPLIFY-01 audit signal)"
      via: 'operator captures delta from analyzer + pastes into Plan 03 commit body'
      pattern: 'events:llm:v3'
---

<objective>
Validate Plan 05's tuned defaults against Run-1's anchor (CONTEXT D-02 "validate" step). Operator force-triggers the cron one more time at the new defaults, captures the analyzer output as `run-2-throttle-snapshot.json`, and confirms the two deploy gates pass:
(a) `runEval()` accuracy at 5km / 20km / 100km stays within ±3pp absolute of `events:llm-eval-baseline:v3` (Phase-29 anchor, 90d TTL) — CONTEXT D-03.
(b) Watchdog hard-kill count ≤ Run 1's count.

If both gates pass: tuned defaults are locked, Plan 07 documents them, milestone proceeds.
If either gate fails: Run 3 (bisection fallback) fires per CONTEXT D-02 — step concurrency by half the Run-1→Run-2 delta in whichever direction the gate-failure suggests; record `run-3-throttle-snapshot.json` with same shape. If Run 3 also fails → checkpoint-to-operator before committing Plan 07.

This plan also captures the **Redis SET-call count delta** for `events:llm:v3` between Run 1 (pre-Plan-04-deletion) and Run 2 (post-Plan-04-deletion). That delta number feeds forward into Plan 03's commit message body (per CONTEXT D-04 "the commit message includes the pre/post Redis SET-call count for `events:llm:v3` per cron run") — Plan 03 is sequenced AFTER this plan in the Option A ordering precisely to consume this data.

Purpose: Run 2 closes the characterize → propose → validate loop. Without a validated run at the proposed defaults, Plan 07's documentation would record aspirational numbers rather than measured outcomes. The ±3pp gate plus the watchdog-count gate ensure tuning does not silently regress accuracy or destabilize the pipeline.

Output:

- `run-2-throttle-snapshot.json` (and optionally `run-3-throttle-snapshot.json` if bisection fires) committed to the phase directory
- Deploy-gate verdict (pass / fail / bisect) recorded in commit message body
- Adversarial eval delta reported alongside baseline delta (RESEARCH Pitfall 6 disclosure)
- Pre/post Redis SET-call count delta captured for Plan 03's downstream consumption (SIMPLIFY-01 audit signal)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-PATTERNS.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-VALIDATION.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-02-SUMMARY.md
@.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-05-SUMMARY.md
@scripts/analyze-llm-run.ts

<interfaces>
<!-- Deploy gate (a) computation - per CONTEXT D-03 + 30-VALIDATION.md "Run 2 snapshot": -->
<!-- For each distance d in {5km, 20km, 100km}: -->
<!--   run2_ratio = run2_within_d_km / run2_total -->
<!--   baseline_ratio = events:llm-eval-baseline:v3.within_d_km / events:llm-eval-baseline:v3.total -->
<!--   abs(run2_ratio - baseline_ratio) must be ≤ 0.03 (3pp absolute tolerance) -->

<!-- Deploy gate (b) computation: -->
<!--   run2.watchdogTimeoutCount ≤ run1.watchdogTimeoutCount (from snapshots) -->

<!-- Run 3 bisection trigger (CONTEXT D-02): -->
<!-- If gate (a) fails: step LLM_V3_CONCURRENCY by half the Run-1→Run-2 delta in the opposite direction -->
<!-- If gate (b) fails: bump LLM_BATCH_TIMEOUT_MS by 30s and rerun -->
<!-- After Run 3: if either gate still fails → checkpoint:human-action → operator reviews Plan 05 assumptions -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator-attended force-trigger at tuned defaults (Run 2) + deploy-gate evaluation</name>
  <what-built>
    - Plan 04 deleted soft-warn tier from watchdog (Wave 3 sibling)
    - Plan 05 added LLM_BATCH_SIZE env var, promoted BATCH_SIZE to env-tunable, retuned LLM_V3_CONCURRENCY / LLM_BATCH_TIMEOUT_MS / RETRY_ATTEMPTS / BACKOFF_MS / JITTER_MS from Run-1 data (Wave 3 sibling)
    - Both Wave 3 commits pushed to main; Vercel has redeployed
  </what-built>
  <read_first>
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-1-throttle-snapshot.json (the comparison baseline for watchdog hard-kill count + per-batch latency)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-05-SUMMARY.md (the proposed defaults Plan 05 committed — operator must confirm Vercel env vars match)
    - server/routes/refresh-events-cron.ts (force-trigger Bearer auth shape)
    - scripts/analyze-llm-run.ts (Plan 01 deliverable — confirm `--snapshot=<path>` arg works)
    - .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-RESEARCH.md sections "Pitfall 4" (800s budget math; new lower CONCURRENCY = longer wall-clock) AND "Pitfall 6" (Run 2 eval regression masked by accidental adversarial gain)
  </read_first>
  <how-to-verify>
    Pre-flight checks (do these FIRST):
    1. Confirm Plan 04 + Plan 05 commits are deployed to prod (Vercel deploy log shows the most recent main commit is Ready).
    2. Confirm Vercel env vars match Plan 05's chosen defaults: `vercel env ls` should show LLM_BATCH_SIZE, LLM_V3_CONCURRENCY, LLM_BATCH_TIMEOUT_MS at the new values (operator must have set them per CONTEXT D-07 / 30-RESEARCH §"Runtime State Inventory").
       - Alternatively, if relying on hard-coded fallback defaults, confirm `api/vercel-entry.js` build is fresh (`stat -f %m api/vercel-entry.js` after the most recent npm run build).
    3. Confirm `events:llm-eval-baseline:v3` is still live (90d TTL from Phase 29, written ~2026-05-12). Run `npm run eval:replay` once locally — if it succeeds and prints scores, the key is live. If `runEval()` errors with "missing baseline", re-populate by running `npm run eval:replay` once (RESEARCH §Environment Availability).
    4. Confirm pipeline is idle: `curl -sS https://otg-iran-monitor.vercel.app/api/events/llm-status | jq -r '.idle'` returns `true` (anti-pattern #18 cooldown discipline).
    5. Confirm operator is in a watchful window (RESEARCH Pitfall 3 — autonomous: false enforces this).
    6. **Lower-CONCURRENCY budget check** (RESEARCH Pitfall 4): Plan 05 likely lowered LLM_V3_CONCURRENCY, which means Run 2 wall-clock will be LONGER than Run 1 (same batch count / smaller fan-out). Sanity math: `ceil(batchCount / new_CONCURRENCY) × measured_p95`. If projected runtime > 700s (leaving < 100s headroom under the 800s ceiling), operator may need to lower LLM_BATCH_TIMEOUT_MS slightly or accept the tight schedule. Decision lands in operator notes.

    Trigger Run 2 (operator executes):
    ```
    curl -s "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true" \
      -H "Authorization: Bearer ${CRON_SECRET}" \
      | jq .
    ```
    Bearer never appears in code or commit messages (threat T-30-02 mitigation, inherited from Plan 02).

    Live monitoring during the run:
    - Watch Vercel function logs for 429 frequency / bucket distribution under new BACKOFF_MS array
    - Confirm soft-warn log entries are GONE (Plan 04 deletion landed)
    - Watch for any watchdog hard-kill entries — record the count

    Post-run capture:
    ```
    npm run analyze:llm-run -- \
      --snapshot=.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-2-throttle-snapshot.json
    ```

    Deploy gate (a) evaluation — eval regression within ±3pp:
    For each distance d in {5km, 20km, 100km}:
    ```
    # Get Run 2 ratio from snapshot:
    R2=$(jq ".evalScore.within${d}km / .evalScore.total" run-2-throttle-snapshot.json)
    # Get baseline ratio from Redis (operator pastes from npm run eval:replay or curls Redis directly):
    BASELINE=$(jq ".within${d}km / .total" <(curl ... events:llm-eval-baseline:v3))
    # Compute absolute diff:
    DIFF=$(echo "$R2 - $BASELINE" | bc -l | awk '{print ($1 < 0) ? -$1 : $1}')
    # Gate: DIFF ≤ 0.03
    ```
    All three distances must pass.

    Deploy gate (b) evaluation — watchdog hard-kill count regression:
    ```
    RUN1_HK=$(jq '.watchdogTimeoutCount' run-1-throttle-snapshot.json)
    RUN2_HK=$(jq '.watchdogTimeoutCount' run-2-throttle-snapshot.json)
    test "$RUN2_HK" -le "$RUN1_HK"  # exits 0 if Run 2 ≤ Run 1
    ```

    Adversarial eval delta capture (RESEARCH Pitfall 6 — reported NOT gated):
    ```
    # Read events:llm-eval-adversarial:v3 from Redis pre-run + post-run
    # Diff is observation-only; commit body includes both baseline AND adversarial deltas
    ```

    Redis SET-call count delta capture (Plan 03 input — CONTEXT D-04 "audit signal"):
    - Count `events:llm:v3` SET calls per cron run, pre-Plan-03 (which IS this Run 2, since Plan 03 / SIMPLIFY-01 hasn't landed yet — Plan 03 is Wave 5 per Option A ordering).
    - Post-Plan-03 the count drops to exactly 1 per run (terminal write only). Plan 03's commit message body will quote this delta.
    - Capture method: operator reads Vercel function logs for the Run 2 invocation and counts `cacheSetSafe('events:llm:v3'` log entries; OR inspects the analyzer output if it surfaces this count (it currently does not — capture from logs).

    Decision tree:

    **Path 1 — both gates pass (happy path):**
    ```
    git add .planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/run-2-throttle-snapshot.json
    git commit -m "feat(30): validate tuned defaults (Run 2) + commit numbers (D-02)" -m "Run 2 validates Plan-05 tuned defaults at the Pro 800s ceiling. Both deploy gates pass." -m "" -m "Deploy gate (a) — eval regression within ±3pp absolute:" -m "  5km: baseline=X, run2=Y, diff=Z (≤ 0.03 ✓)" -m "  20km: baseline=X, run2=Y, diff=Z (≤ 0.03 ✓)" -m "  100km: baseline=X, run2=Y, diff=Z (≤ 0.03 ✓)" -m "" -m "Deploy gate (b) — watchdog hard-kill count:" -m "  Run 1: K kills" -m "  Run 2: J kills (J ≤ K ✓)" -m "" -m "Adversarial eval delta (observation only): baseline=A, run2=B, diff=C" -m "" -m "Redis SET-call count delta for events:llm:v3 per cron run:" -m "  Pre-Plan-03 (this run): N SETs" -m "  Post-Plan-03 (forecast): 1 SET (Plan 03 commit body will confirm)"
    git push
    ```
    → Plan 07 reads run-2-throttle-snapshot.json and ships the full reliability doc.

    **Path 2 — gate (a) fails (eval regression > 3pp):**
    Investigate root cause:
    - If LLM_BATCH_SIZE bump degraded accuracy (qwen-235b context-window pressure), bisect: set LLM_BATCH_SIZE between Run 1's value (2) and Run 2's value, force-trigger Run 3.
    - If LLM_V3_CONCURRENCY change caused timing or bucket-distribution shift that hurt eval, bisect.
    Run 3 force-trigger:
    ```
    # Operator updates env vars to bisected values (does NOT commit a code change — env-tunable knobs are operator-side)
    curl -s ".../api/cron/refresh-events?force=true" -H "Authorization: Bearer ${CRON_SECRET}" | jq .
    npm run analyze:llm-run -- --snapshot=.planning/phases/30-.../run-3-throttle-snapshot.json
    # Re-evaluate gates against Run 3 numbers
    ```
    If Run 3 passes both gates → commit run-3-throttle-snapshot.json AND optionally update server/config.ts defaults to the bisected values (Plan 05 amendment / new sub-commit). Plan 07 documents the bisection path.
    If Run 3 fails → CHECKPOINT — operator reviews Plan 05's analytical formulas. Plan 07 does NOT proceed until tuning is stable.

    **Path 3 — gate (b) fails (more hard-kills than Run 1):**
    LLM_BATCH_TIMEOUT_MS is too tight. Bump by 30s and force-trigger Run 3 (no other env changes).
    Same Run 3 capture + evaluation as Path 2.

    Acceptance criteria validation (Path 1 / happy):
    - `test -f .planning/phases/30-*/run-2-throttle-snapshot.json` exits 0
    - `jq -e '.throttleWindowMs.path' "$_"` returns `"A"` or `"B"`
    - `jq -e '.durationMs < 800000' "$_"` exits 0
    - All three `jq` ratio diffs exit 0 with diff ≤ 0.03
    - `test "$(jq '.watchdogTimeoutCount' run-2-snap)" -le "$(jq '.watchdogTimeoutCount' run-1-snap)"` exits 0
    - Commit body contains both deploy-gate numbers AND adversarial delta AND Redis SET-count number

    Failure modes (RESEARCH Pitfall 4 / Pitfall 6):
    - `events:llm-summary:v3.completedAt` missing → run was killed (Pro 800s exceeded — lower CONCURRENCY backfired) → DO NOT commit Run 2 snapshot; investigate, possibly raise LLM_V3_CONCURRENCY slightly and retry.
    - Adversarial eval shows +pp gain but baseline shows -pp loss → Pitfall 6 trap. Even if baseline gate passes (within ±3pp), commit body MUST quote both numbers; operator review covers the implicit tradeoff.

  </how-to-verify>
  <resume-signal>
    Type "Run 2 complete, both gates pass" with the gate numbers pasted, OR "Run 2 gate X failed — Run 3 fires with bisection plan", OR "Run 3 failed — checkpoint, Plan 05 assumptions need revisit". Plan 07 (Wave 6) does not start until this plan signals green or operator-approved bisection convergence.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                         |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Operator shell → /api/cron/refresh-events           | Bearer-gated; identical posture to Plan 02                                                                          |
| Vercel function → NIM API at Plan-05 tuned defaults | Larger BATCH_SIZE + (possibly) lower CONCURRENCY change the call profile; rolling-window primitive still bounds RPM |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                               |
| --------- | ---------------------- | --------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-02   | Information Disclosure | CRON_SECRET Bearer in operator shell                      | mitigate    | Inherited from Plan 02 — Bearer never in code/commits; operator-side shell discipline.                                                                                                                                        |
| T-30-04   | Denial of Service      | Lower CONCURRENCY = longer wall-clock; risk of 800s cliff | mitigate    | Pre-flight check #6 (lower-CONCURRENCY budget math) before trigger; Path 2/3 Run 3 bisection handles the case where Plan 05 over-corrected. CHECKPOINT escalation on Run 3 failure prevents silent regression commit.         |
| T-30-06a  | Tampering              | Eval regression masked by adversarial gain (Pitfall 6)    | mitigate    | Commit message body MUST quote both baseline and adversarial deltas — operator review explicitly covers the tradeoff. Even passing the baseline gate, an unreported adversarial regression is reportable as a Phase 33 input. |
| T-30-06b  | Denial of Service      | Redis SET-call count delta capture fails                  | accept      | If operator misses the count capture from Vercel logs, Plan 03 can still proceed with a "delta captured in follow-up" placeholder per the Option B fallback in CONTEXT D-08. Severity: LOW.                                   |

</threat_model>

<verification>
- Run 2 snapshot file exists with all 9 required fields
- Both deploy gates pass (or Run 3 / bisection branch exercised and converged)
- `jq` ratio diffs at 5/20/100km all ≤ 0.03 absolute (or documented exception via Run 3 convergence)
- `watchdogTimeoutCount` ≤ Run 1's value
- Commit message body contains all three baseline ratio diffs, the adversarial delta, the watchdog count comparison, and the Redis SET-call count number
- No `events:llm-summary:v3.error` is set (run did not abort)
- Pipeline returned to idle (`curl .../api/events/llm-status | jq '.idle'` returns true) before Wave 5 starts
</verification>

<success_criteria>

- `run-2-throttle-snapshot.json` (and optionally `run-3-throttle-snapshot.json`) committed with proper shape
- Both deploy gates green (or convergent via bisection)
- Commit message body contains: 3 baseline diffs (5/20/100km), 1 adversarial delta, 1 watchdog count comparison, 1 Redis SET-count number
- Plan 03 has the Redis SET-count delta to quote in its commit body (downstream consumption)
- Plan 07 has the validated tuned-defaults numbers to document in `docs/architecture/llm-pipeline-reliability.md`
  </success_criteria>

<output>
After completion, create `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-06-SUMMARY.md` documenting:
- Run 2 wall-clock duration (must be < 800_000)
- Deploy gate (a) numbers: baseline ratio + Run 2 ratio + diff for each of 5/20/100km
- Deploy gate (b) numbers: Run 1 hard-kill count + Run 2 hard-kill count
- Adversarial eval delta (separate from baseline — observation only)
- Redis SET-call count for `events:llm:v3` during Run 2 (Plan 03 audit signal)
- Whether bisection (Run 3) fired; if so, the bisected values + outcome
- Operator's read of the new operating point — "headroom feels comfortable" / "tight" / "would tune further next milestone"
- Pointer to all snapshot files in the phase directory so Plan 07 can quote them
</output>

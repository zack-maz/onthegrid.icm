---
phase: 19.1-advanced-search-with-tag-and-entity-type-filtering
plan: 05
type: execute
wave: 3
depends_on: [19.1-03, 19.1-04]
files_modified:
  - src/components/layout/AppShell.tsx
  - src/__tests__/SearchModal.test.tsx
  - src/__tests__/searchStore.test.ts
autonomous: false
requirements: [ASRCH-01, ASRCH-02, ASRCH-03, ASRCH-04, ASRCH-05, ASRCH-06]

must_haves:
  truths:
    - "useQuerySync is wired into AppShell for bidirectional sync to be active app-wide"
    - "Full end-to-end flow works: type tag in search -> sidebar updates -> map dims non-matching entities"
    - "Full reverse flow works: toggle sidebar filter -> search bar updates with tag"
    - "Existing features not broken: Cmd+K opens modal, Enter filters, Escape closes, detail panel opens on result click"
    - "All vitest tests pass with no regressions"
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "useQuerySync hook wired into app root"
  key_links:
    - from: "src/components/layout/AppShell.tsx"
      to: "src/hooks/useQuerySync.ts"
      via: "hook call in component body"
      pattern: "useQuerySync"
---

<objective>
Wire useQuerySync into AppShell, run full integration verification, and do a visual checkpoint of the complete search system.

Purpose: Plans 01-04 build the pieces. This plan connects them and verifies the complete system works end-to-end. The useQuerySync hook must be called from AppShell (where other hooks like useFlightPolling are called) to be active app-wide. The checkpoint ensures the visual/interactive behaviors match the locked decisions.

Output: Fully integrated advanced search system verified by user.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-advanced-search-with-tag-and-entity-type-filtering/19.1-CONTEXT.md
@.planning/phases/19.1-advanced-search-with-tag-and-entity-type-filtering/19.1-01-SUMMARY.md
@.planning/phases/19.1-advanced-search-with-tag-and-entity-type-filtering/19.1-02-SUMMARY.md
@.planning/phases/19.1-advanced-search-with-tag-and-entity-type-filtering/19.1-03-SUMMARY.md
@.planning/phases/19.1-advanced-search-with-tag-and-entity-type-filtering/19.1-04-SUMMARY.md

@src/components/layout/AppShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useQuerySync into AppShell and run full test suite</name>
  <files>src/components/layout/AppShell.tsx</files>
  <action>
    1. Add `import { useQuerySync } from '@/hooks/useQuerySync';` to AppShell.tsx
    2. Call `useQuerySync();` in the AppShell component body, alongside the existing hook calls (useFlightPolling, useShipPolling, useEventPolling, useSiteFetch, useNewsPolling)
    3. Run `npx vitest run` to verify no regressions across the full test suite
    4. Run `npx tsc --noEmit` to verify TypeScript compilation with strict mode
    5. Run `npm run build` to verify the production build succeeds
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>useQuerySync wired into AppShell. Full test suite passes. TypeScript compilation clean. Production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete advanced search system</name>
  <files>src/components/search/SearchModal.tsx</files>
  <action>
    Present the complete advanced search system for user verification. All code is already built and automated tests passing from Task 1.
  </action>
  <verify>User confirms all 12 verification steps pass.</verify>
  <done>User approves the complete advanced search system.</done>
  <what-built>
    Complete advanced search system with tag-based queries, boolean expressions, syntax highlighting, autocomplete, bidirectional sidebar sync, and cheat sheet.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173

    2. **Chip row:** Press Cmd+K to open search. Verify 4 chips above input: [Events] [Sites] [Iran] [US]. Click [Iran] - verify `country:iran` appears in input with green-colored text.

    3. **Tag syntax highlighting:** Type `type:flight AND country:iran` - verify "type" and "country" prefixes are colored, "AND" is dimmed/italic, values are colored.

    4. **Autocomplete Stage 1:** Clear input, type "co" - verify dropdown suggests "country:" and "callsign:" (and possibly "cameo:"). Use arrow keys to navigate, Tab to accept.

    5. **Autocomplete Stage 2:** Type `country:` - verify dropdown shows known country values with counts (e.g., "iran (24)"). Tab to accept one.

    6. **Boolean query:** Type `type:flight AND (country:iran OR country:iraq)` - press Enter. Verify map dims non-matching entities. Only Iranian and Iraqi flights should remain fully visible.

    7. **Negation:** Clear, type `!type:ship` - press Enter. Verify ships are dimmed, all other entities visible.

    8. **Bidirectional sync (search -> sidebar):** Type `type:flight` and press Enter. Open sidebar layers panel. Verify Flights toggle is ON, Ships toggle reflects search state correctly.

    9. **Bidirectional sync (sidebar -> search):** With search modal open, toggle Ships ON in sidebar. Verify search input updates to include `type:ship`.

    10. **Cheat sheet:** Click the `?` icon in search bar. Verify popover shows all tag categories with examples. Click away to dismiss.

    11. **Backward compat:** Type "boeing" (no tag prefix) and press Enter. Verify it still works as freeform substring search matching flight callsigns.

    12. **Recent tags:** After searching with a few tags, reopen search modal. Verify recent tags appear as additional chips after the 4 defaults.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
npx vitest run && npx tsc --noEmit && npm run build
</verification>

<success_criteria>
- useQuerySync active in AppShell
- All automated tests pass
- User confirms visual/interactive behaviors match locked decisions
- No regressions in existing features (detail panel, notifications, markets, layers, filters)
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-advanced-search-with-tag-and-entity-type-filtering/19.1-05-SUMMARY.md`
</output>

---
phase: 27.4-llm-enrichment-improvements
verified: 2026-04-20T17:00:00Z
status: human_needed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "In a local dev environment with LLM_PIPELINE_V2=true, open DevApiStatus modal and confirm the 'Events' tab appears and renders all 8 blocks (waterfall, histograms, drill-down, call log, budget bars, eval score, DLQ, suspect count)"
    expected: 'Events tab is visible; all 8 blocks render; D-25 gate shows PASS or FAIL correctly; Paused badge shows when soft-cap is active'
    why_human: "Tab visibility gate is `schemaVersion==='v2' && import.meta.env.DEV` — requires an actual LLM pipeline run to populate llmStatus.schemaVersion"
  - test: 'In a local dev environment with LLM_PIPELINE_V2=true, trigger a /api/events cache miss and confirm the v2 extractor runs, writes to events:llm:v2, and the map does not go blank when v2 is cold'
    expected: "Map displays events from v2 enrichment; /api/events/llm-status shows schemaVersion='v2' and populated evalScore; no blank-map regression during cold start"
    why_human: 'End-to-end pipeline behavior requires real LLM provider calls (Cerebras/Groq) and live Redis'
  - test: 'Confirm ground-truth events in .planning/eval/ground-truth-events.json are source-verified (click each sourceUrl and validate the claimed coordinates)'
    expected: 'All 50 events have source URLs pointing to verifiable conflict reporting consistent with the stated lat/lng and hierarchy'
    why_human: 'Content accuracy of the ground-truth benchmark requires human review — automated tools cannot verify whether an article accurately supports the cited coordinates'
---

# Phase 27.4: LLM Enrichment Improvements Verification Report

**Phase Goal:** Raise the LLM-enriched event pipeline to Palantir-grade accuracy and observability. Layered geolocation resolver (own-snapshot → POI-amenity Nominatim → direct Nominatim → 2-pass LLM verify → Bellingcat coord passthrough → GDELT ActionGeo fallback); richer prompts (news + Bellingcat + temporal context blocks); extended output schema (structured place hierarchy, confidence, reasoning, weapon/target, time/duration); dedicated Events section in DevApiStatus (waterfall, histograms, drill-down, call log, budget bars, eval harness, DLQ); flag-gated rollout (LLM_PIPELINE_V2); bounded retry + DLQ + circuit breaker; tracked token budget; cache-version bump (events:llm → events:llm:v2).

**Verified:** 2026-04-20T17:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | LLM_PIPELINE_V2 env flag controls which extractor runs (default false)                                                         | ✓ VERIFIED | `server/config.ts` exports `isPipelineV2(): boolean`; `.env.example` documents `LLM_PIPELINE_V2=false`; `server/lib/llmEventExtractor.ts` barrel calls `isPipelineV2()` at request-time to route to v1 or v2                                                                                                                                                                     |
| 2   | v1 extractor preserved under `llmEventExtractor.v1.ts` for rollback                                                            | ✓ VERIFIED | File exists at `server/lib/llmEventExtractor.v1.ts`; now uses `forwardGeocodeConstrained` with ME_VIEWBOX/ME_COUNTRY_CODES (CR-01 fix applied)                                                                                                                                                                                                                                   |
| 3   | v2 cache keys `events:llm:v2` + `events:llm-summary:v2` coexist with v1 keys                                                   | ✓ VERIFIED | `server/routes/events.ts` computes `LLM_EVENTS_KEY_ACTIVE`/`LLM_SUMMARY_KEY_ACTIVE` via `isPipelineV2()` ternary; v2→v1 graceful fallback on cold start present                                                                                                                                                                                                                  |
| 4   | llmSchema.ts defines `enrichedEventV2`, `GeocodeProvenance`, `derivePrecision`, `deriveSuspect`                                | ✓ VERIFIED | All 4 exports confirmed in `server/lib/llmSchema.ts`; `.strict()` on `locationHierarchyV2` enforces D-05; `enrichedEventAny` discriminated union present                                                                                                                                                                                                                         |
| 5   | Layered resolver with all 6 provenance paths is functional                                                                     | ✓ VERIFIED | `server/lib/llmResolver.ts` implements all 6 branches in D-22 order; POI amenity and 2-pass verify stubs replaced with functional code (Plans 03+05); WR-01 (throttle after cache miss), WR-04 (single-candidate accept) fixes applied                                                                                                                                           |
| 6   | `forwardGeocodeConstrained` in nominatim adapter with viewbox/countrycodes/amenity                                             | ✓ VERIFIED | `server/adapters/nominatim.ts` exports `forwardGeocodeConstrained`; imports `ME_VIEWBOX`, `ME_COUNTRY_CODES` from `server/lib/meBounds.ts`; `amenity`/`q` mutual exclusion present                                                                                                                                                                                               |
| 7   | Circuit breaker, DLQ, and token budget modules exist with correct exports                                                      | ✓ VERIFIED | `llmCircuitBreaker.ts`: `isAvailable`, `record`, `getBreakerState`; `llmDLQ.ts`: `enqueueDLQ`, `listDLQ`, `countDLQ` with WR-02 eviction fix; `llmTokenBudget.ts`: `incrDailyTokens`, `shouldPauseNewEvents`, `prioritizeBySeverity`                                                                                                                                             |
| 8   | `callLLM` in llm-provider.ts has retry + breaker + budget + call-history wiring                                                | ✓ VERIFIED | `server/adapters/llm-provider.ts` imports `isAvailable`, `incrDailyTokens`, `appendCallHistory`; `RETRY_ATTEMPTS=2`, `BACKOFF_MS=[1000,4000]` present; WR-03 fix (BATCH_SIZE_V2) in events.ts                                                                                                                                                                                    |
| 9   | v2 extractor produces enriched events with BATCH_SIZE=2, 3 prompt blocks, resolver integration                                 | ✓ VERIFIED | `server/lib/llmEventExtractor.v2.ts`: `BATCH_SIZE=2`, `SYSTEM_PROMPT_V2`, NEWS/BELLINGCAT/TEMPORAL blocks gated on data presence, `enqueueDLQ` on Zod-fail, `bellingcatByGroup` map, `resolveLocation` called per event                                                                                                                                                          |
| 10  | Eval harness exists with `loadGroundTruth`, `runEval`, accuracy buckets; 50-event ground-truth file                            | ✓ VERIFIED | `server/lib/llmEvalHarness.ts` exports `loadGroundTruth`, `runEval` with `within5km`/`within20km`/`within100km` buckets; `.planning/eval/ground-truth-events.json` has 50 events; harness never calls `callLLM` (A6 preserved)                                                                                                                                                   |
| 11  | `events.ts` has dev-only POST `/llm-replay/:groupKey` with dual-gate + `runEval` wiring                                        | ✓ VERIFIED | Dual-gate present: route registered only when `NODE_ENV !== 'production'` AND in-handler 404 check; `runEval()` called in v2 fire-and-forget block at line 883                                                                                                                                                                                                                   |
| 12  | `DevApiStatus.tsx` has Events tab + 8 observability blocks; `useLLMStatusPolling.ts` extended; `ui.ts` includes `'events'` tab | ✓ VERIFIED | `EventsFiltersSection` with all 8 sub-blocks (WaterfallBlock, HistogramsBlock, DrillDownBlock, CallLogBlock, BudgetBarsBlock, EvalScoreBlock, DlqBlock, SuspectBlock); `DevApiStatusTab` union includes `'events'`; `LLMStatus` has all v2 optional fields including `recentEvents`, `paused`, `callHistory`; `src/types/llm.ts` provides client-side `GeocodeProvenance` mirror |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact                                  | Status     | Details                                                                                                                                                                                                                                                  |
| ----------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server/lib/llmEventExtractor.v1.ts`      | ✓ VERIFIED | Exists; uses `forwardGeocodeConstrained` (CR-01 fix applied)                                                                                                                                                                                             |
| `server/lib/llmEventExtractor.v2.ts`      | ✓ VERIFIED | Exists; `processEventGroupsV2`, `geocodeEnrichedEventsV2`, `BATCH_SIZE=2`, all 3 prompt blocks                                                                                                                                                           |
| `server/lib/llmEventExtractor.ts`         | ✓ VERIFIED | Barrel routes via `isPipelineV2()` to v1 or v2                                                                                                                                                                                                           |
| `server/lib/meBounds.ts`                  | ✓ VERIFIED | `ME_VIEWBOX=[30,15,70,42]`, `ME_COUNTRY_CODES` 22-country string                                                                                                                                                                                         |
| `server/lib/llmSchema.ts`                 | ✓ VERIFIED | `enrichedEventV2`, `enrichedEventAny`, `GeocodeProvenance`, `derivePrecision`, `deriveSuspect`, `EVENT_EXTRACTION_SCHEMA_V2`                                                                                                                             |
| `server/lib/llmResolver.ts`               | ✓ VERIFIED | `resolveLocation`, all 6 provenance branches functional                                                                                                                                                                                                  |
| `server/adapters/nominatim.ts`            | ✓ VERIFIED | `forwardGeocodeConstrained` with `ME_VIEWBOX`/`ME_COUNTRY_CODES` defaults                                                                                                                                                                                |
| `server/lib/llmCircuitBreaker.ts`         | ✓ VERIFIED | `isAvailable`, `record`, `getBreakerState`, `__resetBreakerForTests`                                                                                                                                                                                     |
| `server/lib/llmDLQ.ts`                    | ✓ VERIFIED | `enqueueDLQ`, `listDLQ`, `countDLQ`; WR-02 raw-string eviction fix applied                                                                                                                                                                               |
| `server/lib/llmTokenBudget.ts`            | ✓ VERIFIED | `incrDailyTokens`, `getDailyTokens`, `budgetState`, `shouldPauseNewEvents`, `prioritizeBySeverity`                                                                                                                                                       |
| `server/adapters/llm-provider.ts`         | ✓ VERIFIED | `callLLM` extended with retry, breaker, budget, call-history                                                                                                                                                                                             |
| `.planning/eval/ground-truth-events.json` | ✓ VERIFIED | 50 events with `id`, `description`, `sourceUrl`, `truth`, `hierarchy`; file has `curatedAt`; note: individual events do not have `verifiedAt` (plan spec artifact `contains: 'verifiedAt'` was from the RESEARCH.md sample, not enforced by the harness) |
| `server/lib/llmEvalHarness.ts`            | ✓ VERIFIED | `loadGroundTruth`, `runEval`, `within5km/within20km/within100km` buckets, baseline key `events:llm-eval-baseline:v2`                                                                                                                                     |
| `server/routes/events.ts`                 | ✓ VERIFIED | `/llm-replay` dual-gate, `runEval` call in v2 path, `dlqRecent`/`recentEvents`/`paused` in `/llm-status`, WR-03 batch-size fix, W4 no raw `process.env.LLM_PIPELINE_V2` reads                                                                            |
| `src/hooks/useLLMStatusPolling.ts`        | ✓ VERIFIED | `LLMStatus` extended with `callHistory`, `tokenCounters`, `dlqCount`, `breakerState`, `evalScore`, `provenanceCounts`, `suspectCount`, `recentEvents`, `paused`                                                                                          |
| `src/types/ui.ts`                         | ✓ VERIFIED | `DevApiStatusTab = 'overview' \| 'water' \| 'sites' \| 'events'`                                                                                                                                                                                         |
| `src/stores/uiStore.ts`                   | ✓ VERIFIED | `setDevApiStatusTab` accepts `'events'` via type inheritance                                                                                                                                                                                             |
| `src/components/ui/DevApiStatus.tsx`      | ✓ VERIFIED | `EventsFiltersSection` with all 8 blocks; `PROVENANCE_COLORS`; `Paused — soft cap` badge; `D-25 gate` label; `llm-replay` fetch button                                                                                                                   |
| `src/types/llm.ts`                        | ✓ VERIFIED | `GeocodeProvenance` 6-value union (client-side mirror of server type)                                                                                                                                                                                    |
| `.env.example`                            | ✓ VERIFIED | `LLM_PIPELINE_V2=false` documented                                                                                                                                                                                                                       |

### Key Link Verification

| From                                    | To                                                              | Via                                                                         | Status  |
| --------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------------------- | ------- |
| `events.ts`                             | `isPipelineV2()`                                                | `const pipelineV2 = isPipelineV2()` inside handler body                     | ✓ WIRED |
| `events.ts`                             | `events:llm:v2` / `events:llm-summary:v2`                       | `LLM_EVENTS_KEY_ACTIVE` / `LLM_SUMMARY_KEY_ACTIVE` ternary                  | ✓ WIRED |
| `llmEventExtractor.ts` barrel           | `processEventGroupsV2` / `v1.processEventGroups`                | `isPipelineV2()` routing at call time                                       | ✓ WIRED |
| `llmEventExtractor.v2.ts`               | `resolveLocation` from `llmResolver.ts`                         | `geocodeEnrichedEventsV2` calls per event                                   | ✓ WIRED |
| `llmEventExtractor.v2.ts`               | `enqueueDLQ` from `llmDLQ.ts`                                   | Zod-fail branch calls `enqueueDLQ` per group                                | ✓ WIRED |
| `llm-provider.ts callLLM`               | `isAvailable`, `incrDailyTokens`, `record`, `appendCallHistory` | Gates + increments + history on every provider attempt                      | ✓ WIRED |
| `llmResolver.ts`                        | `forwardGeocodeConstrained`                                     | POI amenity + direct + verify branches all call constrained adapter         | ✓ WIRED |
| `events.ts /llm-status`                 | `listDLQ`, `loadRecentEnrichedEvents`, `shouldPauseNewEvents`   | Parallel `Promise.all` in handler                                           | ✓ WIRED |
| `DevApiStatus.tsx EventsFiltersSection` | `useLLMStatusPolling LLMStatus`                                 | Consumes all v2 fields from hook                                            | ✓ WIRED |
| `DevApiStatus.tsx DrillDownRow`         | `/api/events/llm-replay/:groupKey`                              | `fetch(POST)` on copy button click                                          | ✓ WIRED |
| `llmEvalHarness.ts runEval`             | `resolveLocation`                                               | Called once per ground-truth event, never `callLLM`                         | ✓ WIRED |
| `events.ts fire-and-forget`             | `runEval`                                                       | Called in v2 branch after `geocodeEnrichedEvents` returns, before cache-set | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artifact                                | Data Variable                         | Source                                                                  | Produces Real Data                                                                              | Status    |
| --------------------------------------- | ------------------------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------- |
| `DevApiStatus.tsx EventsFiltersSection` | `llmStatus`                           | `useLLMStatusPolling` polling `/api/events/llm-status`                  | Yes — server reads from `llmProgress` module singleton + Redis DLQ + `loadRecentEnrichedEvents` | ✓ FLOWING |
| `server/routes/events.ts /llm-status`   | `dlqRecent`, `recentEvents`, `paused` | `listDLQ(50)`, `loadRecentEnrichedEvents(50)`, `shouldPauseNewEvents()` | Yes — Redis SMEMBERS + cache read + token counter reads                                         | ✓ FLOWING |
| `llmEvalHarness.ts runEval`             | `ground-truth events`                 | `readFileSync` on `.planning/eval/ground-truth-events.json`             | Yes — committed 50-event file                                                                   | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for live pipeline execution (requires real LLM provider calls and Redis). Static checks performed instead.

| Behavior                                              | Command                                                                       | Result                           | Status |
| ----------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------- | ------ |
| `isPipelineV2()` exists and is exported               | `grep -q "export function isPipelineV2" server/config.ts`                     | Found                            | ✓ PASS |
| No raw `process.env.LLM_PIPELINE_V2` in events.ts     | `grep -c "process.env.LLM_PIPELINE_V2" server/routes/events.ts`               | 0 matches                        | ✓ PASS |
| Ground-truth file has 50 events                       | `python3 -c "import json; d=json.load(open('...')); print(len(d['events']))"` | 50                               | ✓ PASS |
| BATCH_SIZE === 2 in v2 extractor                      | `grep -q "BATCH_SIZE = 2" server/lib/llmEventExtractor.v2.ts`                 | Found                            | ✓ PASS |
| All 6 provenance paths in resolver                    | Grep for all 6 literal strings                                                | All 6 found at dispatch branches | ✓ PASS |
| CR-01 fix: v1 uses `forwardGeocodeConstrained`        | `grep -q "forwardGeocodeConstrained" server/lib/llmEventExtractor.v1.ts`      | Found                            | ✓ PASS |
| WR-02 fix: DLQ uses raw strings for srem              | `grep -q "withParsed\|x.raw" server/lib/llmDLQ.ts`                            | Found                            | ✓ PASS |
| WR-03 fix: BATCH_SIZE_V2 imported in events.ts        | `grep -q "BATCH_SIZE_V2" server/routes/events.ts`                             | Found                            | ✓ PASS |
| WR-04 fix: single-candidate accepted in verify branch | `grep -q "candidates.length === 1" server/lib/llmResolver.ts`                 | Found                            | ✓ PASS |
| WR-01 fix: throttle after cache-miss in resolver      | `grep -q "WR-01" server/lib/llmResolver.ts`                                   | Found at 3 call sites            | ✓ PASS |

### Requirements Coverage

| Requirement                                    | Plans      | Status      | Evidence                                                                                                    |
| ---------------------------------------------- | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| D-01 Structured place hierarchy                | 02, 06     | ✓ SATISFIED | `locationHierarchyV2` in `llmSchema.ts`; `SYSTEM_PROMPT_V2` instructs hierarchy extraction                  |
| D-02 ME viewbox constraint                     | 04         | ✓ SATISFIED | `forwardGeocodeConstrained` defaults to `ME_VIEWBOX`/`ME_COUNTRY_CODES`                                     |
| D-03 POI amenity path                          | 05         | ✓ SATISFIED | `resolveViaPoiAmenity` with `inferAmenity` + `forwardGeocodeConstrained({amenity})`                         |
| D-04 2-pass LLM verify                         | 05         | ✓ SATISFIED | `resolveViaVerifiedTwoPass` with sanity gate + `RERANKER_JSON_SCHEMA`                                       |
| D-05 LLM never emits coordinates               | 02, 03, 06 | ✓ SATISFIED | `locationHierarchyV2.strict()` rejects lat/lng; `SYSTEM_PROMPT_V2` explicitly forbids coordinates           |
| D-06 News block in prompt                      | 06         | ✓ SATISFIED | `--- NEWS BLOCK (tier-tagged) ---` in `buildBatchUserPromptV2`                                              |
| D-07 Bellingcat block in prompt                | 06         | ✓ SATISFIED | `--- BELLINGCAT OSINT (high-trust) ---` block present                                                       |
| D-08 Temporal context block                    | 06         | ✓ SATISFIED | `--- TEMPORAL CONTEXT ---` block with 3-event window                                                        |
| D-10 BATCH_SIZE reduction                      | 06         | ✓ SATISFIED | `BATCH_SIZE = 2` in v2 extractor                                                                            |
| D-11-D-14 v2 schema fields                     | 02, 06     | ✓ SATISFIED | `enrichedEventV2` has `confidence`, `reasoning`, `weaponType`, `targetType`, `timeOfDay`, `durationMinutes` |
| D-15 Events tab in DevApiStatus                | 09         | ✓ SATISFIED | `EventsFiltersSection` with tab registration                                                                |
| D-16-D-19 8 observability blocks               | 09         | ✓ SATISFIED | All 8 blocks (WaterfallBlock through SuspectBlock) implemented                                              |
| D-20 Eval harness                              | 08         | ✓ SATISFIED | `runEval()` with 3-bucket accuracy; baseline persisted to Redis                                             |
| D-21 `/llm-replay` endpoint                    | 08         | ✓ SATISFIED | Dual-gated dev-only POST endpoint; cache-write-free                                                         |
| D-22 GeocodeProvenance 6-path                  | 02, 03, 05 | ✓ SATISFIED | `GEOCODE_PROVENANCE_VALUES` with 6 string literals; all 6 branches dispatch                                 |
| D-23 Suspect flag                              | 02, 06     | ✓ SATISFIED | `deriveSuspect` in `llmSchema.ts`; `suspectCount` in `llmProgress`                                          |
| D-24 LLM_PIPELINE_V2 flag                      | 01         | ✓ SATISFIED | `isPipelineV2()` helper; W4 single-source-of-truth                                                          |
| D-25 Prod-flip gate                            | 08, 09     | ✓ SATISFIED | `D-25 gate (≥80% @20km): PASS/FAIL` in `EvalScoreBlock`                                                     |
| D-26 v1 preserved for rollback                 | 01         | ✓ SATISFIED | `llmEventExtractor.v1.ts` exists and is wired via barrel                                                    |
| D-27 No shadow mode                            | 08         | ✓ SATISFIED | Eval harness uses resolver-only (A6 invariant)                                                              |
| D-28-D-29 Bounded retry + graceful degradation | 07         | ✓ SATISFIED | 2-attempt retry with backoff; `callLLM` returns null when all providers fail                                |
| D-30 DLQ                                       | 07         | ✓ SATISFIED | `llmDLQ.ts`; 200-entry cap; 7d TTL                                                                          |
| D-31 Circuit breaker                           | 07         | ✓ SATISFIED | `llmCircuitBreaker.ts`; 10-call sliding window; 30% threshold; 5-min pause                                  |
| D-32 Token budget                              | 07         | ✓ SATISFIED | `llmTokenBudget.ts`; Redis `llm:tokens:{provider}:YYYY-MM-DD`; 48h TTL                                      |
| D-33 Soft-cap pause                            | 07         | ✓ SATISFIED | `shouldPauseNewEvents()` gates fire-and-forget block                                                        |
| D-34 Hard-cap skip                             | 07         | ✓ SATISFIED | `budgetState === 'hard'` skips provider in `callLLM`                                                        |
| D-35 Priority sort                             | 07         | ✓ SATISFIED | `prioritizeBySeverity()` called before extraction when soft-capped                                          |
| D-36 Budget bars in DevApiStatus               | 09         | ✓ SATISFIED | `BudgetBarsBlock` with Cerebras/Groq progress bars                                                          |
| D-37 v2 cache keys                             | 01         | ✓ SATISFIED | `events:llm:v2` / `events:llm-summary:v2` present                                                           |
| D-38 Dev file cache v2                         | 01         | ✓ SATISFIED | `saveDevLLMCacheV2`/`loadDevLLMCacheV2` writing `llm-events-v2.json`                                        |
| D-39 schemaVersion on summary                  | 01, 02     | ✓ SATISFIED | Optional `schemaVersion: 'v1' \| 'v2'` on `LLMRunSummary` and `LLMPipelineProgress`                         |
| D-40 v1 rollback path                          | 01         | ✓ SATISFIED | `LLM_PIPELINE_V2=false` restores v1 behavior bit-for-bit                                                    |

### Anti-Patterns Found

| File                                      | Pattern                                                                                                                           | Severity | Impact                                                                                                                                             |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.planning/eval/ground-truth-events.json` | `contains: 'verifiedAt'` in plan spec not met — file uses `curatedAt` at file level; individual events have no `verifiedAt` field | ℹ️ Info  | No functional impact — `llmEvalHarness.ts` does not reference `verifiedAt`; the harness loads `events[]` and uses `truth.lat`/`truth.lng` directly |

No blockers or warnings identified. The 3 Info items from the code review (IN-01, IN-02, IN-03) were explicitly deferred by user decision.

### Pre-existing Test Failures (Not Introduced by Phase 27.4)

Per `deferred-items.md`:

- `src/__tests__/filters.test.ts`: 32 failures (precision-filter fixture mismatch, pre-dating Phase 27.4)
- `src/__tests__/entityLayers.test.ts`: 1 failure (`ENTITY_COLORS.other` constant, pre-dating Phase 27.4)
- TypeScript errors in `llmEventExtractor.v1.ts` (lines 146-301) and `events.ts` (lines 173, 180): 31 pre-existing TS errors

These are documented pre-existing issues. Phase 27.4 introduced zero new test regressions (server: 11695/11695 pass; client new tests: all pass).

### Human Verification Required

**1. End-to-end v2 pipeline with Events tab**

**Test:** In a local dev environment, set `LLM_PIPELINE_V2=true` in `.env`, start the server, trigger a cache miss for `/api/events`, wait for the fire-and-forget LLM block to run, then open DevApiStatus modal.

**Expected:** Events tab appears (`showEventsTab = llmStatus?.schemaVersion === 'v2' && import.meta.env.DEV`); all 8 blocks render with data; `D-25 gate` shows PASS or FAIL; `Paused — soft cap` badge hidden (unless soft-cap applies); drill-down shows recent enriched events with provenance color coding.

**Why human:** Tab visibility requires `schemaVersion === 'v2'` which only populates after a real LLM pipeline run completing. Cannot simulate without live Cerebras/Groq API calls.

**2. Cold-start v2→v1 fallback (no blank map)**

**Test:** With `LLM_PIPELINE_V2=true`, flush `events:llm:v2` from Redis but leave `events:llm` populated. Hit `/api/events`.

**Expected:** Response returns events (from v1 key as fallback per Pitfall 1 mitigation); map does not go blank; `/api/events/llm-status` shows `schemaVersion: 'v1'` for the served data.

**Why human:** Requires live Redis and coordinated cache manipulation.

**3. Ground-truth events source verification**

**Test:** Open `.planning/eval/ground-truth-events.json`. For a sample of events (suggest 10), click `sourceUrl` and confirm the article describes the event at the stated lat/lng with the claimed landmark name.

**Expected:** Source URLs are accessible; articles confirm the event description; coordinates are accurate to the stated precision level (`exact` → within 500m, `city` → within 20km).

**Why human:** Source URL content accuracy requires human judgment; 50-event corpus means automated link-checking is feasible but content accuracy checking is not.

### Gaps Summary

No gaps blocking goal achievement. All 12 observable truths are VERIFIED. All D-01 through D-40 requirements are satisfied by committed code. The 1 CR and 4 WRs from the code review are all fixed (committed per `27.4-REVIEW-FIX.md`). The 3 Info items (IN-01/IN-02/IN-03) were explicitly deferred by user decision and are not actionable gaps.

The minor artifact spec discrepancy (`contains: 'verifiedAt'` in Plan 08 vs `curatedAt` at file level in the actual JSON) has no functional impact — the eval harness does not reference `verifiedAt` and loads the file successfully.

Status is `human_needed` rather than `passed` because 3 human verification items require a live environment with real LLM provider calls, Redis access, and source-URL review to fully close.

---

_Verified: 2026-04-20T17:00:00Z_
_Verifier: Claude (gsd-verifier)_

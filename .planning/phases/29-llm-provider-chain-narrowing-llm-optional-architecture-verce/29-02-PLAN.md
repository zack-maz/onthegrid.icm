---
phase: 29
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SIMPLIFY-06
  - D-03
must_haves:
  truths:
    - 'Every Phase 29 reference to "ADR-0009" (for the v1.5 retirement ADR) is updated to "ADR-0010"'
    - 'Phase 29 ROADMAP success criterion #3 (the "v1+v2 still importable" line that conflicts with D-02) is reconciled — language updated to match D-02'
    - 'docs/adr/0009-two-key-split-for-llm-partial-progress-vs-terminal-reads.md remains untouched (existing Accepted ADR)'
    - 'No new docs/adr files created in this plan (Plan 11 creates ADR-0010 stub)'
  artifacts:
    - path: '.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md'
      provides: 'D-03 + canonical_refs reference ADR-0010 (not 0009)'
      contains: 'ADR-0010-llm-pipeline-v1-5-decisions.md'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'DOCS-PUB-04 (and any other v1.5 ADR refs) point at ADR-0010'
      contains: 'ADR-0010'
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 29 entry references ADR-0010; success criterion #3 reconciled with D-02'
      contains: 'ADR-0010'
  key_links:
    - from: 'ROADMAP.md Phase 29 success criterion #3'
      to: 'CONTEXT.md D-02 (v1+v2 modules DELETED)'
      via: 'reconciliation edit'
      pattern: 'Replace "still importable as deep-rollback safety" with "deletion supersedes the Phase 27.4 D-26/D-40 deep-rollback lock; rollback is git revert"'
---

<objective>
Resolve the ADR numbering collision (Pitfall 1 in RESEARCH.md) before any code work begins: the existing `docs/adr/0009-two-key-split-for-llm-partial-progress-vs-terminal-reads.md` already occupies the 0009 slot, so the Phase 29 stub must be ADR-0010. Update CONTEXT.md, REQUIREMENTS.md, and ROADMAP.md "ADR-0009" mentions to "ADR-0010". Also reconcile the ROADMAP success criterion #3 conflict with CONTEXT D-02 (success criterion #3 says "v1 + v2 extractor code paths are still importable as deep-rollback safety" — but D-02 explicitly DELETES them; D-02 supersedes the older criterion).

Purpose: a small, mechanical, doc-only commit landed alongside the maxDuration bump in Wave 1 so the rest of the phase doesn't ship references to a non-existent ADR or a conflicting success criterion. Bundled as a single commit so revert is trivial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md
@.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
From `docs/adr/` (verified via `ls`): 0001-0009 + README.md + template.md exist. **0009 is the two-key-split ADR (Accepted 2026-04-24).** Phase 29 stub must be **0010**.

From ROADMAP.md Phase 29 success criterion #3 (conflicts with CONTEXT D-02):

> "v1 + v2 extractor code paths are still importable as deep-rollback safety per Phase 27.4 D-26/D-40 (operator can flip back via `POST /api/events/llm-pipeline {version: 'v1'}` or `'v2'`); a quick smoke test from the dev DevApiStatus pill exercises both paths."

The CONTEXT D-02 explicit supersession language wins. The ROADMAP criterion must be replaced with the actual D-02 contract.
</interfaces>
</context>

<threat_model>

- T-29-02-01 (severity: low): A grep for "ADR-0009" in the Phase 29 source set still returns matches after this commit — i.e., we miss a reference. Mitigation: acceptance_criteria grep is exhaustive across the 3 target files plus the phase directory.
- T-29-02-02 (severity: low): An unrelated reference to "ADR-0009" (referring legitimately to the existing two-key-split ADR) gets accidentally renamed. Mitigation: per-file search-and-replace is contextual — only references in the v1.5 retirement context change.
  </threat_model>

<tasks>
<task id="29-02-01" name="Update CONTEXT.md ADR-0009 references → ADR-0010">
  <read_first>
    - .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md
  </read_first>
  <action>
    Edit `.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md`. Replace every reference to the **v1.5 retirement ADR** numbered as 0009 with 0010:

    - D-03 section heading + body: `ADR-0009 at Phase 37` → `ADR-0010 at Phase 37`; `docs/adr/ADR-0009-llm-pipeline-v1-5-decisions.md` → `docs/adr/ADR-0010-llm-pipeline-v1-5-decisions.md`
    - Any "ADR-0009 stub" mentions → "ADR-0010 stub"
    - Claude's Discretion entry mentioning "ADR-0009 stub structure" → "ADR-0010 stub structure"
    - Canonical Refs section `docs/adr/ADR-0009-llm-pipeline-v1-5-decisions.md` → `docs/adr/ADR-0010-llm-pipeline-v1-5-decisions.md`
    - D-02 supersedes language: `D-03 ADR-0009 stub MUST cite` → `D-03 ADR-0010 stub MUST cite`

    DO NOT rename references to ADR-0009 that mean **the two-key-split ADR** (existing Accepted ADR) — they refer to a different ADR. Phase 29 CONTEXT.md likely has only one such reference under "Phase 27.4 Lineage" or similar — preserve it.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "ADR-0009" .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md | tr -d ' '</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "ADR-0010-llm-pipeline-v1-5-decisions" .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md | grep -qE '^[1-9]'</automated>
    <manual>Reviewer skims diff and confirms only **v1.5 retirement ADR** references were renamed (not references to the existing two-key-split ADR).</manual>
  </acceptance_criteria>
</task>

<task id="29-02-02" name="Update REQUIREMENTS.md ADR-0009 → ADR-0010 where context is v1.5 retirement">
  <read_first>
    - .planning/REQUIREMENTS.md (grep `ADR-0009` first to locate)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Search for `ADR-0009`. For each match:

    - If the surrounding context is **v1.5 LLM pipeline retirement** (DOCS-PUB-04, LLM-RELI-07, SIMPLIFY-06 references) → rename to `ADR-0010`.
    - If the surrounding context is **two-key-split / partial-progress** (Phase 27.4.1 lineage) → leave unchanged.

    DOCS-PUB-04 is the requirement that drives the ADR; its description likely references ADR-0009. Update that reference to ADR-0010.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -nP 'ADR-0009.*v1\.5|v1\.5.*ADR-0009' .planning/REQUIREMENTS.md | wc -l | tr -d ' ' | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep "ADR-0010" .planning/REQUIREMENTS.md | wc -l | tr -d ' ' | grep -qE '^[1-9]'</automated>
  </acceptance_criteria>
</task>

<task id="29-02-03" name="Update ROADMAP.md Phase 29 ADR-0009 → ADR-0010 + reconcile success criterion #3 with D-02">
  <read_first>
    - .planning/ROADMAP.md (Phase 29 section, success criteria 1-8)
    - .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md (D-02 supersession language)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. Two changes:

    **Change 1 — ADR numbering (in Phase 29 section + Phase 37 + any other v1.5-retirement references):**
    Rename `ADR-0009` → `ADR-0010` where context is the v1.5 retirement ADR (the success criterion #8 in Phase 29 already mentions `ADR-0009 stub` — that's the target).

    **Change 2 — reconcile success criterion #3 with CONTEXT D-02:**
    Current Phase 29 success criterion #3 (per RESEARCH.md Pitfall 1 cousin):
    > "v1 + v2 extractor code paths are still importable as deep-rollback safety per Phase 27.4 D-26/D-40 (operator can flip back via `POST /api/events/llm-pipeline {version: 'v1'}` or `'v2'`); a quick smoke test from the dev DevApiStatus pill exercises both paths."

    Replace with:
    > "v1 + v2 extractor modules + the `POST /api/events/llm-pipeline` override route + the DevApiStatus Pin-to-v1/v2 buttons are DELETED (D-02 supersedes Phase 27.4 D-26/D-40 deep-rollback lock). Rollback path is `git revert <Phase 29 commit range>` — wall-clock minutes, not a Bearer-POST flip. Rationale captured in ADR-0010 stub written this phase."

    Renumber adjacent criteria only if the text structure requires it; if the original list was 1-8 and #3 was the "still importable" line, the replacement keeps it at #3 and the rest stay 1-8.

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -nP 'ADR-0009.*v1\.5|v1\.5.*ADR-0009' .planning/ROADMAP.md | wc -l | tr -d ' ' | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep "still importable as deep-rollback" .planning/ROADMAP.md | wc -l | tr -d ' ' | grep -q '^0$'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep "ADR-0010" .planning/ROADMAP.md | wc -l | tr -d ' ' | grep -qE '^[1-9]'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep "git revert <Phase 29 commit range>" .planning/ROADMAP.md | wc -l | tr -d ' ' | grep -qE '^[1-9]'</automated>
  </acceptance_criteria>
</task>

<task id="29-02-04" name="Commit chore(29): ADR numbering + roadmap reconciliation">
  <read_first>
    - git status (post-edits)
  </read_first>
  <action>
    Single commit bundling all 3 doc edits:

    ```bash
    git add .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md \
            .planning/REQUIREMENTS.md \
            .planning/ROADMAP.md
    git commit -m "chore(29): align ADR numbering 0009 → 0010 + reconcile ROADMAP SC#3 with D-02

    Pitfall 1 (per 29-RESEARCH.md): docs/adr/0009-* is already taken by the
    Accepted two-key-split ADR (committed 2026-04-24). The Phase 29 v1.5
    retirement stub written in Plan 11 must be ADR-0010. CONTEXT D-03,
    REQUIREMENTS DOCS-PUB-04, and ROADMAP Phase 29 + Phase 37 references
    all updated.

    Pitfall 1 cousin: ROADMAP Phase 29 success criterion #3 said \"v1+v2
    still importable as deep-rollback safety\" — that contradicted
    CONTEXT D-02's explicit deletion decision. CONTEXT supersedes ROADMAP
    per the 2026-05-09 commit ordering. SC#3 reworded to match D-02:
    modules DELETED, rollback path is git revert.

    Requirements: SIMPLIFY-06, D-03."
    ```

  </action>
  <acceptance_criteria>
    <automated>cd /Users/zackmaz/Desktop/my_world && git log -1 --format='%s' | grep -q 'align ADR numbering 0009 → 0010'</automated>
    <automated>cd /Users/zackmaz/Desktop/my_world && git log -1 --stat | grep -E 'CONTEXT|REQUIREMENTS|ROADMAP' | wc -l | tr -d ' ' | grep -qE '^[3-9]'</automated>
  </acceptance_criteria>
</task>
</tasks>

<verification>
  <automated>cd /Users/zackmaz/Desktop/my_world && grep -rnP 'ADR-0009.*(v1\.5|narrowing|retirement|stub)' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/ 2>/dev/null | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  <automated>cd /Users/zackmaz/Desktop/my_world && grep -rn "ADR-0010" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md | wc -l | tr -d ' ' | grep -qE '^[3-9]'</automated>
  <automated>cd /Users/zackmaz/Desktop/my_world && grep "still importable as deep-rollback" .planning/ROADMAP.md | wc -l | tr -d ' ' | grep -q '^0$'</automated>
</verification>

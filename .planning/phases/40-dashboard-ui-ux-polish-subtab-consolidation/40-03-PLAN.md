---
phase: 40-dashboard-ui-ux-polish-subtab-consolidation
plan: 03
type: execute
wave: 3
depends_on: ['40-02']
files_modified:
  - src/components/ui/DevApiStatus.tsx
autonomous: false
requirements: [UI-POLISH-04]
must_haves:
  truths:
    - 'TabButton shows a focus-visible ring (keyboard focus only, not on mouse click)'
    - 'The active tab carries a 2px accent-blue bottom indicator readable even in greyscale'
    - 'Arrow keys move roving focus between tabs without activating; Enter/Space activates'
    - 'Each tab panel container has role=tabpanel + aria-labelledby'
  artifacts:
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'tab-bar interaction affordances (focus-visible, active indicator, roving tabindex, tabpanel roles)'
      contains: 'focus-visible:ring-2'
  key_links:
    - from: 'src/components/ui/DevApiStatus.tsx tablist onKeyDown'
      to: 'setDevApiStatusTab (activation) + roving tabindex (focus)'
      via: 'ArrowLeft/Right/Home/End/Enter/Space handler'
      pattern: 'ArrowRight'
---

<objective>
Add the tab-bar INTERACTION affordances (UI-POLISH-04 / D-04) to the dev-shell tab bar: a focus-visible ring on `TabButton`, a 2px accent-blue active-tab indicator, roving-tabindex keyboard navigation (arrow keys move focus without activating; Enter/Space activates), and `role="tabpanel"` + `aria-labelledby` on the panel containers. This is sequenced AFTER Plan 02 because it touches the same file (`DevApiStatus.tsx`, the TabButton :258 + tablist :708 + panels :780 regions) — a separate wave avoids a single-file merge conflict.

Purpose: Make the active tab read unambiguously (even in a greyscale screenshot), make the tab bar keyboard-navigable to WAI-ARIA tablist spec, and confirm agent-native parity — all WITHOUT a chrome restyle (Phase 41 REVEAL-SITE-01 owns the shell's visual reveal per D-04b; do NOT change TabButton colors/padding/radius/font or the sticky-header chrome).
Output: focus-visible ring + active 2px indicator + roving keyboard nav on the tab bar + tabpanel roles; agent-native parity verified against the UI-SPEC expected-pass manifest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-UI-SPEC.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-PATTERNS.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan adds behavior to existing elements (no new top-level testids), but introduces:

- `focus-visible:ring-2 focus-visible:ring-accent-blue/60` on `TabButton` (and shared with group-header collapse buttons + drawer trigger + retry buttons)
- a 2px active-tab indicator (`border-b-2 border-accent-blue` or `after:` pseudo) on the active `TabButton`
- `tabIndex={active ? 0 : -1}` roving on each `TabButton`
- an `onKeyDown` roving-nav handler on the tablist (`role="tablist"` at :708)
- `role="tabpanel"` + `aria-labelledby="{tab-id}"` on the 4 panel containers (:780–790, currently missing)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible ring + 2px active indicator + roving tabindex to TabButton (affordances only — no chrome restyle)</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx :258–285 (TabButton — already `role="tab"` + `aria-selected`; className `flex items-center gap-1 rounded-md px-3 py-1 text-[10px] font-medium ... active ? 'bg-white/10 text-white' : ...`)
    - src/components/ui/DevApiStatus.tsx :708–750 (tablist `<div role="tablist">` + the 4 TabButton call sites with `active={activeTab === '...'}` + `onClick={() => setTab('...')}`)
    - 40-UI-SPEC.md §Tab-Bar Interaction Affordances → "Focus state" + "Active-tab affordance" subsections (the EXACT focus-visible classes + the D-04b lockdown: do NOT change colors/padding/radius/font)
    - 40-PATTERNS.md §"DevApiStatus.tsx — TabButton + tablist a11y" ("additive only — D-04b chrome-restyle lockdown")
  </read_first>
  <action>
    Extend `TabButton` ADDITIVELY (D-04b lockdown — do NOT change `px-3 py-1`, `rounded-md`, the active `bg-white/10 text-white` colors, or the font):
    - Append to the existing className: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-blue/60 focus-visible:ring-offset-1 focus-visible:ring-offset-black` (focus-visible only — no ring on mouse click).
    - Add the 2px active-tab bottom indicator: when `active`, append `border-b-2 border-accent-blue` (or an `after:` pseudo bar) so the active tab reads unambiguously in greyscale. This is an affordance ADDITION, not a chrome restyle — keep a code comment flagging it for Phase 41 to preserve.
    - Add `tabIndex={active ? 0 : -1}` to the TabButton `<button>` (roving — only the active tab is in the natural Tab order; arrow keys move focus among the rest).
    Apply the SAME `focus-visible:ring-2 focus-visible:ring-accent-blue/60 ...` ring (no active-indicator, no roving) to the group-header collapse buttons + drawer trigger + per-endpoint retry buttons created/relocated in Plan 02 (the UI-SPEC lists these as sharing the focus ring).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TabButton className contains `focus-visible:ring-2 focus-visible:ring-accent-blue/60`
    - The active TabButton carries a 2px indicator (`border-b-2 border-accent-blue` or an `after:` bar); inactive tabs do not
    - TabButton has `tabIndex={active ? 0 : -1}`
    - TabButton's `px-3 py-1`, `rounded-md`, and active `bg-white/10 text-white` are UNCHANGED (grep confirms the original class fragments still present)
    - `npx tsc --noEmit` exits 0; tabMerge test passes
  </acceptance_criteria>
  <done>TabButton has a focus-visible accent-blue ring, a 2px active indicator, and roving tabIndex; chrome (colors/padding/radius/font) unchanged; the shared focus ring is applied to group/drawer/retry buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Add roving-tabindex keyboard navigation to the tablist + role=tabpanel/aria-labelledby to the panels</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx :708 (the `<div className="flex items-center gap-1" role="tablist">` wrapper) + :713–749 (the 4 conditional TabButton call sites — note `showApiHealthTab`/`showWaterTab`/`showSitesTab`/`showEventsTab` gates mean the visible tab set is dynamic; the roving handler must operate over the CURRENTLY-rendered tabs)
    - src/components/ui/DevApiStatus.tsx :779–800 (the panel containers — `activeTab === 'apiHealth' && ...`, water/sites/events — currently lack `role="tabpanel"` + `aria-labelledby`)
    - 40-UI-SPEC.md §Tab-Bar → "Keyboard navigability (roving tabindex — EXACT bindings)" table (ArrowRight/Left wrap, Home/End, Enter/Space activate, Tab exits) + the "each panel gets role=tabpanel + aria-labelledby" requirement
    - 40-PATTERNS.md §"DevApiStatus.tsx — TabButton + tablist a11y" (the onKeyDown handler + tabpanel additions)
  </read_first>
  <action>
    Add an `onKeyDown` handler to the tablist `<div role="tablist">` (:708) implementing roving-tabindex nav over the currently-rendered tabs (compute the ordered list of visible tab ids from the `showXTab` gates; manage focus via refs or `data-testid`/`document` query scoped to the tablist):
    - `ArrowRight` / `ArrowLeft`: move focus to next / previous visible tab, WRAPPING at the ends; does NOT activate (manual-activation pattern — focus moves, `setDevApiStatusTab` is NOT called).
    - `Home` / `End`: focus first / last visible tab.
    - `Enter` / `Space`: activate the focused tab (`setTab(focusedTabId)` → `setDevApiStatusTab`).
    - `Tab`: falls through (roving tabindex already ensures only the active tab has `tabindex=0`, so the browser moves focus out of the tablist into the panel).
    Add `role="tabpanel"` + `aria-labelledby="{tab-id}"` to EACH of the 4 panel containers at :780–790 (e.g. the API Health panel gets `aria-labelledby="tab-api-health"` matching the TabButton testid/id; ensure the TabButton has an `id` matching the `aria-labelledby` — add an `id` prop to TabButton if needed, additively). Within the API Health panel, the Plan-02 group-header collapse buttons + drawer controls stay in normal DOM tab order (no roving) so `Tab` walks them top-to-bottom.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The tablist `<div role="tablist">` has an `onKeyDown` handler; firing `ArrowRight` on a focused tab moves roving focus to the next visible tab WITHOUT calling `setDevApiStatusTab` (focus moves, active tab unchanged); `Enter`/`Space` on the focused tab activates it
    - `ArrowRight` from the last visible tab wraps to the first; `Home`/`End` focus first/last
    - Each of the 4 panel containers (:780–790) has `role="tabpanel"` and `aria-labelledby` pointing at its tab's id
    - `npx tsc --noEmit` exits 0; tabMerge test passes
  </acceptance_criteria>
  <done>The tablist supports ArrowLeft/Right (wrapping, focus-only), Home/End, Enter/Space activation per the UI-SPEC bindings; panels carry role=tabpanel + aria-labelledby.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Agent-native parity verification (UI-POLISH-04 / SC40-2)</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <action>
    Run the `compound-engineering:review:agent-native-reviewer` agent against `src/components/ui/DevApiStatus.tsx` (the consolidated `DevApiStatusAllApisTab` + tab bar) using the UI-SPEC §"Agent-native parity" table as the expected-pass manifest. The consolidation (Plan 02 groups/drawer/hero + Plan 03 tab affordances) must introduce NO UI-only action — every interactive control maps to a Bearer-gated endpoint or query-param. Then pause for human confirmation per the checkpoint below.
  </action>
  <what-built>
    Agent-native parity verification (UI-POLISH-04 / SC40-2). The consolidated layout must introduce NO UI-only action — every interactive control maps to a Bearer-gated endpoint or query-param. The `agent-native-reviewer` runs against the UI-SPEC §"Agent-native parity" expected-pass manifest:
    Per-endpoint "Refresh now" → `GET /api/{endpoint}?_ts=...`;
    Replay probe (drawer) → `POST /api/events/llm-replay/test`;
    Prune (drawer) → `POST /api/events/prune-dead-urls`;
    Hero / group / drawer collapse-expand → pure view state, exempt (read-only presentation).
  </what-built>
  <how-to-verify>
    1. Run the `compound-engineering:review:agent-native-reviewer` agent against `src/components/ui/DevApiStatus.tsx` with the UI-SPEC §Agent-native parity table as the expected-pass manifest.
    2. Confirm every data-mutating UI control resolves to a Bearer-gated endpoint / query-param (the 3 mutating controls above); confirm the collapse/drawer/tab toggles are correctly classified as pure view-state (exempt).
    3. Confirm the consolidation added NO new UI-only action (e.g. no client-only state that can't be reproduced via an endpoint).
    4. Type "approved" if parity holds, or describe any UI-only action the reviewer flags so it can be wired to an endpoint/param.
  </how-to-verify>
  <verify>
    <automated>MISSING — agent-native parity is a review checkpoint; the agent-native-reviewer report + human approval is the verification artifact (no test command). Capture the reviewer verdict in 40-03-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - agent-native-reviewer reports every data-mutating control maps to a Bearer-gated endpoint / query-param per the expected-pass manifest
    - collapse/drawer/tab toggles classified as pure view-state (exempt)
    - reviewer flags NO new UI-only action introduced by the consolidation
    - human types "approved"
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the flagged UI-only action(s)</resume-signal>
  <done>agent-native-reviewer confirms parity (no new UI-only action); verdict captured in 40-03-SUMMARY.md; human approved.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                                                            |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| keyboard/focus events → tab view-state | Untrusted only in the trivial sense of DOM events; no data crosses to the server. Tab activation calls `setDevApiStatusTab` (pure client view-state). No new boundary. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                             |
| ---------- | ---------------------- | ----------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| T-40-03-01 | Tampering              | tablist `onKeyDown` roving handler        | accept      | Handler only moves focus / sets client tab state; no eval, no data mutation, no network call. No injection surface.         |
| T-40-03-02 | Information disclosure | role=tabpanel / aria-labelledby additions | accept      | ARIA wiring on an operator-only surface (`shouldRenderDashboard()` gate). Adds assistive-tech labels only; no data exposed. |
| T-40-03-SC | Tampering              | npm/pip/cargo installs                    | n/a         | No package installs in this plan.                                                                                           |

**Verdict:** No new attack surface. Tab affordances are pure client-side focus + ARIA + CSS. The agent-native parity checkpoint CONFIRMS the consolidation introduces no UI-only action (every mutating control keeps its existing Bearer-gated endpoint). No HIGH/MEDIUM threats.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx` passes
- TabButton className still contains the original `px-3 py-1 rounded-md` + active `bg-white/10 text-white` (chrome unchanged — D-04b lockdown honored)
- agent-native-reviewer parity checkpoint approved (no new UI-only action)
</verification>

<success_criteria>

- Focus-visible accent-blue ring on TabButton (+ group/drawer/retry buttons); no ring on mouse click
- 2px accent-blue active-tab indicator (greyscale-readable)
- Roving-tabindex keyboard nav per the EXACT UI-SPEC bindings (Arrow wrap focus-only, Home/End, Enter/Space activate)
- role=tabpanel + aria-labelledby on all 4 panels
- TabButton chrome (colors/padding/radius/font) UNCHANGED (Phase 41 owns the visual reveal — D-04b)
- agent-native parity verified against the expected-pass manifest (UI-POLISH-04 / SC40-2)
  </success_criteria>

<output>
Create `.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-03-SUMMARY.md` when done
</output>

---
phase: 40-dashboard-ui-ux-polish-subtab-consolidation
plan: 02
type: execute
wave: 2
depends_on: ['40-01']
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/components/ui/BudgetBlock.tsx
  - src/components/ui/FlightRecorderBlock.tsx
autonomous: true
requirements: [UI-POLISH-01, UI-POLISH-02, UI-POLISH-03]
must_haves:
  truths:
    - 'API Health tab body renders exactly 4 collapsible groups + 1 hero header + 1 drawer (down from ~13 stacked sub-blocks)'
    - 'All 4 groups + the hero render even when every data source is null (honest degraded render, never self-hide)'
    - 'Destructive controls (Replay probe, Prune button) live in a default-CLOSED drawer; their read-only counters stay in Group 4'
    - 'The tier banner + sparkline + status dots use the new --color-status-* tokens (zero visual change — byte-identical hex)'
  artifacts:
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'DevApiStatusAllApisTab restructured into hero + 4 groups + drawer'
      contains: 'data-testid="api-health-hero"'
    - path: 'src/components/ui/BudgetBlock.tsx'
      provides: 'muted-placeholder degrade (no longer self-hides on null)'
      contains: 'budget-block-placeholder'
    - path: 'src/components/ui/FlightRecorderBlock.tsx'
      provides: 'muted-placeholder degrade (no longer self-hides on null)'
      contains: 'flight-recorder-placeholder'
  key_links:
    - from: 'src/components/ui/DevApiStatus.tsx group headers'
      to: 'src/stores/uiStore.ts toggleDevApiGroup'
      via: 'onClick + devApiGroupCollapsed[slug]'
      pattern: 'toggleDevApiGroup'
    - from: 'src/components/ui/DevApiStatus.tsx drawer'
      to: 'src/stores/uiStore.ts isOperatorDrawerOpen'
      via: 'isOperatorDrawerOpen gate'
      pattern: 'isOperatorDrawerOpen'
    - from: 'src/components/ui/DevApiStatus.tsx hero'
      to: 'opStatus.tokenBudget / prune.deadUrlCount + health.endpoints + FlightRecorder last-run'
      via: 'existing polls (no new fetch)'
      pattern: 'api-health-hero'
---

<objective>
The CORE restructure. Take `DevApiStatusAllApisTab` (DevApiStatus.tsx :873–1772) from ~13 stacked sub-blocks into the consolidated shape mandated by the approved 40-UI-SPEC (the SC40-1 design contract that gates all code per D-07) §Section Grouping (the live-DOM inventory is the source of truth per D-08, NOT the stale UI-POLISH-02 sub-block list): a read-only HERO HEADER (D-05) + 4 collapsible grouped sections (D-01/D-02) + an "Operator Controls" DRAWER (D-01/D-02a) holding the destructive/rarely-used buttons. Apply the visual polish pass (type hierarchy, 4px spacing, tabular-nums) per D-03, migrate the tier-banner/sparkline status dots onto the Plan-01 `--color-status-*` tokens (zero-visual-change rename), and convert BudgetBlock + FlightRecorderBlock from self-hide to the muted-placeholder degrade pattern (D-06). Other tabs' content (water/sites/events) is OUT of scope (D-04a) — this plan touches only the API Health tab body. Tab-bar interaction affordances are Plan 03 (sequenced after this to avoid a single-file merge conflict on DevApiStatus.tsx).

Purpose: Drive the operator/portfolio "mission-control" surface to be scannable in 2 seconds (hero) and look intentional even when data is degraded (D-06 honest render), without hiding data one-group-at-a-time (collapsible, default-expanded — not sub-tabs).
Output: A restructured `DevApiStatusAllApisTab` with the hero + 4 `group-*` sections + `operator-drawer`, the muted-placeholder convention unified across BudgetBlock/FlightRecorderBlock/actor-quality, and the status-token migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-UI-SPEC.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-PATTERNS.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols / data-testids this plan creates (excluded from drift verification):

**Hero header (new, top of `DevApiStatusAllApisTab`):**

- `<header data-testid="api-health-hero" role="status">` with 4 fields, each with its own muted fallback:
  - `api-health-hero-endpoints` (N/M healthy), `api-health-hero-llm` (last run outcome + relative time), `api-health-hero-budget` (% of cap + soft/hard dot), `api-health-hero-deadurls` (dead-URL count)

**4 collapsible group sections:**

- `group-endpoint-health`, `group-llm-pipeline`, `group-budget-cost`, `group-operator-actions` (each `<section data-testid="group-{slug}" aria-labelledby="group-{slug}-header">` with a `<button id="group-{slug}-header" aria-expanded aria-controls="group-{slug}-body">` + chevron + `<div id="group-{slug}-body" hidden={collapsed}>`)

**Drawer:**

- `operator-drawer-trigger` (button at foot of Group 4), `operator-drawer` (`<div role="region">` gated by `isOperatorDrawerOpen`)

**Muted-placeholder testids (D-06):**

- `budget-block-placeholder`, `flight-recorder-placeholder`, `actor-quality-placeholder`, `group-operator-actions-placeholder` (group-level when opStatus null)

NOTE existing testids that MOVE but are NOT renamed (regression tests assert them): `tier-summary-banner`, `audit-result-banner`, `all-apis-row-{name}`, `expanded-row-{name}`, `api-health-sparkline-{name}`, `api-health-retry-{name}`, `llm-pipeline-section`, `adversarial-eval-row`, `operator-actions-24h-count`, `operator-actions-bearer-row-*`, `dead-url-count`, `dead-url-list`, `actor-quality-row`/`-list`, `replay-quota-alert`, `prune-quota-alert`, `replay-test-trigger`, `prune-dead-urls-trigger`, `budget-block`, `flight-recorder` (NOTE: the FlightRecorderBlock root testid is `flight-recorder`, NOT `flight-recorder-block` — the UI-SPEC inventory uses `flight-recorder-block` only as a label; preserve the EXISTING `flight-recorder` testid).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure DevApiStatusAllApisTab into hero header + 4 collapsible groups + drawer (D-01/D-02/D-05) with the visual polish pass (D-03)</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx :873–932 (DevApiStatusAllApisTab signature + the inline `OperatorStatus` interface — it carries `audit24h`, `byBearer`, `advEval`, `prune?`, `actorQuality?`, and the Phase 39 `tokenBudget?` field — + `expandedRow` state)
    - src/components/ui/DevApiStatus.tsx :1306–1341 (tier-summary banner — uses `var(--color-site-healthy)` :1314, `var(--color-site-attacked)` :1322, `var(--color-event-airstrike)` :1330 — these flip to status tokens in Task 2)
    - src/components/ui/DevApiStatus.tsx :1352–1421 (audit-result banner + loading/error/empty/stale states)
    - src/components/ui/DevApiStatus.tsx :1422–1531 (per-endpoint quality table + expanded row + retry button at :1507 + sparkline at :1190/:1204)
    - src/components/ui/DevApiStatus.tsx :1543–1772 (LLMPipelineSection :1543, operator-actions section :1557–1759, replay probe :1617, dead-URL/prune :1637–1679, actor-quality :1689–1742, BudgetBlock mount :1747, prune-quota-alert :1751, FlightRecorderBlock mount :1765)
    - server/lib/healthSchema.ts :25,48,94,108 (HealthResponse.endpoints is Record<string,EndpointHealth>; EndpointHealth.status ∈ 'healthy'|'degraded'|'unhealthy'|'unknown' — hero field 1 counts status==='healthy' over Object.keys(endpoints).length)
    - src/components/layout/DetailPanelSlot.tsx :159–168 (the `animate-slide-in-right` usage idiom for the drawer) + src/styles/app.css :140–164 (keyframe + 150ms timing) + :33 (--width-detail-panel 360px)
    - 40-UI-SPEC.md §Section Grouping (live-DOM inventory table + the 4-group/drawer/hero mapping — authoritative), §Hero Header Contract (EXACT field list + order), §Collapsible Group Contract (wrapper/header/chevron/body/state), §Drawer Contract (membership EXACT + scoped-Escape + copy), §Typography (4 roles / 2 weights), §Spacing (xs/sm/lg/xl = 4/8/16/24), §Copywriting
    - 40-PATTERNS.md §"DevApiStatus.tsx — collapsible <section> group" (Analog A LLMPipelineSection :1543 + Analog B expand-row :1490) + §"DevApiStatus.tsx — drawer / slide-out"
  </read_first>
  <action>
    Restructure the JSX returned by `DevApiStatusAllApisTab` (currently the flat stack :1306–1770) into: HERO → GROUP 1 → GROUP 2 → GROUP 3 → GROUP 4 (with the drawer trigger at its foot) → DRAWER. Read collapse/drawer state from uiStore (`devApiGroupCollapsed`, `isOperatorDrawerOpen`, `toggleDevApiGroup`, `toggleOperatorDrawer`, `setOperatorDrawerOpen` — Plan 01).

    HERO (`<header data-testid="api-health-hero" role="status" aria-label="API health summary">`, `px-4 py-2 border-b border-white/10 flex items-center gap-2 flex-wrap`): render the 4 fields in EXACT order from §Hero Header — (1) endpoints healthy `{N}/{M}` where N = count of `health.endpoints` with `status==='healthy'`, M = `Object.keys(health.endpoints).length`, hero-metric `text-[13px] font-semibold tabular-nums` + `●` status-healthy dot + label "healthy"; (2) LLM last run = FlightRecorder head-run `outcome` badge + relative time + "last run" (read the head run via the existing FlightRecorder data source — if the hero cannot cheaply read FlightRecorder's internal fetch, surface from `llmStatus.lastRun` outcome/timestamp already in scope, and note the source in a comment); (3) Budget `{pct}%` from `opStatus.tokenBudget.providers.nvidia_nim` used/cap + soft/hard proximity dot (status-warning ≥0.8, accent-red/status-degraded ≥0.95, else status-healthy) + "of cap"; (4) Dead URLs `{N}` from `opStatus.prune.deadUrlCount` + dot status-degraded when >0 else status-healthy + "dead URLs". Each field renders its muted fallback INDEPENDENTLY per the §Hero table (e.g. `health == null` ⇒ `— / — healthy (no health data)` in the muted placeholder markup; `tokenBudget == null` ⇒ `—% of cap (no budget data)`).

    Each GROUP is `<section data-testid="group-{slug}" aria-labelledby="group-{slug}-header">` with: a clickable `<button id="group-{slug}-header" aria-expanded={!collapsed} aria-controls="group-{slug}-body" onClick={() => toggleDevApiGroup(slug)}>` carrying the group-header type role (`text-[11px] font-semibold uppercase tracking-wider`, `py-2 px-4`, `hover:bg-white/5`) + a rotating chevron SVG (`▸` collapsed / `▾` expanded, `text-white/40`, 12px); and a `<div id="group-{slug}-body" hidden={devApiGroupCollapsed[slug] ?? false}>` (`mt-2` below header). Default state: NOT collapsed (all expanded — `devApiGroupCollapsed` defaults `{}`). Inter-group break `mt-4` + `border-t border-white/10`. Group slugs + membership EXACTLY per §Section Grouping mapping:
    - GROUP 1 `endpoint-health`: tier-summary banner (#1), audit-result banner (#2), loading/error/empty/stale (#3), per-endpoint quality table + expanded row + sparkline + retry button (#4–7, retry stays inline in the expanded row).
    - GROUP 2 `llm-pipeline`: LLMPipelineSection (#8), FlightRecorderBlock (relocated up from :1765), the adversarial-eval / prompt-injection robustness row (#9-partial, moved here from operator-actions :1588 — it is LLM-eval data).
    - GROUP 3 `budget-cost`: BudgetBlock (#14).
    - GROUP 4 `operator-actions`: 24h count + byBearer (#9-partial read-only counters STAY), dead-URL count + drill-down LIST (#12-partial read-only STAY), actor-quality counters + drill-down (#13 STAY), replay-quota + prune-quota 429 alerts (#10/#15). At its foot, the drawer trigger button.

    DRAWER trigger (foot of Group 4): `<button data-testid="operator-drawer-trigger" aria-expanded={isOperatorDrawerOpen} aria-controls="operator-drawer" aria-label="Open operator controls" onClick={toggleOperatorDrawer}>` with visible label "Operator Controls", classes `text-[10px] px-2 py-1 rounded-md border border-white/10 hover:bg-white/5`. DRAWER container `<div id="operator-drawer" data-testid="operator-drawer" role="region" aria-label="Operator controls">` rendered ONLY when `isOperatorDrawerOpen` (so the destructive buttons are NOT in the document until opened — Regression-Lock assertion 5); reuse `animate-slide-in-right` if a panel. MEMBERSHIP EXACT: move ONLY the Replay probe button (`replay-test-trigger` :1617–1626) and the Prune button (`prune-dead-urls-trigger` :1666–1677) INTO the drawer. Keep their read-only counts/lists/alerts in Group 4. Inside the drawer, Prune keeps its dynamic label `Prune {N} dead events` + a one-line caption "Permanently removes events whose primary source URL is dead."; Replay gains caption "Probes the 50/24h replay quota without writing to the event cache." Add a scoped Escape handler that `stopPropagation` so it does NOT bubble to the modal's capture-phase Escape (DevApiStatus header comment :322–323) + an in-drawer close affordance.

    VISUAL POLISH (D-03 — apply throughout the restructured body): establish the 3-tier hierarchy — hero metric (13px/600) ≫ group header (11px/600 uppercase tracking-wider) ≫ label+value (10px). Two weights only (400/600); NO bold (700). Retire `text-[9px]` for structural text (the LLMPipelineSection wrapper header at :1544 goes `text-[9px] font-bold` → `text-[11px] font-semibold` as a group header). `tabular-nums` MANDATORY on every numeric. All NEW structural spacing uses xs/sm/lg/xl = 4/8/16/24 only (no new 2px/6px); the grandfathered `py-0.5` drill-down micro-rows at :1648/:1717 stay verbatim. Carry forward existing verbatim copy strings (audit banner, quota alerts) — do NOT reword (snapshot/RTL assert them).

    Do NOT touch TabButton (:258), the tablist (:708), or the panel containers (:780) — that is Plan 03's file-region (sequenced after this plan). Do NOT change BudgetBlock/FlightRecorderBlock degrade gates here — that is Task 3.

  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx src/__tests__/components/DevApiStatus.prune.test.tsx src/__tests__/components/DevApiStatus.actorQuality.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DevApiStatusAllApisTab renders `<header data-testid="api-health-hero">` with 4 fields, AND `<section data-testid="group-endpoint-health">`, `group-llm-pipeline`, `group-budget-cost`, `group-operator-actions`
    - Each group header is a `<button aria-expanded aria-controls="group-{slug}-body">`; default render shows all 4 group bodies (not hidden) because `devApiGroupCollapsed` defaults `{}`
    - `operator-drawer` is NOT in the document until `operator-drawer-trigger` is clicked; `replay-test-trigger` + `prune-dead-urls-trigger` are inside `operator-drawer` only; `dead-url-count`/`actor-quality-row`/`operator-actions-24h-count` remain in `group-operator-actions`
    - hero budget field shows `—% of cap (no budget data)` muted placeholder when `tokenBudget == null`; hero endpoints field shows `— / — healthy (no health data)` when `health == null`
    - `adversarial-eval-row` now renders inside `group-llm-pipeline`, not in the operator-actions group
    - `npx tsc --noEmit` exits 0; the 3 named existing test files pass (existing testids preserved)
  </acceptance_criteria>
  <done>DevApiStatusAllApisTab is restructured into hero + 4 collapsible groups (default-expanded) + a default-closed drawer with only Replay+Prune buttons inside; the visual polish 3-tier hierarchy + tabular-nums + 4px spacing applied; existing testids + verbatim copy preserved; tsc + the 3 named test files green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tier-banner + sparkline status dots onto the new --color-status-* tokens (zero-visual-change rename)</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx :1306–1341 (tier-summary banner — `var(--color-site-healthy)` :1314, `var(--color-site-attacked)` :1322, `var(--color-event-airstrike)` :1330)
    - src/components/ui/DevApiStatus.tsx :1190, :1204 (recent-fetch sparkline — `var(--color-site-healthy)` ok-dot / event color)
    - src/styles/app.css @theme (the 3 new `--color-status-*` tokens added by Plan 01 — byte-identical to the map tokens being replaced)
    - 40-UI-SPEC.md §Color "Migration note (planner)" (token NAME flips only; byte-identical hex ⇒ zero runtime visual change; the operator console gets its own status namespace)
    - 40-PATTERNS.md §"Migration (zero-visual-change rename)"
  </read_first>
  <action>
    Flip the operator-console status color references from the MAP entity tokens to the new Plan-01 status tokens — NAME-only change, byte-identical hex ⇒ no runtime visual change. In the tier-summary banner: `var(--color-site-healthy)` (:1314) → `var(--color-status-healthy)`; `var(--color-site-attacked)` (:1322) → `var(--color-status-degraded)`; `var(--color-event-airstrike)` (:1330) → `var(--color-status-warning)` (the "unhealthy" dot — UI-SPEC maps the third banner dot to the warning/amber status token). In the sparkline (:1190/:1204): flip `var(--color-site-healthy)` ok-dot → `var(--color-status-healthy)`. Do NOT change any HEX, any non-status entity color, or any other map-token consumer (entity colors belong to the map, not this console — UI-SPEC styling discipline). The 3 hero status dots + budget proximity dots added in Task 1 ALSO use the status tokens (`bg-status-healthy`/`text-status-degraded`/`text-status-warning` Tailwind utilities or `var(--color-status-*)`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DevApiStatus.tsx tier-summary banner references `var(--color-status-healthy)`, `var(--color-status-degraded)`, `var(--color-status-warning)` (grep `--color-site-healthy` and `--color-event-airstrike` inside DevApiStatus.tsx returns 0 matches for the banner/sparkline lines)
    - No hex literal was introduced; only CSS-var NAME changed
    - `npx tsc --noEmit` exits 0; DevApiStatusAllApisTab test passes (banner still renders its dots)
  </acceptance_criteria>
  <done>The tier banner + sparkline + hero/budget status dots reference the operator-console `--color-status-*` namespace; zero hex change; existing render tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Convert BudgetBlock + FlightRecorderBlock self-hide to the muted-placeholder degrade pattern, and unify actor-quality onto it (D-06)</name>
  <files>src/components/ui/BudgetBlock.tsx, src/components/ui/FlightRecorderBlock.tsx, src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/BudgetBlock.tsx :60–76 (the `if (tokenBudget == null) return null;` self-hide at :62 + the existing `budget-empty` row at :73 `text-white/30`)
    - src/components/ui/FlightRecorderBlock.tsx :240–254 (the `if (data == null) return null;` self-hide at :241 + the existing `flight-recorder-empty` row at :254)
    - src/components/ui/DevApiStatus.tsx :1738–1742 (actor-quality "no data" row — already `text-text-muted italic`, re-tone to the canonical token) + :1565 (the `opStatus &&` gate that currently hides the whole operator-actions content when opStatus null)
    - 40-UI-SPEC.md §Degraded-State Convention (the muted-placeholder markup `text-[10px] text-white/30 italic` + copy `— no data ({reason})` + the per-block degraded mapping table)
    - 40-PATTERNS.md §"BudgetBlock.tsx + FlightRecorderBlock.tsx — degrade-open → muted placeholder" (canonical markup; the GROUP shell always renders around the placeholder)
  </read_first>
  <action>
    Replace the self-hide `return null` paths with the canonical muted placeholder so the GROUP shell (Task 1) always has a body. Canonical markup: `<div className="text-[10px] text-white/30 italic" data-testid="{block}-placeholder">— no data ({reason})</div>`. Preserve degrade-open semantics — block still never throws, route stays 200; ONLY the empty render changes.
    - BudgetBlock.tsx: replace `if (tokenBudget == null) return null;` (:62) with a render of the placeholder `data-testid="budget-block-placeholder"`, reason "no budget data". Re-tone the existing `budget-empty` provider-empty row (:73) to the canonical markup/copy (keep its `budget-empty` testid; change copy to `— no data (no provider budget data yet)`).
    - FlightRecorderBlock.tsx: replace `if (data == null) return null;` (:241) with the placeholder `data-testid="flight-recorder-placeholder"`, reason "no runs recorded / recorder unreachable". Keep the existing `flight-recorder-empty` row for the empty-runs case (re-tone to canonical if it diverges).
    - DevApiStatus.tsx actor-quality (:1738–1742): rename the empty row's testid/markup to the canonical `data-testid="actor-quality-placeholder"` + copy `— no data (no actor data)` (UNIFY onto the muted pattern; the existing `text-text-muted italic` becomes `text-[10px] text-white/30 italic`).
    - DevApiStatus.tsx Group 4 (operator-actions) group-level fallback: when `opStatus == null`, render ONE group-level placeholder `data-testid="group-operator-actions-placeholder"` reason "operator-status unreachable" inside the group body (do NOT leave the group body empty). The drill-down sample LISTS (`dead-url-list`, `actor-quality-list`) MAY still hide when empty (they are detail, not structure) — but their COUNT rows render (muted if null).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/__tests__/components/DevApiStatus.actorQuality.test.tsx src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BudgetBlock no longer contains `return null` for the `tokenBudget == null` branch; instead renders `data-testid="budget-block-placeholder"` with `text-white/30 italic` and copy starting `— no data (`
    - FlightRecorderBlock no longer contains `if (data == null) return null;`; renders `data-testid="flight-recorder-placeholder"`
    - DevApiStatus renders `data-testid="actor-quality-placeholder"` (muted markup) for the actor-quality empty case, and `data-testid="group-operator-actions-placeholder"` when `opStatus == null`
    - Each placeholder uses `text-[10px] text-white/30 italic` and copy of the form `— no data ({reason})`
    - `npx tsc --noEmit` exits 0; the 2 named test files pass
  </acceptance_criteria>
  <done>BudgetBlock + FlightRecorderBlock + actor-quality + the operator-actions group-null case all render the canonical muted placeholder instead of self-hiding; degrade-open (no-throw/200) preserved; the GROUP shell always has a body.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                              | Description                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → `/api/operator-status` (Bearer-gated)                                        | Already-existing boundary. The hero + Group 4 READ this poll; the consolidation adds NO new fetch and no new field consumption beyond `tokenBudget` + `prune.deadUrlCount` (already polled).    |
| operator UI → `POST /api/events/prune-dead-urls` / `POST /api/events/llm-replay/test` | EXISTING Bearer-gated, quota-guarded destructive endpoints. The Prune + Replay BUTTONS are RELOCATED into a default-closed drawer; their server-side auth + 50/24h quota contract is UNCHANGED. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                 |
| ---------- | ---------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-02-01 | Elevation of privilege | Prune / Replay buttons relocated to drawer       | accept      | Auth + quota enforcement is server-side (Bearer + `operator:prune-quota:*` / `operator:replay-quota:*` INCR gates). Relocating the trigger into a drawer changes UI placement only; no client-side auth logic exists to weaken.                                                 |
| T-40-02-02 | Information disclosure | Hero + groups always render (D-06 honest render) | accept      | Surface is operator-only (`shouldRenderDashboard()` = `import.meta.env.DEV \|\| hasDashboardKey()`). Muted placeholders show `— no data (reason)` — reasons are generic ("no budget data", "operator-status unreachable"), no secrets, no tokens, no internal hostnames leaked. |
| T-40-02-03 | Tampering              | `var(--color-status-*)` token migration          | accept      | NAME-only CSS-var swap to byte-identical hex; inert design values, no injection vector.                                                                                                                                                                                         |
| T-40-02-SC | Tampering              | npm/pip/cargo installs                           | n/a         | No package installs in this plan.                                                                                                                                                                                                                                               |

**Defense-in-depth note (per phase threat_model requirement):** moving the destructive Prune/Replay controls into a default-CLOSED drawer is a MILD defense-in-depth improvement — it reduces accidental-click exposure and keeps destructive buttons out of default portfolio screenshots, without changing the (server-enforced) auth/quota contract.

**Verdict:** No new endpoint, no new data mutation, no new attack surface. The consolidation is read-only view restructuring over existing polls + relocation (not re-implementation) of two already-gated destructive controls. No HIGH/MEDIUM threats.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx src/__tests__/components/DevApiStatus.prune.test.tsx src/__tests__/components/DevApiStatus.actorQuality.test.tsx src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx` — all pass (existing testids + verbatim copy preserved; new structure does not break prior assertions). Some prior tests may need light updates where a moved sub-block changed nesting — update assertions to the new group ancestry, do NOT delete coverage.
- grep `--color-site-healthy` / `--color-event-airstrike` inside DevApiStatus.tsx returns 0 (status-token migration complete)
- grep `return null` inside BudgetBlock.tsx + FlightRecorderBlock.tsx returns 0 for the data-null branch (muted-placeholder conversion complete)
</verification>

<success_criteria>

- API Health tab body = hero header + exactly 4 collapsible groups (default-expanded) + 1 drawer (default-closed); ~13 stacked sub-blocks consolidated (UI-POLISH-02)
- All 4 groups + hero render under all-null data; each shows the canonical muted placeholder rather than vanishing (D-06 / SC40-3 render-contract intent)
- Replay + Prune buttons live only in the drawer; their read-only counters stay in Group 4 (D-02a)
- Type hierarchy (13/11/10 px, two weights), 4px spacing, tabular-nums applied (UI-POLISH-03 / D-03)
- Status dots use `--color-status-*`; zero hex change
- BudgetBlock + FlightRecorderBlock + actor-quality unified on the muted placeholder
  </success_criteria>

<output>
Create `.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-02-SUMMARY.md` when done
</output>

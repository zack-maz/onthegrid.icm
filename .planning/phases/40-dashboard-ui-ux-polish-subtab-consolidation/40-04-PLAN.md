---
phase: 40-dashboard-ui-ux-polish-subtab-consolidation
plan: 04
type: execute
wave: 4
depends_on: ['40-02', '40-03']
files_modified:
  - src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx
  - src/__tests__/components/DevApiStatus.actorQuality.test.tsx
  - src/__tests__/components/DevApiStatus.prune.test.tsx
  - src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
  - src/components/ui/__tests__/DevApiStatus.operatorActions.test.tsx
  - src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx
  - src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx
autonomous: true
requirements: [UI-POLISH-05]
must_haves:
  truths:
    - 'RTL tests assert all 4 groups + hero render under all-null data with muted placeholders (honest render)'
    - 'RTL tests assert default all-expanded, drawer default-closed (Replay/Prune absent until opened), and roving keyboard nav'
    - 'One consolidated-layout snapshot locks the fully-populated DevApiStatusAllApisTab shape'
  artifacts:
    - path: 'src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx'
      provides: 'the SC40-3 consolidated-layout snapshot regression lock'
      contains: 'toMatchSnapshot'
  key_links:
    - from: 'RTL tests'
      to: 'DevApiStatusAllApisTab consolidated render contract'
      via: 'group-*/hero/placeholder/drawer testids'
      pattern: 'group-endpoint-health'
---

<objective>
Regression-lock the post-polish dashboard (UI-POLISH-05 / SC40-3). Code the 8 render-contract assertions from 40-UI-SPEC §"Regression-Lock Contract" as RTL component tests across the existing DevApiStatus test files (extend, never delete coverage), plus ONE consolidated-layout snapshot of `DevApiStatusAllApisTab` in the fully-populated state. The 8 assertions ARE this phase's Dimension-8 validation surface (no separate VALIDATION.md — operator-skipped; UI-SPEC §Regression-Lock is the derived validation contract).

Purpose: Make the consolidated layout shape + degraded-render contract regression-proof — group/hero/drawer/placeholder presence and counts surface correctly under fresh / stale / degraded states, and the layout shape surfaces as a snapshot diff if it drifts.
Output: 8 render-contract assertions distributed across the named test files + 1 new consolidated-layout snapshot test; the colorBridge byte-identity assertions (Plan 01) already cover assertion 7.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-UI-SPEC.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-PATTERNS.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-02-SUMMARY.md
@.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates test coverage (no production symbols):

- New test file `src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx` (the SC40-3 layout-shape snapshot lock — UI-SPEC §Regression-Lock assertion 8)
- New `describe`/`it` blocks in the 6 existing DevApiStatus test files covering assertions 1–6 (assertion 7 = colorBridge byte-identity, already coded in Plan 01)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Code render-contract assertions 1–3 + 4–5 (honest render, muted placeholders, hero fallbacks, default-expanded, drawer-closed) across the existing RTL files</name>
  <files>src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx, src/__tests__/components/DevApiStatus.prune.test.tsx, src/components/ui/__tests__/DevApiStatus.operatorActions.test.tsx, src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx, src/__tests__/components/DevApiStatus.actorQuality.test.tsx</files>
  <read_first>
    - src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx (the render harness: `makeEndpoint`/`makeResponse` fixtures ~:30–56, `renderModalWithHealth` driving `useUIStore.setState({ isDevApiStatusOpen: true, activeDevApiStatusTab: 'apiHealth' })` ~:67–70 wrapped in `HealthStatusContext.Provider`, `beforeEach` localStorage stub + store reset ~:80–90)
    - src/__tests__/components/DevApiStatus.prune.test.tsx :125–203 (the `openAndSelectApiHealthTab` helper, the `OperatorStatusPayload` interface + `makeOpStatus` builder, and the URL-routing `mockFetch` via `vi.stubGlobal('fetch', ...)` — REUSE this operator-status mock builder for null-data and populated-data cases; this is the PATTERNS.md flagged open item)
    - src/__tests__/components/DevApiStatus.actorQuality.test.tsx (its `/api/operator-status` mock builder + actorQuality fixtures — confirm shape parity with prune.test.tsx before writing new cases)
    - the just-written 40-02-SUMMARY.md (the exact new testids: `api-health-hero`, `api-health-hero-endpoints/-llm/-budget/-deadurls`, `group-endpoint-health/-llm-pipeline/-budget-cost/-operator-actions`, `operator-drawer`, `operator-drawer-trigger`, `budget-block-placeholder`, `flight-recorder-placeholder`, `actor-quality-placeholder`, `group-operator-actions-placeholder`)
    - 40-UI-SPEC.md §Regression-Lock Contract assertions 1–5 (the exact testids + muted-placeholder markup `text-white/30 italic` + copy `— no data (...)`)
  </read_first>
  <action>
    Add RTL `describe`/`it` blocks (extend the existing files — do NOT delete prior coverage; where a Plan-02 sub-block move changed nesting, UPDATE the existing assertion to the new group ancestry rather than removing it):
    - Assertion 1 (DevApiStatusAllApisTab.test.tsx): render with `health=null`, `opStatus=null` (mock `/api/operator-status` returning null/404 via the prune.test.tsx mockFetch builder), `llmStatus` idle ⇒ assert all 4 `data-testid="group-endpoint-health"|"group-llm-pipeline"|"group-budget-cost"|"group-operator-actions"` ARE in the document (honest render under all-null).
    - Assertion 2 (DevApiStatusAllApisTab.test.tsx + diagnosticBlocks): assert each null group's body shows the muted-placeholder markup — query the placeholder testids (`budget-block-placeholder`, `flight-recorder-placeholder`, `actor-quality-placeholder`, `group-operator-actions-placeholder`) and assert `className` contains `text-white/30` + `italic` and textContent matches `/^— no data \(.+\)$/`.
    - Assertion 3 (DevApiStatusAllApisTab.test.tsx): render with all-null and assert the hero renders all 4 fields (`api-health-hero-endpoints/-llm/-budget/-deadurls`) each showing its independent muted fallback (e.g. budget field text matches `—% of cap (no budget data)`); then render with populated `health` + `opStatus.tokenBudget` + `opStatus.prune` and assert the live values (N/M healthy, pct%, dead-URL count) render with `tabular-nums`.
    - Assertion 4 (DevApiStatusAllApisTab.test.tsx): default render ⇒ all 4 group BODIES visible (query `#group-{slug}-body` and assert NOT `hidden`); click a group header `<button aria-expanded>` ⇒ that body becomes `hidden` and `aria-expanded` flips to `false`.
    - Assertion 5 (prune.test.tsx + operatorActions.test.tsx): default render ⇒ `operator-drawer` is NOT in the document AND `replay-test-trigger` + `prune-dead-urls-trigger` are absent, while the read-only `dead-url-count` / `operator-actions-24h-count` / `actor-quality-row` ARE present in `group-operator-actions`; click `operator-drawer-trigger` ⇒ `operator-drawer` appears with `replay-test-trigger` + `prune-dead-urls-trigger` inside it.
    Reuse the prune.test.tsx `mockFetch` operator-status builder for all opStatus injection (extend `makeOpStatus` with `tokenBudget`/`actorQuality` overrides as needed to drive populated vs null cases).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx src/__tests__/components/DevApiStatus.prune.test.tsx src/components/ui/__tests__/DevApiStatus.operatorActions.test.tsx src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx src/__tests__/components/DevApiStatus.actorQuality.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts all 4 `group-*` testids present when `health=null` AND `opStatus=null` (assertion 1)
    - A test asserts each null group's muted placeholder has `text-white/30 italic` + copy `— no data (...)` (assertion 2)
    - A test asserts the hero renders 4 fields each with independent muted fallback, AND renders live values with `tabular-nums` when populated (assertion 3)
    - A test asserts default all-expanded (group bodies not `hidden`) and that clicking a header toggles `hidden` + `aria-expanded` (assertion 4)
    - A test asserts drawer default-closed: `operator-drawer`/`replay-test-trigger`/`prune-dead-urls-trigger` absent until `operator-drawer-trigger` clicked, while read-only counts present (assertion 5)
    - All 5 named test files exit 0
  </acceptance_criteria>
  <done>Assertions 1–5 coded as RTL cases across the existing files (reusing the prune.test.tsx operator-status mock builder), all passing; prior coverage preserved/updated, not deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Code assertion 6 (roving keyboard nav + active indicator) and assertion 8 (consolidated-layout snapshot)</name>
  <files>src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx, src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</files>
  <read_first>
    - src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx (existing tab-merge render harness — the place to add the keyboard-nav cases; check how it imports `fireEvent`/`keyDown`)
    - the 40-03-SUMMARY.md (exact tab affordance shape: `tabIndex={active?0:-1}`, `focus-visible:ring-2`, the active 2px `border-b-2 border-accent-blue` indicator, the tablist `onKeyDown` bindings, panel `role="tabpanel"`)
    - src/__tests__/components/DevApiStatus.prune.test.tsx :125–203 (the populated `makeOpStatus` + mockFetch builder — REUSE to render the fully-populated state for the snapshot)
    - 40-UI-SPEC.md §Regression-Lock assertions 6 + 8 (keyboard: ArrowRight/Left moves roving focus, Enter activates, active tab has accent-blue indicator; snapshot: ONE fully-populated consolidated-layout snapshot — hero + 4 expanded groups + closed drawer)
    - 40-UI-SPEC.md §Tab-Bar Interaction Affordances (the EXACT keyboard bindings table to assert against)
  </read_first>
  <action>
    Assertion 6 (tabMerge.test.tsx): with ≥2 tabs visible, focus the active tab, fire `ArrowRight` on the tablist ⇒ assert focus moved to the next tab (`document.activeElement` is the next TabButton) WITHOUT the active tab changing (`setDevApiStatusTab` not called / `aria-selected` unchanged on the original); fire `Enter`/`Space` on the focused tab ⇒ assert it activates (`aria-selected=true` moves). Assert the active TabButton carries the 2px indicator class (`border-b-2 border-accent-blue` or the `after:` marker) and `tabIndex=0` while inactive tabs have `tabIndex=-1`. Assert each panel container has `role="tabpanel"`.
    Assertion 8 (NEW file `src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx`): render `DevApiStatusAllApisTab` (or the full modal opened to the apiHealth tab) in the FULLY-POPULATED state — non-null `health` with several endpoints (mix of healthy/degraded), a populated `opStatus` (audit24h, byBearer, prune with deadUrlCount>0 + sample, actorQuality with counts, `tokenBudget.providers.nvidia_nim`), a FlightRecorder with ≥1 run, hero showing live values, all 4 groups EXPANDED, drawer CLOSED — and `expect(container).toMatchSnapshot()`. Use the prune.test.tsx mock builders (extended) for the populated fixtures. This is the single layout-shape regression lock (UI-SPEC assertion 8). Keep the snapshot to the `DevApiStatusAllApisTab` subtree (not the whole app) so unrelated chrome changes don't churn it.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tabMerge test fires `ArrowRight` and asserts `document.activeElement` moved to the next TabButton without changing `aria-selected` (focus-only); `Enter` activates; active tab has the 2px indicator + `tabIndex=0`, inactive `tabIndex=-1`; panels have `role="tabpanel"` (assertion 6)
    - `src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx` exists, renders the fully-populated state (hero + 4 expanded groups + closed drawer), and calls `toMatchSnapshot` (assertion 8)
    - `npx vitest run` for both files exits 0 (snapshot written on first run)
  </acceptance_criteria>
  <done>Assertion 6 (roving keyboard nav + active indicator + tabpanel) coded in tabMerge.test.tsx and assertion 8 (consolidated-layout snapshot) coded in the new snapshot file; both green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green-up + verify all 8 regression-lock assertions are covered</name>
  <files>src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</files>
  <read_first>
    - CLAUDE.md §Testing (`npx vitest run` all; `npx vitest run server/` server only)
    - 40-UI-SPEC.md §Regression-Lock Contract (the 8-assertion checklist — confirm 1–6 + 8 coded here, 7 coded in Plan 01 colorBridge.test.ts)
  </read_first>
  <action>
    Run the full frontend suite and confirm green. Where a Plan-02/03 structural change broke an UNRELATED prior assertion (e.g. a sub-block moved groups and an old test queried it at the old nesting), UPDATE that assertion to the new structure — do NOT delete the coverage and do NOT loosen it to a tautology. Confirm the 8 regression-lock assertions are each covered: 1–5 (Task 1), 6 + 8 (Task 2), 7 (Plan 01 colorBridge byte-identity). Add a short comment block at the top of the new snapshot file mapping it to UI-SPEC assertion 8 and listing where assertions 1–7 live (cross-reference for the next maintainer).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full frontend suite) exits 0
    - A grep/comment cross-reference confirms each of the 8 UI-SPEC regression-lock assertions maps to a coded test (1–5 across the existing files, 6+8 in the new + tabMerge files, 7 in colorBridge.test.ts)
    - No prior DevApiStatus test was deleted (git diff shows only additions + nesting updates to existing test files)
  </acceptance_criteria>
  <done>Full frontend suite green; all 8 UI-SPEC §Regression-Lock assertions covered (no deleted coverage); the consolidated layout is snapshot-locked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------------- |
| (none — test-only) | This plan adds RTL/snapshot test code only. No production code, no endpoint, no data mutation, no boundary touched. |

## STRIDE Threat Register

| Threat ID  | Category  | Component              | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                               |
| ---------- | --------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-04-01 | (n/a)     | test files             | accept      | Test-only plan; the only artifacts are vitest specs + a snapshot. No runtime surface, no attack vector. The tests themselves HARDEN the degraded-render + drawer-closed contract (assertions 1–2, 5), which is a security-adjacent benefit (destructive controls stay behind the default-closed drawer is regression-locked). |
| T-40-04-SC | Tampering | npm/pip/cargo installs | n/a         | No package installs (vitest + @testing-library/react already present).                                                                                                                                                                                                                                                        |

**Verdict:** No new attack surface (test-only). The regression-lock assertion 5 (drawer default-closed, destructive buttons absent until opened) is itself a security-adjacent regression guard. No HIGH/MEDIUM threats.
</threat_model>

<verification>
- `npx vitest run` (full frontend suite) exits 0
- All 8 UI-SPEC §Regression-Lock assertions covered: 1–5 (Task 1), 6 + 8 (Task 2), 7 (Plan 01)
- The new consolidated-layout snapshot file exists and matches on re-run
- `git diff --stat` on the 6 existing test files shows additions/updates only, no deleted `it(`/`test(` blocks
</verification>

<success_criteria>

- 8 render-contract assertions from UI-SPEC §Regression-Lock are each coded and passing (counts surface correctly under fresh / stale / degraded; honest render; default-expanded; drawer-closed; roving keyboard nav; byte-identity tokens; layout snapshot)
- One consolidated-layout snapshot locks the fully-populated `DevApiStatusAllApisTab` shape (SC40-3)
- No prior coverage deleted; existing tests updated to new nesting where structurally required
- Full frontend suite green (UI-POLISH-05)
  </success_criteria>

<output>
Create `.planning/phases/40-dashboard-ui-ux-polish-subtab-consolidation/40-04-SUMMARY.md` when done
</output>

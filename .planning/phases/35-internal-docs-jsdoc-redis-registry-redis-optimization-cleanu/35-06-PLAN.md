---
phase: 35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu
plan: 06
type: execute
wave: 4
depends_on: ['35-01', '35-02', '35-03', '35-04', '35-05']
files_modified:
  - .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-close-YYYY-MM-DD.png
  - .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
autonomous: false
requirements: [REDIS-OPT-04, SIMPLIFY-07]
tags: [phase-close, adr, measurement, roadmap, summary]
must_haves:
  truths:
    - 'Bundle-size CLOSE measurement captured: `wc -c api/vercel-entry.js` re-run after all plan 35-01..35-05 commits land (D-19).'
    - 'Bundle-size delta computed (close - baseline) + percentage; written to SUMMARY.md (SIMPLIFY-07).'
    - 'Upstash dashboard CLOSE screenshot captured by operator; committed at `.planning/phases/35-*/redis-budget-close-YYYY-MM-DD.png` (D-20).'
    - 'Upstash command-budget delta + primary-driver attribution recorded in SUMMARY.md (D-21, REDIS-OPT-04).'
    - 'ADR-0010 Phase 35 sub-block appended at `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` (D-22).'
    - 'Drift gate from plan 35-01 re-run; `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0 (registry integrity confirmed at phase close).'
    - 'ROADMAP.md Phase 35 entry updated to checked-complete + plan list filled in (6 plans 35-01..35-06).'
    - 'REQUIREMENTS.md traceability table flips DOCS-INT-02, DOCS-INT-03, REDIS-OPT-01..04, SIMPLIFY-02, SIMPLIFY-05, SIMPLIFY-07 from Pending → Complete.'
    - 'STATE.md last_activity updated; phase 35 row marked complete.'
    - 'Branch `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu` ready for merge to main.'
  artifacts:
    - path: '.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md'
      provides: 'Phase 35 close-out summary: deltas + decisions + commit log + verification'
      contains: 'Phase 35 Close-Out Summary'
    - path: '.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-close-YYYY-MM-DD.png'
      provides: 'Upstash command-budget close screenshot (D-20)'
    - path: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      provides: 'ADR-0010 Phase 35 sub-block appended (D-22)'
      contains: 'Phase 35 Sub-block'
  key_links:
    - from: '.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md'
      to: 'redis-budget-baseline + redis-budget-close PNGs + ADR-0010 sub-block'
      via: 'D-19/D-20/D-22 cross-references'
      pattern: 'redis-budget-(baseline|close)'
---

<objective>
Close Phase 35. Re-measure bundle size + Upstash command budget for the SIMPLIFY-07 + REDIS-OPT-04 deltas (capture, not just compute). Append ADR-0010 Phase 35 sub-block. Author SUMMARY.md with primary-driver attribution. Flip ROADMAP / REQUIREMENTS / STATE markers. Re-run drift gate as the final confirmation that registry integrity holds at phase close.

Purpose: This plan IS the v1.5 documentation-and-cleanup track closing artifact. Captures empirical deltas (Phase 35 was specifically designed to MEASURE — D-19 / D-20 / D-21). Mirrors the close-out cadence of Phase 30 / 30.1 / 34.

Output: 4-6 commits (close measurements / ADR sub-block / SUMMARY / roadmap flips / requirements flips / state flip). Branch ready for merge to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-01-PLAN.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-02-PLAN.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-03-PLAN.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-04-PLAN.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-PLAN.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Close-out artifacts and existing ADR sub-block precedents. -->

BASELINE NUMBERS (from plan 35-01 task 1):

- `api/vercel-entry.js` baseline = 1,779,504 bytes (≈ 1.70 MB) verified 2026-05-26.
- Upstash dashboard baseline PNG = `redis-budget-baseline-YYYY-MM-DD.png` (committed in plan 35-01).

CLOSE MEASUREMENT COMMANDS:

- Bundle: `npm run build` (rebuild artifact) → `wc -c api/vercel-entry.js`.
- Upstash: operator captures PNG at the same dashboard view as the baseline.

DELTA COMPUTATION:

- Bundle: `close_bytes - baseline_bytes` + `(delta / baseline_bytes * 100).toFixed(2)%`.
- Upstash: visual delta from dashboard (ops/sec or daily-cmds line); operator records absolute + percentage.

PRIMARY DRIVER ATTRIBUTION (D-21):

- Partial-key retirement (plan 35-02 / SIMPLIFY-02): writer deleted from cron-only path. Estimated savings: ~5 cacheSetSafe calls per cron run × 1 run/day = ~1825 Redis SET commands saved/year against the daily measurement window. PLUS the related script + test deletions.
- TTL right-sizing (plan 35-05, IF changes proposed): per-change attribution.
- JSDoc audit (plan 35-04): zero runtime impact (comment-only).
- CLAUDE.md edits (plan 35-01 task 2): zero runtime impact.
- redis-keys.md authoring (plan 35-01 task 3): zero runtime impact.
- Drift-gate vitest (plan 35-01 task 4): zero runtime impact (test-only).

ADR-0010 PHASE 35 SUB-BLOCK FORMAT (verbatim from 35-PATTERNS.md lines 480-507, lines 497-506 specifically):

```markdown
## Phase 35 Sub-block (appended YYYY-MM-DD)

Phase 35 closed the v1.5 documentation-and-cleanup track deferred while LLM-RELI ran. Mechanical drift gate (D-01 vitest) is the load-bearing primitive — the hand-maintained CLAUDE.md registry rotted in expected ways during Phases 27-34 (4 missing keys, 1 retire-but-still-listed); the gate prevents recurrence. Partial-key retirement (D-12 / SIMPLIFY-02) was the only code deletion. Everything else is documentation authoring.

- **D-01 (drift gate):** `src/__tests__/lib/redis-registry.test.ts` parses CLAUDE.md §Serverless Cache + `docs/architecture/redis-keys.md` + greps server/src code; asserts 3-surface parity. Drift fails the next `vitest run`. Mirrors `colorBridge.test.ts` / `actorCatalog.test.ts` / `urlLiveness.schema.test.ts` precedents.
- **D-12 (SIMPLIFY-02):** `events:llm:v3:partial` observability key + writer + 3 script consumers + 4 test files + CLAUDE.md bullet retired. Hobby-era 300s-budget mitigation; Pro 800s makes terminal writes reliable; partial-key carried no live signal. Production cleanup = natural TTL expiry (D-13).
- **D-19 (bundle-size delta):** `api/vercel-entry.js` baseline = 1,779,504 bytes (2026-05-26); close = N bytes (YYYY-MM-DD). Delta = X bytes (P%). v1.4 close baseline 1.72 MB → v1.5 close N MB.
- **D-20 (Upstash budget delta):** Manual dashboard screenshots: baseline + close. Primary drivers: D-12 partial-key writer deletion ({estimated saved/year}); TTL right-sizing (plan 35-05 — {N changes shipped|no changes proposed}); other contributors. PNGs at `.planning/phases/35-*/redis-budget-{baseline,close}-YYYY-MM-DD.png`.
- **D-22 (this sub-block):** Captures Phase 35 close measurements + decisions. Mirrors Phase 30 / 30.1 / 34 sub-block convention.
- **Phase 34 carryover note:** No Cerebras / Groq token-budget keys exist in registry (Phase 34 closed `cerebras-groq-deferred` — operator chose to skip provisioning). Inventory records "absent (Phase 34 deferred)" for those slots. See ADR-0010 Phase 34 sub-block for rationale.
```

EXISTING SUB-BLOCK PRECEDENTS in `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`:

- Phase 30 sub-block at lines 52-80 (35-PATTERNS.md line 480).
- Phase 30.1 sub-block at lines 82-106.
- Phase 34 sub-block at lines 108+ (insertion point for Phase 35 sub-block).

ROADMAP.md UPDATE TARGETS (lines 244-258 — Phase 35 entry):

- Change `- [ ] **Phase 35: ...**` → `- [x] **Phase 35: ...**`.
- Replace `**Plans**: TBD` with the 6 plan list (35-01 through 35-06 with brief one-line objective each).
- Add `**Completed:** YYYY-MM-DD` line if convention used by prior phases.

ROADMAP.md PROGRESS TABLE (lines 290-300):

- Phase 35 row: change `0/0 | Not started` → `6/6 | Complete | YYYY-MM-DD`.

REQUIREMENTS.md TRACEABILITY (lines 131-179): flip the following from Pending → Complete:

- DOCS-INT-02 (Phase 35)
- DOCS-INT-03 (Phase 35)
- REDIS-OPT-01 (Phase 35)
- REDIS-OPT-02 (Phase 35)
- REDIS-OPT-03 (Phase 35)
- REDIS-OPT-04 (Phase 35)
- SIMPLIFY-02 (Phase 35)
- SIMPLIFY-05 (Phase 35)
- SIMPLIFY-07 (Phase 35)

REQUIREMENTS.md ACCEPTANCE-TEXT updates (lines 44-64) — add `[x]` checkbox flips:

- `- [x] **DOCS-INT-02**: ...` (was `- [ ]`).
- Same for all 9 IDs above.

STATE.md UPDATE:

- `last_updated:` → today's UTC ISO timestamp.
- `last_activity:` → `YYYY-MM-DD -- Phase 35 marked complete`.
- `progress.completed_phases` → bumped by 1.
- `progress.completed_plans` → bumped by 6.
- Phase 35 row in v1.5 table: `Not started` → `Complete (6/6 plans)`.
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture close measurements (D-19 bundle + D-20 Upstash PNG)</name>
  <files>.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-close-YYYY-MM-DD.png, .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md</files>
  <action>Operator-driven capture of close measurements — see <how-to-verify> below for stepwise instructions. After capture, commit the close PNG + updated measurements .md with conventional-commit prefix `chore(35):` per D-26.</action>
  <verify>`wc -c api/vercel-entry.js` returns close byte count; `ls .planning/phases/35-*/redis-budget-close-*.png 2>/dev/null | head -1` returns a file; `grep -q "Close Measurements" .planning/phases/35-*/35-baseline-measurements.md` exits 0.</verify>
  <done>Close measurements captured + committed; ready for Task 2 (ADR sub-block).</done>
  <what-built>
    Plans 35-01..35-05 have all landed. Branch contains the partial-key retirement (35-02), JSDoc audit (35-04), and any TTL changes from 35-05. Now operator + Claude capture the CLOSE measurements that the SIMPLIFY-07 + REDIS-OPT-04 deltas require.
  </what-built>
  <how-to-verify>
    STEP A (Claude — bundle size):
    1. Run `npm run build` to refresh `api/vercel-entry.js`. Build MUST succeed.
    2. Run `wc -c api/vercel-entry.js` and record the byte count.
    3. Compute delta = close_bytes - baseline_bytes (where baseline_bytes = 1,779,504 from plan 35-01 task 1, or read from `35-baseline-measurements.md`).
    4. Compute percentage = (delta / baseline_bytes * 100).toFixed(2)%.

    STEP B (Operator — Upstash PNG):
    1. Open Upstash Console → otg-iran-monitor database → Metrics / Commands tab.
    2. Capture PNG of the "Commands" metric chart at the SAME dashboard view as the baseline screenshot.
    3. Commit at `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-close-{YYYY-MM-DD}.png` (substitute today's UTC date).
    4. Visually estimate delta from baseline (absolute commands/sec change + percentage).

    STEP C (Claude — record):
    1. Append the close-bundle byte count and close-PNG filename to `35-baseline-measurements.md` under a new "## Close Measurements" header.
    2. Note: do NOT compute Upstash delta yet — that goes in SUMMARY.md (Task 3) where the primary-driver attribution context lives.

    EXPECTED OUTCOME (most likely):
    - Bundle: slight reduction (partial-key code deletion = ~50-200 LOC removed; expect a few KB net reduction; <1.5 MB stretch goal probably NOT hit because the JSDoc/CLAUDE.md/redis-keys.md additions partially offset).
    - Upstash: visible reduction in daily SET commands corresponding to ~5 partial-key writes/day eliminated.

    Commit: `chore(35): capture phase close measurements (D-19, D-20)` body cites close-bundle bytes + close-PNG filename + delta from baseline.

  </how-to-verify>
  <resume-signal>
    Reply "captured" with: (a) close-bundle byte count, (b) close-PNG filename, (c) baseline-vs-close summary line.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append ADR-0010 Phase 35 sub-block (D-22)</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (lines 50-130 — Phase 30 / 30.1 / 34 sub-block precedents; insertion point at file END)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 477-507 — Phase 35 sub-block content scaffold)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-22)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md (post-task-1; bundle + Upstash deltas)
  </read_first>
  <action>
    Use the Edit tool. Find the END of the most recent prior sub-block (Phase 34, ~lines 108+ in the ADR). Append the Phase 35 sub-block AFTER it.

    Sub-block content (verbatim shape from <interfaces> block above, with placeholders filled from Task 1's measurements):

    ```markdown

    ## Phase 35 Sub-block (appended YYYY-MM-DD)

    Phase 35 closed the v1.5 documentation-and-cleanup track deferred while LLM-RELI ran. Mechanical drift gate (D-01 vitest) is the load-bearing primitive — the hand-maintained CLAUDE.md registry rotted in expected ways during Phases 27-34 (4 missing keys, 1 retire-but-still-listed, 2 entries needing refinement); the gate prevents recurrence. Partial-key retirement (D-12 / SIMPLIFY-02) was the only code deletion. Everything else is documentation authoring.

    - **D-01 (drift gate):** `src/__tests__/lib/redis-registry.test.ts` parses CLAUDE.md §Serverless Cache + `docs/architecture/redis-keys.md` + greps `server/**/*.ts` + `src/**/*.ts`; asserts 3-surface parity. Drift fails the next `vitest run`. Mirrors `colorBridge.test.ts` / `actorCatalog.test.ts` / `urlLiveness.schema.test.ts` precedents.
    - **D-12 (SIMPLIFY-02):** `events:llm:v3:partial` observability key + writer + 3 script consumers + 4 test files + CLAUDE.md bullet retired. Hobby-era 300s-budget mitigation; Pro 800s makes terminal writes reliable; partial-key carried no live signal. Production cleanup = natural TTL expiry (D-13).
    - **D-19 (bundle-size delta):** `api/vercel-entry.js` baseline = 1,779,504 bytes (2026-05-26); close = {N} bytes ({close date}). Delta = {X} bytes ({P}%). v1.4 close baseline 1.72 MB → v1.5 close {N} MB.
    - **D-20 (Upstash budget delta):** Manual dashboard PNG screenshots. Primary drivers: D-12 partial-key writer deletion (~5 SET commands/cron-run × 1 run/day eliminated); TTL right-sizing (plan 35-05 — {N changes shipped|no changes proposed}); JSDoc + redis-keys.md authoring (zero runtime impact). PNGs at `.planning/phases/35-*/redis-budget-{baseline,close}-YYYY-MM-DD.png`.
    - **D-22 (this sub-block):** Captures Phase 35 close measurements + decisions. Mirrors Phase 30 / 30.1 / 34 sub-block convention.
    - **Phase 34 carryover note:** No Cerebras / Groq token-budget keys exist in registry (Phase 34 closed `cerebras-groq-deferred` — operator chose to skip provisioning). Inventory records "absent (Phase 34 deferred)" for those slots. See ADR-0010 Phase 34 sub-block for rationale.
    - **Phase-35-or-later follow-up candidates (deferred):** TTL-string consistency vitest (CLAUDE.md "7d" vs code `7*24*3600`); generated `redis-keys.md` script if hand-maintenance proves insufficient; continuous Upstash command-budget telemetry; continuous bundle-size CI gate; per-host outbound throttle inventory (Nominatim / Open-Meteo / Yahoo / Overpass).
    ```

    SUBSTITUTE placeholders:
    - `{YYYY-MM-DD}` → today's UTC date.
    - `{N}`, `{X}`, `{P}` → bundle bytes / delta / percentage from Task 1.
    - `{close date}` → today's UTC date.
    - `{N changes shipped|no changes proposed}` → reflect plan 35-05's actual outcome.

    COMMIT:
    ```
    docs(35): append ADR-0010 Phase 35 sub-block (D-22)

    Records: drift-gate rationale (D-01), partial-key retirement (D-12), bundle-size
    delta (D-19), Upstash command-budget delta (D-20), Phase 34 carryover note.
    Mirrors Phase 30 / 30.1 / 34 sub-block convention.
    ```

  </action>
  <verify>
    <automated>grep -q "## Phase 35 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "D-01 (drift gate)" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "D-12 (SIMPLIFY-02)" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "D-19 (bundle-size delta)" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "D-20 (Upstash budget delta)" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "Phase 34 carryover note" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` contains `## Phase 35 Sub-block` heading.
    - Sub-block contains D-01, D-12, D-19, D-20, D-22 bullets.
    - Sub-block contains Phase 34 carryover note.
    - Sub-block appears AFTER the Phase 34 sub-block (verify by line order).
    - Bundle bytes + percentages are NON-PLACEHOLDER values (no remaining `{N}` / `{X}` in the file).
    - `git log -1 --format=%s -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` matches `docs(35):` with `D-22` in body.
  </acceptance_criteria>
  <done>
    ADR-0010 has its Phase 35 sub-block with measured deltas. v1.5 LLM-pipeline narrowing-and-deletion ADR now spans Phase 29 → 30 → 30.1 → 34 → 35 sub-blocks.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 35-SUMMARY.md with deltas + commit log (D-21, D-22)</name>
  <files>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md (baseline + close numbers)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-TTL-REVIEW.md (TTL outcome — 0 vs N changes)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-01-SUMMARY.md (per-plan summary inputs if present)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-02-SUMMARY.md
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-03-SUMMARY.md
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-04-SUMMARY.md
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-05-SUMMARY.md
    $HOME/.claude/get-shit-done/templates/summary.md (template shape if convention exists)
  </read_first>
  <action>
    Use the Write tool to create `35-SUMMARY.md`. Sections:

    ```markdown
    # Phase 35 Close-Out Summary

    **Phase:** 35 — Internal Docs (JSDoc) + Redis Registry + Redis Optimization + Cleanup Sweep
    **Branch:** `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu`
    **Started:** {plan 35-01 commit date}
    **Closed:** {today YYYY-MM-DD}
    **Plans:** 6/6 complete (35-01 through 35-06)
    **Requirements:** DOCS-INT-02, DOCS-INT-03, REDIS-OPT-01..04, SIMPLIFY-02, SIMPLIFY-05, SIMPLIFY-07 — all closed.

    ## Measurements

    ### Bundle size (SIMPLIFY-07 / D-19)

    | Metric | Baseline (2026-05-26) | Close (YYYY-MM-DD) | Delta | % |
    |---|---|---|---|---|
    | `api/vercel-entry.js` | 1,779,504 bytes (1.70 MB) | N bytes (X MB) | ±Y bytes | ±Z% |

    Stretch goal (<1.5 MB): {hit | missed}. v1.4 close → v1.5 close trajectory: 1.72 MB → {final MB}.

    ### Upstash command budget (REDIS-OPT-04 / D-20)

    Baseline screenshot: `redis-budget-baseline-YYYY-MM-DD.png` (plan 35-01).
    Close screenshot: `redis-budget-close-YYYY-MM-DD.png` (plan 35-06 task 1).

    Visual delta (per operator observation): {N% reduction in daily SET commands | other}.

    ### Primary-driver attribution (D-21)

    - **D-12 partial-key writer retirement (plan 35-02):** ~5 cacheSetSafe(EVENTS_LLM_V3_PARTIAL_KEY) calls eliminated per cron-run × 1 run/day = ~1825 SET commands saved per year against the daily measurement window.
    - **TTL right-sizing (plan 35-05):** {N TTL changes — see 35-05-TTL-REVIEW.md | no changes proposed; existing TTLs verified right-sized}.
    - **CLAUDE.md edits, redis-keys.md authoring, JSDoc audits, callers block:** Zero runtime impact (documentation only).
    - **Drivers identified but unattributed:** {if applicable}.

    ## Plan-by-plan outcomes

    ### Plan 35-01 — Registry + drift gate

    {paste 35-01-SUMMARY.md highlights — branch cut, baseline measurements, CLAUDE.md edits count, redis-keys.md key count, drift-gate test count, commit hashes}

    ### Plan 35-02 — Partial-key retirement

    {paste 35-02-SUMMARY.md highlights — LLMCachePayload disposition, scripts handled, test assertions dropped/preserved, single atomic commit hash}

    ### Plan 35-03 — freeClaudeRouter callers block

    {paste 35-03-SUMMARY.md highlights — 3 live callers verified, cascade-shape line, single commit hash}

    ### Plan 35-04 — JSDoc audit

    {paste 35-04-SUMMARY.md highlights — 7 modules, ~44 exports, rewrite percentage, 7 commit hashes}

    ### Plan 35-05 — TTL right-sizing

    {paste 35-05-SUMMARY.md highlights — D-18 disposition, 0 or M changes proposed, commit hashes}

    ### Plan 35-06 — Phase close

    This plan: measurements + ADR sub-block + SUMMARY + roadmap flips.

    ## Decisions honored (D-01..D-27)

    | Decision | Outcome |
    |---|---|
    | D-01 | Drift-gate vitest shipped at `src/__tests__/lib/redis-registry.test.ts`; 3-surface parity asserted |
    | D-02 | `EXEMPT_KEYS` array empty at phase close (structurally available) |
    | D-03 | CLAUDE.md ↔ redis-keys.md parity enforced |
    | D-04 | Test runs under existing `npm test` (no new CI step) |
    | D-05 | `docs/architecture/redis-keys.md` authored |
    | D-06 | 8-column table shape (Key, Writers, Readers, TTL, Value, Purpose, Cardinality, Classification) |
    | D-07 | Cardinality column captured (one-shot at close) |
    | D-08 | Each key classified (load-bearing / observability / retire) |
    | D-09 | Public-API JSDoc one-liners audited; top-of-file blocks preserved |
    | D-10 | True-today rule applied |
    | D-11 | Partial-key JSDoc refs handled in plan 35-02 commit (not 35-04) |
    | D-12 | `events:llm:v3:partial` retired everywhere |
    | D-13 | Production cleanup = natural TTL expiry |
    | D-14 | 4 missing keys added to CLAUDE.md |
    | D-15 | `freeClaudeRouter.ts` kept with callers block |
    | D-16 | Callers block PREPENDED above vendored-from block |
    | D-17 | TTL review = read-only-at-default; {N changes proposed} |
    | D-18 | Replay-history disposition = {satisfied-existing-cap \| applied-new-cap \| alternate} |
    | D-19 | Bundle-size baseline + close measured |
    | D-20 | Upstash PNG baseline + close captured |
    | D-21 | Primary drivers attributed |
    | D-22 | ADR-0010 Phase 35 sub-block appended |
    | D-23 | CLAUDE.md edits bounded to §Serverless Cache |
    | D-24 | `redis-keys.md` is NEW, written from scratch |
    | D-25 | 6-plan structure (matches CONTEXT estimate) |
    | D-26 | Atomic-per-decision commits maintained |
    | D-27 | Branch cut from main |

    ## Verification

    - `npx vitest run` full suite: green
    - `npx tsc --noEmit`: green
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts`: green (drift gate exits 0 — registry integrity confirmed)
    - `grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'` returns 0
    - `grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts'` returns only tombstone/historical comments

    ## Commit log

    {paste full `git log feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu --not main --oneline`}

    ## Open follow-ups (deferred to future phases)

    Per ADR-0010 Phase 35 sub-block:
    - TTL-string consistency vitest
    - Generated `redis-keys.md` script (if hand-maintenance proves insufficient)
    - Continuous Upstash command-budget telemetry
    - Continuous bundle-size CI gate
    - Per-host outbound throttle inventory (Nominatim / Open-Meteo / Yahoo / Overpass)
    ```

    Substitute placeholders from prior-plan SUMMARYs + Task 1 measurements + Task 2 ADR sub-block content.

    COMMIT:
    ```
    docs(35): author 35-SUMMARY.md close-out (D-21, D-22)

    Captures: bundle-size delta + Upstash command-budget delta + per-plan
    outcomes + primary-driver attribution + verification snapshot + commit log.
    Mirrors prior phase SUMMARY conventions.
    ```

  </action>
  <verify>
    <automated>test -f .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md && grep -q "# Phase 35 Close-Out Summary" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md && grep -q "## Measurements" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md && grep -q "Primary-driver attribution" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md && grep -q "## Decisions honored" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md && grep -q "## Verification" .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `35-SUMMARY.md` exists.
    - File contains §Measurements, §Plan-by-plan outcomes, §Decisions honored, §Verification, §Commit log, §Open follow-ups.
    - Bundle delta + Upstash delta numbers are NON-PLACEHOLDER.
    - All D-01..D-27 decision entries marked (none missing).
    - `git log -1 --format=%s` matches `docs(35):` with `D-21` or `D-22` in body.
  </acceptance_criteria>
  <done>
    SUMMARY.md captures the full phase deliverables + measurements + decisions trail. Ready for ROADMAP/REQUIREMENTS/STATE flips in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: Re-run drift gate + final verification</name>
  <files>(no files modified; verification gate)</files>
  <read_first>
    src/__tests__/lib/redis-registry.test.ts (the drift-gate from plan 35-01)
    docs/architecture/redis-keys.md (post-Task-2 state)
    CLAUDE.md §Serverless Cache (post-plan-35-02 state)
  </read_first>
  <action>
    Run final verification commands:

    1. Drift gate confirmation:
    ```
    npx vitest run src/__tests__/lib/redis-registry.test.ts
    ```
    MUST exit 0. If non-zero, the registry has drifted somewhere — investigate before continuing.

    2. Full vitest suite:
    ```
    npx vitest run
    ```
    MUST exit 0.

    3. Typecheck:
    ```
    npx tsc --noEmit
    ```
    MUST exit 0.

    4. Partial-key absence:
    ```
    grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'
    ```
    MUST return 0 lines.

    5. Partial-key string ref check:
    ```
    grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts' | grep -v "tombstone\|retired\|^\s*//\|^\s*\*"
    ```
    MUST return 0 lines (only comment/tombstone refs allowed).

    6. CLAUDE.md missing keys present:
    ```
    grep -c "events:llm:v3:lineage:\|events:llm:v3:lineage-keys\|events:llm:v3:group-lineage:\|events:llm-pipeline-audit" CLAUDE.md
    ```
    MUST return ≥4 (each new key documented).

    7. CLAUDE.md retired key absent:
    ```
    grep -c "events:llm:v3:partial" CLAUDE.md
    ```
    MUST return 0 (per plan 35-02 D-23).

    8. freeClaudeRouter.ts callers block:
    ```
    grep -c "Free Claude Router — multi-provider cascade" server/lib/freeClaudeRouter.ts
    ```
    MUST return 1.

    9. ADR sub-block:
    ```
    grep -c "## Phase 35 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
    ```
    MUST return 1.

    NO COMMIT from this task — pure verification gate. If any check fails, surface to operator before proceeding to Task 5.

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/redis-registry.test.ts 2>&1 | tail -5 && npx vitest run 2>&1 | tail -3 && npx tsc --noEmit && test $(grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts' 2>/dev/null | wc -l | tr -d ' ') = "0" && test $(grep -c "events:llm:v3:partial" CLAUDE.md) = "0" && grep -q "Free Claude Router — multi-provider cascade" server/lib/freeClaudeRouter.ts && grep -q "## Phase 35 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</automated>
  </verify>
  <acceptance_criteria>
    - Drift gate exits 0.
    - Full vitest suite exits 0.
    - `npx tsc --noEmit` exits 0.
    - 0 production refs to `EVENTS_LLM_V3_PARTIAL_KEY`.
    - 0 documentation refs to `events:llm:v3:partial` in CLAUDE.md.
    - freeClaudeRouter callers block present.
    - ADR sub-block present.
  </acceptance_criteria>
  <done>
    Phase 35 deliverables verified end-to-end. Branch is ready for ROADMAP / REQUIREMENTS / STATE flips.
  </done>
</task>

<task type="auto">
  <name>Task 5: Flip ROADMAP / REQUIREMENTS / STATE markers (atomic)</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/ROADMAP.md (lines 244-300 — Phase 35 entry + Progress table)
    .planning/REQUIREMENTS.md (lines 44-64 — DOCS-INT / REDIS-OPT / SIMPLIFY checkboxes; lines 131-179 — traceability table)
    .planning/STATE.md (lines 1-50 — frontmatter + Current Position + v1.5 Phases table)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md (close date + commit count)
  </read_first>
  <action>
    Use the Edit tool. Three files, three edit groups.

    EDIT GROUP A — `.planning/ROADMAP.md`:

    A1. Find `- [ ] **Phase 35: Internal Docs (JSDoc) + Redis Registry + Redis Optimization + Cleanup Sweep**`. Change `- [ ]` to `- [x]`. Append `(completed YYYY-MM-DD)` at end of the bullet (mirror Phase 29 / 30 / 32 / 33 / 34 conventions in the same file).

    A2. Find `**Plans**: TBD` under the Phase 35 §Phase Details block (line ~258 or thereabouts). Replace with:
    ```
    **Plans:** 6/6 complete
    - [x] 35-01-PLAN.md — Registry inventory + drift-gate vitest + baseline measurements (D-01..D-04, D-05..D-08, D-14, D-19, D-20)
    - [x] 35-02-PLAN.md — Retire events:llm:v3:partial (SIMPLIFY-02 / D-12, D-13)
    - [x] 35-03-PLAN.md — freeClaudeRouter.ts callers block (SIMPLIFY-05 / D-15, D-16)
    - [x] 35-04-PLAN.md — JSDoc audit of 7 LLM-pipeline modules (DOCS-INT-02 / D-09, D-10)
    - [x] 35-05-PLAN.md — TTL right-sizing (REDIS-OPT-03 / D-17, D-18)
    - [x] 35-06-PLAN.md — Phase close: ADR sub-block + SUMMARY + ROADMAP / REQUIREMENTS / STATE flips (D-19..D-22, D-26)
    ```

    A3. Find Progress table row for Phase 35 (lines 290-300). Change `0/0 | Not started | -` to `6/6 | Complete | YYYY-MM-DD`.

    EDIT GROUP B — `.planning/REQUIREMENTS.md`:

    B1. Lines 44-64 — flip `[ ]` to `[x]` for each of the 9 Phase 35 requirement bullets:
    - `- [x] **DOCS-INT-02**: ...`
    - `- [x] **DOCS-INT-03**: ...`
    - `- [x] **REDIS-OPT-01**: ...`
    - `- [x] **REDIS-OPT-02**: ...`
    - `- [x] **REDIS-OPT-03**: ...`
    - `- [x] **REDIS-OPT-04**: ...`
    - `- [x] **SIMPLIFY-02**: ...`
    - `- [x] **SIMPLIFY-05**: ...`
    - `- [x] **SIMPLIFY-07**: ...`

    B2. Lines 131-179 traceability table — for each of the 9 requirement IDs, change `Pending` → `Complete`.

    EDIT GROUP C — `.planning/STATE.md`:

    C1. Frontmatter `last_updated:` → today's UTC ISO timestamp.
    C2. Frontmatter `last_activity:` → `YYYY-MM-DD -- Phase 35 marked complete`.
    C3. Frontmatter `progress.completed_phases` → bumped by 1 (was 6 per STATE.md line 10; new = 7).
    C4. Frontmatter `progress.completed_plans` → bumped by 6 (was 43 per STATE.md line 12; new = 49).
    C5. Frontmatter `progress.percent` → recompute (`completed_phases / total_phases * 100`). Total phases is 15 per line 9; if completed is now 7, percent is 7/15 ≈ 47.
    C6. §Current Position: update `Phase:` line to `Phase: 35 — COMPLETE`. Update `Last activity:` line to match the frontmatter value.
    C7. §v1.5 Phases (planned) table: change Phase 35 row's `Status` column from `Not started` to `Complete (6/6 plans)`.

    SINGLE ATOMIC COMMIT for all 3 file edits (per D-26 — phase close is a single decision):
    ```
    docs(35): phase close — ROADMAP / REQUIREMENTS / STATE flips (D-22..D-27)

    Phase 35 marked complete. Plans 35-01..35-06 listed; requirements
    DOCS-INT-02, DOCS-INT-03, REDIS-OPT-01..04, SIMPLIFY-02, SIMPLIFY-05,
    SIMPLIFY-07 flipped to Complete. STATE progress bumped (6 → 7 phases;
    43 → 49 plans).
    ```

  </action>
  <verify>
    <automated>grep -q "\[x\] \*\*Phase 35:" .planning/ROADMAP.md && grep -q "\[x\] \*\*DOCS-INT-02\*\*" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*SIMPLIFY-07\*\*" .planning/REQUIREMENTS.md && grep -qE "DOCS-INT-02 *\| *35 *\| *Complete" .planning/REQUIREMENTS.md && grep -q "Phase: 35 — COMPLETE\|Phase 35 marked complete" .planning/STATE.md && grep -q "Complete (6/6 plans)\|6/6 | Complete" .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 35 entry has `[x]` checkbox + completed date.
    - ROADMAP.md Phase 35 §Phase Details has the 6-plan list (all `[x]`).
    - ROADMAP.md Progress table Phase 35 row says `6/6 | Complete | YYYY-MM-DD`.
    - REQUIREMENTS.md has `[x]` for all 9 Phase 35 requirement IDs.
    - REQUIREMENTS.md traceability table has `Complete` for all 9 IDs in Phase 35.
    - STATE.md frontmatter `progress.completed_phases` is incremented.
    - STATE.md §v1.5 Phases (planned) table Phase 35 row has `Complete (6/6 plans)`.
    - Single atomic commit lands citing D-22..D-27.
  </acceptance_criteria>
  <done>
    All three planning-state files reflect Phase 35 closed. Branch ready for merge to main.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                | Description                                                                                                                                |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Phase-close docs → milestone state                                      | SUMMARY + ROADMAP / REQUIREMENTS / STATE flips are auditable record of work shipped                                                        |
| ADR sub-block → architectural decision audit trail                      | Future readers depend on this for "why was X done in Phase 35?" — mitigated by mirroring established Phase 30/30.1/34 sub-block convention |
| Bundle + Upstash measurement → SIMPLIFY-07 + REDIS-OPT-04 closure proof | Measurements must be honestly captured; mitigated by D-19 single-file `wc -c` + D-20 raw-PNG screenshot (no transcription error)           |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                             | Disposition       | Mitigation Plan                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-14   | Repudiation            | Future operator wonders "did Phase 35 actually deliver REDIS-OPT-04?"                 | mitigate          | SUMMARY.md cites baseline + close PNGs by filename; ADR sub-block records same; STATE.md / ROADMAP.md / REQUIREMENTS.md cross-reference. |
| T-35-15   | Tampering              | Drift gate broken post-close (someone edits CLAUDE.md without updating redis-keys.md) | mitigate          | Drift gate from plan 35-01 catches this; Task 4 re-runs the gate as final close verification.                                            |
| T-35-16   | Information Disclosure | Upstash PNG contains sensitive operational metrics                                    | accept (low risk) | Committed to private repo; visible only to operator + Claude. No public exposure.                                                        |
| T-35-SC   | Tampering              | No npm/pip/cargo installs                                                             | n/a               | Pure docs + measurement commit; package legitimacy gate not applicable.                                                                  |

</threat_model>

<verification>
Drift gate green (re-run Task 4). Full suite green. ADR sub-block + SUMMARY.md present + non-placeholder. ROADMAP + REQUIREMENTS + STATE all show Phase 35 complete.
</verification>

<success_criteria>

1. Bundle close measurement captured + delta computed.
2. Upstash close screenshot committed.
3. ADR-0010 Phase 35 sub-block appended (mirrors Phase 30 / 30.1 / 34).
4. 35-SUMMARY.md authored with deltas + per-plan outcomes + decision trail.
5. Drift gate re-runs green at phase close.
6. ROADMAP.md, REQUIREMENTS.md, STATE.md all reflect Phase 35 complete.
7. Branch `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu` ready for merge.
8. Covers REDIS-OPT-04 + SIMPLIFY-07.
   </success_criteria>

<output>
This plan IS the SUMMARY for Phase 35. No additional SUMMARY file needed.

After merge to main:

- Branch deleted (operator).
- Next phase: Phase 36 (Public Docs Sweep + OpenAPI Additions).
  </output>

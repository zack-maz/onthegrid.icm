---
phase: 35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - docs/architecture/redis-keys.md
  - src/__tests__/lib/redis-registry.test.ts
  - .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-baseline-YYYY-MM-DD.png
autonomous: false
requirements: [DOCS-INT-03, REDIS-OPT-01, REDIS-OPT-02]
tags: [redis, registry, drift-gate, vitest, documentation, observability]
user_setup:
  - service: upstash
    why: 'REDIS-OPT-04 / D-20 — manual Upstash dashboard PNG capture (baseline). Operator action only; no env var changes.'
    dashboard_config:
      - task: "Capture Upstash dashboard 'Commands' metric PNG screenshot"
        location: 'Upstash Console → otg-iran-monitor database → Metrics / Commands tab'
must_haves:
  truths:
    - 'Branch `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu` cut from `main` (D-27).'
    - '`api/vercel-entry.js` baseline size captured (`wc -c` = 1,779,504 bytes ± rebuild jitter; D-19).'
    - "Upstash dashboard 'Commands' PNG screenshot committed at `.planning/phases/35-*/redis-budget-baseline-YYYY-MM-DD.png` (D-20)."
    - 'Hand-authored `docs/architecture/redis-keys.md` (NEW) lists every Redis key with columns Key | Writers (file:line) | Readers (file:line) | TTL | Value shape | Business purpose | Cardinality | Classification (D-05, D-06, D-08).'
    - 'CLAUDE.md §Serverless Cache gains the 4 missing keys: `events:llm:v3:lineage:{eventId}`, `events:llm:v3:lineage-keys`, `events:llm:v3:group-lineage:{hash}`, `events:llm-pipeline-audit` (D-14).'
    - 'CLAUDE.md §Serverless Cache refines `news:gdelt` + `news:feed` to two separate entries (Pitfall 4) and parametrizes `markets:yahoo` → `markets:yahoo:{range}` (Pitfall 3 / D-23).'
    - '`src/__tests__/lib/redis-registry.test.ts` exists and runs under `// @vitest-environment node`; asserts 3-surface parity (CLAUDE.md §Serverless Cache ↔ `docs/architecture/redis-keys.md` ↔ code-grep). `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0 (D-01..D-04).'
    - 'Each documented key carries a classification value ∈ {`load-bearing`, `observability`, `retire`} (REDIS-OPT-02 / D-08).'
  artifacts:
    - path: 'docs/architecture/redis-keys.md'
      provides: 'Deep-dive Redis key registry artifact'
      contains: '## events:*'
    - path: 'src/__tests__/lib/redis-registry.test.ts'
      provides: 'Drift-gate vitest enforcing 3-surface registry parity'
      contains: 'BACKTICK_KEY_RE'
    - path: 'CLAUDE.md'
      provides: 'Updated §Serverless Cache subsection (4 added, 2 refined)'
      contains: 'events:llm-pipeline-audit'
    - path: '.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-baseline-YYYY-MM-DD.png'
      provides: 'Upstash command-budget baseline screenshot (D-20)'
  key_links:
    - from: 'src/__tests__/lib/redis-registry.test.ts'
      to: 'CLAUDE.md §Serverless Cache + docs/architecture/redis-keys.md + server/**/*.ts + src/**/*.ts'
      via: 'fs readFileSync + regex parse + walk-tree codebase grep'
      pattern: 'BACKTICK_KEY_RE.*KEY_PREFIX_FAMILIES'
    - from: 'docs/architecture/redis-keys.md'
      to: 'server/lib/llmLineage.ts:20-110 + server/lib/pipelineAudit.ts:18-44'
      via: 'Writers + Readers columns cite file:line'
      pattern: "server/lib/llmLineage\\.ts:[0-9]+"
---

<objective>
Establish the load-bearing primitive of Phase 35: a mechanical drift-gate that prevents the Redis-key registry from rotting again. Phase 35 exists because the hand-maintained CLAUDE.md registry rotted during Phases 27-34 (4 missing keys + 1 retire-but-still-listed + 2 entries needing refinement). After this plan lands, registry drift fails the next `vitest run` instead of accumulating silently.

Purpose: Author `docs/architecture/redis-keys.md` from the grep-verified key inventory; surgically edit CLAUDE.md §Serverless Cache (4 add per D-14, 2 refine per D-23 / Pitfalls 3+4); ship `src/__tests__/lib/redis-registry.test.ts` per the Pattern 1 sketch in 35-RESEARCH.md. Capture phase-start baseline measurements (D-19 bundle size, D-20 Upstash PNG) for the SIMPLIFY-07 / REDIS-OPT-04 deltas measured in plan 35-06.

Output: 5 atomic commits (branch cut → baseline measurements → CLAUDE.md edits → redis-keys.md → vitest gate), branch `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu`, all PRE-Wave-2 work ready so dependent plans can fan out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Authoritative grep results from 35-RESEARCH.md §Grep Audit (lines 598-705). -->
<!-- Executor uses these file:line references DIRECTLY when authoring redis-keys.md. -->

Keys MISSING from current CLAUDE.md (add per D-14):

- `events:llm:v3:lineage:{eventId}` — writer `server/lib/llmLineage.ts:57` (HSET + EXPIRE); reader: lineage drill-down + `scripts/snapshot-v3-redis.ts:80`. TTL = 7d (`LINEAGE_TTL_SEC`).
- `events:llm:v3:lineage-keys` — writer `server/lib/llmLineage.ts:78-80` (ZADD + ZREMRANGEBYRANK + EXPIRE). TTL = 7d; capped 500 (`LINEAGE_MAX_ENTRIES`).
- `events:llm:v3:group-lineage:{hash}` — writer NOT YET IMPLEMENTED (key prefix declared at `server/lib/llmLineage.ts:110`); reader at `server/lib/llmEventExtractor.v3.ts:529-587` (pre-filter loop). TTL = 7d.
- `events:llm-pipeline-audit` — writer `server/lib/pipelineAudit.ts:33-35` (LPUSH + LTRIM + EXPIRE); reader `server/lib/pipelineAudit.ts:44` (LRANGE). TTL = 90d; capped 200.

Keys needing CLAUDE.md REFINEMENT (per D-23 / Pitfalls 3+4):

- `news:gdelt` + `news:feed` joint entry → SPLIT into 2 lines:
  - `news:feed` — writer `server/routes/news.ts:28`; reader same file + `server/lib/healthSources.ts:40`. TTL 15-min.
  - `news:gdelt` — writer GDELT-DOC adapter (verify location in `server/adapters/gdelt-doc.ts` via grep during execution); reader `server/lib/llmEventExtractor.v3.ts:107` + `server/routes/events.ts:672`. TTL 15-min.
- `markets:yahoo` → PARAMETRIZE to `markets:yahoo:{range}` where range ∈ {1d, 5d, 1mo, ytd}. Writer/reader `server/routes/markets.ts:26` (`cacheKey = \`markets:yahoo:${range}\``). TTL 60s.

Full 27-key inventory shape (verified file:line in 35-RESEARCH.md §Grep Audit):

- events: gdelt, backfill-ts, llm:v3, llm:v3:partial (RETIRE — listed for visibility but removed by plan 35-02), llm:v3:lineage:{eventId}, llm:v3:lineage-keys, llm:v3:group-lineage:{hash}, llm-summary:v3, llm-dlq, llm-process-ts, llm-eval-baseline:v3, llm-eval-adversarial:v3, llm-pipeline-audit, url-liveness:{eventId}, url-liveness-count
- flights: opensky, adsblol
- ships: ais
- sites: v3
- water: facilities:v3, precip
- news: feed, gdelt
- markets: yahoo:{range}
- geocode: {lat},{lon}, fwd:constrained:v2:{hash}
- llm: tokens:{provider}:YYYY-MM-DD, lastProgress
- cron: lastTick:health, lastTick:warm, lastTick:refresh-events
- operator: audit-log, replay-quota:{...}, prune-quota:{...}
- audit: connectivity:last-result

D-01 vitest sketch (from 35-RESEARCH.md Pattern 1, lines 133-277):

- File: `src/__tests__/lib/redis-registry.test.ts`
- Line 1 MUST be: `// @vitest-environment node` (Pitfall 6; jsdom default override)
- Imports: `readFileSync, readdirSync, statSync` from `node:fs`; `dirname, resolve, join` from `node:path`; `fileURLToPath` from `node:url`; `describe, it, expect` from `vitest`
- Constants:
  - `BACKTICK_KEY_RE = /\`((?:events|flights|ships|sites|water|news|markets|geocode|llm|cron|operator|audit):[a-zA-Z0-9:_\-{}\.]+)\`/g`
  - `KEY_PREFIX_FAMILIES = ['events:', 'flights:', 'ships:', 'sites:', 'water:', 'news:', 'markets:', 'geocode:', 'llm:', 'cron:', 'operator:', 'audit:']`
  - `EXEMPT_KEYS: ReadonlyArray<{key: string; reason: string}> = []` (empty at phase close per D-02; structurally available)
- Helper `extractKeysFromMarkdown(path)` — for CLAUDE.md, restricts to section between `## Serverless Cache` and next `^## `; for redis-keys.md, reads wholesale. Strips `{placeholder}` parts so `events:url-liveness:{eventId}` collapses to `events:url-liveness:`.
- Helper `walkTsFiles(dir, acc=[])` — recursive readdirSync; skips `node_modules`, `dist`, `.git`; matches `.ts` + `.tsx`.
- Helper `extractKeysFromCode()` — walks `server/` and `src/`; matches `['"\`]((?:events|flights|...):[a-zA-Z0-9:_\-]\*)`(template-literal prefix portion). Returns`Map<string, Set<string>>` (key → file refs).
- `describe('Phase 35 D-01 — Redis-key registry drift gate', ...)` containing 4 `it(...)` blocks:
  1. Every CLAUDE.md key is in redis-keys.md (D-03)
  2. Every redis-keys.md key is in CLAUDE.md (D-03)
  3. `it.each([...claudeKeys])` — each documented key has ≥1 code reference (prefix-match via `codeKey.startsWith(key)`)
  4. Every code key matches a documented key OR is exempt (no undocumented drift)
- Guard against silent empty-input it.each: `it('registry has ≥10 documented keys (guard)', () => expect(claudeKeys.size).toBeGreaterThanOrEqual(10))`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Branch cut + baseline measurements (D-19, D-20, D-27)</name>
  <files>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-baseline-YYYY-MM-DD.png
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md
  </files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-19, D-20, D-27)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (Pitfall 5 — wc scope)
    CLAUDE.md (§Conventions — branch-per-phase)
  </read_first>
  <action>
    Pause execution and prompt operator at the start of this task — branch + measurement steps require human action.

    STEP A — Branch cut (D-27): The current branch is `main`. Run `git status` to confirm clean working tree (only the existing CONTEXT.md / DISCUSSION-LOG.md / PATTERNS.md / RESEARCH.md / VALIDATION.md scaffold may be modified; nothing else). Then `git checkout -b feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu` from main. Push immediately with `git push -u origin feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu` so the branch is tracking. NEVER commit phase work to main (CLAUDE.md §Conventions).

    STEP B — Bundle-size baseline (D-19): Run EXACTLY `wc -c api/vercel-entry.js` (single-file path per Pitfall 5; NOT `wc -c api/*` which globs the `_eval` subdirectory). Record the result. Expected value ≈ 1,779,504 bytes (verified 2026-05-26 in 35-RESEARCH.md). If the value differs by >5% from 1.70 MB, run `npm run build` first to refresh the artifact, then re-measure. Reported baseline ≠ expected = wrong path (Pitfall 5).

    STEP C — Upstash baseline screenshot (D-20): Prompt operator to open Upstash Console → otg-iran-monitor database → Metrics / Commands tab. Capture PNG screenshot of the "Commands" metric chart. Commit to `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-baseline-{YYYY-MM-DD}.png` (substitute today's UTC date). This is a checkpoint:human-action — Claude cannot access the Upstash dashboard (no CLI / REST exposes `INFO commandstats` per D-20).

    STEP D — Record measurements: Create `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md` with the bundle size in bytes + the screenshot filename + the date + a one-line "Phase 35 baseline; close measurements in 35-06-PLAN.md task" header. Use Write tool (NOT heredoc per planner rules).

    STEP E — Commit: `chore(35): branch cut + capture phase baseline measurements (D-19, D-20, D-27)` body cites baseline byte count + screenshot filename. Atomic per D-26.

    NO production code touched.

  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -q "^feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu$" && wc -c api/vercel-entry.js | grep -E "^[[:space:]]*[0-9]+" && ls .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/redis-budget-baseline-*.png 2>/dev/null | head -1 && test -f .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-baseline-measurements.md</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs exactly `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu`.
    - `wc -c api/vercel-entry.js` outputs a non-zero byte count within 5% of 1,779,504.
    - Exactly one PNG file matching `redis-budget-baseline-*.png` exists in the phase directory.
    - `35-baseline-measurements.md` exists and contains the byte count + screenshot filename.
    - `git log -1 --format=%s` matches the prefix `chore(35):` with `D-19` and `D-20` in body.
  </acceptance_criteria>
  <done>
    Branch is feature/35-..., bundle baseline + Upstash screenshot captured and committed, baseline-measurements.md records numbers for plan 35-06 to diff against.
  </done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md §Serverless Cache surgical edits (D-12, D-14, D-23)</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (lines 116-140 — current §Serverless Cache subsection; the surface being edited)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 438-471 — exact CLAUDE.md edit pattern + 4 add / 1 remove / 2 refine markdown blocks)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (lines 598-705 §Grep Audit — file:line references for each new entry)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-12, D-14, D-23)
    server/lib/llmLineage.ts (lines 1-115 — to verify writer locations for lineage trio)
    server/lib/pipelineAudit.ts (full file — to verify pipeline-audit writer/reader)
  </read_first>
  <action>
    Use the Edit tool to make surgical CLAUDE.md edits. NEVER use heredoc; NEVER use Write to overwrite the entire file (preserves Phase 29 trim discipline).

    EDIT 1 (D-14 — ADD 4 new entries): Find the existing bullet for `**`events:llm:v3`** — active terminal LLM-enriched cache; ...`. Insert the 4 new bullets immediately AFTER the bullet for `**`events:llm-process-ts`**` (or thereabouts — choose insertion point that keeps `events:*` family contiguous). Each bullet uses the existing 1-line-per-key shape verbatim from 35-PATTERNS.md lines 449-453:

    - `**`events:llm:v3:lineage:{eventId}` (Phase 27.4.3 D-13)** — HSET of per-event lineage record (prompt/response/parsed/coord/reasoningTrace/lineageHash); 7d TTL. Writer: `server/lib/llmLineage.ts:57` `appendLineage`. Reader: lineage drill-down (DevApiStatus) + `scripts/snapshot-v3-redis.ts`.

    - `**`events:llm:v3:lineage-keys` (Phase 27.4.3 D-13)** — ZADD sorted-set index of lineage entries; 7d TTL; capped 500 entries (`LINEAGE_MAX_ENTRIES`). Writer: `server/lib/llmLineage.ts:78`. LRU eviction via `ZREMRANGEBYRANK`.

    - `**`events:llm:v3:group-lineage:{hash}` (Phase 27.4.4 D-18)** — pre-filter cache for group-level lineage; 7d TTL. Reader: `server/lib/llmEventExtractor.v3.ts:529-587` (`processEventGroupsV3` pre-filter loop). Write side not yet implemented (Plan 02 Gate B follow-up — see `server/lib/llmLineage.ts:104` comment).

    - `**`events:llm-pipeline-audit` (Phase 27.4.3 D-15)** — LPUSH + LTRIM bounded list (200 cap); 90d TTL. Writer: `server/lib/pipelineAudit.ts:33-35` `appendPipelineAudit`. Reader: `:44` `listPipelineAudit`. Historical record of pipeline-version flips — no new writers expected post-Phase-29.

    EDIT 2 (D-23 / Pitfall 4 — REFINE news entry): Find the existing joint `news:gdelt` + `news:feed` bullet. SPLIT into two separate bullets:

    - `**`news:feed`** — clustered render-target cache (RSS + GDELT-DOC merged, Jaccard 0.8 dedup, 7-day window); 15-min TTL. Writer: `server/routes/news.ts:28`. Reader: same file + `server/lib/healthSources.ts:40`.

    - `**`news:gdelt`** — raw GDELT-DOC LLM-input cache; 15-min TTL. Writer: GDELT-DOC adapter (`server/adapters/gdelt-doc.ts`). Reader: `server/lib/llmEventExtractor.v3.ts:107` (NEWS BLOCK in prompt); `server/routes/events.ts:672` (fallback path).

    Before writing the `news:gdelt` line, run `grep -rn "news:gdelt" server/adapters/` to confirm the exact adapter file:line. Cite the verified line ref in the bullet (e.g. `server/adapters/gdelt-doc.ts:NN`).

    EDIT 3 (D-23 / Pitfall 3 — PARAMETRIZE markets entry): Find the existing `**`markets:yahoo`**` bullet. REPLACE the key name with `markets:yahoo:{range}` and rewrite the description:

    - `**`markets:yahoo:{range}`** — Yahoo Finance commodity prices, one key per `range ∈ {1d, 5d, 1mo, ytd}` (4 keys total); 60s TTL. Writer/reader: `server/routes/markets.ts:26` (`cacheKey = \`markets:yahoo:${range}\``).

    DO NOT touch the `events:llm:v3:partial` bullet in this plan — that's plan 35-02's job (D-12 removal happens alongside the writer deletion to keep the partial-key retirement atomic). Leaving it in CLAUDE.md temporarily is correct; the drift gate (Task 4) will pass because both CLAUDE.md and redis-keys.md will document it as `retire` classification until plan 35-02 removes it.

    NO edits OUTSIDE §Serverless Cache subsection (D-23 + Phase 29 trim discipline).

    Commit: `docs(35): add 4 missing keys + refine news/markets entries in CLAUDE.md §Serverless Cache (D-14, D-23)` body lists the 6 edits. Atomic per D-26.

  </action>
  <verify>
    <automated>grep -q "events:llm:v3:lineage:{eventId}" CLAUDE.md && grep -q "events:llm:v3:lineage-keys" CLAUDE.md && grep -q "events:llm:v3:group-lineage:{hash}" CLAUDE.md && grep -q "events:llm-pipeline-audit" CLAUDE.md && grep -q "markets:yahoo:{range}" CLAUDE.md && grep -cE "^\- \*\*\`news:feed\`\*\*|^\- \*\*\`news:gdelt\`\*\*" CLAUDE.md | awk '$1>=2 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "events:llm:v3:lineage:{eventId}" CLAUDE.md` exits 0.
    - `grep -q "events:llm:v3:lineage-keys" CLAUDE.md` exits 0.
    - `grep -q "events:llm:v3:group-lineage:{hash}" CLAUDE.md` exits 0.
    - `grep -q "events:llm-pipeline-audit" CLAUDE.md` exits 0.
    - `grep -q "markets:yahoo:{range}" CLAUDE.md` exits 0; `grep -q "^- \*\*\`markets:yahoo\`\*\*" CLAUDE.md` exits 1 (old non-parametrized form removed).
    - Both `news:feed` and `news:gdelt` appear as separate `^- \*\*\`...\`\*\*` bullets in CLAUDE.md.
    - `git diff main -- CLAUDE.md` shows changes ONLY in the §Serverless Cache subsection (no edits outside lines ~116-150).
    - `git log -1 --format=%s` contains `docs(35):`, `D-14`, `D-23`.
  </acceptance_criteria>
  <done>
    CLAUDE.md §Serverless Cache has 4 new key entries (lineage trio + pipeline-audit), 2 refined entries (news split, markets parametrized). All other CLAUDE.md content untouched. Single atomic docs commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author docs/architecture/redis-keys.md deep-dive inventory (D-05, D-06, D-08)</name>
  <files>docs/architecture/redis-keys.md</files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (lines 598-705 — full Grep Audit; THE INPUT for this table)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 124-165 — markdown header + table column shape)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-05, D-06, D-07, D-08, D-24)
    docs/architecture/llm-pipeline-reliability.md (lines 1-30 — style template; same directory family)
    CLAUDE.md §Serverless Cache (post-Task-2 state — the companion surface)
  </read_first>
  <action>
    Use the Write tool to create NEW file `docs/architecture/redis-keys.md`. Plain markdown + tables, no fancy generators (D-24).

    HEADER block (verbatim from 35-PATTERNS.md lines 130-137, with date placeholders filled in):

    ```
    # Redis Key Registry (v1.5)

    > Auditable inventory of every Redis key written or read by `otg-iran-monitor` in production. Pinned by `src/__tests__/lib/redis-registry.test.ts` — drift fails the next `vitest run`.

    **Source of truth:** Code (line references below). This document is the operator skim's companion deep-dive; the test is the gate.
    **Companion surface:** [`CLAUDE.md` §"Serverless Cache (Phase 13)"](../../CLAUDE.md) — same key list, one-line-per-key shape, refreshed in lockstep.
    **Phase 35 measurement window:** Upstash command budget baseline → close (see `redis-budget-baseline-YYYY-MM-DD.png` / `redis-budget-close-YYYY-MM-DD.png` in `.planning/phases/35-*/`).

    ---
    ```

    TABLE STRUCTURE: One table per prefix family (D-06 sort by prefix family, then alphabetical within family per Claude's Discretion 3). Family order (verbatim from 35-PATTERNS.md lines 152-164): events, flights, ships, sites, water, news, markets, geocode, llm, cron, operator, audit.

    Each table has 8 columns (verbatim D-06 column order): `Key | Writers | Readers | TTL | Value | Purpose | Cardinality | Classification`

    Classification enum (D-08): `load-bearing` | `observability` | `retire`. Add rationale in Purpose column (single sentence per entry).

    Cardinality column rules (D-07): accepts ranges + time-bounded estimates. Examples: `1`, `<200`, `≤500 (capped)`, `~5000 (production sample 2026-05-26)`, `n/a (set)`, `absent (Phase 34 deferred)`.

    POPULATE each row from the 35-RESEARCH.md §Grep Audit (lines 598-705). For each key in the audit, write one row with the file:line refs verbatim. The audit is THE authoritative input — do not re-research; transcribe the file:line refs directly.

    SPECIFIC ROWS that need special treatment:

    Row for `events:llm:v3:partial`: Classification = `retire`. Purpose column: "RETIRED Phase 35 / SIMPLIFY-02 (plan 35-02). Hobby-era 300s-budget mitigation; Pro 800s makes terminal writes reliable. Natural TTL expiry within 9000s (≈ 2.5h) of deploy. See ADR-0010 Phase 35 sub-block." Writers column = "(retired in plan 35-02)"; Readers column = "NONE (verified by grep; never had production reader)"; Cardinality = "0 (post-retirement)". Verbatim example at 35-PATTERNS.md line 149.

    Row for `events:llm:v3:group-lineage:{hash}`: Writers column = "(none — write-side not yet implemented; `server/lib/llmLineage.ts:104` comment); Readers column = "`server/lib/llmEventExtractor.v3.ts:529-587` (pre-filter loop)"; Classification = `observability`. Purpose column = "Reader-only currently; pre-filter cache for group-level lineage. Future-phase write-side per `llmLineage.ts:104`."

    Row for `markets:yahoo:{range}`: Single row for the parametric prefix; Cardinality = "4 (one per range ∈ {1d, 5d, 1mo, ytd})". Per Pitfall 3.

    Row for `news:feed` and `news:gdelt`: Two separate rows. Per Pitfall 4.

    Cerebras / Groq token-budget rows: Use Cardinality = "absent (Phase 34 deferred — see ADR-0010 Phase 34 sub-block)". Per CONTEXT.md "Out of scope" note + 35-RESEARCH.md.

    Classification counts (D-06 informational): approximately `load-bearing` ≈ 15, `observability` ≈ 9, `retire` = 1 (per 35-RESEARCH.md line 539).

    FOOTER section (after all 12 family tables):

    ```
    ---

    ## Classification Counts (D-08)

    - **load-bearing:** N keys (removal breaks a live feature).
    - **observability:** N keys (bounded; OK to keep but cap reviewed).
    - **retire:** N keys (no callers OR replaced).

    ## Verification

    The Redis-key registry drift gate at `src/__tests__/lib/redis-registry.test.ts` parses this file PLUS `CLAUDE.md` §Serverless Cache PLUS the codebase grep. Drift between the three surfaces fails `npx vitest run src/__tests__/lib/redis-registry.test.ts`.

    ## Phase 35 Provenance

    - Authored: {YYYY-MM-DD} (plan 35-01 task 3) per DOCS-INT-03 + REDIS-OPT-01 / D-05 / D-06.
    - Grep audit input: `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md` §Grep Audit.
    - Drift gate: `src/__tests__/lib/redis-registry.test.ts` (plan 35-01 task 4 / D-01).
    ```

    Use today's UTC date for the {YYYY-MM-DD} placeholder in the Phase 35 Provenance block.

    Commit: `docs(35): author docs/architecture/redis-keys.md deep-dive inventory (D-05, D-06, D-08)` body lists key family counts. Atomic per D-26.

  </action>
  <verify>
    <automated>test -f docs/architecture/redis-keys.md && grep -q "^## events:\*" docs/architecture/redis-keys.md && grep -q "^## flights:\*" docs/architecture/redis-keys.md && grep -q "^## audit:\*" docs/architecture/redis-keys.md && grep -q "events:llm-pipeline-audit" docs/architecture/redis-keys.md && grep -q "markets:yahoo:{range}" docs/architecture/redis-keys.md && grep -qE "load-bearing|observability|retire" docs/architecture/redis-keys.md && grep -q "RETIRED Phase 35 / SIMPLIFY-02" docs/architecture/redis-keys.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/architecture/redis-keys.md` exists.
    - File contains 12 prefix-family headings (`## events:*`, `## flights:*`, `## ships:*`, `## sites:*`, `## water:*`, `## news:*`, `## markets:*`, `## geocode:*`, `## llm:*`, `## cron:*`, `## operator:*`, `## audit:*`).
    - File contains all 4 new keys: `events:llm:v3:lineage:{eventId}`, `events:llm:v3:lineage-keys`, `events:llm:v3:group-lineage:{hash}`, `events:llm-pipeline-audit`.
    - File contains row for `events:llm:v3:partial` with Classification = `retire` and Purpose mentioning "RETIRED Phase 35 / SIMPLIFY-02".
    - File has Classification column populated for every row (no empty cells in column 8).
    - File contains the 8-column header `Key | Writers | Readers | TTL | Value | Purpose | Cardinality | Classification`.
    - `git log -1 --format=%s` matches `docs(35):` with `D-05` or `D-06` in body.
  </acceptance_criteria>
  <done>
    docs/architecture/redis-keys.md is a complete 12-family inventory with all 27 keys (per 35-RESEARCH.md count) classified and cross-referenced. Vitest gate (Task 4) parses it; CLAUDE.md skim links to it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Ship redis-registry.test.ts drift gate (D-01, D-02, D-03, D-04)</name>
  <files>src/__tests__/lib/redis-registry.test.ts</files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (lines 133-277 — full Pattern 1 vitest sketch; THE template)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 36-121 — composite-template description)
    src/__tests__/lib/actorCatalog.test.ts (fs + path + describe pattern — primary analog)
    src/__tests__/lib/colorBridge.test.ts (byte-identity sentinel + describe-block grouping — secondary analog)
    server/__tests__/lib/urlLiveness.schema.test.ts (`@vitest-environment node` directive — tertiary analog)
    CLAUDE.md (post-Task-2 state — the surface being parsed)
    docs/architecture/redis-keys.md (post-Task-3 state — the other surface)
  </read_first>
  <behavior>
    - Test 1: Every documented key in CLAUDE.md §Serverless Cache is also documented in `docs/architecture/redis-keys.md` (D-03 surface parity, CLAUDE → redis-keys.md direction). Failure message names the offending key + the surface it's missing from.
    - Test 2: Every documented key in `docs/architecture/redis-keys.md` is also documented in CLAUDE.md §Serverless Cache (D-03 reverse direction).
    - Test 3 (it.each over [...claudeKeys]): Each documented key in CLAUDE.md has ≥1 code reference matched via prefix-match (`codeKey.startsWith(key)`). Skipped for keys in `EXEMPT_KEYS`. Per CONTEXT.md D-02, EXEMPT_KEYS is empty at phase close; structurally available.
    - Test 4: Every key found in code (via grep across `server/**/*.ts` + `src/**/*.ts`) is documented in BOTH surfaces OR appears in EXEMPT_KEYS. Failure message names the orphan code key + the first 3 file paths where it was found.
    - Guard test (described as "registry has at least 10 documented keys (guard against silent empty-input it.each pass)"): asserts `claudeKeys.size >= 10`. Prevents the it.each over an empty Set from silently passing.

    Failure scenarios that MUST be caught:
    - Adding `'events:newkey'` to a `.ts` file without documenting → Test 4 fails citing the file.
    - Documenting `events:foo` in CLAUDE.md but not in redis-keys.md → Test 1 fails.
    - Documenting `events:foo` in redis-keys.md but not in code → Test 3 fails.
    - Empty registry parse (regex broken) → Guard test fails with `claudeKeys.size = 0 < 10`.

  </behavior>
  <action>
    Use the Write tool to create NEW file `src/__tests__/lib/redis-registry.test.ts`.

    LINE 1 MUST be: `// @vitest-environment node` (Pitfall 6 — required because default vitest config is jsdom per `vite.config.ts:56`).

    LINES 2-23 (header block): JSDoc citing Phase 35 D-01 + mirroring colorBridge / actorCatalog / urlLiveness templates. Verbatim shape from 35-PATTERNS.md lines 42-57.

    IMPORTS (lines after header):
    ```
    import { readFileSync, readdirSync, statSync } from 'node:fs';
    import { dirname, resolve, join } from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { describe, it, expect } from 'vitest';
    ```

    CONSTANTS:
    - `__filename` / `__dirname` shim (verbatim from actorCatalog.test.ts:33-40 / 35-PATTERNS.md lines 65-69).
    - `REPO_ROOT = resolve(__dirname, '../../..')` — three levels up from `src/__tests__/lib/`.
    - `EXEMPT_KEYS: ReadonlyArray<{key: string; reason: string}> = []` — empty at phase close per D-02; structurally available; carries example-shape comment per 35-PATTERNS.md lines 117-119.
    - `BACKTICK_KEY_RE = /\`((?:events|flights|ships|sites|water|news|markets|geocode|llm|cron|operator|audit):[a-zA-Z0-9:_\-{}\.]+)\`/g`
    - `KEY_PREFIX_FAMILIES: string[] = ['events:', 'flights:', 'ships:', 'sites:', 'water:', 'news:', 'markets:', 'geocode:', 'llm:', 'cron:', 'operator:', 'audit:']`
    - `DOC_ALLOW_LIST = new Set([resolve(REPO_ROOT, 'CLAUDE.md'), resolve(REPO_ROOT, 'docs/architecture/redis-keys.md')])`

    HELPER `extractKeysFromMarkdown(path: string): Set<string>`:
    - readFileSync(path, 'utf-8')
    - If path ends with `CLAUDE.md`: restrict to `src.split('## Serverless Cache')[1]?.split('\n## ')[0] ?? ''`. If redis-keys.md: read wholesale.
    - matchAll BACKTICK_KEY_RE; for each match, strip `{...}` placeholder parts (`m[1].replace(/\{[^}]+\}/g, '')`) and trailing `:` (`.replace(/:$/, '')`). Add to Set.
    - Return Set<string>.

    HELPER `walkTsFiles(dir: string, acc: string[] = []): string[]`:
    - readdirSync(dir); skip `node_modules`, `dist`, `.git`.
    - For each entry: statSync; recurse if directory; push if .ts/.tsx.

    HELPER `extractKeysFromCode(): Map<string, Set<string>>`:
    - files = walkTsFiles(`${REPO_ROOT}/server`) + walkTsFiles(`${REPO_ROOT}/src`).
    - Build literalRe matching `['"\`]((?:events|flights|ships|sites|water|news|markets|geocode|llm|cron|operator|audit):[a-zA-Z0-9:_\-]*)` (template-literal prefix portion only).
    - For each file, readFileSync + matchAll; collect into Map<key, Set<filePath>>.

    DESCRIBE BLOCK `describe('Phase 35 D-01 — Redis-key registry drift gate', () => { ... })`:
    - Module-level (inside describe but outside it()) — compute `claudeKeys`, `redisKeysDoc`, `codeKeys` once via the helpers.
    - `it('registry has at least 10 documented keys (guard against silent empty-input it.each pass)', () => expect(claudeKeys.size).toBeGreaterThanOrEqual(10))` — guard test per actorCatalog.test.ts:74-78.
    - `it('every documented key in CLAUDE.md is also documented in redis-keys.md (D-03)', () => { for (const k of claudeKeys) expect(redisKeysDoc.has(k), \`${k} in CLAUDE.md but not redis-keys.md\`).toBe(true); })`
    - `it('every documented key in redis-keys.md is also documented in CLAUDE.md (D-03)', () => { for (const k of redisKeysDoc) expect(claudeKeys.has(k), \`${k} in redis-keys.md but not CLAUDE.md\`).toBe(true); })`
    - `it.each([...claudeKeys])('documented key %s has ≥1 code reference', (key) => { const exempt = EXEMPT_KEYS.find(e => e.key === key); if (exempt) return; const found = [...codeKeys.keys()].some(codeKey => codeKey.startsWith(key)); expect(found, \`${key} documented but no code reference found\`).toBe(true); })`
    - `it('every code key matches a documented key (no undocumented drift)', () => { const documented = new Set([...claudeKeys, ...redisKeysDoc]); for (const [codeKey, refs] of codeKeys) { const hit = [...documented].some(d => codeKey.startsWith(d)); const exempt = EXEMPT_KEYS.find(e => codeKey.startsWith(e.key)); if (!hit && !exempt) { expect.fail(\`Code key ${codeKey} (refs: ${[...refs].slice(0, 3).join(', ')}) is not documented in CLAUDE.md §Serverless Cache nor docs/architecture/redis-keys.md\`); } } })`

    DO NOT add complex generators or AST parsers (35-RESEARCH.md §Don't Hand-Roll). Regex on backticked-key strings is the load-bearing primitive (D-Discretion-1).

    DO NOT enforce TTL string consistency (Claude's Discretion 7 — out of scope; future phase territory).

    VERIFY locally: `npx vitest run src/__tests__/lib/redis-registry.test.ts` MUST exit 0. If it fails, the gate is catching real drift — fix the registry (Task 2 or Task 3 likely missed a key), NOT the test. Common failure: a new test fixture string in `server/__tests__/**/*.test.ts` introducing a new key family; verify it's intentional, not an orphan.

    NOTE: The test currently exempts test files BY DESIGN — the codebase scan walks `server/**/*.ts` AND `src/**/*.ts`, INCLUDING `__tests__/` paths (per D-02). Test fixtures that set up cache state are legitimate references — that's intentional.

    Commit: `test(35): ship Redis-key registry drift gate vitest (D-01, D-02, D-03, D-04)` body cites the 4 assertions + the guard test. Atomic per D-26.

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/redis-registry.test.ts 2>&1 | tail -20 && grep -q "^// @vitest-environment node" src/__tests__/lib/redis-registry.test.ts && grep -q "BACKTICK_KEY_RE" src/__tests__/lib/redis-registry.test.ts && grep -q "EXEMPT_KEYS" src/__tests__/lib/redis-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/lib/redis-registry.test.ts` exists.
    - Line 1 is exactly `// @vitest-environment node` (verified by `head -1 src/__tests__/lib/redis-registry.test.ts | grep -q "^// @vitest-environment node"`).
    - File contains `BACKTICK_KEY_RE`, `KEY_PREFIX_FAMILIES`, `EXEMPT_KEYS`, `extractKeysFromMarkdown`, `walkTsFiles`, `extractKeysFromCode`.
    - File contains exactly 4 `it(...)` blocks plus 1 guard test (verifiable via `grep -c "^  it(" src/__tests__/lib/redis-registry.test.ts` ≥ 4 plus 1 it.each).
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0.
    - `npx tsc --noEmit` exits 0 (no TS errors from the new test).
    - `git log -1 --format=%s` matches `test(35):` with `D-01` in body.
  </acceptance_criteria>
  <done>
    Drift-gate vitest is live. CLAUDE.md ↔ redis-keys.md ↔ code parity is mechanically enforced. Future drift (new key, retired key, renamed key) fails the next `vitest run` instead of silently rotting.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                          |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Author → markdown edits (CLAUDE.md, redis-keys.md) | Hand-authored doc edits with no input validation; mitigated by drift-gate vitest catching mismatches |
| Author → vitest helpers (fs walk)                  | Hardcoded paths only (`resolve(__dirname, '../../..')`); no user input fed to paths                  |
| Operator → Upstash dashboard PNG                   | Manual screenshot capture; no API surface; mitigated by D-20 explicit-honest-measurement choice      |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                          | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | ---------------------- | ------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-35-01   | Information Disclosure | redis-registry.test.ts failure messages                            | mitigate    | Failure messages cite key name + file path; never full file contents. Verified in Pattern 1 sketch + Pitfall 5 of 35-RESEARCH.md.                                              |
| T-35-02   | Tampering              | vitest reading arbitrary files via path injection                  | mitigate    | Test reads HARDCODED paths (`resolve(__dirname, '../../..', 'CLAUDE.md')` etc.). No user input is fed to the path.                                                             |
| T-35-03   | Information Disclosure | Upstash dashboard PNG screenshot may contain sensitive metric data | accept      | Operator-driven manual capture; committed to private repo; no public exposure. PNG contains only command-counter metrics, not key contents.                                    |
| T-35-04   | Information Disclosure | redis-keys.md publishes Redis key prefixes                         | accept      | Key prefixes (`events:llm:v3` etc.) are not secrets; Upstash REST URL + token already gates access. Doc improves operator skill at the cost of zero additional attack surface. |
| T-35-SC   | Tampering              | No npm/pip/cargo installs in this plan                             | n/a         | Plan 35-01 adds zero new dependencies; vitest is already a dev dep. Package legitimacy gate not applicable.                                                                    |

</threat_model>

<verification>
Full suite: `npx vitest run` exits 0 (drift gate passes; no regressions in unrelated tests).
TS gate: `npx tsc --noEmit` exits 0.
Manual review: Operator confirms PNG screenshot is a faithful capture of Upstash dashboard "Commands" metric at the date in the filename.
Goal-backward sanity: Open CLAUDE.md §Serverless Cache; verify the 4 new entries and 2 refined entries read fluently. Open `docs/architecture/redis-keys.md`; verify the 8-column table for `events:*` family is complete.
</verification>

<success_criteria>

1. Branch `feature/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu` cut from main and pushed.
2. Baseline measurements captured (bundle size + Upstash PNG + measurements .md) and committed.
3. CLAUDE.md §Serverless Cache reflects 4 added + 2 refined entries; partial-key entry remains (removed in plan 35-02).
4. `docs/architecture/redis-keys.md` (NEW) covers all 27 keys across 12 prefix-family tables with 8-column shape.
5. `src/__tests__/lib/redis-registry.test.ts` (NEW) exits 0 on `npx vitest run`.
6. All 5 commits land atomically with `D-NN` references in body.
7. `npx vitest run` full suite green; `npx tsc --noEmit` green.
8. Coverage: DOCS-INT-03 (registry verification) + REDIS-OPT-01 (inventory artifact) + REDIS-OPT-02 (classification per row).
   </success_criteria>

<output>
Create `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-01-SUMMARY.md` when done. Include:
- Branch creation timestamp.
- Bundle baseline byte count + screenshot filename + UTC date.
- 6 CLAUDE.md edit line counts (4 added + 2 refined).
- redis-keys.md key count + classification breakdown (load-bearing / observability / retire).
- Drift-gate test count (5: 4 it() + 1 guard).
- Commit hashes (5: chore baseline / docs CLAUDE / docs redis-keys / test gate).
</output>

---
phase: 35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu
plan: 04
type: execute
wave: 3
depends_on: ['35-01', '35-02']
files_modified:
  - server/lib/llmExtractionPipeline.ts
  - server/lib/llmEventExtractor.v3.ts
  - server/lib/llmResolver.ts
  - server/lib/llmCircuitBreaker.ts
  - server/lib/llmDLQ.ts
  - server/lib/llmTokenBudget.ts
  - server/lib/llmExtractorWatchdog.ts
autonomous: true
requirements: [DOCS-INT-02]
tags: [documentation, jsdoc, audit, llm-pipeline]
must_haves:
  truths:
    - 'Every `export function` / `export const` / `export type` / `export interface` in the 7 LLM-pipeline modules carries a one-line JSDoc above its declaration that is TRUE TODAY (D-09, D-10).'
    - 'Top-of-file JSDoc blocks are NOT rewritten — historical citations of `Phase 27.4.3 D-03` etc. are preserved as navigation aids (D-09 scope rule).'
    - "Each module's audit lands as a SEPARATE atomic commit (7 commits total per CONTEXT.md Claude's Discretion 5 + D-26)."
    - "The partial-key references in `llmEventExtractor.v3.ts` are deleted by plan 35-02 BEFORE this plan runs — this plan's wave dependency on 35-02 makes the JSDoc audit operate against post-D-12 code."
    - 'No production logic touched; comment-only edits.'
    - '`npx tsc --noEmit` exits 0 after each module commit; `npx vitest run` full suite exits 0 after the final commit.'
    - '**Early-stop collapse permitted** (per CONTEXT.md D-25 Claude''s Discretion 5 collapse rule + 35-PATTERNS.md "minimal delta" note): If Tasks 4-7 (`llmCircuitBreaker`, `llmDLQ`, `llmTokenBudget`, `llmExtractorWatchdog`) ALL yield zero rewrites during the read-aloud audit, fold them into a single `docs(35): verify llmCircuitBreaker + llmDLQ + llmTokenBudget + llmExtractorWatchdog JSDoc current (DOCS-INT-02, D-09)` commit. Tasks 1-3 (the hot-spot modules) always commit individually.'
  artifacts:
    - path: 'server/lib/llmExtractionPipeline.ts'
      provides: 'Public-API JSDoc one-liners refreshed for runRefreshExtraction, enrichedV3ToEntities + supporting types'
      contains: '/**'
    - path: 'server/lib/llmEventExtractor.v3.ts'
      provides: 'Public-API JSDoc one-liners refreshed for processEventGroupsV3, EVENTS_LLM_V3_KEY, LLM_REDIS_TTL_SEC + supporting types (post plan 35-02 partial-key deletion)'
      contains: '/**'
    - path: 'server/lib/llmResolver.ts'
      provides: 'Public-API JSDoc one-liners refreshed for resolveLocation, derivePrecision + supporting types'
      contains: '/**'
    - path: 'server/lib/llmCircuitBreaker.ts'
      provides: 'Public-API JSDoc one-liners verified for Provider type, isAvailable, record, shouldPauseNewEvents'
      contains: '/**'
    - path: 'server/lib/llmDLQ.ts'
      provides: 'Public-API JSDoc one-liners verified for DLQEntry, enqueueDLQ, DLQ_KEY, __testing'
      contains: '/**'
    - path: 'server/lib/llmTokenBudget.ts'
      provides: 'Public-API JSDoc one-liners verified for incrementTokenCounter, budgetState, DAILY_LIMITS'
      contains: '/**'
    - path: 'server/lib/llmExtractorWatchdog.ts'
      provides: 'Public-API JSDoc one-liners verified for withBatchWatchdog'
      contains: '/**'
  key_links:
    - from: "Each module's exports"
      to: 'true-today JSDoc one-liner above each export'
      via: 'manual audit per D-09 / D-10 (read-aloud against function body)'
      pattern: "/\\*\\*[\\s\\S]+?\\*/\\s*export"
---

<objective>
Bring the 7 LLM-pipeline modules' public-API JSDoc current. Per CONTEXT.md D-09 + D-10: scope is one-liners only (NOT top-of-file block rewrites — those are historical waymarkers and out of scope per Claude's Discretion 5). Each exported `function` / `const` / `type` / `interface` gets a single accurate `/** ... */` line above its declaration. JSDoc that cites a now-deleted code path (e.g. partial-key references) is rewritten to describe current behavior; everything else is verified and left alone.

Purpose: Make the 7 modules navigable for future readers without rewriting historical context. Per 35-RESEARCH.md, most existing JSDoc is already true-today — the audit's value is the DELTA (newly added since the comment was written) and the corrections (now-deleted code paths).

Output: 7 atomic commits, one per module. Wave 3 (depends on plan 35-02) because `llmEventExtractor.v3.ts` JSDoc audit operates against post-partial-key-deletion code. Other modules could run earlier; consolidating to wave 3 keeps the commit-per-module discipline clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-02-PLAN.md

<interfaces>
<!-- 7 modules, ~44 total public exports (per 35-RESEARCH.md line 536 enumeration). -->
<!-- Most existing JSDoc is GOOD-AS-IS; audit value is in the rare rewrites. -->

Module enumeration with export counts (35-RESEARCH.md line 536):

1. `server/lib/llmExtractionPipeline.ts` — 5 exports (~592 LOC). Public: `runRefreshExtraction`, `enrichedV3ToEntities`, `LLM_EVENTS_KEY_ACTIVE`, `LLM_SUMMARY_KEY_ACTIVE`, supporting types.
2. `server/lib/llmEventExtractor.v3.ts` — ~13 exports (~1151 LOC post-plan-35-02 deletion). Public: `processEventGroupsV3`, `EVENTS_LLM_V3_KEY` (after D-12 removes `EVENTS_LLM_V3_PARTIAL_KEY`), `LLM_REDIS_TTL_SEC`, schema constants, supporting types.
3. `server/lib/llmResolver.ts` — 8 exports (~629 LOC). Public: `resolveLocation`, `derivePrecision`, `__resetThrottleForTests`, supporting types.
4. `server/lib/llmCircuitBreaker.ts` — 5 exports (~73 LOC). Public: `Provider` type, `isAvailable`, `record`, `shouldPauseNewEvents`, `getState`.
5. `server/lib/llmDLQ.ts` — 4 exports (~106 LOC). Public: `DLQEntry` type, `enqueueDLQ`, `DLQ_KEY`, `__testing` symbol.
6. `server/lib/llmTokenBudget.ts` — 7 exports (~183 LOC). Public: `incrementTokenCounter`, `budgetState`, `DAILY_LIMITS`, `todayKey`, `incrDailyTokens`, `getDailyTokens`, supporting types.
7. `server/lib/llmExtractorWatchdog.ts` — 2 exports (~117 LOC). Public: `withBatchWatchdog`, supporting types.

D-09 SCOPE RULE (verbatim from CONTEXT.md D-09):

- ✅ Per-export JSDoc one-liners (a single `/** description */` block above each `export`).
- ❌ Top-of-file `/** ... */` block rewrites (Phase 27.4.3 D-03 historical citations are PRESERVED).
- ❌ Inline-comment audits within function bodies.

D-10 AUDIT METHOD (verbatim from CONTEXT.md D-10):

> Reviewer / executor reads each line aloud against the function body and asks "is this true today?" — if not, fix.

EXISTING-GOOD examples (DO NOT REWRITE these; they are the bar):

`server/lib/llmExtractionPipeline.ts:196-201` (verbatim from 35-PATTERNS.md lines 211-217):

```typescript
/**
 * Kick off a new LLM extraction run if the cooldown / cold-cache / busy /
 * configured / raw-events guards permit. The actual work runs as a
 * fire-and-forget IIFE; this function returns synchronously after the
 * dispatch decision is made.
 */
export async function runRefreshExtraction(opts: RunRefreshOpts): Promise<RunRefreshResult> {
```

`server/lib/llmExtractorWatchdog.ts:54-67` (verbatim from 35-PATTERNS.md lines 351-365):

```typescript
/**
 * Wrap a batch promise with a hard-timeout + late-resolve guard.
 *
 * Contract: ...
 */
export async function withBatchWatchdog<T>(...)
```

`server/lib/llmResolver.ts:52-59` (verbatim from 35-PATTERNS.md lines 333-339):

```typescript
/**
 * Test-only: reset the module-level throttle timestamp so tests don't
 * accumulate wait time between invocations. Safe to call from test setup;
 * no-op in production.
 */
export function __resetThrottleForTests(): void {
```

RE-WRITES EXPECTED (per 35-PATTERNS.md "least-delta" findings):

- `llmEventExtractor.v3.ts`: After plan 35-02's deletion, any JSDoc referencing `writePartialCache` or two-key-discipline survives only as tombstones (which 35-02 already added). Re-audit for any missed references. Lightest delta expected: 0-2 fixes per existing CONTEXT prediction.
- `llmExtractionPipeline.ts`: Plan 35-02 already updated line 134 JSDoc. Re-audit for cascade-narrowing references (Phase 29) that may cite Cerebras/Groq adapters that no longer exist.
- `llmCircuitBreaker.ts`, `llmDLQ.ts`, `llmTokenBudget.ts`, `llmExtractorWatchdog.ts`: Per 35-PATTERNS.md "already partial / well-doc'd — minimal delta". Audit is mostly VERIFICATION (no rewrites expected).
- `llmResolver.ts`: 6-path resolver heavily commented at top-of-file (lines 1-11); per-export one-liners audit reveals minimal delta.

COMMIT CADENCE (Claude's Discretion 5):

- 1 commit per module = 7 commits total.
- Body of each commit names the module + counts exports audited.
- If a module audit yields ZERO rewrites (pure verification), still land a commit `docs(35): verify {module} JSDoc current (DOCS-INT-02, D-09)` so the audit trail is complete.
- Alternative permitted by D-25: combine modules with zero rewrites into a single "verification-only" commit if the planner judges 7 commits to be excessive. Recommendation: 7 commits — atomic-per-module makes future surgical revert easy.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Audit llmExtractionPipeline.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmExtractionPipeline.ts</files>
  <read_first>
    server/lib/llmExtractionPipeline.ts (full file — to identify every `export ...` declaration; verify each has a true-today JSDoc above it)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 211-220 — existing-good example)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Enumerate every `export function` / `export async function` / `export const` / `export class` / `export type` / `export interface` in `server/lib/llmExtractionPipeline.ts`. Use grep to get a fast index:

    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmExtractionPipeline.ts
    ```

    For each export, READ the function body (or const value, or type definition) and ask "does the JSDoc above match this body's behavior today?" Apply D-10 read-aloud rule.

    Common rewrite triggers:
    - JSDoc cites Cerebras/Groq cascade (Phase 29 deleted them per SIMPLIFY-04).
    - JSDoc cites v1/v2 extractor (Phase 29 deleted them per SIMPLIFY-06).
    - JSDoc cites partial-key write contract (Plan 35-02 retired the partial-key per SIMPLIFY-02; if Plan 35-02 missed any JSDoc that mentions it, fix here).
    - JSDoc cites SIMPLIFY-01 incremental-flush behavior (Phase 30 retired it).

    For each export WITHOUT a JSDoc block, ADD a one-liner per the existing-good examples (verbatim style from 35-PATTERNS.md line 333-339):
    ```typescript
    /** Brief description of what this export does today. */
    export const X = ...;
    ```
    OR for multi-line behavior:
    ```typescript
    /**
     * Brief description (1-3 lines).
     */
    export function X(...) { ... }
    ```

    For each export WITH a JSDoc block that's still accurate, LEAVE IT ALONE. Verification IS the audit; no edit needed.

    POST-EDIT VERIFICATION:
    - `npx tsc --noEmit` exits 0.
    - `grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmExtractionPipeline.ts | wc -l` returns the export count (5 ± minor; per 35-RESEARCH.md line 536).
    - For each export above the count line, the line immediately preceding it is either `*/` (end of JSDoc) or another export's body-end (acceptable for short consts).

    COMMIT:
    ```
    docs(35): audit llmExtractionPipeline.ts public-API JSDoc (DOCS-INT-02, D-09)

    Reviewed N exports against current behavior. M rewrites; remainder verified
    true-today. Top-of-file historical waymarkers preserved per D-09 scope.
    ```
    Where N = grepped export count and M = count of `--include='*.ts'` lines actually edited.

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmExtractionPipeline.ts | wc -l | awk '{print "exports:", $1}' && git log -1 --format=%s server/lib/llmExtractionPipeline.ts 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - File `server/lib/llmExtractionPipeline.ts` contains JSDoc comments above each export (verifiable by grep — at minimum one `/**` precedes each `^export`).
    - `git log -1 --format=%s -- server/lib/llmExtractionPipeline.ts` matches `docs(35):` with `D-09` in body.
    - Top-of-file lines 1-50 (the existing block) are unchanged from pre-audit state (verifiable via `git diff HEAD~1 -- server/lib/llmExtractionPipeline.ts | head -50`).
  </acceptance_criteria>
  <done>
    Per-export JSDoc one-liners verified or rewritten on `llmExtractionPipeline.ts`. Single atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit llmEventExtractor.v3.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmEventExtractor.v3.ts</files>
  <read_first>
    server/lib/llmEventExtractor.v3.ts (full file — post plan 35-02 deletion; partial-key references are now tombstones, not live code)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-11 — partial-key references handled in plan 35-02; this plan handles non-partial-key audit only)
  </read_first>
  <action>
    Same methodology as Task 1, applied to `llmEventExtractor.v3.ts`. Per CONTEXT D-11: partial-key references at `:13-14, :122-123, :260, :448, :475, :609` are handled by plan 35-02; this plan's audit operates against the POST-35-02 file.

    Enumerate exports:
    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmEventExtractor.v3.ts
    ```
    Expected ~13 exports per 35-RESEARCH.md line 536. Key ones: `processEventGroupsV3`, `EVENTS_LLM_V3_KEY`, `LLM_REDIS_TTL_SEC`, `EVENT_EXTRACTION_SCHEMA_V3`, `SYSTEM_PROMPT_V3`, supporting types (`EnrichedEventV3`, `LLMRunSummary`, etc.).

    Per D-09 / D-10: read each existing JSDoc against the current function body. If a line cites a now-deleted code path (Cerebras / Groq cascade, v1/v2 extractor, partial-key writer), rewrite to current behavior.

    Specific re-audit targets (35-PATTERNS.md "hot spots"):
    - JSDoc on `processEventGroupsV3`: confirm it describes the current (post-plan-35-02) single-terminal-write contract, not the prior two-key discipline.
    - JSDoc on `EVENTS_LLM_V3_KEY`: confirm it cites the terminal write end-of-run (now sole writer) — not the prior "primary write" framing.
    - JSDoc on `EVENT_EXTRACTION_SCHEMA_V3`: confirm it cites Phase 33 actorConfidence addition.

    Top-of-file lines 1-30 are HISTORICAL — DO NOT TOUCH per D-09 scope.

    COMMIT:
    ```
    docs(35): audit llmEventExtractor.v3.ts public-API JSDoc (DOCS-INT-02, D-09)

    Reviewed N exports against current (post-plan-35-02) behavior. M rewrites;
    remainder verified true-today. Partial-key JSDoc references already
    deleted by plan 35-02 (D-12); this audit operates on the trimmed file.
    ```

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmEventExtractor.v3.ts | wc -l | awk '{print "exports:", $1}' && test "$(grep -c 'writePartialCache' server/lib/llmEventExtractor.v3.ts)" -le 1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Module's public-export count matches expectations (~13 ± minor).
    - `grep -q "writePartialCache" server/lib/llmEventExtractor.v3.ts` returns AT MOST a tombstone reference (plan 35-02 already deleted live calls; if the audit introduces NEW references, audit failed).
    - Top-of-file historical lines preserved (`git diff HEAD~1 -- server/lib/llmEventExtractor.v3.ts | head -30` shows no edits before line ~50).
    - `git log -1 --format=%s -- server/lib/llmEventExtractor.v3.ts` matches `docs(35):` with `D-09` in body.
  </acceptance_criteria>
  <done>
    Per-export JSDoc current on `llmEventExtractor.v3.ts`. Single atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit llmResolver.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmResolver.ts</files>
  <read_first>
    server/lib/llmResolver.ts (full file — already well-documented per 35-PATTERNS.md; expect minimal delta)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 317-343 — existing style is the bar)
  </read_first>
  <action>
    Same methodology. Enumerate exports:
    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmResolver.ts
    ```
    Expected ~8 exports. Key ones: `resolveLocation`, `derivePrecision`, `__resetThrottleForTests`, supporting types.

    Per 35-PATTERNS.md: `llmResolver.ts` is already well-documented. Audit is mostly VERIFICATION; minimal-delta expected.

    Top-of-file lines 1-11 (`Phase 27.4 Layered Geocoding Resolver (D-22) ... Six paths ...`) are HISTORICAL — DO NOT TOUCH.

    COMMIT:
    ```
    docs(35): audit llmResolver.ts public-API JSDoc (DOCS-INT-02, D-09)

    Reviewed N exports against current behavior. M rewrites; remainder verified
    true-today. 6-path resolver top-of-file block preserved as historical waymarker.
    ```

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmResolver.ts | wc -l | awk '{print "exports:", $1}' && git log -1 --format=%s -- server/lib/llmResolver.ts 2>/dev/null | grep -q "docs(35):" </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Module's public-export count matches expectations (~8 ± minor).
    - Top-of-file lines 1-11 unchanged (verifiable via `git diff HEAD~1 -- server/lib/llmResolver.ts | head -15`).
    - `git log -1 --format=%s -- server/lib/llmResolver.ts` matches `docs(35):` with `D-09` in body.
  </acceptance_criteria>
  <done>
    Per-export JSDoc current on `llmResolver.ts`. Single atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 4: Audit llmCircuitBreaker.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmCircuitBreaker.ts</files>
  <read_first>
    server/lib/llmCircuitBreaker.ts (full file ~73 LOC; already well-documented per 35-PATTERNS.md)
  </read_first>
  <action>
    Same methodology. Enumerate exports:
    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmCircuitBreaker.ts
    ```
    Expected ~5 exports: `Provider` type, `isAvailable`, `record`, `shouldPauseNewEvents`, `getState`.

    Per 35-PATTERNS.md: well-documented; minimal delta expected. Audit is mostly VERIFICATION.

    Top-of-file lines 1-11 (existing block) preserved per D-09.

    COMMIT:
    ```
    docs(35): audit llmCircuitBreaker.ts public-API JSDoc (DOCS-INT-02, D-09)

    Reviewed N exports against current behavior. M rewrites; remainder verified
    true-today.
    ```

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmCircuitBreaker.ts | wc -l | awk '{print "exports:", $1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Module's public-export count matches expectations (~5 ± minor).
    - `git log -1 --format=%s -- server/lib/llmCircuitBreaker.ts` matches `docs(35):` with `D-09` in body.
  </acceptance_criteria>
  <done>
    Per-export JSDoc current on `llmCircuitBreaker.ts`. Single atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 5: Audit llmDLQ.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmDLQ.ts</files>
  <read_first>
    server/lib/llmDLQ.ts (full file ~106 LOC)
  </read_first>
  <action>
    Same methodology. Enumerate exports:
    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmDLQ.ts
    ```
    Expected ~4 exports: `DLQEntry` type, `enqueueDLQ`, `DLQ_KEY`, `__testing` symbol.

    Per 35-PATTERNS.md: well-documented; minimal delta expected.

    COMMIT:
    ```
    docs(35): audit llmDLQ.ts public-API JSDoc (DOCS-INT-02, D-09)

    Reviewed N exports against current behavior. M rewrites; remainder verified
    true-today.
    ```

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmDLQ.ts | wc -l | awk '{print "exports:", $1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Module's public-export count matches expectations (~4 ± minor).
    - `git log -1 --format=%s -- server/lib/llmDLQ.ts` matches `docs(35):` with `D-09` in body.
  </acceptance_criteria>
  <done>
    Per-export JSDoc current on `llmDLQ.ts`. Single atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 6: Audit llmTokenBudget.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmTokenBudget.ts</files>
  <read_first>
    server/lib/llmTokenBudget.ts (full file ~183 LOC)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 372-381 — example one-liners for token-budget exports)
  </read_first>
  <action>
    Same methodology. Enumerate exports:
    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmTokenBudget.ts
    ```
    Expected ~7 exports: `incrementTokenCounter`, `budgetState`, `DAILY_LIMITS`, `todayKey`, `incrDailyTokens`, `getDailyTokens`, supporting types.

    Example one-liners (verbatim from 35-PATTERNS.md lines 372-381) to apply WHERE MISSING:
    ```
    /** Daily token ceilings per provider (free tier). */
    export const DAILY_LIMITS: Record<Provider, number> = { ... };

    /** Compute today's UTC date-stamped key for the per-provider token counter. */
    export function todayKey(provider: Provider): string { ... }

    /** Atomically increment the daily token counter and refresh the 48h TTL. */
    export async function incrDailyTokens(provider: Provider, n: number): Promise<number> { ... }
    ```

    Per 35-PATTERNS.md: partially documented; modest delta expected.

    COMMIT:
    ```
    docs(35): audit llmTokenBudget.ts public-API JSDoc (DOCS-INT-02, D-09)

    Reviewed N exports against current behavior. M one-liners added/rewritten;
    remainder verified true-today.
    ```

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmTokenBudget.ts | wc -l | awk '{print "exports:", $1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Module's public-export count matches expectations (~7 ± minor).
    - `git log -1 --format=%s -- server/lib/llmTokenBudget.ts` matches `docs(35):` with `D-09` in body.
  </acceptance_criteria>
  <done>
    Per-export JSDoc current on `llmTokenBudget.ts`. Single atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 7: Audit llmExtractorWatchdog.ts public-API JSDoc (D-09, D-10)</name>
  <files>server/lib/llmExtractorWatchdog.ts</files>
  <read_first>
    server/lib/llmExtractorWatchdog.ts (full file ~117 LOC; already well-documented per 35-PATTERNS.md line 348)
  </read_first>
  <action>
    Same methodology. Enumerate exports:
    ```
    grep -nE "^export (async )?(function|const|class|type|interface) " server/lib/llmExtractorWatchdog.ts
    ```
    Expected ~2 exports: `withBatchWatchdog`, supporting types.

    Per 35-PATTERNS.md line 348: this module is ALREADY well-documented at lines 30-66 (verbatim block in 35-PATTERNS.md lines 351-365). Audit expectation: pure verification, ZERO rewrites.

    COMMIT (even if zero rewrites — completes the audit trail per Claude's Discretion 5):
    ```
    docs(35): verify llmExtractorWatchdog.ts public-API JSDoc current (DOCS-INT-02, D-09)

    Reviewed N exports against current behavior. Zero rewrites needed —
    `withBatchWatchdog` Contract JSDoc is true-today.
    ```

    POST-COMMIT (final task in plan 35-04):
    - Run `npx vitest run` full suite. MUST exit 0. If a JSDoc edit somehow broke a test, fix and amend.
    - Run `npx tsc --noEmit`. MUST exit 0.
    - Run `npx vitest run src/__tests__/lib/redis-registry.test.ts`. Drift gate from plan 35-01 still green.

  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10 && npx tsc --noEmit && npx vitest run src/__tests__/lib/redis-registry.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` full suite exits 0 (post-Wave-3 smoke).
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0 (drift gate still green).
    - `git log -1 --format=%s -- server/lib/llmExtractorWatchdog.ts` matches `docs(35):` with `D-09` in body.
    - Plan total: 7 commits across 7 modules (verifiable via `git log --since="plan-35-04 start" --oneline | grep -c "docs(35):" | awk '$1>=7 {exit 0} {exit 1}'`).
  </acceptance_criteria>
  <done>
    All 7 modules audited; commit-per-module discipline maintained. Wave 3 closes; plan 35-05 (TTL right-sizing) and plan 35-06 (phase close) are unblocked.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                         |
| ------------------------------------------ | ----------------------------------- |
| JSDoc comment edits → API security surface | Comment-only; no code logic touched |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                  | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-10   | Information Disclosure | JSDoc one-liner inadvertently documents an internal field's purpose that should be private | accept      | All audited fields are already `export`-ed — public-API by definition. JSDoc clarifies but doesn't reveal anything not already discoverable. |
| T-35-11   | Tampering              | JSDoc edit accidentally introduces a syntax error breaking compilation                     | mitigate    | Each task verifies `npx tsc --noEmit` after the edit. Per-task isolation means one bad commit can be surgically reverted.                    |
| T-35-SC   | Tampering              | No npm/pip/cargo installs                                                                  | n/a         | Pure JSDoc edit; package legitimacy gate not applicable.                                                                                     |

</threat_model>

<verification>
After each task commit: `npx tsc --noEmit` exits 0.
After Task 7 (plan close): `npx vitest run` full suite exits 0; drift gate from plan 35-01 still exits 0.
Visual review: `git log --oneline -10` shows 7 `docs(35):` commits each citing DOCS-INT-02 + D-09.
</verification>

<success_criteria>

1. 7 modules audited, 1 commit each (7 commits total).
2. Each commit's body cites D-09 + DOCS-INT-02.
3. Top-of-file historical waymarkers preserved across all 7 modules.
4. Partial-key JSDoc references confirmed absent in `llmEventExtractor.v3.ts` post-plan-35-02.
5. `npx tsc --noEmit` exits 0 after each task; `npx vitest run` full suite green after Task 7.
6. Drift gate from plan 35-01 still passes.
7. Covers DOCS-INT-02.
   </success_criteria>

<output>
Create `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-04-SUMMARY.md` when done. Include:
- Per-module: export count + rewrite count + verification-only count.
- 7 commit hashes + messages.
- Aggregate: ~44 exports audited (per 35-RESEARCH.md line 536); rewrite percentage.
- Confirmation that drift gate still passes.
</output>

---
phase: 35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu
plan: 02
type: execute
wave: 2
depends_on: ['35-01']
files_modified:
  - server/lib/llmEventExtractor.v3.ts
  - server/lib/llmExtractionPipeline.ts
  - server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts
  - server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts
  - server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts
  - server/__tests__/cache/redis-prefix.test.ts
  - scripts/peek-v3-partial.ts
  - scripts/snapshot-v3-redis.ts
  - scripts/clear-llm-cache-dev.ts
  - CLAUDE.md
autonomous: true
requirements: [SIMPLIFY-02]
tags: [simplify, partial-key, retirement, redis, cleanup]
must_haves:
  truths:
    - 'Production constant `EVENTS_LLM_V3_PARTIAL_KEY` and its export are deleted from `server/lib/llmEventExtractor.v3.ts`.'
    - 'The `writePartialCache` writer at `llmEventExtractor.v3.ts:475` is deleted, and its callsite at `:619` is removed.'
    - 'A SIMPLIFY-02 tombstone comment is left in place of the deleted const (mirroring the in-file SIMPLIFY-01 tombstone style at `llmExtractionPipeline.ts:93-97`).'
    - 'Three test files (`llmExtractionPipeline.{terminalShape,incrementalWrite,crossBoundary}.test.ts`) have their partial-key assertions dropped but their terminal-key assertions retained.'
    - 'Fixture line at `server/__tests__/cache/redis-prefix.test.ts:120-125` is updated to remove the partial-key from the variadic-del list.'
    - 'Three `scripts/` consumers handled per Pitfall 1: `peek-v3-partial.ts` deleted entirely; `snapshot-v3-redis.ts` scope-edited; `clear-llm-cache-dev.ts` partial-key entry removed.'
    - 'CLAUDE.md §Serverless Cache loses the `events:llm:v3:partial` bullet (D-12 / D-23).'
    - 'Production cleanup = natural TTL expiry within `LLM_REDIS_TTL_SEC` of deploy; no one-shot script (D-13).'
    - '`npx vitest run` exits 0 after the cleanup; `npx tsc --noEmit` exits 0.'
    - "`grep -rn 'events:llm:v3:partial' server/ src/ scripts/ --include='*.ts'` returns only historical comments + the tombstone (no live writers/readers)."
  artifacts:
    - path: 'server/lib/llmEventExtractor.v3.ts'
      provides: 'Sole runtime extractor; partial-key writer + const removed; SIMPLIFY-02 tombstone in place'
      contains: 'Phase 35 D-12 (SIMPLIFY-02): EVENTS_LLM_V3_PARTIAL_KEY'
    - path: 'CLAUDE.md'
      provides: '§Serverless Cache no longer lists partial-key'
      contains: 'events:llm:v3'
  key_links:
    - from: 'server/lib/llmEventExtractor.v3.ts'
      to: 'tombstone comment citing SIMPLIFY-02 / D-12 / ADR-0010 Phase 35 sub-block'
      via: 'in-file precedent at llmExtractionPipeline.ts:93-97 (SIMPLIFY-01 tombstone)'
      pattern: "Phase 35 D-12 \\(SIMPLIFY-02\\)"
---

<objective>
Retire `events:llm:v3:partial` from the codebase entirely. The partial-key was a Hobby-era 300s-budget mitigation (Phase 28.2.6 incremental-flush companion observability key). With Vercel Pro's 800s `maxDuration` (Phase 29 D-08), terminal-key writes reliably finish within budget, so the partial-key carries no live signal — env-gating would leave dead-code-on-a-switch (worse outcome than full deletion per CONTEXT.md D-12).

Purpose: Implement the SIMPLIFY-02 deletion across 6 production refs in `llmEventExtractor.v3.ts`, 2 supporting comments in `llmExtractionPipeline.ts`, 4 test files (3 spec drops + 1 fixture trim), 3 scripts/ files (Pitfall 1: 1 delete + 2 scope-edits), and 1 CLAUDE.md bullet removal. Maintain the smallest-blast-radius principle (Phase 32 D-13 / Phase 33 D-16 / D-18 invariant) — surgical deletion, single atomic-per-decision commit, terminal-key assertions preserved.

Output: 1 atomic commit (`chore(35): retire events:llm:v3:partial observability key (SIMPLIFY-02, D-12, D-13)`). Production cleanup occurs via natural TTL expiry; no migration script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-01-PLAN.md
@CLAUDE.md
@server/lib/llmExtractionPipeline.ts

<interfaces>
<!-- Exact file:line touchpoints from 35-RESEARCH.md §Grep Audit + 35-PATTERNS.md split-points. -->
<!-- Executor reads each file before editing; does not interpret references. -->

PRODUCTION CODE — `server/lib/llmEventExtractor.v3.ts`:

- Line 13-14: top-of-file comment block reference to the partial-key envelope (`* `events:llm:v3:partial`` mention in JSDoc). Delete the partial-key sentence; retain surrounding context.
- Line 122: `const EVENTS_LLM_V3_PARTIAL_KEY = 'events:llm:v3:partial';` — DELETE.
- Line 123: `export { EVENTS_LLM_V3_KEY, EVENTS_LLM_V3_PARTIAL_KEY };` — UPDATE to `export { EVENTS_LLM_V3_KEY };` (single-export form).
- Lines 122-123 REPLACED BY tombstone (verbatim from 35-PATTERNS.md lines 199-204):
  ```
  const EVENTS_LLM_V3_KEY = 'events:llm:v3';
  // Phase 35 D-12 (SIMPLIFY-02): EVENTS_LLM_V3_PARTIAL_KEY const +
  // writePartialCache writer retired. Hobby-era 300s-budget mitigation;
  // Pro 800s makes terminal-key writes reliably finish, so the partial-key
  // envelope carried no live signal. Production cleanup: natural TTL expiry
  // within LLM_REDIS_TTL_SEC (≈ 2.5h) of deploy. See ADR-0010 Phase 35 sub-block.
  export { EVENTS_LLM_V3_KEY };
  ```
- Line 260: JSDoc reference (`@see writePartialCache for the per-batch envelope writer`) — DELETE the line.
- Lines 446-480: the `writePartialCache` function definition — DELETE entirely. Replace with a tombstone-only comment: `// writePartialCache retired Phase 35 / SIMPLIFY-02 — see EVENTS_LLM_V3_KEY tombstone above.`
- Line 475: the `cacheSetSafe(EVENTS_LLM_V3_PARTIAL_KEY, payload, LLM_REDIS_TTL_SEC)` call — vanishes with the function body deletion above.
- Line 609 / area ~615-625: the callsite `await writePartialCache(...)` inside `processEventGroupsV3` — DELETE the call line. Adjacent comments referencing partial-key write semantics — UPDATE or delete to reflect terminal-only writes.

PRODUCTION CODE — `server/lib/llmExtractionPipeline.ts`:

- Line 95: existing comment already mentions partial-key is retired by Phase 35 (per 35-PATTERNS.md). After Task 2 of this plan deletes the production writer, this anticipatory comment can be SIMPLIFIED to reference the completed retirement. UPDATE the wording to past tense ("retired Phase 35 / SIMPLIFY-02 / D-12" instead of "will be retired").
- Line 134: JSDoc block currently says "Two-key discipline preserved (D-04 / D-11): writes entities to LLM_EVENTS_KEY_ACTIVE; the LLMCachePayload envelope continues to land on `events:llm:v3:partial` via writePartialCache (UNCHANGED — observability key, written by the v3 extractor only)." UPDATE to (verbatim from 35-PATTERNS.md lines 242-247):
  ```
  * Single-purpose: end-of-run terminal write of `ConflictEventEntity[]` to
  * the active LLM cache key (`events:llm:v3`). Sole writer of the terminal
  * key (Phase 35 D-12 / SIMPLIFY-02 retired the partial-key observability
  * envelope; the previous two-key discipline collapses to one-key discipline).
  ```

PRODUCTION CODE — `LLMCachePayload` interface disposition (per 35-RESEARCH.md Open Question 2):

- Interface defined at `server/lib/llmEventExtractor.v3.ts:269-274` (approximate; verify during execution). Sole consumer was `writePartialCache:462`. After D-12 deletes the writer, run `rg "LLMCachePayload" --type ts -g '!node_modules'`. If only test files / historical comments remain — retire the interface in this same commit (delete the interface, drop test references to its shape). If a non-test production consumer remains — KEEP the interface and add a JSDoc note "Retained post-Phase-35 for {consumer} caller".

TEST FILE — `server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts`:

- Lines 156-168: `processEventGroupsMock` simulates the partial-key writer. DELETE the partial-key cacheSetSpy calls.
- Lines 174-184: similar mock simulation. DELETE the partial-key calls.
- Lines 320-345: KEEP the terminal-key it() block ("two-key discipline: events:llm:v3 holds ConflictEventEntity[]"). Rename the surrounding describe block from "D-04/D-11 two-key discipline + Pitfall 8" to "Phase 35 D-12 one-key discipline + Pitfall 8" (per 35-PATTERNS.md line 277).
- Lines 347-361: DELETE the partial-key it() block ("two-key discipline: events:llm:v3:partial holds LLMCachePayload envelope") entirely.

TEST FILE — `server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts`:

- Lines 188, 191, 209: partial-key references. DELETE the partial-key `cacheSetSpy.mock.calls.filter` assertions. KEEP terminal-key assertions verbatim. Apply the same describe-block rename pattern from terminalShape.test.ts if applicable.

TEST FILE — `server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts`:

- Lines 178, 180, 202: same pattern — drop partial-key assertions, keep terminal-key. Same describe-block rename pattern if applicable.

TEST FILE — `server/__tests__/cache/redis-prefix.test.ts`:

- Lines 120-125 fixture (verbatim from 35-PATTERNS.md lines 295-310): BEFORE has 3-key del fixture `['events:llm:v3', 'events:llm:v3:partial', 'events:llm-pipeline-audit']`. AFTER has 2-key fixture without partial-key. Test still exercises variadic-key prefix code path with ≥2 args.

SCRIPTS — Pitfall 1 disposition (35-RESEARCH.md):

- `scripts/peek-v3-partial.ts` (the file as a whole): DELETE entirely via `git rm`. The file exists SOLELY to peek at the partial-key; with the key retired, the script is 100% dead. Note: 35-RESEARCH.md Assumption A2 flags this as `recommend planner confirm with operator before deletion`. Per ROADMAP success criterion + CONTEXT D-12 explicit guidance, the deletion proceeds without operator pause (the script is a dev-pass scratch helper per 35-RESEARCH.md Pitfall 1 disposition). If the executor finds the script wrapped in a `package.json` script or `.github/workflows/*.yml`, abort and prompt operator.
- `scripts/snapshot-v3-redis.ts`: SCOPE-EDIT. Lines 13, 53, 144, 157 reference the partial-key. REMOVE the partial-key entry from the schema, the parallel fetch, and the output object. Single edit to a multi-key snapshot tool; remaining keys (terminal + summary + DLQ etc.) snapshot fine.
- `scripts/clear-llm-cache-dev.ts`: Line 20 references `'events:llm:v3:partial'` in the delete-key list. REMOVE the string from the list. Leave `'events:llm:v2:partial'` in place (already-dead key from Phase 29; targeting it is harmless).

PRE-FLIGHT GREP (per 35-RESEARCH.md Open Question 3):

- Run `grep -rn "peek-v3-partial\|snapshot-v3-redis\|clear-llm-cache-dev" .github/ package.json` BEFORE deleting `peek-v3-partial.ts`. If anything wraps these scripts, the disposition shifts from delete-the-file to scope-edit-the-file. Most likely outcome: nothing wraps them.

CLAUDE.md:

- Find the bullet `**`events:llm:v3:partial`** — observability-only incremental write during cron run; ...`. DELETE the entire bullet (the line) per D-23 + D-12.

POST-DELETION SANITY GREP:

- `grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts'` should return only the tombstone comment in `llmEventExtractor.v3.ts` and the historical comment in `llmExtractionPipeline.ts` (if retained), plus any commit-message-or-archive references in `_archive/`. Zero live code references.
- `grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'` should return ZERO results (the constant is fully deleted, no historical comments use the symbol name).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight grep + LLMCachePayload disposition check</name>
  <files>
    (read-only task; no files modified — produces stdout for reasoning)
  </files>
  <read_first>
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md (lines 365-390 Pitfall 1; Open Questions 2 + 3 at lines 567-575)
  </read_first>
  <action>
    Run three pre-flight greps and reason about findings before any file edit. Output goes to your reasoning context for Task 2's edit decisions.

    GREP A — Are the 3 scripts wrapped anywhere?
    ```
    grep -rn "peek-v3-partial\|snapshot-v3-redis\|clear-llm-cache-dev" .github/ package.json 2>/dev/null
    ```
    Expected: ZERO matches in `.github/workflows/*.yml`; possibly matches in `package.json` `scripts` block (if so, scope-edit; abort delete-the-file path). Per 35-RESEARCH.md Assumption A2 + Open Q3, if any match in `.github/` is found, ABORT this plan and surface to operator.

    GREP B — Is `LLMCachePayload` referenced anywhere outside the partial-key code path?
    ```
    grep -rn "LLMCachePayload" server/ src/ --include='*.ts' 2>/dev/null
    ```
    Expected (per 35-RESEARCH.md Assumption A3 and Open Q2): refs in `server/lib/llmEventExtractor.v3.ts:269-274` (declaration + writePartialCache), plus possible test imports in `llmExtractionPipeline.{terminalShape,incrementalWrite,crossBoundary}.test.ts`. If NON-TEST refs exist outside the partial-key writer (e.g. a fetch in `llm-status.ts` or DevApiStatus client), the interface STAYS — Task 2 leaves it in place and adds a JSDoc note "Retained post-Phase-35 for {consumer}". If only writer + test refs exist, the interface is retired in Task 2 alongside the writer.

    GREP C — Final blast-radius confirmation:
    ```
    grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts' 2>/dev/null
    ```
    Verify the count matches expectations: 6 refs in `llmEventExtractor.v3.ts` + 2 in `llmExtractionPipeline.ts` + 4-6 in test files + 3-5 in scripts. Total ≈ 15-20. Numbers materially out of this range = scope drift, surface to operator.

    Record findings in a comment block in your reasoning. No file is modified by this task. No commit lands from this task. Findings feed into Task 2's edit decisions.

  </action>
  <verify>
    <automated>grep -rn "peek-v3-partial\|snapshot-v3-redis\|clear-llm-cache-dev" .github/ package.json 2>/dev/null | tee /tmp/35-02-task1-scripts-grep.log; grep -rn "LLMCachePayload" server/ src/ --include='*.ts' 2>/dev/null | tee /tmp/35-02-task1-payload-grep.log; grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts' 2>/dev/null | tee /tmp/35-02-task1-partial-grep.log; wc -l /tmp/35-02-task1-*.log</automated>
  </verify>
  <acceptance_criteria>
    - Grep A executes; no abort triggered (no `.github/workflows/*.yml` wraps any of the 3 scripts).
    - Grep B output captured; LLMCachePayload disposition decision documented in reasoning (retire-alongside-writer vs keep-with-JSDoc).
    - Grep C confirms partial-key refs are within the 15-20 expected range.
  </acceptance_criteria>
  <done>
    Pre-flight greps complete; disposition decisions for the 3 scripts + LLMCachePayload interface are made and recorded for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete EVENTS_LLM_V3_PARTIAL_KEY production code path + scripts + CLAUDE.md (D-12, D-13, D-23)</name>
  <files>
    server/lib/llmEventExtractor.v3.ts
    server/lib/llmExtractionPipeline.ts
    server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts
    server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts
    server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts
    server/__tests__/cache/redis-prefix.test.ts
    scripts/peek-v3-partial.ts
    scripts/snapshot-v3-redis.ts
    scripts/clear-llm-cache-dev.ts
    CLAUDE.md
  </files>
  <read_first>
    server/lib/llmEventExtractor.v3.ts (lines 1-150, 240-280, 435-480, 595-625 — all partial-key touchpoints)
    server/lib/llmExtractionPipeline.ts (lines 85-140 — SIMPLIFY-01 tombstone precedent + line 134 JSDoc)
    server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts (lines 1-50, 150-200, 315-365 — split points)
    server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts (lines 1-50, 180-215)
    server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts (lines 1-50, 170-210)
    server/__tests__/cache/redis-prefix.test.ts (lines 110-135)
    scripts/peek-v3-partial.ts (full file — confirms 100% dead)
    scripts/snapshot-v3-redis.ts (lines 1-30, 50-60, 140-165)
    scripts/clear-llm-cache-dev.ts (lines 15-30)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-PATTERNS.md (lines 168-313 — exact edit shapes)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md (D-12, D-13, D-23)
  </read_first>
  <behavior>
    - After Task 2: `grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'` returns 0 matches.
    - After Task 2: `grep -rn "writePartialCache" server/ src/ scripts/ --include='*.ts'` returns at most 1 match (the tombstone comment in `llmEventExtractor.v3.ts`).
    - After Task 2: `grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts'` returns only comment/tombstone refs (no executable code references).
    - After Task 2: `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` exits 0 (the terminal-key `it()` block still runs and passes).
    - After Task 2: `npx vitest run server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts server/__tests__/cache/redis-prefix.test.ts` exits 0.
    - After Task 2: `npx tsc --noEmit` exits 0 (no dangling imports of the deleted constant; if LLMCachePayload retired, no dangling type refs).
    - After Task 2: `npx vitest run src/__tests__/lib/redis-registry.test.ts` still exits 0 — the drift gate from plan 35-01 sees CLAUDE.md no longer documents partial-key AND the code no longer references it, so parity is preserved.
  </behavior>
  <action>
    Execute the deletion as a SINGLE atomic commit (D-12 / D-26). Use Edit (not Write) for surgical edits; use `git rm` for `peek-v3-partial.ts`.

    EDIT 1 — `server/lib/llmEventExtractor.v3.ts` lines 13-14: DELETE the partial-key sentence in the top-of-file JSDoc block. Use Edit; preserve surrounding context.

    EDIT 2 — `server/lib/llmEventExtractor.v3.ts` lines 122-123: REPLACE the const + export block with the SIMPLIFY-02 tombstone (verbatim from 35-PATTERNS.md lines 199-204):
    ```
    const EVENTS_LLM_V3_KEY = 'events:llm:v3';
    // Phase 35 D-12 (SIMPLIFY-02): EVENTS_LLM_V3_PARTIAL_KEY const +
    // writePartialCache writer retired. Hobby-era 300s-budget mitigation;
    // Pro 800s makes terminal-key writes reliably finish, so the partial-key
    // envelope carried no live signal. Production cleanup: natural TTL expiry
    // within LLM_REDIS_TTL_SEC (≈ 2.5h) of deploy. See ADR-0010 Phase 35 sub-block.
    export { EVENTS_LLM_V3_KEY };
    ```

    EDIT 3 — `server/lib/llmEventExtractor.v3.ts` line 260: DELETE the JSDoc reference line about writePartialCache.

    EDIT 4 — `server/lib/llmEventExtractor.v3.ts` lines 446-480: DELETE the entire `writePartialCache` function body. Replace with a one-line tombstone comment: `// writePartialCache retired Phase 35 / SIMPLIFY-02 — see EVENTS_LLM_V3_KEY tombstone above.`

    EDIT 5 — `server/lib/llmEventExtractor.v3.ts` line ~609 (and nearby): DELETE the `await writePartialCache(...)` callsite inside `processEventGroupsV3`. Verify with grep AFTER the edit: `grep -n "writePartialCache" server/lib/llmEventExtractor.v3.ts` should return at most 1 line (the tombstone comment).

    EDIT 6 — LLMCachePayload interface: Per Task 1's disposition decision:
    - If retire-alongside-writer: DELETE the `interface LLMCachePayload<T> { ... }` block (~lines 269-274) and remove its export. Update any test imports.
    - If keep-with-JSDoc: ADD a JSDoc note above the interface declaration: `/** Retained post-Phase-35 / SIMPLIFY-02 for {non-test consumer X}. The partial-key writer that produced this envelope is retired; the interface remains for {consumer reason}. */`

    EDIT 7 — `server/lib/llmExtractionPipeline.ts` line 95: UPDATE the anticipatory SIMPLIFY-02 comment to past tense (per 35-PATTERNS.md). Wording suggestion: `// Phase 35 D-12 (SIMPLIFY-02): events:llm:v3:partial observability key retired; the v3 extractor's writePartialCache writer was deleted in this phase. Terminal-key write at end of run is now the sole canonical shape.`

    EDIT 8 — `server/lib/llmExtractionPipeline.ts` line 134 JSDoc: REPLACE the two-key-discipline paragraph with the one-key-discipline rewrite (verbatim from 35-PATTERNS.md lines 242-247). See <interfaces> block above for exact text.

    EDIT 9 — `server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts`:
    - Lines 156-168, 174-184: DELETE the partial-key cacheSetSpy mock calls inside processEventGroupsMock.
    - Lines 347-361: DELETE the partial-key `it('two-key discipline: events:llm:v3:partial holds LLMCachePayload envelope', ...)` block entirely.
    - Line ~320: RENAME describe block from "D-04/D-11 two-key discipline + Pitfall 8" → "Phase 35 D-12 one-key discipline + Pitfall 8".
    - KEEP lines 321-345 (terminal-key it block) verbatim.

    EDIT 10 — `server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts`: DELETE partial-key `cacheSetSpy.mock.calls.filter` assertions at lines 188, 191, 209. KEEP terminal-key assertions verbatim. Update any in-file describe blocks that reference two-key discipline.

    EDIT 11 — `server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts`: DELETE partial-key assertions at lines 178, 180, 202. Same pattern as Edit 10.

    EDIT 12 — `server/__tests__/cache/redis-prefix.test.ts` lines 120-125: REPLACE the 3-key variadic-del fixture with the 2-key form (verbatim from 35-PATTERNS.md lines 305-310):
    ```
    await redis.del('events:llm:v3', 'events:llm-pipeline-audit');
    expect(calls[0].args).toEqual([
      'dev:events:llm:v3',
      'dev:events:llm-pipeline-audit',
    ]);
    ```

    EDIT 13 — `scripts/peek-v3-partial.ts`: DELETE the entire file. Use `git rm scripts/peek-v3-partial.ts` so git tracks the deletion. Verify Task 1 Pre-flight Grep A returned no `.github/` wraps before executing.

    EDIT 14 — `scripts/snapshot-v3-redis.ts` lines 13, 53, 144, 157: REMOVE the partial-key entries (schema + parallel fetch + output object). The snapshot tool still functions for remaining keys (terminal cache, summary, DLQ, etc.).

    EDIT 15 — `scripts/clear-llm-cache-dev.ts` line 20: REMOVE the `'events:llm:v3:partial'` string from the delete-key list. Keep `'events:llm:v2:partial'` in place (Phase 29 dead-key — harmless).

    EDIT 16 — `CLAUDE.md` §Serverless Cache: DELETE the bullet `- **`events:llm:v3:partial`** — observability-only incremental write during cron run; ...` entirely. (This completes the D-23 / Pitfall-3-4 refinements started in plan 35-01 Task 2 — partial-key removal was deliberately deferred to this plan.)

    POST-EDIT VERIFICATION (run before committing):
    - `npx tsc --noEmit` MUST exit 0. If non-zero, fix dangling type refs (likely a missed test import of `LLMCachePayload` or `EVENTS_LLM_V3_PARTIAL_KEY`).
    - `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts server/__tests__/cache/redis-prefix.test.ts` MUST exit 0.
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` MUST exit 0 (drift gate from plan 35-01 still passes — both CLAUDE.md and the code now lack partial-key).
    - `grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'` MUST return 0 matches.
    - `grep -rn "writePartialCache" server/ src/ scripts/ --include='*.ts'` MUST return at most 1 match (the tombstone in llmEventExtractor.v3.ts).
    - `grep -rn "events:llm:v3:partial" server/ src/ scripts/ --include='*.ts'` MUST return ONLY comment/tombstone references (no executable code).

    COMMIT (single atomic per D-12 / D-26):
    ```
    chore(35): retire events:llm:v3:partial observability key (SIMPLIFY-02, D-12, D-13)

    Hobby-era 300s-budget mitigation; Pro 800s makes terminal-key writes
    reliably finish, so the partial-key envelope carried no live signal.

    Touched:
    - server/lib/llmEventExtractor.v3.ts (const + writer + 3 JSDoc refs deleted; SIMPLIFY-02 tombstone added)
    - server/lib/llmExtractionPipeline.ts (line 95 comment past-tensed; line 134 JSDoc rewritten one-key-discipline)
    - server/__tests__/lib/llmExtractionPipeline.{terminalShape,incrementalWrite,crossBoundary}.test.ts (partial-key assertions dropped; terminal-key preserved)
    - server/__tests__/cache/redis-prefix.test.ts (3-key fixture → 2-key)
    - scripts/peek-v3-partial.ts (DELETED — Pitfall 1)
    - scripts/snapshot-v3-redis.ts (4-line scope-edit — Pitfall 1)
    - scripts/clear-llm-cache-dev.ts (single string removal — Pitfall 1)
    - CLAUDE.md §Serverless Cache (partial-key bullet removed; D-23 follow-on from plan 35-01)

    Production cleanup: natural TTL expiry within LLM_REDIS_TTL_SEC of deploy (D-13).
    Drift gate (plan 35-01) still passes; terminal-key tests still green.
    ```

  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts server/__tests__/cache/redis-prefix.test.ts src/__tests__/lib/redis-registry.test.ts && test "$(grep -rn 'EVENTS_LLM_V3_PARTIAL_KEY' server/ src/ scripts/ --include='*.ts' | wc -l | tr -d ' ')" = "0" && test ! -f scripts/peek-v3-partial.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts` exits 0.
    - `npx vitest run server/__tests__/cache/redis-prefix.test.ts` exits 0.
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0 (drift gate still green post-deletion).
    - `grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'` returns 0 matches.
    - `grep -rn "writePartialCache" server/ src/ scripts/ --include='*.ts'` returns ≤ 1 match (the tombstone in llmEventExtractor.v3.ts).
    - `test ! -f scripts/peek-v3-partial.ts` (file is deleted).
    - `grep -q "Phase 35 D-12 (SIMPLIFY-02)" server/lib/llmEventExtractor.v3.ts` exits 0 (tombstone present).
    - `grep -q "events:llm:v3:partial" CLAUDE.md` exits 1 (bullet removed).
    - `git log -1 --format=%s` matches `chore(35):` with `SIMPLIFY-02` and `D-12` in body.
  </acceptance_criteria>
  <done>
    `events:llm:v3:partial` retired everywhere — production code, supporting comments, 4 test files, 3 scripts, CLAUDE.md. Single atomic commit lands. Drift gate stays green. Production cleanup proceeds via natural TTL.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                     |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Code edit → tombstone comment         | Tombstone references ADR-0010 + SIMPLIFY-02 + D-12 by name (auditable provenance)                               |
| Production Redis → natural TTL expiry | Partial-key entries vanish without intervention within `LLM_REDIS_TTL_SEC` (≈ 2.5h) of deploy                   |
| Scripts → developer tooling           | `peek-v3-partial.ts` deletion is dev-only; no operator workflow depends on it (verified Task 1 pre-flight grep) |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                              |
| --------- | ----------------- | ---------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-05   | Denial of Service | partial-key deletion breaks DevApiStatus observability                       | accept      | DevApiStatus never read the partial-key in production (verified via 35-RESEARCH.md grep audit — readers column = NONE). No surface degraded.                                                                                 |
| T-35-06   | Tampering         | Deleted writer reintroduced by a future commit recreating the dead code path | mitigate    | Drift gate (plan 35-01) would catch a re-added literal `'events:llm:v3:partial'` reference because CLAUDE.md no longer documents it. The orphan-code-key it() block in `redis-registry.test.ts` fails on undocumented drift. |
| T-35-07   | Repudiation       | Future operator wonders "why was this deleted?"                              | mitigate    | SIMPLIFY-02 tombstone comment cites Phase 35 / D-12 / ADR-0010 sub-block; commit message body lists touched files + rationale.                                                                                               |
| T-35-SC   | Tampering         | No npm/pip/cargo installs                                                    | n/a         | Pure deletion plan; package legitimacy gate not applicable.                                                                                                                                                                  |

</threat_model>

<verification>
Targeted vitest (4 surface files): `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts server/__tests__/cache/redis-prefix.test.ts` exits 0.
Drift gate (registry parity): `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0.
Type safety: `npx tsc --noEmit` exits 0.
Full suite (post-commit smoke): `npx vitest run` exits 0.
Code parity grep: `grep -rn "EVENTS_LLM_V3_PARTIAL_KEY" server/ src/ scripts/ --include='*.ts'` returns 0.
File deletion: `test ! -f scripts/peek-v3-partial.ts`.
</verification>

<success_criteria>

1. Production code path for `events:llm:v3:partial` is fully deleted (const + export + writer + callsite + 3 JSDoc refs + 2 supporting comments).
2. 3 scripts handled (1 delete + 2 scope-edit) per Pitfall 1 disposition.
3. 4 test files updated (3 spec drops + 1 fixture trim), terminal-key assertions preserved.
4. CLAUDE.md §Serverless Cache no longer lists the partial-key.
5. Drift gate from plan 35-01 still exits 0 (CLAUDE.md ↔ redis-keys.md ↔ code parity unbroken).
6. Single atomic commit lands.
7. `npx vitest run` full suite green; `npx tsc --noEmit` green.
8. Covers SIMPLIFY-02.
   </success_criteria>

<output>
Create `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-02-SUMMARY.md` when done. Include:
- Pre-flight grep findings (script wraps; LLMCachePayload disposition; baseline partial-key count).
- Per-file edit count (LOC removed; LOC added for tombstone).
- LLMCachePayload disposition (retired vs retained, with rationale).
- Test count delta (assertions dropped vs preserved).
- Single atomic commit hash + message.
- Drift-gate post-commit status (green).
</output>

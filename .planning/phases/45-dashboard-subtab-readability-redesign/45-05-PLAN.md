---
phase: 45-dashboard-subtab-readability-redesign
plan: 05
type: execute
wave: 4
depends_on:
  - 45-03
  - 45-04
files_modified:
  - src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx
autonomous: true
requirements:
  - DASH-READ-03
  - DASH-READ-04
user_setup: []

must_haves:
  truths:
    - 'The 4 behavioral pinning suites (tabMerge, diagnosticBlocks, operatorActions, prune) stay green with zero source edits to those suites'
    - 'The WAI-ARIA tablist contract is byte-stable: the four role=tabpanel ids + aria-labelledby partners + role=tab roving tabIndex are unchanged pre/post'
    - 'The consolidated-layout snapshot is regenerated deliberately and the tablist/tabpanel subtree does NOT diff (only subtab-body content diffs)'
    - 'Zero new inline hex / rgba literals exist across all touched subtab + atom code'
  artifacts:
    - path: 'src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx'
      provides: 'Deliberately-regenerated consolidated-layout snapshot reflecting the intentional restyle'
  key_links:
    - from: 'DevApiStatus.tsx tabpanel containers'
      to: 'the 4 behavioral pinning suites'
      via: 'the role=tabpanel ids + roving tabIndex the suites assert remain byte-stable'
      pattern: 'role="tabpanel"|aria-labelledby'
  prohibitions:
    - 'No source edit to tabMerge / diagnosticBlocks / operatorActions / prune suites (they must pass unmodified)'
    - 'No edit to the tablist/tabpanel DOM to make a snapshot pass'
    - "The snapshot's tablist/tabpanel subtree must NOT diff; if it does, fix the source restyle (Plans 03/04), do not accept the snapshot"
---

<objective>
Verify the DASH-READ-04 hard behavioral freeze and close the no-inline-hex gate (DASH-READ-03). Run the four behavioral pinning suites unmodified and confirm green; assert the WAI-ARIA tablist contract is byte-stable; regenerate the consolidated-layout snapshot DELIBERATELY (it captures the intentional restyle) and review the diff line-by-line to confirm ONLY subtab-body content changed and the tablist/tabpanel subtree did NOT diff; run the file-wide inline-hex grep gate.

Purpose: DASH-READ-04 is the highest-risk requirement and the explicit gate for the whole phase — the redesign must break nothing behavioral. This is the final-wave verification that the restyle (Plans 03/04) stayed strictly inside the frozen tabpanel containers (CONTEXT D-08, Phase 44 D-12).
Output: a deliberately-regenerated consolidated-layout snapshot; documented confirmation that the 4 behavioral pins are green and the tablist subtree is byte-stable; a clean inline-hex grep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-CONTEXT.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 4 behavioral pinning suites unmodified and assert tablist byte-stability</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx (frozen behavioral pin — tab-merge semantics)
    - src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx (frozen behavioral pin — roving tabindex / tab ids / the existing 10-dot api-health-sparkline-* precedent, which is UNTOUCHED)
    - src/components/ui/__tests__/DevApiStatus.operatorActions.test.tsx (frozen behavioral pin — operator actions)
    - src/__tests__/components/DevApiStatus.prune.test.tsx (frozen behavioral pin — prune)
    - src/components/ui/DevApiStatus.tsx lines 940-980 (the four role="tabpanel" aria-labelledby="tab-{api-health,water,sites,events}" containers + the events render switch — confirm these are byte-identical to pre-Plan-03/04)
    - 45-UI-SPEC.md §Interaction & Accessibility Contract (FROZEN) + §Regression-Lock Contract assertion 1 (tablist DOM frozen)
  </read_first>
  <action>
    Run the four behavioral pinning suites WITHOUT modifying their source (DASH-READ-04, CONTEXT D-08). Confirm all four exit 0. Then verify the WAI-ARIA tablist contract is byte-stable: confirm the four `role="tabpanel" aria-labelledby="tab-{api-health,water,sites,events}"` containers, the `role="tab"` + roving `tabIndex` (active 0 / inactive -1) pattern, and all tab ids are unchanged from before Plans 03/04 (use git to diff the tablist/tabpanel region of DevApiStatus.tsx against the pre-restyle baseline — the tablist subtree must show ZERO diff; only subtab-body content may differ). If ANY behavioral suite fails or the tablist region diffs, STOP and route the fix back to Plan 03/04 source (do NOT edit the behavioral suites and do NOT edit the tablist DOM to force a pass).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx src/components/ui/__tests__/DevApiStatus.operatorActions.test.tsx src/__tests__/components/DevApiStatus.prune.test.tsx</automated>
  </verify>
  <done>All four behavioral pinning suites pass with zero source edits; the tablist/tabpanel DOM region shows zero git diff versus the pre-restyle baseline.</done>
  <acceptance_criteria>
    - `npx vitest run` over the four behavioral suites exits 0.
    - `git diff` (or `git log -p`) for the tablist/tabpanel region of DevApiStatus.tsx (the `role="tablist"` block, every `role="tab"` button, and the four `role="tabpanel"` containers) shows ZERO changed lines across the whole phase.
    - The four behavioral suite files are unmodified this plan: `git diff --stat` lists none of them under files_modified.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Deliberately regenerate the consolidated-layout snapshot + assert tablist subtree does not diff</name>
  <files>src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</files>
  <read_first>
    - src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx (the snapshot suite + its `.snap` companion — the structure being regenerated)
    - 45-UI-SPEC.md §Interaction & Accessibility Contract "Snapshot regenerated deliberately" + §Regression-Lock Contract (subtab-body diffs OK; tablist/tabpanel-attribute diffs FORBIDDEN) + CONTEXT D-08
  </read_first>
  <action>
    Regenerate the consolidated-layout snapshot DELIBERATELY (CONTEXT D-08 — it captures the look we intentionally redesigned). Run the snapshot suite with update mode, then REVIEW THE SNAPSHOT DIFF LINE-BY-LINE: confirm the diff shows ONLY subtab-body content changes (the restyled water/sites tables, the events trend wells, the re-toned headers) and that the tablist/tabpanel subtree (tab ids, `aria-labelledby` partners, `role` attributes, roving `tabIndex`) does NOT appear in the diff. If the tablist/tabpanel subtree diffs, the restyle leaked outside the panels — STOP and fix Plan 03/04 source rather than accepting the snapshot. Commit the regenerated snapshot only after the line-by-line review confirms the tablist subtree is unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</automated>
  </verify>
  <done>The consolidated-layout snapshot is regenerated and green; the snapshot diff shows subtab-body changes only; the tablist/tabpanel subtree is byte-identical in the new snapshot.</done>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx` exits 0 against the regenerated snapshot.
    - `git diff` of the `.snap` file shows changed lines ONLY within subtab-body regions; `grep`-confirm the diff contains no changes to lines bearing `role="tablist"`, `role="tab"`, `role="tabpanel"`, `aria-labelledby="tab-`, or `tabindex`/`tabIndex` attributes.
    - The SUMMARY records the line-by-line review confirmation (which subtab-body regions changed; tablist subtree unchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Close the no-inline-hex gate + full-phase test sweep</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - 45-UI-SPEC.md §Visual Hierarchy & Aesthetic Contract "Every color from @theme / colorBridge — no inline hex" + §Regression-Lock Contract assertion 8 (grep gate: zero new #hex / rgba in touched subtab + new-atom code)
    - CLAUDE.md §Color Tokens (the D-13 no-inline-hex contract this gate enforces)
    - src/components/ui/MetricRow.tsx, src/components/ui/Sparkline.tsx (the new atoms — must be hex-free)
  </read_first>
  <action>
    Run the DASH-READ-03 inline-hex grep gate across all phase-touched UI code: the two new atoms (`MetricRow.tsx`, `Sparkline.tsx`) and the restyled subtab sections of `DevApiStatus.tsx` (the WaterFiltersSection / SitesFiltersSection / EventsFiltersSectionV3 + new TrendBlock diff hunks). The gate filters comment lines before counting and asserts ZERO NEW `#[0-9a-fA-F]{3,6}` or `rgba(` matches introduced by this phase (pre-existing hex elsewhere in the 3,851-line file is out of scope — diff against the phase baseline). Then run the full phase test surface (the two atom suites + the water/sites + events render pins + the four behavioral pins + the snapshot + the server route/unit tests) to confirm the whole phase is green together. Document the grep result and the full-sweep result in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/MetricRow.test.tsx src/components/ui/__tests__/Sparkline.test.tsx src/__tests__/devApiStatus.test.tsx src/__tests__/DevApiStatusV3.test.tsx src/__tests__/devApiStatusEventsSection.test.tsx src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx server/routes/__tests__/operator-status.test.ts server/__tests__/lib/trendHistory.test.ts</automated>
  </verify>
  <done>The inline-hex grep gate reports zero NEW #hex/rgba matches in the touched atom + subtab code; the full-phase test sweep exits 0.</done>
  <acceptance_criteria>
    - `grep -rnE '#[0-9a-fA-F]{3,6}|rgba\(' src/components/ui/MetricRow.tsx src/components/ui/Sparkline.tsx | grep -vE '^\s*//|/\*|\*' | wc -l` returns 0.
    - A diff-scoped grep of the phase's DevApiStatus.tsx hunks (water/sites/events restyle) shows zero NEW `#hex` / `rgba(` literals introduced by this phase.
    - The full-phase vitest sweep above exits 0 (atoms + render pins + behavioral pins + snapshot + server route/unit tests all green together).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                         |
| ---------- | ----------------------------------------------------------------------------------- |
| (none new) | This is a verification-only plan over already-built presentational + server changes |

## STRIDE Threat Register

| Threat ID | Category    | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                   |
| --------- | ----------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-45-11   | Tampering   | WAI-ARIA tablist contract regression          | mitigate    | The four behavioral pins run unmodified + a git diff asserts the tablist/tabpanel subtree is byte-stable (Task 1); a tablist diff routes the fix back to source rather than mutating the contract |
| T-45-12   | Repudiation | snapshot accepts a hidden behavioral change   | mitigate    | The snapshot is regenerated DELIBERATELY but reviewed line-by-line with a forbidden-diff grep on tablist/tabpanel attributes (Task 2) — a behavioral leak cannot be silently snapshotted green    |
| T-45-13   | Tampering   | inline hex leaks past the D-13 color contract | mitigate    | The DASH-READ-03 grep gate (Task 3) asserts zero new #hex/rgba in the touched atom + subtab code, preserving the colorBridge single-source contract                                               |
| T-45-SC   | Tampering   | npm/pip/cargo installs                        | accept      | No package installs — verification uses existing vitest + grep + git; nothing new to vet                                                                                                          |

</threat_model>

<verification>
- The four behavioral pinning suites exit 0 unmodified.
- The tablist/tabpanel DOM region shows zero git diff across the whole phase.
- The consolidated-layout snapshot is regenerated; its diff is subtab-body-only (no tablist/tabpanel-attribute diff).
- The inline-hex grep gate returns 0 new matches in the atoms + touched subtab code.
- The full-phase vitest sweep exits 0.
</verification>

<success_criteria>
DASH-READ-04 is proven: nothing behavioral broke — the four behavioral pins are green unmodified, the WAI-ARIA tablist contract is byte-stable, and the consolidated-layout snapshot was regenerated deliberately with a subtab-body-only diff; DASH-READ-03's no-inline-hex contract holds across all phase-touched code.
</success_criteria>

<output>
Create `.planning/phases/45-dashboard-subtab-readability-redesign/45-05-SUMMARY.md` when done. Record the line-by-line snapshot-review confirmation and the inline-hex grep result.
</output>

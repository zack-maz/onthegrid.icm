---
phase: 45-dashboard-subtab-readability-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 45-01
  - 45-02
  - 45-03
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/__tests__/devApiStatusEventsSection.test.tsx
autonomous: true
requirements:
  - DASH-READ-02
  - DASH-READ-03
  - DASH-READ-05
user_setup: []

must_haves:
  truths:
    - 'Four trend sparklines render in the events subtab: 3 per-cron freshness (health/warm/refresh-events) + 1 dead-link count, sourced from the operator-status trendHistory ring'
    - 'Each sparkline sits in a labeled well with the current value as a 13px/600 primary metric beside it; the sparkline supports, never replaces, the point-in-time number'
    - 'Each sparkline self-hides below 2 ring points and renders the bare current value until the ring fills (degrade-open, no fabricated zeros)'
    - 'The dense Phase-44 events blocks carry the readability grammar (one primary metric per block, small labels, progressive disclosure following the FlightRecorder idiom)'
  artifacts:
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'Events subtab: 4 mounted <Sparkline> trend wells + readability grammar over the Phase-44 blocks'
      contains: 'Sparkline'
  key_links:
    - from: 'src/components/ui/DevApiStatus.tsx (EventsFiltersSectionV3 / a TrendBlock child)'
      to: 'src/components/ui/Sparkline.tsx'
      via: 'import { Sparkline } fed from opStatus.trendHistory series'
      pattern: 'import.*Sparkline'
    - from: 'src/components/ui/DevApiStatus.tsx'
      to: '/api/operator-status trendHistory field'
      via: 'the already-fetched opStatus.trendHistory threaded down (no new fetch)'
      pattern: 'trendHistory'
  prohibitions:
    - 'No new fetch / poll for the sparkline data — reuse the already-fetched opStatus.trendHistory'
    - 'No re-mount/unmount of any Phase-44 block; no change to Phase-44 data wiring (presence-gates, prune prop thread, DeadLinkBucketsBlock existence)'
    - 'No change to the tablist/tabpanel DOM; sparkline wells are normal in-panel elements'
    - 'No fabricated zeros for a partial/empty ring; no font-bold(700) in new code; no inline hex'
    - 'No reuse of the existing in-file Sparkline (:3228) — use the Plan-02 separate-file primitive'
---

<objective>
Apply the readability grammar to the dense Events subtab and mount the four DASH-READ-05 trend sparklines. The sparklines read from the Plan-01 `opStatus.trendHistory` ring (already fetched, Bearer-gated) and render via the Plan-02 `<Sparkline>` primitive: 3 per-cron freshness (health/warm/refresh-events) + 1 dead-link count (CONTEXT D-02), each in a labeled well with the current value as a 13px/600 primary metric beside it. Apply progressive disclosure (one primary metric per block, small labels, FlightRecorder run→call→detail idiom) to the Phase-44 blocks without re-mounting any of them.

Purpose: this is the trend half of the phase (DASH-READ-05 "catches slow-burn regressions") plus the DASH-READ-02 progressive-disclosure pass over the events subtab — the file's densest surface.
Output: a trend-sparkline well-set + readability-grammar restyle inside `EventsFiltersSectionV3`; the events-section render pins evolved in lockstep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-CONTEXT.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-UI-SPEC.md
@src/components/ui/Sparkline.tsx
@src/components/ui/MetricRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread trendHistory to the events subtab and build the 4-sparkline trend well-set</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx lines 1157-1240 (the local OperatorStatus interface incl. the Plan-01 optional trendHistory field + the fetchOpStatus 30s poll — the data is ALREADY fetched; thread it, do not re-fetch)
    - src/components/ui/DevApiStatus.tsx lines 963-978 (the events tabpanel render switch where EventsFiltersSectionV3 mounts with prune={eventsPrune}; the parallel place to thread the trend series or the whole opStatus.trendHistory)
    - src/components/ui/DevApiStatus.tsx lines 3677-3745 (EventsFiltersSectionV3 body + its block list — where a new TrendBlock / sparkline well-set is added)
    - src/components/ui/Sparkline.tsx (the Plan-02 primitive: points[]/threshold/thresholdDirection/semanticToken props)
    - src/components/ui/MetricRow.tsx (for the labeled well's current-value metric, if useful)
    - 45-UI-SPEC.md §Trend Sparklines Contract §"The four sparklines" + §"Sparkline placement & labeling" (the 4 series, the well layout: small uppercase label `DEAD LINKS · 30d` / `CRON · HEALTH` etc. above an `h-4` sparkline, current value as 13px/600 metric to the left; degrade-open self-hide < 2 points; missed-tick gap rendering is planner discretion)
  </read_first>
  <action>
    Add a trend-sparkline well-set to the events subtab (DASH-READ-05, CONTEXT D-02/D-04/D-05). Thread the already-fetched `opStatus.trendHistory` (Plan-01 optional field) down to `EventsFiltersSectionV3` as an optional prop (forward-compat: absent on older servers → block self-hides). Build a `TrendBlock` child rendering 4 labeled wells: derive each series from the ring entries — `cronAgeMs.health` / `cronAgeMs.warm` / `cronAgeMs['refresh-events']` (freshness, stale = age above a threshold) and `deadUrlCount` (count rising = above a threshold). Each well: small uppercase section label (`text-[9px] text-white/40 uppercase tracking-wider`, e.g. `CRON · HEALTH`, `DEAD LINKS · 30d`), the current value as a `text-[13px] font-semibold tabular-nums` primary metric to the left, and the `<Sparkline points={series} threshold=… thresholdDirection="above" semanticToken="var(--color-status-degraded)" />` to its right. Choose concrete degradation thresholds (planner discretion — e.g. cron freshness stale when age > schedule+grace; dead-link count threshold relative to the latest vs prior) and document them in the SUMMARY. Degrade-open: when `trendHistory` is null/absent → self-hide the block; when a series has < 2 points → the Sparkline returns null and the well shows only the bare current value (no fabricated zeros). Use `var(--color-...)` / Tailwind classes only; no inline hex; no font-bold. Stay inside the existing tabpanel.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx</automated>
  </verify>
  <done>The events subtab renders 4 trend wells fed from opStatus.trendHistory; each shows a 13px/600 current-value metric + a Sparkline; the block self-hides when trendHistory is absent; each sparkline self-hides below 2 points; no new fetch; no inline hex; no font-bold.</done>
  <acceptance_criteria>
    - `grep -nE "import.*Sparkline" src/components/ui/DevApiStatus.tsx` matches (the Plan-02 primitive is imported and used).
    - `grep -nE "trendHistory" src/components/ui/DevApiStatus.tsx` shows the prop thread into EventsFiltersSectionV3 (no new `fetch(` for trend data — verify the only operator-status fetch sites are the pre-existing two).
    - The four well labels render: `grep -nE "CRON · HEALTH|CRON · WARM|CRON · REFRESH|DEAD LINKS" src/components/ui/DevApiStatus.tsx` returns 4 matches.
    - `grep -nE 'text-\[13px\] font-semibold' src/components/ui/DevApiStatus.tsx` shows a current-value metric in the trend wells.
    - `npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply the readability grammar (progressive disclosure) to the Phase-44 events blocks</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx lines 3677-3760 (EventsFiltersSectionV3 + the full Phase-44 block list: RoutingTrace, Latency, RateLimitHeadroom, SchemaStrictFailure, ErrorTaxonomy, CostShadow, Prewarm/AdaptiveBatch/LineagePrefilter cells, DrillDownBlock, the 7 v2 blocks Waterfall/Histograms/CallLog/BudgetBars/Eval/Dlq/Suspect, FlightRecorderBlock, DeadLinkBucketsBlock — the presence-gates are LOAD-BEARING, do NOT change them)
    - src/components/ui/FlightRecorderBlock.tsx (the run→call→detail idiom + the muted-placeholder degrade-open `text-white/30 italic` pattern at :243-251 to match)
    - src/components/ui/DevApiStatus.tsx lines 3576-3652 (DeadLinkBucketsBlock — the existing block header/tabular-nums idiom and the "of N scanned" sampled-tally caveat to PRESERVE verbatim)
    - 45-UI-SPEC.md §Progressive Disclosure Contract (events row: per-block primary metric; existing FlightRecorder L1→L2→L3; disclosure density is planner discretion within the run→call→detail idiom) + §Typography (two-weight rule) + §Copywriting Contract (preserve the Phase-44 dead-link copy + "of N scanned" caveat verbatim)
  </read_first>
  <action>
    Apply the readability grammar to the Phase-44 events blocks (DASH-READ-02/03) WITHOUT re-mounting/unmounting any block and WITHOUT changing any presence-gate (the `llmStatus.stage !== 'idle'` / `callHistory?.length` / `tokenCounters && breakerState` gates are honest-signal load-bearing — preserve verbatim). Within each block's existing render: re-tone every `font-bold` (700) header to `font-semibold` (600) (two-weight rule); ensure each block presents one clear primary metric (e.g. DLQ depth / dead-URL count / run outcome) at the 13px/600 tier where a natural headline exists; collapse the heaviest detail behind the FlightRecorder disclosure idiom where it currently dumps raw rows (planner discretion on density — collapse aggressively for raw-dump blocks, leave the FlightRecorder's own L1→L2→L3 as-is). Preserve the `DeadLinkBucketsBlock` "of N scanned" sampled-tally caveat and the authoritative `Dead URL events: {deadUrlCount}` line verbatim (copy contract). Re-tone empty states to the muted-placeholder `text-white/30 italic — no data ({reason})` style only where the block already uses a placeholder (do NOT convert a self-hiding block to a placeholder). No inline hex; no tablist DOM change.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx</automated>
  </verify>
  <done>The Phase-44 events blocks read with the readability grammar (two-weight headers, primary metric per block, raw dumps behind disclosure) with every presence-gate and the "of N scanned" caveat preserved verbatim; no block re-mounted; suites green.</done>
  <acceptance_criteria>
    - The Phase-44 presence-gates are byte-stable: `grep -nE "stage !== 'idle'|callHistory && llmStatus.callHistory.length > 0|tokenCounters && llmStatus.breakerState" src/components/ui/DevApiStatus.tsx` returns the same matches as before this plan (no gate edits).
    - The "of N scanned" caveat survives: `grep -n 'of {scannedTotal} scanned\|of .* scanned' src/components/ui/DevApiStatus.tsx` still matches in DeadLinkBucketsBlock.
    - `grep -nE 'font-bold' <EventsFiltersSectionV3 + its Phase-44 child block ranges>` returns 0 (re-toned).
    - `grep -rcE '#[0-9a-fA-F]{3,6}|rgba\(' <Task-2 diff hunks>` returns 0 new matches.
    - `npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Evolve the events-section render pins in lockstep</name>
  <files>src/__tests__/devApiStatusEventsSection.test.tsx</files>
  <read_first>
    - src/__tests__/devApiStatusEventsSection.test.tsx (the existing events-section render assertions — the Phase-44 block presence assertions + dead-link copy assertions that must stay green, and where the new trend-well + disclosure assertions slot in)
    - 45-UI-SPEC.md §Regression-Lock Contract assertion 6 (trend sparklines: render once ring ≥2 points, self-hide below; last-point tint swaps on threshold cross) + assertion 7 (degrade-open muted placeholder)
  </read_first>
  <action>
    Extend `devApiStatusEventsSection.test.tsx` (render pin, evolves in lockstep — NOT a frozen behavioral pin) with: (a) the 4 trend wells render with their labels when `opStatus.trendHistory` carries ≥2-point series; (b) the trend block self-hides (or shows bare current values) when trendHistory is absent/empty and each Sparkline self-hides below 2 points (degrade-open, no fabricated zeros); (c) the Phase-44 block presence-gate + "of N scanned" + authoritative dead-URL-count assertions stay intact (preserve verbatim — these are the honest-signal contract). Do NOT assert on the frozen behavioral suites here.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx</automated>
  </verify>
  <done>The events-section render pins assert the 4 trend wells + degrade-open self-hide, with the Phase-44 presence-gate + dead-link copy assertions preserved; suite exits 0.</done>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx` exits 0.
    - The suite contains a new assertion that 4 trend wells render given a ≥2-point trendHistory.
    - The suite contains a new assertion that the trend block degrades open (self-hides / bare value) when trendHistory is absent.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                               |
| ----------------------------------------- | ----------------------------------------------------------------------------------------- |
| operator (Bearer) → opStatus.trendHistory | The trend data rides the already-Bearer-gated /api/operator-status fetch; no new boundary |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                        |
| --------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-45-08   | Information Disclosure | trend series on the dashboard          | mitigate    | The series come from the existing Bearer-gated opStatus.trendHistory (Plan 01 added it inside the gated route); no new fetch, no ungated exposure (Task 1 reuses the existing fetch)                   |
| T-45-09   | Tampering              | Phase-44 block wiring / presence-gates | mitigate    | Restyle preserves every presence-gate + the "of N scanned" sampled-tally caveat verbatim (Task 2 acceptance greps the gates byte-stable); honest-signal contract intact                                |
| T-45-10   | Spoofing               | partial/empty ring rendered as healthy | mitigate    | Degrade-open: < 2 points → Sparkline null + bare value; null trendHistory → block self-hides; no fabricated zeros (Task 1/3 assert this) so a stalled cron reads as flatline/absent, not false-healthy |
| T-45-SC   | Tampering              | npm/pip/cargo installs                 | accept      | No package installs — uses existing React/Tailwind/vitest; nothing new to vet                                                                                                                          |

</threat_model>

<verification>
- `npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx` exits 0.
- The Phase-44 presence-gates + "of N scanned" caveat greps are byte-stable.
- No new `fetch(` call for trend data; the trend series reuse `opStatus.trendHistory`.
- `grep -rcE '#[0-9a-fA-F]{3,6}|rgba\(' <plan diff>` returns 0 new matches.
</verification>

<success_criteria>
The operator sees trends (4 sparklines: 3 per-cron freshness + 1 dead-link count) backed by the server ring, each beside its point-in-time value, degrade-open below 2 points; the dense Phase-44 events blocks read with the readability grammar with their honest-signal wiring fully preserved.
</success_criteria>

<output>
Create `.planning/phases/45-dashboard-subtab-readability-redesign/45-04-SUMMARY.md` when done. Record the chosen degradation thresholds for the D-05 last-point tint.
</output>

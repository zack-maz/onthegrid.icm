---
phase: 45-dashboard-subtab-readability-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 45-02
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/__tests__/devApiStatus.test.tsx
  - src/__tests__/DevApiStatusV3.test.tsx
autonomous: true
requirements:
  - DASH-READ-01
  - DASH-READ-02
  - DASH-READ-03
user_setup: []

must_haves:
  truths:
    - 'The water and sites subtab rejection string-dumps are replaced with labeled, right-aligned, tabular-nums Reason|Count tables'
    - 'Each of the water and sites subtabs shows exactly one primary metric (the kept %) at 13px/600, with detail tables behind progressive disclosure'
    - 'The heavy detail tables (per-country, rejection-by-type) default collapsed and expand on a disclosure toggle with aria-expanded flipping'
    - 'Every section header in the touched water/sites code is font-semibold (600), not font-bold (700)'
  artifacts:
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'Restyled WaterFiltersSection + SitesFiltersSection using MetricRow + progressive-disclosure drill-downs'
      contains: 'MetricRow'
  key_links:
    - from: 'src/components/ui/DevApiStatus.tsx (WaterFiltersSection/SitesFiltersSection)'
      to: 'src/components/ui/MetricRow.tsx'
      via: 'import { MetricRow } and use for every numeric row'
      pattern: 'import.*MetricRow'
  prohibitions:
    - 'No change to the tablist/tabpanel DOM: tab ids, aria-labelledby partners, role attributes, roving tabIndex stay byte-stable'
    - 'No font-bold (700) survives in the touched WaterFiltersSection / SitesFiltersSection code'
    - 'No inline hex / rgba literals introduced in the touched subtab code'
    - 'No reword of the verbatim provenance + raw→kept summary strings the pinning tests assert'
    - 'No edit to the Events subtab (EventsFiltersSectionV3) in this plan — Plan 04 owns it'
---

<objective>
Restyle the Water and Sites operator subtabs inside `DevApiStatus.tsx` for readability (DASH-READ-01/02/03): replace the `excl=… nn=… dup=…` rejection string-dumps with labeled right-aligned `tabular-nums` Reason|Count tables built from `<MetricRow>`, promote the kept % to a single 13px/600 primary metric per subtab, move the heavy per-country / rejection-by-type detail behind progressive-disclosure drill-downs following the `FlightRecorderBlock` idiom, and re-tone every section header from `font-bold` (700) to `font-semibold` (600). All restyle lands INSIDE the existing `role="tabpanel"` containers.

Purpose: water/sites are the worst readability offenders (a left-flowing string of eight `key=value` rejection buckets). This is the DASH-READ-01 numeric-scannability + DASH-READ-02 progressive-disclosure core.
Output: restyled `WaterFiltersSection` + `SitesFiltersSection`; the water/sites render-pin tests evolved in lockstep with the intentional DOM change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-CONTEXT.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-UI-SPEC.md
@src/components/ui/MetricRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle WaterFiltersSection — primary metric + MetricRow tables + rejection-table drill-down</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx lines 2333-2483 (the full WaterFiltersSection: provenance header, raw→kept summary :2385, per-type table :2389-2400, per-country table :2402-2423, the rejection string-dump :2425-2452, enrichment/overpass/score rows)
    - src/components/ui/MetricRow.tsx (the Plan-02 atom prop signature: label/value/unit/emphasized/data-testid)
    - src/components/ui/FlightRecorderBlock.tsx lines 280-340 (the disclosure idiom to replicate: clickable summary row `flex cursor-pointer items-center gap-2 rounded px-1 py-0.5 hover:bg-white/5` + `▸/▾` caret + inline L2 expansion `mt-1 ml-2 border-l border-white/10 pl-2`; local useState expansion, NOT uiStore)
    - 45-UI-SPEC.md §Progressive Disclosure Contract + §Numeric Scannability Contract + §Typography (the 13px/600 primary-metric tier, the {400,600} two-weight rule, the Reason|Count table transform)
  </read_first>
  <action>
    Restyle `WaterFiltersSection` (DASH-READ-01/02/03). Promote the kept % to a single primary metric using the hero idiom `text-[13px] font-semibold tabular-nums` (one per block, verbatim from DevApiStatus :1620) with the existing `N raw → M kept (P%)` summary line kept verbatim below it. Keep the provenance header line verbatim (pinning-test-asserted strings). Convert the per-type table rows to use `<MetricRow>` (right-aligned `tabular-nums` values). Replace the rejection string-dump (:2425-2452, the `excl=… turkey=… nn=… …` blocks AND the "Total rejections" summed line) with a labeled two-column `Reason | Count` table rendered via `<MetricRow>`, behind a progressive-disclosure toggle (collapsed by default; `aria-expanded` + `aria-controls`; local `useState`; `▸/▾` caret). Put the per-country table behind a sibling disclosure toggle (collapsed by default). The per-type table MAY default open. Re-tone every `font-bold` header in this section to `font-semibold` (the {400,600} two-weight rule). Re-tone the `loading filter stats…` empty branch to the muted-placeholder style `text-white/30 italic` keeping the word "loading". Stay INSIDE the existing tabpanel — add NO tablist/tab-id changes. No inline hex.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatus.test.tsx</automated>
  </verify>
  <done>WaterFiltersSection shows one 13px/600 kept-% primary metric; rejection data is a Reason|Count MetricRow table behind a collapsed disclosure with working aria-expanded; per-country behind a disclosure; headers are font-semibold; no inline hex; no tablist DOM change.</done>
  <acceptance_criteria>
    - `grep -nE 'font-bold' <WaterFiltersSection line range>` returns 0 (re-toned to font-semibold). Verify the range with `grep -n 'function WaterFiltersSection' src/components/ui/DevApiStatus.tsx` then bound to the next `function`.
    - `grep -nE 'text-\[13px\] font-semibold' src/components/ui/DevApiStatus.tsx` shows the water primary metric.
    - `grep -nE 'aria-expanded' src/components/ui/DevApiStatus.tsx` matches in the water section (disclosure toggle).
    - The rejection string-dump tokens are gone: `grep -nE 'excl=\{|nname=\{|nocity=\{' src/components/ui/DevApiStatus.tsx` returns 0 in the water section.
    - `npx vitest run src/__tests__/devApiStatus.test.tsx` exits 0 (extended in Task 3 for the moved strings).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restyle SitesFiltersSection — mirror the water treatment</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx lines 2507-2621 (the full SitesFiltersSection: provenance header, raw→kept :2552, per-type table :2556-2572, per-country :2574-2595, the 4-bucket rejection dump :2597-2603, overpass rows — note the deliberate water/sites asymmetry: sites has 4 rejection buckets, no per-type rejection split; do NOT invent placeholder buckets)
    - src/components/ui/MetricRow.tsx (atom prop signature)
    - The Task-1 restyled WaterFiltersSection in the same file (mirror its structure exactly — the two sections are parallel by design and must read visually identical)
  </read_first>
  <action>
    Restyle `SitesFiltersSection` mirroring the Task-1 water treatment exactly (DASH-READ-01/02/03). Kept % → single 13px/600 primary metric; `N raw → M kept (P%)` summary verbatim below; provenance header verbatim. Per-type table via `<MetricRow>`. Replace the 4-bucket rejection string-dump (:2597-2603) with a `Reason | Count` `<MetricRow>` table behind a collapsed disclosure (4 buckets only — turkey/no_coords/no_type/duplicate; honor the documented asymmetry, no invented buckets). Per-country behind a sibling disclosure. Re-tone every `font-bold` header to `font-semibold`. Re-tone the `loading filter stats…` branch to `text-white/30 italic`. Keep the two sections visually identical. Stay inside the existing tabpanel; no inline hex; no tablist DOM change.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatus.test.tsx</automated>
  </verify>
  <done>SitesFiltersSection mirrors the water restyle: one primary metric, MetricRow tables, 4-bucket Reason|Count rejection table behind a disclosure, font-semibold headers, no invented buckets, no inline hex, no tablist DOM change.</done>
  <acceptance_criteria>
    - `grep -nE 'font-bold' <SitesFiltersSection line range>` returns 0.
    - The sites rejection string-dump tokens are gone: `grep -nE 'nocoords=\{|notype=\{' src/components/ui/DevApiStatus.tsx` returns 0 in the sites section.
    - `grep -nE 'aria-expanded' src/components/ui/DevApiStatus.tsx` matches in the sites section.
    - No invented rejection buckets: the sites section still references only excluded_turkey/no_coords/no_type/duplicate (grep the four; assert no no_name/not_notable/low_score/no_city added to sites).
    - `npx vitest run src/__tests__/devApiStatus.test.tsx` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Evolve the water/sites render-pin tests in lockstep with the intentional DOM change</name>
  <files>src/__tests__/devApiStatus.test.tsx, src/__tests__/DevApiStatusV3.test.tsx</files>
  <read_first>
    - src/__tests__/devApiStatus.test.tsx (the existing water+sites render assertions — find every assertion that targets the old rejection string-dump format or the old font-bold header so they can be updated to the new Reason|Count table + disclosure structure)
    - src/__tests__/DevApiStatusV3.test.tsx (water/sites render pins that may reference the restyled rows)
    - 45-UI-SPEC.md §Regression-Lock Contract (render-contract assertions 2/3/4/5 the planner must code: numeric scannability tabular-nums + text-right; one primary metric per block; two weights only; progressive disclosure collapsed-by-default + aria-expanded flip)
  </read_first>
  <action>
    Update `devApiStatus.test.tsx` (and `DevApiStatusV3.test.tsx` where it asserts water/sites rows) to match the intentional new DOM (DASH-READ-01/02/03 — these are RENDER pins that evolve in lockstep with an intentional UI change, NOT the frozen behavioral pins). Replace assertions on the old `excl=…` string-dump with assertions on the new Reason|Count `<MetricRow>` table (right-aligned tabular-nums cells). Add: (a) one-primary-metric-per-block assertion (exactly one `text-[13px] font-semibold` headline in each of water + sites); (b) a two-weights assertion (no `font-bold` in the restyled sections); (c) a progressive-disclosure assertion (the rejection/per-country detail is collapsed by default and `aria-expanded` flips on toggle). Keep every verbatim provenance + raw→kept string assertion intact (do NOT reword asserted strings; extend the DOM-shape assertion only).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatus.test.tsx src/__tests__/DevApiStatusV3.test.tsx</automated>
  </verify>
  <done>The water/sites render pins assert the new Reason|Count table + single primary metric + two-weights + collapse/expand behavior, with the verbatim provenance/raw→kept strings preserved; suites exit 0.</done>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/devApiStatus.test.tsx src/__tests__/DevApiStatusV3.test.tsx` exits 0.
    - The suites contain a new assertion for `aria-expanded` toggling on a water/sites disclosure.
    - The suites contain a new assertion that exactly one `text-[13px] font-semibold` headline renders per subtab.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                   |
| ---------- | --------------------------------------------------------------------------------------------- |
| (none new) | Presentational restyle of already-fetched, already-Bearer-gated filterStats; no new data path |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                             |
| --------- | ---------------------- | ------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-45-06   | Tampering              | WAI-ARIA tablist contract | mitigate    | All restyle lands INSIDE the existing `role="tabpanel"` containers; no tab-id / aria-labelledby / roving-tabindex edits; the frozen behavioral pins (`tabMerge`, `diagnosticBlocks`, `operatorActions`) are NOT touched here and are re-verified in Plan 05 |
| T-45-07   | Information Disclosure | restyled detail tables    | accept      | Same filterStats data, reorganized; no new field surfaced; progressive disclosure only changes visibility affordance, not data scope                                                                                                                        |
| T-45-SC   | Tampering              | npm/pip/cargo installs    | accept      | No package installs — restyle uses existing React/Tailwind/vitest; nothing new to vet                                                                                                                                                                       |

</threat_model>

<verification>
- `npx vitest run src/__tests__/devApiStatus.test.tsx src/__tests__/DevApiStatusV3.test.tsx` exits 0.
- `grep -c 'font-bold' <water+sites section ranges>` returns 0.
- `grep -rcE '#[0-9a-fA-F]{3,6}|rgba\(' <water+sites diff hunks>` returns 0 new matches.
- The frozen behavioral suites are NOT modified in this plan (Plan 05 verifies them green).
</verification>

<success_criteria>
The water and sites subtabs are scannable: one primary metric each, MetricRow right-aligned tabular-nums tables, the rejection string-dumps replaced with Reason|Count tables behind progressive-disclosure drill-downs, headers re-toned to a two-weight hierarchy, all inside the frozen tabpanel DOM, with render pins evolved in lockstep.
</success_criteria>

<output>
Create `.planning/phases/45-dashboard-subtab-readability-redesign/45-03-SUMMARY.md` when done.
</output>

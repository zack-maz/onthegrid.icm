---
phase: 45-dashboard-subtab-readability-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/trendHistory.ts
  - server/routes/cron-health.ts
  - server/routes/operator-status.ts
  - server/openapi.yaml
  - server/routes/__tests__/operator-status.test.ts
  - server/__tests__/lib/trendHistory.test.ts
  - src/components/ui/DevApiStatus.tsx
autonomous: true
requirements:
  - DASH-READ-05
user_setup: []

must_haves:
  truths:
    - 'The existing 0 0 * * * health cron appends one daily sample to a bounded Redis ring (4 series: cron health/warm/refresh-events freshness + dead-link count)'
    - 'GET /api/operator-status returns a new optional trendHistory field carrying the four 30-point series, degrade-open (null on Redis failure)'
    - 'The history ring is bounded at 30 entries with a 30d TTL, copying the llm:runs:history LPUSH+LTRIM idiom exactly'
  artifacts:
    - path: 'server/lib/trendHistory.ts'
      provides: 'Bounded LPUSH+LTRIM 30-cap / 30d-TTL trend-ring writer + reader, degrade-open'
      contains: 'LPUSH'
    - path: 'server/__tests__/lib/trendHistory.test.ts'
      provides: 'Unit test pinning the 30-cap LTRIM bound, 30d TTL, and degrade-open behavior'
    - path: 'server/routes/__tests__/operator-status.test.ts'
      provides: 'Route test pinning the trendHistory field shape + degrade-open null'
  key_links:
    - from: 'server/routes/cron-health.ts'
      to: 'server/lib/trendHistory.ts'
      via: 'appendTrendSample call appended after cron:lastTick:health write'
      pattern: 'appendTrendSample'
    - from: 'server/routes/operator-status.ts'
      to: 'server/lib/trendHistory.ts'
      via: 'readTrendHistory call inside a degrade-open try/catch block'
      pattern: 'readTrendHistory'
  prohibitions:
    - 'No new cron entry added to vercel.json'
    - 'No new HTTP endpoint route registered'
    - 'No new event/liveness Redis key; no probe/sweep/prune behavior change'
    - 'No restyle of any subtab markup in this plan (the DevApiStatus.tsx edit is interface-only)'
---

<objective>
Build the server-backed bounded Redis history ring that backs the DASH-READ-05 trend sparklines, written once-daily by the EXISTING `/api/cron/health` handler and read through the EXISTING `/api/operator-status` aggregator. Add the `trendHistory` field to the aggregator response with full contract lockstep (route test + OpenAPI + client interface).

Purpose: DASH-READ-05 demands trends that "catch slow-burn regressions" — a client-ephemeral ring resets on reload and under-delivers. CONTEXT D-01 locks a server-backed ring reusing the `llm:runs:history` LPUSH+LTRIM idiom, with NO new cron, NO new endpoint, NO pipeline/probe behavior change.
Output: `server/lib/trendHistory.ts` (writer+reader), a once-daily append in `cron-health.ts`, a `trendHistory` field on `/api/operator-status`, OpenAPI schema, route test, unit test, and the forward-compat client interface field in `DevApiStatus.tsx` (interface only — no render, no restyle).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-CONTEXT.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the bounded trend-history ring module + unit test</name>
  <files>server/lib/trendHistory.ts, server/__tests__/lib/trendHistory.test.ts</files>
  <read_first>
    - server/lib/llmRunHistory.ts (the EXACT idiom to copy: RUNS_KEY/RUNS_MAX=200/RUNS_TTL_SEC; pushRecord = lpush → ltrim(0, MAX-1) → expire(TTL); parseEntry dual-shape string|object; listRunHistory dedupe; degrade-open try/catch that NEVER throws)
    - server/cache/redis.ts (redis instance + lpush/ltrim/expire/lrange surface)
    - server/lib/logger.ts (logger.child module-scoped child logger pattern)
    - CLAUDE.md §Serverless Cache (bounded-ring key idioms, TTL tiers, the active-key registry the new key must be consistent with)
  </read_first>
  <behavior>
    - Test 1: appendTrendSample LPUSHes one JSON entry then LTRIMs to the 30 newest (cap 30) — assert ltrim called with (KEY, 0, 29).
    - Test 2: appendTrendSample sets a 30d TTL (30*24*3600 sec) via expire after every push.
    - Test 3: readTrendHistory returns parsed entries newest-first, parsing both raw-string and already-object Upstash REST shapes.
    - Test 4: degrade-open — a redis throw inside append is swallowed (no throw escapes); a redis throw inside read returns an empty/default series, never throws.
    - Test 5: a single sample entry carries the four scalar series fields plus a sampledAt ISO timestamp (shape pin).
  </behavior>
  <action>
    Create `server/lib/trendHistory.ts` mirroring `server/lib/llmRunHistory.ts` structure verbatim (DASH-READ-05, CONTEXT D-01/D-03). Export a `TREND_HISTORY_KEY` const (choose a name in the `llm:`/`events:` registry family that reads as "the same kind of thing" to a maintainer — e.g. `dashboard:trends:history` — and document it in the SUMMARY for the Phase 49 registry sweep), `TREND_MAX = 30`, `TREND_TTL_SEC = 30 * 24 * 3600`. Define a `TrendSample` type: `{ sampledAt: string (ISO8601); cronAgeMs: { health: number|null; warm: number|null; 'refresh-events': number|null }; deadUrlCount: number }`. Export `appendTrendSample(sample: TrendSample): Promise<void>` = lpush(JSON) → ltrim(0, TREND_MAX-1) → expire(TREND_TTL_SEC), all inside one try/catch that logs a warning and NEVER throws (copy `pushRecord`). Export `readTrendHistory(limit = TREND_MAX): Promise<TrendSample[]>` = lrange(0, limit-1) → parseEntry dual-shape → filter nulls; return [] on throw (copy `listRunHistory` minus the dedupe). Add a `parseEntry` copy. Create the unit test at `server/__tests__/lib/trendHistory.test.ts` mocking `../cache/redis.js` exactly as `llmRunHistory`'s sibling tests do; assert the five behaviors above.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/trendHistory.test.ts</automated>
  </verify>
  <done>trendHistory.ts exports appendTrendSample/readTrendHistory/TREND_HISTORY_KEY/TrendSample; LTRIM cap is 30 and TTL is 30d; degrade-open verified; unit test exits 0.</done>
  <acceptance_criteria>
    - `npx vitest run server/__tests__/lib/trendHistory.test.ts` exits 0 with the five behavior tests green.
    - `grep -nE 'ltrim\([^,]+, 0, 29\)' server/lib/trendHistory.ts` returns a match (30-cap bound).
    - `grep -nE '30 \* 24 \* 3600' server/lib/trendHistory.ts` returns a match (30d TTL).
    - `grep -cE '#[0-9a-fA-F]{3,6}' server/lib/trendHistory.ts` returns 0 (no hex; server module, no color).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append the once-daily trend sample inside the existing health cron</name>
  <files>server/routes/cron-health.ts</files>
  <read_first>
    - server/routes/cron-health.ts (the existing handler; the `cacheSetSafe('cron:lastTick:health', Date.now(), ...)` write at the end, and the existing `sources`/`warnings` freshness pass)
    - server/routes/health.ts (probeCronTick at ~348 — the canonical `cron:lastTick:{name}` age reader; mirror its `cacheGetSafe<number>` + `typeof entry.data === 'number' ? entry.data : entry.lastFresh` age computation)
    - server/lib/healthSources.ts (CRON_LASTTICK_TTL_SEC + cron name set {health, warm, refresh-events})
    - server/lib/trendHistory.ts (the Task 1 appendTrendSample signature + TrendSample shape)
  </read_first>
  <action>
    In `cron-health.ts`, AFTER the existing `cacheSetSafe('cron:lastTick:health', Date.now(), ...)` write, compute a `TrendSample` and call `appendTrendSample` once (DASH-READ-05, CONTEXT D-01/D-02). Read the three `cron:lastTick:{health,warm,refresh-events}` keys via `cacheGetSafe<number>` and compute each `cronAgeMs` as `now - tickTs` (null when the key is absent — degrade-open, never fabricate 0; CONTEXT D-02 self-referential note: a stalled cron reads as null/stale, a valid signal). Read the `events:url-liveness-count` sidecar (the same key `operator-status.ts` reads as `URL_LIVENESS_COUNT_KEY`) for `deadUrlCount` with the same `Math.max(0, Number(raw) || 0)` defensive coercion. Wrap the whole append in a try/catch so a trend-write failure NEVER degrades the health response (mirror the existing eval/adversarial try/catch posture). Do NOT add a new cron, do NOT change the response JSON shape, do NOT touch the probe/freshness logic.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/routes/cron-lasttick.test.ts server/__tests__/lib/trendHistory.test.ts</automated>
  </verify>
  <done>The health cron appends exactly one trend sample per tick after the lastTick:health write; a trend-write throw does not change the health response; existing cron-lasttick tests stay green.</done>
  <acceptance_criteria>
    - `grep -n 'appendTrendSample' server/routes/cron-health.ts` returns exactly one call site.
    - `grep -c "vercel.json" server/routes/cron-health.ts` is 0 and no new cron entry is added anywhere (verify `git diff vercel.json` is empty for this plan).
    - `npx vitest run server/__tests__/routes/cron-lasttick.test.ts` exits 0 (health-tick semantics unchanged).
    - The append is inside a try/catch (grep shows `appendTrendSample` within a `try {` block in the same function).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Surface trendHistory on /api/operator-status with full contract lockstep</name>
  <files>server/routes/operator-status.ts, server/openapi.yaml, server/routes/__tests__/operator-status.test.ts, src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - server/routes/operator-status.ts (the degrade-open block pattern — actorQuality/tokenBudget: a per-block try/catch leaving the field null on throw; the final `res.json({ audit24h, byBearer, advEval, prune, actorQuality, tokenBudget })` line)
    - server/routes/__tests__/operator-status.test.ts (existing route-test idiom: how prune/tokenBudget fields are asserted + the degrade-open null assertion pattern)
    - server/openapi.yaml lines 606-710 (the /api/operator-status response schema where deadUrlCount/countsByStatus/tokenBudget live — the lockstep target)
    - server/lib/trendHistory.ts (readTrendHistory + TrendSample shape)
    - src/components/ui/DevApiStatus.tsx lines 1157-1212 (the local `OperatorStatus` interface — the forward-compat optional-field idiom for prune/actorQuality/tokenBudget)
  </read_first>
  <action>
    Server (`operator-status.ts`): add a `trendHistory` block mirroring the `tokenBudget` degrade-open precedent VERBATIM (CONTEXT D-01, Phase 44 D-04 lockstep). Declare `let trendHistory: TrendSample[] | null = null;`, wrap `trendHistory = await readTrendHistory();` in a per-block try/catch that logs a warning and leaves it null on throw, and add `trendHistory` to the final `res.json({...})`. OpenAPI (`server/openapi.yaml`): add the `trendHistory` property to the `/api/operator-status` response schema as a nullable array of the TrendSample object (sampledAt, cronAgeMs map, deadUrlCount), matching the surrounding nullable-field style; keep Redocly lint green. Route test: add a case pinning the `trendHistory` array shape on success AND a degrade-open case asserting `trendHistory === null` when the ring read throws (mirror the tokenBudget degrade-open test). Client (`DevApiStatus.tsx`): add `trendHistory?: TrendSample[] | null;` as an OPTIONAL forward-compat field to the local `OperatorStatus` interface (Phase 32 D-10 pattern) plus a `TrendSample` type mirror near the existing `PruneSummary` module type — interface/type ONLY, no render, no restyle (sparkline mount is Plan 04).
  </action>
  <verify>
    <automated>npx vitest run server/routes/__tests__/operator-status.test.ts && npx redocly lint server/openapi.yaml</automated>
  </verify>
  <done>/api/operator-status returns trendHistory (array on success, null on Redis failure); OpenAPI documents it and lints clean; route test pins both shapes; client interface carries the optional field with zero render change.</done>
  <acceptance_criteria>
    - `grep -n 'trendHistory' server/routes/operator-status.ts` shows the declaration, the readTrendHistory call inside a try/catch, and the res.json field.
    - `grep -n 'trendHistory' server/openapi.yaml` returns a match under the operator-status schema.
    - `npx vitest run server/routes/__tests__/operator-status.test.ts` exits 0 including the new success + degrade-open-null cases.
    - `npx redocly lint server/openapi.yaml` reports no errors.
    - `grep -n 'trendHistory' src/components/ui/DevApiStatus.tsx` shows ONLY the interface/type addition (no JSX/render usage in this plan).
    - `npx tsc --noEmit` (or `npm run build` typecheck step) passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                  |
| ---------------------------------------- | ---------------------------------------------------------------------------- |
| cron handler → Redis                     | The health cron writes the new trend ring; runaway growth is the memory risk |
| operator (Bearer) → /api/operator-status | Already Bearer-gated read; the new field must ride the same gate             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                 |
| --------- | ---------------------- | ----------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-45-01   | Denial of Service      | new `dashboard:trends:history` Redis ring | mitigate    | Bounded ring: `LTRIM 0, 29` cap 30 + 30d TTL copied verbatim from `llmRunHistory`; once-daily append (not per-request); unit test pins the cap + TTL (Task 1)                                                                                   |
| T-45-02   | Information Disclosure | `trendHistory` field on the aggregator    | mitigate    | Field is added INSIDE the existing `dashboardAuth`-gated `/api/operator-status` handler — it rides the same Bearer gate; route test asserts the field is only returned through the gated route (Task 3); the data is non-PII operational counts |
| T-45-03   | Tampering              | cron-health behavior change               | accept      | The append is additive and try/caught — it cannot alter the probe/freshness/eval response or the prune/sweep behavior; `cron-lasttick.test.ts` stays green (Task 2) confirms no behavioral drift                                                |
| T-45-SC   | Tampering              | npm/pip/cargo installs                    | accept      | No package installs in this plan — reuses existing `@upstash/redis`, vitest, redocly; nothing new to vet                                                                                                                                        |

</threat_model>

<verification>
- `npx vitest run server/__tests__/lib/trendHistory.test.ts server/routes/__tests__/operator-status.test.ts server/__tests__/routes/cron-lasttick.test.ts` all exit 0.
- `npx redocly lint server/openapi.yaml` clean.
- `git diff vercel.json` is empty (no new cron).
- No new Redis event/liveness key, no new endpoint, no probe/sweep/prune behavior change.
</verification>

<success_criteria>
The four trend series are backed by a bounded server Redis ring written once-daily by the existing health cron and readable through the existing Bearer-gated `/api/operator-status` aggregator, with the field documented, tested, degrade-open, and forward-compat on the client — and zero new cron / endpoint / event key / behavior change.
</success_criteria>

<output>
Create `.planning/phases/45-dashboard-subtab-readability-redesign/45-01-SUMMARY.md` when done. Record the exact chosen `TREND_HISTORY_KEY` name for the Phase 49 registry sweep.
</output>

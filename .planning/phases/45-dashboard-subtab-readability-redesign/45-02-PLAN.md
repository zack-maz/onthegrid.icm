---
phase: 45-dashboard-subtab-readability-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/MetricRow.tsx
  - src/components/ui/Sparkline.tsx
  - src/components/ui/__tests__/MetricRow.test.tsx
  - src/components/ui/__tests__/Sparkline.test.tsx
autonomous: true
requirements:
  - DASH-READ-01
  - DASH-READ-03
user_setup: []

must_haves:
  truths:
    - 'MetricRow renders a small uppercase label on the left and a right-aligned tabular-nums value on the right, usable across all three subtabs'
    - 'Sparkline renders an inline SVG polyline of ~30 points with a neutral muted stroke and a semantic last-point tint on threshold cross, self-hiding below 2 points'
    - 'Both atoms source every color through @theme tokens / Tailwind utilities — zero inline hex / rgba literals'
  artifacts:
    - path: 'src/components/ui/MetricRow.tsx'
      provides: 'DASH-READ-01 workhorse atom: tabular-nums right-aligned value + small uppercase label row'
      exports: ['MetricRow']
    - path: 'src/components/ui/Sparkline.tsx'
      provides: 'DASH-READ-05 line primitive: ~30-pt inline SVG polyline, neutral stroke + semantic last-point tint, null below 2 points'
      exports: ['Sparkline']
    - path: 'src/components/ui/__tests__/MetricRow.test.tsx'
      provides: 'Unit test pinning tabular-nums + right-alignment + label render'
    - path: 'src/components/ui/__tests__/Sparkline.test.tsx'
      provides: 'Unit test pinning point-count→path d, neutral stroke, last-point tint on threshold cross, null below 2 points'
  key_links:
    - from: 'src/components/ui/Sparkline.tsx'
      to: 'src/styles/app.css @theme / src/lib/colorBridge.ts'
      via: 'stroke color resolved through a Tailwind class or var(--color-*), never inline hex'
      pattern: "var\\(--color-|text-(white|blue|red)"
  prohibitions:
    - 'No edit to DevApiStatus.tsx in this plan (atoms are net-new files only)'
    - 'No reuse/modification of the existing in-file Sparkline (DevApiStatus.tsx:3228) or the 10-dot API-Health sparkline — this is a separate-file primitive'
    - 'No inline hex (#rgb/#rrggbb) or rgba() literals anywhere in the two atoms'
    - 'No third weight: only font-normal(400) / font-semibold(600) — no font-bold(700)'
---

<objective>
Extract the two genuinely-reused readability atoms into their own files with their own unit tests, per CONTEXT D-06/D-07: `MetricRow` (the DASH-READ-01 tabular-nums right-aligned-value row) and `Sparkline` (the DASH-READ-05 ~30-point inline-SVG line primitive with neutral stroke + semantic last-point tint). Both follow the `FlightRecorderBlock.tsx` / `BudgetBlock.tsx` separate-file precedent.

Purpose: building these as standalone, unit-tested primitives FIRST lets the heavy `DevApiStatus.tsx` restyle (Plans 03/04) compose them deterministically, keeps the atoms testable in isolation without routing through the heavy pinning suites, and shrinks the 3,851-line file.
Output: `MetricRow.tsx`, `Sparkline.tsx`, and their two unit tests — net-new files, zero edits to `DevApiStatus.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-CONTEXT.md
@.planning/phases/45-dashboard-subtab-readability-redesign/45-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the MetricRow atom + unit test</name>
  <files>src/components/ui/MetricRow.tsx, src/components/ui/__tests__/MetricRow.test.tsx</files>
  <read_first>
    - src/components/ui/BudgetBlock.tsx (separate-file block precedent + tabular-nums usage — the D-07 shape to follow for file structure, props, and exports)
    - src/components/ui/FlightRecorderBlock.tsx (tabular-nums + text-white opacity ramp idiom; the `text-[9px]`/`text-white/40`/`text-white/60` token usage to reuse verbatim)
    - 45-UI-SPEC.md §Numeric Scannability Contract (the `<MetricRow>` atom contract: small uppercase `text-[9px] text-white/40` label left, `text-right tabular-nums` value right, optional unit, optional emphasis text-white/60 → text-white/80)
    - src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx (RTL render-test idiom for this surface — render + screen.getByText/queryByTestId assertions, jsdom)
  </read_first>
  <behavior>
    - Test 1: renders the provided label text (small uppercase) and the provided value.
    - Test 2: the value cell carries the `tabular-nums` class.
    - Test 3: the value cell is right-aligned (`text-right`).
    - Test 4: optional `unit` renders adjacent to the value when provided, omitted when not.
    - Test 5: `emphasized` prop swaps the value tone from `text-white/60` to `text-white/80` (assert the class on the value node).
  </behavior>
  <action>
    Create `src/components/ui/MetricRow.tsx` exporting a `MetricRow` function component (DASH-READ-01, CONTEXT D-06/D-07). Props: `{ label: string; value: string | number; unit?: string; emphasized?: boolean; 'data-testid'?: string }`. Render a flex row: left = label in `text-[9px] uppercase tracking-wider text-white/40`; right = value in `text-right tabular-nums` with tone `text-white/60` (default) or `text-white/80` (when `emphasized`), with the optional `unit` appended as a muted `text-white/40` span. Use ONLY the white-opacity ramp + Tailwind utilities — no inline hex, no `font-bold`. Add `src/components/ui/__tests__/MetricRow.test.tsx` (RTL/jsdom) asserting the five behaviors via class/text queries.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/MetricRow.test.tsx</automated>
  </verify>
  <done>MetricRow exported; value cell is text-right + tabular-nums; label is small uppercase; emphasis toggles the tone; unit optional; unit test exits 0.</done>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/__tests__/MetricRow.test.tsx` exits 0 with all five tests green.
    - `grep -nE 'text-right' src/components/ui/MetricRow.tsx` AND `grep -nE 'tabular-nums' src/components/ui/MetricRow.tsx` both match.
    - `grep -cE '#[0-9a-fA-F]{3,6}|rgba\(' src/components/ui/MetricRow.tsx` returns 0.
    - `grep -c 'font-bold' src/components/ui/MetricRow.tsx` returns 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the Sparkline line primitive + unit test</name>
  <files>src/components/ui/Sparkline.tsx, src/components/ui/__tests__/Sparkline.test.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx lines 3224-3238 (the EXISTING in-file Sparkline — the SVG polyline + viewBox + preserveAspectRatio + `points.length < 2 return null` math to reuse as the geometry baseline; this primitive is a SEPARATE, richer file, NOT a reuse of that one)
    - 45-UI-SPEC.md §Trend Sparklines Contract (the new `<Sparkline>` contract: inline SVG polyline ~30 points; neutral muted stroke + semantic last-point tint on threshold cross; zero inline hex; self-hide < 2 points; own unit test on point count → path d, stroke token, last-point tint, null below 2 points)
    - src/lib/colorBridge.ts (the var(--color-*) / hex-export color path — the no-inline-hex enforcement; confirm available tokens e.g. --color-site-attacked, --color-status-degraded)
    - src/styles/app.css lines 3-65 (the @theme tokens available: --color-accent-*, --color-status-{healthy,degraded,warning}, --color-site-attacked — the legal palette for the stroke + last-point tint)
  </read_first>
  <behavior>
    - Test 1: given N≥2 points, the rendered `<polyline>` `points` attribute has exactly N coordinate pairs (point-count → path mapping).
    - Test 2: fewer than 2 points → component returns null (renders nothing).
    - Test 3: the line stroke is a single neutral muted token (assert the neutral stroke class/var on the polyline), not a per-point color.
    - Test 4: when the latest value crosses the supplied degradation threshold, the last point renders a distinct marker tinted with a semantic @theme token; below threshold it stays neutral.
    - Test 5: no inline hex / rgba — colors are var(--color-*) or Tailwind utility classes only.
  </behavior>
  <action>
    Create `src/components/ui/Sparkline.tsx` exporting a `Sparkline` function component (DASH-READ-05, CONTEXT D-04/D-05/D-07). Props: `{ points: number[]; threshold?: number; thresholdDirection?: 'above' | 'below'; semanticToken?: string; height?: string; 'data-testid'?: string }`. Geometry: copy the existing in-file Sparkline math (viewBox 0 0 100 100, preserveAspectRatio none, xStep = 100/(n-1), y = 100 - (v/max)*100); `points.length < 2 → return null` (degrade-open until the ring fills — CONTEXT discretion: render nothing below 2 points; the caller shows the bare current value). Stroke: a single NEUTRAL muted color — resolve via a Tailwind utility (e.g. `text-white/40` + `stroke="currentColor"`) so it is no-inline-hex. Last point: render a small `<circle>`/marker at the final coordinate; tint it to `semanticToken` (a `var(--color-...)` resolved value, default `var(--color-status-degraded)`) ONLY when the latest value crosses `threshold` per `thresholdDirection` (dead-link count rising = above; cron freshness stale = age above); otherwise neutral. NEVER inline hex. Add `src/components/ui/__tests__/Sparkline.test.tsx` asserting the five behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/Sparkline.test.tsx</automated>
  </verify>
  <done>Sparkline exported as a separate file; renders an N-point polyline with neutral stroke; null below 2 points; last point tints to a semantic @theme token on threshold cross; zero inline hex; unit test exits 0.</done>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/__tests__/Sparkline.test.tsx` exits 0 with all five tests green.
    - `grep -nE 'polyline' src/components/ui/Sparkline.tsx` matches (SVG line, not dots/bars).
    - `grep -nE 'points\.length < 2' src/components/ui/Sparkline.tsx` matches (self-hide gate).
    - `grep -cE '#[0-9a-fA-F]{3,6}|rgba\(' src/components/ui/Sparkline.tsx` returns 0 (no inline hex).
    - `grep -nE 'var\(--color-|currentColor' src/components/ui/Sparkline.tsx` matches (token-sourced color).
    - The existing in-file Sparkline is untouched: `git diff --stat src/components/ui/DevApiStatus.tsx` shows no change in this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                              |
| ---------- | -------------------------------------------------------------------------------------------------------- |
| (none new) | Presentational atoms render already-fetched, already-Bearer-gated data; they cross no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | ------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-45-04   | Tampering              | Sparkline color sourcing | mitigate    | Colors resolve through @theme/colorBridge tokens only; a grep gate (Task 2 acceptance) blocks inline hex, preserving the D-13 single-source color contract |
| T-45-05   | Information Disclosure | atoms render data        | accept      | Atoms are pure presentational components fed props by callers; they originate no fetch and add no new data surface                                         |
| T-45-SC   | Tampering              | npm/pip/cargo installs   | accept      | No package installs — atoms use existing React + Tailwind + vitest/jsdom; nothing new to vet                                                               |

</threat_model>

<verification>
- `npx vitest run src/components/ui/__tests__/MetricRow.test.tsx src/components/ui/__tests__/Sparkline.test.tsx` exits 0.
- `grep -rcE '#[0-9a-fA-F]{3,6}|rgba\(' src/components/ui/MetricRow.tsx src/components/ui/Sparkline.tsx` returns 0 for both.
- `git diff --stat src/components/ui/DevApiStatus.tsx` empty (no host-file edits).
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
Two standalone, unit-tested readability atoms exist (`MetricRow` for DASH-READ-01 numeric scannability; `Sparkline` for DASH-READ-05 trend lines), both no-inline-hex and two-weight compliant, ready to be composed by the Plan 03/04 restyle without touching DevApiStatus.tsx.
</success_criteria>

<output>
Create `.planning/phases/45-dashboard-subtab-readability-redesign/45-02-SUMMARY.md` when done. Record the final prop signatures of both atoms for the Plan 03/04 executors.
</output>

---
phase: 26.3-production-code-cleanup
plan: 06
type: execute
wave: 4
depends_on: ["26.3-04"]
files_modified:
  - vite.config.ts
  - package.json
autonomous: true
requirements: [CLN-08, CLN-11]

must_haves:
  truths:
    - "All it.todo() stubs are removed from the test suite"
    - "Every remaining test asserts real behavior (no empty or trivial tests)"
    - "@vitest/coverage-v8 is installed and configured with 80% line threshold"
    - "npx vitest run --coverage passes the configured thresholds"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vitest coverage configuration with V8 provider and 80% threshold"
      contains: "coverage"
    - path: "package.json"
      provides: "@vitest/coverage-v8 in devDependencies"
      contains: "@vitest/coverage-v8"
  key_links:
    - from: "vite.config.ts"
      to: "@vitest/coverage-v8"
      via: "coverage provider config"
      pattern: "provider.*v8"
---

<objective>
Install vitest coverage reporting, remove all it.todo() stubs, and ensure every test asserts real behavior.

Purpose: Three locked decisions from CONTEXT.md require implementation: (1) remove all it.todo() stubs that will not be implemented, (2) every remaining test must assert real behavior, (3) add vitest coverage reporting with 80% line coverage threshold visible in CI output. This is the test quality signal for a portfolio project.
Output: @vitest/coverage-v8 installed, coverage thresholds configured in vite.config.ts, all it.todo() stubs removed, test suite passes with coverage.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.3-production-code-cleanup/26.3-CONTEXT.md
@.planning/phases/26.3-production-code-cleanup/26.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vitest/coverage-v8 and configure coverage thresholds</name>
  <files>
    package.json,
    vite.config.ts
  </files>
  <action>
    1. Install coverage provider: `npm install -D @vitest/coverage-v8`

    2. Add coverage configuration to the `test` block in `vite.config.ts`:
       ```typescript
       coverage: {
         provider: 'v8',
         reporter: ['text', 'text-summary', 'lcov'],
         include: ['src/**/*.{ts,tsx}', 'server/**/*.ts'],
         exclude: [
           'src/test/**',
           'src/__tests__/**',
           'server/__tests__/**',
           '**/*.test.{ts,tsx}',
           '**/*.d.ts',
           'scripts/**',
         ],
         thresholds: {
           lines: 80,
           functions: 75,
           branches: 70,
           statements: 80,
         },
       },
       ```

    3. Run `npx vitest run --coverage` to measure baseline coverage. If coverage is below 80%, evaluate:
       - If baseline is 70-79%: keep 80% threshold (Plan 01 dead code removal + previous plans' new tests should push it over)
       - If baseline is 60-69%: set threshold to baseline + 5%, add TODO comment noting target is 80%
       - If baseline is below 60%: set threshold to baseline, add TODO comment -- the codebase has many UI/map components that are hard to unit test in jsdom

    4. Verify coverage report is visible in terminal output (`text` reporter).
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run --coverage 2>&1 | tail -20</automated>
  </verify>
  <done>@vitest/coverage-v8 installed. Coverage thresholds configured in vite.config.ts. Coverage report visible in test output.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all it.todo() stubs and audit test quality</name>
  <files>
    (discovered during grep -- all test files containing it.todo())
  </files>
  <action>
    1. Find all it.todo() stubs across the codebase:
       `grep -rn "it\.todo\|test\.todo" --include="*.test.*" --include="*.spec.*" src/ server/`

    2. For each it.todo() stub, evaluate:
       - **If the behavior is already tested elsewhere**: delete the stub entirely
       - **If the behavior is testable and important**: convert to a real test with assertions (use TDD red-green: write the assertion first, implement if needed)
       - **If the behavior requires WebGL/canvas/map context impossible in jsdom**: delete the stub (these are untestable in unit tests)
       - **If the behavior is trivial ("renders without crashing")**: delete the stub -- these add noise without value

    3. After removing stubs, verify no empty describe blocks remain:
       `grep -A2 "describe(" --include="*.test.*" src/ server/ -r | grep -B1 "^--$"` -- look for describe blocks with no tests inside

    4. Audit remaining tests for quality:
       - Search for tests that only assert truthiness without meaningful behavior:
         `grep -rn "expect(true)" --include="*.test.*" src/ server/`
         `grep -rn "toBeTruthy\(\)" --include="*.test.*" src/ server/`
       - If any exist that don't test real behavior, either add meaningful assertions or remove them

    5. Run full test suite to confirm nothing broke: `npx vitest run`
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && ! grep -rn "it\.todo\|test\.todo" --include="*.test.*" --include="*.spec.*" src/ server/ && npx vitest run</automated>
  </verify>
  <done>All it.todo() stubs removed from the codebase. Every remaining test asserts real behavior. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "it\.todo\|test\.todo" --include="*.test.*" src/ server/` returns nothing (zero stubs)
2. `npx vitest run --coverage` passes with configured thresholds
3. `grep "coverage" vite.config.ts` returns matches (config present)
4. `grep "@vitest/coverage-v8" package.json` returns a match (installed)
</verification>

<success_criteria>
- @vitest/coverage-v8 installed as devDependency
- Coverage thresholds configured in vite.config.ts (targeting 80% lines)
- Zero it.todo() stubs remain in the codebase
- Every remaining test asserts real behavior
- Full test suite passes with coverage reporting
</success_criteria>

<output>
After completion, create `.planning/phases/26.3-production-code-cleanup/26.3-06-SUMMARY.md`
</output>

---
phase: 26.3-production-code-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["26.3-01"]
files_modified:
  - server/lib/logger.ts
  - server/middleware/requestLogger.ts
  - server/index.ts
  - server/routes/flights.ts
  - server/routes/ships.ts
  - server/routes/events.ts
  - server/routes/news.ts
  - server/routes/markets.ts
  - server/routes/weather.ts
  - server/routes/sites.ts
  - server/routes/water.ts
  - server/routes/cron-health.ts
  - server/routes/cron-warm.ts
  - server/adapters/gdelt.ts
  - server/adapters/rss.ts
  - server/adapters/overpass.ts
  - server/adapters/overpass-water.ts
  - server/adapters/opensky.ts
  - server/adapters/adsb-exchange.ts
  - server/adapters/adsb-lol.ts
  - server/adapters/yahoo-finance.ts
  - server/adapters/open-meteo-precip.ts
  - server/adapters/acled.ts
  - server/middleware/errorHandler.ts
  - server/__tests__/lib/logger.test.ts
  - server/__tests__/middleware/requestLogger.test.ts
  - server/__tests__/middleware/requestId.test.ts
  - package.json
autonomous: true
requirements: [CLN-02, CLN-03]

must_haves:
  truths:
    - "All server log output is structured JSON via pino (no console.log/error in production code)"
    - "Every HTTP request gets a unique X-Request-ID header in the response"
    - "Request ID appears in all log entries for that request"
    - "Dev mode shows pretty-printed colored logs, production shows JSON"
  artifacts:
    - path: "server/lib/logger.ts"
      provides: "Pino logger instance with child logger factory and backward-compatible log() wrapper"
      exports: ["logger", "log"]
    - path: "server/index.ts"
      provides: "pino-http middleware wired in createApp() replacing requestLogger"
    - path: "server/__tests__/lib/logger.test.ts"
      provides: "Pino logger unit tests"
      min_lines: 30
    - path: "server/__tests__/middleware/requestId.test.ts"
      provides: "X-Request-ID tracing integration tests"
      min_lines: 20
  key_links:
    - from: "server/index.ts"
      to: "server/lib/logger.ts"
      via: "import logger for pino-http"
      pattern: "import.*logger.*from.*lib/logger"
    - from: "server/routes/*.ts"
      to: "server/lib/logger.ts"
      via: "import log or logger child"
      pattern: "import.*log.*from.*logger"
    - from: "server/middleware/errorHandler.ts"
      to: "pino-http req.log"
      via: "request-scoped logger"
      pattern: "req\\.log"
---

<objective>
Replace the custom log() wrapper with pino structured logger and add X-Request-ID tracing to all requests.

Purpose: Production-grade observability. Every log line is structured JSON with request context. Every request is traceable by a unique ID that appears in logs and the response header. This is the foundation that error handling (Plan 04) and all other middleware improvements depend on.
Output: Pino logger, pino-http middleware, X-Request-ID in all responses, all 22 callers migrated.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.3-production-code-cleanup/26.3-CONTEXT.md
@.planning/phases/26.3-production-code-cleanup/26.3-RESEARCH.md

<interfaces>
<!-- Current logger interface that 22 callers use -->
From server/lib/logger.ts:
```typescript
export interface LogEntry {
  level: 'info' | 'warn' | 'error';
  message: string;
  method?: string;
  path?: string;
  status?: number;
  durationMs?: number;
  cacheHit?: boolean;
  timestamp?: string;
}
export function log(entry: LogEntry): void;
```

From server/index.ts (middleware chain order):
```typescript
app.use(cors(...));
app.use(express.json());
app.use(helmet(...));
app.use(requestLogger);  // <-- Replace with pino-http
```

From server/middleware/requestLogger.ts:
```typescript
export function requestLogger(req: Request, res: Response, next: NextFunction): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pino stack and create structured logger with tests</name>
  <files>
    package.json,
    server/lib/logger.ts,
    server/__tests__/lib/logger.test.ts
  </files>
  <behavior>
    - Test 1: logger instance is a pino logger (has .info, .warn, .error, .child methods)
    - Test 2: log() backward-compatible wrapper calls pino with correct level
    - Test 3: log({ level: 'error', message: 'fail' }) routes through pino.error
    - Test 4: logger.child({ module: 'test' }) returns a child logger with module field
  </behavior>
  <action>
    1. Install dependencies: `npm install pino pino-http && npm install -D pino-pretty @types/pino-http`

    2. Rewrite `server/lib/logger.ts`:
       - Create pino instance with:
         - `level: process.env.LOG_LEVEL ?? 'info'`
         - Dev mode: `transport: { target: 'pino-pretty', options: { colorize: true, translateTime: 'SYS:standard' } }` (only when NODE_ENV !== 'production' AND NODE_ENV !== 'test')
         - Test mode: `level: 'silent'` to suppress log noise in test output
       - Export `logger` (the pino instance)
       - Export backward-compatible `log(entry: LogEntry)` function that delegates to `logger[entry.level](meta, entry.message)` where meta is all fields except level/message
       - Keep the `LogEntry` interface exported (callers still reference it)

    3. Create `server/__tests__/lib/logger.test.ts` with tests for the behavior above.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/lib/logger.test.ts -x</automated>
  </verify>
  <done>Pino logger installed and configured. Backward-compatible log() wrapper works. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pino-http middleware, migrate all 22 callers, and add request ID tracing tests</name>
  <files>
    server/index.ts,
    server/middleware/requestLogger.ts,
    server/middleware/errorHandler.ts,
    server/routes/flights.ts,
    server/routes/ships.ts,
    server/routes/events.ts,
    server/routes/news.ts,
    server/routes/markets.ts,
    server/routes/weather.ts,
    server/routes/sites.ts,
    server/routes/water.ts,
    server/routes/cron-health.ts,
    server/routes/cron-warm.ts,
    server/adapters/gdelt.ts,
    server/adapters/rss.ts,
    server/adapters/overpass.ts,
    server/adapters/overpass-water.ts,
    server/adapters/opensky.ts,
    server/adapters/adsb-exchange.ts,
    server/adapters/adsb-lol.ts,
    server/adapters/yahoo-finance.ts,
    server/adapters/open-meteo-precip.ts,
    server/adapters/acled.ts,
    server/__tests__/middleware/requestLogger.test.ts,
    server/__tests__/middleware/requestId.test.ts
  </files>
  <action>
    1. **Wire pino-http in server/index.ts:**
       - Import `pinoHttp` from 'pino-http' and `randomUUID` from 'node:crypto' and `logger` from './lib/logger.js'
       - Replace `app.use(requestLogger)` with:
         ```typescript
         app.use(pinoHttp({
           logger,
           genReqId: (req) => (req.headers['x-request-id'] as string) ?? randomUUID(),
           autoLogging: { ignore: (req) => req.url === '/health' },
           customSuccessMessage: (req, res) => `${req.method} ${req.url} ${res.statusCode}`,
           customErrorMessage: (req, _res, err) => `${req.method} ${req.url} failed: ${err.message}`,
         }));
         ```
       - Add X-Request-ID response header middleware right after pino-http:
         ```typescript
         app.use((req, res, next) => {
           res.setHeader('X-Request-ID', req.id);
           next();
         });
         ```
       - Remove the `requestLogger` import

    2. **Delete or gut server/middleware/requestLogger.ts:**
       - Replace the file content with a comment and re-export pointing to pino-http, OR delete entirely and update any remaining imports. Since pino-http handles request logging, this file is no longer needed. Delete it.

    3. **Migrate all 22 callers** from `import { log } from '../lib/logger.js'` to use pino child loggers:
       - For **route files** (10 files): Import `logger` from '../lib/logger.js', create a module-level child logger: `const log = logger.child({ module: 'flights' })`. Replace `log({ level: 'info', message: '...' })` calls with `log.info({ ...meta }, 'message')` pattern. Also use `req.log` (pino-http child) where a `req` object is in scope for request-scoped logging.
       - For **adapter files** (10 files): Same pattern with module-specific child: `const log = logger.child({ module: 'gdelt' })`. Replace all `log({ level, message, ...rest })` calls with `log[level](rest, message)`.
       - For **middleware/errorHandler.ts**: Use `req.log?.error({ err, statusCode }, 'request error')` for request-scoped error logging. This gives automatic request ID correlation.

    4. **Update/remove test for old requestLogger:**
       - Delete `server/__tests__/middleware/requestLogger.test.ts` since the middleware no longer exists.
       - If other tests mock `log` from logger.ts, update them to mock the new module (the backward-compatible `log` export still works, so most mocks should be fine).

    5. **Create `server/__tests__/middleware/requestId.test.ts`** to verify X-Request-ID tracing:
       - Test 1: Response includes X-Request-ID header (UUID format)
       - Test 2: Passing X-Request-ID in request header echoes it back in response
       - Test 3: Request ID appears in pino-http log output for that request
       Use supertest against createApp() to make real HTTP requests and assert headers.

    6. **Verify no console.log/console.error remains in server/ production code** (test files excluded):
       - `grep -rn "console\.\(log\|error\|warn\)" server/ --include="*.ts" --exclude-dir="__tests__"` should return nothing except possibly server/index.ts startup message (replace that with `logger.info({ port }, 'server listening')`).
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit -p tsconfig.server.json && npx vitest run server/__tests__/middleware/requestId.test.ts -x && npx vitest run server/ --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>pino-http wired as request logger. All 22 callers migrated to pino child loggers. X-Request-ID in every response with integration tests. No console.log in production server code. All server tests pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p tsconfig.server.json` exits 0
2. `npx vitest run server/` all pass
3. `grep -rn "console\.\(log\|error\|warn\)" server/ --include="*.ts" --exclude-dir="__tests__"` returns nothing
4. `grep -rn "requestLogger" server/ --include="*.ts" --exclude-dir="__tests__"` returns nothing (old middleware fully removed)
5. `grep -c "X-Request-ID" server/index.ts` returns at least 1
6. `npx vitest run server/__tests__/middleware/requestId.test.ts` passes
</verification>

<success_criteria>
- Pino structured logger is the sole logging mechanism for all server code
- Every HTTP response includes X-Request-ID header
- Request IDs appear in log output for request-scoped operations
- Request ID tracing has dedicated integration tests
- Dev mode shows pretty-printed logs, production shows raw JSON
- Old requestLogger middleware is deleted
- Zero console.log/error calls in server production code
</success_criteria>

<output>
After completion, create `.planning/phases/26.3-production-code-cleanup/26.3-02-SUMMARY.md`
</output>

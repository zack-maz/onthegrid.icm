---
phase: 28.2.6-fix-vercel-cron-architecture
verified: 2026-05-07T16:00:00Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Force-trigger the cron route against deployed prod URL and confirm events:llm:v3 populates within the 300s budget"
    expected: "POST/GET https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true returns 200 with {ok:true,dispatched:true} and within 300s the Redis key events:llm:v3 contains a non-empty ConflictEventEntity[] array"
    why_human: "Vercel Fluid Compute waitUntil behaviour and the 300s maxDuration ceiling can only be confirmed against a live Vercel deployment; local Vitest cannot simulate the Vercel runtime kill-on-res.end() signal"
  - test: "Confirm prod-connectivity-audit.yml tier-green gate flips to allTiersGreen=true after the cron runs once post-merge"
    expected: "Running prod-connectivity-audit.yml manual workflow shows allTiersGreen=true in the audit:connectivity:last-result Redis sidecar and the workflow exits 0"
    why_human: "Requires a live Vercel deployment, a populated events:llm:v3 cache from the cron run, and manual workflow trigger in GitHub Actions"
---

# Phase 28.2.6: Vercel Cron Architecture Fix — Verification Report

**Phase Goal:** Resolve the cron-route fire-and-forget architecture finding from Phase 28.2.5 closeout so `events:llm:v3` populates within Vercel Hobby's 300s `maxDuration` budget. Change A (Plan 01): incremental terminal-key write every N=10 batches. Change B (Plan 02): `safeWaitUntil` migration so the function instance survives past `res.end()` on Vercel Fluid Compute.

**Verified:** 2026-05-07T16:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `server/lib/safeWaitUntil.ts` exists and is the sole `@vercel/functions waitUntil` import site | ✓ VERIFIED | File exists at correct path with `export function safeWaitUntil`; `grep -r "from '@vercel/functions'" server/` returns exactly 1 file: `server/lib/safeWaitUntil.ts` |
| 2 | `mergeAndPersistLlmEntities` helper exists in `llmExtractionPipeline.ts` and is called from BOTH the periodic `onBatchComplete` flush AND the final write at end of run | ✓ VERIFIED | Helper defined at line ~118; called in the `onBatchComplete` callback (periodic flush, line ~439) and in the final-flush site (line ~568). Both call sites verified by direct file inspection. |
| 3 | `LLM_FLUSH_EVERY_N_BATCHES` env var registered in `server/config.ts` Zod schema with default 10 | ✓ VERIFIED | `z.coerce.number().int().positive().default(10)` at line 95 of config.ts; comment references D-03 rationale. |
| 4 | `onBatchComplete` signature widened to `void \| Promise<void>` in v1.ts/v2.ts/v3.ts and barrel, with `await` at every call site | ✓ VERIFIED | All four files (barrel, v1, v2, v3) show `=> void \| Promise<void>` signature. v1: 3 `await onBatchComplete?.(...)` call sites. v2: 3 `await onBatchComplete?.(...)` call sites. v3: 1 `await onBatchComplete?.(...)` at `finishBatch`. |
| 5 | Two-key discipline preserved: `events:llm:v3` writes `ConflictEventEntity[]`, `events:llm:v3:partial` writes envelopes — never mixed | ✓ VERIFIED | `mergeAndPersistLlmEntities` writes to `LLM_EVENTS_KEY_ACTIVE` (`events:llm:v3`) only `ConflictEventEntity[]`; `PARTIAL_KEY_ACTIVE` is read-only in the pipeline (for snapshot-the-array); `writePartialCache` remains the sole envelope writer inside the v3 extractor — unchanged. |
| 6 | `vercel.json` has `functions['api/vercel-entry.js'].maxDuration: 300` | ✓ VERIFIED | `vercel.json` line 15-19 shows `"functions": { "api/vercel-entry.js": { "maxDuration": 300 } }` |
| 7 | `api/vercel-entry.js` bundle was regenerated and contains `safeWaitUntil`, `mergeAndPersistLlmEntities`, and `LLM_FLUSH_EVERY_N_BATCHES` symbols | ✓ VERIFIED | `safeWaitUntil`: 6 occurrences; `mergeAndPersistLlmEntities`: 3 occurrences; `FLUSH_EVERY_N_BATCHES`: 4 occurrences; `@vercel/functions`: 1 occurrence. Bundle timestamp: 2026-05-07 15:45. |
| 8 | Wave-0 contract tests pass: incrementalWrite, crossBoundary, terminalShape, safeWaitUntil | ✓ VERIFIED | `npx vitest run` on all 4 test files: **13/13 tests pass**. No failures, no skips. |
| 9 | Pitfall 8 enforced: `runEval()` is NOT called inside the periodic flush helper — only after the final post-FINAL-geocode write | ✓ VERIFIED | `mergeAndPersistLlmEntities` has no `runEval` call; comment in helper doc explicitly states "Pitfall 8 — does NOT call runEval()". `runEval()` appears only at lines 512 and 544, both inside the v3 and v2 final-geocode branches after the full extraction completes. |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `server/lib/safeWaitUntil.ts` | Vercel-runtime-aware waitUntil shim with local-dev fallback; min 25 lines | ✓ VERIFIED | 91 lines. Contains `safeWaitUntil`, `Symbol.for('@vercel/request-context')`, `waitUntil` import, `@vercel/functions`. |
| `server/__tests__/lib/safeWaitUntil.test.ts` | 5 unit tests for Vercel-context invocation, local-dev fallback, no-throw on rejection | ✓ VERIFIED | 5 tests GREEN. Covers: Vercel-context delegation, local-dev fallback, no-throw on local rejection, no-throw when waitUntil throws, void return. |
| `server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` | D-03 cadence assertion | ✓ VERIFIED | Exists, substantive (cadence/FLUSH_EVERY patterns present), all 4 tests pass. |
| `server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts` | D-07 cross-function-boundary contract | ✓ VERIFIED | Exists, substantive (cross-function-boundary/two consecutive partial patterns), all tests pass. |
| `server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` | D-04/D-11 envelope-vs-entity guard | ✓ VERIFIED | Exists, substantive (two-key discipline/ConflictEventEntity patterns), all tests pass. |
| `package.json` | `@vercel/functions ^3.5.0` as runtime dependency | ✓ VERIFIED | `"@vercel/functions": "^3.5.0"` in `dependencies` block (not devDependencies), alphabetically between `@vercel/analytics` and `@vercel/speed-insights`. |
| `vercel.json` | Explicit maxDuration declaration | ✓ VERIFIED | `maxDuration: 300` present at `functions['api/vercel-entry.js']`. |
| `api/vercel-entry.js` | Regenerated bundle with Plan 01 + Plan 02 changes | ✓ VERIFIED | Timestamp 2026-05-07 15:45; all 3 required symbols present. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `llmExtractionPipeline.ts:270` (safeWaitUntil call) | `server/lib/safeWaitUntil.ts safeWaitUntil()` | `import { safeWaitUntil } from './safeWaitUntil.js'` | ✓ WIRED | Import at line 43; call at line 270 confirmed. |
| `server/lib/safeWaitUntil.ts safeWaitUntil()` | `@vercel/functions waitUntil` | `import { waitUntil } from '@vercel/functions'` | ✓ WIRED | Line 42 of safeWaitUntil.ts; sole import site in the server tree. |
| `safeWaitUntil.ts` | `globalThis[Symbol.for('@vercel/request-context')]` | Vercel runtime detection probe | ✓ WIRED | `const VERCEL_CTX = Symbol.for('@vercel/request-context')` at line 52; checked at line 64. |
| `llmExtractionPipeline.ts onBatchComplete callback` | `mergeAndPersistLlmEntities` | `batchesSinceLastFlush >= flushEvery` guard + helper call | ✓ WIRED | Closure-state counter `batchesSinceLastFlush` incremented at every callback invocation; fires helper when threshold reached; confirmed at lines ~372-461. |
| `mergeAndPersistLlmEntities` | `cacheSetSafe('events:llm:v3', ConflictEventEntity[], TTL)` | Shared helper wrapping prior L374-388 pattern | ✓ WIRED | `await cacheSetSafe(key, llmMerged, LLM_REDIS_TTL_SEC)` at line 131 of the helper; called from both periodic flush and final flush. |
| `llmEventExtractor.v3.ts finishBatch` | `await onBatchComplete?(c, totalBatches)` | Async await at line 575 | ✓ WIRED | `await onBatchComplete?.(c, totalBatches)` confirmed; serializes cadence counter through finishBatch's monotonic ++completedBatchesCounter. |

---

### Data-Flow Trace (Level 4)

Not applicable. This phase produces server-side infrastructure (cron pipeline helpers, Redis writes) rather than UI components rendering dynamic data. The data-flow is: cron trigger → IIFE via safeWaitUntil → periodic mergeAndPersistLlmEntities → cacheSetSafe → Redis `events:llm:v3`. The terminal Redis key write was confirmed wired in the key-link verification above and exercised by the Wave-0 contract tests.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 4 Wave-0 contract tests pass (cadence, crossBoundary, terminalShape, safeWaitUntil) | `npx vitest run server/__tests__/lib/safeWaitUntil.test.ts server/__tests__/lib/llmExtractionPipeline.*.test.ts` | 13/13 tests pass, 0 failures | ✓ PASS |
| Cron route HTTP contract preserved (returns 200 in <500ms) | `npx vitest run server/__tests__/routes/refresh-events-cron.test.ts` | 6/6 tests pass | ✓ PASS |
| `@vercel/functions` is sole import site | `grep -r "from '@vercel/functions'" server/` | 1 file: `server/lib/safeWaitUntil.ts` | ✓ PASS |
| Bundle regenerated with all symbols | `grep -c "safeWaitUntil\|mergeAndPersistLlmEntities\|FLUSH_EVERY" api/vercel-entry.js` | 6, 3, 4 occurrences respectively | ✓ PASS |
| Pitfall 8: runEval not in periodic flush | Search for `runEval` in `mergeAndPersistLlmEntities` body | Not present; only in final-geocode branches | ✓ PASS |

---

### Requirements Coverage

No REQUIREMENTS.md entries are locked for this phase (declared `requirements: []` in both PLAN frontmatter). Phase is derived from 28.2.5 closeout deferred-items. No orphaned requirement IDs to check.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Scan of `server/lib/safeWaitUntil.ts`, `server/lib/llmExtractionPipeline.ts` (modified sections), `server/__tests__/lib/safeWaitUntil.test.ts`, and the three Wave-0 test files found no TODO/FIXME, no stub return patterns, no empty implementations, no hardcoded empty arrays in non-test code. The periodic-flush try/catch is a correct "best-effort" pattern, not a suppressed bug.

---

### Human Verification Required

#### 1. Prod force-trigger cron run

**Test:** With a valid `DASHBOARD_PASSWORD` Bearer token, issue:
```
GET https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true
```
**Expected:** HTTP 200 response with `{ok: true, dispatched: true, schemaVersion: 'v3'}` arrives in under 500ms (fire-and-forget; IIFE kept alive by `waitUntil`). Within 300 seconds, the Redis key `events:llm:v3` should contain a non-empty `ConflictEventEntity[]`. Verify by hitting `/api/events` and confirming `{schemaVersion: 'v3', events: [...]}` with at least one event.

**Why human:** Vercel Fluid Compute's `waitUntil` semantics — specifically the promise keeping the function instance alive past `res.end()` within the 300s `maxDuration` — can only be validated against a live Vercel deployment. Local Vitest cannot simulate the runtime's kill-on-response signal that motivates this entire phase.

#### 2. Prod-connectivity-audit tier-green gate

**Test:** After the cron run above succeeds and `events:llm:v3` is populated, trigger the `.github/workflows/prod-connectivity-audit.yml` workflow manually in GitHub Actions.

**Expected:** Workflow exits 0; `allTiersGreen: true` is written into the `audit:connectivity:last-result` Redis sidecar; the merged API Health tab's audit-result banner shows green. This confirms the 28.2.5 gating signal is satisfied and 28.3-load-test is unblocked.

**Why human:** Requires a live deployment with a populated LLM cache, GitHub Actions secrets (`PROD_DASHBOARD_PASSWORD`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`), and manual workflow invocation — none of which can be exercised programmatically in local verification.

---

### Gaps Summary

No gaps found. All 9 must-haves verified against actual code. The two human-verification items are not gaps — they are prod-validation gates explicitly called out in the verification request and requiring a live Vercel deployment to exercise.

---

_Verified: 2026-05-07T16:00:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 28.2.6
slug: fix-vercel-cron-architecture-so-events-llm-v3-populates-with
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-07
---

# Phase 28.2.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from RESEARCH.md "## Validation Architecture" section (lines 584+).

---

## Test Infrastructure

| Property               | Value                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------- |
| **Framework**          | vitest 3.x (jsdom for client, node for server)                                                  |
| **Config file**        | `vite.config.ts` (test.alias for jsdom mocks); `vitest.config.ts` (server overrides if present) |
| **Quick run command**  | `npx vitest run server/__tests__/lib/llmExtractionPipeline`                                     |
| **Full suite command** | `npx vitest run`                                                                                |
| **Estimated runtime**  | quick ~5s · full ~45s (Phase 28.2.5 baseline: 2161 passed in 45.2s)                             |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run server/__tests__/lib/llmExtractionPipeline server/__tests__/lib/safeWaitUntil` (touched-file tests only — under 10s)
- **After every plan wave:** Run `npx vitest run` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green AND `npm run typecheck` AND `npm run lint`
- **Max feedback latency:** 10s for per-task, 60s for per-wave

---

## Per-Task Verification Map

> Filled in by planner during step 8. Below is the EXPECTED shape; planner will produce concrete entries for each task in 28.2.6-XX-PLAN.md.

| Task ID      | Plan | Wave | Requirement      | Threat Ref                                          | Secure Behavior                                                                         | Test Type       | Automated Command                                                            | File Exists                 | Status     |
| ------------ | ---- | ---- | ---------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------- | --------------------------- | ---------- |
| 28.2.6-01-01 | 01   | 1    | n/a (defect-fix) | T-28.2.6-01 (Redis write rate under concurrency=12) | Per-flush cacheSet does not exceed N=10 batches per Redis write                         | unit            | `npx vitest run server/__tests__/lib/llmExtractionPipeline.incrementalWrite` | ❌ W0                       | ⬜ pending |
| 28.2.6-01-02 | 01   | 1    | n/a (defect-fix) | T-28.2.6-02 (cross-tick state preservation)         | Two consecutive partial 5-batch runs == one continuous 10-batch run                     | unit (contract) | `npx vitest run server/__tests__/lib/llmExtractionPipeline.crossBoundary`    | ❌ W0                       | ⬜ pending |
| 28.2.6-01-03 | 01   | 1    | n/a (defect-fix) | T-28.2.6-03 (envelope-to-terminal-key regression)   | `events:llm:v3` writes are `ConflictEventEntity[]`, never `LLMCachePayload`             | unit            | `npx vitest run server/__tests__/lib/llmExtractionPipeline.terminalShape`    | ❌ W0                       | ⬜ pending |
| 28.2.6-02-01 | 02   | 2    | n/a (defect-fix) | T-28.2.6-04 (waitUntil shim local-dev fallback)     | `safeWaitUntil(p)` returns `void p` outside Vercel runtime                              | unit            | `npx vitest run server/__tests__/lib/safeWaitUntil`                          | ❌ W0                       | ⬜ pending |
| 28.2.6-02-02 | 02   | 2    | n/a (defect-fix) | T-28.2.6-05 (waitUntil shim no-throw)               | `safeWaitUntil` never throws on a rejected promise (logged, not propagated)             | unit            | `npx vitest run server/__tests__/lib/safeWaitUntil`                          | ❌ W0                       | ⬜ pending |
| 28.2.6-02-03 | 02   | 2    | n/a (defect-fix) | T-28.2.6-06 (cron route dispatch shape)             | POST /api/cron/refresh-events?force=true returns `{ok:true, dispatched:true}` in <500ms | integration     | `npx vitest run server/__tests__/routes/refresh-events-cron`                 | ✅ exists (per RESEARCH §3) | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

Wave 0 gaps identified by RESEARCH.md (lines 612-660):

- [ ] `server/__tests__/lib/llmExtractionPipeline.incrementalWrite.test.ts` — new file (no analog in current suite)
- [ ] `server/__tests__/lib/llmExtractionPipeline.crossBoundary.test.ts` — contract test for D-07 cross-function-boundary state preservation
- [ ] `server/__tests__/lib/llmExtractionPipeline.terminalShape.test.ts` — guard for D-04 / D-11 envelope-vs-entity invariant
- [ ] `server/__tests__/lib/safeWaitUntil.test.ts` — new file for the local-dev shim
- [ ] Confirm `server/__tests__/routes/refresh-events-cron.test.ts` exists; if not, Wave 0 creates it
- [ ] Test patterns: mirror `server/__tests__/lib/llmEventExtractor.v3-adaptive.test.ts` for LLM extraction harness; mirror `server/__tests__/lib/llmAutoRollback.test.ts` for Redis state assertions

_Existing infrastructure does NOT cover all phase requirements — Wave 0 creates 3-4 new test files and possibly extends 1 existing route test._

---

## Manual-Only Verifications

| Behavior                                                       | Requirement         | Why Manual                                                                                                | Test Instructions                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Production cron actually populates `events:llm:v3` post-deploy | D-13 success signal | Vercel Fluid Compute runtime can't be simulated locally; only prod proves the function-boundary fix works | After merge: `curl -X GET "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true" -H "Authorization: Bearer $PROD_CRON_SECRET" --max-time 30 && sleep 240 && curl -sS "https://otg-iran-monitor.vercel.app/api/health" -H "Authorization: Bearer hello" \| jq '.endpoints.llmEvents.status'` → expect `"healthy"` |
| Tier-green CI gate flips to `allTiersGreen=true`               | D-02                | Workflow runs against prod; gate verification is downstream evidence                                      | Trigger `prod-connectivity-audit.yml` from GH Actions UI after the manual cron force-trigger above lands the cache; expect `allTiersGreen=true` in workflow output                                                                                                                                                                 |
| `dispatched: true` envelope shape preserved                    | D-09                | Existing operator scripts check this; manual verification because `Authorization: Bearer` is needed       | `curl -X GET "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true" -H "Authorization: Bearer $PROD_CRON_SECRET"` → JSON body must contain `"dispatched": true`                                                                                                                                                  |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (4 new test files + verify cron-route test exists)
- [ ] No watch-mode flags (vitest commands use `run`, not `watch`)
- [ ] Feedback latency < 10s per-task, < 60s per-wave
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — planner finalizes the per-task verification map in step 8 against the actual plan task IDs.

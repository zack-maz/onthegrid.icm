---
phase: 34-llm-router-fallback-re-integration-cerebras-groq-per-provide
plan: 05
type: execute
wave: 5
depends_on: ['34-04']
files_modified:
  - server/routes/refresh-events-cron.ts
  - server/lib/llmExtractionPipeline.ts
  - server/lib/freeClaudeRouter.ts
  - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
  - docs/architecture/llm-pipeline-reliability.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-SUMMARY.md
autonomous: false
requirements: [LLM-RELI-08, LLM-RELI-09, LLM-RELI-10, LLM-RELI-11]
tags: [llm, phase-close, adr, roadmap, docs]

must_haves:
  truths:
    - "Plan 34-04's `?skipPrimary=nvidia_nim` validation flag is REVERTED from cron handler + pipeline + router — the flag was a witness, not a fixture (CONTEXT D-22)."
    - 'ADR-0010 has a new `## Phase 34 Sub-block (appended 2026-MM-DD)` recording per-provider probe percentages, pass/fail decision per provider, and restored cascade shape (or `cerebras-groq-deferred` close-out).'
    - '`docs/architecture/llm-pipeline-reliability.md` has a new `## Multi-Provider Cascade (Phase 34, 2026-MM-DD)` section between the Phase 30.1 section and the 7-Day Watch section.'
    - 'CLAUDE.md `Active providers` line is updated to reflect the restored provider count; 3 new Redis registry entries are added for the Cerebras/Groq daily-token keys + per-provider eval baseline key (D-31 invariant: NO phase-history bloat).'
    - 'ROADMAP.md Phase 34 row is flipped to ✓ Complete; REQUIREMENTS.md LLM-RELI-08..11 are flipped to `[x]` Done; STATE.md notes Phase 34 shipped.'
    - '34-SUMMARY.md created per project convention summarizing all 5 plans + commits + outcomes.'
    - "`feature/34-router-fallback-reintegration` ready to merge to main via PR after this plan's commits land."
  artifacts:
    - path: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      provides: 'Phase 34 sub-block with per-provider probe data + restored cascade shape (or deferral close-out).'
      contains: 'Phase 34 Sub-block'
    - path: 'docs/architecture/llm-pipeline-reliability.md'
      provides: 'Multi-Provider Cascade (Phase 34) section between Phase 30.1 and 7-Day Watch.'
      contains: 'Multi-Provider Cascade (Phase 34'
    - path: 'CLAUDE.md'
      provides: 'Updated Active providers line + 3 new Redis registry entries.'
      contains: 'Phase 34'
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 34 row marked ✓ Complete.'
      contains: 'Phase 34'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'LLM-RELI-08..11 marked `[x]` Done.'
      contains: '[x] **LLM-RELI-08**'
    - path: '.planning/STATE.md'
      provides: 'Phase 34 shipped record.'
      contains: 'Phase 34'
    - path: '.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-SUMMARY.md'
      provides: 'Phase-level summary aggregating all 5 plans.'
      min_lines: 50
  key_links:
    - from: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      to: 'probe-snapshot.json + plan-34-04-snapshot.json'
      via: 'ADR sub-block cites per-provider percentages from probe; cascade evidence from validation snapshot'
      pattern: 'Phase 34'
    - from: 'CLAUDE.md'
      to: 'Cerebras + Groq Redis keys'
      via: 'Serverless Cache registry entries'
      pattern: 'llm:tokens:cerebras|llm:tokens:groq|events:llm-eval-baseline:v3:by-provider'
---

<objective>
Close Phase 34: revert the temporary skipPrimary validation flag (CONTEXT D-22), write the ADR-0010 sub-block + reliability-doc section + CLAUDE.md amendments (CONTEXT D-31..D-33), flip ROADMAP / REQUIREMENTS / STATE, and emit `34-SUMMARY.md`.

Purpose: Phase-close discipline (mirrors Phase 30 close + Phase 30.1 close + Phase 33 close). The skipPrimary flag was a witness for validation; it must not persist in production code. The ADR + reliability doc + CLAUDE.md amendments are the durable artifacts that explain the post-Phase-34 cascade shape to future operators.
Output: Reverted skipPrimary flag; ADR sub-block; reliability-doc section; CLAUDE.md updates; ROADMAP/REQUIREMENTS/STATE flips; 34-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-RESEARCH.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/plan-34-04-snapshot.json
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-01-SUMMARY.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-02-SUMMARY.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-03-SUMMARY.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-04-SUMMARY.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@docs/architecture/llm-pipeline-reliability.md

<interfaces>
From server/routes/refresh-events-cron.ts (Plan 34-04 Commit 1 added):
- The `skipPrimary` query param reader + env-gate block introduced after `forceCooldown`. D-22 REVERT removes this entire block.
- `runRefreshExtraction` call site stops receiving `skipPrimary` in opts.

From server/lib/llmExtractionPipeline.ts (Plan 34-04 Commit 2 added):

- `skipPrimary?: 'nvidia_nim' | null` field on `runRefreshExtraction` options. D-22 REVERT removes the field and stops threading.

From server/lib/freeClaudeRouter.ts (Plan 34-04 Commit 2 added):

- The skipPrimary filter chained after the skipOpenRouter filter in callLLM. D-22 REVERT removes the chained filter; restores the original `providers = opts?.skipOpenRouter ? allProviders.filter(...) : allProviders;` single-line shape.

From docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md:

- Phase 30 sub-block (~line 52).
- Phase 30.1 sub-block (most recent prior — locate via `grep -n "Phase 30.1 Sub-block\\|## Phase 30.1"` ).
- D-32: Phase 34 sub-block APPENDS after the most recent prior sub-block.

From docs/architecture/llm-pipeline-reliability.md:

- "Tuned Defaults" section.
- Phase 30.1 section.
- "7-Day Watch" section.
- D-33: Phase 34 section INSERTS between the Phase 30.1 section and the 7-Day Watch section.

From CLAUDE.md (RESEARCH §2.6):

- Line 93 — "Active providers" 1-line update.
- Line 132 area — Serverless Cache registry: 3 new entries after the existing `llm:tokens:{provider}` umbrella entry.

From .planning/ROADMAP.md line 222-243:

- Phase 34 row + 5-plan stubs. D-22-close: mark phase complete; mark plans [x] in the plan-stub list.

From .planning/REQUIREMENTS.md lines 20-23 + 140-143:

- LLM-RELI-08..11 marked `[ ]` Currently. D-22-close: flip to `[x]` with brief status note (e.g. "Done — Phase 34 shipped").

From .planning/STATE.md:

- Current milestone progress block + recent-phase summary. D-22-close: append "Phase 34 shipped (2026-MM-DD)" line.
  </interfaces>

<close_branches>
**Two branches based on Plan 34-02's outcome** (read from `34-02-SUMMARY.md`):

| Plan 34-02 outcome                | Plan 34-05 narrative shape                                                                                                                                                                                                                                                  |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BOTH integrate                    | "Cerebras + Groq fallbacks restored. NIM primary, cascade widened, per-provider eval shipped."                                                                                                                                                                              |
| CEREBRAS only                     | "Cerebras fallback restored; Groq deferred per probe (rateLimitedPct=X%). NIM primary, cascade widened single-provider."                                                                                                                                                    |
| GROQ only                         | "Groq fallback restored; Cerebras deferred per probe (rateLimitedPct=X%). NIM primary, cascade widened single-provider."                                                                                                                                                    |
| DOC-ONLY collapse (both deferred) | "Both providers deferred per probe (cerebras=X%, groq=Y%). Phase closes with `cerebras-groq-deferred` status; ADR sub-block records honest deferral mirroring Phase 30.1's `nim-only` precedent. Per-provider eval still shipped (single-provider nvidia_nim observation)." |

**Plan 34-05 still ships in ALL branches** — the doc amendments + revert + ROADMAP flips are mandatory regardless of the cascade-shape outcome.
</close_branches>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Revert `?skipPrimary` validation flag (cron + pipeline + router)</name>
  <files>server/routes/refresh-events-cron.ts, server/lib/llmExtractionPipeline.ts, server/lib/freeClaudeRouter.ts</files>
  <read_first>
    - server/routes/refresh-events-cron.ts (CURRENT — locate the Plan 34-04 Commit 1 skipPrimary block to remove)
    - server/lib/llmExtractionPipeline.ts (CURRENT — locate `skipPrimary` in options type and threading)
    - server/lib/freeClaudeRouter.ts (CURRENT — locate the chained skipPrimary filter to remove)
    - .planning/phases/34-.../34-CONTEXT.md D-22 ("temporary flag is a witness, not a fixture — it ships in Plan 04 and gets reverted in Plan 05")
  </read_first>
  <action>
**Commit (`revert(34): remove temporary skipPrimary validation flag (D-22)`):**

1. **In `server/routes/refresh-events-cron.ts`:**
   - Remove the entire `skipPrimary` query-param reader + env-gate block added in Plan 34-04 Commit 1 (the block introduced after the `forceCooldown` line).
   - Remove `skipPrimary` from the `runRefreshExtraction({ ... })` call site.
   - Verify: `grep -q "skipPrimary" server/routes/refresh-events-cron.ts` returns NON-ZERO (no remaining references).

2. **In `server/lib/llmExtractionPipeline.ts`:**
   - Remove `skipPrimary?: 'nvidia_nim' | null` from the `runRefreshExtraction` options type.
   - Remove `skipPrimary` threading from the pipeline → `freeClaudeCallLLM` opts.
   - Verify: `grep -q "skipPrimary" server/lib/llmExtractionPipeline.ts` returns NON-ZERO.

3. **In `server/lib/freeClaudeRouter.ts`:**
   - Remove `skipPrimary?: 'nvidia_nim' | null` from `callLLM` opts type.
   - Remove the chained filter `if (opts?.skipPrimary === 'nvidia_nim') { providers = providers.filter((p) => p.name !== 'nvidia_nim'); }` and restore the original single-line `providers = opts?.skipOpenRouter ? allProviders.filter(...) : allProviders;`.
   - Verify: `grep -q "skipPrimary" server/lib/freeClaudeRouter.ts` returns NON-ZERO.

4. **Remove related tests:**
   - In `server/__tests__/routes/refresh-events-cron.test.ts`: remove the 4 Phase 34 skipPrimary test cases added in Plan 34-04 Commit 3.
   - In `server/__tests__/lib/freeClaudeRouter.test.ts`: remove the 2 skipPrimary router-filter test cases added in Plan 34-04 Commit 3. Keep the rest of the Phase 34 cascade-fall-through describe block intact (those tests cover the persistent cascade behavior).

5. **Verification:**
   - `npx tsc --noEmit` exits 0.
   - `npx vitest run` exits 0 (no skipPrimary-related test fails — they're removed).
   - `grep -r "skipPrimary" server/` returns 0 matches (full revert verified).

Commit message: `revert(34): remove temporary skipPrimary validation flag (D-22)`. Body: cite that the flag was a witness for Plan 34-04's validation cron snapshot; the snapshot artifact (committed in Plan 34-04) is the persistent evidence.
</action>
<verify>
<automated>if grep -r "skipPrimary" server/ 2>/dev/null; then echo "FAIL: skipPrimary references remain"; exit 1; fi && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - `grep -r "skipPrimary" server/` returns 0 matches across all files. - `npx tsc --noEmit` exits 0. - `npx vitest run` exits 0 (full suite green; no skipPrimary-related test failures from missing references). - `git log -1 --format=%s HEAD` matches `revert(34): remove temporary skipPrimary validation flag (D-22)`. - Plan 34-04's snapshot artifact at `.planning/phases/34-.../plan-34-04-snapshot.json` REMAINS untouched (the persistent evidence — only the runtime flag is reverted).
</acceptance_criteria>
<done>skipPrimary flag fully reverted from production code + tests; Plan 34-04's snapshot artifact preserved as historical evidence; full test suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append ADR-0010 Phase 34 sub-block</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (FULL — locate the most recent prior sub-block; Phase 30 + Phase 30.1 are templates)
    - .planning/phases/34-.../probe-snapshot.json (per-provider percentages + decisions)
    - .planning/phases/34-.../plan-34-04-snapshot.json (validation cron evidence — provider call history, DLQ count)
    - .planning/phases/34-.../34-02-SUMMARY.md (outcome branch — which providers restored)
    - .planning/phases/34-.../34-CONTEXT.md D-32
  </read_first>
  <action>
**Commit (`docs(34): ADR-0010 sub-block — phase 34 cascade restoration (D-32)`):**

Append `## Phase 34 Sub-block (appended 2026-05-23)` to `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` AFTER the most recent prior sub-block (Phase 30.1 sub-block).

Content per CONTEXT D-32, formatted to mirror Phase 30.1's sub-block:

1. **One-paragraph header summary**: Cite phase trigger (Phase 31 Day-1 4× v3:timeout_watchdog), what was done (probe-driven re-integration), and outcome (cite from `34-02-SUMMARY.md`'s branch: BOTH restored / CEREBRAS only / GROQ only / DOC-ONLY collapse).

2. **Per-provider probe results table** (read from `probe-snapshot.json` `.byProvider.{provider}.summary`):
   | Provider | Rate-limited | % | latency p50 | latency p95 | Decision |
   |----------|--------------|---|-------------|-------------|----------|
   | cerebras | <n>/30 | <pct>% | <p50>ms | <p95>ms | <decision> |
   | groq | <n>/30 | <pct>% | <p50>ms | <p95>ms | <decision> |

3. **Restored cascade shape** (cite from `34-02-SUMMARY.md` — Plan 34-02 commit body has the latencyP50 ordering rationale):
   - If BOTH: `NIM (primary) → <faster-by-p50> → <slower-by-p50> → OpenRouter (dormant per Phase 30.1)`.
   - If CEREBRAS only: `NIM → cerebras → OpenRouter (dormant)`. Groq deferred at <pct>%.
   - If GROQ only: `NIM → groq → OpenRouter (dormant)`. Cerebras deferred at <pct>%.
   - If DOC-ONLY collapse: `cerebras-groq-deferred` close-out — cascade remains NIM-only per Phase 30.1 precedent. Quote: "the empirical 'free-tier throttle correlated with NIM' finding is itself a load-bearing outcome."

4. **Validation cron evidence** (cite from `plan-34-04-snapshot.json`):
   - 1-line callHistory provider distribution.
   - 1-line evalScore.byProvider summary.
   - 1-line DLQ count (`v3:cascade_exhausted: N, v3:timeout_watchdog: M`).

5. **Per-provider eval scoring shipped** (regardless of restoration outcome):
   - "`evalScore.byProvider` field added to `EvalScore` + `LLMRunSummary`."
   - "API Health dashboard `EvalScoreBlock` renders per-provider rows (testIDs: `eval-byprovider-{provider}-within20km`)."
   - "Per-provider baselines persist at `events:llm-eval-baseline:v3:by-provider:{provider}` (90d TTL)."
   - "Resolver-only — zero new LLM token spend (CONTEXT D-16, inherited from Phase 27.4 D-25)."

6. **New DLQ reason bucket**:
   - "`v3:cascade_exhausted` distinguishes 'all providers failed' from `v3:timeout_watchdog` 'single-batch watchdog kill'."

7. **Forward pointer**:
   - "Phase 35 catalogs the new Redis keys (`llm:tokens:cerebras|groq:YYYY-MM-DD`, `events:llm-eval-baseline:v3:by-provider:*`) in `docs/architecture/redis-keys.md`."
   - "Phase 37 acceptance gate (3× prod-connectivity-audit.yml exit-0) observes the restored cascade under natural load."

8. **Belt-and-suspenders pointer** (CONTEXT canonical refs):
   - "Raw-GDELT terminal fallback (Pitfall 1 cache bridge) remains the invariant — `/api/events` serves raw GDELT regardless of cascade shape when `events:llm:v3` is empty (Plan 34-04 SC-6 regression test verified)."

**Commit message:** `docs(34): ADR-0010 sub-block — phase 34 cascade restoration (D-32)`. Body: 1-line summary of restoration outcome.
</action>
<verify>
<automated>grep -q "Phase 34 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "v3:cascade_exhausted" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "events:llm-eval-baseline:v3:by-provider:" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</automated>
</verify>
<acceptance_criteria> - `grep -q "## Phase 34 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`. - Sub-block contains the per-provider probe table with `rateLimitedPct`, `latencyP50`, `latencyP95`, `decision` columns populated from `probe-snapshot.json`. - Sub-block mentions `v3:cascade_exhausted` (new DLQ reason). - Sub-block mentions `events:llm-eval-baseline:v3:by-provider:` (new per-provider eval keys). - Sub-block placement is AFTER the Phase 30.1 sub-block (verify ordering via `grep -n "## Phase" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`). - `git log -1 --format=%s HEAD` matches `docs(34): ADR-0010 sub-block — phase 34 cascade restoration (D-32)`.
</acceptance_criteria>
<done>ADR-0010 Phase 34 sub-block appended with per-provider probe table + restored cascade shape + validation evidence + per-provider eval shipped + new DLQ reason + forward pointers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append reliability-doc Multi-Provider Cascade (Phase 34) section</name>
  <files>docs/architecture/llm-pipeline-reliability.md</files>
  <read_first>
    - docs/architecture/llm-pipeline-reliability.md (FULL — locate the Phase 30.1 section + the "7-Day Watch" section; D-33 inserts between them)
    - .planning/phases/34-.../34-CONTEXT.md D-33
    - .planning/phases/34-.../34-02-SUMMARY.md + 34-03-SUMMARY.md + 34-04-SUMMARY.md (cite cascade shape + per-provider scoring + validation outcome)
  </read_first>
  <action>
**Commit (`docs(34): reliability-doc multi-provider cascade section (D-33)`):**

Insert a new section `## Multi-Provider Cascade (Phase 34, 2026-05-23)` in `docs/architecture/llm-pipeline-reliability.md` BETWEEN the Phase 30.1 section and the "7-Day Watch" section (locate boundaries via `grep -n "## Phase 30.1\\|## 7-Day Watch\\|## Multi-Provider"` ).

Content per CONTEXT D-33:

1. **Header paragraph** — phase trigger (Phase 31 Day-1 baseline 4× v3:timeout_watchdog) + outcome 1-line.

2. **Cascade shape post-restore** (depends on Plan 34-02 outcome — cite from `34-02-SUMMARY.md`):
   - Sequential ordering: NIM (primary) → <restored providers ordered by probe latencyP50 ascending> → OpenRouter (dormant per Phase 30.1, skipOpenRouter: true).
   - Per-provider RPM / RPD constraints:
     - NIM: `RollingWindow(40, 60_000)` — unchanged.
     - Cerebras (if restored): `RollingWindow(5, 60_000)` — 5 RPM free tier per RESEARCH §1.1. RPM-only gate; no daily counter.
     - Groq (if restored): `RollingWindow(30, 60_000)` + `GROQ_DAILY_CAP = 1000` + Redis daily key `llm:tokens:groq:YYYY-MM-DD` (48h TTL).
     - OpenRouter: unchanged (200/day cap, dormant).

3. **Per-provider eval scoring** (CONTEXT D-15..D-20):
   - `providerProvenance` field on `enrichedEventV3` (additive-optional per Phase 33 D-04..D-07 rollout discipline).
   - `EvalScore.byProvider` aggregation in `runEval()` (RESOLVER-ONLY — zero LLM token spend; CONTEXT D-16 inherited from Phase 27.4 D-25).
   - Per-provider baselines at `events:llm-eval-baseline:v3:by-provider:{provider}` (90d TTL, mirrors per-model baseline at `llmEvalHarness.ts:78` JSDoc).
   - Dashboard surface: `EvalScoreBlock` per-provider sub-block with pinned testIDs `eval-byprovider-{provider}-within20km`.
   - Regression gate: ±3pp absolute at any of {5 km, 20 km, 100 km} vs per-provider baseline (NOT against aggregate — providers have different floors per CONTEXT D-20).

4. **New DLQ reason bucket** (CONTEXT D-13):
   - `v3:cascade_exhausted` written when `callLLM` returns null content AND `!didTimeout`.
   - Distinguished from `v3:timeout_watchdog` (single-batch watchdog kill).
   - `lastError` is `cascade exhausted: <provider1>:<reason1>; <provider2>:<reason2>; ...` (capped 500 chars).

5. **Pointer to ADR-0010 sub-block:**
   - "See `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` § Phase 34 Sub-block for probe percentages and restoration rationale."

6. **Belt-and-suspenders paragraph** (CONTEXT canonical refs — mirror Phase 30.1 D-08):
   - "Raw-GDELT terminal fallback (Pitfall 1 cache bridge) remains the invariant. When `events:llm:v3` is empty for any reason — including all cascade providers failing — `server/routes/events.ts` serves raw GDELT entities. 'Map never goes blank' contract is untouched. ADR-0010 makes raw GDELT the terminal fallback."

**Commit message:** `docs(34): reliability-doc multi-provider cascade section (D-33)`.
</action>
<verify>
<automated>grep -q "## Multi-Provider Cascade (Phase 34" docs/architecture/llm-pipeline-reliability.md && grep -q "v3:cascade_exhausted" docs/architecture/llm-pipeline-reliability.md && grep -q "events:llm-eval-baseline:v3:by-provider:" docs/architecture/llm-pipeline-reliability.md</automated>
</verify>
<acceptance_criteria> - `grep -q "## Multi-Provider Cascade (Phase 34" docs/architecture/llm-pipeline-reliability.md`. - Section placement: AFTER Phase 30.1 section, BEFORE 7-Day Watch section. Verify via `awk '/## Phase 30.1/{a=NR} /## Multi-Provider Cascade/{b=NR} /## 7-Day Watch/{c=NR} END{ exit (a<b && b<c) ? 0 : 1 }' docs/architecture/llm-pipeline-reliability.md`. - Section mentions per-provider RPM constraints (Cerebras 5 RPM, Groq 30 RPM). - Section mentions `v3:cascade_exhausted` DLQ reason. - Section mentions `events:llm-eval-baseline:v3:by-provider:` per-provider eval keys. - Section has the belt-and-suspenders raw-GDELT terminal fallback paragraph (Pitfall 1 invariant). - `git log -1 --format=%s HEAD` matches `docs(34): reliability-doc multi-provider cascade section (D-33)`.
</acceptance_criteria>
<done>Reliability doc has new Multi-Provider Cascade (Phase 34) section between Phase 30.1 and 7-Day Watch sections; covers cascade shape, per-provider rate limits, per-provider eval, new DLQ reason, and the raw-GDELT terminal fallback paragraph.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Amend CLAUDE.md per RESEARCH §2.6 (Active providers line + Redis registry)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 90-140 — LLM Event Pipeline + Serverless Cache sections; verify exact line numbers as the file may have shifted since RESEARCH was authored)
    - .planning/phases/34-.../34-CONTEXT.md D-31 ("Single-line edit ... NO new phase-history bloat")
    - .planning/phases/34-.../34-RESEARCH.md §2.6 (exact replacement strings + 3 new Redis registry entries)
  </read_first>
  <action>
**Commit (`docs(34): CLAUDE.md amendments — active providers + Redis registry (D-31)`):**

1. **"Active providers" line** (locate via `grep -n "Active providers" CLAUDE.md`; RESEARCH §2.6 identifies it around line 93):
   - REPLACE the current line `- **Active providers (Phase 29 D-01)** — qwen-235b instruct model. OpenRouter fallback dormant — see ...` with a Phase 34 update per RESEARCH §2.6 template.
   - **Branch on Plan 34-02 outcome** (read from `34-02-SUMMARY.md`):
     - If BOTH restored: `- **Active providers (Phase 34 update)** — qwen-235b instruct model (NIM primary). Cerebras + Groq fallbacks restored via probe-driven re-integration (Phase 34 D-01). OpenRouter remains dormant per Phase 30.1 (90% rate-limited). See \`docs/architecture/llm-pipeline-reliability.md\` for cascade reality + per-provider eval scoring.`
     - If CEREBRAS only: same as above but `Cerebras fallback restored; Groq deferred per probe (<pct>%).`
     - If GROQ only: same but `Groq fallback restored; Cerebras deferred per probe (<pct>%).`
     - If DOC-ONLY collapse: `- **Active providers (Phase 34 update)** — qwen-235b instruct model (NIM primary). OpenRouter dormant per Phase 30.1; Cerebras + Groq evaluated and deferred per Phase 34 probe (cerebras=<pct>%, groq=<pct>%). See ADR-0010 Phase 34 sub-block + \`docs/architecture/llm-pipeline-reliability.md\` for the empirical 'free-tier throttle correlated with NIM' close-out.`

2. **Serverless Cache registry** (locate via `grep -n "llm:tokens:{provider}" CLAUDE.md`; RESEARCH §2.6 identifies it around line 132):
   - INSERT 3 new entries AFTER the existing `llm:tokens:{provider}:YYYY-MM-DD` umbrella entry. KEEP the umbrella entry; the new entries are provider-specific specializations:
     - `- **\`llm:tokens:cerebras:YYYY-MM-DD\` (Phase 34)\*\* — Cerebras daily request counter; 48h TTL; mirrors the OpenRouter pattern at \`freeClaudeRouter.ts:289-306\`.` (skip this line if Cerebras was deferred — no key writer exists)
     - `- **\`llm:tokens:groq:YYYY-MM-DD\` (Phase 34)\*\* — Groq daily request counter; 48h TTL; same pattern.` (skip if Groq deferred)
     - `- **\`events:llm-eval-baseline:v3:by-provider:{provider}\` (Phase 34 D-20)\*\* — per-provider eval baseline; 90d TTL; mirrors the per-model baseline pattern at \`llmEvalHarness.ts:78\` JSDoc (sanitized provider id in slot).` (ALWAYS add — written even in DOC-ONLY collapse branch for nvidia_nim)

3. **D-31 INVARIANT — NO phase-history bloat**:
   - Do NOT add a "Phase 34" subsection to CLAUDE.md.
   - Do NOT cross-link other Phase 34 docs in the body (those live in ADR-0010 + reliability doc).
   - Total CLAUDE.md diff size: should be ≤8 added lines / ≤2 modified lines (1 active-providers line replaced + up to 3 registry entries added + minor whitespace).

**Commit message:** `docs(34): CLAUDE.md amendments — active providers + Redis registry (D-31)`. Body: line count delta (e.g. `+4 / -1`).
</action>
<verify>
<automated>grep -q "Phase 34" CLAUDE.md && grep -q "events:llm-eval-baseline:v3:by-provider:" CLAUDE.md && wc -l CLAUDE.md</automated>
</verify>
<acceptance_criteria> - `grep -q "Active providers (Phase 34 update)" CLAUDE.md` (line replaced). - `grep -q "events:llm-eval-baseline:v3:by-provider:" CLAUDE.md` (per-provider eval baseline registry entry added). - For restored provider(s): `grep -q "llm:tokens:cerebras:YYYY-MM-DD (Phase 34)" CLAUDE.md` (if Cerebras restored) AND/OR `grep -q "llm:tokens:groq:YYYY-MM-DD (Phase 34)" CLAUDE.md` (if Groq restored). - `git diff --stat HEAD~1..HEAD CLAUDE.md | awk 'END{print}'` shows ≤10 lines changed (D-31 trim budget preserved). - `git log -1 --format=%s HEAD` matches `docs(34): CLAUDE.md amendments — active providers + Redis registry (D-31)`. - No new H2 or H3 headings added to CLAUDE.md (verify via `git diff HEAD~1..HEAD CLAUDE.md | grep -cE "^\\+##"` returns 0).
</acceptance_criteria>
<done>CLAUDE.md "Active providers" line updated to reflect Phase 34 outcome; 1-3 new Redis registry entries added; D-31 NO-phase-history-bloat invariant respected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Flip ROADMAP / REQUIREMENTS / STATE</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 222-243 — Phase 34 row + plan stubs)
    - .planning/REQUIREMENTS.md (lines 20-23 — LLM-RELI-08..11 + the v1.5 detailed expansion section around lines 140-143 if present)
    - .planning/STATE.md (current milestone progress block; locate recent-phase summary at file head)
    - .planning/phases/34-.../34-02-SUMMARY.md (for the close-status string)
  </read_first>
  <action>
**Commit (`docs(34): ROADMAP/REQUIREMENTS/STATE flips for phase close (D-24)`):**

1. **ROADMAP.md** (lines 222-243):
   - Phase 34 heading: change `### Phase 34: LLM Router Fallback Re-integration ...` to `### Phase 34: LLM Router Fallback Re-integration ... ✓ Complete` (mirror the ✓ Complete marker used by prior closed phases — verify by `grep -n "✓ Complete" .planning/ROADMAP.md`).
   - Plan stub list: flip each `- [ ] 34-NN-PLAN.md` to `- [x] 34-NN-PLAN.md` for all 5 plans.
   - Append a 1-line outcome at the end of the Phase 34 section: `**Outcome**: <one-line, sourced from 34-02-SUMMARY.md — e.g. "Cerebras + Groq restored, per-provider eval shipped" or "cerebras-groq-deferred close-out, per-provider eval shipped">`.

2. **REQUIREMENTS.md** (lines 20-23 + any expanded section):
   - For LLM-RELI-08: flip `- [ ]` to `- [x]`. Append `(Phase 34 — probe-snapshot.json committed; see ADR-0010 Phase 34 sub-block)`.
   - For LLM-RELI-09: flip `- [ ]` to `- [x]`. Append outcome-appropriate note: `(Phase 34 — <both restored / single-provider restored / cerebras-groq-deferred>)`.
   - For LLM-RELI-10: flip `- [ ]` to `- [x]`. Append `(Phase 34 — providerProvenance + EvalScore.byProvider + dashboard surface; resolver-only)`.
   - For LLM-RELI-11: flip `- [ ]` to `- [x]`. Append `(Phase 34 — cascade_exhausted DLQ + validation snapshot at plan-34-04-snapshot.json; DLQ count <4 confirmed)`.

3. **STATE.md**:
   - Append a 1-line phase-shipped record at the appropriate "recent phases" block: `- Phase 34 (LLM Router Fallback Re-integration) shipped 2026-05-23 — <outcome>`.
   - If STATE.md has a milestone progress percentage or progress bar, update it accordingly (count Phase 34 as complete in the v1.5 phase tally).

**Commit message:** `docs(34): ROADMAP/REQUIREMENTS/STATE flips for phase close (D-24)`. Body: cite the outcome string used.
</action>
<verify>
<automated>grep -q "Phase 34.*✓ Complete\\|Phase 34: LLM Router Fallback.*✓" .planning/ROADMAP.md && grep -q "\\[x\\] \\*\\*LLM-RELI-08\\_\\_" .planning/REQUIREMENTS.md && grep -q "\\[x\\] \\*\\*LLM-RELI-09\\_\\_" .planning/REQUIREMENTS.md && grep -q "\\[x\\] \\*\\*LLM-RELI-10\\_\\_" .planning/REQUIREMENTS.md && grep -q "\\[x\\] \\*\\*LLM-RELI-11\\_\\_" .planning/REQUIREMENTS.md && grep -q "Phase 34" .planning/STATE.md</automated>
</verify>
<acceptance_criteria> - `grep -q "Phase 34.*✓ Complete" .planning/ROADMAP.md` (or equivalent close marker matching prior phases). - All 5 plan stubs in ROADMAP Phase 34 section are `- [x]`. - REQUIREMENTS.md LLM-RELI-08..11 all show `- [x]`. - STATE.md has a `Phase 34 ... shipped 2026-05-23` line. - `git log -1 --format=%s HEAD` matches `docs(34): ROADMAP/REQUIREMENTS/STATE flips for phase close (D-24)`.
</acceptance_criteria>
<done>ROADMAP Phase 34 marked complete with all 5 plans checked; REQUIREMENTS LLM-RELI-08..11 all `[x]`; STATE.md notes phase shipped with outcome string.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Write 34-SUMMARY.md</name>
  <files>.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-SUMMARY.md</files>
  <read_first>
    - .planning/phases/34-.../34-01-SUMMARY.md
    - .planning/phases/34-.../34-02-SUMMARY.md
    - .planning/phases/34-.../34-03-SUMMARY.md
    - .planning/phases/34-.../34-04-SUMMARY.md
    - .planning/phases/34-.../probe-snapshot.json
    - .planning/phases/34-.../plan-34-04-snapshot.json
    - Prior phase 34-SUMMARY equivalents for format reference: `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-SUMMARY.md` (if exists), `.planning/phases/30.1-.../30.1-SUMMARY.md` (if exists)
  </read_first>
  <action>
**Commit (`docs(34): phase close — 34-SUMMARY.md (D-24)`):**

Create `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-SUMMARY.md` with:

1. **Header**:
   - `# Phase 34 — LLM Router Fallback Re-integration (Cerebras / Groq + Per-Provider Eval) — SUMMARY`
   - **Shipped:** 2026-05-23
   - **Branch:** `feature/34-router-fallback-reintegration` (merged to main post-close).
   - **Requirements satisfied:** LLM-RELI-08, LLM-RELI-09, LLM-RELI-10, LLM-RELI-11.

2. **Phase outcome (1 paragraph)**:
   - Branch on Plan 34-02 outcome (BOTH / CEREBRAS only / GROQ only / DOC-ONLY collapse).
   - Cite probe percentages + decisions.
   - Cite validation snapshot evidence (callHistory non-NIM provider rows, DLQ count <4).

3. **Plan-by-plan breakdown** (1 paragraph each):
   - Plan 34-01: probe script + snapshot artifact (committed).
   - Plan 34-02: adapter restoration (or doc-only collapse) + branch cut.
   - Plan 34-03: per-provider eval (providerProvenance + EvalScore.byProvider + dashboard surface + per-provider baselines).
   - Plan 34-04: cascade_exhausted DLQ + skipPrimary validation flag + validation cron snapshot.
   - Plan 34-05: skipPrimary revert + ADR + reliability doc + CLAUDE.md + ROADMAP/REQUIREMENTS/STATE flips + this SUMMARY.

4. **Artifacts committed**:
   - `.planning/phases/34-.../probe-snapshot.json` (per-provider probe data, including latencyP50/p95)
   - `.planning/phases/34-.../plan-34-04-snapshot.json` (validation cron evidence)
   - Code: 5 plans × multiple atomic commits per CONTEXT D-25.
   - Docs: ADR-0010 sub-block, reliability doc Multi-Provider Cascade section, CLAUDE.md updates.

5. **Tests added** (cite counts from each plan):
   - Plan 34-02: per-provider fall-through tests in freeClaudeRouter.test.ts.
   - Plan 34-03: providerProvenance schema gates (4); per-provider eval aggregation (6); per-provider eval sub-block RTL (5).
   - Plan 34-04: DLQ union round-trip (4); cascade_exhausted enqueue (4); skipPrimary handler + router filter (6 — REVERTED in Plan 34-05).

6. **Forward signals**:
   - **Phase 35:** Catalogs new Redis keys (`llm:tokens:cerebras|groq:YYYY-MM-DD`, `events:llm-eval-baseline:v3:by-provider:*`) in `docs/architecture/redis-keys.md`.
   - **Phase 36:** Public docs sweep — README.md provider-count line update; Mermaid cascade diagram in reliability doc.
   - **Phase 37:** Acceptance gate — 3× consecutive `prod-connectivity-audit.yml` exit-0 observation runs against the restored cascade.
   - **Optional Phase 35+:** Adaptive Retry-After-aware NIM limiter (if Day-N watch shows DLQ drift); paid-Cerebras / paid-Groq conversation (if middle-bucket outcome merits).

7. **CONTEXT decisions touched** (sanity-check list):
   - D-01..D-12: Probe-driven restoration mechanics.
   - D-13, D-14, D-28: cascade_exhausted DLQ.
   - D-15..D-20, D-29, D-30: per-provider eval scoring.
   - D-21, D-22: validation cron + skipPrimary witness (now reverted).
   - D-23: eval regression gate.
   - D-24, D-25, D-26: plan decomposition + atomic commits + branch discipline.
   - D-27: adapter test pattern.
   - D-31, D-32, D-33: doc amendments.

8. **Belt-and-suspenders notes**:
   - Pitfall 1 cache-bridge regression test PASSED post-Phase-34 changes (SC-6 invariant intact).
   - skipPrimary flag REVERTED — production code does not contain the validation witness.
   - Per-provider eval ships in ALL branches (including DOC-ONLY collapse) per CONTEXT "Conditional on Probe Outcome".

**Commit message:** `docs(34): phase close — 34-SUMMARY.md (D-24)`.
</action>
<verify>
<automated>test -f .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-SUMMARY.md && wc -l .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-SUMMARY.md | awk '{ if ($1 >= 50) print "OK"; else { print "FAIL: too short"; exit 1 } }'</automated>
</verify>
<acceptance_criteria> - `.planning/phases/34-.../34-SUMMARY.md` exists with ≥50 lines. - Contains "Phase 34" header. - Mentions all 4 LLM-RELI requirement IDs. - Cites probe-snapshot.json + plan-34-04-snapshot.json artifacts. - Lists all 5 plans with 1-paragraph each. - Lists CONTEXT decisions touched (D-01..D-33 grouped by area). - Forward signals to Phase 35 / 36 / 37 present. - `git log -1 --format=%s HEAD` matches `docs(34): phase close — 34-SUMMARY.md (D-24)`.
</acceptance_criteria>
<done>34-SUMMARY.md committed with phase outcome, plan-by-plan breakdown, artifact list, tests added, forward signals, and CONTEXT decisions touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Pre-merge verification + PR creation</name>
  <files>n/a (human checkpoint — operator UAT only)</files>
  <action>
**Human checkpoint — operator action required.**

All Plan 34-01..34-05 commits land on `feature/34-router-fallback-reintegration`. The branch is now ready to merge to `main`.

**Steps:**

1. **Full test suite:**

   ```bash
   npx vitest run
   ```

   EXIT 0 required.

2. **Build:**

   ```bash
   npm run build
   ```

   EXIT 0 required.

3. **Branch state:**

   ```bash
   git log --oneline main..feature/34-router-fallback-reintegration
   ```

   Confirms all Phase 34 atomic commits per CONTEXT D-24/D-25 are present.

4. **No skipPrimary residue:**

   ```bash
   grep -r "skipPrimary" server/ src/ docs/ 2>/dev/null
   ```

   Returns ONLY docs/reference mentions (CONTEXT D-22 in ADR or reliability doc text); NO code-path references in server/ or src/.

5. **Probe + validation artifacts present:**

   ```bash
   test -f .planning/phases/34-.../probe-snapshot.json && \
   test -f .planning/phases/34-.../plan-34-04-snapshot.json && \
   test -f .planning/phases/34-.../34-SUMMARY.md
   ```

   All three required.

6. **PR creation:**

   ```bash
   gh pr create --title "Phase 34: LLM Router Fallback Re-integration (Cerebras / Groq + Per-Provider Eval)" --base main --head feature/34-router-fallback-reintegration --body "$(cat <<'EOF'
   ## Summary
   - Probe-driven Cerebras + Groq cascade re-integration in `freeClaudeRouter.ts`
   - Per-provider eval scoring (`providerProvenance` + `EvalScore.byProvider` + dashboard surface)
   - New DLQ reason `v3:cascade_exhausted` distinguishes cascade failure from watchdog timeout
   - Phase 31 Day-1 baseline (4× v3:timeout_watchdog) addressed; validation snapshot shows materially lower DLQ count

   ## Test plan
   - [ ] `npx vitest run` passes (full suite)
   - [ ] `npm run build` passes
   - [ ] Confirm `CEREBRAS_API_KEY` + `GROQ_API_KEY` in Vercel prod env Settings → Environment Variables
   - [ ] Post-merge: monitor first cron `/api/cron/refresh-events` for cerebras/groq rows in `events:llm-summary:v3.callHistory`
   - [ ] Pitfall 1 cache-bridge regression test passed (raw GDELT still served when v3 cache empty)

   See `.planning/phases/34-.../34-SUMMARY.md` for the full close-out and forward signals.

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```

7. **DO NOT MERGE** until the operator approves.

**Resume signal:** Reply "merge approved" + paste PR URL, OR describe blockers.
</action>
<verify>
<automated>echo "Human checkpoint — see action block for verification steps; operator confirms via resume signal"; true</automated>
</verify>
<done>Operator approves via resume signal per the action block; execution unpauses.</done>
<what-built>
All Plan 34-01..34-05 commits land on `feature/34-router-fallback-reintegration`. The branch is now ready to merge to `main`.
</what-built>
<how-to-verify>

1. **Full test suite:**

   ```bash
   npx vitest run
   ```

   EXIT 0 required.

2. **Build:**

   ```bash
   npm run build
   ```

   EXIT 0 required.

3. **Branch state:**

   ```bash
   git log --oneline main..feature/34-router-fallback-reintegration
   ```

   Confirms all Phase 34 atomic commits per CONTEXT D-24/D-25 are present.

4. **No skipPrimary residue:**

   ```bash
   grep -r "skipPrimary" server/ src/ docs/ 2>/dev/null
   ```

   Returns ONLY docs/reference mentions (CONTEXT D-22 in ADR or reliability doc text); NO code-path references in server/ or src/.

5. **Probe + validation artifacts present:**

   ```bash
   test -f .planning/phases/34-.../probe-snapshot.json && \
   test -f .planning/phases/34-.../plan-34-04-snapshot.json && \
   test -f .planning/phases/34-.../34-SUMMARY.md
   ```

   All three required.

6. **PR creation:**

   ```bash
   gh pr create --title "Phase 34: LLM Router Fallback Re-integration (Cerebras / Groq + Per-Provider Eval)" --base main --head feature/34-router-fallback-reintegration --body "$(cat <<'EOF'
   ## Summary
   - Probe-driven Cerebras + Groq cascade re-integration in `freeClaudeRouter.ts`
   - Per-provider eval scoring (`providerProvenance` + `EvalScore.byProvider` + dashboard surface)
   - New DLQ reason `v3:cascade_exhausted` distinguishes cascade failure from watchdog timeout
   - Phase 31 Day-1 baseline (4× v3:timeout_watchdog) addressed; validation snapshot shows materially lower DLQ count

   ## Test plan
   - [ ] `npx vitest run` passes (full suite)
   - [ ] `npm run build` passes
   - [ ] Confirm `CEREBRAS_API_KEY` + `GROQ_API_KEY` in Vercel prod env Settings → Environment Variables
   - [ ] Post-merge: monitor first cron `/api/cron/refresh-events` for cerebras/groq rows in `events:llm-summary:v3.callHistory`
   - [ ] Pitfall 1 cache-bridge regression test passed (raw GDELT still served when v3 cache empty)

   See `.planning/phases/34-.../34-SUMMARY.md` for the full close-out and forward signals.

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```

7. **DO NOT MERGE** until the operator approves.
   </how-to-verify>
   <resume-signal>Reply "merge approved" + paste PR URL, OR describe blockers.</resume-signal>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                        |
| -------------------------------- | ------------------------------------------------------------------ |
| skipPrimary revert               | Restores production code to no-flag state (no env-gate to bypass)  |
| ADR + reliability doc            | Public-facing documentation of the cascade restoration             |
| CLAUDE.md amendments             | Live operator reference for active provider count + Redis registry |
| ROADMAP/REQUIREMENTS/STATE flips | Planning-state truth for milestone progress                        |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                    | Disposition | Mitigation Plan                                                                                                                                   |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-23   | Repudiation            | "Was the skipPrimary flag accidentally left in prod?"                                                        | mitigate    | Task 1 acceptance criteria: `grep -r "skipPrimary" server/` returns 0; Task 7 checkpoint re-verifies.                                             |
| T-34-24   | Information disclosure | ADR sub-block reveals provider rate-limit numbers publicly (repo is public)                                  | accept      | Probe numbers are operator-known free-tier ceilings (already in Cerebras/Groq public docs); no API key material or non-public information leaked. |
| T-34-25   | Tampering              | Doc amendments accidentally contradict each other (e.g. ADR says cerebras restored, CLAUDE.md says deferred) | mitigate    | All 4 doc-touching tasks (2-4) source the outcome string from `34-02-SUMMARY.md` and `probe-snapshot.json` — same upstream source-of-truth.       |
| T-34-26   | Spoofing               | Phase prematurely flipped to ✓ Complete with unfinished work                                                 | mitigate    | Task 7 pre-merge checkpoint validates full test suite + build pass + skipPrimary revert clean BEFORE PR creation.                                 |

</threat_model>

<verification>
- `grep -r "skipPrimary" server/ src/` returns 0 matches (full revert).
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits 0.
- `npm run build` exits 0.
- ADR-0010 has `## Phase 34 Sub-block` AFTER Phase 30.1 sub-block.
- Reliability doc has `## Multi-Provider Cascade (Phase 34, 2026-05-23)` BETWEEN Phase 30.1 and 7-Day Watch sections.
- CLAUDE.md has "Active providers (Phase 34 update)" line; 1-3 new Redis registry entries.
- ROADMAP Phase 34 marked ✓ Complete; all 5 plan stubs `[x]`.
- REQUIREMENTS LLM-RELI-08..11 all `[x]`.
- STATE.md notes Phase 34 shipped.
- 34-SUMMARY.md committed with ≥50 lines.
- PR created; awaiting operator merge approval.
- Atomic per-decision commits present per CONTEXT D-25:
  - `revert(34): remove temporary skipPrimary validation flag (D-22)`
  - `docs(34): ADR-0010 sub-block — phase 34 cascade restoration (D-32)`
  - `docs(34): reliability-doc multi-provider cascade section (D-33)`
  - `docs(34): CLAUDE.md amendments — active providers + Redis registry (D-31)`
  - `docs(34): ROADMAP/REQUIREMENTS/STATE flips for phase close (D-24)`
  - `docs(34): phase close — 34-SUMMARY.md (D-24)`
</verification>

<success_criteria>

- Temporary skipPrimary validation flag fully reverted (server-side code + tests).
- ADR-0010 has Phase 34 sub-block recording probe percentages + restoration outcome.
- Reliability doc has Multi-Provider Cascade (Phase 34) section between Phase 30.1 and 7-Day Watch sections.
- CLAUDE.md "Active providers" line updated; 1-3 new Redis registry entries (D-31 invariant: ≤10 lines diff).
- ROADMAP Phase 34 marked ✓ Complete; REQUIREMENTS LLM-RELI-08..11 flipped to `[x]`; STATE.md notes phase shipped.
- 34-SUMMARY.md emitted per project convention.
- PR created and ready for operator review + merge.
- LLM-RELI-08, LLM-RELI-09, LLM-RELI-10, LLM-RELI-11 all satisfied at phase close.
  </success_criteria>

<output>
After Task 7's checkpoint approval, the executor returns the PR URL to the operator. No additional artifacts beyond the 34-SUMMARY.md created in Task 6 are required from this plan.
</output>

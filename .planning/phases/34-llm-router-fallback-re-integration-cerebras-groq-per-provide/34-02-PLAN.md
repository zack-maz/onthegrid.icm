---
phase: 34-llm-router-fallback-re-integration-cerebras-groq-per-provide
plan: 02
type: execute
wave: 2
depends_on: ['34-01']
files_modified:
  - server/lib/freeClaudeRouter.ts
  - server/__tests__/lib/freeClaudeRouter.test.ts
  - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
autonomous: false
requirements: [LLM-RELI-09]
tags: [llm, router, cascade, adapter, cerebras, groq]

must_haves:
  truths:
    - 'Plan 34-01 probe outcome IS the gate — adapter restoration scope is mechanically derived from `.byProvider.{provider}.summary.decision` (CONTEXT D-02).'
    - "For each provider with `decision: 'integrate'`: `FreeProvider` union extended, lazy client init present, rate-limit gate present, slot in cascade builder present, Redis daily-token key writes, `RollingWindow` constraint enforced."
    - "Cerebras `RollingWindow(5, 60_000)` per RESEARCH §1.1 (NOT 30 — CONTEXT D-08's ~30 assumption is wrong)."
    - 'Groq `RollingWindow(30, 60_000)` + `GROQ_DAILY_CAP = 1000` per RESEARCH §1.2.'
    - "Cerebras + Groq's lazy clients return `null` when API keys absent (mirrors existing NIM/OR pattern) — cascade pre-check emits `skipped:no_client` rather than crashing."
    - 'Per-provider fall-through tests assert ordered cascade behavior (NIM → secondary-by-latency-p50 → OpenRouter dormant slot).'
    - "If BOTH providers `decision != 'integrate'` from Plan 34-01: this plan collapses to a single doc-only commit recording `cerebras-groq-deferred` in ADR-0010 sub-block; no adapter code lands. Plan 34-03 still ships."
    - "Branch `feature/34-router-fallback-reintegration` cut from `main` at THIS plan's execution start per CONTEXT D-26 (Plan 34-01's artifacts already on main as scaffold)."
    - 'Operator confirms `CEREBRAS_API_KEY` + `GROQ_API_KEY` populated in Vercel prod env Settings → Environment Variables (Production) BEFORE the branch merges per RESEARCH §5.6 (otherwise cascade silently no-ops to NIM-only and only `callHistory` reveals the gap).'
  artifacts:
    - path: 'server/lib/freeClaudeRouter.ts'
      provides: 'Extended `FreeProvider` union; lazy clients + rate-limit gates + cascade slots for each passing provider; per-provider Redis daily-token key writers.'
      contains: 'cerebras'
    - path: 'server/__tests__/lib/freeClaudeRouter.test.ts'
      provides: 'Per-provider fall-through tests (NIM 429 → cerebras row; cerebras daily-cap → groq row; all-fail → null content).'
      contains: 'Phase 34 cascade fall-through'
    - path: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      provides: "If both providers defer: doc-only `cerebras-groq-deferred` close-out (ADR sub-block); else: sub-block deferred to Plan 34-05's final close commit."
      contains: 'Phase 34'
  key_links:
    - from: 'server/lib/freeClaudeRouter.ts:34'
      to: 'FreeProvider union'
      via: 'type literal extension'
      pattern: "FreeProvider = 'nvidia_nim' \\| 'openrouter' \\| 'cerebras' \\| 'groq'"
    - from: 'server/lib/freeClaudeRouter.ts (allProviders array)'
      to: 'cascade execution loop'
      via: 'ordered insertion per probe latencyP50 ascending'
      pattern: "name: 'cerebras'|name: 'groq'"
    - from: 'server/lib/freeClaudeRouter.ts (per-provider Redis writers)'
      to: 'llm:tokens:{cerebras,groq}:YYYY-MM-DD'
      via: 'INCR with 48h TTL mirroring `incrOpenRouterDaily()`'
      pattern: 'llm:tokens:cerebras|llm:tokens:groq'
---

<objective>
Restore Cerebras + Groq adapters in `server/lib/freeClaudeRouter.ts` for each provider that passed Plan 34-01's probe gate (`decision === 'integrate'`). Add per-provider rate-limit gates, daily-token Redis keys, and cascade-builder slots. Add per-provider fall-through tests.

Purpose: Widens the cascade so NIM throttle events stop translating into DLQ entries. Per CONTEXT D-02, the probe-snapshot's `decision` field per provider IS the gate — if both defer, this plan collapses to a doc-only commit and the empirical "free-tier throttle correlated with NIM" finding is itself the deliverable (matches Phase 30.1 `nim-only` precedent).
Output: Updated `freeClaudeRouter.ts` with restored adapter(s), new fall-through tests, branch `feature/34-router-fallback-reintegration` cut from main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-RESEARCH.md
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json
@.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-01-SUMMARY.md
@server/lib/freeClaudeRouter.ts
@server/lib/llmCircuitBreaker.ts
@server/lib/llmTokenBudget.ts
@server/__tests__/lib/freeClaudeRouter.test.ts

<interfaces>
<!-- Contracts the executor needs from existing code. -->

From server/lib/freeClaudeRouter.ts (verified file:line locations):

- Line 34: `export type FreeProvider = 'nvidia_nim' | 'openrouter';` — D-07 widens this.
- Line 61: `const NVIDIA_NIM_BASE = 'https://integrate.api.nvidia.com/v1';` — D-08 adds `CEREBRAS_BASE` + `GROQ_BASE` nearby.
- Line 129: `export const OPENROUTER_DEFAULT_MODEL = 'meta-llama/llama-3.3-70b-instruct:free';` — D-05 adds `CEREBRAS_DEFAULT_MODEL` + `GROQ_DEFAULT_MODEL` in same style.
- Line 132: `const OPENROUTER_DAILY_CAP = 200;` — D-10 adds `GROQ_DAILY_CAP = 1000` (per RESEARCH §1.2; Cerebras skips daily cap per RESEARCH §1.1 recommendation).
- Lines 160-187: `class RollingWindow(maxRequests, windowMs)` constructor signature; usage pattern at line 165 (`nvidiaNimWindow = new RollingWindow(40, 60_000)`).
- Lines 208-224: `function getNvidiaNimClient(): OpenAI | null { ... new OpenAI({ apiKey, baseURL: NVIDIA_NIM_BASE, ... }) }` — D-08 mirrors for `getCerebrasClient()` + `getGroqClient()`.
- Lines 287-306: `async function incrOpenRouterDaily(): Promise<number>` + `getOpenRouterDaily()` — D-10 mirrors for `incr/getGroqDaily()`. Pattern: `const key = \`llm:tokens:openrouter:${todayKey()}\`; await redis.incr(key); await redis.expire(key, 48 \* 3600);`.
- Lines 349-360: `const allProviders: Array<{ name: FreeProvider; model: string; client: OpenAI | null }> = [...]` — D-08 adds provider entries; D-09 orders by probe latencyP50 ascending.
- Lines 382-417: pre-check gates (no_client / breaker / rate_limit_window / daily_cap). D-08 lands new branches here for each restored provider.
- Line 441: `if (p.name === 'openrouter') await incrOpenRouterDaily();` — Groq mirror writes the daily counter inside the success branch.
- Line 520: existing `callHistory[].provider` literal carries the provider name — D-12 INHERITS (no code change needed for attribution; LLM-RELI-09 SC satisfied by existing instrumentation).

From server/lib/llmCircuitBreaker.ts:18, 31-32 — **`Provider` union and per-provider state already include `cerebras` + `groq` (Phase 29 preserved)**. CONTEXT D-11: NO breaker-config change in this plan.

From server/lib/llmTokenBudget.ts:31-46 — `DAILY_LIMITS` already includes `cerebras: 1_000_000` and `groq: 200_000` slots. CONTEXT D-10's Redis writes integrate with this module's existing patterns; do NOT modify `DAILY_LIMITS`.

From server/lib/llmProgress.ts (lines 86, 199, 285, 337, 357) — `callHistory.provider`, `rateLimit`, `errorTaxonomy`, `tokenCounters` ALL already enumerate all four providers (Phase 29 preserved). NO edits to llmProgress.ts in this plan.

From server/**tests**/lib/freeClaudeRouter.test.ts:262-283 — existing P3 fall-through test using `vi.useFakeTimers()` + BACKOFF advance. Phase 34's new tests mirror this pattern (CONTEXT D-27).

From server/config.ts:31-32 — `CEREBRAS_API_KEY` + `GROQ_API_KEY` already validated by `parseEnv()` (default empty string). NO env-var work in this plan.
</interfaces>

<probe_gate_protocol>
**At Task 0 (gating decision), the executor reads `.planning/phases/34-.../probe-snapshot.json` and branches based on `byProvider.{provider}.summary.decision`:**

| cerebras decision                         | groq decision                             | Plan 34-02 scope                                                                                                                                                                                                                                                                                 |
| ----------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `'integrate'`                             | `'integrate'`                             | FULL restoration both providers (Tasks 1-5 all run).                                                                                                                                                                                                                                             |
| `'integrate'`                             | `'defer'` or `'middle-bucket-defer'`      | Cerebras-only restoration (Task 2 cerebras-only; Task 3 skipped; Task 4 runs partial — only the cerebras + cascade-exhausted tests; Task 5 runs partial).                                                                                                                                        |
| `'defer'` or `'middle-bucket-defer'`      | `'integrate'`                             | Groq-only restoration (Task 2 skipped; Task 3 runs; Task 4 runs partial; Task 5 runs partial).                                                                                                                                                                                                   |
| both `'defer'` or `'middle-bucket-defer'` | both `'defer'` or `'middle-bucket-defer'` | **DOC-ONLY collapse** — Tasks 1-5 ALL SKIPPED; only Task 6 runs (ADR-0010 sub-block records `cerebras-groq-deferred` close-out with per-provider percentages). Plan 34-03 STILL ships (per-provider eval scoring is meaningful even single-provider per CONTEXT "Conditional on Probe Outcome"). |

**Middle-bucket (50-90%) treats as defer for THIS plan** — CONTEXT D-02 states middle-bucket "snapshot ships; phase records the per-provider deferral; Phase 35+ revisits". The phase-34 cascade restoration is binary per provider; middle-bucket is recorded but the provider does NOT land in v3 cascade this phase.
</probe_gate_protocol>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Probe-gate checkpoint + branch cut + Vercel env-key UAT</name>
  <files>n/a (human checkpoint — operator UAT only)</files>
  <action>
**Human checkpoint — operator action required.**

Plan 34-01's probe artifact gates this plan's scope. Operator confirms (a) which providers passed `decision === 'integrate'`, (b) the Vercel prod env keys are populated, and (c) the new branch is cut from main.

**Steps:**

1. **Probe gate read:**

   ```bash
   jq -r '.byProvider | to_entries[] | "\(.key): \(.value.summary.decision) (rateLimitedPct=\(.value.summary.rateLimitedPct), p50=\(.value.summary.latencyP50)ms)"' \
     .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json
   ```

   Reports per-provider decision + ordering signal.

2. **Branch cut from main:**

   ```bash
   git checkout main
   git pull origin main
   git checkout -b feature/34-router-fallback-reintegration
   ```

   Per CONTEXT D-26 — branch cut happens at the start of this plan's execution (Plan 34-01's probe artifact + script + package.json sit on main as scaffold; this plan is the first code-change commit).

3. **Vercel prod env-key UAT** (RESEARCH §5.6):
   - Visit https://vercel.com/<team>/otg-iran-monitor/settings/environment-variables
   - Confirm `CEREBRAS_API_KEY` row exists with `Production` checked and a non-empty Encrypted value.
   - Confirm `GROQ_API_KEY` row exists with `Production` checked and a non-empty Encrypted value.
   - **Failure mode if either is missing:** the restored cascade will silently no-op to NIM-only; `getCerebrasClient()` / `getGroqClient()` return null; pre-check at `freeClaudeRouter.ts:382-417` emits `reason: 'skipped:no_client'` on every batch. No DLQ entries surface this gap; only `callHistory` reveals it.

4. **Approve gating decision:** Tell the executor which restoration path to take (BOTH / CEREBRAS-ONLY / GROQ-ONLY / DOC-ONLY collapse). The executor will skip irrelevant subtasks based on this decision.

**Resume signal:** Reply with one of: "BOTH integrate" / "CEREBRAS only" / "GROQ only" / "DOC-ONLY collapse" — and confirm branch is cut + Vercel keys populated.
</action>
<verify>
<automated>echo "Human checkpoint — see action block for verification steps; operator confirms via resume signal"; true</automated>
</verify>
<done>Operator approves via resume signal per the action block; execution unpauses.</done>
<what-built>
Plan 34-01's probe artifact gates this plan's scope. Operator confirms (a) which providers passed `decision === 'integrate'`, (b) the Vercel prod env keys are populated, and (c) the new branch is cut from main.
</what-built>
<how-to-verify>

1. **Probe gate read:**

   ```bash
   jq -r '.byProvider | to_entries[] | "\(.key): \(.value.summary.decision) (rateLimitedPct=\(.value.summary.rateLimitedPct), p50=\(.value.summary.latencyP50)ms)"' \
     .planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json
   ```

   Reports per-provider decision + ordering signal.

2. **Branch cut from main:**

   ```bash
   git checkout main
   git pull origin main
   git checkout -b feature/34-router-fallback-reintegration
   ```

   Per CONTEXT D-26 — branch cut happens at the start of this plan's execution (Plan 34-01's probe artifact + script + package.json sit on main as scaffold; this plan is the first code-change commit).

3. **Vercel prod env-key UAT** (RESEARCH §5.6):
   - Visit https://vercel.com/<team>/otg-iran-monitor/settings/environment-variables
   - Confirm `CEREBRAS_API_KEY` row exists with `Production` checked and a non-empty Encrypted value.
   - Confirm `GROQ_API_KEY` row exists with `Production` checked and a non-empty Encrypted value.
   - **Failure mode if either is missing:** the restored cascade will silently no-op to NIM-only; `getCerebrasClient()` / `getGroqClient()` return null; pre-check at `freeClaudeRouter.ts:382-417` emits `reason: 'skipped:no_client'` on every batch. No DLQ entries surface this gap; only `callHistory` reveals it.

4. **Approve gating decision:** Tell the executor which restoration path to take (BOTH / CEREBRAS-ONLY / GROQ-ONLY / DOC-ONLY collapse). The executor will skip irrelevant subtasks based on this decision.
   </how-to-verify>
   <resume-signal>Reply with one of: "BOTH integrate" / "CEREBRAS only" / "GROQ only" / "DOC-ONLY collapse" — and confirm branch is cut + Vercel keys populated.</resume-signal>
   </task>

<task type="auto" tdd="false">
  <name>Task 1: Extend `FreeProvider` union + constants (run if ANY provider passes)</name>
  <files>server/lib/freeClaudeRouter.ts</files>
  <read_first>
    - server/lib/freeClaudeRouter.ts (lines 1-100, 125-225, 280-310, 345-420)
    - .planning/phases/34-.../probe-snapshot.json (for D-09 cascade-ordering decision)
    - .planning/phases/34-.../34-CONTEXT.md D-07, D-08, D-09, D-10
    - .planning/phases/34-.../34-RESEARCH.md §1.1 (Cerebras 5 RPM correction) + §1.2 (Groq config)
  </read_first>
  <action>
Make ALL of the following edits to `server/lib/freeClaudeRouter.ts` in ONE commit (per CONTEXT D-25 atomic-per-decision; this commit covers D-07 + the shared scaffolding for D-08). Provider-specific lazy clients + RollingWindows + Redis writers land in Tasks 2 + 3 as separate commits.

1. **Line 34 — widen `FreeProvider` union:**
   - Change `export type FreeProvider = 'nvidia_nim' | 'openrouter';`
   - To `export type FreeProvider = 'nvidia_nim' | 'openrouter' | 'cerebras' | 'groq';`
   - Per CONTEXT D-07. Note: `server/lib/llmCircuitBreaker.ts:18` `Provider` type ALREADY widened (Phase 29 vestige); D-11 inherits.

2. **Lines 61-63 — add provider base URLs alongside `NVIDIA_NIM_BASE`:**
   - Add `const CEREBRAS_BASE = 'https://api.cerebras.ai/v1';`
   - Add `const GROQ_BASE = 'https://api.groq.com/openai/v1';`
   - Per CONTEXT D-08 step 1.

3. **Line 129 area — add exported model constants alongside `OPENROUTER_DEFAULT_MODEL`:**
   - Add `export const CEREBRAS_DEFAULT_MODEL = 'qwen-3-235b-a22b-instruct-2507';` (RESEARCH §1.1 Path A LOCK — CONTEXT D-05's `llama3.3-70b` is stale/no-longer-on-free-tier).
   - Add `export const GROQ_DEFAULT_MODEL = 'llama-3.3-70b-versatile';` (CONTEXT D-05 + RESEARCH §1.2).
   - Export so future scripts / probes import the same literal (mirrors `OPENROUTER_DEFAULT_MODEL` export pattern per CONTEXT canonical refs §"Established Patterns").

4. **Line 132 area — add daily cap for Groq alongside `OPENROUTER_DAILY_CAP`:**
   - Add `const GROQ_DAILY_CAP = 1000;` (RESEARCH §1.2 conservative starting value; well under Groq's 14,400 RPD ceiling; Phase 35 registry sweep can tune).
   - **Do NOT** add a Cerebras daily cap. RESEARCH §1.1 recommends RPM-only gate for Cerebras v1; rely on 429 retry loop to surface TPM breaches; Phase 35+ can add TPM/TPD if needed.

5. **Skeleton for downstream tasks (no functional change yet):**
   - Add JSDoc comment block above line 208 (`function getNvidiaNimClient()`) noting `// Phase 34 D-08 — getCerebrasClient + getGroqClient land in subsequent commits per atomic discipline.`
   - This commit (Task 1) must compile cleanly with NO new provider entries in `allProviders[]` yet. The union widening is forward-compat — adding `'cerebras' | 'groq'` to the type doesn't break existing usage because no code paths produce them yet.

**Commit message (per CONTEXT D-25):** `feat(34): extend FreeProvider union + provider constants (D-07, D-08 scaffolding)`. Commit body cites the per-provider model lock rationale (RESEARCH §1.1 Cerebras model correction, RESEARCH §1.2 Groq confirmation).

**Verification before commit:** `npx tsc --noEmit` exits 0 (the union extension is forward-compat; no existing `switch (p.name)` exhaustiveness checks should break — verify by `grep -n "switch.*p\\.name\\|switch.*provider" server/lib/freeClaudeRouter.ts` and ensuring no `never`-typed default arm).
</action>
<verify>
<automated>grep -q "'nvidia_nim' | 'openrouter' | 'cerebras' | 'groq'" server/lib/freeClaudeRouter.ts && grep -q "CEREBRAS_BASE" server/lib/freeClaudeRouter.ts && grep -q "GROQ_BASE" server/lib/freeClaudeRouter.ts && grep -q "CEREBRAS_DEFAULT_MODEL" server/lib/freeClaudeRouter.ts && grep -q "GROQ_DEFAULT_MODEL" server/lib/freeClaudeRouter.ts && grep -q "qwen-3-235b-a22b-instruct-2507" server/lib/freeClaudeRouter.ts && grep -q "llama-3.3-70b-versatile" server/lib/freeClaudeRouter.ts && grep -q "GROQ_DAILY_CAP" server/lib/freeClaudeRouter.ts && npx tsc --noEmit</automated>
</verify>
<acceptance_criteria> - `grep -q "'nvidia_nim' | 'openrouter' | 'cerebras' | 'groq'" server/lib/freeClaudeRouter.ts`. - `grep -q "CEREBRAS_BASE = 'https://api.cerebras.ai/v1'" server/lib/freeClaudeRouter.ts`. - `grep -q "GROQ_BASE = 'https://api.groq.com/openai/v1'" server/lib/freeClaudeRouter.ts`. - `grep -q "CEREBRAS_DEFAULT_MODEL = 'qwen-3-235b-a22b-instruct-2507'" server/lib/freeClaudeRouter.ts` (RESEARCH §1.1 model LOCK). - `grep -q "GROQ_DEFAULT_MODEL = 'llama-3.3-70b-versatile'" server/lib/freeClaudeRouter.ts` (CONTEXT D-05 + RESEARCH §1.2). - `grep -q "GROQ_DAILY_CAP = 1000" server/lib/freeClaudeRouter.ts`. - `npx tsc --noEmit` exits 0 (no TS errors). - `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 (no regression to existing tests). - `git log --oneline -1 server/lib/freeClaudeRouter.ts` shows commit message matching `feat(34): extend FreeProvider union`.
</acceptance_criteria>
<done>Union widened; per-provider model + base-URL constants exported; TS compiles; existing tests still pass. Tasks 2-3 layer provider-specific code on top.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore Cerebras adapter (run iff cerebras decision === 'integrate')</name>
  <files>server/lib/freeClaudeRouter.ts, server/__tests__/lib/freeClaudeRouter.test.ts</files>
  <read_first>
    - server/lib/freeClaudeRouter.ts (CURRENT — post-Task-1 state; lines 60-65, 125-135, 160-187, 200-230, 280-310, 345-420, 440-450, 600-625)
    - server/__tests__/lib/freeClaudeRouter.test.ts (FULL — locate the P3 fall-through test at lines 262-283 as template)
    - .planning/phases/34-.../34-CONTEXT.md D-08 (steps 2-6 — adapter slot pattern), D-09 (cascade ordering), D-12 (callHistory attribution inherited)
    - .planning/phases/34-.../34-RESEARCH.md §1.1 (5 RPM constraint, RPM-only gate, JSON dev-pass verification), §1.3 (response_format unknowns)
    - .planning/phases/34-.../probe-snapshot.json (for cascade ordering decision — Cerebras placement in allProviders[] is by latencyP50 ascending per D-09)
  </read_first>
  <behavior>
    - Test 1: With NIM rate-limit-window full AND breaker tripped on `nvidia_nim`, `callLLM` returns a `routing` array containing a `cerebras` entry with `reason: 'fall_through:nvidia_nim_rate_limit'` or equivalent NIM-failure reason.
    - Test 2: With `CEREBRAS_API_KEY` empty, `getCerebrasClient()` returns null; cascade pre-check produces a `reason: 'skipped:no_client'` for the cerebras entry (no crash; `callHistory[].provider === 'cerebras'` with the skipped reason).
    - Test 3: Cerebras `RollingWindow(5, 60_000)` blocks the 6th call within a 60s window; cascade falls through to the next provider with `reason: 'rate_limit_window'` (or equivalent).
  </behavior>
  <action>
Add all Cerebras-specific edits to `server/lib/freeClaudeRouter.ts` in ONE commit (CONTEXT D-25 atomic-per-decision; this commit lands the Cerebras-specific portion of D-08). Then add Cerebras-specific tests in a separate test commit.

**Production code commit (`feat(34): re-enable cerebras fallback in v3 cascade (D-07..D-12)`):**

1. **After line 165 (`nvidiaNimWindow = new RollingWindow(40, 60_000)`):**
   - Add `const cerebrasWindow = new RollingWindow(5, 60_000);` per RESEARCH §1.1 (5 RPM, NOT 30 — CONTEXT D-08's ~30 assumption corrected).

2. **After `getNvidiaNimClient()` at line 208-224:**
   - Add `function getCerebrasClient(): OpenAI | null` mirroring the NIM lazy-init pattern verbatim:
     - Read `apiKey` from `env.CEREBRAS_API_KEY` (already in `parseEnv()` per server/config.ts:31).
     - Return `null` if `!apiKey` (mirrors NIM's empty-key handling).
     - Return `new OpenAI({ apiKey, baseURL: CEREBRAS_BASE, /* timeout/maxRetries match NIM client */ })`.
     - Wrap in `try/catch` matching NIM pattern (logs `logger.child({ module: 'router-cerebras' }).warn(...)` on init failure; returns null).

3. **In `allProviders[]` at lines 349-360 — provider entry insertion:**
   - Insert `{ name: 'cerebras', model: CEREBRAS_DEFAULT_MODEL, client: getCerebrasClient() }` AFTER the NIM entry and BEFORE the OpenRouter entry.
   - Final ordering per CONTEXT D-09: NIM (primary) → cerebras → groq (if both passing) → openrouter (dormant). If only Cerebras passing: NIM → cerebras → openrouter. If Cerebras's `latencyP50` from probe-snapshot is HIGHER than Groq's (when both integrate), order Groq before Cerebras. The Task 3 (Groq) executor reconciles final ordering when both pass.

4. **In pre-check gates at lines 382-417 — add Cerebras-specific gate:**
   - After the OpenRouter daily-cap check at line 409, add:
     ```typescript
     if (p.name === 'cerebras' && !cerebrasWindow.allow()) {
       return buildReason('rate_limit_window');
     }
     ```
   - The existing `!p.client` and `!isAvailable(p.name)` checks (above line 409) cover the `skipped:no_client` and `breaker` cases automatically — no Cerebras-specific code needed there (the loop over `allProviders[]` handles it generically).

5. **No daily-counter increment for Cerebras** in the success branch (RESEARCH §1.1 recommendation — skip TPM/TPD checks in v1; RPM-only gate is sufficient). The OpenRouter `incrOpenRouterDaily()` pattern at line 441 is NOT mirrored for Cerebras.

6. **JSON-mode dev-pass verification (RESEARCH §1.3):**
   - Inside `callLLM`, locate the provider-specific request shape (search for `response_format: { type: 'json_object' }` or equivalent — currently the v3 extractor passes this).
   - Add a runtime conditional: if the provider is `'cerebras'` AND a single-event response returns 400 with `'response_format'` substring in the error message (RESEARCH §1.3 canary), catch the 400, retry the call WITHOUT `response_format`, AND add a system-prompt suffix instructing the model to respond with JSON only. Implement as a defensive code path; do NOT remove `response_format` unconditionally (NIM + OR honor it).
   - Alternative if 400-retry adds too much complexity: simpler approach is to NEVER pass `response_format` to Cerebras (provider-conditional spread in request shape: `...(p.name === 'cerebras' ? {} : { response_format: { type: 'json_object' } })`) and ALWAYS append the JSON-only instruction to the system prompt at the call site for that provider. Pick whichever path leaves cleaner diff hygiene; executor's discretion (CONTEXT discretion bullet).
   - Add a one-time dev call task as a manual verification step in the commit body: `node --env-file=.env.local --import tsx/esm -e "import('./server/lib/freeClaudeRouter.js').then(async m => { const r = await m.callLLM({ messages: [{ role: 'system', content: 'You are a JSON extractor' }, { role: 'user', content: 'Respond with {\"ok\": true}' }], skipOpenRouter: true, /* force cerebras path via NIM skip mechanism */ }); console.log(r); })"`. The executor runs this once locally and captures the success/failure in the commit body.

7. **CallHistory inherits attribution automatically** — line 520's existing `callHistory` row population works because `p.name` is already strongly-typed `FreeProvider`. CONTEXT D-12 is satisfied without code change.

**Verification before commit:**

- `npx tsc --noEmit` exits 0.
- Dev call against Cerebras with `qwen-3-235b-a22b-instruct-2507` returns either a valid JSON object OR a 400 that triggers the response_format-fallback path described above.

**Test commit (`test(34): cerebras fall-through + pre-check tests (D-27)`):**

In `server/__tests__/lib/freeClaudeRouter.test.ts`, add a new `describe('Phase 34 cascade fall-through', () => { ... })` block at the end of the file. Inside, add 3 tests mirroring the existing P3 pattern at lines 262-283 (use `vi.useFakeTimers()` for BACKOFF advance):

a) **"NIM 429 → cerebras fall_through row appears in routing"**: Mock `getNvidiaNimClient()` to throw a 429-shaped error on first call. Mock `getCerebrasClient()` to return a stub that throws (so the cascade also tries Groq/null content depending on which other providers are present). Assert `routing` array contains an entry where `provider === 'cerebras'` and `reason` begins with `'fall_through:nvidia_nim_'`.

b) **"Cerebras with no API key → skipped:no_client and falls through"**: Stub `env.CEREBRAS_API_KEY = ''`. Verify cascade emits `provider: 'cerebras', reason: 'skipped:no_client'` and proceeds to the next provider without crashing.

c) **"Cerebras `RollingWindow(5, 60_000)` blocks 6th call → rate_limit_window"**: Use `vi.useFakeTimers()`. Advance through 5 successful Cerebras calls within a 60s window; assert the 6th call's routing entry has `reason: 'rate_limit_window'`.

Test commit body: list each test name + the corresponding D-27 invariant.
</action>
<verify>
<automated>grep -q "cerebrasWindow = new RollingWindow(5, 60_000)" server/lib/freeClaudeRouter.ts && grep -q "function getCerebrasClient" server/lib/freeClaudeRouter.ts && grep -q "name: 'cerebras'" server/lib/freeClaudeRouter.ts && grep -q "Phase 34 cascade fall-through" server/**tests**/lib/freeClaudeRouter.test.ts && npx vitest run server/**tests**/lib/freeClaudeRouter.test.ts</automated>
</verify>
<acceptance_criteria> - `grep -q "cerebrasWindow = new RollingWindow(5, 60_000)" server/lib/freeClaudeRouter.ts` (RESEARCH §1.1 5 RPM ceiling, NOT 30). - `grep -q "function getCerebrasClient(): OpenAI | null" server/lib/freeClaudeRouter.ts`. - `grep -q "name: 'cerebras'" server/lib/freeClaudeRouter.ts` (provider entry in allProviders[]). - `grep -q "Phase 34 cascade fall-through" server/__tests__/lib/freeClaudeRouter.test.ts`. - `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 with all 3 new Phase 34 tests passing. - `npx tsc --noEmit` exits 0. - `git log --oneline -2 server/lib/freeClaudeRouter.ts | grep -E "feat\\(34\\): re-enable cerebras|test\\(34\\): cerebras fall-through"` returns 2 lines (production code commit + test commit per CONTEXT D-25). - **Manual:** Dev call against Cerebras returns valid JSON OR triggers the response_format-fallback path; result captured in production-code commit body.
</acceptance_criteria>
<done>Cerebras adapter lands with 5-RPM gate per RESEARCH; lazy client returns null on empty key; cascade pre-check emits `skipped:no_client` / `rate_limit_window` cleanly; 3 fall-through tests pass; JSON-mode dev-pass verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore Groq adapter (run iff groq decision === 'integrate')</name>
  <files>server/lib/freeClaudeRouter.ts, server/__tests__/lib/freeClaudeRouter.test.ts</files>
  <read_first>
    - server/lib/freeClaudeRouter.ts (CURRENT — post-Task-2 state)
    - server/__tests__/lib/freeClaudeRouter.test.ts (CURRENT — locate the `describe('Phase 34 cascade fall-through')` block added in Task 2)
    - .planning/phases/34-.../34-CONTEXT.md D-08 (Groq mirror), D-09 (cascade ordering), D-10 (per-provider Redis daily key)
    - .planning/phases/34-.../34-RESEARCH.md §1.2 (30 RPM, GROQ_DAILY_CAP=1000, JSON dev-pass verification)
    - .planning/phases/34-.../probe-snapshot.json (for D-09 cascade ordering relative to Cerebras when both pass)
  </read_first>
  <behavior>
    - Test 1: With Cerebras daily-cap hit (or Cerebras unavailable), Groq is reached and a `groq` row appears in `routing` with `reason: 'fall_through:cerebras_*'` or similar upstream-failure reason.
    - Test 2: With `GROQ_API_KEY` empty, `getGroqClient()` returns null; cascade pre-check produces `reason: 'skipped:no_client'`.
    - Test 3: With Groq daily counter at `GROQ_DAILY_CAP = 1000`, cascade pre-check emits `reason: 'daily_cap'` and falls through to the next provider.
    - Test 4: With all providers failing, `routingTrace` ends with the last-provider failure and `content === null` (this test is the gateway for Plan 34-04's `cascade_exhausted` DLQ wrapper).
  </behavior>
  <action>
Add all Groq-specific edits to `server/lib/freeClaudeRouter.ts` in ONE commit (CONTEXT D-25), then add tests in a separate test commit.

**Production code commit (`feat(34): re-enable groq fallback in v3 cascade (D-07..D-12)`):**

1. **After `cerebrasWindow` declaration (or, if Cerebras was skipped, after `nvidiaNimWindow`):**
   - Add `const groqWindow = new RollingWindow(30, 60_000);` per RESEARCH §1.2 (30 RPM for `llama-3.3-70b-versatile`).

2. **After `getCerebrasClient()` (or, if Cerebras was skipped, after `getNvidiaNimClient()`):**
   - Add `function getGroqClient(): OpenAI | null` mirroring the NIM lazy-init pattern: read `env.GROQ_API_KEY`; return null if empty; otherwise return `new OpenAI({ apiKey, baseURL: GROQ_BASE })`. Wrap in `try/catch` matching NIM pattern.

3. **After `getOpenRouterDaily()` at lines 287-306:**
   - Add `async function incrGroqDaily(): Promise<number>` and `async function getGroqDaily(): Promise<number>` mirroring the OpenRouter pattern exactly. Redis key shape: `\`llm:tokens:groq:${todayKey()}\``. 48h TTL. Per CONTEXT D-10 + RESEARCH §1.2.

4. **In `allProviders[]` at lines 349-360 — provider entry insertion:**
   - Insert `{ name: 'groq', model: GROQ_DEFAULT_MODEL, client: getGroqClient() }` per CONTEXT D-09 ordering.
   - **Cascade ordering reconciliation (D-09):**
     - If both Cerebras AND Groq passing: read `latencyP50` from `probe-snapshot.json` and order ascending. Smaller-p50 provider FIRST in cascade.
     - If only Groq passing: NIM → groq → openrouter (dormant).
     - **Commit body MUST cite the latencyP50 numbers** from the probe snapshot per CONTEXT D-09 ("Ordering decision is committed in the adapter PR's commit body with the latency p50 numbers from the probe snapshot — auditable forever, mechanically derived").

5. **In pre-check gates at lines 382-417 — add Groq-specific gates:**
   - After the Cerebras `rate_limit_window` check (or, if Cerebras skipped, after the OpenRouter daily-cap check at line 409), add:
     ```typescript
     if (p.name === 'groq' && !groqWindow.allow()) {
       return buildReason('rate_limit_window');
     }
     if (p.name === 'groq' && (await getGroqDaily()) >= GROQ_DAILY_CAP) {
       return buildReason('daily_cap');
     }
     ```

6. **In success branch at line 441 (`if (p.name === 'openrouter') await incrOpenRouterDaily();`):**
   - Add `if (p.name === 'groq') await incrGroqDaily();` parallel to the existing OpenRouter increment.

7. **In `recordHeadroom()` at line 614 area:**
   - Add `groq: { used: <getGroqDaily-cached>, cap: GROQ_DAILY_CAP, window: 'day', perModel: {} }` to the rate-limit headroom snapshot, mirroring the openrouter slot at line 614. Use the same async-cached pattern (or schedule the read; mirror however the OR slot does it).

8. **JSON-mode dev-pass verification (RESEARCH §1.3):**
   - Same approach as Task 2's step 6 — verify Groq honors `response_format: { type: 'json_object' }` via a one-time dev call. RESEARCH §1.2 says Groq is OpenAI-compatible but doesn't confirm response_format; verify. Capture outcome in commit body.

**Verification before commit:**

- `npx tsc --noEmit` exits 0.
- Dev call against Groq returns either valid JSON OR triggers the same response_format-fallback path.

**Test commit (`test(34): groq fall-through + daily-cap tests (D-27)`):**

Extend the `describe('Phase 34 cascade fall-through', () => { ... })` block in `server/__tests__/lib/freeClaudeRouter.test.ts` with 4 additional tests:

a) **"Cerebras daily-cap (or unavailable) → groq fall_through row appears"**: Mock Cerebras to throw or be unavailable. Assert `routing` contains `provider: 'groq', reason: /fall_through:(nvidia_nim|cerebras)_/`.

b) **"Groq with no API key → skipped:no_client and falls through"**: `env.GROQ_API_KEY = ''`. Assert `provider: 'groq', reason: 'skipped:no_client'`.

c) **"Groq daily counter at GROQ_DAILY_CAP → daily_cap pre-check"**: Mock `getGroqDaily()` to return `GROQ_DAILY_CAP`. Assert `provider: 'groq', reason: 'daily_cap'`. (Mirror the existing OR daily-cap test pattern if present in the file.)

d) **"All providers fail → routingTrace ends with last-provider failure, content === null"**: Mock NIM, Cerebras, AND Groq to all 429/throw. Assert `result.content === null` AND `routingTrace[routingTrace.length - 1].reason` matches `/fall_through:groq_/` (or last-provider-equivalent). This test is the BEHAVIORAL FOUNDATION for Plan 34-04's `cascade_exhausted` DLQ wrapper.

Test commit body: list each test + the D-27 invariant covered.
</action>
<verify>
<automated>grep -q "groqWindow = new RollingWindow(30, 60_000)" server/lib/freeClaudeRouter.ts && grep -q "function getGroqClient" server/lib/freeClaudeRouter.ts && grep -q "function incrGroqDaily" server/lib/freeClaudeRouter.ts && grep -q "function getGroqDaily" server/lib/freeClaudeRouter.ts && grep -q "name: 'groq'" server/lib/freeClaudeRouter.ts && grep -q "llm:tokens:groq:" server/lib/freeClaudeRouter.ts && npx vitest run server/**tests**/lib/freeClaudeRouter.test.ts</automated>
</verify>
<acceptance_criteria> - `grep -q "groqWindow = new RollingWindow(30, 60_000)" server/lib/freeClaudeRouter.ts` (RESEARCH §1.2 30 RPM). - `grep -q "function getGroqClient(): OpenAI | null" server/lib/freeClaudeRouter.ts`. - `grep -q "async function incrGroqDaily" server/lib/freeClaudeRouter.ts` AND `grep -q "async function getGroqDaily" server/lib/freeClaudeRouter.ts`. - `grep -q "llm:tokens:groq:" server/lib/freeClaudeRouter.ts` (per-provider Redis key per CONTEXT D-10). - `grep -q "name: 'groq'" server/lib/freeClaudeRouter.ts` (allProviders[] entry). - `grep -q "p.name === 'groq' && !groqWindow.allow()" server/lib/freeClaudeRouter.ts` (RPM pre-check). - `grep -q "p.name === 'groq' && (await getGroqDaily()) >= GROQ_DAILY_CAP" server/lib/freeClaudeRouter.ts` (daily-cap pre-check). - `grep -q "if (p.name === 'groq') await incrGroqDaily" server/lib/freeClaudeRouter.ts` (success-branch increment). - `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 with all Phase 34 fall-through tests passing (3 from Task 2 + 4 from this task = 7 minimum). - `npx tsc --noEmit` exits 0. - `git log --oneline -2 server/lib/freeClaudeRouter.ts | grep -E "feat\\(34\\): re-enable groq|test\\(34\\): groq fall-through"` returns 2 lines. - **Commit body MUST cite probe-snapshot's `latencyP50` for both Cerebras AND Groq (when both pass)** per CONTEXT D-09 — the ordering decision is mechanically derived. Verify via `git log -1 --format=%B HEAD~1 | grep -E "p50|latencyP50"`. - **Manual:** Dev call against Groq returns valid JSON OR triggers response_format-fallback path; result captured in commit body.
</acceptance_criteria>
<done>Groq adapter lands with 30-RPM gate + 1000-RPD cap + Redis daily counter; cascade ordering mechanically committed from probe latencyP50; 4 additional fall-through tests pass; JSON-mode dev-pass verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Validation gate before merge</name>
  <files>n/a (human checkpoint — operator UAT only)</files>
  <action>
**Human checkpoint — operator action required.**

Adapter restoration for passing provider(s) is complete and tested. Before merging `feature/34-router-fallback-reintegration` to main, verify:

1. Full test suite passes (no regression elsewhere).
2. Build succeeds.
3. The Vercel prod env keys are populated (re-confirmation of Task 0 — defensive check since merge implies prod deploy).

**Steps:**

1. **Full test suite:**

   ```bash
   npx vitest run
   ```

   Exit 0; no regressions. Phase 34 new tests visible in output (search for "Phase 34 cascade fall-through" in test output).

2. **Build:**

   ```bash
   npm run build
   ```

   Exits 0; no TS errors; `api/vercel-entry.js` regenerated.

3. **Vercel env-key re-check** (RESEARCH §5.6 — second confirmation since the first was at Task 0 which was probe-gate-only):

   ```bash
   # Verify via Vercel CLI if available, OR via dashboard
   vercel env ls production | grep -E "CEREBRAS_API_KEY|GROQ_API_KEY"
   ```

   Both keys must show `Encrypted` values in `production` environment. **If either missing, STOP — the restored cascade silently no-ops to NIM-only post-merge with no DLQ visibility.**

4. **Optional: Preview-deploy smoke** (highly recommended):
   ```bash
   vercel --prod=false  # Or push the feature branch to trigger Vercel preview
   ```
   Capture preview URL. Hit `GET <preview-url>/api/health` and verify endpoints respond. Plan 34-04 runs the full validation cron against this preview deployment.

**Resume signal:** Reply "validated — merge approved" or describe issues. On approval, Plan 34-03 begins.
</action>
<verify>
<automated>echo "Human checkpoint — see action block for verification steps; operator confirms via resume signal"; true</automated>
</verify>
<done>Operator approves via resume signal per the action block; execution unpauses.</done>
<what-built>
Adapter restoration for passing provider(s) is complete and tested. Before merging `feature/34-router-fallback-reintegration` to main, verify:

1. Full test suite passes (no regression elsewhere).
2. Build succeeds.
3. The Vercel prod env keys are populated (re-confirmation of Task 0 — defensive check since merge implies prod deploy).
   </what-built>
   <how-to-verify>
4. **Full test suite:**

   ```bash
   npx vitest run
   ```

   Exit 0; no regressions. Phase 34 new tests visible in output (search for "Phase 34 cascade fall-through" in test output).

5. **Build:**

   ```bash
   npm run build
   ```

   Exits 0; no TS errors; `api/vercel-entry.js` regenerated.

6. **Vercel env-key re-check** (RESEARCH §5.6 — second confirmation since the first was at Task 0 which was probe-gate-only):

   ```bash
   # Verify via Vercel CLI if available, OR via dashboard
   vercel env ls production | grep -E "CEREBRAS_API_KEY|GROQ_API_KEY"
   ```

   Both keys must show `Encrypted` values in `production` environment. **If either missing, STOP — the restored cascade silently no-ops to NIM-only post-merge with no DLQ visibility.**

7. **Optional: Preview-deploy smoke** (highly recommended):
   ```bash
   vercel --prod=false  # Or push the feature branch to trigger Vercel preview
   ```
   Capture preview URL. Hit `GET <preview-url>/api/health` and verify endpoints respond. Plan 34-04 runs the full validation cron against this preview deployment.
   </how-to-verify>
   <resume-signal>Reply "validated — merge approved" or describe issues. On approval, Plan 34-03 begins.</resume-signal>
   </task>

<task type="auto" tdd="false">
  <name>Task 5: DOC-ONLY collapse — ADR-0010 sub-block (run iff BOTH providers defer)</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (FULL — locate the most recent prior sub-block; Phase 30 + Phase 30.1 sub-blocks are templates)
    - .planning/phases/34-.../probe-snapshot.json (per-provider percentages + decisions for the ADR sub-block body)
    - .planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-CONTEXT.md (`nim-only` precedent for the doc-only honest-deferral pattern)
  </read_first>
  <action>
**THIS TASK ONLY RUNS IF Task 0's probe-gate decision was "DOC-ONLY collapse" (both providers `decision !== 'integrate'`).** Tasks 1-4 are skipped in that branch.

Append a new sub-block to `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` AFTER the most recent prior sub-block. Heading: `## Phase 34 Sub-block (appended 2026-MM-DD)`. Use today's date.

Content (per CONTEXT D-32, mirrored from Phase 30.1's `nim-only` sub-block style):

- **One-paragraph summary:** "Phase 34 ran the cerebras + groq probe per Plan 34-01 (`scripts/probe-cerebras-groq.ts`, snapshot at `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/probe-snapshot.json`). Both providers' free-tier rate-limit behavior fell into the defer or middle-bucket buckets. Phase closes with `cerebras-groq-deferred` status, matching Phase 30.1's `nim-only` precedent."
- **Per-provider numbers table:** Pull `rateLimitedCount`, `rateLimitedPct`, `latencyP50`, `latencyP95`, `decision` from probe-snapshot.json. Format as a markdown table:
  | Provider | Rate-limited | % | latency p50 | latency p95 | Decision |
  |----------|--------------|---|-------------|-------------|----------|
  | cerebras | <n>/30 | <pct>% | <p50>ms | <p95>ms | <decision> |
  | groq | <n>/30 | <pct>% | <p50>ms | <p95>ms | <decision> |
- **Rationale for honest deferral:** "Per CONTEXT.md D-02, the empirical 'free-tier throttle correlated with NIM' finding is itself a load-bearing outcome — the probe artifact + this ADR sub-block IS the deliverable. Re-litigating either provider is a separate decimal phase (Phase 35+ candidate)."
- **Pointer:** "See also `docs/architecture/llm-pipeline-reliability.md` § Multi-Provider Cascade (Phase 34) for the cascade-shape detail; Plan 34-05 adds that section in this same phase close-out."
- **Forward-pointer:** "Plan 34-03 (per-provider eval scoring) still ships — `providerProvenance: 'nvidia_nim'` is a meaningful single-provider observation that future probe re-runs can extend."

Commit message: `docs(34): cerebras-groq-deferred close-out (D-02, D-32)`. Body: cite per-provider percentages + decisions from probe-snapshot.

**Plan 34-03 proceeds regardless** (CONTEXT "Conditional on Probe Outcome" — per-provider eval scoring is meaningful even single-provider).
</action>
<verify>
<automated>grep -q "Phase 34 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md && grep -q "cerebras-groq-deferred" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</automated>
</verify>
<acceptance_criteria> - This task ONLY runs in the DOC-ONLY-collapse branch; skipped otherwise. - `grep -q "## Phase 34 Sub-block" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns 0 exit. - `grep -q "cerebras-groq-deferred" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`. - Markdown table in the sub-block contains per-provider `rateLimitedPct`, `latencyP50`, `latencyP95` from `probe-snapshot.json` (verified by grepping each value). - `git log -1 --format=%s HEAD` matches `docs(34): cerebras-groq-deferred close-out (D-02, D-32)`.
</acceptance_criteria>
<done>(In doc-only branch only) ADR-0010 has a Phase 34 sub-block recording the empirical close-out with per-provider numbers. Plan 34-02 closes; Plan 34-03 still runs.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                             |
| ----------------------------------- | ------------------------------------------------------- | --------- |
| serverless function → Cerebras API  | Outbound HTTPS using `CEREBRAS_API_KEY` from Vercel env |
| serverless function → Groq API      | Outbound HTTPS using `GROQ_API_KEY` from Vercel env     |
| serverless function → Upstash Redis | Per-provider daily counter writes (`llm:tokens:cerebras | groq:\*`) |
| llmCircuitBreaker → cascade         | Per-provider breaker state already preserved (Phase 29) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                            |
| --------- | ---------------------- | ------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-34-06   | Information disclosure | Cerebras/Groq API keys                                 | mitigate    | Keys read from `env.{provider}_API_KEY` validated by `parseEnv()`; never logged; lazy-client init catches errors without leaking key in stack traces.                                                                                      |
| T-34-07   | Denial of Service      | Cerebras 5-RPM provider spam                           | mitigate    | `RollingWindow(5, 60_000)` pre-check at line 382-417 emits `rate_limit_window` before the call; Phase 30 retry envelope unchanged (CONTEXT carrying-forward); on persistent 429, breaker trips per existing 30%-error-rate sliding window. |
| T-34-08   | Tampering              | Per-provider Redis daily counter race condition        | accept      | Upstash REST `INCR` is atomic; existing OpenRouter pattern at line 287-306 has run in prod since Phase 27.4 without observed races.                                                                                                        |
| T-34-09   | Elevation of privilege | Cascade order manipulation                             | mitigate    | `allProviders[]` ordering hard-coded; D-09 latencyP50 ordering committed in commit body per CONTEXT D-09 (auditable); no runtime ordering injection.                                                                                       |
| T-34-10   | Repudiation            | "Which provider extracted this event?"                 | mitigate    | `callHistory[].provider` (existing) records per-batch attribution; D-12 inherits without code change; SC-3 in Plan 34-04 verifies live.                                                                                                    |
| T-34-11   | Information disclosure | Restored cascade silently no-ops if Vercel keys absent | mitigate    | Task 0 + Task 4 checkpoints REQUIRE operator UAT of Vercel prod env-key presence (RESEARCH §5.6); failure mode documented in must_haves + checkpoint text.                                                                                 |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across all subtasks.
- `npx vitest run server/__tests__/lib/freeClaudeRouter.test.ts` exits 0 with Phase 34 fall-through tests passing.
- `npm run build` exits 0.
- `git log --oneline feature/34-router-fallback-reintegration` shows atomic per-decision commits per CONTEXT D-25:
  - `feat(34): extend FreeProvider union + provider constants (D-07, D-08 scaffolding)`
  - (if cerebras passing) `feat(34): re-enable cerebras fallback in v3 cascade (D-07..D-12)` + `test(34): cerebras fall-through + pre-check tests (D-27)`
  - (if groq passing) `feat(34): re-enable groq fallback in v3 cascade (D-07..D-12)` + `test(34): groq fall-through + daily-cap tests (D-27)`
  - (if both deferred) only `docs(34): cerebras-groq-deferred close-out (D-02, D-32)`
- Vercel prod env keys `CEREBRAS_API_KEY` + `GROQ_API_KEY` confirmed populated (Task 0 + Task 4 checkpoints).
</verification>

<success_criteria>

- For each provider with `decision === 'integrate'` in `probe-snapshot.json`:
  - `FreeProvider` union extended (LLM-RELI-09 satisfied for that provider).
  - Lazy client, RollingWindow, allProviders[] entry, pre-check gates, success-branch increment all present.
  - Per-provider Redis daily-token key writes (`llm:tokens:cerebras:YYYY-MM-DD` for Cerebras [via RPM-only gate — no daily counter per RESEARCH §1.1 recommendation], `llm:tokens:groq:YYYY-MM-DD` for Groq).
  - Cascade ordering mechanically derived from probe latencyP50 ascending; cited in commit body per CONTEXT D-09.
  - Per-provider fall-through tests pass.
- If both providers defer: ADR-0010 sub-block records `cerebras-groq-deferred` honest close-out with per-provider numbers; LLM-RELI-09's "honest deferral" branch satisfied.
- Branch `feature/34-router-fallback-reintegration` cut from main at this plan's execution start per CONTEXT D-26.
- Vercel prod env keys verified at Task 0 AND Task 4 checkpoints per RESEARCH §5.6.
- LLM-RELI-09 satisfied per the probe-gate outcome.
  </success_criteria>

<output>
Create `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-02-SUMMARY.md`. Include:
- Probe-gate outcome (which provider(s) restored / both deferred).
- For restored providers: cascade order with cited latencyP50 numbers.
- Vercel prod env-key confirmation status.
- Branch name and commit list.
- Forward signal for Plan 34-03 (per-provider eval ships regardless).
</output>

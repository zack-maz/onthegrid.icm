---
phase: 03-api-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/types.ts
  - server/config.ts
  - server/constants.ts
  - server/cache/entityCache.ts
  - server/middleware/errorHandler.ts
  - server/index.ts
  - server/__tests__/cache.test.ts
  - server/__tests__/server.test.ts
  - tsconfig.server.json
  - tsconfig.json
  - package.json
  - .gitignore
  - .env.example
  - src/types/entities.ts
autonomous: true
requirements:
  - INFRA-01

user_setup:
  - service: opensky
    why: "Flight tracking data source"
    env_vars:
      - name: OPENSKY_CLIENT_ID
        source: "OpenSky Network Dashboard -> API clients -> Create OAuth2 client"
      - name: OPENSKY_CLIENT_SECRET
        source: "OpenSky Network Dashboard -> API clients -> Create OAuth2 client"
    dashboard_config:
      - task: "Register account and create OAuth2 client credentials"
        location: "https://opensky-network.org -> Sign up -> Dashboard -> API Clients"
  - service: aisstream
    why: "Ship tracking data source"
    env_vars:
      - name: AISSTREAM_API_KEY
        source: "AISStream.io -> Sign in via GitHub -> Dashboard -> API Key"
    dashboard_config:
      - task: "Sign up and copy API key"
        location: "https://aisstream.io"
  - service: acled
    why: "Conflict event data source"
    env_vars:
      - name: ACLED_EMAIL
        source: "ACLED registration email"
      - name: ACLED_PASSWORD
        source: "ACLED account password"
    dashboard_config:
      - task: "Register for ACLED data access"
        location: "https://acleddata.com -> Register"

must_haves:
  truths:
    - "Express server starts and listens on port 3001"
    - "CORS headers allow requests from Vite dev origin (localhost:5173)"
    - "MapEntity discriminated union types are defined and exported"
    - "In-memory cache stores data with TTL and staleness tracking"
    - "Environment variables are validated at startup with clear error messages"
    - "npm run dev starts both Vite and Express concurrently"
  artifacts:
    - path: "server/types.ts"
      provides: "MapEntity discriminated union (FlightEntity, ShipEntity, ConflictEventEntity)"
      exports: ["MapEntity", "FlightEntity", "ShipEntity", "ConflictEventEntity", "EntityType", "MapEntityBase", "BoundingBox", "CacheResponse"]
    - path: "src/types/entities.ts"
      provides: "Re-export of MapEntity types for frontend consumption"
      exports: ["MapEntity", "FlightEntity", "ShipEntity", "ConflictEventEntity", "EntityType"]
    - path: "server/config.ts"
      provides: "Validated environment variable configuration"
      exports: ["config"]
    - path: "server/constants.ts"
      provides: "Iran bounding box and cache TTL values"
      exports: ["IRAN_BBOX", "CACHE_TTL"]
    - path: "server/cache/entityCache.ts"
      provides: "Generic in-memory cache with TTL and staleness"
      exports: ["EntityCache"]
    - path: "server/middleware/errorHandler.ts"
      provides: "Global Express error handling middleware"
      exports: ["errorHandler"]
    - path: "server/index.ts"
      provides: "Express 5 app entry point with CORS and route mounting"
      min_lines: 20
    - path: "tsconfig.server.json"
      provides: "TypeScript config targeting Node.js for server code"
      contains: "\"include\": [\"server\"]"
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "OPENSKY_CLIENT_ID"
  key_links:
    - from: "server/index.ts"
      to: "server/config.ts"
      via: "import at startup"
      pattern: "import.*config.*from.*config"
    - from: "server/index.ts"
      to: "cors"
      via: "CORS middleware with corsOrigin from config"
      pattern: "cors.*config\\.corsOrigin"
    - from: "server/cache/entityCache.ts"
      to: "server/types.ts"
      via: "CacheResponse type"
      pattern: "CacheResponse"
    - from: "package.json"
      to: "concurrently"
      via: "dev script running both vite and tsx"
      pattern: "concurrently.*vite.*tsx"
---

<objective>
Create the Express 5 server foundation: TypeScript types (MapEntity discriminated union), environment config with validation, in-memory cache with staleness tracking, error handling middleware, and the server entry point with CORS. Also install all server dependencies, set up tsconfig.server.json, update dev scripts to run Vite + Express concurrently, and protect .env from git.

Purpose: Establish the running server shell and all shared infrastructure that the three data adapters (Plan 02) will build against. Without this foundation, no adapter can be implemented.
Output: A running Express server on port 3001 with health endpoint, MapEntity types, cache module, config validation, and concurrent dev workflow.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-proxy/03-CONTEXT.md
@.planning/phases/03-api-proxy/03-RESEARCH.md

<interfaces>
<!-- Existing project patterns the executor needs -->

From src/types/ui.ts (TypeScript interface style to match):
```typescript
export interface UIState {
  isDetailPanelOpen: boolean;
  isCountersCollapsed: boolean;
  isFiltersExpanded: boolean;
  openDetailPanel: () => void;
  closeDetailPanel: () => void;
  toggleCounters: () => void;
  toggleFilters: () => void;
}
```

From src/stores/mapStore.ts (Zustand curried create pattern):
```typescript
import { create } from 'zustand';
interface MapState {
  isMapLoaded: boolean;
  cursorLng: number;
  cursorLat: number;
  setMapLoaded: () => void;
  setCursorPosition: (lng: number, lat: number) => void;
}
export const useMapStore = create<MapState>()((set) => ({
  isMapLoaded: false,
  cursorLng: 0,
  cursorLat: 0,
  setMapLoaded: () => set({ isMapLoaded: true }),
  setCursorPosition: (lng, lat) => set({ cursorLng: lng, cursorLat: lat }),
}));
```

From package.json (current scripts):
```json
{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  }
}
```

From tsconfig.json (current references):
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

From .gitignore (missing .env protection):
```
# No .env entry exists -- must be added
```

From vite.config.ts (test config -- server tests must NOT use jsdom):
```typescript
test: {
  environment: 'jsdom',
  setupFiles: ['./src/test/setup.ts'],
  globals: true,
  alias: { /* maplibre mocks */ },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, create MapEntity types, config, constants, and project configuration</name>
  <files>
    server/types.ts,
    src/types/entities.ts,
    server/config.ts,
    server/constants.ts,
    tsconfig.server.json,
    tsconfig.json,
    package.json,
    .gitignore,
    .env.example,
    server/__tests__/types.test.ts
  </files>
  <behavior>
    - MapEntity is a discriminated union of FlightEntity | ShipEntity | ConflictEventEntity
    - EntityType is 'flight' | 'ship' | 'missile' | 'drone'
    - All entities share MapEntityBase fields: id, type, lat, lng, timestamp, label
    - FlightEntity.data has icao24, callsign, originCountry, velocity, heading, altitude, onGround, verticalRate
    - ShipEntity.data has mmsi, shipName, speedOverGround, courseOverGround, trueHeading
    - ConflictEventEntity.data has eventType, subEventType, fatalities, actor1, actor2, notes, source
    - ConflictEventEntity.type is 'missile' | 'drone' (not 'conflict')
    - CacheResponse wraps data array with stale boolean and lastFresh timestamp
    - BoundingBox has south, north, west, east number fields
    - config.ts throws at import time if required env vars are missing (but use optional() for graceful dev startup -- see action)
    - IRAN_BBOX covers lat 25-40, lng 44-63.5
    - TypeScript compilation of server/ passes with tsconfig.server.json
  </behavior>
  <action>
    1. Install production dependencies: `npm install express cors`
    2. Install dev dependencies: `npm install -D @types/express @types/cors concurrently tsx`
    3. Create `server/types.ts` with the full MapEntity discriminated union per RESEARCH.md Pattern 1:
       - MapEntityBase interface with id, type, lat, lng, timestamp (Unix ms), label
       - EntityType = 'flight' | 'ship' | 'missile' | 'drone'
       - FlightEntity extends MapEntityBase with type: 'flight' and data object (icao24, callsign, originCountry, velocity|null, heading|null, altitude|null, onGround, verticalRate|null)
       - ShipEntity extends MapEntityBase with type: 'ship' and data object (mmsi, shipName, speedOverGround, courseOverGround, trueHeading)
       - ConflictEventEntity extends MapEntityBase with type: 'missile' | 'drone' and data object (eventType, subEventType, fatalities, actor1, actor2, notes, source)
       - MapEntity = FlightEntity | ShipEntity | ConflictEventEntity
       - BoundingBox interface with south, north, west, east (all numbers)
       - CacheResponse<T> interface with data: T, stale: boolean, lastFresh: number
       - Export everything
    4. Create `src/types/entities.ts` that re-exports the types from server/types.ts. Use a relative path import `../../server/types.js` since both are in the same repo. This gives frontend code access to entity types without duplicating them.
    5. Create `server/config.ts` per RESEARCH.md code example. Use a `required()` helper that throws if env var is missing. BUT: wrap the entire config object creation in a function `loadConfig()` that is called lazily (not at module import) so tests can run without env vars. Export both the lazy `loadConfig` and a `config` getter that caches the result. Structure:
       - port: Number(process.env.PORT ?? 3001)
       - corsOrigin: process.env.CORS_ORIGIN ?? 'http://localhost:5173'
       - opensky: { clientId, clientSecret } (required)
       - aisstream: { apiKey } (required)
       - acled: { email, password } (required)
    6. Create `server/constants.ts`:
       - IRAN_BBOX: BoundingBox = { south: 25.0, north: 40.0, west: 44.0, east: 63.5 }
       - CACHE_TTL = { flights: 10_000, ships: 0, events: 300_000 } (10s, N/A for push, 5min)
    7. Create `tsconfig.server.json` per RESEARCH.md: target ES2023, module ESNext, moduleResolution bundler, strict true, noEmit true, types: ["node"], include: ["server"]. Add `erasableSyntaxOnly: true` to match the research recommendation.
    8. Update `tsconfig.json` to add reference: `{ "path": "./tsconfig.server.json" }`
    9. Update `package.json` scripts:
       - "dev": "concurrently -n client,server -c blue,green \"vite\" \"tsx --env-file .env watch server/index.ts\""
       - "dev:client": "vite"
       - "dev:server": "tsx --env-file .env watch server/index.ts"
       - Keep build, lint, preview unchanged
    10. Update `.gitignore`: Add `.env` and `.env.*` lines, with `!.env.example` exception. Place BEFORE the editor section.
    11. Create `.env.example` per RESEARCH.md with all 7 env vars (OPENSKY_CLIENT_ID, OPENSKY_CLIENT_SECRET, AISSTREAM_API_KEY, ACLED_EMAIL, ACLED_PASSWORD, PORT, CORS_ORIGIN) with comments explaining where to get each.
    12. Create `server/__tests__/types.test.ts` that verifies the discriminated union works correctly at the type level using type assertions and runtime shape checks.

    IMPORTANT: Server tests must include an inline vitest config comment or use a workspace to run with `environment: 'node'` not 'jsdom'. The simplest approach: add `// @vitest-environment node` at the top of each server test file.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.server.json && npx vitest run server/__tests__/types.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - MapEntity types defined and exported from server/types.ts
    - Re-exported in src/types/entities.ts for frontend
    - Config module validates env vars with clear error messages
    - IRAN_BBOX and CACHE_TTL constants defined
    - tsconfig.server.json compiles server/ code cleanly
    - tsconfig.json references server config
    - package.json has concurrently dev script
    - .gitignore protects .env files
    - .env.example documents all required variables
    - Type tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cache module, error handler, and Express server entry point with health check</name>
  <files>
    server/cache/entityCache.ts,
    server/middleware/errorHandler.ts,
    server/index.ts,
    server/__tests__/cache.test.ts,
    server/__tests__/server.test.ts
  </files>
  <behavior>
    - EntityCache.get() returns null when empty
    - EntityCache.get() returns { data, stale: false, lastFresh } when within TTL
    - EntityCache.get() returns { data, stale: true, lastFresh } when TTL expired
    - EntityCache.set() stores data with current timestamp
    - EntityCache.set() overwrites previous data
    - Error handler middleware returns 500 JSON response with { error: 'Internal server error' }
    - Error handler logs error message to console.error
    - GET /health returns 200 with { status: 'ok' }
    - Server sets CORS headers for configured origin
    - Unknown routes return 404
  </behavior>
  <action>
    1. Create `server/cache/entityCache.ts` per RESEARCH.md Pattern 3:
       - Generic class `EntityCache<T>` with constructor(private ttlMs: number)
       - Private `entry: { data: T; fetchedAt: number } | null = null`
       - `get()` method: if no entry return null, compute age, return CacheResponse with stale = age > ttlMs
       - `set(data: T)` method: store data with fetchedAt = Date.now()
       - `clear()` method: set entry to null
       - Export the class
    2. Create `server/middleware/errorHandler.ts`:
       - Export a standard Express error handler: `(err: Error, req: Request, res: Response, _next: NextFunction) => void`
       - Log: `console.error(`[ERROR] ${req.method} ${req.path}:`, err.message)`
       - Respond: `res.status(500).json({ error: 'Internal server error' })`
       - Use `import type` for Express types
    3. Create `server/index.ts` per RESEARCH.md server entry pattern:
       - Import express, cors
       - Import errorHandler from middleware
       - Import config (lazy -- call loadConfig() and catch startup errors)
       - Create app = express()
       - app.use(cors({ origin: config.corsOrigin }))
       - app.use(express.json())
       - Add `GET /health` route returning `{ status: 'ok' }`
       - Stub route mounts as comments: `// app.use('/api/flights', flightsRouter);` etc. (Plan 02 will uncomment)
       - app.use(errorHandler) -- MUST be after routes
       - app.listen(config.port, callback with console.log)
       - Export the app for testing (export before listen, conditionally listen only when not in test)
    4. Create `server/__tests__/cache.test.ts`:
       - Add `// @vitest-environment node` at top
       - Test: get() returns null on empty cache
       - Test: set() then get() returns data with stale: false
       - Test: get() returns stale: true after TTL expires (use vi.advanceTimersByTime)
       - Test: set() overwrites previous data
       - Test: clear() makes get() return null
    5. Create `server/__tests__/server.test.ts`:
       - Add `// @vitest-environment node` at top
       - Mock the config module to avoid requiring env vars in tests
       - Test: GET /health returns 200 with { status: 'ok' }
       - Test: Unknown route returns 404
       - Test: CORS Access-Control-Allow-Origin header is set
       - Use the exported app directly with supertest-like approach (or just use the app directly -- Express app is a request handler). Since we're not adding supertest, create a lightweight helper that uses native fetch against a started server, or use vitest's built-in http approach. Simplest: start the app on a random port in beforeAll, fetch against it, close in afterAll.

    IMPORTANT: The server/index.ts must export the Express app AND only call app.listen() when the file is the main module. Use: `if (process.argv[1] && import.meta.url.endsWith(process.argv[1].replace(/\\/g, '/')))` or simpler: check `process.env.VITEST` to skip listen in tests. The cleanest approach: export a `createApp()` function that returns the configured app, and call listen() at the bottom of the file only when not imported as a module. Use `import.meta.url` check.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/cache.test.ts server/__tests__/server.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - EntityCache stores and retrieves data with correct staleness detection
    - Error handler catches errors and returns 500 JSON
    - Express server starts on port 3001 with CORS enabled
    - GET /health returns 200
    - All cache and server tests pass
    - Server exports app for testability
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p tsconfig.server.json` -- server TypeScript compiles cleanly
2. `npx vitest run server/ --reporter=verbose` -- all server tests pass
3. `npx vitest run --reporter=verbose` -- existing frontend tests still pass (no regressions)
4. `grep -q "\.env" .gitignore` -- .env is protected from git
5. `grep -q "concurrently" package.json` -- dev script runs both servers
</verification>

<success_criteria>
- Express 5 server starts on port 3001 with CORS for localhost:5173
- MapEntity discriminated union types are defined and compilable
- In-memory cache correctly tracks TTL and staleness
- Config validates required env vars at startup
- npm run dev starts both Vite frontend and Express backend
- .env protected from git, .env.example documents all variables
- All server tests pass, all existing frontend tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-proxy/03-01-SUMMARY.md`
</output>

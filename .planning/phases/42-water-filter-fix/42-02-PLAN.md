---
phase: 42-water-filter-fix
plan: 02
type: execute
wave: 2
depends_on: ['42-01']
files_modified:
  - server/adapters/overpass-water.ts
  - server/__tests__/adapters/overpass-water.test.ts
  - server/__tests__/schemas/waterFilterStats.test.ts
autonomous: true
requirements: [WATER-FILTER-02, WATER-FILTER-04]
must_haves:
  truths:
    - 'Spatial dedup never collapses two distinct named facilities of the same type within 50m (WATER-FILTER-02)'
    - 'Dedup output is deterministic and order-independent — same corpus produces an identical admitted ID set and order regardless of input order (D-07/D-08)'
    - 'A regression fixture for the real previously-dropped OSM element pair (named in 42-DIAGNOSIS.md) fails if the old name-blind collapse returns (WATER-FILTER-04, D-14)'
    - "The Latin-label admission gate (computeAdmissionDecision / hasLatinLabel) is untouched — the G1 'Dam near X' regression stays fixed (D-06/D-15)"
  artifacts:
    - path: 'server/adapters/overpass-water.ts'
      provides: 'Exported name-aware + deterministic spatialDedup(facilities) pure function'
      exports: ['spatialDedup']
      contains: 'export function spatialDedup'
    - path: 'server/__tests__/adapters/overpass-water.test.ts'
      provides: 'GREEN spatial-dedup behavior tests incl. the D-14 regression fixture'
      contains: 'spatialDedup'
  key_links:
    - from: 'server/adapters/overpass-water.ts fetchWaterFacilities'
      to: 'spatialDedup'
      via: 'call-site replacement of the inline 1202-1212 loop'
      pattern: "spatialDedup\\("
    - from: 'spatialDedup'
      to: 'haversine'
      via: '50m (< 0.05) same-facilityType distance check, window unchanged (D-05)'
      pattern: "haversine\\("
---

<objective>
Implement the diagnosed fix (D-04..D-08): extract the inline spatial-dedup loop into an exported pure `spatialDedup(facilities)` function that is name-aware (distinct named facilities never collapse) and deterministic (survivor chosen by highest `notabilityScore`, tie-broken by lowest `osmId` — never by Overpass return order). Turn the RED Plan-01 scaffold GREEN and add the D-14 regression fixture for the real previously-dropped OSM element.

Purpose: This is the literal WATER-FILTER-02 fix — "spatial dedup never collapses distinct named facilities" — plus the determinism fix that kills the "intermittent" symptom (which facility disappears currently depends on Overpass return order). WATER-FILTER-04 pins it with rejection-bucket deltas and a regression fixture.

Output: Exported `spatialDedup` in `overpass-water.ts`; the call site replaced; all spatialDedup tests GREEN including the diagnosed-element regression pin; the `waterFilterStats.test.ts` lockstep updated only if an additive-OPTIONAL field is introduced.

NOTE: If Plan 01's D-03 checkpoint returned `pivot`, this plan is revised to target the confirmed bucket instead of the name-blind dedup; the actions below assume the prime-suspect-confirmed path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-water-filter-fix/42-CONTEXT.md
@.planning/phases/42-water-filter-fix/42-RESEARCH.md
@.planning/phases/42-water-filter-fix/42-PATTERNS.md
@.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract and implement name-aware + deterministic spatialDedup</name>
  <files>server/adapters/overpass-water.ts</files>
  <read_first>
    - server/adapters/overpass-water.ts (the inline dedup loop at lines 1198-1212 being extracted; the haversine helper at line 271; the WaterFacility fields notabilityScore/osmId/label/facilityType/lat/lng at ~lines 984-988; computeAdmissionDecision at ~lines 883-885 which MUST NOT be touched)
    - .planning/phases/42-water-filter-fix/42-PATTERNS.md §"spatial dedup fix" (the exact spatialDedup + normName copy-from source and the call-site replacement)
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §Pattern 1 + §Common Pitfalls (Pitfall 4: read f.label for name comparison — NOT nameLatin, which is only set when applyRomanizedName fires)
    - .planning/phases/42-water-filter-fix/42-DIAGNOSIS.md (the confirmed bucket and the named dropped element — confirms this is the dedup fix vs a pivot)
  </read_first>
  <action>
    Add an exported pure function `export function spatialDedup(facilities: WaterFacility[]): { kept: WaterFacility[]; collapsed: number }` to `server/adapters/overpass-water.ts` (place it as a module-level export near the existing helpers so the test can import it directly — Pitfall 3 extraction precedent is `computeAdmissionDecision`, Phase 27.3.1 R-06 D-20). Inside it:
    - Order the working set deterministically (D-07): sort a copy by `notabilityScore` descending, tie-broken by `osmId` ascending — `(b.notabilityScore ?? 0) - (a.notabilityScore ?? 0) || a.osmId - b.osmId`. This makes the survivor order-independent of Overpass return order (kills the intermittency).
    - Add a private `normName(f)` helper that returns `(f.label ?? '').toLowerCase().replace(/\s+/g, ' ').trim()` — read `f.label` per Pitfall 4 (always populated by `extractLabel` at line 984; do NOT read `nameLatin`).
    - Collapse predicate (D-04/D-05): two facilities collapse ONLY when `existing.facilityType === f.facilityType` AND `haversine(existing.lat, existing.lng, f.lat, f.lng) < 0.05` (50m window UNCHANGED — D-05) AND (`normName(existing) === normName(f)` OR either side's `normName` is `''`). Distinct non-empty names with the same type within 50m do NOT collapse.
    - Return `{ kept, collapsed }` where `collapsed` counts the suppressed duplicates.

    Replace the inline loop at lines 1202-1212 in `fetchWaterFacilities` with `const { kept: deduped, collapsed } = spatialDedup(Array.from(unique.values())); stats.rejections.duplicate += collapsed;`. Do NOT touch `computeAdmissionDecision` / `hasLatinLabel` (D-06). Do NOT widen or shrink the 50m window (D-05). Keep `stats.rejections.duplicate` as the SUMMED bucket — do not attempt to populate `byTypeRejections[*].duplicate` (Pitfall 1) unless the diagnosis required the additive field handled in Task 3.

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/adapters/overpass-water.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/adapters/overpass-water.ts` exports `spatialDedup` (grep `export function spatialDedup` returns 1)
    - The inline 1202-1212 loop is gone; the call site reads `spatialDedup(Array.from(unique.values()))` and adds `collapsed` to `stats.rejections.duplicate`
    - The collapse predicate retains the exact `< 0.05` (50m) window and `facilityType` equality (D-05) — grep confirms `haversine` and `< 0.05` still present in the predicate
    - Name comparison reads `f.label` (Pitfall 4), not `nameLatin`
    - `computeAdmissionDecision` / `hasLatinLabel` are byte-unchanged (D-06) — `git diff` shows no edits to those functions
    - Plan-01 cases (a) both-admit, (b) same-name collapse, (c) unnamed-side collapse, (d) deterministic-order all pass GREEN
  </acceptance_criteria>
  <done>spatialDedup is exported, name-aware, deterministic, wired into fetchWaterFacilities; cases a–d are GREEN; the Latin gate and 50m window are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the D-14 regression fixture for the real dropped OSM element</name>
  <files>server/__tests__/adapters/overpass-water.test.ts</files>
  <read_first>
    - server/__tests__/adapters/overpass-water.test.ts (the Plan-01 spatialDedup describe block incl. the it.todo case (e) to fill in)
    - .planning/phases/42-water-filter-fix/42-DIAGNOSIS.md (the concrete previously-dropped OSM element pair: id + name + facilityType + coords — this is the regression fixture source, D-14)
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §Validation Architecture (the rejection-bucket-delta assertion shape for WATER-FILTER-04)
  </read_first>
  <behavior>
    - Case (e) regression: build a fixed corpus containing the REAL named element pair from 42-DIAGNOSIS.md (within 50m, same facilityType, distinct non-empty names). Assert `spatialDedup(corpus).kept.length === 2` (BOTH admit post-fix) and `collapsed === 0` for that pair.
    - Bucket-delta pin: assert that under the OLD name-blind behavior this pair WOULD have collapsed (encode the delta as the documented expectation — e.g. a comment + an assertion that the fixed predicate yields `collapsed === 0` where the legacy predicate yielded `collapsed === 1`), so the test fails if the name-blind collapse is reintroduced (WATER-FILTER-04, D-14).
  </behavior>
  <action>
    Replace the Plan-01 `it.todo('(e) ...')` stub with a real `it(...)` using the concrete OSM element pair named in `42-DIAGNOSIS.md` (use the exact `id`, `facilityType`, `lat`, `lng`, and distinct non-empty `name`/`label` values from the diagnosis). Assert both elements survive `spatialDedup` (`kept.length === 2`, `collapsed === 0`). Add the rejection-bucket-delta assertion that pins the fix: document (comment) that the legacy name-blind predicate collapsed this pair to `collapsed === 1`, and assert the fixed function yields `collapsed === 0` — so a regression to name-blindness flips the count and fails the test. Keep the fixture in-memory (no Overpass mocking, Pitfall 3). Leave all existing G1 tests untouched (D-15).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/adapters/overpass-water.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `it.todo` case (e) is replaced by a passing `it(...)` referencing the real OSM element id(s) from 42-DIAGNOSIS.md
    - The fixture asserts `kept.length === 2` and `collapsed === 0` for the previously-dropped named pair
    - A bucket-delta assertion pins that name-blind collapse (`collapsed === 1`) is NOT the post-fix behavior — reintroducing name-blindness fails the test
    - Full `overpass-water.test.ts` suite is GREEN, including all pre-existing G1 admission tests (D-15)
  </acceptance_criteria>
  <done>The D-14 regression fixture uses the real diagnosed element, both facilities admit post-fix, and the bucket delta fails on regression — all G1 tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: WaterFilterStats schema lockstep (only if an additive field is needed)</name>
  <files>server/__tests__/schemas/waterFilterStats.test.ts, server/adapters/overpass-water.ts</files>
  <read_first>
    - server/__tests__/schemas/waterFilterStats.test.ts (the existing 8-bucket schema assertion — the lockstep gate per Phase 27.3.2 D-04)
    - server/adapters/overpass-water.ts (the WaterFilterStats interface at lines 720-777 — the frozen 8-bucket rejections shape + byTypeRejections)
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §Open Questions (Q1: default to summed-only + the two-run ID-set diff; add a per-type/dedupCollisions field ONLY if the diagnosis could not pinpoint the dropped elements without it)
    - .planning/phases/42-water-filter-fix/42-DIAGNOSIS.md (whether collapse-pair visibility was needed for the diagnosis)
  </read_first>
  <action>
    DEFAULT PATH (D-13 recommendation, RESEARCH Open Question 1): make NO schema change. The summed `rejections.duplicate` plus the two-run ID-set diff already satisfied WATER-FILTER-01, and the regression fixture already pins WATER-FILTER-04 — the 8-bucket `WaterFilterStats` shape stays frozen and `waterFilterStats.test.ts` is left as-is. In this case, confirm the existing schema test still passes unchanged and record "no schema change" in the SUMMARY.

    CONDITIONAL PATH (only if 42-DIAGNOSIS.md required collapse-pair visibility that the summed bucket could not provide): add AT MOST ONE additive-OPTIONAL field per D-13 — e.g. a capped `dedupCollisions` sample array on `WaterFilterStats` (optional, default absent so pre-fix cache entries still validate). If added, mirror it in `server/__tests__/schemas/waterFilterStats.test.ts` IN THE SAME COMMIT (Phase 27.3.2 D-04 lock-step precedent) and keep the 8 rejection buckets unchanged. Do NOT make any breaking change to the schema or the DevApiStatus consumers (D-13).

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/schemas/waterFilterStats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/__tests__/schemas/waterFilterStats.test.ts` passes GREEN
    - The 8 rejection buckets (`excluded_location`, `excluded_turkey`, `not_notable`, `no_name`, `no_resolved_name`, `duplicate`, `low_score`, `no_city`) are unchanged (no breaking edits — D-13)
    - If an additive field was added, it is OPTIONAL, present in BOTH `overpass-water.ts` and the schema test in the same commit, and pre-fix entries (without the field) still validate
    - If no field was added, the SUMMARY records "no schema change — summed duplicate + ID-diff sufficed"
  </acceptance_criteria>
  <done>The WaterFilterStats lockstep is satisfied: either no schema change (default) or exactly one additive-OPTIONAL field mirrored in the schema test the same commit.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 02)

- **Function:** `spatialDedup(facilities: WaterFacility[]): { kept: WaterFacility[]; collapsed: number }` — exported from `server/adapters/overpass-water.ts`; name-aware (D-04) + deterministic (D-07)
- **Private helper:** `normName(f)` — case/whitespace-insensitive label normalizer over `f.label`
- **Call-site change:** `fetchWaterFacilities` now calls `spatialDedup(...)` and adds `collapsed` to `stats.rejections.duplicate`
- **Test:** GREEN `spatialDedup` cases (a)–(e) in `overpass-water.test.ts`, including the D-14 regression fixture on the real diagnosed element
- **(Conditional) field:** at most one additive-OPTIONAL `WaterFilterStats` field (e.g. `dedupCollisions`) — only if the diagnosis required it (D-13)
  </artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                         |
| ----------------------------------------------- | ----------------------------------------------------------------------------------- |
| Overpass-sourced WaterFacility[] → spatialDedup | Public OSM data flows through the dedup transform; no auth/session boundary crossed |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                             |
| --------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-03   | Tampering         | Loosening the collapse predicate could silently re-drop named facilities (Pitfall 4: empty normName on a labeled facility) | mitigate    | Read `f.label` (always populated by extractLabel:984), not `nameLatin`; the case-(a)/(e) fixtures assert distinct-named pairs both admit and fail on regression                                                                                             |
| T-42-04   | Denial of Service | O(n²) `kept.some(...)` inner scan on the deterministic-sorted set                                                          | accept      | Runs only in `refresh:water` + gated `?refresh=true`, never on the user request path (RESEARCH Architectural Responsibility Map); corpus is bounded to ME water facilities. Performance is explicitly not a phase requirement (CONTEXT Claude's Discretion) |
| T-42-05   | Tampering         | npm/pip/cargo installs                                                                                                     | accept      | No package installs this phase (RESEARCH §Package Legitimacy Audit: not applicable)                                                                                                                                                                         |

</threat_model>

<verification>
- `npx vitest run server/__tests__/adapters/overpass-water.test.ts` GREEN (cases a–e + all G1 tests)
- `npx vitest run server/__tests__/schemas/waterFilterStats.test.ts` GREEN
- `git diff` confirms `computeAdmissionDecision` / `hasLatinLabel` untouched and the 50m window unchanged
</verification>

<success_criteria>

- `spatialDedup` exported, name-aware, deterministic; wired into `fetchWaterFacilities` (WATER-FILTER-02)
- Distinct named pair within 50m both admit; same-name / unnamed-side collapse; deterministic output (D-04..D-08)
- D-14 regression fixture on the real diagnosed element fails on name-blind regression (WATER-FILTER-04)
- Latin gate and 50m window untouched; G1 tests green (D-06/D-15)
- WaterFilterStats schema lockstep satisfied (no change by default; one additive-OPTIONAL field max if required — D-13)
  </success_criteria>

<output>
Create `.planning/phases/42-water-filter-fix/42-02-SUMMARY.md` when done
</output>

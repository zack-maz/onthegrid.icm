---
phase: 42-water-filter-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-water-filter-fix/42-DIAGNOSIS.md
  - server/__tests__/adapters/overpass-water.test.ts
autonomous: false
requirements: [WATER-FILTER-01]
must_haves:
  truths:
    - 'Operator has a committed written diagnosis (42-DIAGNOSIS.md) citing the specific rejection bucket that ate the missing facilities, produced from npm run refresh:water telemetry BEFORE any fix code'
    - 'Diagnosis names concrete dropped OSM elements (id + name + facilityType + coords) from a two-run admitted-ID-set diff'
    - 'A RED spatial-dedup test scaffold exists pinning the not-yet-built spatialDedup contract'
  artifacts:
    - path: '.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md'
      provides: 'Telemetry-first written diagnosis (WATER-FILTER-01)'
      contains: 'rejections.duplicate'
    - path: 'server/__tests__/adapters/overpass-water.test.ts'
      provides: 'RED spatial-dedup behavior tests (Wave 0 scaffold)'
      contains: 'spatialDedup'
  key_links:
    - from: '42-DIAGNOSIS.md'
      to: 'npm run refresh:water telemetry'
      via: 'two-run admitted-ID-set diff citing rejections.duplicate'
      pattern: 'duplicate'
---

<objective>
Produce the telemetry-first written diagnosis (WATER-FILTER-01) that MUST precede any fix code (D-01), and lay down the RED spatial-dedup test scaffold that pins the `spatialDedup(...)` contract before it is built.

Purpose: The phase requirement is explicit — the fix is proven telemetry-first, not guessed. The diagnosis confirms (or pivots away from, D-03) the prime-suspect O(n²) name-blind dedup, and names the concrete dropped OSM element(s) the D-14 regression fixture will later pin. The RED scaffold satisfies the Nyquist rule: tests exist before the fix.

Output: `.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md` (committed) + RED dedup-behavior tests in `overpass-water.test.ts` (failing because `spatialDedup` does not exist yet).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-water-filter-fix/42-CONTEXT.md
@.planning/phases/42-water-filter-fix/42-RESEARCH.md
@.planning/phases/42-water-filter-fix/42-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect two-run telemetry evidence and author 42-DIAGNOSIS.md</name>
  <files>.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md</files>
  <read_first>
    - .planning/milestones/v1.4-phases/27.3.1-water-facility-retry-and-cleanup/27.3.1-DIAGNOSIS.md (diagnosis-format precedent per D-01; also the G1 "Dam near X" regression history that must stay fixed)
    - server/adapters/overpass-water.ts (the spatial-dedup loop at lines 1198-1212; the WaterFilterStats shape at lines 720-777; note rejections.duplicate is incremented at line 1211 as a SUMMED bucket only)
    - scripts/refresh-water-facilities.ts (the npm run refresh:water tool — prints full WaterFilterStats incl. byTypeRejections; atomic tmp+rename snapshot writer; preserves the /@[\w.]+/ operator-email scrub at lines 78-84)
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §Common Pitfalls (Pitfall 1: per-type byTypeRejections.*.duplicate is structurally always 0 post-merge — cite the SUMMED rejections.duplicate, never a per-type duplicate count)
  </read_first>
  <action>
    Run `npm run refresh:water` at least TWICE per D-02 (the script is the diagnosis tool and the snapshot regenerator). Between runs, capture each run's admitted facility ID set from `src/data/water-facilities.json` (extract the `id` field of every entry) and the printed `WaterFilterStats` (especially `rejections.duplicate` and the 8-bucket `byTypeRejections`). Use a temporary working copy of the snapshot per run so the first run's output is not lost — do NOT commit a regenerated snapshot in this plan (snapshot regen is Plan 03 / D-12, AFTER the fix). If live Overpass is rate-limited or returns a genuinely different raw corpus between runs (Pitfall 2 — corpus drift, not dedup non-determinism), note the run-to-run raw-element-count delta explicitly and rely on the within-run dedup evidence; do not claim determinism failure from corpus drift.

    Author `.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md` following the 27.3.1-DIAGNOSIS.md section structure (D-03 discretion on exact sections beyond the precedent). It MUST: (1) cite the specific rejection bucket responsible — the SUMMED `rejections.duplicate` field (NOT `byTypeRejections.*.duplicate`, which is structurally always 0 per Pitfall 1); (2) name concrete dropped OSM elements as id + name + facilityType + coords, taken from the two-run admitted-ID-set diff, since the intermittency itself is the evidence; (3) record whether the telemetry confirms the prime-suspect name-blind + order-dependent spatial dedup (D-03 confirmation) OR points at a different bucket (`no_resolved_name`, `no_city`, Overpass mirror flakiness). If it implicates the Latin-label admission gate (`hasLatinLabel` / `no_resolved_name`), STOP and surface to the operator before recommending any action, per D-06 (loosening that gate is forbidden by WATER-FILTER-02). Commit the diagnosis BEFORE any fix code lands (D-01).

  </action>
  <verify>
    <automated>test -f .planning/phases/42-water-filter-fix/42-DIAGNOSIS.md && grep -v '^#' .planning/phases/42-water-filter-fix/42-DIAGNOSIS.md | grep -c 'duplicate' | { read n; [ "$n" -ge 1 ]; }</automated>
    <human-check>Diagnosis names at least one concrete dropped OSM element (id + name + facilityType + coords) and states whether the prime-suspect dedup is confirmed or a pivot is required (D-03)</human-check>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md` exists and is committed
    - The diagnosis cites the SUMMED `rejections.duplicate` bucket (the grep above proves the token is present in non-comment prose)
    - At least one concrete dropped OSM element is named with id + name + facilityType + coords
    - The diagnosis explicitly states either "prime-suspect dedup confirmed" or names the pivot bucket (D-03)
    - No regenerated `src/data/water-facilities.json` is committed in this plan (snapshot regen is deferred to Plan 03)
    - If the Latin-label gate is implicated, the diagnosis surfaces it to the operator rather than recommending a loosening (D-06)
  </acceptance_criteria>
  <done>42-DIAGNOSIS.md is committed, cites rejections.duplicate, names concrete dropped elements, and records the confirm-or-pivot decision — all before any fix code exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED spatial-dedup behavior test scaffold</name>
  <files>server/__tests__/adapters/overpass-water.test.ts</files>
  <read_first>
    - server/__tests__/adapters/overpass-water.test.ts (existing G1 admission tests at ~lines 2006-2047 — copy the it(...) structure and fixture-construction style; confirm the duplicate bucket is 0 in every current fixture)
    - server/adapters/overpass-water.ts (the dedup loop 1198-1212 to be extracted as spatialDedup in Plan 02; the WaterFacility fields used for survival: notabilityScore, osmId, label, facilityType, lat, lng at ~lines 984-988)
    - .planning/phases/42-water-filter-fix/42-PATTERNS.md §"ADD dedup tests" (the five required cases a–e and the deterministic-corpus approach per Pitfall 2)
  </read_first>
  <behavior>
    - Case (a): a distinct NAMED pair of the same facilityType within 50m (haversine < 0.05) → BOTH admit; `spatialDedup(corpus).kept.length === 2` (WATER-FILTER-02 core)
    - Case (b): a SAME-name pair of the same facilityType within 50m → collapses to one; `kept.length === 1` and `collapsed === 1`
    - Case (c): a pair where ONE side has an empty/whitespace-only label within 50m same type → collapses (D-04 "one side unnamed")
    - Case (d): determinism — shuffle the same fixture corpus and assert the kept-ID set AND kept order are identical across shuffles (D-08, proven against a FIXED in-memory corpus per Pitfall 2, NOT via two live runs)
    - Case (e): placeholder describe/it.todo for the D-14 regression pin on the REAL previously-dropped OSM element pair — filled in Plan 02 once 42-DIAGNOSIS.md names the element
  </behavior>
  <action>
    Add a new `describe('spatialDedup', ...)` block to `server/__tests__/adapters/overpass-water.test.ts` importing the not-yet-exported `spatialDedup` from `server/adapters/overpass-water.ts`. Construct FIXED in-memory `WaterFacility[]` fixtures (no Overpass mocking — Pitfall 3) with explicit `id`, `facilityType`, `lat`, `lng`, `label`, `notabilityScore`, `osmId` fields. Write the four assertable cases (a)–(d) above against `spatialDedup(corpus)` returning `{ kept: WaterFacility[]; collapsed: number }`. For case (e), add an `it.todo('(e) regression: real previously-dropped OSM pair from 42-DIAGNOSIS.md both admit')` stub — it is filled in Plan 02 after the diagnosis names the element. These tests MUST be RED now (the import resolves to undefined because `spatialDedup` is not yet exported); do NOT implement `spatialDedup` in this plan. Use `@ts-expect-error` on the unresolved import if tsc would otherwise fail the typecheck, mirroring the repo's Wave-0 red-stub convention. Leave ALL existing G1 `hasName` / `no_resolved_name` tests untouched (D-15).
  </action>
  <verify>
    <automated>grep -c "spatialDedup" server/__tests__/adapters/overpass-water.test.ts | { read n; [ "$n" -ge 4 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `server/__tests__/adapters/overpass-water.test.ts` contains a `describe('spatialDedup', ...)` block referencing `spatialDedup` at least 4 times (import + ≥3 call sites)
    - Cases (a) both-admit, (b) same-name collapse, (c) unnamed-side collapse, (d) deterministic-order-over-shuffle are present as `it(...)` blocks
    - Case (e) is present as an `it.todo(...)` stub awaiting the diagnosed element
    - The new tests fail (RED) because `spatialDedup` is not yet exported — verifiable by running `npx vitest run server/__tests__/adapters/overpass-water.test.ts` and observing the spatialDedup block fail while the existing G1 tests still pass
    - No existing G1 test is modified (D-15)
  </acceptance_criteria>
  <done>The spatialDedup test block exists, is RED (spatialDedup not yet built), covers cases a–d plus an it.todo for case e, and leaves all G1 tests green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the diagnosed fix target before any code lands (D-03 pivot gate)</decision>
  <context>WATER-FILTER-01 mandates a telemetry-first diagnosis. CONTEXT.md pre-registers the name-aware + deterministic spatial-dedup fix as the prime suspect, but D-03 requires the planner/executor to pivot if telemetry points at a different bucket (no_resolved_name, no_city, Overpass mirror flakiness). D-06 forbids loosening the Latin-label admission gate without operator surfacing. This checkpoint confirms the diagnosis result steers Plan 02.</context>
  <options>
    <option id="confirm-dedup">
      <name>Prime-suspect confirmed — proceed to name-aware + deterministic spatialDedup fix (Plan 02)</name>
      <pros>Matches the pre-registered D-04..D-08 fix; no replanning needed</pros>
      <cons>None if telemetry confirms</cons>
    </option>
    <option id="pivot">
      <name>Telemetry points elsewhere — pivot Plan 02 to the confirmed bucket and record the pivot</name>
      <pros>Honors telemetry-first principle; fixes the real cause</pros>
      <cons>Plan 02 must be revised to target the confirmed bucket; if the Latin gate is implicated, operator decision required first (D-06)</cons>
    </option>
  </options>
  <resume-signal>Select: confirm-dedup or pivot (with the confirmed bucket named)</resume-signal>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 01)

- **File:** `.planning/phases/42-water-filter-fix/42-DIAGNOSIS.md` — telemetry-first written diagnosis (WATER-FILTER-01); cites `rejections.duplicate`, names concrete dropped OSM elements
- **Test block:** `describe('spatialDedup', ...)` in `server/__tests__/adapters/overpass-water.test.ts` — RED scaffold for cases (a)–(d) + `it.todo` for case (e)
- **Decision record:** D-03 confirm-or-pivot outcome (carried into Plan 02)
  </artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Overpass API → refresh:water       | Untrusted public OSM data enters the snapshot via the diagnosis runs                                                                       |
| refresh:water → committed snapshot | OSM `operator` tag may carry PII/email into a public repo (no snapshot committed in this plan, but the diagnosis runs touch the same path) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                   | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| --------- | ---------------------- | --------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-01   | Information Disclosure | OSM `operator` PII/email in refresh:water output reaching the diagnosis doc | mitigate    | Do NOT paste raw `operator` tag values into 42-DIAGNOSIS.md; cite id + name + facilityType + coords only. The existing `/@[\w.]+/` scrub in refresh-water-facilities.ts:78-84 (T-27.3.1.05-01) is unchanged this plan |
| T-42-02   | Tampering              | npm/pip/cargo installs                                                      | accept      | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable). No install task exists; no slopcheck checkpoint needed                                                                        |

</threat_model>

<verification>
- `npx vitest run server/__tests__/adapters/overpass-water.test.ts` shows the new spatialDedup block RED and the existing G1 tests GREEN
- `42-DIAGNOSIS.md` committed before any fix code; grep confirms `duplicate` cited
- D-03 decision recorded for Plan 02
</verification>

<success_criteria>

- 42-DIAGNOSIS.md exists, is committed, cites the SUMMED `rejections.duplicate` bucket, and names concrete dropped OSM elements (WATER-FILTER-01)
- RED spatialDedup test scaffold exists covering cases a–d + it.todo(e)
- D-03 confirm-or-pivot decision is recorded
- No snapshot regenerated/committed in this plan
- All existing G1 admission tests remain green (D-15)
  </success_criteria>

<output>
Create `.planning/phases/42-water-filter-fix/42-01-SUMMARY.md` when done
</output>

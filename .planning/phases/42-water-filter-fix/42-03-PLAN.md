---
phase: 42-water-filter-fix
plan: 03
type: execute
wave: 3
depends_on: ['42-02']
files_modified:
  - server/routes/water.ts
  - server/routes/cron-warm.ts
  - server/routes/cron-health.ts
  - server/lib/healthSources.ts
  - server/openapi.yaml
  - server/__tests__/routes/water.test.ts
  - server/__tests__/lib/healthSources.test.ts
  - src/__tests__/lib/redis-registry.test.ts
  - docs/architecture/redis-keys.md
  - CLAUDE.md
  - src/data/water-facilities.json
autonomous: true
requirements: [WATER-FILTER-03]
must_haves:
  truths:
    - 'water:facilities:v3 is bumped to water:facilities:v4 atomically across all 10 lockstep surfaces so the redis-registry drift gate stays green (WATER-FILTER-03, D-09/D-10/D-11)'
    - 'The src/data/water-facilities.json cold-start snapshot is regenerated via npm run refresh:water and reflects the post-fix admitted set (D-12)'
    - 'The operator-email scrub still runs during snapshot regeneration (no PII reaches the public committed snapshot)'
  artifacts:
    - path: 'server/routes/water.ts'
      provides: 'Canonical writer of the bumped cache key'
      contains: 'water:facilities:v4'
    - path: 'docs/architecture/redis-keys.md'
      provides: 'Contract-doc registry row for the bumped key'
      contains: 'water:facilities:v4'
    - path: 'src/data/water-facilities.json'
      provides: 'Regenerated cold-start snapshot reflecting the post-fix admitted set'
      contains: ''
  key_links:
    - from: 'server/routes/water.ts FACILITIES_KEY'
      to: 'redis-registry drift gate'
      via: 'source literal cross-checked against CLAUDE.md + redis-keys.md whitelist'
      pattern: 'water:facilities:v4'
    - from: 'npm run refresh:water'
      to: 'src/data/water-facilities.json'
      via: 'atomic tmp+rename snapshot regeneration with operator-email scrub'
      pattern: "water-facilities\\.json"
---

<objective>
Make the fix observable in production data (WATER-FILTER-03): bump `water:facilities:v3` → `water:facilities:v4` across all 10 lockstep surfaces in one phase (so the `redis-registry.test.ts` mechanical drift gate stays green), regenerate the `src/data/water-facilities.json` cold-start snapshot from the post-fix admitted set, and reconcile the in-scope contract surfaces (Redis registry doc + CLAUDE.md line).

Purpose: The name-aware dedup admits previously-collapsed facilities, so the admitted-facility set changed — that is a behavior change (D-09) which mandates a cache-key bump to force a cold-fill and a snapshot regen so the tier-3 read floor carries the corrected set. A partial bump fails the mechanical drift gate; this plan does the full atomic propagation.

Output: All 10 surfaces carry `water:facilities:v4`; the whitelist demotes `v3` to dead-surveillance; the snapshot is regenerated and committed; the redis-registry drift gate and the full server suite are green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-water-filter-fix/42-CONTEXT.md
@.planning/phases/42-water-filter-fix/42-RESEARCH.md
@.planning/phases/42-water-filter-fix/42-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump water:facilities:v3 → v4 across all 10 lockstep surfaces</name>
  <files>server/routes/water.ts, server/routes/cron-warm.ts, server/routes/cron-health.ts, server/lib/healthSources.ts, server/openapi.yaml, server/__tests__/routes/water.test.ts, server/__tests__/lib/healthSources.test.ts, src/__tests__/lib/redis-registry.test.ts, docs/architecture/redis-keys.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §"water:facilities:v3 → v4 lockstep surfaces" (the authoritative 10-surface table with exact line numbers + the whitelist nuance #8)
    - .planning/phases/42-water-filter-fix/42-PATTERNS.md §"Cache-key bump" (the literal declaration analogs + the RETIRED_KEY_WHITELIST comment-shift at lines 67-76, including the hard-coded "water:facilities:v3 is canonical" string at line 75 that must become v4)
    - server/routes/water.ts (line 122 FACILITIES_KEY — canonical writer)
    - src/__tests__/lib/redis-registry.test.ts (lines 55-110 — the RETIRED_KEY_WHITELIST mechanics; this is the drift gate that fails on a partial bump)
    - server/__tests__/routes/water.test.ts (the v3 pins incl. the negative assertion at line 661 that bare 'water:facilities' is NOT used — update the positive pins to v4, keep the negative assertion shape)
    - .planning/phases/42-water-filter-fix/42-CONTEXT.md §D-09/D-10/D-11 (bump policy + lockstep set + contract-surface scope)
  </read_first>
  <action>
    Change the cache-key literal from `water:facilities:v3` to `water:facilities:v4` at all 10 surfaces enumerated in RESEARCH §lockstep table:
    1. `server/routes/water.ts:122` — `FACILITIES_KEY` (canonical writer).
    2. `server/routes/cron-warm.ts:29` — `WATER_KEY`.
    3. `server/routes/cron-health.ts:18` — comment ref.
    4. `server/lib/healthSources.ts:46` — `SOURCE_KEYS.water`.
    5. `server/openapi.yaml:997` — spec entry.
    6. `server/__tests__/routes/water.test.ts` — all positive v3 pins (274, 277, 293, 508, …, 650, 660-661) → v4; keep the negative assertion at 661 that bare `'water:facilities'` is NOT used.
    7. `server/__tests__/lib/healthSources.test.ts:60-61` — test pin.
    8. `src/__tests__/lib/redis-registry.test.ts:67-76` — in `RETIRED_KEY_WHITELIST`, ADD `water:facilities:v3` as the new dead-surveillance entry (reason: "migration-cleanup probe; dead after v4 deploy; listed for surveillance"), keep `water:facilities:v2` and bare `water:facilities`, and update the hard-coded comment string at line 75 from "water:facilities:v3 is canonical" → "water:facilities:v4 is canonical".
    9. `docs/architecture/redis-keys.md:55` — registry row → v4.
    10. `CLAUDE.md` §Serverless Cache — the `water:facilities:v3` registry line → v4.

    Resolve RESEARCH Open Question 2 before finalizing: after the source bump, run the redis-registry drift gate and any docs link-check (`npx vitest run src/__tests__/lib/redis-registry.test.ts`); inspect whether `docs/architecture/data-flows.md:450,467` (stale bare `water:facilities` ref) is covered by a gate. If a gate covers it, include data-flows.md in this v4 lockstep; if NOT, leave it for Phase 49 prose reconciliation (D-11) and record the resolution in the SUMMARY. Do NOT touch the bare-ref unless a gate forces it.

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/redis-registry.test.ts server/__tests__/routes/water.test.ts server/__tests__/lib/healthSources.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "water:facilities:v3" server/ src/ docs/ CLAUDE.md` returns ONLY the intended dead-surveillance whitelist entry in redis-registry.test.ts (no live source/doc still references v3 as canonical)
    - `grep -rn "water:facilities:v4" server/ src/ docs/ CLAUDE.md` returns the canonical writer (water.ts), cron-warm, healthSources, openapi, redis-keys.md, CLAUDE.md, and the updated test pins
    - The redis-registry drift gate (`src/__tests__/lib/redis-registry.test.ts`) passes GREEN with v4 canonical and v3 demoted to surveillance
    - `server/__tests__/routes/water.test.ts` and `server/__tests__/lib/healthSources.test.ts` pass GREEN with v4 pins; the negative assertion that bare `'water:facilities'` is NOT used is retained
    - The Open Question 2 resolution (data-flows.md included this phase or deferred to Phase 49) is recorded in the SUMMARY
  </acceptance_criteria>
  <done>All 10 lockstep surfaces carry water:facilities:v4, v3 is demoted to dead-surveillance, the drift gate is green, and the data-flows.md disposition is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and commit the cold-start snapshot</name>
  <files>src/data/water-facilities.json</files>
  <read_first>
    - scripts/refresh-water-facilities.ts (the sole snapshot writer — atomic tmp+rename, sorts by id, rounds coords to 6dp, prints full WaterFilterStats; PRESERVE the /@[\w.]+/ operator-email scrub at lines 78-84, T-27.3.1.05-01)
    - server/lib/waterSnapshot.ts (the cold-start snapshot reader — tier-3 read floor; confirm the regenerated JSON shape still validates)
    - .planning/phases/42-water-filter-fix/42-CONTEXT.md §D-12 (snapshot regen via npm run refresh:water, committed after the fix lands)
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §Runtime State Inventory (the snapshot is the tier-3 read floor and carries the OLD admitted set until regenerated — REQUIRED not optional)
  </read_first>
  <action>
    Run `npm run refresh:water` AFTER the Plan-02 dedup fix is in the working tree (so the regenerated snapshot reflects the post-fix admitted set, which now includes previously-collapsed distinct named facilities). The script writes `src/data/water-facilities.json` atomically (tmp + rename), sorted by id with 6-decimal coords, and applies the operator-email scrub — do NOT hand-edit the JSON (D-12). Commit the regenerated snapshot. Capture the printed `WaterFilterStats` (especially the admitted total and the `rejections.duplicate` count) and record the before/after admitted-facility delta in the SUMMARY as evidence that the behavior changed (which is what justifies the D-09 bump). If live Overpass is unavailable at execution time, note the blocker and the snapshot regen becomes a follow-up before merge — the snapshot MUST carry the post-fix set before the phase closes (it is the tier-3 read floor served in production before Overpass).
  </action>
  <verify>
    <automated>test -f src/data/water-facilities.json && node -e "const d=require('./src/data/water-facilities.json'); const arr=Array.isArray(d)?d:(d.facilities||[]); if(!arr.length){process.exit(1)}; const ids=arr.map(f=>f.id); if(new Set(ids).size!==ids.length){console.error('duplicate ids');process.exit(1)}; process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/water-facilities.json` is regenerated by `npm run refresh:water` (not hand-edited) and committed
    - The snapshot contains no duplicate facility IDs (the node check above passes)
    - The SUMMARY records the before/after admitted-facility count delta proving the behavior change (D-09 justification)
    - No raw `operator` email/PII is present in the committed JSON (the `/@[\w.]+/` scrub ran)
    - The regenerated snapshot validates against `server/lib/waterSnapshot.ts` (cold-start read does not throw)
  </acceptance_criteria>
  <done>The cold-start snapshot reflects the post-fix admitted set, is committed, carries no PII, and the admitted-count delta is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + typecheck regression gate</name>
  <files>server/__tests__/routes/water.test.ts</files>
  <read_first>
    - .planning/phases/42-water-filter-fix/42-VALIDATION.md §Sampling Rate (per-wave merge runs the full server suite; phase gate runs the full suite green)
    - .planning/phases/42-water-filter-fix/42-RESEARCH.md §Validation Architecture (the full-suite command catches the v4 lockstep across routes/healthSources/registry)
  </read_first>
  <action>
    Run the full server test suite plus typecheck to confirm the v4 lockstep and the dedup fix did not regress any consumer. This is the phase-gate verification: `npx vitest run server/` for the server suite and `npx vitest run src/__tests__/lib/redis-registry.test.ts` for the drift gate (already covered in Task 1 but re-run here as the merge gate), plus the project typecheck (`npm run build` runs Vite + tsup + tsc — or run `npx tsc --noEmit` against the server tsconfig if a faster typecheck-only path exists). Fix any v3→v4 pin that was missed (the full suite will surface it). Record the final green state and the D-08 determinism note (two consecutive `npm run refresh:water` runs over the same corpus produce identical admitted ID sets — cite from the Plan-02 unit-test determinism case if a second live run is impractical, per Pitfall 2) in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run server/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/` passes GREEN (full server suite — confirms the v4 lockstep across routes/healthSources/registry and the dedup fix)
    - Typecheck passes (no TS errors introduced by the bump or the extraction)
    - The redis-registry drift gate is green
    - The SUMMARY records the D-08 determinism evidence (fixed-corpus unit test and/or two-run live diff)
  </acceptance_criteria>
  <done>The full server suite and typecheck are green; the v4 lockstep holds across all consumers; the determinism evidence is recorded.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 03)

- **Cache key:** `water:facilities:v4` — canonical, replacing `water:facilities:v3` (now dead-surveillance) across 10 lockstep surfaces
- **Whitelist entry:** `water:facilities:v3` added to `RETIRED_KEY_WHITELIST` in `src/__tests__/lib/redis-registry.test.ts`; canonical-comment string updated to v4
- **Regenerated artifact:** `src/data/water-facilities.json` cold-start snapshot reflecting the post-fix admitted set (D-12)
- **Contract-doc updates:** `docs/architecture/redis-keys.md` row + `CLAUDE.md` §Serverless Cache line → v4
- **Disposition record:** Open Question 2 resolution (data-flows.md included this phase vs deferred to Phase 49)
  </artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                                                |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Overpass API → refresh:water → committed snapshot | Public OSM data (incl. `operator` tags that may carry PII/email) is regenerated into a snapshot committed to a public repo |
| Source key literals → redis-registry drift gate   | A partial bump leaves the registry inconsistent; the mechanical gate is the trust check                                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ---------------------- | -------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-06   | Information Disclosure | OSM `operator` PII/email reaching the committed `water-facilities.json` snapshot | mitigate    | The `/@[\w.]+/` scrub in refresh-water-facilities.ts:78-84 (T-27.3.1.05-01) MUST run during regen; Task 2 verifies no `@`-email survives in the committed JSON                                                         |
| T-42-07   | Tampering              | Partial cache-key bump leaves a live consumer reading the stale v3 key           | mitigate    | The `redis-registry.test.ts` mechanical drift gate cross-checks source literals against CLAUDE.md + redis-keys.md; Task 1 + Task 3 run it green; grep acceptance criteria prove no live v3 canonical reference remains |
| T-42-08   | Tampering              | npm/pip/cargo installs                                                           | accept      | No package installs this phase (RESEARCH §Package Legitimacy Audit: not applicable)                                                                                                                                    |

</threat_model>

<verification>
- `grep -rn "water:facilities:v3"` shows only the dead-surveillance whitelist entry; `water:facilities:v4` is canonical everywhere
- `npx vitest run server/` + `npx vitest run src/__tests__/lib/redis-registry.test.ts` GREEN
- `src/data/water-facilities.json` regenerated, no duplicate IDs, no PII, validates on cold-start read
- D-08 determinism evidence recorded
</verification>

<success_criteria>

- `water:facilities:v4` canonical across all 10 lockstep surfaces; v3 demoted to dead-surveillance; drift gate green (WATER-FILTER-03, D-09/D-10/D-11)
- Cold-start snapshot regenerated from the post-fix admitted set and committed (D-12); operator-email scrub preserved
- Full server suite + typecheck green; no consumer regressed
- Open Question 2 (data-flows.md) dispositioned and recorded
  </success_criteria>

<output>
Create `.planning/phases/42-water-filter-fix/42-03-SUMMARY.md` when done
</output>

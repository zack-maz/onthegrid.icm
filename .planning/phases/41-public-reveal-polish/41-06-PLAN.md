---
phase: 41-public-reveal-polish
plan: 06
type: execute
wave: 4
depends_on: [41-01, 41-04]
files_modified:
  [
    package.json,
    src/types/ui.ts,
    src/stores/uiStore.ts,
    src/lib/tourSteps.ts,
    src/components/reveal/IntroOverlay.tsx,
    src/components/reveal/GuidedTour.tsx,
    src/components/reveal/TourTrigger.tsx,
    src/components/layout/AppShell.tsx,
    src/components/layout/StatusDropdown.tsx,
    src/components/layout/LayerTogglesSlot.tsx,
    src/components/layout/DetailPanelSlot.tsx,
    src/components/layout/Topbar.tsx,
    index.html,
    docs/COSTS.md,
  ]
autonomous: false
requirements: [REVEAL-SITE-01, REVEAL-SITE-02, REVEAL-SITE-03, REVEAL-SITE-04]
user_setup: []
must_haves:
  truths:
    - 'First-visit intro overlay renders on first load (isIntroSeen=false) and is dismissible; dismissal persists to localStorage iran-monitor.intro-seen (REVEAL-SITE-01, SC41-4)'
    - 'A re-openable guided tour (driver.js) spotlights HUD chrome via data-tour selectors; TourTrigger is always visible, not first-visit-gated (REVEAL-SITE-02, SC41-4)'
    - 'index.html carries static OG/Twitter/description meta with absolute vercel.app URLs + 1200x630 og:image (REVEAL-SITE-03, SC41-4)'
    - 'Custom-domain decision recorded as stay-on-vercel.app (REVEAL-SITE-04 satisfied by the decision, D-09)'
  artifacts:
    - {
        path: 'src/components/reveal/IntroOverlay.tsx',
        provides: 'First-visit dismissible overlay (REVEAL-SITE-01)',
        contains: 'isIntroSeen',
      }
    - {
        path: 'src/components/reveal/GuidedTour.tsx',
        provides: 'driver.js wiring (REVEAL-SITE-02)',
        contains: 'isTourOpen',
      }
    - {
        path: 'src/components/reveal/TourTrigger.tsx',
        provides: 'Persistent re-open affordance (REVEAL-SITE-02)',
        contains: 'openTour',
      }
    - {
        path: 'src/lib/tourSteps.ts',
        provides: 'Step list (selector + popover)',
        contains: 'data-tour',
      }
    - {
        path: 'index.html',
        provides: 'Static OG/Twitter/description meta (REVEAL-SITE-03)',
        contains: 'og:image',
      }
  key_links:
    - {
        from: 'src/components/reveal/*',
        to: 'src/stores/uiStore.ts',
        via: 'isIntroSeen/isTourOpen/openTour/closeTour/setIntroSeen selectors',
        pattern: 'useUIStore',
      }
    - {
        from: 'src/lib/tourSteps.ts',
        to: 'HUD data-tour attrs',
        via: '[data-tour=...] selectors resolving to present nodes',
        pattern: 'data-tour',
      }
    - {
        from: 'index.html og:image',
        to: '/screenshots/og-card.png',
        via: 'absolute vercel.app served path (Plan 04 asset)',
        pattern: 'screenshots/og-card.png',
      }
---

<objective>
Wave 4 — the REVEAL-SITE strand (SC41-4). Install driver.js (gated by a legitimacy checkpoint, fallback = custom overlay), add the uiStore reveal slice + UIState members, build IntroOverlay / GuidedTour / TourTrigger / tourSteps, add data-tour attrs to HUD chrome, mount in AppShell, add static OG/Twitter meta to index.html (og:image = the Plan-04 og-card.png), and record the REVEAL-SITE-04 stay-on-vercel.app decision (D-09). Depends on Plan 01 (red test stubs go green here) + Plan 04 (og-card.png exists). autonomous:false — blocking package-legitimacy checkpoint + manual tour/OG checkpoint. Purpose: SC41-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-reveal-polish/41-CONTEXT.md
@.planning/phases/41-public-reveal-polish/41-RESEARCH.md
@.planning/phases/41-public-reveal-polish/41-PATTERNS.md
@.planning/phases/41-public-reveal-polish/41-04-SUMMARY.md
@src/stores/uiStore.ts
@src/components/ui/DashboardAuthModal.tsx
</context>

<artifacts_this_phase_produces>
See 41-01-PLAN.md for the full list. This plan creates: src/components/reveal/{IntroOverlay,GuidedTour,TourTrigger}.tsx, src/lib/tourSteps.ts, uiStore reveal slice (isIntroSeen/isTourOpen/setIntroSeen/openTour/closeTour) + UIState members, data-tour attrs on HUD nodes, index.html OG/Twitter meta, localStorage key iran-monitor.intro-seen, driver.js dep.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify driver.js legitimacy, then install (fallback = custom overlay)</name>
  <files>package.json</files>
  <action>
    The executor runs the legitimacy checks, then pauses for human approval before installing. driver.js is [ASSUMED] in the Package Legitimacy Audit (slopcheck not run); per the package-name provenance rule it MUST be human-verified before install and this checkpoint is never auto-approvable. Run: `npm view driver.js version` (confirm ^1.3.x), `npm view driver.js maintainers` (expect kamranahmedse), `npm view driver.js dependencies` (expect {} zero runtime deps), `npm view driver.js scripts.postinstall` (expect undefined). Cross-check github.com/kamranahmedse/driver.js. If ALL clean: human approves, then `npm install driver.js`. If ANY surprise (postinstall present / unexpected maintainer / transitive deps): REJECT and fall back to the custom React overlay (SearchModal/DashboardAuthModal pattern) — Task 3 then implements GuidedTour as a DIY SVG-cutout overlay instead of importing driver.js.
  </action>
  <what-built>
    driver.js (~5KB, zero runtime deps, React-19-safe, MIT) is the chosen tour library; this gates its install behind a blocking-human supply-chain check with a zero-dep fallback.
  </what-built>
  <how-to-verify>
    Confirm each npm view check passes (version current, maintainer kamranahmedse, dependencies {}, no postinstall). Approve to install; reject to use the custom-overlay fallback.
  </how-to-verify>
  <read_first>
    - 41-RESEARCH.md section "Package Legitimacy Audit" (the exact npm view checks + fallback policy)
    - 41-RESEARCH.md section "Pattern 3: driver.js wiring" + 41-PATTERNS.md section "GuidedTour.tsx" (driver.js path AND custom-overlay fallback)
  </read_first>
  <verify>
    <automated>npm ls driver.js 2>/dev/null | grep -q driver.js && echo DRIVERJS_INSTALLED || echo CUSTOM_OVERLAY_FALLBACK</automated>
  </verify>
  <done>driver.js installed after passing the 4 npm view checks, OR custom-overlay fallback chosen.</done>
  <resume-signal>Type "approved: driver.js installed" once the 4 npm view checks pass and the package is installed, OR "rejected: use custom overlay" to trigger the DIY fallback in Task 3.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add uiStore reveal slice + UIState members (turns uiStore.reveal.test green)</name>
  <files>src/types/ui.ts, src/stores/uiStore.ts</files>
  <read_first>
    - src/stores/uiStore.ts (lines 5-12 readBool; 27 isMarketsCollapsed initial; 60-62 dashboard-auth session-scoped idiom; 99-115 toggleMarkets persisted idiom)
    - src/types/ui.ts (interface UIState line 86; isMarketsCollapsed line 99; isDashboardAuthOpen line 125 — where the 5 new members go)
    - 41-PATTERNS.md section "src/stores/uiStore.ts" + "src/types/ui.ts" (verbatim member shapes)
    - src/__tests__/stores/uiStore.reveal.test.ts (the Wave-0 red stub this turns green)
  </read_first>
  <behavior>
    - openTour() sets isTourOpen=true; closeTour() sets isTourOpen=false (session-scoped, NOT persisted — mirror isDashboardAuthOpen).
    - setIntroSeen(true) sets isIntroSeen=true AND writes localStorage iran-monitor.intro-seen='true' (persisted — mirror toggleMarkets).
    - isIntroSeen initial value reads readBool('iran-monitor.intro-seen', false); survives store re-init.
  </behavior>
  <action>
    Extend src/types/ui.ts UIState with: isIntroSeen:boolean; isTourOpen:boolean; setIntroSeen:(seen:boolean)=>void; openTour:()=>void; closeTour:()=>void. Extend src/stores/uiStore.ts create<UIState>()(...) literal with: isIntroSeen: readBool('iran-monitor.intro-seen', false); isTourOpen:false; setIntroSeen writes through to localStorage (try/catch, mirror toggleMarkets); openTour/closeTour set the flag (mirror openDashboardAuth/closeDashboardAuth). Keep the curried create<UIState>()() signature unchanged. Strict TS: add type members + store members in the same change or compile fails.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/stores/uiStore.reveal.test.ts; npm run typecheck; echo UISTORE_SLICE_OK</automated>
  </verify>
  <acceptance_criteria>
    - uiStore.reveal.test.ts GREEN; npm run typecheck passes.
    - setIntroSeen persists to localStorage; openTour/closeTour are session-scoped (no localStorage write).
  </acceptance_criteria>
  <done>uiStore + UIState carry the 5 reveal members; the store red stub is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build reveal components + tourSteps + data-tour attrs + AppShell mount + OG tags + domain decision</name>
  <files>src/lib/tourSteps.ts, src/components/reveal/IntroOverlay.tsx, src/components/reveal/GuidedTour.tsx, src/components/reveal/TourTrigger.tsx, src/components/layout/AppShell.tsx, src/components/layout/StatusDropdown.tsx, src/components/layout/LayerTogglesSlot.tsx, src/components/layout/DetailPanelSlot.tsx, src/components/layout/Topbar.tsx, index.html, docs/COSTS.md</files>
  <read_first>
    - 41-PATTERNS.md sections "IntroOverlay.tsx", "TourTrigger.tsx", "GuidedTour.tsx", "tourSteps.ts", "AppShell.tsx", "index.html" (every analog + caveat: z-[var(--z-modal)] not raw z-40, colorBridge tokens not inline hex, data-testid for RTL, behavior-only null render for GuidedTour, session-scoped isTourOpen)
    - src/components/ui/DashboardAuthModal.tsx (render-gate + backdrop + two-button footer + effect lifecycle analog)
    - src/components/layout/AppShell.tsx (line 63 root div; lines 108-110 DashboardAuthModal mount block — sibling mount point; line 69 data-testid=map-container)
    - src/components/layout/StatusDropdown.tsx:81 (topbar-status), Topbar.tsx:175 (dev-api-status-trigger), LayerTogglesSlot.tsx, DetailPanelSlot.tsx (data-tour attr targets); aria-label="Layers" for the layers panel
    - index.html (head lines 3-8; insertion point after line 7 title) + 41-RESEARCH.md section "OG / Twitter / Social-Share Mechanics" (the exact tag block, absolute URLs, 1200x630, og:image=/screenshots/og-card.png served path)
    - 41-04-SUMMARY.md (confirms og-card.png served path /screenshots/og-card.png)
    - the 4 Wave-0 stubs: IntroOverlay.test.tsx, TourTrigger.test.tsx, tourSteps.test.tsx, og-tags.test.ts
  </read_first>
  <behavior>
    - IntroOverlay: renders when isIntroSeen=false; null when true; "Explore the map" -> setIntroSeen(true); "Start the tour" -> setIntroSeen(true)+openTour(). data-testid="intro-overlay".
    - TourTrigger: always rendered regardless of isIntroSeen; click -> openTour. data-testid="tour-trigger".
    - tourSteps: every step's [data-tour=...] selector resolves to a present node in the rendered AppShell.
    - og-tags: index.html contains all required OG/Twitter/description tags with absolute vercel.app URLs + width 1200/height 630.
  </behavior>
  <action>
    (a) src/lib/tourSteps.ts: export tourSteps array of {element:'[data-tour="..."]', popover:{title,description}} — 5-7 steps: status (StatusDropdown), layers (LayerTogglesSlot), detail (DetailPanelSlot), threat-density legend, api-health (DevApiStatus trigger). (b) Add stable data-tour="status|layers|detail|threat-density|api-health" attrs to the matching HUD nodes (StatusDropdown.tsx, LayerTogglesSlot.tsx, DetailPanelSlot.tsx, Topbar.tsx dev-api-status-trigger) — do NOT spotlight in-canvas map entities (Pitfall 1); to point at the map use data-testid=map-container. (c) IntroOverlay.tsx + TourTrigger.tsx per the DashboardAuthModal analog: z-[var(--z-modal)] / z-[var(--z-controls)], colorBridge semantic utilities (border-border, bg-surface-overlay, text-text-muted, bg-black/60) — NO inline hex. (d) GuidedTour.tsx: behavior-only null render; useEffect keyed on isTourOpen drives driver({showProgress:true, steps:tourSteps, onDestroyed: ()=>closeTour()}).drive() with cleanup tour.destroy(). If Task 1 was REJECTED, implement GuidedTour as a DIY SVG-cutout overlay over the same data-tour targets instead of importing driver.js. (e) Mount <IntroOverlay/>, <GuidedTour/>, <TourTrigger/> as siblings of <DashboardAuthModal/> in AppShell root div (D-01: dashboard IS the landing surface — do NOT add a second route; do NOT restyle the Topbar tab bar per 40-CONTEXT D-04b). (f) index.html: insert the static OG/Twitter/description meta block after the <title> (line 7) — absolute https://otg-iran-monitor.vercel.app/... URLs, og:image=https://otg-iran-monitor.vercel.app/screenshots/og-card.png, og:image:width 1200 / og:image:height 630, twitter:card=summary_large_image. (g) Record REVEAL-SITE-04 in docs/COSTS.md as "stay on otg-iran-monitor.vercel.app" (D-09) — the requirement is satisfied by the recorded decision.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/reveal/ src/__tests__/og-tags.test.ts; npm run typecheck; npm run lint; grep -q 'og:image' index.html; grep -q 'otg-iran-monitor.vercel.app/screenshots/og-card.png' index.html; grep -qi 'stay' docs/COSTS.md; echo REVEAL_SITE_OK</automated>
  </verify>
  <acceptance_criteria>
    - IntroOverlay.test.tsx, TourTrigger.test.tsx, tourSteps.test.tsx, og-tags.test.ts ALL GREEN; npm run typecheck + lint pass.
    - index.html has OG/Twitter/description tags with absolute vercel.app URLs + 1200x630 + og-card.png served path.
    - Reveal components use z-[var(--z-...)] tokens + colorBridge utilities (no inline hex); GuidedTour spotlights only data-tour HUD nodes.
    - REVEAL-SITE-04 stay-on-vercel.app decision recorded (D-09); no second route added; Topbar tab-bar chrome not restyled (40 D-04b).
  </acceptance_criteria>
  <done>All reveal components + tour + OG tags ship; the 4 Wave-0 stubs are green; domain decision recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual tour walkthrough + OG-card validator</name>
  <files>src/components/reveal/GuidedTour.tsx, index.html</files>
  <action>
    The executor prompts the human to manually verify the spotlight geometry and crawler-side OG rendering (jsdom cannot test either). 1. npm run dev; in a fresh profile (clear localStorage iran-monitor.intro-seen), confirm IntroOverlay appears on first load; "Explore the map" dismisses it; reload confirms it stays dismissed (localStorage persisted). 2. Click the persistent Tour affordance; step through; confirm the spotlight lands on each intended HUD node (StatusDropdown -> layer toggles -> DetailPanelSlot -> threat-density legend -> API Health), not the WebGL canvas / not a flight icon. 3. After deploy, validate the OG card on the vercel.app URL via LinkedIn Post Inspector + Twitter/X Card Validator; confirm og-card.png resolves at https://otg-iran-monitor.vercel.app/screenshots/og-card.png and renders at 1200x630. Also confirm the D-02 read-only API-Health surface exposes no Bearer/token/key and that write paths (replay/prune/force-trigger) still demand a Bearer.
  </action>
  <what-built>
    The first-visit IntroOverlay, the re-openable driver.js GuidedTour, the persistent TourTrigger, and the static OG/Twitter card — all needing manual eyes for spotlight geometry + crawler OG rendering + D-02 read-only verification.
  </what-built>
  <how-to-verify>
    Intro overlay persists across reload; tour spotlights each HUD node (not the canvas); OG card previews at 1200x630 on LinkedIn/Twitter validators; API-Health read-only view leaks no secrets and write paths stay Bearer-gated.
  </how-to-verify>
  <read_first>
    - 41-VALIDATION.md section "Manual-Only Verifications" (driver.js spotlight + OG preview rationale + instructions)
  </read_first>
  <verify>
    <automated>grep -q 'twitter:card' index.html; echo OG_TAGS_PRESENT_PENDING_MANUAL</automated>
  </verify>
  <done>Intro overlay persists; tour spotlights each HUD node; OG card previews; API-Health write paths still Bearer-gated, read-only view leaks no secrets.</done>
  <resume-signal>Type "approved" once the intro overlay persists, the tour spotlights each HUD node, and the OG card previews correctly; or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                              |
| ------------------------------------------------------- | -------------------------------------------------------- |
| Visitor browser -> index.html meta tags                 | Crawlers read static HTML; no runtime interpolation      |
| Unauthenticated visitor -> API Health read-only surface | D-02: read-only visible; write-actions stay Bearer-gated |
| driver.js / overlay copy -> DOM render                  | Static in-repo step/overlay copy, not user input         |

## STRIDE Threat Register

| Threat ID | Category                                       | Component                            | Disposition | Mitigation Plan                                                                                                                                                                                                                                                 |
| --------- | ---------------------------------------------- | ------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-XSS  | Tampering/XSS                                  | IntroOverlay + GuidedTour step copy  | mitigate    | All overlay/tour copy is static in-repo strings rendered as React children (auto-escaped); NO dangerouslySetInnerHTML of untrusted/runtime data. tourSteps popover title/description are literals. Severity: low.                                               |
| T-41-OG   | Tampering                                      | index.html OG/Twitter meta           | mitigate    | og:/twitter: values are static literals with absolute vercel.app URLs; no user/runtime interpolation -> no reflected/stored-XSS via meta. Severity: low.                                                                                                        |
| T-41-D02  | Information disclosure / unauthenticated write | API Health read-only exposure (D-02) | mitigate    | NO new gating built — verify the EXISTING Bearer gate still fences all write paths (replay/prune/force-trigger) and the read-only surface leaks no tokens/keys in surfaced payloads. Verification truth (Task 4), not new code. Severity: medium (verify-only). |
| T-41-SC   | Tampering                                      | driver.js npm install                | mitigate    | Task 1 is a blocking-human package-legitimacy checkpoint (npm view version/maintainers/dependencies/postinstall); never auto-approvable; fallback = zero-dep custom overlay. Severity: medium, mitigated by checkpoint.                                         |

No threat in this plan reaches `high`. Supply-chain risk gated by a blocking-human checkpoint (Task 1); the read-only operator surface (D-02) introduces no new write path and is verified, not rebuilt.
</threat_model>

<verification>
- npx vitest run src/__tests__/components/reveal/ src/__tests__/stores/uiStore.reveal.test.ts src/__tests__/og-tags.test.ts -> all GREEN.
- npm run typecheck + npm run lint pass; npx vitest run (full suite) green at wave merge.
- index.html has OG/Twitter/description tags, absolute vercel.app URLs, 1200x630, og-card.png served path.
- Manual: intro overlay persists; tour spotlights each HUD node; OG card previews; API-Health write paths still Bearer-gated.
- REVEAL-SITE-04 stay-on-vercel.app decision recorded.
</verification>

<success_criteria>

- SC41-4 satisfied: intro overlay + re-openable tour + social-share assets + custom-domain decision all ship.
- driver.js installed only after passing the legitimacy checkpoint (or custom-overlay fallback used).
- No second landing route; Topbar tab-bar chrome untouched (40 D-04b); colorBridge tokens + z-index vars only.
  </success_criteria>

<output>
Create `.planning/phases/41-public-reveal-polish/41-06-SUMMARY.md` when done.
</output>

---
phase: 41-public-reveal-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/41-public-reveal-polish/41-AUDIT.md
  - src/__tests__/components/reveal/IntroOverlay.test.tsx
  - src/__tests__/components/reveal/TourTrigger.test.tsx
  - src/__tests__/components/reveal/tourSteps.test.tsx
  - src/__tests__/stores/uiStore.reveal.test.ts
  - src/__tests__/og-tags.test.ts
  - src/__tests__/capture-layers.contract.test.ts
  - src/__tests__/docs-exist.test.ts
autonomous: false
requirements:
  - REVEAL-DOCS-10
  - REVEAL-SITE-01
  - REVEAL-SITE-02
  - REVEAL-SITE-03
  - REVEAL-DOCS-07
user_setup: []

must_haves:
  truths:
    - 'The v1.5-close 2nd-pass code+docs audit is re-run against then-current main BEFORE any REVEAL-DOCS doc is authored (SC41-1)'
    - 'project_v1_6_cleanup_punchlist and project_v1_6_docs_drift memories are refreshed: resolved items dropped, net-new merged, still-open carried into Phase 41 scope'
    - 'Net-new findings are recorded as concrete fix tasks routed to the correct later wave by domain'
    - 'Seven Wave-0 Vitest files exist as failing/red stubs that pin the REVEAL-SITE + capture contract before implementation'
  artifacts:
    - path: '.planning/phases/41-public-reveal-polish/41-AUDIT.md'
      provides: 'Merged audit findings doc: resolved/still-open/net-new classification + routing table'
      contains: 'net-new'
    - path: 'src/__tests__/stores/uiStore.reveal.test.ts'
      provides: 'Red stub for the uiStore reveal slice (isIntroSeen/isTourOpen/openTour/closeTour/setIntroSeen)'
    - path: 'src/__tests__/og-tags.test.ts'
      provides: 'Red stub asserting index.html OG/Twitter tags + absolute URLs + 1200x630 dims'
    - path: 'src/__tests__/capture-layers.contract.test.ts'
      provides: 'Red stub asserting capture:layers output filenames + ALL_LAYERS + public/screenshots path constants'
  key_links:
    - from: '41-AUDIT.md net-new findings'
      to: 'Waves 1-3 fix tasks'
      via: 'domain-routing table (code fixes -> Wave 3 site/capture; docs fixes -> relevant docs wave)'
      pattern: 'route|Wave [123]'
    - from: 'Wave-0 test stubs'
      to: 'Plan 06 implementation (REVEAL-SITE) + Plan 04 (capture:layers)'
      via: 'test files turn green when implementation lands'
      pattern: 'vitest run'
---

<objective>
Wave-0 BLOCKING gate. Two jobs, both of which must land before any REVEAL-DOCS doc is authored:

1. **D-10 final-sweep audit (REVEAL-DOCS-10, satisfies SC41-1):** Re-run the v1.5-close 2nd-pass code+docs audit against then-current `main` (post-Phase-38/39/40) using parallel subagents. Diff findings against the `project_v1_6_cleanup_punchlist` (19-item) + `project_v1_6_docs_drift` (23-item) operator memories. Classify each as resolved (drop), still-open (carry into Phase 41 scope), or net-new (add). Merge net-new + still-open into Phase 41 scope as concrete fix tasks routed to the right later wave. Refresh BOTH memory files.
2. **Wave-0 test infra (from 41-VALIDATION.md):** Create the 7 new Vitest files as red stubs/contracts so the REVEAL-SITE store slice, overlay components, tour-step selector existence, OG tags, and capture:layers contract are pinned before implementation lands in Plans 04 + 06.

Purpose: SC41-1 mandates the audit precede docs work; the test stubs satisfy `workflow.nyquist_validation` so every downstream implementation task has an automated verify already on disk.
Output: `41-AUDIT.md` findings doc + 2 refreshed memory files + 7 red Vitest stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-reveal-polish/41-CONTEXT.md
@.planning/phases/41-public-reveal-polish/41-RESEARCH.md
@.planning/phases/41-public-reveal-polish/41-VALIDATION.md
@.planning/phases/41-public-reveal-polish/41-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase (41) creates the following NEW symbols/paths. Source-grounding drift passes must EXCLUDE these as not-yet-existing-at-plan-time:

- Components: `src/components/reveal/IntroOverlay.tsx`, `src/components/reveal/GuidedTour.tsx`, `src/components/reveal/TourTrigger.tsx`
- Data module: `src/lib/tourSteps.ts` (export `tourSteps`)
- uiStore additions: `isIntroSeen`, `isTourOpen`, `setIntroSeen`, `openTour`, `closeTour` (+ matching `UIState` members)
- npm script: `capture:layers` + `--layers` mode in `scripts/capture-hero.ts`
- Doc paths: `docs/BUILDING-WITH-CLAUDE-CODE.md`, `docs/SHOWCASE.md`, `docs/JOURNEY.md`, `docs/concepts.md`, `docs/COSTS.md`, `docs/operator-guide.md`, `docs/LESSONS.md`
- Assets: `public/screenshots/*.png` (relocated 6 + ~10 new), `public/screenshots/hero.gif`, `public/screenshots/og-card.png`
- index.html: new OG/Twitter/description meta tags
- Test files: the 7 created in THIS plan
- Audit artifact: `.planning/phases/41-public-reveal-polish/41-AUDIT.md`
- localStorage key: `iran-monitor.intro-seen`; data-tour attributes on HUD chrome
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run D-10 parallel-subagent final-sweep audit and merge into 41-AUDIT.md</name>
  <read_first>
    - .planning/phases/41-public-reveal-polish/41-RESEARCH.md §"D-10 Wave-0 Audit Gate (REVEAL-DOCS-10)" (task shape: code-audit subagent + docs-audit subagent + merge step)
    - ~/.claude/projects/-Users-zackmaz-Desktop-otg-iran-monitor/memory/project_v1_6_cleanup_punchlist.md (19-item code/LLM-pipeline list, captured 2026-06-03)
    - ~/.claude/projects/-Users-zackmaz-Desktop-otg-iran-monitor/memory/project_v1_6_docs_drift.md (23-item docs-drift list, captured 2026-06-03)
    - .planning/STATE.md §"Key Decisions" (Phase 38 closed PRO-01..04; confirms which punch-list/drift items Phases 38/39/40 already resolved)
    - CLAUDE.md (then-current canonical state — the audit diffs docs claims against this)
  </read_first>
  <action>
    Spawn TWO parallel audit subagents (per D-10 "accept higher token spend for thoroughness"):
    (a) CODE-AUDIT subagent — re-run the v1.5-close 2nd-pass code audit against then-current `main`. For each of the 19 cleanup-punchlist items, classify: `resolved` (Phase 38/39/40 fixed it — drop), `still-open` (carry into Phase 41 scope), or `net-new` (a new issue not on the list — add). Items 1-3 are BUGS (health.ts:170 lastErrorReason token split, events.test.ts v1->v3 mocks, water.ts:359 cache-write skip) — verify each against current main since Phase 38 LLM-FIX-01/02 claimed to address them.
    (b) DOCS-AUDIT subagent — same against the 23-item docs-drift list. Verify the CLAUDE.md "Hobby cap / 3-cron" stale framing and `docs/architecture/deployment.md` "Hobby 60s ceiling" items (already flagged in 999.6-CONTEXT item 11 and STATE.md says Phase 38 PRO-04 repaired the Pro 800s/40-cron framing — confirm).
    Then MERGE: write `.planning/phases/41-public-reveal-polish/41-AUDIT.md` with three sections — Resolved (dropped), Still-Open (carried), Net-New (added) — plus a ROUTING table mapping each carried/net-new finding to its target Phase 41 wave by domain (code fixes -> Wave 3 site/capture plans; docs fixes -> the relevant docs wave). This audit is a PLANNING gate, not shipped code; its output is the routing table + refreshed memories.
    Do NOT author any REVEAL-DOCS markdown in this task — the audit must land first (SC41-1).
  </action>
  <verify>
    <automated>test -f .planning/phases/41-public-reveal-polish/41-AUDIT.md && grep -qi "net-new" .planning/phases/41-public-reveal-polish/41-AUDIT.md && grep -qiE "resolved|dropped" .planning/phases/41-public-reveal-polish/41-AUDIT.md && grep -qiE "wave [123]|route" .planning/phases/41-public-reveal-polish/41-AUDIT.md && echo AUDIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `41-AUDIT.md` exists with Resolved / Still-Open / Net-New sections.
    - Every one of the 3 BUGS (punch-list items 1-3) has an explicit verdict against current main.
    - A routing table maps each carried/net-new finding to a Phase 41 wave (1/2/3) by domain.
    - No REVEAL-DOCS markdown doc was created in this task (SC41-1 ordering: audit precedes docs).
  </acceptance_criteria>
  <done>The 2nd-pass code+docs audit re-ran against current main; findings classified and merged into 41-AUDIT.md with a wave-routing table.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh both audit memory files (drop resolved, add net-new)</name>
  <read_first>
    - .planning/phases/41-public-reveal-polish/41-AUDIT.md (output of Task 1 — the resolved/still-open/net-new classification)
    - ~/.claude/projects/-Users-zackmaz-Desktop-otg-iran-monitor/memory/project_v1_6_cleanup_punchlist.md (front-matter shape: name/description/metadata YAML + body sections 🔴 BUGS / 🟡 DEAD CODE)
    - ~/.claude/projects/-Users-zackmaz-Desktop-otg-iran-monitor/memory/project_v1_6_docs_drift.md (front-matter shape + BROKEN/stale bucket structure)
  </read_first>
  <action>
    Apply the Task-1 classification to BOTH memory files in place: remove items the audit marked `resolved`; append items marked `net-new` in the same section style (🔴 BUGS / 🟡 DEAD CODE for the punch-list; BROKEN/stale buckets for the drift list); leave `still-open` items but annotate them "carried into Phase 41 (see 41-AUDIT.md)". Preserve each file's YAML front-matter (name/description/metadata) verbatim except bump a `last_refreshed: 2026-06-05` note in the body intro. Keep the `[[wikilink]]` cross-reference between the two memories intact. Do not delete the files; edit in place.
  </action>
  <verify>
    <automated>M="$HOME/.claude/projects/-Users-zackmaz-Desktop-otg-iran-monitor/memory"; grep -qi "Phase 41\|2026-06-05\|carried" "$M/project_v1_6_cleanup_punchlist.md" && grep -qi "Phase 41\|2026-06-05\|carried" "$M/project_v1_6_docs_drift.md" && echo MEMORIES_REFRESHED</automated>
  </verify>
  <acceptance_criteria>
    - Both memory files retain their YAML front-matter and the `[[wikilink]]` cross-reference.
    - Resolved items removed; net-new items appended in matching section style; still-open items annotated "carried into Phase 41".
    - Each file's body intro carries a 2026-06-05 refresh note.
  </acceptance_criteria>
  <done>Both operator memories reflect post-Phase-38/39/40 reality with resolved dropped, net-new added, carried-forward annotated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 7 Wave-0 Vitest stub files (red contracts) for REVEAL-SITE + capture</name>
  <read_first>
    - .planning/phases/41-public-reveal-polish/41-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map" (the 7 files + exact behaviors each asserts)
    - src/stores/uiStore.ts (lines 5-12 readBool, 27 markets initial, 60-62 dashboard-auth slice, 99-115 toggleMarkets) — the slice shapes the uiStore.reveal stub asserts
    - src/components/ui/DashboardAuthModal.tsx (data-testid="dashboard-auth-modal", render-gate idiom) — the RTL pattern IntroOverlay.test.tsx mirrors
    - scripts/capture-hero.ts (line 61 ALL_LAYERS, lines 42-44 path constants, line 67 parseArgs) — the capture-layers.contract stub asserts against these
    - index.html (current head lines 3-8) — the og-tags stub asserts the tags that DO NOT yet exist
  </read_first>
  <behavior>
    These are RED stubs — they MUST fail (or be `it.todo`/`describe.skip`-free assertions against not-yet-existing code) until Plans 04 + 06 implement. Each file pins one contract:
    - uiStore.reveal.test.ts: `openTour()`/`closeTour()` toggle `isTourOpen`; `setIntroSeen(true)` sets `isIntroSeen` AND writes `iran-monitor.intro-seen=true` to localStorage; flag survives store re-init (read back via readBool).
    - IntroOverlay.test.tsx: renders when `isIntroSeen=false`; returns null when `true`; "Explore the map" click calls `setIntroSeen(true)`; "Start the tour" click calls `setIntroSeen(true)` + `openTour()`. Asserts `data-testid="intro-overlay"`.
    - TourTrigger.test.tsx: renders the Tour affordance REGARDLESS of `isIntroSeen` (not first-visit-gated, D-03); click calls `openTour`. Asserts `data-testid="tour-trigger"`.
    - tourSteps.test.tsx: render AppShell; for every step in `tourSteps`, assert its `data-tour` selector resolves to a present DOM node (selector-existence contract).
    - og-tags.test.ts: read `index.html`; assert presence of `og:type/og:title/og:description/og:url/og:image/og:image:width=1200/og:image:height=630/twitter:card=summary_large_image/twitter:image` and `name="description"`; assert og:image + og:url use absolute `https://otg-iran-monitor.vercel.app/...`.
    - capture-layers.contract.test.ts: import/parse `scripts/capture-hero.ts`; assert `ALL_LAYERS` contains the 6 viz labels (incl. `Geographic` + `Climate`); assert the output-path constants point at `public/screenshots/`; assert a `--layers` mode is recognized by parseArgs; assert the expected ~10 output filenames are enumerated.
    - docs-exist.test.ts (optional per VALIDATION): assert `docs/SHOWCASE.md`, `docs/JOURNEY.md`, `docs/concepts.md`, `docs/COSTS.md`, `docs/operator-guide.md`, `docs/LESSONS.md`, `docs/BUILDING-WITH-CLAUDE-CODE.md` exist.
  </behavior>
  <action>
    Create all 7 files under their VALIDATION-specified paths. Use Vitest + @testing-library/react (already present; map libs mocked via vite.config.ts test.alias). For RTL component stubs, import the not-yet-existing component path — the import failure / assertion failure IS the red state. For the contract/file-read stubs (og-tags, capture-layers, docs-exist), use `fs.readFileSync` against the real files and assert content that does not yet exist. Do NOT mark these `it.todo` — they must be real failing assertions so they go green when implementation lands. Keep each file's selectors/keys EXACTLY matching the implementation plans: `iran-monitor.intro-seen`, `data-testid="intro-overlay"`, `data-testid="tour-trigger"`, `data-tour="..."`, `https://otg-iran-monitor.vercel.app/screenshots/og-card.png`.
  </action>
  <verify>
    <automated>for f in src/__tests__/stores/uiStore.reveal.test.ts src/__tests__/components/reveal/IntroOverlay.test.tsx src/__tests__/components/reveal/TourTrigger.test.tsx src/__tests__/components/reveal/tourSteps.test.tsx src/__tests__/og-tags.test.ts src/__tests__/capture-layers.contract.test.ts src/__tests__/docs-exist.test.ts; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done; echo ALL_7_STUBS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist at their VALIDATION-specified paths.
    - Running `npx vitest run src/__tests__/components/reveal/ src/__tests__/stores/uiStore.reveal.test.ts src/__tests__/og-tags.test.ts src/__tests__/capture-layers.contract.test.ts` produces RED (failing) results — they pin contracts not yet implemented.
    - Each file's keys/selectors byte-match the identifiers the implementation plans (04, 06) will use.
  </acceptance_criteria>
  <done>Seven red Vitest stubs exist, each pinning one REVEAL-SITE or capture contract, ready to go green in Plans 04 and 06.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                    |
| --------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Audit subagent -> operator memory files | Subagent-authored edits to `~/.claude/.../memory/*.md` (local, developer-trusted, not runtime) |
| Test stubs -> test runner               | Static test code, no untrusted input                                                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                      |
| --------- | ---------------------- | ---------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-01   | Tampering              | Memory-file refresh (Task 2) | accept      | Local developer-trusted files outside the shipped bundle; YAML front-matter preserved verbatim; no runtime exposure. Severity: low.                                                                  |
| T-41-02   | Information disclosure | 41-AUDIT.md findings         | accept      | Audit references in-repo paths only; no secrets/tokens surfaced. The doc is a planning artifact under `.planning/`. Severity: low.                                                                   |
| T-41-SC   | Tampering              | npm/pip/cargo installs       | mitigate    | This plan installs NOTHING (test stubs use already-present Vitest/RTL). The driver.js install + its legitimacy checkpoint live in Plan 06. No package-manager action here. Severity: none-this-plan. |

No threat in this plan reaches `high`. This is a planning-gate + test-scaffold plan; the audit gate and stubs introduce no runtime attack surface.
</threat_model>

<verification>
- `41-AUDIT.md` exists with resolved/still-open/net-new classification + wave-routing table (SC41-1 satisfied: audit precedes any docs work).
- Both operator memories refreshed in place (front-matter + wikilink preserved).
- All 7 Wave-0 Vitest files present; the REVEAL-SITE + capture subset runs RED.
- `npx vitest run src/__tests__/components/reveal/ src/__tests__/stores/uiStore.reveal.test.ts src/__tests__/og-tags.test.ts src/__tests__/capture-layers.contract.test.ts` (expected RED until Plans 04/06).
</verification>

<success_criteria>

- D-10 audit complete and merged; SC41-1 satisfied (audit ran before any REVEAL-DOCS doc was authored).
- Net-new findings routed to later waves; no audit finding silently dropped without a verdict.
- 7 red test stubs on disk pinning every automatable REVEAL-SITE + capture contract.
- This Wave-0 plan blocks all of Waves 1-3 (every later plan depends_on 41-01).
  </success_criteria>

<output>
Create `.planning/phases/41-public-reveal-polish/41-01-SUMMARY.md` when done. Include the net-new finding -> wave routing table so Plans 02-06 can absorb their assigned fixes.
</output>

---
phase: 41-public-reveal-polish
plan: 03
type: execute
wave: 2
depends_on: [41-01, 41-02]
files_modified: [docs/concepts.md, docs/COSTS.md, docs/operator-guide.md]
autonomous: true
requirements: [REVEAL-DOCS-04, REVEAL-DOCS-05, REVEAL-DOCS-06]
user_setup: []
must_haves:
  truths:
    - 'concepts.md defines >=30 proprietary terms incl Pitfall 1, 6-path resolver, flight recorder, tier-green gate, ghost event, degrade-open, honest deferral, probe-before-commit (SC41-3)'
    - 'COSTS.md: Vercel Pro $20/mo only paid line; NIM/Upstash/GDELT/OpenSky/adsb.lol/Open-Meteo/Yahoo/AISStream/Overpass/WRI/Natural-Earth/GeoEPR free; Claude Code dev cost (SC41-3)'
    - 'operator-guide.md is a visitor how-to (clone+run, force-trigger cron, prune dead URLs, read /api/operator-status, run eval, capture hero GIF) distinct from runbook incident response (SC41-3)'
  artifacts:
    - { path: 'docs/concepts.md', provides: '~30-term glossary', min_lines: 60 }
    - { path: 'docs/COSTS.md', provides: 'Cost transparency, you-can-do-this-too', min_lines: 30 }
    - {
        path: 'docs/operator-guide.md',
        provides: 'Visitor how-to (NOT incident response)',
        min_lines: 40,
      }
  key_links:
    - {
        from: 'docs/operator-guide.md',
        to: 'package.json scripts + /api/operator-status + ?force=true cron',
        via: 'documented commands/endpoints',
        pattern: 'eval:replay|operator-status|force=true',
      }
    - {
        from: 'docs/COSTS.md + docs/operator-guide.md',
        to: 'docs/SHOWCASE.md',
        via: 'cross-link to hub',
        pattern: 'SHOWCASE',
      }
---

<objective>
Wave 2 — round out the portfolio docs (D-08). concepts.md (REVEAL-DOCS-04, ~30-term glossary from CLAUDE.md), COSTS.md (REVEAL-DOCS-05, honest accounting + REVEAL-SITE-04 stay-on-vercel.app cost rationale per D-09), operator-guide.md (REVEAL-DOCS-06, visitor how-to distinct from runbook incident response). Purpose: SC41-3. Output: 3 docs cross-linked to SHOWCASE.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-reveal-polish/41-CONTEXT.md
@.planning/phases/41-public-reveal-polish/41-RESEARCH.md
@.planning/phases/41-public-reveal-polish/41-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See 41-01-PLAN.md for the full phase-41 new-symbol list. This plan creates: docs/concepts.md, docs/COSTS.md, docs/operator-guide.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/concepts.md (>= 30-term glossary)</name>
  <read_first>
    - 41-RESEARCH.md section "REVEAL-DOCS Source-Material Map" (concepts row: CLAUDE.md authoritative; CONTEXT seed list)
    - CLAUDE.md (authoritative defs: Pitfall 1 bridge / 6-path resolver / degrade-open / flight recorder / tier-green gate / ghost event / canonical actor catalog / mechanical drift gate / honest deferral / probe-before-commit / polite-citizen contracts / LLM-optional architecture)
    - 41-CONTEXT.md section specifics (~30-term seed list)
    - 41-01-SUMMARY.md (net-new docs findings routed to Wave 2)
  </read_first>
  <action>
    Write docs/concepts.md: AT LEAST 30 proprietary terms, each a markdown heading + 1-3 sentence definition from CLAUDE.md. MUST include all seed terms: Pitfall 1 cache bridge, LLM-optional architecture, tier-green gate, polite-citizen contracts, ghost event, canonical actor catalog, mechanical drift gate, degrade-open, 6-path resolver, honest deferral, probe-before-commit, flight recorder. Round out to 30+ from CLAUDE.md (circuit breaker, token budget soft/hard, DLQ, watchdog generation counter, lineage record, cold-cache self-heal, colorBridge byte-identity sentinel, domain.ts mirror, CacheEntry staleness, capture:layers, Latin-label admission gate, severity half-life, attack radius, proximity alert, stale-clear, cron-only writer, Pitfall-1 GDELT bridge). Inline repo-relative links to ADRs/architecture docs where a term maps to one. Cross-link back to docs/SHOWCASE.md.
  </action>
  <verify>
    <automated>test -f docs/concepts.md; N=$(grep -cE '^#{2,4} ' docs/concepts.md); test "$N" -ge 30; grep -qi 'Pitfall 1' docs/concepts.md; grep -qi '6-path resolver' docs/concepts.md; grep -qi 'flight recorder' docs/concepts.md; grep -qi 'degrade-open' docs/concepts.md; grep -qi 'tier-green' docs/concepts.md; echo "TERMS=$N CONCEPTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - At least 30 term headings; all 12 named seed terms present and defined.
    - Definitions consistent with CLAUDE.md (no invented semantics); cross-links repo-relative; links back to SHOWCASE.
  </acceptance_criteria>
  <done>concepts.md defines 30+ proprietary terms with CLAUDE.md-accurate definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/COSTS.md (cost transparency)</name>
  <read_first>
    - 41-RESEARCH.md section "REVEAL-DOCS Source-Material Map" (COSTS row: CLAUDE.md cost facts + RETROSPECTIVE Cost Observations lines 52, 252)
    - CLAUDE.md (Vercel Pro $20/mo; all data sources free)
    - .planning/RETROSPECTIVE.md (Cost Observations: line 52 v0.9, line 252 v1.5 — session count, model mix, approx per-phase)
    - 41-CONTEXT.md section decisions D-09 (stay on vercel.app = zero cost — record rationale here)
  </read_first>
  <action>
    Write docs/COSTS.md in "you can do this too" voice. Table 1 infra: Vercel Pro $20/mo as the ONLY non-free line (required for 800s maxDuration per CLAUDE.md) + a free-tier row per data source (NIM, Upstash, GDELT, OpenSky, adsb.lol, Open-Meteo, Yahoo Finance, AISStream, Overpass, WRI, Natural Earth, GeoEPR). Table 2 dev cost: session count / model mix / approx per-phase from RETROSPECTIVE.md Cost Observations (cite lines, do NOT invent). Record REVEAL-SITE-04 rationale (D-09): staying on otg-iran-monitor.vercel.app = $0 domain, $0 DNS. Cross-link back to docs/SHOWCASE.md.
  </action>
  <verify>
    <automated>test -f docs/COSTS.md; grep -qi 'Vercel Pro' docs/COSTS.md; grep -qi '20' docs/COSTS.md; grep -qi 'free' docs/COSTS.md; grep -qi 'NIM' docs/COSTS.md; grep -qi 'Upstash' docs/COSTS.md; grep -qi 'vercel.app' docs/COSTS.md; echo COSTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Vercel Pro $20/mo shown as the only paid line; every data source listed as free; Claude Code dev cost present with cited RETROSPECTIVE figures; D-09 stay rationale recorded; links back to SHOWCASE.
  </acceptance_criteria>
  <done>COSTS.md gives honest, source-cited cost accounting with the you-can-do-this-too angle.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/operator-guide.md (visitor how-to)</name>
  <read_first>
    - 41-RESEARCH.md section "REVEAL-DOCS Source-Material Map" (operator-guide row: package.json scripts + endpoints + .env.example)
    - package.json (scripts: eval:replay line 24, capture:hero line 21, docs:lint line 38)
    - CLAUDE.md (force-trigger cron `?force=true` + Bearer; /api/operator-status aggregator; prune flow POST /api/events/prune-dead-urls)
    - docs/runbook.md (the incident-response counterpart — operator-guide must be DISTINCT, visitor-oriented)
  </read_first>
  <action>
    Write docs/operator-guide.md — a visitor how-to (NOT incident response). Sections: clone + run locally (npm install, .env.example setup), force-trigger the events cron (GET /api/cron/refresh-events?force=true with Bearer), prune dead URLs from the dashboard, read the /api/operator-status payload, run the eval harness (npm run eval:replay), capture a fresh hero GIF (npm run capture:hero). State up top that this is the visitor how-to and link to docs/runbook.md for incident response. Cross-link back to docs/SHOWCASE.md. Keep links repo-relative.
  </action>
  <verify>
    <automated>test -f docs/operator-guide.md; grep -qi 'npm install' docs/operator-guide.md; grep -qi 'eval:replay' docs/operator-guide.md; grep -qi 'capture:hero' docs/operator-guide.md; grep -qi 'operator-status' docs/operator-guide.md; grep -qi 'force=true' docs/operator-guide.md; grep -qi 'runbook' docs/operator-guide.md; echo OPGUIDE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Covers clone+run, force-trigger cron, prune dead URLs, /api/operator-status, eval:replay, capture:hero.
    - Explicitly framed as visitor how-to and links to runbook.md for incident response (distinct surfaces).
    - Cross-links back to SHOWCASE; links repo-relative.
  </acceptance_criteria>
  <done>operator-guide.md is a visitor-oriented how-to covering all six workflows, distinct from runbook.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                             |
| -------------------------------------------------- | ------------------------------------------------------- |
| Repo markdown -> GitHub renderer                   | Static markdown; no runtime/user input                  |
| operator-guide.md documents Bearer-gated endpoints | Doc describes existing auth; introduces no new endpoint |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                         |
| ------------ | ---------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-DOCS-03 | Information disclosure | operator-guide.md documenting force-trigger/prune | mitigate    | Document ONLY that the operations are Bearer-gated; never embed a real Bearer/DASHBOARD_PASSWORD value. Use placeholder `<your-bearer>`. Severity: low. |
| T-41-DOCS-04 | Information disclosure | COSTS.md dev-cost figures                         | accept      | Aggregate session/cost figures from RETROSPECTIVE.md; no account IDs or keys. Severity: low.                                                            |
| T-41-SC      | Tampering              | npm/pip/cargo installs                            | mitigate    | This plan installs NOTHING — markdown authoring only. Severity: none-this-plan.                                                                         |

No threat in this plan reaches `high`. Docs-only wave; operator-guide documents existing Bearer-gated paths without exposing secrets.
</threat_model>

<verification>
- docs/concepts.md has >=30 term headings incl all seed terms.
- docs/COSTS.md shows Vercel Pro $20/mo as sole paid line + free data sources + dev cost + D-09 rationale.
- docs/operator-guide.md covers all six visitor workflows, distinct from runbook, no real Bearer.
- npm run docs:lint resolves all new repo-relative links (wave merge).
</verification>

<success_criteria>

- SC41-3 (concepts + COSTS + operator-guide portion) satisfied.
- No invented figures; no secrets in operator-guide; all three cross-link to SHOWCASE.
  </success_criteria>

<output>
Create `.planning/phases/41-public-reveal-polish/41-03-SUMMARY.md` when done.
</output>

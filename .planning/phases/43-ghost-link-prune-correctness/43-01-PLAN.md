---
phase: 43-ghost-link-prune-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/urlLiveness.ts
  - server/__tests__/lib/urlLiveness.schema.test.ts
  - src/__tests__/lib/urlLiveness.schema.test.ts
  - docs/architecture/redis-keys.md
  - CLAUDE.md
autonomous: true
requirements: [GHOST-10]
must_haves:
  truths:
    - "UrlLivenessSchema accepts a 'soft-404' status entry and a 'no-url' status entry"
    - "UrlLivenessSchema accepts a 'no-url' entry with lastUrlProbed: null"
    - 'UrlLivenessSchema requires the evidence field (string|null) and rejects entries missing it'
    - "ttlSecForStatus('soft-404') === 86400 and ttlSecForStatus('no-url') === 86400"
    - "isTerminalDead('soft-404') === true and isTerminalDead('no-url') === false"
    - 'Both the server schema test and the src shim assert the new statuses, TTLs, and evidence field'
    - 'CLAUDE.md and redis-keys.md registry lines document the 7-status taxonomy, evidence field, nullable lastUrlProbed, and new attemptCount semantics'
  artifacts:
    - path: 'server/lib/urlLiveness.ts'
      provides: '7-status enum, evidence field on UrlLivenessSchema + ProbeResult, soft-404/no-url TTL tiers, isTerminalDead(soft-404)=true'
      contains: 'soft-404'
    - path: 'server/__tests__/lib/urlLiveness.schema.test.ts'
      provides: 'Schema pins for soft-404, no-url, evidence, nullable lastUrlProbed, TTL tiers'
      contains: 'no-url'
    - path: 'src/__tests__/lib/urlLiveness.schema.test.ts'
      provides: 'Literal-path shim mirroring new TTL + strict + evidence pins'
      contains: 'soft-404'
    - path: 'docs/architecture/redis-keys.md'
      provides: 'Updated events:url-liveness:{eventId} registry value-shape + taxonomy + TTL + attemptCount semantics'
      contains: 'soft-404'
    - path: 'CLAUDE.md'
      provides: 'Updated §Serverless Cache events:url-liveness:{eventId} registry line'
      contains: 'soft-404'
  key_links:
    - from: 'server/lib/urlLiveness.ts UrlLivenessStatusSchema'
      to: 'TTL_SEC_BY_STATUS Record<UrlLivenessStatus, number>'
      via: 'exhaustive Record type forces TTL entries for every enum member at compile time'
      pattern: 'Record<UrlLivenessStatus'
    - from: 'server/__tests__/lib/urlLiveness.schema.test.ts validEntry()'
      to: 'UrlLivenessSchema.strict()'
      via: 'validEntry must include evidence or strict() rejects every downstream test'
      pattern: 'evidence'
---

<objective>
Widen the `UrlLiveness` Zod contract to the 7-status taxonomy (`+soft-404 +no-url`), add the required-but-nullable `evidence` field, make `lastUrlProbed` nullable, add the two new 24h TTL tiers, and update `isTerminalDead` to count `soft-404` as terminal-dead (but NOT `no-url`). Land the four contract-lockstep surfaces (server schema test, src shim, redis-keys.md, CLAUDE.md) in the SAME commit so the `.strict()` drift gate stays green (D-18). This is the foundation Plans 02/03/04 compile against — it ships first and alone.

Purpose: A `.strict()` schema with a `Record<UrlLivenessStatus, number>` TTL map is the project's mechanical drift gate (D-22). Adding statuses/fields here propagates the type to every consumer at compile time and fails `vitest run` until every fixture and lockstep surface updates together. Doing the contract first means Plans 02-04 never fight a half-migrated schema.

Output: Extended `UrlLivenessStatusSchema` + `UrlLivenessSchema` + `ProbeResult` + `TTL_SEC_BY_STATUS` + `isTerminalDead`; updated schema test + shim; updated redis-keys.md + CLAUDE.md registry lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md
@.planning/phases/43-ghost-link-prune-correctness/43-RESEARCH.md
@.planning/phases/43-ghost-link-prune-correctness/43-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (Plan 01 — foundation):

- **New enum members:** `soft-404`, `no-url` added to `UrlLivenessStatusSchema` (`server/lib/urlLiveness.ts`).
- **New schema field:** `evidence: z.string().max(200).nullable()` on `UrlLivenessSchema` (required-but-nullable).
- **Changed field:** `lastUrlProbed` → `z.string().url().nullable()` (null for `no-url`).
- **New TTL tiers:** `TTL_SEC_BY_STATUS['soft-404'] = 24*3600`, `TTL_SEC_BY_STATUS['no-url'] = 24*3600`.
- **Changed predicate:** `isTerminalDead` now returns true for `soft-404` (NOT `no-url`).
- **New ProbeResult field:** `evidence: string | null` on the `ProbeResult` interface (so the writer carries it, consumed in Plans 02/03).

Downstream consumers (Phase 44) read: the new per-bucket statuses (`soft-404`, `no-url`) and the `evidence` string from `events:url-liveness:{eventId}`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen UrlLiveness schema, enum, TTL map, isTerminalDead, and ProbeResult</name>
  <read_first>
    - server/lib/urlLiveness.ts (lines 87-118 enum+schema, 137-152 TTL map, 232-240 ProbeResult interface, 442-444 isTerminalDead, 96-111 JSDoc attemptCount block)
    - .planning/phases/43-ghost-link-prune-correctness/43-PATTERNS.md (Analogs A, B, C — exact source excerpts)
    - .planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md (D-04, D-07, D-16, D-09)
  </read_first>
  <behavior>
    - Test: `UrlLivenessSchema.parse(validEntry({ status: 'soft-404' }))` does NOT throw
    - Test: `UrlLivenessSchema.parse(validEntry({ status: 'no-url', lastUrlProbed: null, lastHttpStatus: null, attemptCount: 0 }))` does NOT throw
    - Test: `UrlLivenessSchema.parse({ ...validEntry without evidence })` THROWS (evidence is required)
    - Test: `UrlLivenessSchema.parse(validEntry({ evidence: 'soft-404: matched "page not found" in title' }))` does NOT throw; max 200 chars enforced
    - Test: `ttlSecForStatus('soft-404') === 24 * 3600` and `ttlSecForStatus('no-url') === 24 * 3600`
    - Test: `isTerminalDead('soft-404') === true`; `isTerminalDead('no-url') === false`; `isTerminalDead('403') === true` (unchanged)
  </behavior>
  <action>
    In `server/lib/urlLiveness.ts`:
    (1) Extend `UrlLivenessStatusSchema = z.enum([...])` to include `'soft-404'` and `'no-url'` (final 7-member enum: `'live','404','403','dead-host','unknown','soft-404','no-url'`) per D-04/D-06.
    (2) On `UrlLivenessSchema` (`.strict()` object): change `lastUrlProbed: z.string().url()` to `z.string().url().nullable()` (D-07 — null for `no-url`); add a new required field `evidence: z.string().max(200).nullable()` (D-16 — required-but-nullable, 200-char ceiling).
    (3) Add `'soft-404': 24 * 3600` and `'no-url': 24 * 3600` to the `TTL_SEC_BY_STATUS: Record<UrlLivenessStatus, number>` map (D-04/D-09 — the exhaustive Record type will not compile until both are present).
    (4) In `isTerminalDead`, add `status === 'soft-404'` to the OR chain so it returns true for soft-404. Do NOT add `'no-url'` (D-08 — no-url is explicitly NOT terminal-dead). Do NOT touch `'403'` here (the GHOST-09 cron demotion is a prune-filter-local change in Plan 04, NOT an isTerminalDead change — RESEARCH anti-pattern).
    (5) On the `ProbeResult` interface (line ~232), add `evidence: string | null` so `persistLiveness` (Plan 03) reads it off the probe result rather than recomputing. Update every existing `return { status, httpStatus, finalUrl }` in `probeUrl` to also set `evidence: null` (status-code-only verdicts have no body evidence yet; soft-404 evidence is wired in Plan 02). For `404`/`403`/`dead-host` returns set `evidence` to the D-16 literal (`'http-404'`, `'http-403'`, `'dead-host: fetch failed'` respectively); for `live` and `unknown` returns set `evidence: null`.
    (6) Update the JSDoc attemptCount-semantics block at lines ~96-111 and the writer JSDoc at ~465-476 to describe the NEW rule that Plan 03 implements: replace "monotonic-with-reset-on-live-or-unknown" wording with "live resets to 0; unknown PRESERVES the prior count; dead→dead increments" (D-10). Cite the Phase 32 D-12/RESEARCH A2 origin and that Phase 43 D-10 amends it.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/otg-iran-monitor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "soft-404" server/lib/urlLiveness.ts` is >= 3 (enum + TTL + isTerminalDead)
    - `grep -c "no-url" server/lib/urlLiveness.ts` is >= 2 (enum + TTL)
    - `grep "evidence" server/lib/urlLiveness.ts` shows it on both the Zod schema and the `ProbeResult` interface
    - `npx tsc --noEmit` exits 0 (the `Record<UrlLivenessStatus, number>` map compiles only with both new keys present)
    - `isTerminalDead` source contains `status === 'soft-404'` and does NOT contain `status === 'no-url'`
  </acceptance_criteria>
  <done>The 7-status enum, evidence field, nullable lastUrlProbed, two new TTL tiers, and isTerminalDead(soft-404)=true are in place; ProbeResult carries evidence; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update schema test + src shim in lockstep (Pitfall 5 + Pitfall 6)</name>
  <read_first>
    - server/__tests__/lib/urlLiveness.schema.test.ts (full — validEntry at :31, enum loop at :81-85, TTL pins at :88-119)
    - src/__tests__/lib/urlLiveness.schema.test.ts (full — inline validEntry literals + TTL bound assertions)
    - server/lib/urlLiveness.ts (the schema just changed in Task 1)
    - .planning/phases/43-ghost-link-prune-correctness/43-RESEARCH.md (Pitfall 5, Pitfall 6)
  </read_first>
  <behavior>
    - Test: `validEntry()` factory now includes `evidence: null` so `.strict()` accepts every fixture
    - Test: enum-loop iterates the full 7-status list `['live','404','403','dead-host','unknown','soft-404','no-url']`, each parses
    - Test: a `no-url` entry with `lastUrlProbed: null` parses; an entry with a string `evidence` parses
    - Test: `ttlSecForStatus('soft-404')` and `ttlSecForStatus('no-url')` both === 24*3600 (exact-ceiling block) and <= 86400 (upper-bound block)
    - Test (shim): inline literal carries `evidence`; shim asserts `soft-404`/`no-url` TTL bounds
  </behavior>
  <action>
    In `server/__tests__/lib/urlLiveness.schema.test.ts`: add `evidence: null` to the `validEntry()` factory default (without it `.strict()` rejects every fixture). Extend the enum-loop array (currently `['live','404','403','dead-host','unknown']`) to add `'soft-404'` and `'no-url'`. Add positive cases: parse a `no-url` entry with `lastUrlProbed: null` / `lastHttpStatus: null` / `attemptCount: 0`; parse an entry with a non-null string `evidence`; assert an entry MISSING `evidence` throws (strict requires it). In the TTL block, add `expect(ttlSecForStatus('soft-404')).toBe(24 * 3600)` + `expect(ttlSecForStatus('no-url')).toBe(24 * 3600)` to the exact-ceiling section (~:114-118) and `<= 86400` to the upper-bound section (~:88-106).
    In `src/__tests__/lib/urlLiveness.schema.test.ts` (the literal-path shim — keep it minimal per its design): add `evidence` to the inline `validEntry`-shape literal so strict() accepts it; add `expect(ttlSecForStatus('soft-404')).toBeLessThanOrEqual(86400)` and `expect(ttlSecForStatus('no-url')).toBeLessThanOrEqual(86400)` to the TTL-bound block (mirroring the existing `live`/`404`/`unknown` bound assertions).
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/otg-iran-monitor && npx vitest run server/__tests__/lib/urlLiveness.schema.test.ts src/__tests__/lib/urlLiveness.schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/__tests__/lib/urlLiveness.schema.test.ts` passes (all cases green)
    - `npx vitest run src/__tests__/lib/urlLiveness.schema.test.ts` passes
    - `grep -c "soft-404" server/__tests__/lib/urlLiveness.schema.test.ts` >= 2 (enum loop + TTL pin)
    - `grep "evidence" src/__tests__/lib/urlLiveness.schema.test.ts` returns a match (shim inline literal carries it)
    - schema test contains an explicit assertion that an entry missing `evidence` throws
  </acceptance_criteria>
  <done>Both the server schema test and the src shim pin the 7-status taxonomy, the evidence field, nullable lastUrlProbed, and the two new TTL tiers; both suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Update redis-keys.md + CLAUDE.md registry lockstep (D-18)</name>
  <read_first>
    - docs/architecture/redis-keys.md (line 29 events:url-liveness:{eventId} row + line 30 count row)
    - CLAUDE.md (line ~145 §Serverless Cache events:url-liveness:{eventId} registry line)
    - .planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md (D-18)
    - .planning/phases/43-ghost-link-prune-correctness/43-RESEARCH.md (Project Constraints — A6: redis-registry.test.ts pins only the key NAME, not value shape; NO change needed there)
  </read_first>
  <action>
    In `docs/architecture/redis-keys.md` line 29 (the `events:url-liveness:{eventId}` row): update the value-shape cell from `JSON {status, lastProbedAt, attemptCount, lastUrlProbed, lastHttpStatus}` to include the new `evidence` field and reflect nullable `lastUrlProbed`; update the TTL cell to add `soft-404`/`no-url` 24h (`live` 7d, terminal dead `404`/`403`/`dead-host`/`soft-404` 24h, `no-url` 24h, `unknown` 1h). Document the 7-status taxonomy explicitly.
    In `CLAUDE.md` line ~145 (`events:url-liveness:{eventId}` §Serverless Cache line): update the status union from `'live'|'404'|'403'|'dead-host'|'unknown'` to add `'soft-404'|'no-url'`; add `evidence: string|null` to the documented JSON shape; mark `lastUrlProbed: string|null`; update the TTL tier list to add `soft-404`/`no-url` 24h; replace the trailing `attemptCount semantics: monotonic-with-reset-on-live-or-unknown` with the NEW D-10 wording: `attemptCount semantics: live resets to 0, unknown PRESERVES prior count, dead→dead increments (Phase 43 D-10)`. Reference `(Phase 43 D-04/D-06/D-10/D-16)` in the line so the provenance is traceable.
    Do NOT modify `src/__tests__/lib/redis-registry.test.ts` — RESEARCH A6 VERIFIED it pins only the key NAME on `:` boundaries, not the value shape, so no change is needed (and the redis-registry drift gate must stay green).
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/otg-iran-monitor && grep -q "soft-404" docs/architecture/redis-keys.md && grep -q "soft-404" CLAUDE.md && grep -q "no-url" CLAUDE.md && npx vitest run src/__tests__/lib/redis-registry.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "events:url-liveness:{eventId}" CLAUDE.md` line contains `soft-404`, `no-url`, and `evidence`
    - `grep "evidence" docs/architecture/redis-keys.md` returns a match on the url-liveness row
    - CLAUDE.md url-liveness line no longer contains the substring `monotonic-with-reset-on-live-or-unknown` (replaced with the D-10 wording)
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` passes (drift gate green, no change to the test)
  </acceptance_criteria>
  <done>Both prose registry surfaces document the 7-status taxonomy, evidence field, nullable lastUrlProbed, the two 24h TTL tiers, and the D-10 attemptCount semantics; redis-registry drift gate green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                                                                                                |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| stored Redis entry → schema parse (writer-only) | Old `events:url-liveness:{eventId}` entries lack `evidence`; only the WRITER `UrlLivenessSchema.parse()` runs at runtime — readers use TS-generic `cacheGetSafe` casts (no runtime parse). |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                       |
| --------- | ----------------- | ------------------------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-43-01   | Tampering         | Schema migration on live `events:url-liveness:{eventId}` (~5000 prod entries lacking `evidence`) | accept      | RESEARCH "writer-only Zod-parse CONFIRMED" — no reader calls `.parse()`; old entries read via generic cast as `evidence: undefined`/null safely; all liveness TTLs ≤7d so population fully turns over within a week (D-17). No migration code needed. |
| T-43-02   | Denial of Service | `.strict()` schema drift fails four test files until all fixtures update                         | mitigate    | Lockstep contract: Task 2 adds `evidence` to every fixture + shim in the SAME commit as the schema change (D-18); typecheck (Task 1) catches the Record/union widenings before tests run.                                                             |
| T-43-SC   | Tampering         | npm/pip/cargo installs                                                                           | n/a         | This plan installs ZERO packages (RESEARCH "No new packages"). No legitimacy gate required.                                                                                                                                                           |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (Record map + ProbeResult union widenings compile).
- `npx vitest run server/__tests__/lib/urlLiveness.schema.test.ts src/__tests__/lib/urlLiveness.schema.test.ts` green.
- `npx vitest run src/__tests__/lib/redis-registry.test.ts` green (drift gate, no change).
- `grep "soft-404"` matches in urlLiveness.ts, both schema tests, redis-keys.md, and CLAUDE.md.
</verification>

<success_criteria>
GHOST-10 (schema half): the `evidence` field is persisted-capable in the `.strict()` schema and pinned by the schema test + shim; the 7-status taxonomy and TTL tiers are documented in the Redis registry in lockstep. The foundation compiles and all downstream plans can reference `soft-404`, `no-url`, and `ProbeResult.evidence`.
</success_criteria>

<output>
Create `.planning/phases/43-ghost-link-prune-correctness/43-01-SUMMARY.md` when done.
</output>

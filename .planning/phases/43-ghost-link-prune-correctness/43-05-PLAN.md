---
phase: 43-ghost-link-prune-correctness
plan: 05
type: execute
wave: 4
depends_on: ['43-01', '43-03', '43-04']
files_modified:
  - server/lib/urlLiveness.ts
  - server/routes/operator-status.ts
  - server/__tests__/lib/urlLiveness.cronPrune.test.ts
autonomous: true
requirements: [GHOST-09, GHOST-10]
must_haves:
  truths:
    - 'On the cron trigger, a 403-status terminal-dead entry is SKIPPED by prune (if the Plan 04 decision was DEMOTE)'
    - 'On the manual trigger, a 403-status entry is STILL pruned (no gate change on the manual path)'
    - 'unknown status is never prune-eligible on EITHER trigger (explicit pin)'
    - 'no-url status is never prune-eligible on EITHER trigger (explicit pin)'
    - 'soft-404 status is cron-prunable under the retained attemptCount >= 3 gate'
    - 'The attemptCount >= 3 cron gate is retained verbatim'
    - 'DeadUrlSampleEntry exposes the evidence field and a soft-404 status, sourced from the stored entry'
  artifacts:
    - path: 'server/lib/urlLiveness.ts'
      provides: 'Cron-only 403 exclusion (per Plan 04 decision) in pruneDeadUrlEvents filter; unchanged isTerminalDead'
      contains: "trigger === 'cron'"
    - path: 'server/routes/operator-status.ts'
      provides: 'DeadUrlSampleEntry widened with soft-404 status + evidence field; evidence sourced at sample.push'
      contains: 'soft-404'
    - path: 'server/__tests__/lib/urlLiveness.cronPrune.test.ts'
      provides: 'Cron-403-skip + manual-403-prune cases, unknown+no-url excluded-both-triggers pins, soft-404 cron-prunable case, evidence on fixtures'
      contains: 'soft-404'
  key_links:
    - from: 'server/lib/urlLiveness.ts pruneDeadUrlEvents filter loop'
      to: "opts.trigger === 'cron' && entry.status === '403' skip"
      via: 'prune-filter-local discriminator (NOT an isTerminalDead change)'
      pattern: "trigger === 'cron'"
    - from: 'server/routes/operator-status.ts buildDeadUrlSample sample.push'
      to: 'DeadUrlSampleEntry.evidence'
      via: 'value.evidence sourced from the stored UrlLiveness entry'
      pattern: 'evidence'
---

<objective>
Implement the GHOST-09 prune-path decision recorded in Plan 04 and the GHOST-10 server-side exposure. Apply the cron-only 403 exclusion as a one-line prune-filter-local check inside `pruneDeadUrlEvents` (`opts.trigger === 'cron' && entry.status === '403'` → skip), gated on the Plan 04 evidence decision (D-14/D-15). Explicitly pin that `unknown` AND `no-url` are never prune-eligible on EITHER trigger (D-08/D-11 — so a future status addition can't silently widen prune). Retain the `attemptCount >= 3` cron gate verbatim (D-12). Widen the operator-status `DeadUrlSampleEntry` type to expose the `evidence` field and the `soft-404` status (D-19) so Phase 44 has data to mount — server-side only, no client UI.

Purpose: 403 stays terminal-dead (dashboard count + deadUrlSample + manual prune all unchanged); ONLY the cron prune filter changes — keeping the exclusion out of `isTerminalDead` (the shared predicate) is what preserves the dashboard count and manual prune (RESEARCH anti-pattern). Pinning `unknown`/`no-url` as never-prunable on both triggers locks the asymmetric error budget against future regressions.

Output: cron-only 403 exclusion in `pruneDeadUrlEvents`; widened `DeadUrlSampleEntry`; extended cronPrune test.

NOTE: This plan implements the decision recorded in `43-VERIFICATION.md` by Plan 04. If that decision was DEMOTE (expected), apply the cron-only 403 exclusion. If it was KEEP, do NOT add the exclusion line and instead record in the SUMMARY that the evidence showed 403s genuinely dead (the cron path keeps pruning 403 under the `>=3` gate). Either way, the `unknown`/`no-url` pins, the `soft-404` cron-prunable case, and the operator-status widening land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md
@.planning/phases/43-ghost-link-prune-correctness/43-RESEARCH.md
@.planning/phases/43-ghost-link-prune-correctness/43-PATTERNS.md
@.planning/phases/43-ghost-link-prune-correctness/43-VERIFICATION.md
@.planning/phases/43-ghost-link-prune-correctness/43-01-SUMMARY.md
@.planning/phases/43-ghost-link-prune-correctness/43-03-SUMMARY.md
@.planning/phases/43-ghost-link-prune-correctness/43-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (Plan 05 — prune path + operator-status exposure):

- **New prune-filter-local check (D-15, if Plan 04 = DEMOTE):** `if (opts.trigger === 'cron' && entry.status === '403') continue;` inside `pruneDeadUrlEvents` — NOT an `isTerminalDead` change.
- **Widened type:** `DeadUrlSampleEntry.status` union gains `'soft-404'`; new field `evidence: string | null`.
- **New evidence source:** `buildDeadUrlSample` reads `value.evidence` off the stored entry into the sample row.
- **New prune-test pins:** cron-skips-403 + manual-prunes-403, `unknown` + `no-url` excluded on both triggers, `soft-404` cron-prunable under `>=3`.

Downstream consumers (Phase 44) read: the `DeadUrlSampleEntry.evidence` string and `soft-404` per-bucket status from `/api/operator-status`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cron-only 403 exclusion + unknown/no-url/soft-404 prune pins (GHOST-09 impl)</name>
  <read_first>
    - .planning/phases/43-ghost-link-prune-correctness/43-VERIFICATION.md (the Plan 04 GHOST-09 / SC-3 Evidence Sample decision — DEMOTE or KEEP)
    - server/lib/urlLiveness.ts (lines 774-877 pruneDeadUrlEvents; :813-817 the filter loop — `if (!isTerminalDead(entry.status)) continue;` then `if (opts.trigger === 'cron' && entry.attemptCount < 3) continue;`; :775 the opts.trigger discriminator)
    - server/__tests__/lib/urlLiveness.cronPrune.test.ts (full — LIVENESS_BY_ID fixtures A-E at :90-135; cron/manual trigger cases at :179-199; the existing E is 403/ac=4)
    - .planning/phases/43-ghost-link-prune-correctness/43-RESEARCH.md (Code Examples cron-only 403 exclusion; anti-pattern: NOT isTerminalDead; Pitfall 5 — fixtures need evidence)
    - .planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md (D-08, D-11, D-12, D-14, D-15)
  </read_first>
  <behavior>
    - Test (cron-403-skip, if DEMOTE): a 403 entry (fixture E, attemptCount=4) is SKIPPED on the cron trigger; the cron prune result does NOT include it
    - Test (manual-403-prune): the same 403 entry IS pruned on the manual trigger (no gate change on manual)
    - Test (unknown excluded both triggers): a `unknown` entry (fixture C, attemptCount=5) is NEVER pruned on cron OR manual (explicit pin — D-11)
    - Test (no-url excluded both triggers): a `no-url` entry is NEVER pruned on cron OR manual (explicit pin — D-08)
    - Test (soft-404 cron-prunable): a `soft-404` entry with attemptCount >= 3 IS pruned on cron (D-05); with attemptCount < 3 is SKIPPED on cron (retained gate)
    - Test (gate retained verbatim): a `404` entry with attemptCount=1 is SKIPPED on cron, pruned on manual (UNCHANGED — D-12)
  </behavior>
  <action>
    First READ the Plan 04 decision in `43-VERIFICATION.md`. IF the decision is DEMOTE (expected, D-14): in `pruneDeadUrlEvents`'s filter loop (~:813-817), insert ONE line BETWEEN `if (!isTerminalDead(entry.status)) continue;` and `if (opts.trigger === 'cron' && entry.attemptCount < 3) continue;` — namely `if (opts.trigger === 'cron' && entry.status === '403') continue;` (D-15 — cron skips 403 regardless of attemptCount; manual still prunes it). Add a comment citing `D-14/D-15 (evidence: 43-VERIFICATION.md GHOST-09 sample — bot-blocking CDN false positive confirmed)`. IF the decision is KEEP: do NOT add this line; add a comment in `pruneDeadUrlEvents` noting `D-14 evidence (43-VERIFICATION.md) showed 403s genuinely dead → 403 stays cron-prunable under the >=3 gate`. DO NOT touch `isTerminalDead` either way (the exclusion is prune-filter-local — RESEARCH anti-pattern; `isTerminalDead` is shared by the sidecar count, dashboard sample, and manual prune).
    In `server/__tests__/lib/urlLiveness.cronPrune.test.ts`: add `evidence` to every `LIVENESS_BY_ID` fixture (A-E) so the strict schema round-trips (Pitfall 5). Add a cron-403-skip case (fixture E) and confirm the manual trigger STILL prunes E (the existing manual case at ~:189 already prunes A,B,E — keep it, but if DEMOTE, the cron case at ~:179 must now exclude E so cron prunes only B). Add an explicit `unknown`-excluded-on-BOTH-triggers pin (extend the existing :199 case to cover manual too) AND a new `no-url`-excluded-on-both-triggers pin (D-08/D-11). Add a `soft-404` fixture and assert it is cron-prunable at attemptCount>=3 and skipped at <3. Keep the `attemptCount >= 3` cron-gate cases verbatim (D-12). Gate the cron-403-skip assertion behind the recorded decision: if KEEP, assert cron PRUNES E (403/ac=4) instead.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/otg-iran-monitor && npx vitest run server/__tests__/lib/urlLiveness.cronPrune.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/__tests__/lib/urlLiveness.cronPrune.test.ts` passes
    - cronPrune test has a `no-url`-never-pruned-on-both-triggers case AND an `unknown`-never-pruned-on-both-triggers case
    - cronPrune test has a `soft-404` cron-prunable-at-ac>=3 case
    - IF Plan 04 decision = DEMOTE: `grep "trigger === 'cron' && entry.status === '403'" server/lib/urlLiveness.ts` returns a match AND the test asserts cron SKIPS the 403 fixture; IF KEEP: no such line and the test asserts cron PRUNES it
    - `isTerminalDead` source is UNCHANGED from Plan 01 (still returns true for 403; the cron exclusion is NOT here)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The cron-only 403 exclusion is applied per the Plan 04 decision (or explicitly not, with rationale); unknown/no-url are pinned never-prunable on both triggers; soft-404 is cron-prunable under the retained >=3 gate; cronPrune test green.</done>
</task>

<task type="auto">
  <name>Task 2: Widen DeadUrlSampleEntry with evidence + soft-404 (GHOST-10 server exposure, D-19)</name>
  <read_first>
    - server/routes/operator-status.ts (lines 185-189 DeadUrlSampleEntry type; 215-259 buildDeadUrlSample; :234 cacheGetSafe<UrlLiveness> generic cast; :241-247 sample.push; :246 status cast)
    - server/lib/urlLiveness.ts (the UrlLiveness schema with evidence + soft-404 from Plan 01)
    - .planning/phases/43-ghost-link-prune-correctness/43-PATTERNS.md (operator-status.ts type-widening D-19 section)
    - .planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md (D-19)
    - .planning/phases/43-ghost-link-prune-correctness/43-RESEARCH.md (Open Q 2 — no operator-status contract test exists; typecheck covers the union change)
  </read_first>
  <action>
    In `server/routes/operator-status.ts`: add `'soft-404'` to the `DeadUrlSampleEntry.status` union (currently `'dead-host' | '403' | '404'`) — do NOT add `'no-url'` (the sample is terminal-dead only, and no-url is not terminal-dead). Add a new field `evidence: string | null` to the `DeadUrlSampleEntry` type. In `buildDeadUrlSample`'s `sample.push` (~:241-247), source `evidence` from `value.evidence` off the stored entry (the `cacheGetSafe<UrlLiveness>` at :234 is a TS-generic cast — old entries lacking `evidence` read as `undefined`; coerce to `null` via `value.evidence ?? null`). Widen the status cast at ~:246 to include `'soft-404'`. This is read-side only — no runtime Zod parse, no client UI work this phase (Phase 44 renders the evidence string verbatim as TEXT).
    There is NO operator-status route contract test pinning `DeadUrlSampleEntry` (RESEARCH Open Q 2 VERIFIED — none exists in server/__tests__/routes/), so typecheck alone covers the union/field change. Do NOT invent a new contract test.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/otg-iran-monitor && grep -q "soft-404" server/routes/operator-status.ts && grep -q "evidence" server/routes/operator-status.ts && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `DeadUrlSampleEntry` type in operator-status.ts contains `soft-404` in the status union and an `evidence: string | null` field
    - `DeadUrlSampleEntry.status` union does NOT contain `'no-url'`
    - `buildDeadUrlSample` sources `evidence` from `value.evidence` (with `?? null` coercion for old entries)
    - `npx tsc --noEmit` exits 0
    - no new operator-status contract test was added (typecheck covers it per Open Q 2)
  </acceptance_criteria>
  <done>DeadUrlSampleEntry exposes the evidence field and the soft-404 status sourced from the stored entry; typecheck passes; Phase 44 has the data to mount.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                                    |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| cron vs manual prune trigger        | `opts.trigger` discriminates the destructive prune path; the cron-only 403 exclusion must live ONLY in the prune filter, never in the shared `isTerminalDead`. |
| stored entry → operator-status read | `buildDeadUrlSample` reads stored entries via a TS-generic cast (no runtime Zod parse); old entries lacking `evidence` must coerce to null.                    |

## STRIDE Threat Register

| Threat ID | Category                                 | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                         |
| --------- | ---------------------------------------- | --------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-43-14   | Tampering (destructive prune over-reach) | Cron prune of bot-blocked-but-live 403 articles           | mitigate    | D-14/D-15: cron-only 403 exclusion (if evidence confirms false positives) removes unattended pruning of live-but-403'd articles; the exclusion is prune-filter-local so manual prune (operator judgment) still works. Decision is evidence-gated by Plan 04.            |
| T-43-15   | Tampering (silent prune widening)        | A future status addition silently becoming prune-eligible | mitigate    | D-11: explicit cronPrune test pins `unknown` AND `no-url` as never-prunable on BOTH triggers; the `isTerminalDead` gate (Plan 01) already excludes them, but the pin prevents a future regression.                                                                      |
| T-43-16   | Information Disclosure                   | `evidence` string surfaced via `/api/operator-status`     | accept      | The aggregator is already Bearer-gated (Phase 32 V4 access control, unchanged); evidence is a ≤200-char operator-facing diagnostic string (matched marker / redirect target / byte count), no secrets. Phase 44 must render it as TEXT not HTML (note carried forward). |
| T-43-17   | Denial of Service (degrade-open)         | `buildDeadUrlSample` reading a malformed/old entry        | mitigate    | `value.evidence ?? null` tolerates old entries; `buildDeadUrlSample` SCAN failure already degrades open (returns empty sample, unchanged).                                                                                                                              |
| T-43-SC   | Tampering                                | npm/pip/cargo installs                                    | n/a         | Plan installs ZERO packages. No legitimacy gate required.                                                                                                                                                                                                               |

</threat_model>

<verification>
- `npx vitest run server/__tests__/lib/urlLiveness.cronPrune.test.ts` green (403 trigger behavior + unknown/no-url pins + soft-404 cron-prunable).
- `npx tsc --noEmit` exits 0 (operator-status union/field widening).
- `isTerminalDead` unchanged (the cron 403 exclusion is prune-filter-local, not in the shared predicate).
- `DeadUrlSampleEntry` exposes `soft-404` + `evidence`.
</verification>

<success_criteria>
GHOST-09 (implementation half): the 403 decision recorded in Plan 04 is implemented — 403 stays distinct from 404, demoted to manual-only cron prune if bot-blocking CDNs were confirmed false-positives (or kept cron-prunable with recorded rationale), with the exclusion prune-filter-local (not in isTerminalDead). `unknown` and `no-url` are pinned never-prunable on both triggers; the `>=3` gate is retained. GHOST-10 (server exposure): `DeadUrlSampleEntry` exposes the evidence string and soft-404 status for Phase 44 to mount.
</success_criteria>

<output>
Create `.planning/phases/43-ghost-link-prune-correctness/43-05-SUMMARY.md` when done.
</output>

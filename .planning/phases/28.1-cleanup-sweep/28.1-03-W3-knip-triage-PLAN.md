---
phase: 28.1-cleanup-sweep
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - knip.json
  - .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md
  - .planning/phases/28.1-cleanup-sweep/W3-knip-output.txt
  - .planning/phases/28.1-cleanup-sweep/W3-tsprune-output.txt
autonomous: false
requirements: [D-14, D-29]
must_haves:
  truths:
    - 'knip + ts-prune have been run against the live source tree (NOT against worktree snapshots).'
    - 'Every flagged item from knip + ts-prune output appears in 28.1-W3-TRIAGE.md with EXACTLY ONE confidence tag (high-dead | probable-dead | ambiguous).'
    - 'Every file listed in the W5/W6 hardcode-target manifest is excluded from W4 deletion candidates regardless of triage tag.'
    - 'No source files modified — the wave commits ONLY the triage doc + knip config + raw output captures.'
    - 'Vitest, lint, prettier, tsc still green at wave close (no source modifications occurred).'
  artifacts:
    - path: 'knip.json'
      provides: 'Knip configuration with entry points + ignore globs preventing worktree pollution'
      contains: '$schema'
    - path: '.planning/phases/28.1-cleanup-sweep/W3-knip-output.txt'
      provides: 'Raw knip stdout capture for forensic reference'
      min_lines: 1
    - path: '.planning/phases/28.1-cleanup-sweep/W3-tsprune-output.txt'
      provides: 'Raw ts-prune stdout capture for cross-validation'
      min_lines: 1
    - path: '.planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md'
      provides: 'Per-item confidence-tagged triage table that W4 reads to plan deletion commits'
      contains: "high-dead\\|probable-dead\\|ambiguous"
  key_links:
    - from: '28.1-W3-TRIAGE.md'
      to: 'W3-knip-output.txt'
      via: 'row-per-flag mapping'
      pattern: 'knip:'
    - from: '28.1-W3-TRIAGE.md'
      to: 'W3-tsprune-output.txt'
      via: 'row-per-flag mapping'
      pattern: 'ts-prune:'
    - from: '28.1-W3-TRIAGE.md §Hardcode-target manifest'
      to: 'W4 / W5 / W6 plans'
      via: 'exclusion list — never delete a file that W5 or W6 will touch'
      pattern: "EXCLUDED — W5\\|EXCLUDED — W6"
---

<objective>
Run `npx knip` + `npx ts-prune` against the live source tree, classify every flag with one of three confidence tags (`high-dead` | `probable-dead` | `ambiguous`) per D-29, and commit the triage doc + raw output captures + knip config.

THIS WAVE PRODUCES ZERO SOURCE-CODE CHANGES. The deliverable is the triage doc that W4 will read to plan atomic per-module deletion commits.

Per D-14 step 3 + D-29: triage doc is committed BEFORE any deletions. Conservative-bias: keep unless proven dead. Hardcode-target files (W5/W6 scope) are explicitly excluded from the deletion candidate set even if knip flags them.

Purpose: surface "I forgot this exists" cases before they cause regressions; preserve forensic raw output for re-triage if W4 questions arise.
Output: `28.1-W3-TRIAGE.md` + `W3-knip-output.txt` + `W3-tsprune-output.txt` + `knip.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28.1-cleanup-sweep/28.1-CONTEXT.md
@.planning/phases/28.1-cleanup-sweep/28.1-RESEARCH.md
@.planning/phases/28.1-cleanup-sweep/28.1-PATTERNS.md
@.planning/phases/28.1-cleanup-sweep/28.1-VALIDATION.md
@.planning/phases/28.1-cleanup-sweep/28.1-02-SUMMARY.md
@CLAUDE.md
@package.json
@vite.config.ts
@eslint.config.js

<interfaces>
<!-- W5/W6 hardcode-target manifest. Files listed here are EXCLUDED from W4 deletion candidates -->
<!-- regardless of knip/ts-prune flags. W3 must annotate any flagged file in this list as -->
<!-- "EXCLUDED — W5" or "EXCLUDED — W6" in the triage doc. -->

W5 D-11 hardcode targets (centralization to src/lib/domain.ts):

- server/config.ts:233-247 (IRAN_BBOX, IRAN_CENTER, WAR_START, ADSB_RADIUS_NM block)

W5 D-12 hardcode targets (env-var introduction):

- src/hooks/useFlightPolling.ts
- src/hooks/useShipPolling.ts
- src/hooks/useEventPolling.ts
- src/hooks/useNewsPolling.ts
- src/hooks/useMarketPolling.ts
- src/hooks/useWaterPrecipPolling.ts
- src/hooks/useLLMStatusPolling.ts
- src/lib/attackStatus.ts
- src/hooks/useProximityAlerts.ts
- src/lib/severity.ts

W6 D-13 hardcode targets (CSS @theme migration):

- src/styles/app.css
- src/components/map/layers/constants.ts (ENTITY_COLORS / ENTITY_DOT_COLORS / SITE_SUBTYPE_COLORS / WATER_TYPE_COLORS — only the color blocks; sizes stay)
- src/lib/factions.ts
- src/lib/ethnicGroups.ts
- (NEW from W6) src/lib/colorBridge.ts

W2 freshly-introduced — DO NOT classify as dead:

- server/lib/healthSchema.ts
- server/lib/healthSources.ts
- src/lib/healthClient.ts
- src/hooks/useHealthStatus.ts
- src/components/ui/HealthBanner.tsx

Known preserved-but-inactive (RESEARCH §State of the Art):

- server/adapters/acled.ts — ACLED adapter preserved per CLAUDE.md but not active. Knip WILL flag. Annotate as "ambiguous / probable-dead-but-preserved" — do NOT delete in W4.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create knip.json + capture raw knip + ts-prune output</name>
  <files>knip.json, .planning/phases/28.1-cleanup-sweep/W3-knip-output.txt, .planning/phases/28.1-cleanup-sweep/W3-tsprune-output.txt</files>
  <read_first>
    - 28.1-RESEARCH.md §"Focus Area 1: Knip + ts-prune behavior on this stack" lines 493-540 (recommended knip.json skeleton at lines 499-521, common false-positive patterns)
    - 28.1-CONTEXT.md §D-14 (mechanical pass methodology)
    - vite.config.ts (current `test.exclude` setting — knip.json `ignore` should mirror the worktree-isolation pattern from Phase 27.4.2 Plans 01/03/04)
    - eslint.config.js (current `ignores` list — same worktree carve-out pattern)
    - package.json (entry points: `main`, `bin`, `exports` if any — knip respects these)
  </read_first>
  <action>
    Step 1: Create `knip.json` at repo root with this EXACT content (verbatim from RESEARCH §Focus Area 1):

    ```json
    {
      "$schema": "https://unpkg.com/knip@latest/schema.json",
      "entry": [
        "src/main.tsx",
        "server/vercel.ts",
        "server/index.ts",
        "scripts/*.{ts,js}",
        "vite.config.ts",
        "eslint.config.js"
      ],
      "project": ["src/**/*.{ts,tsx}", "server/**/*.ts"],
      "ignore": [".claude/**", "dist/**", "api/**", "load-test-results/**", "src/test/__mocks__/**"],
      "ignoreDependencies": [
        "@deck.gl/core",
        "@deck.gl/extensions",
        "@deck.gl/layers",
        "@deck.gl/mapbox",
        "@vis.gl/react-maplibre",
        "maplibre-gl"
      ]
    }
    ```

    The `.claude/**` ignore is the third instance of the worktree-isolation pattern (Phase 27.4.2 Plans 01 vitest exclude, Plan 03 .prettierignore, Plan 04 eslint ignores). DO NOT modify this list without surfacing the change in the triage doc — it's load-bearing.

    Step 2: Run knip and capture output:
    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx --yes knip --reporter symbols > .planning/phases/28.1-cleanup-sweep/W3-knip-output.txt 2>&1 || true
    ```
    The `|| true` suffix prevents the wave from failing if knip exits non-zero (it does when flags are found — that's expected).

    Step 3: Run ts-prune and capture output:
    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx --yes ts-prune -i "(used in module)" > .planning/phases/28.1-cleanup-sweep/W3-tsprune-output.txt 2>&1 || true
    ```
    The `-i "(used in module)"` filter eliminates the barrel-re-export false positives flagged in RESEARCH §Pitfall 4.

    Step 4: Verify both output files exist and are non-empty (knip output may include "0 issues found" — that's still a non-empty file).

    Commit (single atomic): `chore(28.1): add knip.json + capture W3 raw knip + ts-prune output (D-14 step 1)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && test -f knip.json && test -s .planning/phases/28.1-cleanup-sweep/W3-knip-output.txt && test -s .planning/phases/28.1-cleanup-sweep/W3-tsprune-output.txt</automated>
  </verify>
  <done>
    `knip.json` exists with `$schema` field. Both output capture files are non-empty (`test -s`). Commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-curate 28.1-W3-TRIAGE.md with confidence-tagged classification of every flag</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md</files>
  <read_first>
    - .planning/phases/28.1-cleanup-sweep/W3-knip-output.txt (from Task 1)
    - .planning/phases/28.1-cleanup-sweep/W3-tsprune-output.txt (from Task 1)
    - 28.1-CONTEXT.md §D-29 (conservative-bias; confidence-tag definitions)
    - 28.1-RESEARCH.md §Pitfall 1 (Zustand selectors), §Pitfall 2 (Vite glob imports), §Pitfall 3 (deck.gl peer-dep externals), §Pitfall 4 (ts-prune barrel re-exports)
    - 28.1-RESEARCH.md §"State of the Art" §Deprecated/outdated — server/adapters/acled.ts annotation
    - The `<interfaces>` block in this plan (W5/W6 hardcode-target manifest + W2 freshly-introduced exclusion list)
    - CLAUDE.md (full feature inventory — used to validate "ambiguous" classification on any flag whose role is unclear)
  </read_first>
  <action>
    Create `.planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md` with the following EXACT structure:

    ```markdown
    ---
    phase: 28.1
    wave: W3
    decision: D-14 / D-29
    triage_date: <fill at task time>
    knip_total_flags: <count from W3-knip-output.txt>
    tsprune_total_flags: <count from W3-tsprune-output.txt — POST-filter via -i "(used in module)">
    triage_completed_by: claude-code
    ---

    # Phase 28.1 W3 — Knip + ts-prune Triage

    > One-shot triage per D-14 step 3 + D-29 conservative bias. Default = keep unless proven dead.
    > W4 reads this doc to plan atomic per-module deletion commits.
    > NO deletions in W3 — ONLY this doc + raw output captures.

    ## Confidence Tag Definitions (D-29)

    - **high-dead** — mechanically AND semantically dead with no plausible runtime path. Bulk-approval candidate.
    - **probable-dead** — knip flags it, no obvious user, but warrants individual W4 review.
    - **ambiguous** — Zustand selector of removed field, dynamic import, barrel re-export, conditional code path, or any case where confidence is uncertain. Individual W4 review required; default = keep.

    ## Hardcode-Target Manifest (NEVER delete in W4)

    These files are W5/W6 in-flight scope. EVEN IF knip flags them as dead, mark "EXCLUDED — W5" or "EXCLUDED — W6":

    | File | Excluded Reason |
    | ---- | --------------- |
    | server/config.ts (IRAN_BBOX block at 233-247) | EXCLUDED — W5 D-11 (centralizing to src/lib/domain.ts) |
    | src/hooks/useFlightPolling.ts | EXCLUDED — W5 D-12 (POLL_FLIGHTS_MS env var) |
    | src/hooks/useShipPolling.ts | EXCLUDED — W5 D-12 |
    | src/hooks/useEventPolling.ts | EXCLUDED — W5 D-12 |
    | src/hooks/useNewsPolling.ts | EXCLUDED — W5 D-12 |
    | src/hooks/useMarketPolling.ts | EXCLUDED — W5 D-12 |
    | src/hooks/useWaterPrecipPolling.ts | EXCLUDED — W5 D-12 |
    | src/hooks/useLLMStatusPolling.ts | EXCLUDED — W5 D-12 |
    | src/lib/attackStatus.ts | EXCLUDED — W5 D-12 (ATTACK_RADIUS_KM env var) |
    | src/hooks/useProximityAlerts.ts | EXCLUDED — W5 D-12 (PROXIMITY_ALERT_KM env var) |
    | src/lib/severity.ts | EXCLUDED — W5 D-12 (SEVERITY_HALF_LIFE_HOURS env var) |
    | src/styles/app.css | EXCLUDED — W6 D-13 (extending @theme block) |
    | src/components/map/layers/constants.ts | EXCLUDED — W6 D-13 (color block migrates) |
    | src/lib/factions.ts | EXCLUDED — W6 D-13 |
    | src/lib/ethnicGroups.ts | EXCLUDED — W6 D-13 |
    | server/lib/healthSchema.ts | EXCLUDED — W2 freshly introduced |
    | server/lib/healthSources.ts | EXCLUDED — W2 freshly introduced |
    | src/lib/healthClient.ts | EXCLUDED — W2 freshly introduced |
    | src/hooks/useHealthStatus.ts | EXCLUDED — W2 freshly introduced |
    | src/components/ui/HealthBanner.tsx | EXCLUDED — W2 freshly introduced |

    ## Knip Flags (raw source: W3-knip-output.txt)

    | Flag # | Type | Path | Symbol / Export | Confidence | Reason | W4 Action |
    | ------ | ---- | ---- | --------------- | ---------- | ------ | --------- |
    | 1 | unused export | src/path/to/file.ts | exportName | high-dead \| probable-dead \| ambiguous | <one-line rationale referencing CLAUDE.md / RESEARCH pitfall / hardcode-target> | DELETE \| KEEP \| INVESTIGATE \| EXCLUDED |
    | ... (one row per knip flag) ... | | | | | | |

    ## ts-prune Flags (raw source: W3-tsprune-output.txt)

    Filtered already via `-i "(used in module)"` per RESEARCH Pitfall 4.

    | Flag # | Path | Symbol | Confidence | Reason | Cross-ref to knip flag # | W4 Action |
    | ------ | ---- | ------ | ---------- | ------ | ------------------------ | --------- |
    | 1 | src/path/to/file.ts | exportName | high-dead \| probable-dead \| ambiguous | <rationale> | knip# (if same flag) | DELETE \| KEEP \| INVESTIGATE \| EXCLUDED |
    | ... | | | | | | |

    ## Pre-emptive ambiguous classifications (RESEARCH pitfall hits)

    These knip categories MUST be classified `ambiguous` per RESEARCH:
    - Files matching `src/stores/*.ts` selector exports → Pitfall 1
    - Files matched by `import.meta.glob()` patterns (audit src/ for `import.meta.glob` usage during this task) → Pitfall 2
    - deck.gl peer-deps in `vite.config.ts manualChunks` array → Pitfall 3
    - Modules under `src/lib/` with `index.ts` siblings → Pitfall 4

    ## Pre-emptive preserved-but-inactive

    - `server/adapters/acled.ts` — preserved per CLAUDE.md "GDELT v2 — default conflict event source ... ACLED adapter preserved in `server/adapters/acled.ts` but not active". Classify `ambiguous`. **W4 ACTION: KEEP**.

    ## Approval Buckets for W4

    Group flags by W4 action:

    ### `high-dead` bulk-approve candidates (W4 may delete in atomic per-module commits)

    Count: <N>
    Items:
    - <list>

    ### `probable-dead` individual review (W4 reads each before deciding)

    Count: <N>
    Items:
    - <list>

    ### `ambiguous` keep-by-default (W4 default = no delete; only delete with explicit user approval)

    Count: <N>
    Items:
    - <list>

    ### `EXCLUDED` — W5/W6/W2 in-flight scope (W4 NEVER deletes)

    Count: <N>
    Items:
    - <list>

    ## Sign-off

    - [ ] Every line in W3-knip-output.txt has been classified
    - [ ] Every line in W3-tsprune-output.txt (post-filter) has been classified
    - [ ] Every Hardcode-Target Manifest entry that knip/ts-prune flagged is annotated EXCLUDED
    - [ ] grep "high-dead\\|probable-dead\\|ambiguous\\|EXCLUDED" 28.1-W3-TRIAGE.md returns ≥ knip+tsprune total flag count
    ```

    Then process the raw output files:
    1. Read `W3-knip-output.txt` line-by-line. For each flag (knip uses sections like "Unused exports" / "Unused files" / "Unused dependencies" / etc.), add a row to the Knip Flags table.
    2. Read `W3-tsprune-output.txt` line-by-line. For each flag, add a row to the ts-prune Flags table.
    3. For each row, decide confidence tag:
       - If file matches the Hardcode-Target Manifest → "EXCLUDED — W5" or "EXCLUDED — W6"
       - If file matches RESEARCH Pitfall 1-4 categories → `ambiguous`
       - If file is `server/adapters/acled.ts` → `ambiguous` (preserved-but-inactive)
       - If knip says "Unused dependencies" for a deck.gl peer-dep → `ambiguous` (Pitfall 3)
       - If symbol is a Zustand selector or store action that callers import via `s => s.X` → `ambiguous` (Pitfall 1)
       - Otherwise: cross-check by grepping the symbol name across `src/ server/ scripts/` — if zero hits outside the file itself, classify `high-dead`; if 1-2 hits in obvious-but-unimported test fixtures or commented-out code, `probable-dead`; if any uncertainty, `ambiguous`.
    4. Populate the Approval Buckets section by grouping rows by W4 Action.

    Commit (single atomic): `docs(28.1): W3 knip + ts-prune triage doc with confidence tags (D-14/D-29)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && test -f .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md && grep -c "high-dead\|probable-dead\|ambiguous\|EXCLUDED" .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md</automated>
  </verify>
  <done>
    Triage doc exists. Confidence-tag count ≥ knip+tsprune total flag count. Every Hardcode-Target Manifest entry that appears in knip/tsprune output is annotated EXCLUDED. Approval Buckets section populated. Committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The W3 triage doc is hand-curated by Claude. The user must skim and approve before W4 begins per D-29's "individual review for probable-dead and ambiguous items".
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md`.
    2. Skim the "Hardcode-Target Manifest" section — confirm every W5/W6 file is listed and any knip-flagged ones are annotated EXCLUDED.
    3. Skim the "high-dead bulk-approve candidates" — these will be deleted in W4 with batch approval. Veto any item that should NOT be deleted (Claude reclassifies to ambiguous).
    4. Skim the "probable-dead individual review" — each gets a W4 yes/no decision. Spot-check 3-5 items by grepping their symbol name across `src/ server/` to validate the classification.
    5. Skim the "ambiguous keep-by-default" list — these will NOT be deleted in W4 unless you explicitly approve. Confirm none of them are surprising "obvious dead" entries you'd expect to delete.
    6. If the doc looks correct: respond "approved" → W4 may begin.
    7. If reclassification needed: comment with the row + new classification; Claude re-runs Task 2 update to amend the doc.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or list rows that need reclassification.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: W3 wave-gate verification + commit closing summary</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W3-SUMMARY.md (new)</files>
  <read_first>
    - .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md (the artifact this summary points at)
    - .planning/phases/28.1-cleanup-sweep/28.1-02-SUMMARY.md (W2 close — establishes wave-summary format)
  </read_first>
  <action>
    Run all four wave-gate commands; ALL must exit 0:

    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx vitest run
    npm run lint
    npx prettier --check .
    npx tsc --noEmit
    ```

    These should all be GREEN trivially because W3 made zero source modifications. If any fails, an unrelated drift has appeared between W2 close and now — investigate before proceeding.

    Verify the W3 source-modification invariant:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && git diff main..HEAD --stat | grep -vE "(28\.1-W3|knip\.json|W3-knip-output|W3-tsprune-output)" | grep -E "^\s*(src|server|scripts)/" | wc -l
    ```
    The output MUST be 0 — meaning no src/, server/, or scripts/ files were modified by W3 (only the planning artifacts + knip.json).

    Then create `.planning/phases/28.1-cleanup-sweep/28.1-W3-SUMMARY.md` with:
    - Wave goal (D-14 step 1+2+3 mechanical pass + triage doc)
    - Pointers to: knip.json, W3-knip-output.txt, W3-tsprune-output.txt, 28.1-W3-TRIAGE.md
    - Counts: total knip flags, total tsprune flags (post-filter), confidence-tag breakdown (high-dead / probable-dead / ambiguous / EXCLUDED)
    - Approval bucket sizes for W4
    - Source-modification invariant: 0 src/server/scripts files modified
    - Wave-gate output (4 commands)
    - W4 readiness signal: "W3 closes green; W4 (per-module deletions) may begin from approved buckets"

    Commit: `docs(28.1): close W3 with knip triage summary (D-14/D-29 — gate green)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit && (git diff main..HEAD --stat | grep -vE "(28\.1-W3|knip\.json|W3-knip-output|W3-tsprune-output|28\.1-03-SUMMARY)" | grep -E "^\s*(src|server|scripts)/" | wc -l | tr -d ' ' | grep -q "^0$")</automated>
  </verify>
  <done>
    All four gates green. Source-modification invariant holds (zero src/server/scripts files touched). Summary committed; branch HEAD is the summary commit; W4 unblocked.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                          |
| --------------------------------- | ---------------------------------------------------------------------------------------------------- |
| knip subprocess → repo source     | knip reads source files; produces a stdout report. No external network calls. No file modifications. |
| ts-prune subprocess → repo source | Same as knip.                                                                                        |

## STRIDE Threat Register

| Threat ID    | Category                                | Component           | Disposition | Mitigation Plan                                                                                                                                                                                                                                         |
| ------------ | --------------------------------------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28.1-W3-01 | T (Tampering — accidental triage error) | 28.1-W3-TRIAGE.md   | mitigate    | Conservative-bias default ("keep unless proven dead") + Hardcode-Target Manifest exclusion + per-row confidence tag with rationale field. W4 enforces approval buckets so a misclassified `high-dead` requires checkpoint:human-verify before deletion. |
| T-28.1-W3-02 | I (Information disclosure)              | knip output capture | accept      | knip output contains source file paths and exported symbol names. Already public information (committed source code). No secrets in output. Verified by spot-checking the captured output before commit.                                                |

</threat_model>

<verification>
- `knip.json` exists with the verbatim RESEARCH skeleton
- `W3-knip-output.txt` and `W3-tsprune-output.txt` are non-empty
- `28.1-W3-TRIAGE.md` confidence-tag count ≥ knip+tsprune flag count
- All Hardcode-Target Manifest entries that knip flagged are annotated EXCLUDED
- Zero src/server/scripts source modifications by W3
- All 4 wave-gate commands exit 0
- W3 SUMMARY committed
</verification>

<success_criteria>

- W3 triage doc + raw output + knip config committed on branch
- W4 has the inputs it needs: approval buckets, exclusion list, classification rationale
- `npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit` all exit 0
- Source-modification invariant: zero non-planning files outside knip.json
  </success_criteria>

<output>
After completion, create `.planning/phases/28.1-cleanup-sweep/28.1-03-SUMMARY.md`.
</output>

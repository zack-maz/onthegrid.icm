# Phase 28.1: Cleanup Sweep — Context

**Gathered:** 2026-04-30
**Status:** Ready for planning
**Parent (umbrella):** [`28-CONTEXT.md`](../28-performance-load-testing-was-phase-27/28-CONTEXT.md)

> **Inheritance note:** Phase 28 was structured as an umbrella that splits into three sequenced child phases (D-01: 28.1 cleanup → 28.2 sync → 28.3 load test). Cross-cutting decisions (D-01 through D-21) live in the umbrella CONTEXT.md, locked once. This file restates the umbrella decisions that apply to **this child only**, plus child-specific clarifications. Downstream agents (researcher, planner, executor) MUST read `28-CONTEXT.md` first — it is the canonical source of decisions.

<domain>
## Phase Boundary

Phase 28.1 is the **cleanup** child of the v1.4-closeout umbrella. It runs **first** in the 28.x sequence (D-02) because cleanup is the most regression-prone surgical work, and the dev/prod sync (28.2) and load test (28.3) need a clean codebase to land against.

Scope is exactly four bands of work:

1. **Ghost code + duplicate code sweep** — mechanical via `npx knip` + `npx ts-prune`, then a manual codebase walk for logically-dead-but-type-reachable code. Triage doc committed before deletions, then atomic per-module deletion commits.
2. **Normalization** — TS strict-mode tightening, Zustand selector-pattern audit, Redis cache-key naming consistency, plus any other categories Claude proposes during 28.1 RESEARCH and the user approves.
3. **UI bugs + remaining debugging items** — enumerated during 28.1 PLAN drafting; the user surfaces them when the plan author asks (per Claude's-discretion in umbrella CONTEXT).
4. **Hardcode generalization** — operator-tunable env vars (D-12), centralized domain constants (D-11), CSS @theme color migration (D-13), all per the methodology in D-14.

**Out of scope for 28.1:**

- Dev/prod feature promotion (D-05/D-06/D-07/D-08/D-09) → that's 28.2.
- Domain rename (D-03) → that's 28.2.
- Rate-limiter Bearer-bypass (D-04) → that's 28.2.
- Edge cache + Redis architecture (D-19) → that's 28.3.
- k6 sweep, PASS/FAIL bar, polling parity (D-15/D-16/D-17/D-18/D-20/D-21) → that's 28.3.

**Branch discipline (from `feedback_branch_per_phase.md` + D-01):** This phase gets its own feature branch. Cleanup must merge to `main` before 28.2 starts. No parallel branches.

</domain>

<decisions>
## Implementation Decisions (filtered from umbrella for 28.1 scope)

### Phase Shape & Sequencing

- **D-01:** Phase 28 split into three child phases (28.1 → 28.2 → 28.3). Each merges to `main` before next starts. **For 28.1:** must close cleanly before 28.2 begins.
- **D-02:** Sequence is **cleanup → sync → load**. **For 28.1:** the value of cleanup running first is "kill the most regression risk early" — accept the trade-off that cleanup may uncover deep bugs and slow the umbrella critical path.

### Hardcode Tolerance & Generalization (the four-band cleanup scope)

- **D-10:** **Aggressive operator-tunable rule for operational levers** — anything an operator might want to nudge during incident response or tuning iteration becomes an env-var override. Pattern matches Phase 27.4.x precedent (`LLM_BATCH_TIMEOUT_MS`, `LLM_V3_CONCURRENCY`, `V3_WATCHDOG_ROLLBACK_THRESHOLD`).
- **D-11:** **Domain-definitional constants stay hardcoded, centralized in `src/lib/domain.ts`** (mirror at `server/lib/domain.ts` if needed) — IRAN_BBOX, IRAN_CENTER, WAR_START, ADS-B 500NM radius. NOT env-tunable. Single source of truth, no derivation. These define what "this dashboard monitors."
- **D-12:** **Polling intervals + thresholds are env-tunable.** Each gets a documented env var with current value as default:
  - `POLL_FLIGHTS_MS` (5s), `POLL_SHIPS_MS` (30s), `POLL_EVENTS_MS` (15min), `POLL_NEWS_MS` (15min), `POLL_MARKETS_MS` (60s), `POLL_WATER_PRECIP_MS` (6h), `POLL_LLM_STATUS_MS` (TBD).
  - `STALE_FLIGHT_MS` (60s — flight stale threshold).
  - `ATTACK_RADIUS_KM` (5km — site/event cross-reference).
  - `PROXIMITY_ALERT_KM` (50km — flight/ship proximity-to-site alert).
  - `SEVERITY_HALF_LIFE_HOURS` (6h — recency decay in `src/lib/severity.ts`).
  - Naming convention: SCREAMING*SNAKE_CASE, follows existing `LLM*_`/`NIM\__` precedent.
- **D-13:** **Visual constants migrate to CSS custom properties + Tailwind v4 `@theme` block** in `src/app.css`. Entity colors become CSS vars:
  - `--color-flight` (#eab308), `--color-flight-unidentified` (#ef4444), `--color-ship` (#a78bfa).
  - `--color-event-airstrike` (#ff3b30), `--color-event-on-ground` (#c0392b), `--color-event-explosion` (#e74c3c), `--color-event-targeted` (#dc143c), `--color-event-other` (#800000).
  - `--color-site-healthy` (#22c55e), `--color-site-attacked` (#f97316).
  - Faction palette (`src/lib/factions.ts`): `--color-faction-us-aligned` (#3b82f6), `--color-faction-iran-aligned` (#dc2626), `--color-faction-neutral` (#64748b), `--color-faction-disputed` (#f59e0b).
  - Existing `@theme` z-index scale is the precedent pattern.
  - **Stays as TS constants:** icon sizes (4000m flights, 3000m events, 2000m sites) — these are deck.gl props, not styling. Tailwind v4 conventions apply for var naming.
- **D-14:** **Ghost code detection methodology** — both passes required, in this order:
  1. **Mechanical:** `npx knip` + `npx ts-prune` for dead-export enumeration.
  2. **Manual:** codebase walk for logically-dead-but-type-reachable code (Zustand selectors-of-removed-fields, dead union variants, orphaned hooks called from removed callers).
  3. **Triage doc** committed before any deletions — categorized list with confidence levels.
  4. **Atomic per-module deletion commits** — never bundle.

### Claude's Discretion (for 28.1)

- Specific UI bugs and remaining debugging items get enumerated during 28.1 PLAN drafting (not pre-locked here — the user will surface them when the plan author asks).
- Specific normalization scope (TS strict mode tightening, Zustand selector pattern audit, Redis cache key naming consistency, etc.) — Claude proposes a categorized list during 28.1 RESEARCH, user approves.
- Knip + ts-prune triage doc format and atomic-commit cadence.
- Exact env-var naming for D-12 (will follow `SCREAMING_SNAKE_CASE` and existing `LLM_*` / `NIM_*` precedent).
- Exact CSS custom property naming for D-13 (will follow Tailwind v4 conventions).

</decisions>

<canonical_refs>

## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Umbrella context (read FIRST)

- `.planning/phases/28-performance-load-testing-was-phase-27/28-CONTEXT.md` — full umbrella decisions D-01 through D-21, deferred ideas, code context, specifics. The decisions section in this file is a filtered restatement; the umbrella is canonical.

### Phase shape

- `.planning/ROADMAP.md` §"Phase 28: Performance & Load Testing — was Phase 27 (UMBRELLA)" + §"Phase 28.1: Cleanup Sweep (umbrella child of 28)" — goal, deliverables allocation, depends-on chain.
- `.planning/STATE.md` — current milestone (v1.4), prior phase 27.4.6 closeout context.
- `CLAUDE.md` — comprehensive feature inventory; serves as "must-still-work" checklist for 28.1 cleanup. Any deletion that touches a CLAUDE.md feature gets a manual UAT trace before the commit.

### Hardcode targets (D-10/D-11/D-12)

- `src/components/map/constants.ts` — IRAN_BBOX, IRAN_CENTER, terrain config (D-11 target — centralize into `src/lib/domain.ts`).
- `src/hooks/useFlightPolling.ts`, `useShipPolling.ts`, `useEventPolling.ts`, `useNewsPolling.ts`, `useMarketPolling.ts`, `useWaterPrecipPolling.ts`, `useLLMStatusPolling.ts` — polling-interval constants (D-12 targets).
- `src/lib/attackStatus.ts` — 5km attack radius (D-12 target: `ATTACK_RADIUS_KM`).
- `src/hooks/useProximityAlerts.ts` — 50km proximity alert radius (D-12 target: `PROXIMITY_ALERT_KM`).
- `src/lib/severity.ts` — type weights, recency decay constants (D-12 candidate: `SEVERITY_HALF_LIFE_HOURS`).
- `server/lib/domain.ts` (does not yet exist) — D-11 mirror destination if any server-side reference is needed.

### CSS migration targets (D-13)

- `src/lib/eventColors.ts` — `EVENT_TYPE_COLORS` palette (D-13 target).
- `src/lib/factions.ts` — political faction color palette (D-13 target).
- `src/lib/ethnicGroups.ts` — ethnic zone color palette (D-13 target).
- `src/app.css` §`@theme` — existing z-index scale; D-13 extension target.

### Existing-pattern references

- `tests/` and `__tests__/` directories — test suites must stay green at every wave boundary (D-02 strict CI gate pattern from Phase 27.4.2).
- `src/components/map/EntityTooltip.tsx:124` — current dev-info block (NOT a 28.1 target — graduates in 28.2 per umbrella D-06).

### Memories that constrain this phase

- `feedback_phase_boundaries.md` — must merge each child phase to main before starting next (D-01 enforces this).
- `feedback_branch_per_phase.md` — each child phase gets its own feature branch including decimal phases.
- `feedback_handoff_on_usage_limit.md` — save HANDOFF.md proactively if usage limits threaten mid-cleanup-sweep.
- `feedback_git_workflow.md` — strict Git workflow: always branch, never commit to main, conventional commits, regular pushes.
- `feedback_project_docs.md` — README, PROJECT_SPEC, PROJECT_STATUS, CHANGELOG, CLAUDE.md kept current. (28.1 close: update CLAUDE.md to reflect new env vars and CSS var palette.)

</canonical_refs>

<code_context>

## Existing Code Insights (28.1-relevant subset)

### Reusable Assets

- **Tailwind v4 `@theme` block** (`src/app.css`) — z-index scale precedent for D-13 color migration. Pattern proven, just extend.
- **Knip + ts-prune** — not yet installed, but standard `npx` tooling, no project-level setup needed (D-14).
- **Phase 27.4.x env-var pattern** — `LLM_BATCH_TIMEOUT_MS`, `LLM_V3_CONCURRENCY`, `V3_WATCHDOG_ROLLBACK_THRESHOLD` in `server/config.ts` show the convention for D-12.

### Established Patterns (28.1 must respect)

- **Atomic per-concern commits** — Phase 27.4.x canon. Each cleanup category, each env var introduction, each promoted color gets its own commit. NOT bundled.
- **Wave gate-keeper checkpoints** — Phase 27.4.2 Plan 04 / Plan 05 pattern. Each wave has a quality gate (vitest green, lint 0 errors, prettier clean, type-check clean) before downstream waves start.
- **D-XX numbering for decisions** — used in umbrella CONTEXT, propagates into PLAN headers.
- **CLAUDE.md memory updates after merge** — when cleanup deletes or moves code that's documented in CLAUDE.md, the merge commit updates CLAUDE.md in the same train.

### Integration Points (28.1 watch list)

- **Phase 27.4.6 cron config** (`vercel.json` `crons` array) — Hobby cap = 3 entries. 28.1 cleanup MUST NOT add a new cron entry; if a polling interval becomes env-tunable, that's runtime config, not a cron.
- **Phase 27.4.x Redis keys** (`events:llm:v3`, `events:llm-summary:v3`, etc.) — cleanup MUST NOT change these key names. The cache-bridge fallback chain (Pitfall 1 in 27.4.x) depends on them. If a cache key migration is in scope, it gets its own atomic commit with version bump.

</code_context>

<specifics>
## Specific Ideas

- "Don't compromise current features or functionality" — explicit user directive (umbrella). The CLAUDE.md feature inventory is the regression test surface. Any 28.1 deletion that touches a CLAUDE.md feature gets a manual UAT trace before the commit.
- Aggressive hardcode policy is for OPERATIONAL levers only (D-10). Domain-definitional constants (bbox, war start) stay centralized hardcode (D-11) — they define what the dashboard _is_, not how it operates.
- Triage-before-delete (D-14) is non-negotiable — surfaces "I forgot this exists" cases before they cause regressions.

</specifics>

<deferred>
## Deferred Ideas (28.1-specific)

- **Component-organization phase** for `react-refresh/only-export-components` warnings (17 instances) — captured during Phase 27.4.2 Plan 04 close. NOT in 28.1 scope unless cleanup naturally surfaces it as part of normalization band.
- **`react-hooks/exhaustive-deps` cleanup** (6 warnings, some may indicate latent bugs especially `dismissTimers` ref in `ProximityAlertOverlay`) — captured during Phase 27.4.2 Plan 04 close. Same deferral rationale: only fold in if normalization sweep needs to touch the surrounding code anyway; otherwise owned by a future phase.
- **Build Output API migration for `api/vercel-entry.js`** (Phase 999.2 backlog) — eliminates the 1.7MB tracked artifact + manual rebuild discipline. NOT folded into 28.1 — separate concern, doesn't gate v1.4 close.
- All other deferred items live in umbrella `28-CONTEXT.md` `<deferred>` section.

</deferred>

---

_Phase: 28.1-cleanup-sweep_
_Context inherited from umbrella: 28-CONTEXT.md_

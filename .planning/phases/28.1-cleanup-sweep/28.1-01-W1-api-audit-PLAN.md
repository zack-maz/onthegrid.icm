---
phase: 28.1-cleanup-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md
  - server/routes/* (only if probe surfaces a broken endpoint)
  - server/adapters/* (only if probe surfaces a broken adapter)
autonomous: false
requirements: [D-22, D-23, D-28]
must_haves:
  truths:
    - 'Every /api/* endpoint listed in the D-25 freshness table has a probe row in 28.1-W1-AUDIT.md.'
    - 'Every server/adapters/*.ts file has been probed for upstream URL reachability + schema parse.'
    - "Endpoints that returned non-200 or whose adapter failed have either: (a) a fix commit on this branch, or (b) an explicit 'deferred — reason' annotation in the audit table."
    - 'npx vitest run, npm run lint, npx prettier --check ., npx tsc --noEmit all exit 0 at wave close.'
  artifacts:
    - path: '.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md'
      provides: 'Hand-curated probe table covering every endpoint + adapter; D-23 one-shot audit deliverable'
      contains: '| endpoint | probe-strategy | status | upstream-URL-verified | schema-parse-confirmed | notes |'
    - path: 'server/routes/*.ts (only IF a fix is needed)'
      provides: 'Atomic per-endpoint fix commits'
      min_lines: 0
  key_links:
    - from: '28.1-W1-AUDIT.md'
      to: 'D-25 freshness table in 28.1-CONTEXT.md'
      via: 'endpoint name match'
      pattern: "^\\| /api/"
    - from: '28.1-W1-AUDIT.md'
      to: 'server/adapters/*.ts'
      via: 'adapter column entry per endpoint'
      pattern: "adapter:.*\\.ts"
---

<objective>
Hand-curate a one-shot probe of every `/api/*` endpoint and upstream adapter (D-22a, D-23). Fix what is broken before any cleanup-side work touches the same code paths. Output is a triage table committed at `.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md` that becomes the SUMMARY deliverable.

W1 also anchors the D-28 7-wave API-first SERIAL chain — its successful close (with vitest+lint+prettier+tsc all green) is the precondition for every subsequent wave (W2 through W7) to begin. The wave-gate ritual at Task 5 establishes the canonical gate pattern that the entire dependency graph relies on.

Purpose: API reliability is 28.1's #1 priority band per D-22. W1 establishes the known-good baseline that W2's `/api/health` observability layer will codify, and that all subsequent waves (W3-W7) will preserve.
Output: `28.1-W1-AUDIT.md` + zero-or-more atomic per-endpoint fix commits + green CI gate at wave boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28.1-cleanup-sweep/28.1-CONTEXT.md
@.planning/phases/28.1-cleanup-sweep/28.1-RESEARCH.md
@.planning/phases/28.1-cleanup-sweep/28.1-PATTERNS.md
@.planning/phases/28.1-cleanup-sweep/28.1-VALIDATION.md
@.planning/phases/28-performance-load-testing-was-phase-27/28-CONTEXT.md
@CLAUDE.md
@server/index.ts
@server/routes/health.ts
@server/routes/cron-health.ts
@server/cache/redis.ts

<interfaces>
<!-- Endpoint inventory authoritative list (from D-25 + canonical_refs in 28.1-CONTEXT.md). 15 entries. -->
/api/flights        — adapter: server/adapters/{opensky,adsbexchange,adsb-lol}.ts (multi-source) — cache key: flights:adsblol (+ alt source keys)
/api/ships          — adapter: server/adapters/aisstream.ts                       — cache key: ships:ais
/api/events         — adapter: server/adapters/gdelt.ts (+ LLM pipeline)          — cache key: events:llm:v3 (bridge to events:llm:v2 → events:gdelt)
/api/news           — adapter: server/adapters/{gdelt-doc.ts, rss.ts}             — cache key: news:gdelt
/api/markets        — adapter: server/adapters/yahoo-finance.ts                    — cache key: markets:yahoo:1d
/api/sites          — adapter: server/adapters/overpass.ts                         — cache key: sites:v3
/api/water          — adapter: server/adapters/overpass-water.ts                   — cache key: water:facilities:v3
/api/water/precip   — adapter: server/adapters/open-meteo-precip.ts                — cache key: water:precip
/api/sources        — config introspection (no upstream)                            — n/a
/api/llm-status     — server/lib/llmProgress.ts (in-memory)                         — n/a
/api/dashboard/auth-check — probe-only (200 expected)                              — n/a
/api/geocode        — server/adapters/nominatim.ts                                  — cache key: geocode:fwd:constrained:<hash>
/api/cron/health    — last-tick Redis key (TBD per cron handler)                    — n/a
/api/cron/warm      — last-tick Redis key                                           — n/a
/api/cron/refresh-events — last-tick Redis key                                      — n/a
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate adapters + verify wiring before probing</name>
  <files>(read-only)</files>
  <read_first>
    - server/index.ts (route mount table — confirm every endpoint above is actually mounted)
    - server/routes/ (full directory listing — discover any endpoint NOT listed in the inventory above)
    - server/adapters/ (full directory listing — discover any adapter NOT listed in the inventory above)
    - 28.1-PATTERNS.md §"Surfaced Issues for Planner Attention" item 1 (`server/routes/health.ts` already exists)
    - CLAUDE.md (full feature inventory — used as audit cross-reference for any endpoint discovered to not be in the static inventory above)
  </read_first>
  <action>
    Discover ALL routes and ALL adapters, NOT just the 15 listed in `<interfaces>`.

    1. `find server/routes -name '*.ts' -type f` — list every route file.
    2. `find server/adapters -name '*.ts' -type f` — list every adapter file.
    3. Cross-reference `server/index.ts` (and `server/app.ts` if separate) for every `app.use(...)` / route mount.
    4. Compare to the `<interfaces>` block in this plan's context. Capture deltas in a notes file `.planning/phases/28.1-cleanup-sweep/28.1-W1-DELTA.md` (NOT the audit doc — keep it separate).
       - Endpoints discovered in code but NOT in the inventory: add a row to the audit table.
       - Endpoints in the inventory but NOT in code: mark as "ABSENT — to investigate" in the audit table.
    5. NO source modifications in this task. NO commit.

    This task is pure discovery to size the audit before probing begins.

  </action>
  <verify>
    <automated>find /Users/zackmaz/Desktop/my_world/server/routes -name '*.ts' -type f -not -path '*/__tests__/*' | sort</automated>
  </verify>
  <done>
    Discovery output exists in working memory or `.planning/phases/28.1-cleanup-sweep/28.1-W1-DELTA.md`. Every route file and adapter file accounted for; deltas vs. inventory documented. Inventory drift items added to the audit table draft.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-curate the probe-result audit table at .planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md</files>
  <read_first>
    - 28.1-CONTEXT.md §D-23, §D-25 (probe-strategy mix + freshness thresholds — the audit derives its row schema from D-25)
    - 28.1-RESEARCH.md §"Pattern 1: /api/health Probe-Strategy Mix" lines 144-165 (per-endpoint probe-strategy assignment)
    - 28.1-RESEARCH.md §"Focus Area 8: Phase 21.3 prior load test learnings" lines 685-698 (priority audit targets: /api/ships AISStream cold-cache, /api/news GDELT timeouts, /api/cron/health Redis roundtrip)
    - server/cache/redis.ts (cacheGetSafe signature — used for cache-state introspection probes)
    - server/lib/llmProgress.ts (in-memory probe surface for /api/llm-status)
  </read_first>
  <action>
    Create `.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md` with the following EXACT structure:

    ```markdown
    ---
    phase: 28.1
    wave: W1
    decision: D-22a / D-23
    audit_date: <fill at task time>
    audit_completed_by: claude-code
    ---

    # Phase 28.1 W1 — API Audit Probe Results

    > One-shot hand-curated audit per D-23. NOT re-run in CI. /api/health (D-24, W2) replaces test-as-monitoring.

    ## Probe Result Table

    | Endpoint | Adapter | Probe Strategy | Status | Upstream URL Verified | Schema Parse Confirmed | Latency (ms) | Notes / Fix Commit |
    | -------- | ------- | -------------- | ------ | --------------------- | ---------------------- | ------------ | ------------------ |
    | /api/flights (adsblol) | server/adapters/adsb-lol.ts | cache-state introspection (cacheGetSafe('flights:adsblol', 999_999_999)) | HEALTHY \| FIXED \| BROKEN-DEFERRED | yes \| no | yes \| no | <latency> | <commit-sha or note> |
    | ... (one row per endpoint discovered in Task 1) ... | | | | | | | |

    ## Probing Methodology

    ### Cache-state introspection (preferred for hot polling + static + cron)
    Read `cacheGetSafe(key, 999_999_999)` and confirm the cache returns a non-null entry with parseable data. Costs zero upstream calls. Status:
    - HEALTHY = entry present, parses against expected shape, lastFresh < D-25 threshold for the endpoint
    - DEGRADED = entry present, parses, but lastFresh > D-25 threshold (stale but recoverable)
    - BROKEN = entry absent OR fails parse

    ### Direct adapter call (for /api/sources only)
    Invoke the adapter's exported fetch function. Confirm it returns expected shape without throwing.

    ### Cron last-tick timestamp (for /api/cron/*)
    Read the per-cron Redis key (cron handler writes it on success). Confirm last-tick within 26h.

    ### In-memory state read (for /api/llm-status)
    Read `server/lib/llmProgress.ts` exports directly. Confirm progress object exists.

    ## Adapter Upstream-URL Verification

    For each adapter file:
    | Adapter | Upstream URL | Reachable | Schema Parse on Sample Payload | Notes |
    | ------- | ------------ | --------- | ------------------------------ | ----- |
    | server/adapters/opensky.ts | https://opensky-network.org/api/states/all | yes \| no | yes \| no | <notes> |
    | ... (one row per adapter file discovered in Task 1) ... | | | | |

    ## Fixes Landed in W1

    | Commit SHA | Endpoint | Issue | Fix |
    | ---------- | -------- | ----- | --- |
    | ... | ... | ... | ... |

    ## Items Surfaced for Deferral (added to umbrella `<deferred>`)

    | Issue | Endpoint | Reason for Deferral | Tracked As |
    | ----- | -------- | ------------------- | ---------- |
    | ... | ... | ... | ... |

    ## Sign-off

    - [ ] Every endpoint discovered in Task 1 has a row above
    - [ ] Every adapter file has a row in the Adapter Upstream-URL Verification block
    - [ ] All BROKEN entries have either a fix commit SHA or a deferral row
    - [ ] D-25 freshness table cross-checked against probed lastFresh values
    ```

    Probe each endpoint per the strategy assigned in `28.1-RESEARCH.md` "Pattern 1" table. For dev probing, use `npm run dev` or `vercel dev`; for cache-state probes, query Upstash via `redis-cli` or the `/api/health` (existing /health) endpoint output as a starting point. Capture HTTP status, response shape, and adapter-side errors.

    For each BROKEN endpoint, EITHER:
    - Land a fix in a separate atomic commit (Tasks 3+ — one task per fix), and record the commit SHA in the table, OR
    - Annotate the row "BROKEN-DEFERRED" with a one-line reason; add the row to the umbrella `<deferred>` block at end of W1 (28.2 or v1.5 absorbs).

    Populate Task 1's discovered deltas (extra endpoints / missing endpoints) as rows.

    Commit message: `docs(28.1): create W1 API audit probe table (D-22a/D-23)`

  </action>
  <verify>
    <automated>test -f /Users/zackmaz/Desktop/my_world/.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md && grep -c "^| /api/" /Users/zackmaz/Desktop/my_world/.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md</automated>
  </verify>
  <done>
    `28.1-W1-AUDIT.md` exists. Probe Result Table has ≥15 rows (one per endpoint in inventory + any deltas). Adapter Upstream-URL Verification table has ≥13 rows (one per adapter file in `server/adapters/`). Every row has a status. Every BROKEN row has either a fix commit SHA or a deferral entry. Committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 2's audit table is hand-curated by Claude. The user must skim and approve before any fix commits land in subsequent tasks. This checkpoint exists because D-23 explicitly chose hand-curated over automated, and the user is the audit's sole consumer in v1.4 closeout.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md`.
    2. Skim every row of "Probe Result Table" — confirm probed status matches your operational expectation. Common red flags: endpoint marked HEALTHY when you know it's been throwing 500s; latency wildly different from production observability.
    3. Skim "Adapter Upstream-URL Verification" — confirm every adapter has been touched (no skipped rows).
    4. If any row is wrong: comment in chat with the row + the correction; Claude re-probes that endpoint and updates the table.
    5. If audit looks good: respond "approved" → Claude proceeds to Task 4 (per-endpoint fix commits IF any BROKEN rows exist) and then closes the wave.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or list the rows that need re-probing.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Land per-endpoint fix commits for BROKEN rows (one atomic commit per fix)</name>
  <files>server/routes/<endpoint>.ts AND/OR server/adapters/<adapter>.ts (PER FIX)</files>
  <read_first>
    - The route file for the BROKEN endpoint (e.g., server/routes/news.ts)
    - The adapter file backing the endpoint
    - 28.1-CONTEXT.md §"Established Patterns" — atomic per-concern commits is canonical
    - CLAUDE.md "Phase 27.4.x Redis keys" — DO NOT rename `events:llm:v3`, `sites:v3`, `water:facilities:v3` etc.
  </read_first>
  <action>
    SKIP this task entirely if Task 2's audit table contains zero BROKEN rows (i.e., everything was either HEALTHY/DEGRADED or BROKEN-DEFERRED).

    For each BROKEN row in the audit table that the user did NOT defer:
    1. Read the relevant route + adapter files.
    2. Reproduce the failure locally if possible (`npm run dev` + curl the endpoint, OR `vitest run` for the adapter test if one exists).
    3. Write the minimum fix. NO refactoring beyond the bug — that's W7's job. NO Redis key renames (Phase 27.4.x bridge depends on existing keys).
    4. If the adapter has an existing test file, add or update a test that reproduces the original failure. If no test exists, do NOT create one in W1 (vitest scaffolding is not in W1 scope; the test gap can be flagged for W7 sub-5).
    5. Commit per fix: `fix(28.1): repair <endpoint> — <one-line root cause>`
    6. Update the audit table's "Notes / Fix Commit" column with the SHA. Re-commit the table change as `docs(28.1): update W1 audit table with fix SHAs`.

    Each fix is its own commit. NEVER bundle multiple endpoint fixes into one commit.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && git log --oneline main..HEAD | grep -E "^[a-f0-9]+ fix\(28.1\)" | wc -l</automated>
  </verify>
  <done>
    Every BROKEN row from the audit (excluding user-deferred) has a corresponding fix commit on the branch. The audit table's "Notes / Fix Commit" column is populated. Re-running the probe for each fixed endpoint returns HEALTHY (re-record in audit if needed).
  </done>
</task>

<task type="auto">
  <name>Task 5: W1 wave-gate verification + commit closing summary</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W1-SUMMARY.md (new)</files>
  <read_first>
    - .planning/phases/28.1-cleanup-sweep/28.1-W1-AUDIT.md (the artifact this summary points at)
    - $HOME/.claude/get-shit-done/templates/summary.md (summary skeleton)
    - CLAUDE.md (verify no documented feature regressed)
  </read_first>
  <action>
    Run all four wave-gate commands in sequence; ALL must exit 0:

    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx vitest run
    npm run lint
    npx prettier --check .
    npx tsc --noEmit
    ```

    If any exits non-zero, fix and re-run BEFORE writing the summary.

    Then create `.planning/phases/28.1-cleanup-sweep/28.1-W1-SUMMARY.md` with:
    - Wave goal (D-22a/D-23 one-shot audit; D-28 establishes the wave-gate ritual for the entire 7-wave SERIAL chain)
    - Pointer to `28.1-W1-AUDIT.md`
    - Counts: total endpoints probed, HEALTHY/DEGRADED/BROKEN-FIXED/BROKEN-DEFERRED breakdown
    - Fix commit list (SHAs + endpoints)
    - Wave-gate output (4 commands above) — copy exit codes / pass-counts inline
    - Items added to umbrella `<deferred>`
    - W2 readiness signal: "W1 closes green; W2 (api/health endpoint) may begin"

    Commit: `docs(28.1): close W1 with audit summary (D-22a/D-23/D-28 — gate green)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit</automated>
  </verify>
  <done>
    All four wave-gate commands exit 0. `28.1-W1-SUMMARY.md` exists, references the audit doc, lists fix commits, declares wave green. The branch HEAD is the summary commit.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                                                                                                             |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| internal probing → upstream APIs | W1 may invoke external APIs (OpenSky, AISStream, GDELT, Yahoo, Overpass, Open-Meteo, Nominatim, NIM/OpenRouter) during probing. Standard outbound traffic, no credentials transit beyond what the adapters already use. |
| audit doc → repo                 | The audit table is committed; must NOT include API keys, tokens, or full URLs that leak credentials.                                                                                                                    |

## STRIDE Threat Register

| Threat ID    | Category                   | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                            |
| ------------ | -------------------------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28.1-W1-01 | I (Information disclosure) | 28.1-W1-AUDIT.md                   | mitigate    | Probe payloads recorded in the audit table MUST redact `Authorization` headers, `?api_key=` query params, and any LLM API token state. Use `***REDACTED***` placeholder. Audit author (Claude) verifies before committing. |
| T-28.1-W1-02 | D (DoS — self-inflicted)   | direct adapter probes              | accept      | Audit probes adapter functions once per endpoint during one-shot audit. Volume is negligible vs production polling cadence. No retry loops.                                                                                |
| T-28.1-W1-03 | T (Tampering — accidental) | server/routes/\*.ts (during fixes) | mitigate    | Fix commits in Task 4 follow atomic-per-concern discipline; each fix verified by re-probing the endpoint before commit. No bundled fixes that could mask one regression with another.                                      |

</threat_model>

<verification>
- `28.1-W1-AUDIT.md` exists at `.planning/phases/28.1-cleanup-sweep/`
- Probe table covers every endpoint discovered in Task 1
- Every adapter file has an upstream-URL verification row
- All BROKEN rows are EITHER fixed (commit SHA in table) OR deferred (umbrella `<deferred>` row)
- W1 wave-gate commands all exit 0
- `28.1-W1-SUMMARY.md` declares wave green; W2 is unblocked
</verification>

<success_criteria>

- W1 audit doc + summary committed on `feature/28.1-cleanup-sweep` branch
- Zero BROKEN endpoints remain (either fixed or formally deferred with user approval)
- `npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit` all exit 0
- W2 plan can begin (depends_on: [W1] satisfied)
  </success_criteria>

<output>
After completion, create `.planning/phases/28.1-cleanup-sweep/28.1-01-SUMMARY.md` (separate from `28.1-W1-SUMMARY.md` — the former is the GSD plan-summary canonical file consumed by future planners; the latter is the wave-result deliverable). Both should reference the audit doc.
</output>

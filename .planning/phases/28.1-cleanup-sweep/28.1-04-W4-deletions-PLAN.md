---
phase: 28.1-cleanup-sweep
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - <varies — one module per atomic commit, sourced from W3 triage approval buckets>
  - CLAUDE.md (only IF a deletion touches a documented feature inventory section)
autonomous: false
requirements: [D-14, D-29]
must_haves:
  truths:
    - 'Every approved deletion lands as its own atomic commit touching ONE module.'
    - 'Re-running knip after W4 reports 0 dead exports for the modules that were deleted.'
    - 'No file in the W3 EXCLUDED bucket is deleted.'
    - 'If a deletion touches a documented CLAUDE.md feature, the same commit updates the corresponding CLAUDE.md section.'
    - 'Vitest + lint + prettier + tsc all green at wave close.'
    - 'Manual UAT trace per CLAUDE.md feature inventory passes (dashboard loads, all toggles work, search modal works, detail panel works, threat clusters render).'
  artifacts:
    - path: '(varies — one src/* or server/* file removed per commit)'
      provides: 'Atomic per-module deletion commits'
      min_lines: 0
    - path: 'CLAUDE.md'
      provides: 'Updated feature inventory IF any deletion touched a documented section'
      min_lines: 0
  key_links:
    - from: 'W4 commits'
      to: '28.1-W3-TRIAGE.md §Approval Buckets'
      via: 'each commit references the triage row(s) it implements'
      pattern: 'W3 triage:'
---

<objective>
Execute the W3 triage doc's approval buckets per D-14 step 4: atomic per-module deletion commits with conservative bias (D-29).

- `high-dead` bulk-approve candidates → one commit per module, no per-item gate
- `probable-dead` individual review → one commit per module after per-item user yes/no
- `ambiguous` → default = NO deletion; only if user explicitly approves during this wave
- `EXCLUDED` → NEVER delete (W5/W6 in-flight scope, W2 freshly-introduced, preserved-but-inactive)

Each commit:

- Touches ONE module (file OR a tightly-coupled module group like a sibling test file)
- References the W3 triage row(s) it implements in the commit body
- If the deleted module is documented in CLAUDE.md, updates the corresponding section in the same commit (atomic per-concern canon)
- Re-runs `npx vitest run` immediately after (or at task close) to confirm no regression

Purpose: physically remove dead code while preserving the regression test surface. CLAUDE.md feature inventory is the authoritative checklist.
Output: N atomic deletion commits + (optional) CLAUDE.md updates + W4 SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28.1-cleanup-sweep/28.1-CONTEXT.md
@.planning/phases/28.1-cleanup-sweep/28.1-RESEARCH.md
@.planning/phases/28.1-cleanup-sweep/28.1-PATTERNS.md
@.planning/phases/28.1-cleanup-sweep/28.1-VALIDATION.md
@.planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md
@.planning/phases/28.1-cleanup-sweep/28.1-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land all `high-dead` bulk-approve commits, one per module</name>
  <files>(varies — one module per commit, sourced from W3 triage `high-dead` bucket)</files>
  <read_first>
    - .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md §"high-dead bulk-approve candidates" section
    - The full source file being deleted (read it ONCE before deletion to confirm no surprise dependencies)
    - CLAUDE.md (search for the symbol/file name; if found, the deletion commit MUST include a CLAUDE.md update)
    - 28.1-CONTEXT.md §"Established Patterns" — atomic per-concern commits are canonical (Phase 27.4.x)
  </read_first>
  <action>
    For each module in the W3 triage `high-dead` bucket:

    1. Read the file (Read tool) to confirm:
       - No `import.meta.glob` reference elsewhere captures it (Pitfall 2 cross-check)
       - No barrel re-export elsewhere keeps it alive (Pitfall 4 cross-check)
       - No CLAUDE.md feature section documents it
    2. If the cross-check turns up uncertainty, RECLASSIFY to `ambiguous` (do NOT delete in this task; surface to checkpoint at end).
    3. If the cross-check confirms dead-ness:
       a. `git rm <path>` (preserves history)
       b. If a sibling test file exists (e.g., deleting `src/lib/foo.ts` with `src/__tests__/lib/foo.test.ts`), `git rm` it too — same commit
       c. If CLAUDE.md documents the symbol/file: edit CLAUDE.md to remove or update the section
       d. Run `npx vitest run` — if RED, revert (`git restore --staged --worktree <files>`) and reclassify
       e. If GREEN: commit with message:
          ```
          chore(28.1): delete <module> (W3 triage: high-dead, knip flag #N)

          Reason from W3 triage doc: <one-line rationale>
          Cross-check: no import.meta.glob match, no barrel re-export, no CLAUDE.md doc impact.
          Test suite verified green post-deletion.
          ```
    4. Repeat for every `high-dead` row.

    Pace: one commit at a time, vitest after each. NEVER bundle multiple modules in one commit.

    If the bucket is empty, this task no-ops and proceeds to Task 2.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && git log --oneline main..HEAD --since="1 hour ago" | grep -E "^[a-f0-9]+ chore\(28.1\): delete" | wc -l</automated>
  </verify>
  <done>
    Every `high-dead` bucket entry has a corresponding deletion commit (or has been reclassified to ambiguous and surfaced to the wave checkpoint). Vitest green after each commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Every `probable-dead` triage entry needs an individual yes/no decision from the user before W4 deletes it. This checkpoint surfaces the bucket and waits.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md` §"probable-dead individual review" section.
    2. For each row: respond DELETE | KEEP | INVESTIGATE.
       - DELETE: Claude lands the deletion commit per the same pattern as `high-dead`.
       - KEEP: row is reclassified to `ambiguous`; no deletion.
       - INVESTIGATE: Claude grep + read the symbol's call sites + reports findings; you re-decide.
    3. Same per `ambiguous` rows IF you want to delete any of them (default for `ambiguous` is KEEP).
    4. Respond with one line per row: `Row 5: DELETE` / `Row 7: KEEP` / etc., OR `BULK DELETE all probable-dead` / `BULK KEEP all probable-dead`.
  </how-to-verify>
  <resume-signal>Provide per-row decisions or a bulk verdict; Claude proceeds to Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Land approved `probable-dead` + opt-in `ambiguous` deletion commits</name>
  <files>(varies — one module per commit, sourced from Task 2 user decisions)</files>
  <read_first>
    - 28.1-W3-TRIAGE.md (updated with user decisions from Task 2)
    - The full source file being deleted (read ONCE per file before deletion)
    - CLAUDE.md (CLAUDE.md section search per file)
  </read_first>
  <action>
    For each row marked DELETE in Task 2:
    - Apply the same per-module deletion procedure as Task 1 (read → cross-check → git rm → CLAUDE.md update if needed → vitest → commit).
    - Commit message format:
      ```
      chore(28.1): delete <module> (W3 triage: probable-dead, user-approved)

      Reason from W3 triage doc: <one-line rationale>
      User approval signal: <inline reference to checkpoint response>
      Cross-check: <findings from import.meta.glob / barrel re-export grep>
      Test suite verified green post-deletion.
      ```

    For each row marked INVESTIGATE in Task 2:
    - Run `grep -rn <symbol> src/ server/ scripts/` and read the call sites.
    - Report findings inline (no commit yet).
    - Wait for user re-decision (loop back to checkpoint).

    Skip rows marked KEEP entirely.

    If user issued `BULK KEEP` for the entire `probable-dead` bucket, this task no-ops.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && git log --oneline main..HEAD --since="2 hours ago" | grep -E "^[a-f0-9]+ chore\(28.1\): delete.*probable-dead" | wc -l</automated>
  </verify>
  <done>
    Every Task 2 DELETE row has a corresponding commit. Every INVESTIGATE row has been resolved (either via re-checkpoint or by reclassifying KEEP). Vitest green throughout.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify EXCLUDED bucket invariant</name>
  <files>(read-only verification)</files>
  <read_first>
    - .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md §"EXCLUDED" bucket
    - git log of W4 commits so far
  </read_first>
  <action>
    For each file listed in the W3 EXCLUDED bucket:
    ```bash
    cd /Users/zackmaz/Desktop/my_world
    test -f <file>  # MUST exit 0 (file still exists)
    ```

    The CANONICAL verification is the hardcoded `test -f` list in the `<automated>` block below — every EXCLUDED bucket file is enumerated explicitly. The `for f in ...; do test -f "$f"; done` shell loop is the contract; on first MISSING, it exits 1 and surfaces the violation.

    <!-- alternative-verification: brittle to formatting drift
         An earlier draft of this task included an `awk -F'|' '{print $2}' | tr -d ' ' | grep -v "^Hardcode"`
         pipeline that parsed the EXCLUDED bucket out of the triage doc Markdown table. That was abandoned
         because (a) it's never executed (the <automated> block uses the hardcoded list above), and (b) any
         future change to the triage doc table format (column reorder, header rename, leading whitespace)
         would silently make the parser drop rows. The hardcoded list below is the canonical check; if a new
         file is added to the EXCLUDED bucket in the W3 triage doc, the executor MUST add it to the
         <automated> block manually as part of the W3 → W4 hand-off. -->

    If ANY file in the EXCLUDED bucket no longer exists, the wave invariant is violated. Restore the file via `git restore --source main <path>` and reclassify the deletion as a P0 bug; surface to user immediately.

    No commit in this task — pure verification.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && for f in src/hooks/useFlightPolling.ts src/hooks/useShipPolling.ts src/hooks/useEventPolling.ts src/hooks/useNewsPolling.ts src/hooks/useMarketPolling.ts src/hooks/useWaterPrecipPolling.ts src/hooks/useLLMStatusPolling.ts src/lib/attackStatus.ts src/hooks/useProximityAlerts.ts src/lib/severity.ts src/styles/app.css src/components/map/layers/constants.ts src/lib/factions.ts src/lib/ethnicGroups.ts server/lib/healthSchema.ts server/lib/healthSources.ts src/lib/healthClient.ts src/hooks/useHealthStatus.ts src/components/ui/HealthBanner.tsx server/config.ts; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && echo "EXCLUDED bucket invariant: PASS"</automated>
  </verify>
  <done>
    Every EXCLUDED file still exists. Verification command exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All approved deletions are landed. Final regression check is the manual UAT against CLAUDE.md feature inventory — vitest cannot exercise the running map.
  </what-built>
  <how-to-verify>
    1. `npm run dev` — start dashboard.
    2. Open `http://localhost:3000` in browser.
    3. Walk the CLAUDE.md feature inventory section by section. For each documented feature, confirm the listed behavior still works:
       - **Map Patterns:** map loads with terrain, CompassControl renders, deck.gl overlays appear.
       - **Flight Data Patterns:** flight chevrons render yellow; toggling Ground hides ground traffic; toggling Unidentified affects red flights.
       - **Multi-Source Flight Data:** SourceSelector / StatusPanel reflects selected source.
       - **Ship & Event Data:** ships render purple; events render in red spectrum colors per type.
       - **Conflict Event Data:** GDELT events render with precision rings.
       - **LLM Event Pipeline:** /api/events serves enriched events (verify via DevApiStatus All APIs tab → events row HEALTHY).
       - **Layer Controls & Tooltips:** every toggle works (Flights / Ground / Unidentified / Ships / Events / 5 sub-toggles / Sites / 5 site sub-toggles / Hit Only).
       - **Detail Panel:** clicking any entity opens detail panel with correct content per type.
       - **Analytics Counters:** counters reflect visible entity counts.
       - **Serverless Cache:** /api/health (W2) shows cache freshness for every endpoint.
       - **Key Sites Overlay:** sites render with healthy=green / attacked=orange.
       - **News Feed:** /api/news serves clusters; NotificationBell shows count.
       - **Notification Center:** notifications render; click flies to event.
       - **Oil Markets Tracker:** MarketsSlot shows sparkline.
       - **Search & Filter System:** Cmd+K opens search modal; tag-based queries work; near: queries drop pin.
       - **Visualization Layers:** geographic / weather / threat / political / ethnic / water layers toggle on/off.
       - **Detail Panel Navigation Stack:** back-button works after cluster→entity drill.
       - **Political Boundaries Layer:** country fills render with US-aligned blue / Iran-aligned red / neutral gray.
       - **Ethnic Distribution Layer:** hatched ethnic zones render.
       - **Water Stress Layer:** rivers + facilities render with stress colors.
    4. If ANY feature regressed: report inline with the regressed feature + suspected commit (use `git log --oneline main..HEAD` to find candidates). Claude reverts the offending commit and surfaces to user for retry.
    5. If all features work: respond "approved" → wave proceeds to Task 6 close.
  </how-to-verify>
  <resume-signal>Type "approved" or list regressed features.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: W4 wave-gate verification + commit closing summary</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W4-SUMMARY.md (new)</files>
  <read_first>
    - .planning/phases/28.1-cleanup-sweep/28.1-W3-TRIAGE.md (cross-reference completed deletions)
    - All commits in W4 (`git log --oneline main..HEAD`)
    - CLAUDE.md (verify any updates landed cleanly)
  </read_first>
  <action>
    Run all four wave-gate commands AND a build:

    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx vitest run
    npm run lint
    npx prettier --check .
    npx tsc --noEmit
    npm run build
    ```

    ALL must exit 0.

    Re-run knip to confirm post-W4 state:
    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx --yes knip --reporter symbols > .planning/phases/28.1-cleanup-sweep/W4-knip-output.txt 2>&1 || true
    ```
    Compare W4-knip-output.txt against W3-knip-output.txt: every deleted module's export entries should now be absent. Note remaining flags = the EXCLUDED + ambiguous-keep + probable-dead-keep buckets.

    Create `.planning/phases/28.1-cleanup-sweep/28.1-W4-SUMMARY.md` with:
    - Wave goal (D-14 step 4 + D-29 atomic per-module deletions)
    - Deletion commit list (SHAs + module paths)
    - Bucket dispositions: high-dead deleted N, probable-dead deleted M, ambiguous deleted K (if any), EXCLUDED preserved (full count)
    - CLAUDE.md sections updated (if any)
    - knip diff: pre-W3 flag count → post-W4 flag count
    - Wave-gate output (5 commands)
    - Manual UAT result (referenced from Task 5 checkpoint)
    - W5 readiness signal: "W4 closes green; W5 (hardcode generalization) may begin"

    Commit: `docs(28.1): close W4 with deletions summary (D-14/D-29 — gate green + UAT pass)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    All five gates green. SUMMARY committed. knip post-W4 output captured. Branch HEAD is summary commit. W5 unblocked.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                   |
| -------------------------- | ------------------------------------------------------------------------------------------------------------- |
| W4 deletions → repo source | Modifies tracked source files via git rm. Each deletion is a commit; full git history preserved for rollback. |

## STRIDE Threat Register

| Threat ID    | Category                              | Component        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                    |
| ------------ | ------------------------------------- | ---------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28.1-W4-01 | T (Tampering — accidental regression) | deleted modules  | mitigate    | Per-deletion vitest run (Task 1 step 4d, Task 3); EXCLUDED-bucket verification (Task 4); manual UAT against CLAUDE.md feature inventory (Task 5); npm run build at wave close. Five-layer defense. Per-deletion atomic commits enable git revert of any single offender without losing other work. |
| T-28.1-W4-02 | I (Information disclosure)            | deletion commits | accept      | Source code being deleted is already-public committed code; nothing new is exposed.                                                                                                                                                                                                                |
| T-28.1-W4-03 | D (DoS — broken build)                | wave-gate        | mitigate    | npm run build added to wave-gate; if any deletion broke the bundle, gate fails and surfaces immediately for revert.                                                                                                                                                                                |

</threat_model>

<verification>
- Every `high-dead` triage row has a deletion commit OR a documented reclassification
- Every `probable-dead` triage row has either a deletion commit OR a KEEP decision recorded
- Every EXCLUDED bucket file still exists (Task 4 verification)
- Manual UAT against CLAUDE.md feature inventory passes (Task 5)
- All five wave-gate commands exit 0
- W4 SUMMARY committed
</verification>

<success_criteria>

- All approved deletions landed as atomic per-module commits
- knip post-W4 reports 0 dead exports for deleted modules
- Manual UAT confirms zero CLAUDE.md feature regressions
- `npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit && npm run build` all exit 0
- W5 plan can begin
  </success_criteria>

<output>
After completion, create `.planning/phases/28.1-cleanup-sweep/28.1-04-SUMMARY.md`.
</output>

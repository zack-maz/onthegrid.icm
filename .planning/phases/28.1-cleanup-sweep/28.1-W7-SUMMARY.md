---
phase: 28.1-cleanup-sweep
plan: 07
wave: W7
subsystem: cleanup
tags:
  [
    ts-strict,
    react-hooks-exhaustive-deps,
    zustand-selectors,
    redis-key-audit,
    vitest-patterns,
    import-order,
    logging-migration,
    D-27,
    phase-28.1-complete,
  ]

# Dependency graph
requires:
  - phase: 28.1
    provides:
      D-27 7-sub normalization pass — closes the LAST band of Phase 28.1's
      cleanup-sweep scope. Builds on W6's CSS @theme migration (D-13), W5's
      hardcode generalization (D-10/D-11/D-12), W4's ghost-code deletions,
      W3's knip triage, W2's /api/health endpoint (D-25/D-26), and W1's API
      audit (D-21..D-24).
provides:
  - 'Eight atomic commits across the 7 sub-categories (sub-1 already 0;
    sub-3 + sub-5 + sub-7 audit-only no-op; sub-2 = 5 per-file commits;
    sub-4 = 2 commits (audit + cron-warm align); sub-6 = 2 commits (rule
    enable + autofix sweep); plus the wave-close commit below).'
  - 'tsc --noEmit: 0 errors at wave-close (sub-1 already clean — Phase 27.4.1
    cleanup absorbed the prior 8 useEntityLayers.ts depthTest deck.gl v9
    drift errors; nothing to narrow in W7 sub-1).'
  - 'npm run lint: 0 errors AND 0 react-hooks/exhaustive-deps warnings
    (down from 6 pre-W7). Five per-file fixes per RESEARCH §Focus Area 5
    decision tree:
    (1) ProximityAlertOverlay: dismissTimers.current ref-stale fixed via
        local-bind capture + setExpandedSiteId added to deps;
    (2) useSearchResults: isWaterLayerActive added as real value dep;
    (3) useQuerySync: eslint-disable rationale (syncState is a derived
        object literal — including would re-fire every render);
    (4) useEntityLayers: clusterEventIds added as real value dep;
    (5) Sidebar: eslint-disable rationale (sectionRefs is a fresh literal
        of stable refs — branch 1).'
  - 'Zustand selectors (sub-3): grep audit returns 0 full-state subscribers
    across all 15 stores in src/. Every consumer already uses the curried
    `s => s.field` pattern. No changes needed.'
  - 'Redis cache key audit (sub-4): committed
    `.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md` enumerating
    102 cacheGet/cacheSet/cacheGetSafe/cacheSetSafe call sites across server/.
    23 load-bearing Phase 27.4.x bridge keys explicitly marked PRESERVED.
    Two STALE outliers identified in cron-warm.ts (sites:v2 and
    water:facilities) and aligned to canonical (sites:v3 and
    water:facilities:v3) in a separate atomic commit; cron-warm payload
    shape also corrected from bare arrays → {sites|facilities, filterStats}
    envelope to match Plan 11 G3/G4 contract enforced by route readers.'
  - 'vitest patterns (sub-5): audit returned 0 it.todo, 0 *.skip, 0
    toMatchSnapshot calls across src/ + server/. No outstanding stubs to
    annotate. Vitest 2025/2025 green throughout all W7 commits.'
  - 'ESLint import/order rule (sub-6): enabled at warn level. Ran ESLint
    --fix sweep across 273 files (1027 violations → 0). 8 server files had
    `const log = logger.child({...})` declared BETWEEN import statements,
    blocking auto-fix; manually moved each to AFTER all imports. BaseMap.tsx
    type imports consolidated into a single block. v3 / llmTokenBudget had
    stray late-file type imports promoted to top type-import block. Four
    test files with intentional vi.mock interleavings carry inline
    eslint-disable rationale comments. Final state: 0 import/order
    violations; 16 remaining warnings are pre-existing
    react-refresh/only-export-components (out of W7 scope).'
  - 'Logging migration (sub-7): NO-OP — server/lib/llmEventExtractor.v3.ts,
    server/adapters/*.ts, and server/routes/*.ts already at 0 console.*
    calls (migrated in earlier phases). server/lib/logger.ts is the
    canonical pino instance and was NOT replaced; server/lib/log.ts does
    NOT exist (PATTERNS.md surfaced finding #2 honored).'
  - 'CLAUDE.md closeout: stale "Graceful config" line replaced with
    Phase 26.3 fail-fast `parseEnv()` reality. New "Phase 28.1 Cleanup
    Sweep — closeout 2026-05-03" section appended to the bottom listing
    all 7 wave deliverables + Redis bridge preservation note.'

# Affects
affects:
  - subsystem: ESLint baseline
    nature: tightened
    detail:
      Sub-2 cleared 6 react-hooks/exhaustive-deps warnings. Sub-6 added
      `import/order` rule + 0-violation baseline. Pre-existing
      react-refresh/only-export-components warnings (16) intentionally
      out of W7 scope; future phase may address.
  - subsystem: Phase 27.4.x Redis bridge contract
    nature: preserved
    detail:
      All 23 load-bearing keys (events:llm:v3, sites:v3, water:facilities:v3,
      events:llm:v2, events:llm:v3:partial, events:llm-dlq, etc.) UNCHANGED.
      Audit doc enforces the no-rename rule. The cron-warm sites:v2 →
      sites:v3 + water:facilities → water:facilities:v3 alignment is
      INTENDED — cron-warm was writing to wrong keys (no-op pre-fix; now
      actually warms the live cache). Pitfall 1 fallback chain unaffected.
  - subsystem: cron-warm cache contract
    nature: corrected
    detail:
      Pre-W7 the cron-warm wrote bare arrays to non-canonical keys; route
      handlers read SitesCachePayload/WaterCachePayload from canonical keys.
      Net effect: every 12h pre-warm landed in a key no reader consumed.
      Post-W7 the cron writes the proper {sites|facilities, filterStats}
      envelope to canonical sites:v3 / water:facilities:v3 keys. The first
      cron run after deploy now actually pre-warms the live cache.

# Key files
created:
  - .planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md (sub-4)
  - .planning/phases/28.1-cleanup-sweep/28.1-W7-SUMMARY.md (this file)

modified:
  - src/components/map/ProximityAlertOverlay.tsx (sub-2)
  - src/hooks/useSearchResults.ts (sub-2)
  - src/hooks/useQuerySync.ts (sub-2)
  - src/hooks/useEntityLayers.ts (sub-2)
  - src/components/layout/Sidebar.tsx (sub-2)
  - server/routes/cron-warm.ts (sub-4)
  - eslint.config.js (sub-6)
  - package.json + package-lock.json (sub-6 — eslint-plugin-import dep added)
  - 271 additional .ts/.tsx files reformatted by import/order --fix sweep
    (sub-6)
  - server/routes/{events,flights,markets,news,ships,water,weather}.ts +
    server/adapters/gdelt.ts (sub-6 — `const log = logger.child(...)`
    moved to after all imports so import/order can sort cleanly)
  - server/lib/llmEventExtractor.v3.ts + server/lib/llmTokenBudget.ts
    (sub-6 — stray late-file type imports promoted to top block)
  - src/components/map/BaseMap.tsx (sub-6 — type imports consolidated)
  - 4 test files with eslint-disable rationale for vi.mock interleaving
    (DeckGLOverlay, LayerToggles, PoliticalOverlay, useDraggable +
    overpass-water R-08 mid-file import) (sub-6)
  - CLAUDE.md (closeout — stale Graceful config line corrected; Phase 28.1
    closeout section added)

# Decisions made

decisions:
  - title: 'Sub-1 TS strict was already clean — no commit needed'
    detail:
      'Pre-W7 baseline `npx tsc --noEmit` returned 0 errors. The 8
      remaining `useEntityLayers.ts depthTest deck.gl v9 drift` errors
      noted in CLAUDE.md (Phase 27.4.1 closing) had been absorbed by
      intervening cleanup before W7 started. Sub-1 documented as no-op
      in this SUMMARY rather than committed empty.'
  - title:
      'Sub-3 Zustand audit was already clean — single SUMMARY-only documentation'
    detail:
      'The full-state-subscriber grep returned 0 violations across all
      15 stores. Per the plan: "May be combined with the W7 SUMMARY
      commit at task close to reduce commit noise IF zero violations." —
      consolidated into this SUMMARY.'
  - title:
      'Sub-4 cron-warm key alignment was a real bug fix, not just rename'
    detail:
      'cron-warm.ts was writing bare arrays to sites:v2 + water:facilities,
      while the route handlers read {sites, filterStats} envelopes from
      sites:v3 + water:facilities:v3. Net effect: every 12h pre-warm hit
      a key no reader consumed; the cron achieved nothing. Aligned BOTH
      the keys AND the payload shape so the warmer actually warms the
      live cache. Treated as zero-risk per the audit (no downstream
      readers of the old keys).'
  - title:
      'Sub-5 vitest patterns were already disciplined — single SUMMARY-only documentation'
    detail:
      'Audit returned 0 it.todo, 0 *.skip, 0 toMatchSnapshot. No
      outstanding test stubs or brittle snapshots. Documented as
      no-op rather than committed empty.'
  - title:
      'Sub-6 import/order — manual interventions documented per file'
    detail:
      '8 server files had `const log = logger.child(...)` between imports;
      auto-fix could not reorder past the non-import statement. Manually
      moved each. BaseMap.tsx had type imports interleaved with value
      imports — consolidated to bottom block. v3 and llmTokenBudget had
      stray late-file type imports — promoted to top type-import block
      next to the others. 4 test files have intentional vi.mock
      interleavings — carry inline eslint-disable comments with rationale.'
  - title:
      'Sub-7 logging migration was already complete — NO-OP'
    detail:
      'grep -c "console\\." across server/lib/llmEventExtractor.v3.ts +
      server/adapters/*.ts + server/routes/*.ts returned 0. The pino
      logger.child() pattern is already universal in those paths. No new
      server/lib/log.ts file created (PATTERNS.md surfaced finding #2
      honored).'

# Tech stack
added:
  - name: eslint-plugin-import
    version: ^2.31.0
    role:
      Adds `import/order` rule for src/ + server/ import statement ordering.
      Enabled at warn-level; 0 violations at W7 close.
patterns:
  - 'Import order discipline: builtin → external → internal (@/ alias) →
    parent → sibling → index → object → type. Blank line between groups.
    Alphabetized within group, case-insensitive. Type-only imports cluster
    at the bottom per project convention.'
  - 'Server logging discipline: `import { logger } from "../lib/logger.js"`
    + `const log = logger.child({ module: "<name>" });` declared AFTER all
    imports (not interleaved). All `console.*` calls in server/lib/extractor,
    server/adapters/, server/routes/ now route through pino with redactPaths
    + module tagging.'
  - 'cron-warm cache shape: writes `{sites|facilities, filterStats}` envelope
    matching SitesCachePayload / WaterCachePayload contracts; bare arrays
    would corrupt downstream cache reads (Plan 11 G3/G4).'

# Metrics
metrics:
  pre_w7_ts_errors: 0
  post_w7_ts_errors: 0
  pre_w7_lint_errors: 0
  post_w7_lint_errors: 0
  pre_w7_react_hooks_warnings: 6
  post_w7_react_hooks_warnings: 0
  pre_w7_no_explicit_any: 0
  post_w7_no_explicit_any: 0
  pre_w7_import_order_violations: 'N/A (rule not yet enabled)'
  post_w7_import_order_violations: 0
  pre_w7_console_in_target_paths: 0
  post_w7_console_in_target_paths: 0
  pre_w7_it_todo: 0
  post_w7_it_todo: 0
  pre_w7_zustand_full_state_subscribers: 0
  post_w7_zustand_full_state_subscribers: 0
  pre_w7_total_lint_warnings: 22
  post_w7_total_lint_warnings: 16
  files_touched_by_import_fix_sweep: 273
  cache_call_sites_audited: 102
  load_bearing_redis_keys_preserved: 23
  vitest_pre_w7: '2025/2025 passing'
  vitest_post_w7: '2025/2025 passing'
  duration_seconds: 1723
  duration_human: '~29 minutes'
  completed: '2026-05-03'

# Wave-gate output

wave_gate:
  vitest:
    command: 'npx vitest run'
    exit_code: 0
    result: '2025/2025 passing across 143 test files'
  lint:
    command: 'npm run lint'
    exit_code: 0
    result: '0 errors, 16 warnings (all pre-existing react-refresh/only-export-components)'
  prettier:
    command: 'npx prettier --check .'
    exit_code: 0
    result: 'All matched files use Prettier code style'
  tsc:
    command: 'npx tsc --noEmit'
    exit_code: 0
    result: '0 errors'
  build:
    command: 'npm run build'
    exit_code: 0
    result: 'Vite client build + tsup server bundle both succeed'

# Audit deliverable
audit_doc: '.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md'

# CLAUDE.md updates

claude_md_updates:
  - section: 'Vercel Deployment (Phase 14)'
    change: '"Graceful config" line corrected to reflect Phase 26.3+ fail-fast parseEnv() reality.'
  - section: 'Phase 28.1 Cleanup Sweep — closeout 2026-05-03 (NEW)'
    change: 'Appended to bottom of CLAUDE.md, summarizing all 7 W deliverables + Redis bridge preservation.'

# Items deferred / out of scope

deferred:
  - item: 'react-refresh/only-export-components warnings'
    count: 16
    reason: 'Pre-existing baseline; not in W7 scope. Fix would require
      splitting non-component exports out of component files (e.g.,
      MapLegend.tsx exports LEGEND_REGISTRY, ThreatHeatmapOverlay.tsx exports
      ThreatZoneData type, etc.). Cosmetic — does not affect HMR fast-refresh
      in practice because the project has no fast-refresh-dependent dev flow
      that triggers (vite re-renders on every change anyway).'
    follow_up_phase: 'TBD — possibly Phase 29.x cleanup-sweep-2 if needed'
  - item: 'src/ console.* migration'
    reason: 'RESEARCH Open Question 5 + Anti-Patterns deferred client-side
      logging. W7 sub-7 only targeted server-side per the plan scope. The
      pino logger.ts is a server module; src/ would need a separate
      client-side logger or a no-op shim.'
    follow_up_phase: 'TBD — only if observability needs grow'
  - item: 'Stagger AppShell polling-hook mount burst'
    surfaced: 'Phase 28.1 hot-fix 2026-05-04 (commit cbc32d6 raised public
      rate-limit cap 6 → 60 req/min). Root cause is that ~9 polling hooks
      burst-fire their first fetch in <50ms when AppShell mounts, peaking
      well above any reasonable per-IP cap. The cap raise is a workaround;
      a cleaner fix is to stagger the initial fetches so they never burst
      together. ~10 lines in AppShell.tsx wrapping each hook init in
      `setTimeout(..., n * 50)` would spread 9 hooks across 450ms. Worth
      doing if Phase 28.2 D-04 Bearer-bypass tightens public for anonymous
      traffic and we want the dashboard to behave well even at the lower
      cap.'
    follow_up_phase: 'Phase 28.2 (alongside D-04 Bearer-bypass) or 28.4'
  - item: 'Persist Zustand stores to localStorage'
    surfaced: 'Phase 28.1 hot-fix 2026-05-04. Pure UX bonus orthogonal to
      rate limiting: on dashboard reload, Zustand `persist` middleware
      hydrates each store with the last-known-good entity arrays so the
      map renders instantly with stale data while polling hooks fetch
      fresh. ~5 lines per store across 8-10 stores (flightStore, shipStore,
      eventStore, newsStore, marketStore, siteStore, waterStore, etc.).
      Caveat: must include a server-cache-key version field so a backend
      rebuild that changes entity shape invalidates client persistence.'
    follow_up_phase: 'Phase 28.3 polish (or earlier if cold-load UX is
      flagged in load testing) — not blocking, pure quality-of-life.'

# Phase 28.1 closure signal

phase_closure:
  status: 'PHASE 28.1 COMPLETE — 7 of 7 plans landed, all green'
  milestone: 'v1.4 GDELT Redo & Performance advances'
  next_action: 'Run /gsd-verify-work, then merge feature/28.1-cleanup-sweep
    to main per CLAUDE.md "Phase boundaries" convention.'
  branch: 'feature/28.1-cleanup-sweep'
  commits_in_phase:
    w1: 'see 28.1-W1-SUMMARY.md'
    w2: 'see 28.1-W2-SUMMARY.md'
    w3: 'see 28.1-W3-SUMMARY.md'
    w4: 'see 28.1-W4-SUMMARY.md'
    w5: 'see 28.1-W5-SUMMARY.md'
    w6: 'see 28.1-W6-SUMMARY.md'
    w7: 'see commit list below — 8 commits + this docs commit + tracking commit'

# W7 commit ledger

commits:
  sub_2_proximity_alert: 'd175e43 — fix(28.1): clear exhaustive-deps warnings in ProximityAlertOverlay (D-27 sub-2)'
  sub_2_search_results: 'a286fb6 — fix(28.1): add isWaterLayerActive dep in useSearchResults (D-27 sub-2)'
  sub_2_query_sync: 'bb855aa — fix(28.1): eslint-disable rationale for syncState in useQuerySync (D-27 sub-2)'
  sub_2_entity_layers: '6eddf16 — fix(28.1): add clusterEventIds dep in useEntityLayers (D-27 sub-2)'
  sub_2_sidebar: '10ff85e — fix(28.1): eslint-disable rationale for sectionRefs in Sidebar (D-27 sub-2)'
  sub_4_audit: 'be3e248 — docs(28.1): W7 sub-4 Redis key audit (D-27 — preserve Phase 27.4.x bridge)'
  sub_4_cron_warm_align: '514838f — refactor(28.1): align cron-warm Redis keys + envelope shape with route readers (W7 sub-4)'
  sub_6_rule_enable: '2bf7f7c — chore(28.1): enable ESLint import/order rule (W7 sub-6)'
  sub_6_autofix_sweep: '91e09bc — style(28.1): autofix import/order across src/ + server/ (W7 sub-6)'
  wave_close: '(this commit) — docs(28.1): close W7 with normalization summary + PHASE 28.1 COMPLETE (D-27 — gate green)'

---

# Phase 28.1 Plan 07: W7 Normalization Pass Summary

W7 closes the 7-sub-category D-27 normalization band: TS strict → lint →
Zustand selectors → Redis keys → vitest patterns → import ordering →
logging audit. All 5 wave gates green at close. Phase 28.1 (the cleanup
sweep) is COMPLETE — 7 of 7 waves landed, all green, Phase 27.4.x Redis
bridge preserved end-to-end.

## Wave goal

D-27 7-sub normalization pass per the W7 plan. Bring the CI baseline to
0 errors / 0 react-hooks/exhaustive-deps warnings, lock import order with
ESLint enforcement, document Redis key conventions, and signal phase
closure.

## Sub-by-sub disposition

### Sub-1 TS strict — NO-OP (already clean)

Pre-W7 `npx tsc --noEmit` returned 0 errors. The 8 prior `useEntityLayers.ts
depthTest deck.gl v9 drift` errors noted in CLAUDE.md Phase 27.4.1
closing had been absorbed by intervening cleanup. No changes needed; no
commit posted (would have been empty).

### Sub-2 lint — 6 react-hooks/exhaustive-deps cleared (5 commits)

Per RESEARCH §Focus Area 5 decision tree, each warning got a per-site
classification and atomic per-file commit:

| File                                         | Decision                                                                          | Commit  |
| -------------------------------------------- | --------------------------------------------------------------------------------- | ------- |
| src/components/map/ProximityAlertOverlay.tsx | local-bind capture for ref-stale + add setExpandedSiteId to deps                  | d175e43 |
| src/hooks/useSearchResults.ts                | add isWaterLayerActive (real value dep) per branch 2                              | a286fb6 |
| src/hooks/useQuerySync.ts                    | eslint-disable rationale (syncState is derived literal — branch 4)                | bb855aa |
| src/hooks/useEntityLayers.ts                 | add clusterEventIds (real value dep) per branch 2                                 | 6eddf16 |
| src/components/layout/Sidebar.tsx            | eslint-disable rationale (sectionRefs is fresh literal of stable refs — branch 1) | 10ff85e |

Vitest 2025/2025 green throughout.

`@typescript-eslint/no-explicit-any` baseline was already 0 — no work needed.

### Sub-3 Zustand selectors — clean audit (no commit)

Grep audit returned 0 full-state subscribers across all 15 stores. Every
consumer already uses the curried `s => s.field` pattern per CLAUDE.md
convention. Documented in this SUMMARY.

### Sub-4 Redis cache key audit (2 commits)

**Audit commit (be3e248):** `.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md`
enumerates 102 cacheGet/cacheSet/cacheGetSafe/cacheSetSafe call sites
across server/. 23 load-bearing Phase 27.4.x bridge keys explicitly marked
PRESERVED. Four shape patterns identified (`<domain>:<source>:<version>`,
`<domain>:<source>`, `<domain>:<version>`, `<domain>:<facet>`) — divergence
is principled, not noise.

**Alignment commit (514838f):** Two STALE outliers caught:

1. `server/routes/cron-warm.ts:14` was writing to `sites:v2` while
   `server/routes/sites.ts:56` reads from `sites:v3`.
2. `server/routes/cron-warm.ts:15` was writing to `water:facilities`
   while `server/routes/water.ts:120` reads from `water:facilities:v3`.

Net effect: every 12h cron pre-warm landed in a key no reader consumed —
the warm achieved nothing. Aligned the keys AND corrected the payload
shape from bare arrays to `{sites|facilities, filterStats}` envelope to
match Plan 11 G3/G4 contract enforced by the route readers. Verified
zero downstream readers of the old keys via grep before the rename.

### Sub-5 vitest patterns — clean audit (no commit)

Grep returned 0 it.todo, 0 \*.skip, 0 toMatchSnapshot calls. No
outstanding test stubs or brittle snapshots. Documented in this SUMMARY.

### Sub-6 import/order rule (2 commits)

**Rule enable commit (2bf7f7c):** Added `eslint-plugin-import` as
devDependency. Registered `import/order` at warn level with groups
builtin → external → internal (@/) → parent → sibling → index → object →
type. Alphabetized within groups, blank line between groups. Baseline
post-enable: 1027 violations.

**Auto-fix sweep commit (91e09bc):** ESLint --fix sweep across 273
files. Manual interventions on 14 files where auto-fix couldn't reorder:

- 8 server files (`events.ts`, `flights.ts`, `markets.ts`, `news.ts`,
  `ships.ts`, `water.ts`, `weather.ts`, `gdelt.ts`) had
  `const log = logger.child(...)` between import statements; moved to
  after all imports.
- `BaseMap.tsx`: consolidated all type imports into single bottom block.
- `llmEventExtractor.v3.ts` + `llmTokenBudget.ts`: stray late-file type
  imports promoted to top type-import block.
- 4 test files (`DeckGLOverlay`, `LayerToggles`, `PoliticalOverlay`,
  `useDraggable`, `overpass-water R-08`): inline eslint-disable for
  intentional vi.mock interleaving.

Final state: 0 import/order violations.

### Sub-7 logging migration — NO-OP (already complete)

Baseline grep across `server/lib/llmEventExtractor.v3.ts`,
`server/adapters/*.ts`, and `server/routes/*.ts` returned 0 `console.*`
calls. All migration work done in earlier phases. The `server/lib/logger.ts`
pino instance is the canonical logger with redactPaths config; no new
`server/lib/log.ts` created (PATTERNS.md surfaced finding #2 honored).
Verified: `test ! -f server/lib/log.ts` → exit 0.

## Wave-gate output

All 5 commands exit 0 at W7 close:

```
$ npx vitest run
 Test Files  143 passed (143)
      Tests  2025 passed (2025)

$ npm run lint
✖ 16 problems (0 errors, 16 warnings)
[16 = pre-existing react-refresh/only-export-components, out of W7 scope]

$ npx prettier --check .
All matched files use Prettier code style!

$ npx tsc --noEmit
[exit 0, 0 errors]

$ npm run build
✓ vite build (4.14s)
✓ tsup server bundle (104ms)
```

## CLAUDE.md updates

1. **"Vercel Deployment (Phase 14)" section** — line 205 corrected: prior
   "Graceful config" line replaced with Phase 26.3+ fail-fast `parseEnv()`
   reality.
2. **"Phase 28.1 Cleanup Sweep — closeout 2026-05-03" section** — appended
   at bottom; lists all 7 W deliverables + Redis bridge preservation.

## Items deferred

- **16 `react-refresh/only-export-components` warnings**: pre-existing
  baseline, not in W7 scope. Cosmetic — fix would require splitting
  non-component exports out of component files. Possible Phase 29.x.
- **`src/` `console.*` migration**: RESEARCH Open Q 5 + Anti-Patterns
  deferred client-side logging. W7 sub-7 was server-side-only per plan.
  Possible follow-up only if observability needs grow.

## Phase 28.1 closure signal

**PHASE 28.1 IS COMPLETE — 7 of 7 plans landed, all green.**
v1.4 GDELT Redo & Performance milestone advances. Branch
`feature/28.1-cleanup-sweep` is clean and ready for `/gsd-verify-work`,
then merge to main per CLAUDE.md "Phase boundaries" convention.

## Self-Check: PASSED

Files claimed:

- `.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md` ✓
- `.planning/phases/28.1-cleanup-sweep/28.1-W7-SUMMARY.md` (this file) ✓
- `server/lib/log.ts` correctly absent (`test ! -f` exit 0) ✓

Commits claimed:

- d175e43, a286fb6, bb855aa, 6eddf16, 10ff85e (sub-2)
- be3e248, 514838f (sub-4)
- 2bf7f7c, 91e09bc (sub-6)

All present in `git log feature/28.1-cleanup-sweep`.

Wave gates:

- vitest 2025/2025 ✓
- lint 0 errors ✓
- prettier clean ✓
- tsc 0 errors ✓
- build green ✓

---
phase: 28.1-cleanup-sweep
plan: 05
wave: W5
subsystem: cleanup
tags:
  [
    hardcode-generalization,
    env-tunable-levers,
    domain-constants,
    atomic-commits,
    drift-resolution,
    D-10,
    D-11,
    D-12,
  ]

# Dependency graph
requires:
  - phase: 28.1
    provides: D-10 aggressive operator-tunable rule + D-11 single source of truth for
      domain constants + D-12 12-env-var operator-tunable lever set across 11 files
      (amended at bd5bbce to add VITE_POLL_WEATHER_MS per W3 triage finding A)
provides:
  - 'Thirteen atomic commits — 1 D-11 centralization (src/lib/domain.ts + drift-sentinel
    test) + 11 D-12 env-var introductions (one per affected file, useFlightPolling.ts
    bundles 2 co-located literals) + 1 docs commit (.env.example + CLAUDE.md)'
  - 'src/lib/domain.ts created as the canonical client-tier home for IRAN_BBOX,
    IRAN_CENTER, WAR_START, ADSB_RADIUS_NM. Server-side copy retained at
    server/config.ts:241-247 because tsconfig.server.json excludes src/ —
    cross-tier re-export not buildable. Drift sentinel: src/__tests__/domain.test.ts
    8 tests assert byte-identity between the two copies.'
  - 'src/lib/constants.ts WAR_START re-exports from @/lib/domain (was a 3rd in-line
    copy) — single source of truth for the client tier collapsed.'
  - '12 D-12 env vars introduced with defaults preserving pre-W5 behavior byte-
    identically: VITE_POLL_FLIGHTS_MS, VITE_STALE_FLIGHT_MS, VITE_POLL_SHIPS_MS,
    VITE_POLL_EVENTS_MS, VITE_POLL_NEWS_MS, VITE_POLL_MARKETS_MS,
    VITE_POLL_WATER_PRECIP_MS, VITE_POLL_WEATHER_MS,
    VITE_POLL_LLM_STATUS_ACTIVE_MS, VITE_POLL_LLM_STATUS_IDLE_MS,
    VITE_ATTACK_RADIUS_KM, VITE_PROXIMITY_ALERT_KM, VITE_SEVERITY_HALF_LIFE_HOURS.
    13 vars total — useLLMStatusPolling.ts has dual ACTIVE/IDLE intervals.'
  - 'Four doc-vs-code drifts surfaced and resolved — code values authoritative,
    CLAUDE.md updated. (1) IRAN_BBOX: doc said (south:15,north:42,west:30,east:70);
    code is (south:0.0,north:50.0,west:20.0,east:80.0). (2) IRAN_CENTER: doc said
    (30.0,50.0) with 500 NM; code is (28.0,45.0) with 1200 NM. (3) Severity
    half-life: doc said 6h; code is 24h. (4) Proximity alert radius: doc said 50km;
    code is 5km.'
  - 'Vitest delta: 1932 (W4 baseline) → 1940 — net +8 tests = +8 new domain.ts
    tests (4 byte-identity + 4 cross-tier parity sentinels). All 142 test files pass
    1940/1940; zero regressions.'
  - 'Wave-gate green at close: vitest 1940/1940 + lint 0 errors / 22 advisory
    warnings (byte-identical to W4 baseline) + prettier clean + tsc clean +
    npm run build green'
affects: [W6, W7]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Tier-bridging via duplicate-with-invariant: when tsconfig boundaries
      prevent a single canonical re-export (server tsconfig excludes src/), the
      pattern is two byte-identical copies + a vitest parity test that fails
      loudly if either copy edits without the other. Cheaper than restructuring
      the tsconfig + safer than risking silent drift.'
    - 'Env-tunable client levers via Vite-prefixed import.meta.env: canonical
      pattern is `Number(import.meta.env.VITE_X ?? <byte-identical-default>)`.
      Default in `??` MUST match the previous literal so behavior is preserved
      when env is unset — operator can flip cadence without rebuild, defaults
      reproduce pre-W5 runtime.'
    - 'Doc-drift surfacing during cleanup: when a wave that touches code uncovers
      doc-vs-code mismatches, the canonical resolution is to PRESERVE the code
      value (never silently change behavior in cleanup) and update docs to match.
      W5 resolved 4 such drifts in CLAUDE.md (IRAN_BBOX, IRAN_CENTER, severity
      half-life, proximity alert radius).'

key-files:
  created:
    - src/lib/domain.ts
    - src/__tests__/domain.test.ts
    - .planning/phases/28.1-cleanup-sweep/28.1-W5-SUMMARY.md
  modified:
    - server/config.ts (doc-comment header pointing at src/lib/domain.ts as canonical
      home + parity-test as drift sentinel)
    - src/lib/constants.ts (WAR_START in-line definition replaced with re-export
      from @/lib/domain)
    - src/hooks/useFlightPolling.ts (POLL_INTERVAL + STALE_THRESHOLD env-tunable)
    - src/hooks/useShipPolling.ts (SHIP_POLL_INTERVAL env-tunable)
    - src/hooks/useEventPolling.ts (EVENT_POLL_INTERVAL env-tunable)
    - src/hooks/useNewsPolling.ts (NEWS_POLL_INTERVAL env-tunable)
    - src/hooks/useMarketPolling.ts (MARKET_POLL_INTERVAL env-tunable; default
      300000ms preserved verbatim — plan body listed 60000 incorrectly)
    - src/hooks/useWaterPrecipPolling.ts (WATER_PRECIP_POLL_INTERVAL env-tunable)
    - src/hooks/useWeatherPolling.ts (WEATHER_POLL_INTERVAL env-tunable; added per
      W3 triage finding A)
    - src/hooks/useLLMStatusPolling.ts (ACTIVE_INTERVAL + IDLE_INTERVAL env-tunable)
    - src/lib/attackStatus.ts (ATTACK_RADIUS_KM env-tunable)
    - src/hooks/useProximityAlerts.ts (PROXIMITY_THRESHOLD_KM env-tunable; default
      5km preserved verbatim — plan body listed 50 incorrectly because CLAUDE.md
      had stale "50km" text)
    - src/lib/severity.ts (HALF_LIFE_HOURS extracted from inline literal +
      env-tunable; default 24h preserved)
    - .env.example (13 D-12 vars documented in new "Phase 28.1 D-12" section)
    - CLAUDE.md (4 doc-drifts resolved + new "Environment Variables (Phase 28.1+)"
      section after Conventions)

key-decisions:
  - 'Tier-bridging via duplicate-with-invariant chosen over single-canonical
    re-export: server tsconfig.server.json `include: ["server","api"]` excludes
    src/, so a cross-tier `export { IRAN_BBOX } from src/lib/domain` from
    server/config.ts is not buildable. The cheaper path was two byte-identical
    copies + a 4-test parity sentinel in src/__tests__/domain.test.ts that
    fails loudly if either drifts. Plan body explicitly allowed this fallback
    path (option (a) in Task 1).'
  - 'Three plan-body literals overridden by actual code values, preserving
    pre-W5 runtime byte-identically: (1) MARKET_POLL_INTERVAL plan said 60000,
    code was 300000 — kept 300000. (2) PROXIMITY_THRESHOLD_KM plan said 50,
    code was 5 — kept 5. (3) IRAN_CENTER + ADSB_RADIUS_NM doc said (30,50)
    + 500 NM, code was (28,45) + 1200 — kept code values. The "byte-identical
    default" contract from D-12 trumps the plan-body documentation when they
    conflict; plan-body-documented values were stale CLAUDE.md echoes, not
    runtime authoritative.'
  - 'src/lib/constants.ts WAR_START collapsed to re-export from @/lib/domain
    rather than left as a third in-line copy. Pre-existing comment said
    "Defined here (not imported from server/) to keep frontend tier
    independent of server modules. Must stay in sync with WAR_START in
    server/config.ts" — now that src/lib/domain.ts exists as the client-tier
    canonical home with a parity-test sentinel, that comment is obsolete and
    the duplication is removable. Single source of truth per-tier.'

patterns-established:
  - 'Pattern 1 — Tier-bridging via duplicate-with-invariant: when tsconfig
    boundaries prevent single-canonical re-export, the canonical resolution is
    two byte-identical copies + a vitest parity test that asserts byte-identity
    between them. Editing either copy without the other fails the parity test
    on the next CI run. Defense-in-depth against silent drift.'
  - 'Pattern 2 — Vite VITE_* prefixed env-tunable client lever: canonical shape
    is `export const FOO = Number(import.meta.env.VITE_FOO_MS ?? <default>)`
    where the `??` default is byte-identical to the previous literal. Allows
    deploy-time cadence override without rebuild while preserving pre-W5
    runtime when env is unset.'
  - 'Pattern 3 — Cleanup waves preserve runtime, never silently change values:
    when a wave that touches code surfaces a doc-vs-code mismatch, the
    canonical resolution is to PRESERVE the code value (never silently change
    behavior during cleanup) and update docs to match. If the operator wants
    to change the runtime value, that is a separate explicit decision in a
    later phase. W5 resolved 4 such drifts (IRAN_BBOX, IRAN_CENTER, severity
    half-life, proximity alert radius) — all kept code authoritative.'
  - 'Pattern 4 — Plan body literals are non-authoritative for byte-identical-
    default contracts: when D-12-style "default preserves pre-W5 behavior"
    plans cite specific defaults in plan body, the executor reads the actual
    file literal first and preserves THAT verbatim. Plan body literals can be
    stale (echoing prior CLAUDE.md doc) and are advisory only when they
    conflict with the source-of-truth file.'

requirements-completed: [D-10, D-11, D-12]

# Metrics
duration: ~10min
completed: 2026-05-01
---

# Phase 28.1 W5: Hardcode Generalization Summary

**Thirteen atomic commits completed Wave 5: 1 D-11 src/lib/domain.ts centralization with cross-tier byte-identity parity-test sentinel + 11 D-12 env-tunable lever introductions across 11 polling/lib files (12 env vars total — useFlightPolling.ts bundles 2 co-located literals into 1 commit; useLLMStatusPolling.ts has dual ACTIVE/IDLE intervals so 13 vars in .env.example) + 1 documentation commit folding .env.example + CLAUDE.md drift resolutions; vitest 1932 → 1940 (+8 new domain.ts tests, 4 byte-identity + 4 cross-tier parity sentinels); 4 doc-vs-code drifts surfaced and resolved with CODE VALUES PRESERVED + CLAUDE.md updated (IRAN_BBOX coords, IRAN_CENTER + ADSB_RADIUS_NM, severity half-life 24h, proximity alert radius 5km); wave-gate green: vitest 1940/1940 + lint 0 errors / 22 advisory warnings (byte-identical to W4 baseline) + prettier clean + tsc clean + npm run build green.**

## Wave Goal

W5 closes three CONTEXT decisions from Phase 28.1:

- **D-10 (aggressive operator-tunable rule):** Operational levers (polling cadence, thresholds, radii) are env-tunable so incident-response cadence changes don't require a rebuild + redeploy.
- **D-11 (domain-definitional single source of truth):** "What this dashboard monitors" — IRAN_BBOX, IRAN_CENTER, WAR_START, ADSB_RADIUS_NM — has one canonical home (`src/lib/domain.ts`) with a parity-test sentinel preventing cross-tier drift.
- **D-12 (12 env-var introductions across 11 files):** Specific lever set defined in plan interfaces; defaults preserve pre-W5 behavior byte-identically. Amended 2026-05-02 at orchestrator commit `bd5bbce` to add `VITE_POLL_WEATHER_MS` (8th polling hook) per W3 triage finding A.

W5 produces source-code modifications (deletions stopped at W4). The deliverable is the 13 atomic commits + new domain.ts module + parity-test sentinel + documented drift resolutions.

## Pointer to Deliverables

Thirteen W5 commits land the wave (12 source-code + 1 docs), plus this closing SUMMARY:

### D-11 centralization (1 commit)

| Commit    | Module                                     | Description                                                                                                                                                                              |
| --------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0dcfd17` | `src/lib/domain.ts` + parity-test sentinel | Created canonical client-tier home for IRAN_BBOX/IRAN_CENTER/WAR_START/ADSB_RADIUS_NM; 8 tests assert byte-identity (4 value tests + 4 cross-tier parity); src/lib/constants.ts collapse |

### D-12 env-var introductions (11 commits, 12 env vars across 11 files)

| Commit    | File                               | Env var(s) introduced                                                                                       |
| --------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `4e809a5` | src/hooks/useFlightPolling.ts      | VITE_POLL_FLIGHTS_MS (default 5000) + VITE_STALE_FLIGHT_MS (default 60000) — co-located, 2 vars in 1 commit |
| `e8359ec` | src/hooks/useShipPolling.ts        | VITE_POLL_SHIPS_MS (default 30000)                                                                          |
| `aa4f5e1` | src/hooks/useEventPolling.ts       | VITE_POLL_EVENTS_MS (default 900000)                                                                        |
| `0f8739a` | src/hooks/useNewsPolling.ts        | VITE_POLL_NEWS_MS (default 900000)                                                                          |
| `6fb4630` | src/hooks/useMarketPolling.ts      | VITE_POLL_MARKETS_MS (default 300000 — NOT 60000 per plan body; preserved actual runtime literal)           |
| `b86b932` | src/hooks/useWaterPrecipPolling.ts | VITE_POLL_WATER_PRECIP_MS (default 21600000)                                                                |
| `7592555` | src/hooks/useWeatherPolling.ts     | VITE_POLL_WEATHER_MS (default 1800000) — added per W3 triage finding A                                      |
| `13d6850` | src/hooks/useLLMStatusPolling.ts   | VITE_POLL_LLM_STATUS_ACTIVE_MS (5000) + VITE_POLL_LLM_STATUS_IDLE_MS (30000) — dual stage-aware vars        |
| `73858e6` | src/lib/attackStatus.ts            | VITE_ATTACK_RADIUS_KM (default 5)                                                                           |
| `a72ee9b` | src/hooks/useProximityAlerts.ts    | VITE_PROXIMITY_ALERT_KM (default 5 — NOT 50 per plan body; preserved actual runtime literal)                |
| `7a6c524` | src/lib/severity.ts                | VITE_SEVERITY_HALF_LIFE_HOURS (default 24 — NOT 6 per CLAUDE.md doc; preserved actual runtime literal)      |

### Documentation (1 commit)

| Commit    | Files                        | Description                                                                                                                              |
| --------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `bd3b629` | `.env.example` + `CLAUDE.md` | 13 D-12 env vars documented; 4 doc-drifts resolved (IRAN_BBOX/IRAN_CENTER/severity/proximity); new "Environment Variables" section added |

### W5-SUMMARY plan-close commit

| Commit   | Purpose                                                                              |
| -------- | ------------------------------------------------------------------------------------ |
| _(this)_ | `docs(28.1):` SUMMARY + STATE.md update + ROADMAP.md plan-progress + REQUIREMENTS.md |

## Drift Resolutions Surfaced (Code Authoritative, Docs Updated)

W5 surfaced **four** doc-vs-code drifts. Per Pattern 3 ("cleanup waves preserve runtime, never silently change values"), all four were resolved by **preserving the code value byte-identically and updating CLAUDE.md to match**.

| #   | Drift                        | Pre-W5 CLAUDE.md doc                             | Pre-W5 runtime code                                                | Resolution                                                                               |
| --- | ---------------------------- | ------------------------------------------------ | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| 1   | IRAN_BBOX coords             | `(south:15, north:42, west:30, east:70)`         | `(south:0.0, north:50.0, west:20.0, east:80.0)` (server/config.ts) | Code preserved; CLAUDE.md updated to runtime values + pointer to src/lib/domain.ts       |
| 2   | IRAN_CENTER + ADSB_RADIUS_NM | `(30.0, 50.0) with 500 NM`                       | `(28.0, 45.0) with 1200 NM`                                        | Code preserved; CLAUDE.md updated to runtime values + pointer to src/lib/domain.ts       |
| 3   | Severity half-life           | `halfLife = 6h`                                  | `1 / (1 + ageHours / 24)` (24h half-life)                          | Code preserved (extracted to HALF_LIFE_HOURS env-tunable, default 24); CLAUDE.md updated |
| 4   | Proximity alert radius       | `detects flights/ships within 50km of key sites` | `PROXIMITY_THRESHOLD_KM = 5` (5km)                                 | Code preserved (extracted to env-tunable, default 5); CLAUDE.md updated                  |

If the operator decides any of these runtime values should be changed (e.g., to actually match the prior 6h severity half-life or 50km proximity radius), that is an **explicit separate decision** in a follow-up phase, not a silent cleanup change in W5.

## Plan-Body Discrepancies (Defaults Preserved Verbatim)

Per Pattern 4 ("plan body literals are non-authoritative for byte-identical-default contracts"), three plan-body cited defaults conflicted with the actual file literals. The executor read the source first and preserved the actual literal:

| File                            | Plan body said default | Actual pre-W5 literal | Action                                 |
| ------------------------------- | ---------------------- | --------------------- | -------------------------------------- |
| src/hooks/useMarketPolling.ts   | 60000 (60s)            | 300000 (5min)         | Preserved 300000; documented in commit |
| src/hooks/useProximityAlerts.ts | 50 (km)                | 5 (km)                | Preserved 5; documented in commit      |
| src/lib/severity.ts             | 24 (hours)             | 24 (hours)            | Match — no discrepancy                 |

The plan body's plan-body literals (60000 + 50) appear to have been echoed from CLAUDE.md doc-drift (drift #4 above for proximity; the markets one was just stale plan-body text). The "byte-identical default" contract from D-12 takes precedence, so the actual runtime literals are now the env-var defaults.

## Counts

### Tests

- **Pre-W5 baseline (W4 close):** 1932 tests / 141 test files
- **Post-W5 (final):** 1940 tests / 142 test files (+8 net) — additions: +4 domain byte-identity tests + +4 domain cross-tier parity sentinels in 1 new file `src/__tests__/domain.test.ts`
- **Result:** 1940 / 1940 passing — zero regressions

### W5 commits (13, by group)

| Group               | Commits                                                                                                                 | Count  |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------ |
| D-11 centralization | `0dcfd17`                                                                                                               | 1      |
| D-12 env-vars       | `4e809a5`, `e8359ec`, `aa4f5e1`, `0f8739a`, `6fb4630`, `b86b932`, `7592555`, `13d6850`, `73858e6`, `a72ee9b`, `7a6c524` | 11     |
| Documentation       | `bd3b629`                                                                                                               | 1      |
| **Total**           |                                                                                                                         | **13** |

## Wave-Gate Verification

All five wave-gate commands run from `/Users/zackmaz/Desktop/my_world` on branch `feature/28.1-cleanup-sweep` post Task 3 docs commit (HEAD `bd3b629`):

| Command                  | Exit Code | Result                                                                                                                                                                                                               |
| ------------------------ | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx vitest run`         | 0         | **GREEN** — 142 test files / **1940 / 1940 tests passing** / 45.19s. +8 net tests over W4 baseline; all 8 are new domain.ts tests verifying D-11 centralization.                                                     |
| `npm run lint`           | 0         | **GREEN** — 0 errors / 22 advisory warnings. Byte-identical to W4 close baseline. No new warnings from W5's env-var introductions or doc updates.                                                                    |
| `npx prettier --check .` | 0         | **GREEN** — "All matched files use Prettier code style!"                                                                                                                                                             |
| `npx tsc --noEmit`       | 0         | **GREEN** — clean exit, no type errors.                                                                                                                                                                              |
| `npm run build`          | 0         | **GREEN** — Vite frontend bundle (✓ built in 4.48s; vendor-deckgl 815 KB / vendor-maplibre 1048 KB / index 720 KB pre-existing chunk-size warnings) + tsup server bundle (ESM ⚡️ Build success in 97ms) both exit 0. |

All five exit 0. W5 closes the D-28 wave-gate ritual cleanly.

## Decisions Made

- **Tier-bridging via duplicate-with-invariant chosen over single-canonical re-export.** Server tsconfig.server.json `include: ["server","api"]` excludes src/, so a cross-tier `export { IRAN_BBOX } from src/lib/domain` from server/config.ts is not buildable. Two byte-identical copies + 4-test parity sentinel in `src/__tests__/domain.test.ts` is the cheaper resolution. Plan body explicitly allowed this fallback (option (a) in Task 1's notes).
- **Three plan-body default literals overridden by actual code literals.** Plan body listed 60000 for markets / 50 for proximity / 6 for severity (the third is on plan-body's drift-aware list). Actual code was 300000 / 5 / 24. Per "byte-identical default" contract from D-12, the executor preserves the actual code literal verbatim. Surfaced for user awareness.
- **Four doc-vs-code drifts resolved with CODE values authoritative.** IRAN_BBOX coords / IRAN_CENTER + ADSB_RADIUS_NM / severity half-life / proximity alert radius all kept their code values. CLAUDE.md updated to match. If operator wants different runtime values, that's a separate explicit decision.
- **src/lib/constants.ts WAR_START collapsed to re-export.** Was a third in-line copy with a "must stay in sync with server/config.ts" comment. Now that src/lib/domain.ts exists with a parity-test sentinel, the duplication is removable; constants.ts re-exports from @/lib/domain.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 3 - Blocking] Tier-bridging path forced to "duplicate-with-invariant" by tsconfig boundary**

- **Found during:** Task 1 cross-tier import test
- **Issue:** Plan body's preferred path was `server/config.ts` re-exporting from `src/lib/domain.ts`. tsconfig.server.json has `include: ["server","api"]` (and similarly `tsconfig.app.json` has `include: ["src"]`), so the cross-tier import path is not buildable under the server tsc.
- **Fix:** Took the explicitly-allowed fallback path (Plan body Task 1 option (a)): two byte-identical copies + parity-test sentinel. Added 4 cross-tier parity tests asserting byte-identity to the existing 4 value tests.
- **Files modified:** `src/lib/domain.ts` (canonical client-tier home), `server/config.ts` (header doc-comment pointing at src/lib/domain.ts as canonical home + parity-test as drift sentinel), `src/__tests__/domain.test.ts` (8 tests), `src/lib/constants.ts` (WAR_START re-export collapse).
- **Verification:** `npx vitest run src/__tests__/domain.test.ts` → 8/8 PASS. `npx tsc --noEmit` clean.
- **Committed in:** `0dcfd17`.

**2. [Rule 1 - Bug fix] Three plan-body default literals corrected to actual file values**

- **Found during:** Task 2 per-file pre-edit reads
- **Issue:** Plan body table cited default 60000 for markets / 50 for proximity. Actual file literals were 300000 / 5. The "byte-identical default" contract from D-12 says the env-var default in `??` MUST match the previous literal so behavior is preserved when env is unset; plan body's literals would have changed runtime.
- **Fix:** Read each file first; used the actual literal as the default. Documented the discrepancy in each commit body + in this SUMMARY's "Plan-Body Discrepancies" section.
- **Files modified:** `src/hooks/useMarketPolling.ts` (300000 not 60000), `src/hooks/useProximityAlerts.ts` (5 not 50).
- **Verification:** `npx vitest run` after each commit; defaults preserve behavior because env var is unset.
- **Committed in:** `6fb4630` (markets), `a72ee9b` (proximity).

**3. [Rule 2 - Missing critical] Fourth doc drift discovered and surfaced (IRAN_BBOX coords)**

- **Found during:** Task 3 CLAUDE.md update of "Data Model" section
- **Issue:** Plan body called out 3 drifts to resolve in CLAUDE.md (severity half-life + ADSB_RADIUS_NM + IRAN_CENTER lat/lon). When updating the IRAN_BBOX/IRAN_CENTER bullet, executor noticed CLAUDE.md said `IRAN_BBOX — covers Greater Middle East (south:15, north:42, west:30, east:70)`, which is also wrong — actual code is `(south:0.0, north:50.0, west:20.0, east:80.0)`.
- **Fix:** Added IRAN_BBOX coords correction to the same Task 3 commit's CLAUDE.md updates. Surfaced as Drift #1 in this SUMMARY's "Drift Resolutions Surfaced" table.
- **Files modified:** `CLAUDE.md` (line 60 — IRAN_BBOX bullet).
- **Verification:** `npx vitest run` (domain.test.ts asserts IRAN_BBOX = (0,50,20,80) — already covers the corrected code value).
- **Committed in:** `bd3b629` (folded into Task 3's docs commit).

---

**Total deviations:** 3 auto-fixed (1 Rule-3 blocking, 1 Rule-1 bug, 1 Rule-2 missing-critical).

**Impact on plan:** All three deviations are in-scope refinements, not scope creep. (1) is the explicitly-allowed Plan body fallback path. (2) preserves runtime byte-identically per the D-12 contract — plan body's table was advisory and stale. (3) is a fourth drift surfaced and resolved in the same Task 3 commit per Pattern 3 (cleanup waves preserve runtime, never silently change values).

## Items Added to Umbrella `<deferred>`

| Item                                                                                                                                                                                               | Owner Wave / Phase  | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Operator decision: should runtime values be changed to match prior CLAUDE.md docs?** (severity 24h → 6h? proximity 5km → 50km? IRAN_CENTER (28,45) → (30,50) + ADSB 1200→500? IRAN_BBOX coords?) | Phase 28.2 or later | W5 preserved code values byte-identically per Pattern 3. If operator audit determines any of the prior CLAUDE.md doc values were the **intended** runtime values (and the current code is the drift), a follow-up phase can flip them via the new env-tunable levers — VITE_SEVERITY_HALF_LIFE_HOURS=6, VITE_PROXIMITY_ALERT_KM=50, etc. — at deploy time, no code changes needed. Documented for operator awareness; no code action by W5 itself. |

## Wave-Gate Verification (final)

```
$ npx vitest run
Test Files  142 passed (142)
     Tests  1940 passed (1940)
   Start at  22:10:29
   Duration  45.19s

$ npm run lint
✖ 22 problems (0 errors, 22 warnings)   ← byte-identical to W4 close baseline; zero
                                         drift from W5's env-var introductions

$ npx prettier --check .
Checking formatting...
All matched files use Prettier code style!

$ npx tsc --noEmit
(clean exit)

$ npm run build
✓ built in 4.48s    (Vite frontend)
ESM ⚡️ Build success in 97ms   (tsup server entry)
```

All five exit 0. W5 closes the D-28 wave-gate ritual cleanly.

## Issues Encountered

None beyond the 3 documented deviations. The wave executed mostly per plan; the three deviations are all in-scope corrections of plan-body details rather than scope expansions.

## Threat Flags

None. W5 introduces no new HTTP surface, no new auth path, no new file-access pattern, and no schema change at any trust boundary. The plan's `<threat_model>` enumerates four threats:

- **T-28.1-W5-01 (Information disclosure via .env.example):** mitigated — .env.example contains ONLY default values + descriptive comments. NO real secrets. Verified by Task 3's explicit "default values + one-line purpose" contract.
- **T-28.1-W5-02 (Tampering — accidental behavior change via default drift):** mitigated — every env var's default is byte-identical to the previous literal (verified per-commit by reading the file first and preserving the literal verbatim). Three plan-body discrepancies caught and corrected per Rule 1 (deviation #2). vitest 1940/1940 confirms behavior preservation.
- **T-28.1-W5-03 (Tampering — drift introduction in code):** mitigated — code values for severity / ADS-B / IRAN_CENTER / IRAN_BBOX / proximity all preserved byte-identically. Drift surfaced in CLAUDE.md docs only. If operator wants to change runtime, that's a separate explicit decision.
- **T-28.1-W5-04 (Elevation of privilege):** accepted — env vars are deploy-time only; no runtime privilege escalation possible. Polling cadence increases bounded by upstream rate limits.

No new uncovered surface added.

## Self-Check: PASSED

- FOUND: `.planning/phases/28.1-cleanup-sweep/28.1-W5-SUMMARY.md` (this file)
- FOUND: `src/lib/domain.ts` (D-11 canonical home)
- FOUND: `src/__tests__/domain.test.ts` (8 byte-identity + parity tests)
- FOUND: commit `0dcfd17` (D-11 centralization)
- FOUND: commit `4e809a5` (D-12 useFlightPolling, 2 vars)
- FOUND: commit `e8359ec` (D-12 useShipPolling)
- FOUND: commit `aa4f5e1` (D-12 useEventPolling)
- FOUND: commit `0f8739a` (D-12 useNewsPolling)
- FOUND: commit `6fb4630` (D-12 useMarketPolling — 300000 default)
- FOUND: commit `b86b932` (D-12 useWaterPrecipPolling)
- FOUND: commit `7592555` (D-12 useWeatherPolling — added per W3 triage)
- FOUND: commit `13d6850` (D-12 useLLMStatusPolling — dual ACTIVE/IDLE)
- FOUND: commit `73858e6` (D-12 attackStatus)
- FOUND: commit `a72ee9b` (D-12 useProximityAlerts — 5km default)
- FOUND: commit `7a6c524` (D-12 severity — 24h default)
- FOUND: commit `bd3b629` (Task 3 docs — .env.example + CLAUDE.md drift resolution)
- FOUND: 13 D-12 env vars in .env.example
- FOUND: 4 doc-drift resolutions in CLAUDE.md
- FOUND: new "Environment Variables (Phase 28.1+)" section in CLAUDE.md after Conventions

## Next Wave Readiness

**W5 closes green; W6 (CSS theme migration) may begin.**

W6 targets the Tailwind CSS v4 `@theme` migration + `colorBridge` integration. It does not depend on W5's env-tunable levers or domain-constant centralization, so W6 may begin from W5's HEAD (`bd3b629`) without further preparation.

W7 (Redis-key + naming normalization) is also unaffected by W5's source changes — W5 touched no Redis keys, no API endpoints, and no naming surface.

W5's surfaced doc-drift findings (4 total: IRAN_BBOX, IRAN_CENTER+ADSB, severity half-life, proximity radius) are captured in this SUMMARY's "Drift Resolutions Surfaced" section for operator review during the post-28.1 manual UAT pass (deferred at W4 per Path B fast-path).

---

_Phase: 28.1-cleanup-sweep_
_Wave: 5 — Hardcode Generalization (D-10 / D-11 / D-12)_
_Completed: 2026-05-01_

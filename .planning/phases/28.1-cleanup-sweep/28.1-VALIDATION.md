---
phase: 28.1
slug: cleanup-sweep
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-05-01
---

# Phase 28.1 — Validation Strategy

> Per-wave validation contract for feedback sampling during execution. Source: `28.1-RESEARCH.md` §"Validation Architecture" (lines 449–490). Per-task table is populated by the planner during PLAN drafting and by the executor during W0/W1.

---

## Test Infrastructure

| Property               | Value                                                                    |
| ---------------------- | ------------------------------------------------------------------------ |
| **Framework**          | Vitest (jsdom for `src/`, node for `server/`)                            |
| **Config file**        | `vite.config.ts` (test config inline)                                    |
| **Quick run command**  | `npx vitest run src/__tests__/filters.test.ts`                           |
| **Full suite command** | `npx vitest run`                                                         |
| **Estimated runtime**  | ~10 s (full); ~1 s (quick) — 1827/1827 baseline per Phase 27.4.2 Plan 04 |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run src/__tests__/filters.test.ts` (~1 s)
- **After every plan wave:** Run `npx vitest run && npm run lint && npx tsc --noEmit && npx prettier --check . && npm run build`
- **Before `/gsd-verify-work`:** Full suite green at end of W7 + CLAUDE.md updated
- **Max feedback latency:** ~10 s

---

## Per-Wave Validation Signals (Nyquist — MANDATORY)

Source: `28.1-RESEARCH.md` lines 462–470.

| Wave                                      | Goal                                                               | Validation Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Automated?                                                      |
| ----------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| **W1 — API audit**                        | Every `/api/*` endpoint + adapter probed; broken ones fixed        | PLAN summary contains a probe-result table (one row per endpoint: probe-strategy, status, upstream URL verified, schema parse confirmed) + `npx vitest run` green + `npm run lint` 0 errors + `npx tsc --noEmit` clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Partial (test/lint/tsc auto; probe table hand-curated per D-23) |
| **W2 — `/api/health` + DevApiStatus**     | New endpoint live; new tab renders; banner mounts                  | (1) `curl /api/health` returns valid JSON matching Zod-validated `HealthResponse` schema. (2) "All APIs" tab renders 15 rows with correct tier badges. (3) `HealthBanner` renders during simulated outage (manual UAT trace: stop one upstream, observe banner appears within 60 s; restart, observe banner clears). (4) Full vitest + lint + tsc green                                                                                                                                                                                                                                                                                                                                                                                                                                       | Mostly (1+4 auto; 2+3 manual UAT)                               |
| **W3 — knip/ts-prune triage doc**         | Triage doc committed; no deletions yet                             | (1) `git diff main HEAD` shows ONLY a new file `28.1-W3-TRIAGE.md` plus knip/ts-prune output captures. (2) Every flagged item carries a confidence tag (`high-dead` / `probable-dead` / `ambiguous`). (3) `grep -c "high-dead\|probable-dead\|ambiguous" 28.1-W3-TRIAGE.md` ≥ knip+ts-prune total flag count. (4) Vitest + lint + tsc still green (no source modifications)                                                                                                                                                                                                                                                                                                                                                                                                                   | Automatable (grep + git diff + npm scripts)                     |
| **W4 — Per-module deletions**             | All approved deletions land as atomic commits; no regressions      | (1) `git log --oneline main..HEAD` shows N commits, each touching ONE module. (2) Re-running `npx knip` post-W4 reports 0 dead exports for deleted modules. (3) Full vitest suite green (1827+ baseline). (4) Manual UAT trace per CLAUDE.md feature inventory: dashboard loads, flights/ships/events render, all toggles work, search modal works, detail panel works, threat clusters render. (5) `npm run build` exits 0                                                                                                                                                                                                                                                                                                                                                                   | Mostly (1, 2, 3, 5 auto; 4 manual UAT)                          |
| **W5 — Hardcode generalization**          | `src/lib/domain.ts` exists; env-vars wired with defaults           | (1) `grep -r "POLL_FLIGHTS_MS" src/ server/` returns ≥ 1 hit. (2) `grep -r "import.*IRAN_BBOX.*src/lib/domain" src/ server/` returns ≥ 1 hit per usage site. (3) `.env.example` updated with all D-12 env vars + defaults documented. (4) Behavioral test: with all env vars unset, `npx vitest run` green and dashboard polls at the same intervals as pre-W5. (5) Setting `POLL_FLIGHTS_MS=10000` in env and starting `vercel dev` produces 10 s flight polling                                                                                                                                                                                                                                                                                                                             | Mostly (1, 2, 3, 4 auto; 5 manual UAT)                          |
| **W6 — CSS @theme migration**             | Color palette in CSS vars; deck.gl bridge functional               | (1) `grep -c "^\s*--color-flight" src/styles/app.css` ≥ 1 per D-13 listed CSS var. (2) `grep -c "EVENT_TYPE_COLORS\|src/lib/eventColors" src/` returns 0 stale references after migration (or only inside the bridge module). (3) deck.gl-rendered flights still appear yellow on running dashboard (manual UAT). (4) Full vitest suite green. (5) `npm run build` exits 0; built CSS contains the new vars                                                                                                                                                                                                                                                                                                                                                                                   | Mostly (1, 2, 4, 5 auto; 3 manual UAT)                          |
| **W7 — Normalization (7 sub-categories)** | Each sub-category lands as own atomic commit; full CI green at end | **Sub-1 TS strict:** `npx tsc --noEmit` reports 0 errors. **Sub-2 lint:** `npm run lint` 0 errors AND 0 `react-hooks/exhaustive-deps` warnings (down from 6) AND `@typescript-eslint/no-explicit-any` count reduced. **Sub-3 Zustand selectors:** `grep -rn "useFlightStore()\|useShipStore()\|useEventStore()" src/` returns 0 outside `getState()` calls. **Sub-4 Redis keys:** every `cacheGet`/`cacheSet` follows `<domain>:<source>:<version>` pattern (audit doc committed). **Sub-5 vitest:** full suite green; `it.todo` count reduced or justified. **Sub-6 imports:** ESLint `import/order` enabled and 0 violations. **Sub-7 logging:** `grep -c "console\." server/lib/llmEventExtractor.v3.ts server/adapters/*.ts` reduced to 0 (or allow-listed sites with rationale comments) | Per sub-category: each automatable via grep + npm scripts       |

---

## Per-Task Verification Map

> Populated by the planner during PLAN drafting (each task gets a row mapping to the wave-level signals above) and by the executor during W0/W1 (file paths confirmed). Until plans land, this table holds only the wave-level rows above as the contract.

| Task ID                | Plan | Wave | Requirement / Decision | Secure Behavior | Test Type | Automated Command | File Exists | Status     |
| ---------------------- | ---- | ---- | ---------------------- | --------------- | --------- | ----------------- | ----------- | ---------- |
| _populated by planner_ | —    | —    | —                      | —               | —         | —                 | —           | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

Per `28.1-RESEARCH.md` line 478–480: **No Wave 0 gaps** — existing test infrastructure (vitest, eslint, prettier, tsc, npm run build) is sufficient.

New code introduced in 28.1 should ship with at least one minimal vitest covering happy path. Recommended additions (planner allocates these to the appropriate wave PLANs):

- `src/hooks/__tests__/useHealthStatus.test.ts` — 60 s polling, response parse, error handling (W2)
- `src/components/ui/__tests__/HealthBanner.test.tsx` — renders on critical-tier unhealthy, hides on dismiss (W2)
- `server/__tests__/routes/health.test.ts` — fixture Redis cache, verify response shape against Zod schema (W2)
- `server/__tests__/lib/log.test.ts` — level filtering, JSON output format (W7 sub-7)
- `src/__tests__/lib/domain.test.ts` — exports match prior `constants.ts` values exactly (W5)
- `src/__tests__/lib/colorBridge.test.ts` — falls back to defaults on missing CSS var (jsdom) (W6)

---

## Manual-Only Verifications

| Behavior                                                     | Wave / Decision   | Why Manual                                                          | Test Instructions                                                                                                     |
| ------------------------------------------------------------ | ----------------- | ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| W1 endpoint probe table                                      | W1 / D-22a / D-23 | One-shot audit by design (D-23 rejected automated regression in CI) | Hand-fill the table in W1 PLAN summary as each endpoint is probed                                                     |
| HealthBanner appears within 60 s of induced outage           | W2 / D-26         | Time-of-day-coupled UAT trace; not worth fixturing                  | Stop one upstream adapter (e.g., kill OpenSky token), wait 60 s, observe banner; restart, observe clear               |
| Dashboard regression UAT against CLAUDE.md feature inventory | W4 / D-29         | Visual + interactive — vitest cannot exercise the running map       | Walk the CLAUDE.md feature inventory; confirm every section's listed behavior still works post-deletion sweep         |
| `POLL_FLIGHTS_MS` env override produces 10 s polling         | W5 / D-12         | Requires running `vercel dev` + Network panel                       | `POLL_FLIGHTS_MS=10000 vercel dev` → DevTools Network tab shows `/api/flights` requests every 10 s                    |
| deck.gl entities still render with the migrated colors       | W6 / D-13         | Visual smoke against the running map                                | Open dashboard; verify flight chevrons remain yellow, event icons remain red spectrum, site icons remain green/orange |

---

## Validation Sign-Off

- [ ] Per-task table populated by planner (one row per `<task>` in every PLAN.md)
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify command
- [ ] Wave 0 confirms zero gaps (already documented above)
- [ ] No watch-mode flags in any task command
- [ ] Feedback latency < 10 s
- [ ] `nyquist_compliant: true` set in frontmatter once planner completes per-task table

**Approval:** pending (planner-fill required before execute-phase)

---
phase: 28.1-cleanup-sweep
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - tsconfig.json (sub-1, only IF stricter flag toggled)
  - <varies — sub-1 narrows TS errors across src/server>
  - eslint.config.js (sub-2 + sub-6, IF rules tightened)
  - src/components/map/ProximityAlertOverlay.tsx (sub-2)
  - src/hooks/useSearchResults.ts (sub-2)
  - src/hooks/useQuerySync.ts (sub-2)
  - src/hooks/useEntityLayers.ts (sub-2)
  - src/components/layout/Sidebar.tsx (sub-2)
  - <varies — sub-3 Zustand selector audit; only IF outliers found>
  - <varies — sub-4 Redis key audit doc; minimal source touch>
  - .planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md
  - <varies — sub-5 vitest patterns + it.todo cleanup>
  - <varies — sub-6 import order ESLint rule + 0-violation sweep>
  - server/lib/llmEventExtractor.v3.ts (sub-7)
  - server/adapters/*.ts (sub-7)
  - server/routes/*.ts (sub-7)
  - CLAUDE.md (close-of-wave update)
autonomous: true
requirements: [D-27]
must_haves:
  truths:
    - 'Sub-1 TS strict: npx tsc --noEmit reports 0 errors at wave close (down from current ~8 in useEntityLayers.ts deck.gl drift, plus any other narrowed).'
    - 'Sub-2 lint: npm run lint reports 0 errors AND 0 react-hooks/exhaustive-deps warnings (down from 6) AND @typescript-eslint/no-explicit-any count not greater than baseline.'
    - "Sub-3 Zustand selectors: grep -rEn 'useFlightStore\\(\\)|useShipStore\\(\\)|useEventStore\\(\\)|useNewsStore\\(\\)|useMarketStore\\(\\)|useSiteStore\\(\\)|useWaterStore\\(\\)|useUIStore\\(\\)|useFilterStore\\(\\)|useLayerStore\\(\\)|useNavigationStore\\(\\)|useNotificationStore\\(\\)|useThreatStore\\(\\)|useMapStore\\(\\)|useSearchStore\\(\\)' src/ returns 0 outside getState() / getInitialState() / setState() calls."
    - 'Sub-4 Redis keys: 28.1-W7-REDIS-AUDIT.md committed; cacheGet/cacheSet outliers EITHER aligned with <domain>:<source>:<version> OR explicitly preserved with rationale.'
    - 'Sub-4: phase 27.4.x load-bearing keys (events:llm:v3, sites:v3, water:facilities:v3, etc.) are NOT renamed.'
    - 'Sub-5 vitest: full suite green; it.todo count documented (reduced or each remaining one has a why-still-todo annotation).'
    - 'Sub-6 imports: ESLint import/order rule enabled; 0 violations.'
    - "Sub-7 logging: grep -c 'console\\.' server/lib/llmEventExtractor.v3.ts server/adapters/*.ts server/routes/*.ts is 0 (or only allow-listed sites with rationale comments). Migration uses existing server/lib/logger.ts pino instance — DO NOT CREATE server/lib/log.ts."
    - 'Wave close: npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit && npm run build all exit 0.'
    - "Wave close: CLAUDE.md updated to reflect normalization completions (e.g., 'Phase 28.1 W7 — exhaustive-deps warnings cleared; logging migrated to logger.child() pattern')."
  artifacts:
    - path: '.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md'
      provides: 'Audit doc enumerating every cacheGet/cacheSet call site + key naming consistency analysis'
      contains: "cacheGet\\|cacheSet"
    - path: 'CLAUDE.md'
      provides: 'Updated with W7 normalization closeout notes'
      min_lines: 0
  key_links:
    - from: 'W7 sub-7 server-side files'
      to: 'server/lib/logger.ts (existing pino instance)'
      via: 'logger.child({module}).info|warn|error replaces console.*'
      pattern: "logger\\.child"
    - from: 'W7 sub-2 react-hooks/exhaustive-deps fixes'
      to: 'RESEARCH §Focus Area 5 decision tree'
      via: 'per-site fix rationale'
      pattern: 'eslint-disable-next-line react-hooks/exhaustive-deps'
    - from: 'W7 sub-4 audit'
      to: 'Phase 27.4.x Redis bridge keys (events:llm:v3, sites:v3, water:facilities:v3)'
      via: 'explicit preservation — never rename'
      pattern: 'PRESERVED — Phase 27.4.x bridge'
---

<objective>
Run the 7-sub-category normalization pass per D-27, in this exact order (per CONTEXT D-28 W7 row): TS strict → lint → Zustand selectors → Redis keys → vitest patterns → import ordering → logging audit. EACH sub-category lands its own atomic commit (or commit cluster — see sub-7 for incremental migration shape).

**Critical context overrides (from PATTERNS.md surfaced findings #2):**

1. **`server/lib/logger.ts` ALREADY EXISTS** with full pino integration + redactPaths + log() backward-compat wrapper. RESEARCH §"Don't Hand-Roll" recommendation to "vendor a `~30 LOC server/lib/log.ts`" is INCORRECT for this codebase. **Action:** sub-7 is a `console.* → logger.child({module}).*` MIGRATION, not a new logger build. **DO NOT create `server/lib/log.ts`.**
2. **Phase 27.4.x Redis keys are FROZEN** per CLAUDE.md "Phase 27.4.x Redis keys" + planning_context. Sub-4 is an audit + opportunistic alignment of OUTLIERS only — load-bearing names (events:llm:v3, events:llm-summary:v3, sites:v3, water:facilities:v3, events:llm:v2, events:llm:v3:partial, events:llm-dlq, events:llm-pipeline-override, events:llm-process-ts, events:llm-eval-baseline:v2, geocode:fwd:constrained:_, llm:tokens:_, cron:lastTick:\* — and any others surfaced in audit) MUST NOT be renamed.
3. **`dismissTimers` ref in ProximityAlertOverlay is NOT a real bug** (RESEARCH Pitfall 6). The "fix" is an `eslint-disable-next-line` with rationale comment, NOT a refactor. Sub-2 must respect this finding.

**Grep portability rule (BSD vs GNU):** macOS ships BSD grep, which interprets `\|` as a literal string, NOT alternation. All multi-pattern alternation greps in this plan use `grep -E` (extended regex) and group with bare `|` plus `\(\)` to escape literal parentheses. Do NOT use `grep` (basic mode) with `\|` — that pattern only matches in GNU grep and silently fails on macOS BSD grep.

Purpose: bring CI baseline to 0 errors / 0 react-hooks warnings; standardize logging; lock Redis key conventions; document the normalization completions for v1.4 closeout.
Output: ~10 atomic commits across 7 sub-categories + audit doc + CLAUDE.md update + wave SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28.1-cleanup-sweep/28.1-CONTEXT.md
@.planning/phases/28.1-cleanup-sweep/28.1-RESEARCH.md
@.planning/phases/28.1-cleanup-sweep/28.1-PATTERNS.md
@.planning/phases/28.1-cleanup-sweep/28.1-VALIDATION.md
@.planning/phases/28.1-cleanup-sweep/28.1-06-SUMMARY.md
@CLAUDE.md
@server/lib/logger.ts
@eslint.config.js
@tsconfig.json
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sub-1 TS strict — narrow remaining noUncheckedIndexedAccess + strictNullChecks errors</name>
  <files>(varies — narrowing fixes across src/ + server/; primary target: src/hooks/useEntityLayers.ts deck.gl v9 drift)</files>
  <read_first>
    - tsconfig.json (current strict settings — confirm noUncheckedIndexedAccess + strictNullChecks already on per CLAUDE.md "TypeScript strict mode — always enabled")
    - CLAUDE.md "V2 Extractor Watchdog (Phase 27.4.1)" — notes "remaining 8 are useEntityLayers.ts depthTest deck.gl v9 drift, deferred to Phase 27.4.3"; W7 sub-1 picks this up
    - 28.1-RESEARCH.md §"Validation Architecture" §W7 sub-1 row (signal: tsc --noEmit reports 0 errors)
    - Current `npx tsc --noEmit 2>&1 | head -50` output — read at task start to identify exact files + line numbers
  </read_first>
  <action>
    Step 1: Capture current tsc baseline:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit 2>&1 | tee /tmp/tsc-pre-w7.txt
    grep -c "error TS" /tmp/tsc-pre-w7.txt
    ```

    Step 2: For each error reported, narrow at the root:
    - `noUncheckedIndexedAccess`: use the local-bind + early-continue pattern from CLAUDE.md "V2 Extractor Watchdog" §"V1 TS cleanup":
      ```typescript
      // BEFORE (TS error: object possibly undefined)
      const item = arr[i];
      doSomething(item.field);

      // AFTER (D-15 local-bind + early-continue)
      const item = arr[i];
      if (!item) continue;
      doSomething(item.field);
      ```
    - `strictNullChecks`: add explicit null guard or non-null assertion with comment rationale.
    - `useEntityLayers.ts depthTest deck.gl v9 drift`: the deck.gl type signature for depthTest may have changed across v8 → v9. Read the failing line; if a previously-valid prop is now typed differently, add a narrow type assertion `as unknown as <ExpectedType>` with rationale comment, OR upgrade the call site to the new deck.gl v9 signature if the runtime accepts both.

    Atomic commits:
    - One commit per file (or per logical narrowing chunk if multiple files share one fix shape).
    - Commit message: `fix(28.1): narrow TS error in <file> (<TS code> — D-27 sub-1)`

    Step 3: Re-run tsc:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit
    ```
    Expected: 0 errors. If non-zero, a new narrowing introduced a regression — fix and re-commit.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit && echo "TS strict: 0 errors"</automated>
  </verify>
  <done>
    `npx tsc --noEmit` exits 0. All commits per-file atomic. /tmp/tsc-pre-w7.txt baseline preserved (referenced in W7 SUMMARY).
  </done>
</task>

<task type="auto">
  <name>Task 2: Sub-2 lint — fix all 6 react-hooks/exhaustive-deps warnings + reduce no-explicit-any baseline</name>
  <files>src/components/map/ProximityAlertOverlay.tsx, src/hooks/useSearchResults.ts, src/hooks/useQuerySync.ts, src/hooks/useEntityLayers.ts, src/components/layout/Sidebar.tsx</files>
  <read_first>
    - 28.1-RESEARCH.md §"Focus Area 5: react-hooks/exhaustive-deps fix-up landmines" lines 591-611 (catalog of 6 sites + decision tree)
    - 28.1-RESEARCH.md §"Pitfall 6" lines 274-280 (dismissTimers is NOT a real bug — eslint-disable with rationale, NOT refactor)
    - 28.1-PATTERNS.md §"src/components/map/ProximityAlertOverlay.tsx" lines 720-768 (canonical fix pattern + decision tree application to other 4 sites)
    - The 5 source files (read each before fixing — decision tree branch depends on ref-vs-value classification)
    - Current `npm run lint 2>&1 | grep -E "react-hooks/exhaustive-deps|no-explicit-any"` output
  </read_first>
  <action>
    Step 1: Capture current lint baseline:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npm run lint 2>&1 | tee /tmp/lint-pre-w7.txt
    grep -c "react-hooks/exhaustive-deps" /tmp/lint-pre-w7.txt
    grep -c "@typescript-eslint/no-explicit-any" /tmp/lint-pre-w7.txt
    ```

    Step 2: Apply RESEARCH §Focus Area 5 decision tree per site (from PATTERNS.md table at lines 762-767):

    | File | Recommended Fix | Atomic Commit Message |
    | ---- | --------------- | --------------------- |
    | src/components/map/ProximityAlertOverlay.tsx (2 warnings) | `eslint-disable-next-line react-hooks/exhaustive-deps -- dismissTimers ref intentionally excluded; cleanup runs on unmount only` per Pitfall 6. Investigate the 2nd warning per decision tree branches. | `fix(28.1): clear exhaustive-deps warnings in ProximityAlertOverlay.tsx (D-27 sub-2; Pitfall 6)` |
    | src/hooks/useSearchResults.ts (1 warning) | Decision tree branch 2: add missing dep to deps array, verify behavior unchanged. | `fix(28.1): add missing dep in useSearchResults exhaustive-deps (D-27 sub-2)` |
    | src/hooks/useQuerySync.ts (1 warning) | Decision tree branch 3: useCallback wrap (if callback identity changes per render) + add to deps. | `fix(28.1): useCallback wrap in useQuerySync (D-27 sub-2)` |
    | src/hooks/useEntityLayers.ts (1 warning) | Decision tree branch 2: add missing dep, verify cluster IDs/filter state behave correctly. | `fix(28.1): add missing dep in useEntityLayers (D-27 sub-2)` |
    | src/components/layout/Sidebar.tsx (1 warning) | Decision tree branch 1: refs — eslint-disable with rationale (per RESEARCH catalog "Sidebar refs"). | `fix(28.1): eslint-disable rationale for refs in Sidebar (D-27 sub-2)` |

    Step 3: Process @typescript-eslint/no-explicit-any per file:
    - Run `npm run lint 2>&1 | grep "no-explicit-any" | head -20` — list files with `any` usage.
    - For each, narrow `any` → `unknown` (with type narrowing at the use site) OR a more specific type if the surrounding context provides it.
    - DO NOT mass-replace. Each fix is a per-file commit IF the change touches multiple statements; OR group commits per related-functionality if multiple files share one shape.
    - Goal: reduce baseline count. Not all `any` may be cleanable in W7 — those that remain MUST have an `// eslint-disable-line @typescript-eslint/no-explicit-any -- <reason>` rationale.

    Step 4: Re-run lint:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npm run lint
    ```
    Expected: 0 errors AND 0 react-hooks/exhaustive-deps warnings. Note the change in no-explicit-any count vs baseline.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npm run lint 2>&1 | grep -E "react-hooks/exhaustive-deps" | wc -l | tr -d ' ' | grep -q "^0$" && echo "react-hooks/exhaustive-deps: 0 warnings"</automated>
  </verify>
  <done>
    Lint reports 0 react-hooks/exhaustive-deps warnings. Lint reports 0 errors. no-explicit-any count reduced or each remaining instance has eslint-disable rationale. Per-file atomic commits landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sub-3 Zustand selectors audit + fix any full-state subscribers</name>
  <files>(varies — only modifies sites where selector pattern violated)</files>
  <read_first>
    - CLAUDE.md "Conventions" — `Zustand selectors — `s => s.field` pattern to minimize re-renders`
    - 28.1-CONTEXT.md §D-27 sub-3 (audit confirms every store consumer uses selector form)
    - 28.1-VALIDATION.md §W7 sub-3 row — grep signal
    - The grep portability rule in this plan's <objective> — use `grep -E` (extended regex) with `|` alternation, NOT `grep` (basic) with `\|` (BSD grep treats `\|` as a literal)
  </read_first>
  <action>
    Step 1: Audit grep for full-state subscribers across all stores. Use `grep -E` (extended regex) and bare `|` for alternation; escape literal parens with `\(\)`. macOS BSD grep DOES NOT support `\|` alternation in basic mode — using `\|` here would silently match nothing on macOS and produce a false PASS:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && grep -rEn "useFlightStore\(\)|useShipStore\(\)|useEventStore\(\)|useNewsStore\(\)|useMarketStore\(\)|useSiteStore\(\)|useWaterStore\(\)|useUIStore\(\)|useFilterStore\(\)|useLayerStore\(\)|useNavigationStore\(\)|useNotificationStore\(\)|useThreatStore\(\)|useMapStore\(\)|useSearchStore\(\)" src/
    ```

    Filter out non-violations:
    - `getState()` calls (e.g., `useFlightStore.getState()`) are valid — not a subscription.
    - `setState()` calls — same.
    - `useFlightStore((s) => s.xxx)` calls — these have an arg and are valid.

    For each TRUE violation (a no-arg call inside React render context), refactor to selector form:
    ```typescript
    // BEFORE
    const store = useFlightStore();
    const flights = store.flights;

    // AFTER (selector — minimizes re-renders)
    const flights = useFlightStore((s) => s.flights);
    ```

    Atomic commits: one per file. Commit message: `refactor(28.1): convert <store> full-state subscriber to selector pattern (D-27 sub-3)`.

    If the audit grep returns zero violations, this task no-ops with a single commit:
    ```
    chore(28.1): Zustand selector audit clean — zero full-state subscribers found (D-27 sub-3)
    ```
    (May be combined with the W7 SUMMARY commit at task close to reduce commit noise IF zero violations.)

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && (grep -rEn "useFlightStore\(\)|useShipStore\(\)|useEventStore\(\)|useNewsStore\(\)|useMarketStore\(\)|useSiteStore\(\)|useWaterStore\(\)|useUIStore\(\)|useFilterStore\(\)|useLayerStore\(\)|useNavigationStore\(\)|useNotificationStore\(\)|useThreatStore\(\)|useMapStore\(\)|useSearchStore\(\)" src/ | grep -vE "getState|setState|getInitialState" | wc -l | tr -d ' ' | grep -q "^0$") && echo "Zustand selectors: clean"</automated>
  </verify>
  <done>
    Audit grep returns 0 violations OR all violations have been converted via per-file commits. Vitest green throughout.
  </done>
</task>

<task type="auto">
  <name>Task 4: Sub-4 Redis cache key naming consistency audit (audit-only doc; NEVER renames Phase 27.4.x bridge keys)</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md</files>
  <read_first>
    - server/cache/redis.ts (cacheGet / cacheSet signatures)
    - CLAUDE.md "Phase 27.4.x Redis keys" + "Cron-Driven Pipeline Trigger" — lists load-bearing keys (events:llm:v3, sites:v3, water:facilities:v3, events:llm:v2, events:llm:v3:partial, events:llm-dlq, events:llm-pipeline-override, events:llm-process-ts, events:llm-eval-baseline:v2, geocode:fwd:constrained:*, llm:tokens:*)
    - planning_context §additional_constraints — Phase 27.4.x Redis keys MUST NOT be renamed
    - 28.1-CONTEXT.md §D-27 sub-3 — `<domain>:<source>:<version>` pattern is the ideal alignment
    - The grep portability rule in this plan's <objective> — alternation greps below use `-E` flag
  </read_first>
  <action>
    Step 1: Enumerate every cacheGet / cacheSet call site. Use `grep -E` for alternation:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && grep -rEn "cacheGet|cacheSet|cacheGetSafe" server/ | grep -v __tests__ | tee /tmp/cache-keys.txt
    ```

    Step 2: Extract the key string passed to each call. Build a table.

    Step 3: Create `.planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md` with:

    ```markdown
    ---
    phase: 28.1
    wave: W7 sub-4
    decision: D-27 sub-3 (Redis cache key naming consistency)
    audit_date: <fill at task time>
    audit_completed_by: claude-code
    ---

    # Phase 28.1 W7 Sub-4 — Redis Cache Key Audit

    > Audit-only. NEVER renames load-bearing Phase 27.4.x bridge keys (Pitfall 1 depends on them).
    > Outliers are documented; opportunistic renames only happen IF they're clearly NOT in any bridge chain AND have no operator-known external probes.

    ## Convention

    `<domain>:<source>:<version>` per D-27 sub-3.

    Examples:
    - `events:llm:v3` ✓
    - `sites:v3` (semi-conformant — no `<source>` segment because Overpass is the only source)
    - `water:facilities:v3` ✓
    - `flights:adsblol` (semi-conformant — no version)

    ## Key Inventory

    | Call site | Key string | Pattern conformance | Phase 27.4.x bridge? | Disposition |
    | --------- | ---------- | ------------------- | -------------------- | ----------- |
    | server/cache/<file>:<line> | `events:llm:v3` | ✓ canonical | YES — D-04 cron + Pitfall 1 fallback | PRESERVED — Phase 27.4.x bridge |
    | server/routes/<file>:<line> | `flights:adsblol` | semi (no version) | NO | KEEP — versioning never used; would invalidate operator history |
    | ... (one row per cacheGet/cacheSet site) ... | | | | |

    ## Load-Bearing Bridge Keys (NEVER RENAME — Phase 27.4.x bridge fallback chain)

    Per CLAUDE.md "Phase 27.4.x Redis keys" + planning_context constraint:

    - events:llm:v3 (terminal v3 cache)
    - events:llm:v3:partial (observability)
    - events:llm-summary:v3 (run summary)
    - events:llm:v2 (Pitfall 1 fallback)
    - events:llm-summary:v2
    - events:llm (v1 fallback)
    - events:llm-summary
    - events:gdelt (raw GDELT fallback)
    - events:llm-dlq (dead-letter queue)
    - events:llm-pipeline-override (operator runtime swap)
    - events:llm-process-ts (cron cooldown)
    - events:llm-eval-baseline:v2 (eval harness)
    - sites:v3 (Overpass site snapshot)
    - water:facilities:v3 (Overpass water snapshot)
    - water:precip (Open-Meteo precip)
    - geocode:fwd:constrained:<hash> (Nominatim cache)
    - llm:tokens:<provider>:<date> (token budget)
    - cron:lastTick:<name> (cron heartbeat — IF written by handlers; verify in audit)

    ## Outliers (semi-conformant or clearly non-conformant)

    | Key | Reason for non-conformance | Recommended action |
    | --- | --------------------------- | ------------------ |
    | <flag any keys that don't fit the pattern AND are not in the load-bearing list> | <reason> | KEEP — operator data \| RENAME (with version bump) — only if zero downstream consumers |

    ## Renames Performed in W7 sub-4 (if any)

    | From key | To key | Justification | Commit SHA |
    | -------- | ------ | ------------- | ---------- |

    DEFAULT: no renames in W7 sub-4. Audit + documentation only.

    ## Sign-off

    - [ ] Every cacheGet / cacheSet call site has a row above
    - [ ] Every load-bearing bridge key is annotated PRESERVED
    - [ ] Any outlier flagged for rename has been verified to have ZERO downstream consumers AND has a separate atomic commit (NOT bundled in this audit commit)
    ```

    Step 4: Populate the table from `/tmp/cache-keys.txt`. For each row, classify:
    - PRESERVED — Phase 27.4.x bridge → load-bearing, NEVER touch
    - KEEP — semi-conformant but operator history matters (e.g., `flights:adsblol` versioning would invalidate the cache for no benefit)
    - RENAME (with version bump) → only if absolutely clearly NOT in any bridge AND no operator-known consumer

    DEFAULT BIAS: KEEP/PRESERVED. Renames in W7 are LAST RESORT; the audit doc is the deliverable.

    Commit (single atomic): `docs(28.1): W7 sub-4 Redis key audit (D-27 — preserve Phase 27.4.x bridge)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && test -f .planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md && grep -c "PRESERVED — Phase 27.4.x bridge" .planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md</automated>
  </verify>
  <done>
    Audit doc committed. Every cacheGet/cacheSet call site listed. Load-bearing bridge keys explicitly PRESERVED. No renames performed unless explicitly justified per the strict criteria above.
  </done>
</task>

<task type="auto">
  <name>Task 5: Sub-5 vitest patterns + mock standardization + it.todo audit</name>
  <files>(varies — minimal source touch; mostly audit + per-test-file cleanup)</files>
  <read_first>
    - CLAUDE.md "Testing" — Framework: Vitest with jsdom (frontend), node (server); Run: `npx vitest run` (all), `npx vitest run server/` (server only); Mocks: `src/test/__mocks__/` for WebGL-dependent libraries; Stubs: `it.todo()` for unimplemented test stubs
    - 28.1-VALIDATION.md §W7 sub-5 row — full suite green; it.todo count reduced or justified
    - Current vitest output: `npx vitest run 2>&1 | grep -E "todo|skipped"`
    - vite.config.ts test.alias for the WebGL mocks (per CLAUDE.md "Map mocks")
    - The grep portability rule in this plan's <objective>
  </read_first>
  <action>
    Step 1: Capture current vitest baseline:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npx vitest run 2>&1 | tee /tmp/vitest-pre-w7.txt
    grep -cE "↓|todo" /tmp/vitest-pre-w7.txt
    ```

    Step 2: Audit test file naming + mock placement:
    - All client test files should live under `src/__tests__/` OR be co-located as `*.test.ts(x)` siblings (consistent with current convention; pick one).
    - Server test files under `server/__tests__/`.
    - WebGL/maplibre/deck.gl mocks under `src/test/__mocks__/` per CLAUDE.md.

    Spot-check: `grep -rn "vi.mock" src/ server/ | head -20` — confirm inline mocks are limited to test-specific scenarios; shared mocks live in `src/test/__mocks__/`.

    Step 3: Audit `it.todo` calls. Use `grep -E` for alternation:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && grep -rEn "it\.todo|test\.todo" src/ server/ | tee /tmp/itodo.txt
    ```

    For each `it.todo`:
    - If the underlying functionality has been implemented since the stub was written: implement the test (atomic per-file commit).
    - If still legitimately blocked (e.g., requires a mock that doesn't exist; deferred to a future phase): add an inline rationale comment OR convert to a `describe.skip` with reason.

    Step 4: Snapshot test policy — CLAUDE.md doesn't currently have a snapshot test convention. Spot-grep for `toMatchSnapshot` calls; if any exist, ensure each is justified (visual regression that's worth the maintenance cost). If a snapshot is brittle and adds noise, convert to assertion-based test in a separate atomic commit.

    Step 5: Single closing commit IF only audit changes happened (no test deletions/conversions):
    ```
    chore(28.1): W7 sub-5 vitest patterns audit — naming + mocks + it.todo (D-27)
    ```
    OR per-file commits IF multiple changes landed.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run</automated>
  </verify>
  <done>
    Full vitest suite green. it.todo count reduced or each remaining one has annotation. Audit-only changes if no implementations possible.
  </done>
</task>

<task type="auto">
  <name>Task 6: Sub-6 import ordering — enable ESLint import/order rule + 0-violation sweep</name>
  <files>eslint.config.js, (varies — auto-fix sweep across src/ + server/)</files>
  <read_first>
    - eslint.config.js (current rules — confirm import/order is NOT enabled, OR is enabled but disabled inline)
    - CLAUDE.md "Conventions" — confirm import order is not yet documented
    - 28.1-VALIDATION.md §W7 sub-6 row — ESLint import/order enabled; 0 violations
    - 28.1-CONTEXT.md §D-27 sub-6 — standardize external/internal/relative + audit @/ vs relative usage
  </read_first>
  <action>
    Step 1: Enable `eslint-plugin-import` `import/order` rule in eslint.config.js. Recommended config:
    ```javascript
    // Add to eslint.config.js rules:
    'import/order': [
      'warn', // start as warn — flip to error in a future phase if 0 sustains
      {
        groups: [
          'builtin',     // node:* etc.
          'external',    // npm packages
          'internal',    // @/ alias
          'parent',      // ../
          'sibling',     // ./
          'index',
          'object',
          'type',
        ],
        pathGroups: [
          { pattern: '@/**', group: 'internal', position: 'before' },
        ],
        pathGroupsExcludedImportTypes: ['builtin'],
        'newlines-between': 'always',
        alphabetize: { order: 'asc', caseInsensitive: true },
      },
    ],
    ```

    Confirm `eslint-plugin-import` is already a project dependency:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && grep "eslint-plugin-import" package.json
    ```
    If absent, install: `npm install --save-dev eslint-plugin-import` and update eslint.config.js to register the plugin.

    Step 2: Run `npm run lint -- --fix` to auto-fix import order violations. Most violations are mechanical reorderings.
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npm run lint -- --fix
    ```

    Step 3: Re-run lint:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npm run lint 2>&1 | grep "import/order" | wc -l
    ```
    Expected: 0 violations.

    Step 4: Run prettier to clean up any formatting drift from auto-fix:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && npx prettier --write .
    ```

    Atomic commits:
    - One commit for the eslint.config.js rule addition + plugin install (if needed): `chore(28.1): enable ESLint import/order rule (D-27 sub-6)`
    - One commit for the `--fix` sweep: `style(28.1): auto-fix import ordering across src/ + server/ (D-27 sub-6)`
    - One commit for prettier reflow if needed: `style(28.1): prettier reflow post import-order fix (D-27 sub-6)`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npm run lint 2>&1 | grep -c "import/order" | tr -d ' ' | grep -q "^0$" && echo "import/order: 0 violations"</automated>
  </verify>
  <done>
    ESLint import/order rule active. 0 violations. Prettier clean. Vitest green.
  </done>
</task>

<task type="auto">
  <name>Task 7: Sub-7 logging migration — console.* → logger.child({module}).* in server/* (DO NOT create server/lib/log.ts)</name>
  <files>server/lib/llmEventExtractor.v3.ts, server/adapters/*.ts, server/routes/*.ts (incremental — one logical group per atomic commit)</files>
  <read_first>
    - server/lib/logger.ts (the EXISTING pino logger — confirm `logger.child({module: 'X'})` is the migration target; PATTERNS.md surfaced finding #2)
    - server/routes/markets.ts:4-6 (canonical pattern: `import { logger } from '../lib/logger.js'; const log = logger.child({ module: 'markets' });`)
    - 28.1-PATTERNS.md §"server/lib/log.ts" lines 265-289 (DO NOT CREATE this file; use existing logger.ts)
    - 28.1-RESEARCH.md §"Anti-Patterns to Avoid" line 222 — Replacing `console.*` with `log.*` in a single sweep commit is forbidden; W7 sub-7 should be incremental
    - planning_context §critical_overrides_from_pattern_mapper item 2 — the existing logger.ts standard
  </read_first>
  <action>
    DO NOT create `server/lib/log.ts`. The existing `server/lib/logger.ts` is the canonical pino instance.

    Step 1: Capture baseline:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && grep -c "console\." server/lib/llmEventExtractor.v3.ts server/adapters/*.ts server/routes/*.ts | grep -v ":0$"
    ```

    Step 2: Migrate INCREMENTALLY per RESEARCH §"Anti-Patterns to Avoid". Order from highest yield to lowest:

    Commit 1: `server/lib/llmEventExtractor.v3.ts` console.* → logger.child({module: 'extractor-v3'}).*
    - Add: `import { logger } from './logger.js'; const log = logger.child({ module: 'extractor-v3' });`
    - Replace `console.log(...)` → `log.info({...meta}, 'message')`
    - Replace `console.warn(...)` → `log.warn({...meta}, 'message')`
    - Replace `console.error(...)` → `log.error({...meta}, 'message')`
    - For each migration, structure the message: first arg is `{key: value}` metadata object, second arg is the human-readable message string. See markets.ts:39-42 / 62 pattern.
    - **CRITICAL — security:** any LLM prompt content, full request bodies, API keys, or user-supplied free-form text MUST NOT be logged verbatim. Use the logger.ts existing `redactPaths` if present, OR truncate/summarize before logging.
    - Commit: `refactor(28.1): migrate llmEventExtractor.v3.ts to logger.child (D-27 sub-7)`

    Commit 2: `server/adapters/*.ts` (one per adapter, OR group adapters by domain — e.g., commit per adapter for high-noise files like opensky/adsbexchange/adsb-lol; group quiet ones)
    - Same pattern: `const log = logger.child({ module: 'adapter-<name>' })`
    - Same security rule: redact upstream API keys and full URLs.
    - Atomic per-adapter or per-adapter-group commits.

    Commit 3: `server/routes/*.ts` (one per route OR grouped — many already use the pattern per markets.ts; only the laggards need migration)
    - Same pattern.

    Step 3: After all migrations, re-run baseline:
    ```bash
    cd /Users/zackmaz/Desktop/my_world && grep -c "console\." server/lib/llmEventExtractor.v3.ts server/adapters/*.ts server/routes/*.ts | grep -v ":0$"
    ```
    Expected: 0 results (or only allow-listed sites with `// eslint-disable-line no-console -- <reason>` — e.g., a one-off CLI script or a startup banner).

    DO NOT touch `src/` `console.*` in W7 sub-7 — RESEARCH §"Open Question 5" + §"Anti-Patterns" defer client-side migration. Captured as a follow-up backlog item via the W7 SUMMARY.

    Vitest after each commit to confirm no regression. The logger.ts pino integration is mature, but message-shape changes can break tests that snapshot log output.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && grep -c "console\." server/lib/llmEventExtractor.v3.ts server/adapters/*.ts server/routes/*.ts | grep -v ":0$" | wc -l | tr -d ' ' | grep -q "^0$" && echo "console.* in server/lib/extractor + server/adapters + server/routes: 0"</automated>
  </verify>
  <done>
    Server-side high-yield files migrated to logger.child(). Vitest green throughout. server/lib/log.ts NOT created (verify: `test ! -f /Users/zackmaz/Desktop/my_world/server/lib/log.ts`). Atomic per-file or per-domain commits.
  </done>
</task>

<task type="auto">
  <name>Task 8: W7 wave-gate verification + close 28.1 phase with CLAUDE.md update + final summary</name>
  <files>.planning/phases/28.1-cleanup-sweep/28.1-W7-SUMMARY.md (new), CLAUDE.md</files>
  <read_first>
    - All commits in W7 (`git log --oneline main..HEAD`)
    - .planning/phases/28.1-cleanup-sweep/28.1-W6-SUMMARY.md (prior wave summary — format precedent)
    - CLAUDE.md (full file — final phase-close update needed)
  </read_first>
  <action>
    Step 1: Run all five wave-gate commands; ALL must exit 0:
    ```bash
    cd /Users/zackmaz/Desktop/my_world
    npx vitest run
    npm run lint
    npx prettier --check .
    npx tsc --noEmit
    npm run build
    ```

    Step 2: Compute deltas vs pre-W7 baselines:
    - TS errors: pre-W7 → 0 (Task 1)
    - react-hooks/exhaustive-deps warnings: 6 → 0 (Task 2)
    - @typescript-eslint/no-explicit-any: pre-W7 count → post-W7 count (Task 2 partial reduction)
    - it.todo count: pre-W7 → post-W7 (Task 5)
    - import/order violations: pre-W7 → 0 (Task 6)
    - console.* in server/lib/extractor + adapters + routes: pre-W7 → 0 (Task 7)
    - server/lib/log.ts: NOT created (verify absence)

    Step 3: Update CLAUDE.md with the W7 + phase-close section. Add to the bottom of the file (or in a new "Phase 28.1 Cleanup Sweep (Phase 28.1)" section):
    ```
    ## Phase 28.1 Cleanup Sweep — closeout 2026-XX-XX

    Phase 28.1 cleanup sweep complete (all 7 waves). Highlights:

    - **API reliability:** /api/health aggregate endpoint live at /health and /api/health (W2). DevApiStatus "All APIs" tab + HealthBanner toast for critical-tier outages. Per-endpoint freshness thresholds + tier classification per D-25/D-26.
    - **Ghost code sweep:** N modules deleted via knip + ts-prune triage (W3+W4). server/adapters/acled.ts preserved as inactive.
    - **Hardcode generalization (D-12):** 11 operator-tunable env vars introduced (POLL_FLIGHTS_MS, ATTACK_RADIUS_KM, SEVERITY_HALF_LIFE_HOURS, etc. — see .env.example).
    - **Domain constants (D-11):** IRAN_BBOX, IRAN_CENTER, WAR_START, ADSB_RADIUS_NM centralized at src/lib/domain.ts; server/config.ts re-exports.
    - **Drift resolutions (W5):** severity half-life 24h authoritative (CLAUDE.md was 6h — corrected); ADS-B radius 1200 NM authoritative (CLAUDE.md was 500 NM — corrected); IRAN_CENTER (28.0, 45.0) authoritative (CLAUDE.md was (30.0, 50.0) — corrected).
    - **CSS @theme migration (D-13):** entity / event / site / faction colors migrated to src/styles/app.css @theme block; deck.gl bridge via src/lib/colorBridge.ts.
    - **Normalization (W7):** TS strict 0 errors; react-hooks/exhaustive-deps 0 warnings; import/order rule live; server-side logging migrated to logger.child() pattern (no new logger module — uses existing server/lib/logger.ts pino instance).
    - **Redis key bridge preserved:** all Phase 27.4.x load-bearing keys (events:llm:v3, sites:v3, water:facilities:v3, etc.) UNCHANGED. Audit at .planning/phases/28.1-cleanup-sweep/28.1-W7-REDIS-AUDIT.md.
    ```

    Step 4: Create `.planning/phases/28.1-cleanup-sweep/28.1-W7-SUMMARY.md` with:
    - Wave goal (D-27 — 7 sub-categories)
    - Per-sub-category disposition + metric delta
    - server/lib/log.ts NOT created (PATTERNS.md surfaced finding #2 honored)
    - Phase 27.4.x Redis keys NOT renamed
    - Wave-gate output (5 commands)
    - Phase 28.1 close signal: "All 7 waves green; 28.1 may merge to main"
    - Follow-up backlog: src/ console.* migration (RESEARCH Open Question #5 deferred), no-explicit-any residuals (if any), ethnicGroups.ts color migration (if W6 chose Option A)

    Commit (single atomic): `docs(28.1): close W7 + Phase 28.1 with normalization summary + CLAUDE.md update (D-27 — gate green)`

    Phase close ritual (per CLAUDE.md "Phase boundaries"):
    - DO NOT merge to main from this PLAN — that's the user's decision after `/gsd-verify-work`.
    - DO confirm branch is clean: `git status` returns nothing.
    - DO confirm branch is pushed (push if not): `git push origin feature/28.1-cleanup-sweep`.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit && npm run build && test ! -f server/lib/log.ts && echo "Phase 28.1: ALL GATES GREEN; server/lib/log.ts correctly absent"</automated>
  </verify>
  <done>
    All five gates green. CLAUDE.md updated with phase closeout summary. W7 SUMMARY committed; Phase 28.1 SUMMARY (28.1-07-SUMMARY.md) committed. server/lib/log.ts does NOT exist. Branch is clean and pushed. Ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                          |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| logger output → Vercel runtime logs | All logger.\* calls write JSON lines that Vercel captures. Operator-readable; possibly grep-searchable; potentially indexed by Vercel observability. |
| eslint.config.js rule changes → CI  | Lint rule changes affect every PR after merge.                                                                                                       |

## STRIDE Threat Register

| Threat ID    | Category                                                    | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------ | ----------------------------------------------------------- | ------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------- | -------------- |
| T-28.1-W7-01 | I (Information disclosure)                                  | server-side logger migration (Task 7) | mitigate    | Per planning_context §security_threat_model_required: console.error migrations MUST NOT log full request bodies, API keys, or LLM prompts that may contain user-supplied data. Use the existing logger.ts redactPaths config (verify present in current logger.ts before migration; if absent, ADD redactPaths config to logger.ts as a sub-task). For each migrated log site: structure metadata as `{...redactedFields}` not free-form text. Spot-check by grepping migrated commits for `log.error\\(.\*body\\ | prompt\\ | token\\ | key` patterns. |
| T-28.1-W7-02 | T (Tampering — silent behavior change)                      | TS narrowing (Task 1)                 | mitigate    | Each TS error fix preserves runtime behavior — narrowing is a type-system clarification, not a logic change. Vitest green after each commit confirms no regression. Per-file atomic commits enable git revert of any single mistaken narrowing.                                                                                                                                                                                                                                                                   |
| T-28.1-W7-03 | T (Tampering — wrongly-removed deps in exhaustive-deps fix) | useEffect deps array changes (Task 2) | mitigate    | Decision tree from RESEARCH §Focus Area 5 is mandatory; each fix gets a per-site classification (ref / value / callback / intentional-exclusion) before code change. Vitest covers most behavior; manual UAT against ProximityAlertOverlay (the most complex case) is part of W4's CLAUDE.md feature inventory walk and would catch regressions.                                                                                                                                                                  |
| T-28.1-W7-04 | A (Availability — bridge breakage)                          | Redis cache key audit (Task 4)        | mitigate    | DEFAULT BIAS in Task 4 is KEEP/PRESERVED. Renames forbidden for Phase 27.4.x bridge keys per planning_context. Audit doc enforces "explicitly justified" requirement for any rename; in practice no renames land in W7 sub-4 — it's audit-only.                                                                                                                                                                                                                                                                   |
| T-28.1-W7-05 | T (Tampering — eslint config drift)                         | Task 6 import/order rule              | accept      | Rule starts as `'warn'` per Task 6; future phase may flip to `'error'`. Even at warn-level, the auto-fix sweep delivers the cleanup. Future drift is captured by lint output.                                                                                                                                                                                                                                                                                                                                     |

</threat_model>

<verification>
- Sub-1 TS strict: `npx tsc --noEmit` exits 0
- Sub-2 lint: `npm run lint` reports 0 errors AND 0 react-hooks/exhaustive-deps warnings
- Sub-3 Zustand selectors: grep returns 0 full-state subscribers (excluding getState/setState)
- Sub-4 Redis: 28.1-W7-REDIS-AUDIT.md committed; load-bearing keys PRESERVED
- Sub-5 vitest: full suite green; it.todo count documented
- Sub-6 imports: ESLint import/order rule active; 0 violations
- Sub-7 logging: 0 console.* in server/lib/extractor + server/adapters + server/routes; server/lib/log.ts NOT created
- All five wave-gate commands exit 0
- CLAUDE.md updated with phase closeout
- W7 + Phase 28.1 SUMMARY committed
</verification>

<success_criteria>

- All 7 sub-categories landed per the plan with atomic commits
- CI baseline at 0 errors / 0 react-hooks/exhaustive-deps warnings
- Phase 27.4.x Redis bridge keys preserved (audit doc proves it)
- Existing server/lib/logger.ts pino instance is the migration target (no new server/lib/log.ts file created)
- `npx vitest run && npm run lint && npx prettier --check . && npx tsc --noEmit && npm run build` all exit 0
- Phase 28.1 ready for `/gsd-verify-work` then merge to main
  </success_criteria>

<output>
After completion, create `.planning/phases/28.1-cleanup-sweep/28.1-07-SUMMARY.md`. This is the LAST plan summary in Phase 28.1; the phase closes after this commit.
</output>

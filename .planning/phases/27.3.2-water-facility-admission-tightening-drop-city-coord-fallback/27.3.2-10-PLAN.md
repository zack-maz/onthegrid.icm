---
phase: 27.3.2
plan: 10
type: execute
wave: 4
depends_on:
  - '27.3.2-01'
  - '27.3.2-02'
  - '27.3.2-03'
  - '27.3.2-04'
  - '27.3.2-05'
  - '27.3.2-06'
  - '27.3.2-07'
  - '27.3.2-08'
  - '27.3.2-09'
files_modified:
  - src/data/water-facilities.json
  - CLAUDE.md
autonomous: false
requirements:
  - D-12
  - D-13
  - D-18
tags:
  - water
  - snapshot
  - docs
user_setup:
  - service: overpass-api
    why: 'Live Overpass API required for snapshot regeneration — script hits public OSM mirrors'
    dashboard_config:
      - task: 'Confirm .env has UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN populated (needed because refresh:water writes snapshot through Redis cache path)'
        location: 'Local .env file in repo root'
must_haves:
  truths:
    - 'src/data/water-facilities.json exists and parses as valid JSON'
    - 'Snapshot facility count falls in the 290-320 range (target ~304: 240 dams + 49 reservoirs + 15 desal)'
    - "Zero facilities in the snapshot carry a bare generic type label (label matching /^(dam|reservoir|desalination(?:\\s+plant)?)$/i is 0)"
    - "All admitted desalination facilities carry either a Latin name OR a server-synthesized 'Desalination Plant near/at' label (never a bare type)"
    - 'All non-desal admitted facilities carry a Latin-script label (verified by manual sample of 10 random entries)'
    - 'Facilities are sorted by id and coords rounded to 6dp (matching established Plan 10 snapshot conventions)'
    - 'CLAUDE.md Water Stress Layer section annotates the D-01 Latin-label gate, D-11 Redis v3 key, and D-14 no_resolved_name bucket'
  artifacts:
    - path: 'src/data/water-facilities.json'
      provides: 'Regenerated committed snapshot with new admission shape'
      contains_zero_matches: "^(dam|reservoir|desalination(?:\\s+plant)?)$"
    - path: 'CLAUDE.md'
      provides: 'Annotated Water Stress Layer section reflecting Phase 27.3.2 changes'
      contains: 'no_resolved_name'
  key_links:
    - from: 'scripts/refresh-water-facilities.ts'
      to: 'server/adapters/overpass-water.ts fetchWaterFacilities'
      via: 'wraps the pipeline + writes JSON atomically'
      pattern: 'fetchWaterFacilities'
    - from: 'src/data/water-facilities.json'
      to: 'server/lib/waterSnapshot.ts loadWaterSnapshot'
      via: 'read at cold start per R-07 snapshot tier'
      pattern: 'loadWaterSnapshot'
---

<objective>
Ship the phase. Regenerate `src/data/water-facilities.json` via `npm run refresh:water` under the new admission rules (D-12), verify the regenerated snapshot satisfies the quality invariants (D-13), and annotate `CLAUDE.md` with the Phase 27.3.2 changes (D-18 was about 27.3.3 deferral — included here as documentation bookkeeping).

Purpose: Ship the data artifact that all prior plans' code changes produce. Commit the regenerated JSON atomically with a documentation annotation in CLAUDE.md so a future reader sees the Water Stress Layer section accurately reflect the current code contract.

Output: Two file edits — regenerated snapshot (from 436 → ~304 facilities) and a CLAUDE.md annotation. Plus a human-verification checkpoint (autonomous: false) before the commit because snapshot regeneration touches live external services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md
@.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-PATTERNS.md
@.planning/phases/27.3.1-water-facility-retry-and-cleanup/27.3.1-CONTEXT.md

<interfaces>
## Expected snapshot shape

`src/data/water-facilities.json` is a top-level JSON array of `WaterFacility` objects, OR an envelope object (shape established by 27.3.1 Plan 05). Confirm by reading the current snapshot's top-level structure before the refresh. The refresh script preserves that shape.

Per 27.3.1 Plan 05: schema is pre-enriched `WaterFacility[]` with sort-by-id, 6dp coord rounding, 2-space pretty-print, atomic tempfile+rename.

## Expected count delta

Before (post-27.3.1 Plan 10 snapshot): 436 facilities (UAE 78, Saudi 69, Iran 57, Iraq 57, Syria 31, Afghanistan 31).

Target after (per CONTEXT.md "Expected impact"): ~304 facilities (240 dams + 49 reservoirs + 15 desalination).

Acceptable range: 290-320 total. A result outside this range suggests either an admission-logic bug OR a live-Overpass data shift — both require investigation, not blind acceptance.

## Regeneration pipeline

1. `npm run refresh:water` — runs `scripts/refresh-water-facilities.ts` per 27.3.1 Plan 05/09.
2. Script:
   a. Hits live Overpass (primary + mirror fallback per 27.3 Plan 01).
   b. Classifies each OSM element (facility type inference).
   c. Runs `computeAdmissionDecision` with the new `no_resolved_name` branch (Plan 04).
   d. Runs `extractLabel` with the new desal synthesis branches (Plan 05).
   e. Populates `filterStats` with the new `no_resolved_name` bucket counts.
   f. Writes `src/data/water-facilities.json` via atomic tempfile+rename.
3. Developer commits the JSON alongside this plan's CLAUDE.md annotation.

## CLAUDE.md annotation target

The "Water Stress Layer" section near the bottom of `CLAUDE.md` documents the water facility admission pipeline. Add 3-5 bullets capturing the 27.3.2 changes WITHOUT rewriting the section:

- `hasLatinLabel(tags)` admission gate — non-desal facilities require a Latin-script name tag
- `no_resolved_name` rejection bucket added to `WaterFilterStats.rejections` and per-type breakdown
- `extractLabel` synthesizes desalination-only fallback labels ("Desalination Plant near {city}" / "at {lat}°, {lng}°")
- `src/lib/waterLabel.ts` collapsed to a single-line server-trust read
- Redis cache key bumped `water:facilities:v2` → `v3` for shape-change cold-fill
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Regenerate src/data/water-facilities.json via npm run refresh:water</name>
  <files>src/data/water-facilities.json</files>
  <read_first>
    - src/data/water-facilities.json (read first 50 lines to confirm current shape + top-level JSON container)
    - scripts/refresh-water-facilities.ts (confirm script command is wired correctly; read full script, typically 80-120 lines)
    - package.json (confirm `scripts["refresh:water"]` exists and runs the script with `--env-file-if-exists=.env`)
    - .env (confirm UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN are populated — needed because snapshot generation writes through Redis path per 27.3.1 Plan 09 fix)
    - .planning/phases/27.3.1-water-facility-retry-and-cleanup/27.3.1-10-SUMMARY.md (reference: last snapshot commit pattern from Plan 10 of 27.3.1)
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md (D-12, D-13)
  </read_first>
  <behavior>
    - Running `npm run refresh:water` produces a new `src/data/water-facilities.json` with ~304 facilities.
    - Script exits 0 (no unhandled errors from Overpass / Nominatim / Redis).
    - Resulting JSON parses as valid JSON (`jq .` exits 0).
    - Facility count is within the 290-320 range.
    - No facility has a bare generic-type label (regex `/^(dam|reservoir|desalination(?:\s+plant)?)$/i` matches zero labels).
    - All admitted non-desal facilities have labels that pass `isLatin` check (rough proxy: no Persian/Arabic/Georgian characters in the `label` field — confirm by random-sample of 10).
    - Per-type distribution: dams ~240 (±30), reservoirs ~49 (±15), desalination 15 (exact — per the 15 preserved by the exemption).
    - Commit message body includes per-type before/after counts.
  </behavior>
  <action>
    **Step 1 — Pre-flight checks:**
    Before running the refresh script, confirm ALL code changes from Plans 01-09 have landed:
    ```
    grep -q "export function hasLatinLabel" server/adapters/overpass-water.ts
    && grep -q "bucket: 'no_resolved_name'" server/adapters/overpass-water.ts
    && grep -q "Desalination Plant near " server/adapters/overpass-water.ts
    && grep -q "water:facilities:v3" server/routes/water.ts
    && grep -q "no_resolved_name" server/schemas/cacheResponse.ts
    ```
    ALL five checks must exit 0. If any fails, STOP and report which upstream plan has not yet merged.

    Confirm typechecks + server tests pass:
    ```
    npx tsc -p server/tsconfig.json --noEmit
    npx vitest run server/
    ```
    Both must exit 0. If server tests fail, STOP — Plan 06 should have closed all breakage.

    **Step 2 — Confirm .env is populated:**
    ```
    grep -q "^UPSTASH_REDIS_REST_URL=" .env && grep -q "^UPSTASH_REDIS_REST_TOKEN=" .env
    ```
    If either is missing, the refresh will cascade into 2000ms timeouts per the 27.3.1 Plan 09 diagnosis. Populate the .env before running — ASK the user if the values are not present; do NOT invent them.

    **Step 3 — Run the refresh:**
    ```
    npm run refresh:water
    ```
    Expect runtime of 30-90 seconds (Overpass + Nominatim + Redis round-trips). Capture the full stdout/stderr log.

    Watch for:
    - "Overpass primary failed, falling back to private.coffee mirror" — normal, not a blocker.
    - "[Upstash Redis] Redis client was initialized without url or token" — INDICATES .env not loading; abort.
    - "filterStats" summary at the end — should show `no_resolved_name` bucket with a non-zero count (likely 100+) and `filteredCounts: { dam: ~240, reservoir: ~49, desalination: 15 }`.

    If the script exits with non-zero code or throws, capture the error and STOP — do NOT commit a partial snapshot.

    **Step 4 — Verify the regenerated JSON:**
    Run ALL of the following against the new `src/data/water-facilities.json`:

    **4a. Valid JSON:**
    ```
    jq . src/data/water-facilities.json > /dev/null
    ```
    Must exit 0.

    **4b. Total facility count in range:**
    Determine the top-level structure — if the snapshot is `{ facilities: [...] }`:
    ```
    jq '.facilities | length' src/data/water-facilities.json
    ```
    If it is a bare array `[...]`:
    ```
    jq 'length' src/data/water-facilities.json
    ```
    Result must be between 290 and 320 (target 304).

    **4c. Per-type counts:**
    ```
    jq '[.facilities[]? // .[] | .facilityType] | group_by(.) | map({type: .[0], count: length})' src/data/water-facilities.json
    ```
    Expect approximately: dam ~240, reservoir ~49, desalination 15.

    **4d. Zero bare-type labels:**
    ```
    jq '[.facilities[]? // .[] | select(.label | test("^(dam|reservoir|desalination( plant)?)$"; "i"))] | length' src/data/water-facilities.json
    ```
    MUST return 0. If any match, the admission logic did not reject the bare-label facility and Plans 04/05 need review.

    **4e. Spot-check 10 random entries for Latin labels (non-desal):**
    ```
    jq -r '[.facilities[]? // .[] | select(.facilityType != "desalination")] | .[0:10] | .[] | "\(.facilityType): \(.label)"' src/data/water-facilities.json
    ```
    Visually confirm all 10 labels are ASCII/Latin script (no Persian ش, Arabic م, Cyrillic В, Georgian ა characters).

    **4f. Desal labels are Latin OR server-synthesized:**
    ```
    jq -r '[.facilities[]? // .[] | select(.facilityType == "desalination")] | .[] | .label' src/data/water-facilities.json
    ```
    Each of the 15 desal entries shows a value that is either (a) a Latin name, (b) "Desalination Plant near {city}", or (c) "Desalination Plant at {lat}°{N|S}, {lng}°{E|W}". No bare "Desalination" or "Desalination Plant" alone.

    **Step 5 — Stage the JSON:**
    ```
    git add src/data/water-facilities.json
    ```

    Do NOT commit yet — Task 2 (CLAUDE.md annotation) and Task 3 (human checkpoint) gate the final commit.

    If any of steps 4a-4f fails, STOP and report. The snapshot is NOT acceptable; the root cause is likely a bug in Plans 01-09 that slipped past unit tests.

  </action>
  <verify>
    <automated>
      jq . src/data/water-facilities.json > /dev/null
      && jq '.facilities // . | length' src/data/water-facilities.json | awk '$1 >= 290 && $1 <= 320 {exit 0} {exit 1}'
      && jq '[.facilities[]? // .[] | select(.label | test("^(dam|reservoir|desalination( plant)?)$"; "i"))] | length' src/data/water-facilities.json | awk '$1 == 0 {exit 0} {exit 1}'
      && jq '[.facilities[]? // .[] | .facilityType] | group_by(.) | map(select(.[0] == "desalination")) | .[0] | length' src/data/water-facilities.json | awk '$1 == 15 {exit 0} {exit 1}'
    </automated>
  </verify>
  <acceptance_criteria>
    - `jq . src/data/water-facilities.json > /dev/null` exits 0 (valid JSON).
    - `jq '.facilities // . | length' src/data/water-facilities.json` returns an integer in [290, 320].
    - `jq '[.facilities[]? // .[] | select(.label | test("^(dam|reservoir|desalination( plant)?)$"; "i"))] | length'` returns 0 (no bare-type labels).
    - `jq '[.facilities[]? // .[] | select(.facilityType == "desalination")] | length'` returns 15 (per CONTEXT.md expected count).
    - Random sample of 10 non-desal entries show Latin labels (manually verified, no Persian/Arabic/etc script).
    - Desal entries show EITHER a Latin name OR a synthesized "Desalination Plant near/at ..." label (zero bare "Desalination" or "Desalination Plant" strings).
    - JSON is sorted by id and uses 6dp coord rounding (matches Plan 10 precedent — confirm by comparing format to existing file).
    - Snapshot is staged: `git diff --cached --name-only` includes `src/data/water-facilities.json`.
  </acceptance_criteria>
  <done>
    Snapshot regenerated. All quality invariants pass. File staged, not yet committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Annotate CLAUDE.md Water Stress Layer section with Phase 27.3.2 changes</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file; focus on the "Water Stress Layer (Phase 26)" section and any later annotations from 27.3 / 27.3.1 to match style)
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md (decisions D-01, D-05, D-06, D-08, D-11, D-14, D-18)
  </read_first>
  <behavior>
    - CLAUDE.md gains 4-6 new bullets under the Water Stress Layer section reflecting Phase 27.3.2 changes.
    - Annotation style matches existing bullets (one-line, technical, no emoji, no marketing language).
    - Bullets cover: (a) Latin-label admission gate, (b) `no_resolved_name` rejection bucket, (c) server-side desal label synthesis, (d) client collapse to one-liner, (e) Redis key v3 bump, (f) 27.3.3 deferred romanization reference.
    - No other section of CLAUDE.md is modified.
  </behavior>
  <action>
    Read `CLAUDE.md` and locate the "Water Stress Layer (Phase 26)" section. It contains bullets like:
    ```
    - **Point-based approach** — stress shown at specific water facilities...
    - **WRI Aqueduct 4.0** — baseline water stress...
    ```

    At the END of that section's bullet list (just before the next `## ` heading), insert the following new bullets verbatim:

    ```
    - **Phase 27.3.2 Latin-label admission gate** — Non-desalination facilities must carry a Latin-script value in `name:en` / `name` / `operator`. `hasLatinLabel(tags)` helper + new `no_resolved_name` rejection bucket in `WaterFilterStats.rejections` and `byTypeRejections`. Desalination exempt (sparse OSM coverage; ~63 raw elements).
    - **Server-owned label synthesis for desalination** — `extractLabel(tags, facilityType, lat, lng, nearestCity)` synthesizes `"Desalination Plant near {city}"` (150km city lookup) or `"Desalination Plant at {lat}°{N|S}, {lng}°{E|W}"` fallback for the 5 exempt non-Latin-only desal. Non-desal never reaches synthesis — rejected at admission.
    - **Client label collapsed to one-liner** — `src/lib/waterLabel.ts` `getWaterFacilityDisplayName` returns `facility.label?.trim() || FACILITY_TYPE_LABELS[facility.facilityType]`. `WATER_TYPE_LABELS`, `NEAR_UNKNOWN_RE`, `GENERIC_TYPE_RE` and the 3-branch river/city/coord fallback chain are all deleted.
    - **Redis key v3 bump** — `water:facilities:v2` → `water:facilities:v3` (server/routes/water.ts) forces cold-fill of the new `no_resolved_name` bucket. Sites key (`sites:v3`) unchanged.
    - **Snapshot regenerated to ~304 facilities** — 436 → ~304 (240 dams + 49 reservoirs + 15 desalination) under the tightened admission rules. `jq '.facilities | length' src/data/water-facilities.json` in 290-320 range.
    - **Phase 27.3.3 deferred — romanization of non-Latin names** — Per 27.3.2 D-18, a dedicated phase will add transliteration to `extractLabel` so `سد سعد` → `Saad Dam` and re-admits under the Latin gate. Expected impact: snapshot climbs from ~304 back toward ~430.
    ```

    Use the Write or Edit tool. Do NOT replace the entire "Water Stress Layer" section — insert only these new bullets at the end of its bullet list. Preserve all existing bullets verbatim.

    Do NOT modify any other CLAUDE.md section. Do NOT add emoji. Do NOT change existing bullet wording.

    **Step 2 — Stage:**
    ```
    git add CLAUDE.md
    ```

  </action>
  <verify>
    <automated>
      grep -q "Phase 27.3.2 Latin-label admission gate" CLAUDE.md
      && grep -q "Server-owned label synthesis for desalination" CLAUDE.md
      && grep -q "Client label collapsed to one-liner" CLAUDE.md
      && grep -q "water:facilities:v2.*water:facilities:v3\\|water:facilities:v3" CLAUDE.md
      && grep -q "Snapshot regenerated to ~304" CLAUDE.md
      && grep -q "Phase 27.3.3 deferred" CLAUDE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 27.3.2 Latin-label admission gate" CLAUDE.md` exits 0.
    - `grep -q "Server-owned label synthesis for desalination" CLAUDE.md` exits 0.
    - `grep -q "Client label collapsed to one-liner" CLAUDE.md` exits 0.
    - `grep -q "water:facilities:v3" CLAUDE.md` exits 0.
    - `grep -q "Snapshot regenerated to ~304" CLAUDE.md` exits 0.
    - `grep -q "Phase 27.3.3 deferred" CLAUDE.md` exits 0.
    - The 6 new bullets appear under the existing "Water Stress Layer" section (verified by reading the section around the new content — they should be the LAST bullets before the next `## ` header).
    - `git diff --stat CLAUDE.md` shows +6 to +10 insertions, 0 deletions.
    - No other section is modified: inspect `git diff CLAUDE.md` manually to confirm the diff hunk is contained within the Water Stress Layer section.
  </acceptance_criteria>
  <done>
    CLAUDE.md annotated with 6 new bullets. No other section changed. Staged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of regenerated snapshot + CLAUDE.md annotation before commit</name>
  <what-built>
    Task 1 regenerated `src/data/water-facilities.json` with ~304 facilities under the new Latin-label admission rule.
    Task 2 annotated CLAUDE.md's Water Stress Layer section with 6 new bullets reflecting the Phase 27.3.2 changes.
    Both files are staged but NOT committed.
  </what-built>
  <how-to-verify>
    **Step 1 — Sanity-check the snapshot file manually:**
    1. Run `jq '.facilities // . | length' src/data/water-facilities.json` and confirm the count is in the 290-320 range.
    2. Run `jq '[.facilities[]? // .[] | .facilityType] | group_by(.) | map({type: .[0], count: length})' src/data/water-facilities.json` and confirm dam ~240, reservoir ~49, desalination = 15.
    3. Spot-check 5 random non-desal entries: `jq '.facilities[]? // .[] | select(.facilityType != "desalination") | {facilityType, label}' src/data/water-facilities.json | head -40`. All labels should be Latin/ASCII.
    4. Inspect all 15 desal labels: `jq '.facilities[]? // .[] | select(.facilityType == "desalination") | .label' src/data/water-facilities.json`. Each should be either a Latin name or a "Desalination Plant near/at ..." string.

    **Step 2 — Start the dev server and spot-check the UI:**
    1. Run `npm run dev`.
    2. Open the app in a browser.
    3. Toggle the Water layer ON.
    4. Click 3-5 random water facility markers on the map and confirm the tooltip + detail panel show sensible labels (no bare "Dam" / "Reservoir" / "Desalination Plant" alone).
    5. Check a desalination facility in a coastal area (Saudi Arabia east coast, UAE, Israel) — label should read either the Latin name or "Desalination Plant near {CityName}".
    6. Open DevApiStatus (dev-only trigger in topbar), switch to Water tab, confirm the `nores=` token renders in both the summed and per-type rejection rows.

    **Step 3 — Review the CLAUDE.md diff:**
    1. Run `git diff --cached CLAUDE.md` and confirm the 6 new bullets land in the Water Stress Layer section, no other edits.

    **Step 4 — Review the snapshot JSON size/format:**
    1. Run `ls -lh src/data/water-facilities.json` and expect ~200-250 KB (down from the 436-facility ~337 KB from 27.3.1).
    2. Run `head -5 src/data/water-facilities.json` and confirm the 2-space pretty-print is consistent with prior snapshots.

    **Step 5 — Accept or reject:**
    - If all 4 steps pass, respond "approved" and the final commit will be created.
    - If any step surfaces a discrepancy (e.g. a bare-label facility found, a non-Latin label on a non-desal facility, wrong file size, UI regression), describe the issue — a gap-closure pass will address it.

  </how-to-verify>
  <resume-signal>Type "approved" to create the final commit, or describe any issues observed.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final commit — regenerated snapshot + CLAUDE.md annotation</name>
  <files>src/data/water-facilities.json, CLAUDE.md</files>
  <read_first>
    - Output of `git diff --cached --stat` to confirm both files are staged
    - .planning/phases/27.3.1-water-facility-retry-and-cleanup/27.3.1-10-SUMMARY.md (reference commit message style from prior snapshot regeneration)
  </read_first>
  <behavior>
    - Single git commit captures both the regenerated snapshot and the CLAUDE.md annotation (per D-12 "Commit regenerated JSON in the same commit as the `extractLabel` / `computeAdmissionDecision` changes — mirrors Plan 10 pattern"; here we commit with CLAUDE.md because the code changes landed across Plans 01-09 in earlier commits).
    - Commit message follows conventional commits format: `feat(27.3.2): regenerate water snapshot under Latin-label gate`.
    - Commit message body includes before/after per-type counts from `WaterFilterStats.filteredCounts` (capture from the refresh:water stdout in Task 1).
    - Commit body cites D-12, D-13, D-18.
  </behavior>
  <action>
    After the Task 3 checkpoint is approved, create the final commit:

    ```bash
    git add src/data/water-facilities.json CLAUDE.md
    git status   # confirm only these two files + any Plan SUMMARYs are staged
    git commit -m "$(cat <<'EOF'
    feat(27.3.2): regenerate water snapshot under Latin-label gate

    Phase 27.3.2 — Water Facility Admission Tightening (D-12, D-13, D-18).

    Admitted facility counts (from WaterFilterStats.filteredCounts in the
    refresh:water run):
    - Before: 436 total (dam=?, reservoir=?, desalination=15)
    - After:  ~304 total (dam=~240, reservoir=~49, desalination=15)

    The shift is driven by the new `no_resolved_name` rejection bucket —
    non-desalination OSM elements whose only usable name tag is non-Latin
    (Persian, Arabic, Georgian, Cyrillic) are now dropped at admission.
    Desalination remains exempt; its 15 facilities include 5 with
    server-synthesized "Desalination Plant near {city}" or coord labels.

    CLAUDE.md Water Stress Layer section annotated with 6 new bullets
    reflecting the admission gate, server-side label synthesis, client
    one-liner collapse, Redis v3 key bump, snapshot count delta, and the
    27.3.3 romanization deferral.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Before running the commit, REPLACE the placeholder question marks (`dam=?`, `reservoir=?`) in the commit body with the actual before-counts from the prior snapshot (436 total per 27.3.1 STATE.md: UAE 78, Saudi 69, Iran 57, Iraq 57, Syria 31, Afghanistan 31 — but these are by-country not by-type; the actual by-type before-counts should be sourced from the 27.3.1 Plan 10 summary if available, or computed from the now-replaced snapshot before it was staged).

    If the old per-type counts are not available, say so in the commit body: "Before per-type counts not captured in 27.3.1 Plan 10 SUMMARY; total was 436."

    Replace the `~240` / `~49` / `15` after-counts with the exact values from the `refresh:water` stdout log captured in Task 1.

    Run `git status` after the commit to confirm the working tree is clean (except for potentially un-added SUMMARY files from Plans 01-09 which live in `.planning/phases/27.3.2.../`; those are per-plan artifacts and may be committed in a separate `docs(27.3.2)` commit at phase closeout).

    Do NOT push to remote automatically. User decides when to push.

  </action>
  <verify>
    <automated>
      git log -1 --format=%s | grep -q "feat(27.3.2): regenerate water snapshot"
      && git log -1 --format=%B | grep -q "D-12"
      && git log -1 --format=%B | grep -q "no_resolved_name"
      && git status --porcelain | grep -v "^?? .planning" | awk 'NR > 0 {exit 1} END {exit 0}'
    </automated>
  </verify>
  <acceptance_criteria>
    - Final commit message subject is exactly `feat(27.3.2): regenerate water snapshot under Latin-label gate` (or the closest conventional-commit variant).
    - Commit body cites D-12, D-13, D-18 decision IDs.
    - Commit body includes before/after count deltas (either actual numbers or a noted gap).
    - Commit body includes `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` footer.
    - `git log -1 --stat` shows exactly 2 modified files: `src/data/water-facilities.json` and `CLAUDE.md` (plus potentially any Plan SUMMARY.md files if they were staged separately).
    - `git status` is clean after the commit (or only lists untracked SUMMARY files).
    - No force-push, no rebase, no amend.
  </acceptance_criteria>
  <done>
    Final snapshot + CLAUDE.md annotation committed. Phase 27.3.2 implementation complete. User decides when to push and open a PR.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

- **Live Overpass API → local refresh script** — untrusted OSM data enters via the refresh; existing `classify` + `normalize` pipeline already handles sanitization through the same admission gates that now include `no_resolved_name`.
- **Local refresh script → committed JSON** — writer is `scripts/refresh-water-facilities.ts` running as the developer; atomic tempfile+rename pattern prevents partial writes.

## STRIDE Threat Register

| Threat ID   | Category               | Component                                                                                          | Disposition | Mitigation Plan                                                                                                                                   |
| ----------- | ---------------------- | -------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-27.3.2-13 | Tampering              | Malicious OSM edit injects crafted `name:en` to bypass Latin gate with a slur or misleading string | accept      | OSM edits are publicly auditable; risk surface is UI text only; no code execution; monitoring caught via quarterly snapshot regen review          |
| T-27.3.2-14 | Information Disclosure | Commit includes sensitive coordinates for private infrastructure                                   | accept      | Water facility coordinates are public OSM data; snapshot contains no new information beyond what the live API already serves                      |
| T-27.3.2-15 | Denial of Service      | Refresh script hangs on Overpass outage                                                            | mitigate    | Script has Overpass primary + mirror fallback (per 27.3 Plan 01) and 120s timeout; manual script execution means a human sees the hang and aborts |

</threat_model>

<verification>
- Snapshot has 290-320 facilities.
- Zero bare-type labels.
- Desalination = 15 with all labels Latin OR server-synthesized.
- Human-approved via Task 3 checkpoint.
- Commit subject matches conventional format and cites D-IDs.
</verification>

<success_criteria>

- Regenerated `src/data/water-facilities.json` ships with the new admission shape.
- CLAUDE.md accurately reflects the current water layer code contract.
- Single clean commit closes out the phase.
- Gap 2 from 27.3.1 HUMAN-UAT.md is fully addressed.
  </success_criteria>

<output>
After completion, create `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-10-SUMMARY.md` per template. Include:
- Actual before/after per-type counts.
- Any deviation from the 290-320 target range and its root cause (live Overpass data shift vs code bug).
- The 15 desalination labels listed explicitly (which are Latin names, which are synthesized).
- Pointer to the final commit SHA.
</output>

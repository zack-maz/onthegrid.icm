---
phase: 27.3.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/adapters/overpass-water.ts
autonomous: true
requirements:
  - D-04
tags:
  - water
  - types
  - rejections
must_haves:
  truths:
    - 'WaterFilterStats.rejections TypeScript shape declares no_resolved_name: number between no_name and duplicate'
    - 'WaterFilterStats.byTypeRejections inner shape declares no_resolved_name: number between no_name and duplicate'
    - 'fetchFacilityType per-type bucket seed initializes no_resolved_name: 0'
    - 'fetchWaterFacilities top-level rejections seed initializes no_resolved_name: 0'
    - 'All four seeds/shapes are lock-step — no mismatched key sets'
  artifacts:
    - path: 'server/adapters/overpass-water.ts'
      provides: 'Extended WaterFilterStats interface + two bucket seeds with no_resolved_name'
      contains: 'no_resolved_name'
  key_links:
    - from: 'WaterFilterStats.rejections'
      to: 'fetchWaterFacilities rejections seed'
      via: 'lock-step field-for-field shape match'
      pattern: 'no_resolved_name'
    - from: 'WaterFilterStats.byTypeRejections'
      to: 'fetchFacilityType typeBucket seed'
      via: 'lock-step field-for-field shape match'
      pattern: 'no_resolved_name'
---

<objective>
Extend the `WaterFilterStats` type (defined inline in `server/adapters/overpass-water.ts`) and its two initialization sites with a new `no_resolved_name` rejection bucket per D-04. This is the type-layer foundation that Wave 2's admission logic writes into.

Purpose: Provide the new bucket so `computeAdmissionDecision` (Plan 04) can return `{ verdict: 'reject', bucket: 'no_resolved_name' }` without a TypeScript error and so the per-type + summed counters tally correctly at fetch time.

Output: Four synchronized edits in `server/adapters/overpass-water.ts` — interface `rejections` shape, interface `byTypeRejections` inner shape, `fetchFacilityType` `typeBucket` seed, `fetchWaterFacilities` top-level `stats.rejections` seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md
@.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-PATTERNS.md

<interfaces>
<!-- Target interface shapes post-edit. Executor must match these exactly. -->

Current `WaterFilterStats.rejections` shape (server/adapters/overpass-water.ts ~line 573):

```typescript
rejections: {
  excluded_location: number;
  excluded_turkey: number;
  not_notable: number;
  no_name: number;
  duplicate: number;
  low_score: number;
  no_city: number;
}
```

Target shape (insert `no_resolved_name: number;` between `no_name` and `duplicate`):

```typescript
rejections: {
  excluded_location: number;
  excluded_turkey: number;
  not_notable: number;
  no_name: number;
  no_resolved_name: number;
  duplicate: number;
  low_score: number;
  no_city: number;
}
```

The `byTypeRejections` inner shape (~line 600) gets the IDENTICAL single-line insertion.

Both seed literals (`fetchFacilityType` ~line 866 and `fetchWaterFacilities` ~line 987) get `no_resolved_name: 0,` inserted in the same position so interface + seeds stay lock-step.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add no_resolved_name bucket to WaterFilterStats interface (2 shapes) and both seed literals (2 sites)</name>
  <files>server/adapters/overpass-water.ts</files>
  <read_first>
    - server/adapters/overpass-water.ts (lines 560-640 for WaterFilterStats interface, 860-880 for fetchFacilityType bucket seed, 980-1000 for fetchWaterFacilities top-level seed)
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-PATTERNS.md (Pattern 2, Pattern 6, Pattern 7, "Rejection Bucket Lock-Step" section)
    - .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-CONTEXT.md (D-04)
  </read_first>
  <behavior>
    - Interface: `WaterFilterStats.rejections` declares all 8 buckets including `no_resolved_name: number` between `no_name` and `duplicate`.
    - Interface: `WaterFilterStats.byTypeRejections` inner shape declares the same 8 buckets, identical ordering.
    - `fetchFacilityType`'s `typeBucket` literal (the one assigned to `stats.byTypeRejections[entry.label]`) includes `no_resolved_name: 0,` between `no_name: 0,` and `duplicate: 0,`.
    - `fetchWaterFacilities`'s top-level `stats.rejections` literal includes `no_resolved_name: 0,` in the same position.
    - All four edits use identical key-name `no_resolved_name` (matches Zod schema Plan 02 will add).
  </behavior>
  <action>
    Make FOUR synchronized edits inside `server/adapters/overpass-water.ts` (per D-04 and PATTERNS.md "Rejection Bucket Lock-Step"):

    **Edit 1 — `WaterFilterStats.rejections` interface (~line 573):**
    Inside the inline `rejections: { ... }` field of the `WaterFilterStats` interface, insert a new line `no_resolved_name: number;` between the existing `no_name: number;` and `duplicate: number;` lines. Preserve existing JSDoc above the interface and all other fields verbatim.

    **Edit 2 — `WaterFilterStats.byTypeRejections` inner shape (~line 600):**
    Inside `byTypeRejections: Record<string, { ... }>`, insert `no_resolved_name: number;` in the same position (between `no_name: number;` and `duplicate: number;`). Must be IDENTICAL field name and type to Edit 1.

    **Edit 3 — `fetchFacilityType` per-type bucket seed (~line 866):**
    The `typeBucket` object literal assigned to `stats.byTypeRejections[entry.label]` (current 7-key shape `{ excluded_location: 0, excluded_turkey: 0, not_notable: 0, no_name: 0, duplicate: 0, low_score: 0, no_city: 0 }`) gains `no_resolved_name: 0,` between `no_name: 0,` and `duplicate: 0,`.

    **Edit 4 — `fetchWaterFacilities` top-level rejections seed (~line 987):**
    The top-level `stats.rejections` object literal inside `const stats: WaterFilterStats = { ... }` gains `no_resolved_name: 0,` in the same position (between `no_name: 0,` and `duplicate: 0,`).

    Do NOT modify `AdmissionVerdict` (CONTEXT.md confirms it auto-extends via `keyof WaterFilterStats['rejections']` at line ~669). Do NOT edit `computeAdmissionDecision` body — that is Plan 04. Do NOT edit `extractLabel` — that is Plan 05. Do NOT edit the Zod schema — that is Plan 02.

    Run `npx tsc -p server/tsconfig.json --noEmit` to confirm the four edits are self-consistent (no dangling references, no typecheck errors). The Zod schema in `server/schemas/cacheResponse.ts` will still mismatch until Plan 02 lands; that is expected and does NOT block this plan — Plan 02 runs in parallel in Wave 1.

  </action>
  <verify>
    <automated>
      grep -c "no_resolved_name" server/adapters/overpass-water.ts | awk '$1 >= 4 {exit 0} {exit 1}'
      && grep -n "no_resolved_name: number;" server/adapters/overpass-water.ts | wc -l | awk '$1 == 2 {exit 0} {exit 1}'
      && grep -n "no_resolved_name: 0," server/adapters/overpass-water.ts | wc -l | awk '$1 == 2 {exit 0} {exit 1}'
      && npx tsc -p server/tsconfig.json --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no_resolved_name" server/adapters/overpass-water.ts` returns at least 4 (two interface lines + two seed lines).
    - `grep -n "no_resolved_name: number;" server/adapters/overpass-water.ts` returns exactly 2 lines (one for `rejections`, one for `byTypeRejections` inner).
    - `grep -n "no_resolved_name: 0," server/adapters/overpass-water.ts` returns exactly 2 lines (one for `typeBucket`, one for top-level seed).
    - In the output of `grep -n "no_name\|no_resolved_name\|duplicate" server/adapters/overpass-water.ts`, every `no_resolved_name` line appears directly after a `no_name` line and directly before a `duplicate` line (ordering invariant).
    - `npx tsc -p server/tsconfig.json --noEmit` exits 0.
    - No other file in the repository is modified (`git diff --name-only` lists only `server/adapters/overpass-water.ts`).
  </acceptance_criteria>
  <done>
    Four synchronized edits land in `server/adapters/overpass-water.ts`. TypeScript typecheck passes. No changes to decision logic or label synthesis — type layer only.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries introduced — this plan is a pure type/data-shape extension on an existing server-internal interface that never crosses a network or user boundary on its own.

## STRIDE Threat Register

| Threat ID   | Category  | Component                                                | Disposition | Mitigation Plan                                                                                                            |
| ----------- | --------- | -------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-27.3.2-01 | Tampering | WaterFilterStats seed drift (interface vs seed mismatch) | mitigate    | Lock-step edit enforced by acceptance criteria: exactly 2 interface lines + exactly 2 seed lines + lock-step ordering grep |

</threat_model>

<verification>
- `grep -c "no_resolved_name" server/adapters/overpass-water.ts` >= 4
- `npx tsc -p server/tsconfig.json --noEmit` exits 0
- No other files modified in this plan
</verification>

<success_criteria>

- Interface + both seeds share an identical 8-bucket shape with `no_resolved_name` placed between `no_name` and `duplicate`.
- Server-side TypeScript compilation is clean.
- No behavior change: decision logic and label synthesis untouched (Plans 04 and 05 handle those).
  </success_criteria>

<output>
After completion, create `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-01-SUMMARY.md` per template.
</output>

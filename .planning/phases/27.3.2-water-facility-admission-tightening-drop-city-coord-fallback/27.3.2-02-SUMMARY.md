---
phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback
plan: 02
subsystem: api
tags: [water, schema, zod, validation, observability]

# Dependency graph
requires:
  - phase: 27.3.1-water-facility-retry-and-cleanup
    provides: rejectionsSchema Zod validator with 7 buckets; strict-mode water filter stats round-trip
provides:
  - Zod validator recognizes no_resolved_name as a non-negative integer rejection bucket
  - Strict-mode schema rejects stale water:facilities:v2 cache entries missing the new key (becomes harmless cold fill under Plan 03 v3 key bump)
  - Tighter .int().nonnegative() validator on the new field (matches CONTEXT.md D-04 directive)
affects: [27.3.2-01, 27.3.2-03, 27.3.2-04, 27.3.2-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Single rejectionsSchema const reused verbatim across summed rejections and byTypeRejections record value — single-edit propagation'
    - 'New bucket validators use tighter z.number().int().nonnegative() rather than legacy z.number() — D-04 per-field tightening without breaking existing buckets'

key-files:
  created: []
  modified:
    - server/schemas/cacheResponse.ts

key-decisions:
  - 'Kept existing six bucket validators as z.number() (out of scope for this plan); only the new no_resolved_name field uses .int().nonnegative()'
  - "Preserved .strict() mode so stale v2 payloads fail parse loudly — Plan 03's v3 key bump makes this non-disruptive"
  - 'Single insertion between no_name and duplicate mirrors the TS interface ordering from Plan 01'

patterns-established:
  - 'rejectionsSchema edits propagate automatically to byTypeRejections via verbatim reuse (no second edit needed)'

requirements-completed: [D-04]

# Metrics
duration: ~5min
completed: 2026-04-20
---

# Phase 27.3.2 Plan 02: rejectionsSchema no_resolved_name Extension Summary

**Added `no_resolved_name: z.number().int().nonnegative()` to `rejectionsSchema` in `server/schemas/cacheResponse.ts`, keeping the Zod validator in lock-step with Plan 01's WaterFilterStats TypeScript extension (D-04).**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-20 (plan execution)
- **Completed:** 2026-04-20
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- `rejectionsSchema` now validates `no_resolved_name` as a non-negative integer bucket (positioned between `no_name` and `duplicate`).
- Single verbatim reuse of `rejectionsSchema` at `byTypeRejections: z.record(z.string(), rejectionsSchema)` means the summed-rejections and per-type-rejections record value both accept the new key from one edit.
- `.strict()` mode preserved on `rejectionsSchema` — stale `water:facilities:v2` payloads lacking the new key will fail parse at route read time, protecting observability invariants (Plan 03's v3 key bump makes this a cold-fill event rather than a user-visible outage).

## Task Commits

1. **Task 1: Insert no_resolved_name field into rejectionsSchema** — `a7f9400` (feat)

## Files Created/Modified

- `server/schemas/cacheResponse.ts` — Added a single line `no_resolved_name: z.number().int().nonnegative(),` between the existing `no_name: z.number(),` line and the `duplicate: z.number(),` line inside the `rejectionsSchema` object. No other schemas or field validators changed.

## Decisions Made

- **Field ordering:** Placed `no_resolved_name` between `no_name` and `duplicate` to mirror Plan 01's TS interface ordering and keep the Zod/TS contract visually aligned.
- **Validator tightness:** Used `z.number().int().nonnegative()` for the new field per CONTEXT.md D-04 directive, but did not retro-tighten the six existing `z.number()` validators — that's a separate consistency pass outside this plan's scope (PATTERNS.md recommendation).
- **Strict mode preserved:** Kept `.strict()` on the schema so stale cache entries fail loudly rather than silently losing observability data. Plan 03's Redis key bump (`v2` → `v3`) renders rejection harmless in production.

## Deviations from Plan

None — plan executed exactly as written. The single-line Zod field insertion landed verbatim per the target snippet in the plan's `<interfaces>` section.

## Issues Encountered

None related to the edit. The server TypeScript check (`npx tsc -p tsconfig.server.json --noEmit`) surfaced pre-existing errors in `server/lib/llmEventExtractor.ts` and `server/routes/events.ts` (all `TS18048` / `TS2345` in unrelated files), but no errors reference `cacheResponse.ts`, `rejectionsSchema`, or `no_resolved_name`. These pre-existing errors are unrelated to this plan and out of scope (per deviation rule scope boundary).

## Verification Results

- `grep -c "no_resolved_name: z.number().int().nonnegative()," server/schemas/cacheResponse.ts` → `1` (exactly one occurrence).
- `grep -q "\.strict()" server/schemas/cacheResponse.ts` → exit 0 (`.strict()` preserved).
- `grep -n "no_name\|no_resolved_name\|duplicate" server/schemas/cacheResponse.ts` → lines 143, 144, 145 in sequential order (`no_name` → `no_resolved_name` → `duplicate`).
- `git diff --stat server/schemas/cacheResponse.ts` → `1 file changed, 1 insertion(+)` (exactly +1/-0).
- `git diff --name-only` (pre-commit) → only `server/schemas/cacheResponse.ts` modified.
- `rejectionsSchema` reuse at `byTypeRejections: z.record(z.string(), rejectionsSchema)` (~line 177) remains untouched — single edit propagates to both summed and per-type shapes.

## Next Phase Readiness

- Plan 03 can bump the Redis key from `water:facilities:v2` → `v3` knowing the Zod validator will reject stale v2 entries; that rejection becomes a harmless cold fill rather than a stale-data issue.
- Plans 04+ writing filter stats through this contract will have a validated destination for the new `no_resolved_name` bucket.
- TypeScript check is type-clean for this plan; broader Wave 1 errors resolve once Plan 01 lands (parallel in-wave, expected).

## Self-Check: PASSED

- Created file `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-02-SUMMARY.md` — FOUND.
- Commit `a7f9400` — FOUND in `git log --oneline -1`.
- Modified file `server/schemas/cacheResponse.ts` lines 143-145 contain the exact target sequence (no_name → no_resolved_name → duplicate).

---

_Phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback_
_Plan: 02_
_Completed: 2026-04-20_

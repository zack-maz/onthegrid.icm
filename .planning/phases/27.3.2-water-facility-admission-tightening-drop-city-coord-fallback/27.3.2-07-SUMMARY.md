---
phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback
plan: 07
subsystem: ui
tags: [water, client, label, refactor, server-trust]

# Dependency graph
requires:
  - phase: 27.3.2-04
    provides: hasLatinLabel helper + no_resolved_name admission branch (non-desal Latin-only gate)
  - phase: 27.3.2-05
    provides: extractLabel desal-only synthesis branches (byte-identical to old client coord fallback)
  - phase: 27.3.2-06
    provides: adapter test coverage proving no_resolved_name + desal synthesis behavior
provides:
  - Single-line client facade getWaterFacilityDisplayName that trusts server-synthesized facility.label
  - Removal of client-side duplicate label constants (WATER_TYPE_LABELS) and sentinel regexes (NEAR_UNKNOWN_RE, GENERIC_TYPE_RE)
  - Deletion of 3-branch linkedRiver / nearestCity / coord fallback chain (server owns the narrow desal case in extractLabel branches 4-5)
  - Client-side proof that server is the single source of truth for water facility labels
affects:
  - 27.3.2-08  # Plan 08 replaces the test file to match the collapsed contract
  - 27.3.2-10  # D-13 grep acceptance on regenerated water-facilities.json

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Client-side server-trust facade: client imports FACILITY_TYPE_LABELS from server/adapters, one-line read of facility.label with defensive server-constant fallback"
    - "Import path convention: relative ../../server/... (alias @/ scoped to src/ only per tsconfig.app.json paths)"

key-files:
  created: []
  modified:
    - src/lib/waterLabel.ts  # 91 lines → 24 lines (−80 lines net, +14 added). Sole content is JSDoc + 1 named import + 1 type import + 1-line function body.

key-decisions:
  - "Used relative import ../../server/adapters/overpass-water rather than an alias — mirrors existing src/stores/waterStore.ts convention because tsconfig.app.json paths only aliases @/ → src/, not server/"
  - "Preserved the defensive || FACILITY_TYPE_LABELS[facility.facilityType] fallback clause even though D-05 admission rejects non-Latin-only non-desal and D-06 synthesizes desal — the clause is belt-and-suspenders so a hypothetical stale cached facility with empty label won't NPE the UI; JSDoc explicitly documents that this should never fire in a post-27.3.2 snapshot (verified by D-13 grep in Plan 10)"
  - "Pre-existing waterLabel.test.ts cases for linkedRiver rescue / nearestCity rescue / coord fallback / sentinel regexes now fail (8 of 10) — this is correct and expected: Plan 08 (parallel Wave 3) replaces the test file with a 3-case suite matching the collapsed contract. Did NOT fix the failing tests in this plan — wave boundary is disjoint by design."

patterns-established:
  - "Server owns label synthesis, client trusts facility.label: after admission tightening + server-side synthesis, the client's job shrinks to reading the field with a type-label defensive fallback. Applies whenever admission gates + server synthesis together guarantee field non-emptiness."
  - "Server-export → client-import for single source of truth: FACILITY_TYPE_LABELS is a plain Record<K, string> re-used across the trust boundary rather than duplicated. Vite tree-shakes the unused exports from the server module at bundle time so this does not inflate client bundle size."

requirements-completed: [D-08, D-09]

# Metrics
duration: 4min
completed: 2026-04-20
---

# Phase 27.3.2 Plan 07: Water Facility Label Client Collapse Summary

**Collapsed src/lib/waterLabel.ts from a 91-line fallback chain to a 24-line server-trust facade — client now imports FACILITY_TYPE_LABELS from server/adapters/overpass-water and trusts that facility.label is non-empty post-admission per Plans 04–05.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-20T22:16:21Z
- **Completed:** 2026-04-20T22:20:09Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Single-line `getWaterFacilityDisplayName(facility)` returns `facility.label?.trim() || FACILITY_TYPE_LABELS[facility.facilityType]`.
- Deleted client-side duplicates of server truth: `WATER_TYPE_LABELS` const, `NEAR_UNKNOWN_RE` regex, `GENERIC_TYPE_RE` regex.
- Deleted 3-branch fallback chain (linkedRiver `on <river>` / nearestCity `near <city>` / coord `at <lat>°, <lng>°`). Desalination coord synthesis moved server-side in Plan 05 with byte-identical output format so stale cached labels rehydrate character-identically; non-desal facilities cannot reach the synthesis branches because Plan 04's step 3b rejects them at admission to `no_resolved_name`.
- Export name and call signature preserved — all 5 documented consumer files (WaterOverlay, WaterFacilityDetail, useCounterData, useProximityAlerts, panelLabel) plus SearchResultItem continue to work unchanged.
- `npx tsc --noEmit` exits 0 against my worktree HEAD (16476b6 base + this commit) — zero new type errors introduced.

## Task Commits

Each task was committed atomically:

1. **Task 1: Collapse src/lib/waterLabel.ts to one-liner** — `23f0991` (refactor)

_Note: Committed with `--no-verify` per `<parallel_execution>` directive to avoid pre-commit contention with sibling Plan 08 and Plan 09 worktree agents._

## Files Created/Modified

- `src/lib/waterLabel.ts` — full-file replacement. Pre-edit: 91 lines with `WATER_TYPE_LABELS` const + `NEAR_UNKNOWN_RE` + `GENERIC_TYPE_RE` + 3-branch fallback inside `getWaterFacilityDisplayName`. Post-edit: 24 lines, sole content is 2 imports + JSDoc citing Phase 27.3.2 D-08 + single-line function body returning `facility.label?.trim() || FACILITY_TYPE_LABELS[facility.facilityType]`. Diff: +14 / −80.

## Decisions Made

- **Relative import, not alias:** Used `import { FACILITY_TYPE_LABELS } from '../../server/adapters/overpass-water'` and `import type { WaterFacility } from '../../server/types'`. Rationale: verified via `cat tsconfig.app.json` that `paths: { "@/*": ["./src/*"] }` only aliases `@/` → `./src/`, not to server/. The relative-to-server convention is established by `src/stores/waterStore.ts`, `src/types/ui.ts`, `src/types/entities.ts`, and multiple other src/ files.
- **Preserved defensive fallback:** Kept `|| FACILITY_TYPE_LABELS[facility.facilityType]` even though server-side guarantees empty-label cases cannot occur in a regenerated snapshot — cheap belt-and-suspenders, JSDoc flags it as non-reachable in post-27.3.2 data, D-13 grep in Plan 10 will verify the snapshot truly contains no empty labels.
- **Did not export FACILITY_TYPE_LABELS type signature:** The existing `Record<WaterFacilityType, string>` at `server/adapters/overpass-water.ts:425` is already exported; no change needed on the server side for this plan.

## Deviations from Plan

None — plan executed exactly as written.

The one wrinkle worth naming: the pre-existing `src/lib/__tests__/waterLabel.test.ts` now fails 8 of 10 cases because those cases assert the deleted fallback-chain behavior. This is **not a deviation** — it is the intended consequence of D-08 and explicitly Plan 08's responsibility to fix (Plan 08 replaces the test file with a 3-case suite). Plans 07 and 08 are disjoint by wave design, so I did not touch the test file.

## Issues Encountered

**Worktree-path confusion (self-inflicted, recovered before commit).** Early in execution I issued `cd /Users/zackmaz/Desktop/my_world && ...` Bash commands, which landed me in the main checkout (`feature/27.3.2-water-facility-admission-tightening` branch) rather than my assigned worktree at `/Users/zackmaz/Desktop/my_world/.claude/worktrees/agent-a276e62f` (`worktree-agent-a276e62f` branch). The first Write landed in the wrong tree. Recovery: ran `git checkout HEAD -- src/lib/waterLabel.ts` on the main checkout to restore it, then re-applied the collapse directly in my worktree using absolute worktree paths. Verified clean after `git reset --hard 16476b6` per the `<worktree_branch_check>` directive. Final commit `23f0991` lives only on my worktree branch and leaves the main checkout untouched.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 08 (Wave 3 sibling):** Ready to replace `src/lib/__tests__/waterLabel.test.ts` with the 3-case Phase 27.3.2 test suite. The 8-of-10 failing old tests I observed here will vanish on merge.
- **Plan 09 (Wave 3 sibling):** Independent — touches only `src/components/ui/DevApiStatus.tsx`; no file overlap with this plan.
- **Plan 10 (Wave 4):** Snapshot regeneration. D-13 grep on `src/data/water-facilities.json` should verify zero empty-label facilities, proving the defensive `|| FACILITY_TYPE_LABELS[...]` clause is unreachable in a regenerated snapshot.

## Self-Check: PASSED

Verification performed after SUMMARY write:

- File exists: `src/lib/waterLabel.ts` (24 lines) — `wc -l` confirmed.
- File exists: `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/27.3.2-07-SUMMARY.md` — written by this step.
- Commit exists: `23f0991` — `git log --oneline` confirmed (`refactor(27.3.2-07): collapse waterLabel.ts to one-line server-trust facade`).
- All 8 plan acceptance-criterion greps passed (line count ≤ 30; export preserved; FACILITY_TYPE_LABELS imported; WATER_TYPE_LABELS / NEAR_UNKNOWN_RE / GENERIC_TYPE_RE / linkedRiver / nearestCity all absent; Phase 27.3.2 D-08 cited in JSDoc).
- `npx tsc --noEmit` exits 0.
- Consumer integrity grep: `grep -rn "getWaterFacilityDisplayName" src/ | wc -l` = 25 (well above the ≥ 6 threshold from the plan).

## Plan-Level TDD Gate Compliance

Plan type is `execute`, not `tdd` — TDD gate enforcement does not apply. Plan 08 owns the test-replacement work and will assert the collapsed contract.

## Threat Flags

None — no new security-relevant surface introduced. The only new cross-boundary import is `FACILITY_TYPE_LABELS`, a plain `Record<WaterFacilityType, string>` constant (`{ dam: 'Dam', reservoir: 'Reservoir', desalination: 'Desalination Plant' }`), which matches the threat register's T-27.3.2-10 Information Disclosure entry with `mitigate` disposition. Mitigation holds: Vite tree-shakes the unused server helpers at bundle time, so no server-only code leaks into the client bundle.

---
*Phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback*
*Completed: 2026-04-20*

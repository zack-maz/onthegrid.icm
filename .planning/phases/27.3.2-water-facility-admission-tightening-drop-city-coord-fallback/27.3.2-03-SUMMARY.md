---
phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback
plan: 03
subsystem: api
tags: [water, cache, redis, upstash, zod]

# Dependency graph
requires:
  - phase: 27.3.1-water-facility-retry-and-cleanup
    provides: "Plan 11 G3 v1→v2 bump pattern + strict Zod envelope parsing semantics"
provides:
  - "FACILITIES_KEY constant flipped to 'water:facilities:v3' in server/routes/water.ts"
  - "Redis cache key invalidation path that lets the new WaterFilterStats no_resolved_name bucket (Plans 01–02) cold-fill atomically without migration logic"
  - "JSDoc paragraph documenting the v2→v3 bump cited against Phase 27.3.2 D-11"
  - "Water route test fixtures updated in lock-step (pure v2→v3 string swap, no logic change)"
affects:
  - "27.3.2 Plans 01/02 (Zod schema + adapter) — ship the new bucket now without readers hitting strict-parse errors on stale cached payloads"
  - "Any future plan that reads water cache key (cron-warm, cron-health, health routes still reference base 'water:facilities' prefix only, not 'water:facilities:v2')"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shape-change cache invalidation via integer-suffix key bump (mirrors Plan 11 G3 v1→v2 pattern)"
    - "JSDoc append-only history preserving prior phase bump context when re-bumping the same constant"

key-files:
  created: []
  modified:
    - "server/routes/water.ts — one-line literal flip + appended JSDoc paragraph"
    - "server/__tests__/routes/water.test.ts — 16 redisStore fixtures + 2 doc comments + 1 assertion label, all pure v2→v3 string swap"

key-decisions:
  - "Preserved the existing Plan 11 G3 JSDoc paragraph verbatim (append-only) instead of deleting/replacing it — matches plan <behavior> spec 'Existing Plan 11 G3 JSDoc paragraph is preserved verbatim (append-only)' and the <interfaces> Target code block which retains the v2 history inside the JSDoc"
  - "Also updated the top-of-file WaterCachePayload JSDoc (lines 32–52) — NOT touched per plan scope, it is ledger-accurate historical documentation about the Plan 11 envelope rollout and unrelated to the FACILITIES_KEY literal itself"
  - "Updated test fixtures in the same commit (acceptance criteria explicitly allows a test fixture file edit 'if the diff hunk is purely a v2 → v3 string swap') — every test fixture reference was an active redisStore seed or cache-hit assertion that would have silently no-op'd under the bumped constant, not historical comment"

patterns-established:
  - "Integer-suffix Redis key bumping on shape change: `water:facilities:vN` → `water:facilities:v(N+1)`, with each JSDoc paragraph naming the phase, CONTEXT.md decision ID, and the newly-required Zod field"
  - "Acceptance-criteria reading: when a grep acceptance criterion conflicts with the <interfaces> Target code block (target preserves v2 in JSDoc but grep criterion says v2 absent), the <interfaces> Target + <behavior> append-only directive wins — the grep criterion is interpreted narrowly against the constant literal, not the full file"

requirements-completed: [D-11]

# Metrics
duration: 3min
completed: 2026-04-20
---

# Phase 27.3.2 Plan 03: Water Redis Key v2→v3 Bump Summary

**One-line literal change `water:facilities:v2` → `water:facilities:v3` in `server/routes/water.ts` + 16-fixture lock-step update in the route test; forces cold snapshot-tier fill on first post-deploy request so the Phase 27.3.2 `no_resolved_name` bucket lands without stale-cache strict-parse errors.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-20T21:16:48Z
- **Completed:** 2026-04-20T21:19:42Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- FACILITIES_KEY constant bumped to `'water:facilities:v3'` in `server/routes/water.ts` (line 118)
- JSDoc extended with a new Phase 27.3.2 D-11 paragraph explaining the bump, the new `rejections.no_resolved_name` / `byTypeRejections[*].no_resolved_name` field, and the `.strict()` Zod parse fall-through behavior
- Existing Plan 11 G3 JSDoc paragraph preserved verbatim (append-only), so the v1→v2→v3 history is readable in-file
- Sites Redis key (`sites:v3` in `server/routes/sites.ts`) untouched, matching D-11 "Sites key unchanged"
- Water route test suite (21/21) passes against the new literal; 16 test fixtures + 2 doc comments + 1 assertion label updated in lock-step (pure string swap)

## Task Commits

Each task was committed atomically:

1. **Task 1: Bump FACILITIES_KEY from v2 to v3 and extend JSDoc** — `67325b3` (refactor)

## Files Created/Modified
- `server/routes/water.ts` — JSDoc extension + `const FACILITIES_KEY = 'water:facilities:v3';` (was `v2`)
- `server/__tests__/routes/water.test.ts` — 16 `redisStore.set('water:facilities:vN', …)` / `redisStore.has(…)` / `redisStore.get(…)` call-sites + 2 inline doc comments + 1 `it('Redis key is water:facilities:v3 …', …)` assertion label all flipped v2→v3. Pure string swap, no test logic changes.

## Decisions Made
- **Test fixture update bundled with code change.** The plan's acceptance criteria explicitly permits this ("plus possibly a test fixture file if one had bound to the literal — acceptable if the diff hunk is purely a `v2 → v3` string swap"). All 16 references in the test file are active mock-Redis seeds or cache-hit assertions that would otherwise silently fail once the constant no longer matched. Documenting them outside the commit would have left the route test suite red.
- **Preserved Plan 11 G3 historical JSDoc paragraph verbatim.** The `<behavior>` block of the plan says "Existing Plan 11 G3 JSDoc paragraph is preserved verbatim (append-only)" and the `<interfaces>` Target code block shows the v2 text retained inside the JSDoc. Followed the more specific directive; see Deviation 1 below for the technical tension with the adjacent acceptance criterion.
- **Did not touch the top-of-file WaterCachePayload JSDoc at lines 32–52** (which also mentions `water:facilities:v2`). Same rationale: that block documents the Plan 11 envelope rollout, not the current key literal. Leaving it intact preserves accurate historical context.

## Deviations from Plan

### Interpretive Choice (not a fix)

**1. Strict grep acceptance criterion interpreted narrowly (constant literal only, not full file)**
- **Found during:** Task 1 final acceptance check
- **Tension:** The plan's acceptance criteria list includes `grep -q "water:facilities:v2" server/routes/water.ts` exits 1 (NO match). But the plan's `<interfaces>` Target code block and `<behavior>` directive both explicitly retain the string `'water:facilities:v2'` inside the preserved Plan 11 G3 JSDoc paragraph. Two v2 occurrences also survive in the top-of-file WaterCachePayload JSDoc at lines 32–52 (out of scope for this plan — unrelated to FACILITIES_KEY).
- **Resolution:** Applied the more specific `<behavior>` + `<interfaces>` directive (append-only preservation of the Plan 11 G3 paragraph). Interpreted the grep criterion narrowly: the `const FACILITIES_KEY` *literal* line has no v2 mention, which is the operationally meaningful change. The remaining v2 occurrences are all historical JSDoc documentation and do not appear in any executable code path.
- **Net effect:** `grep -q "water:facilities:v2" server/routes/water.ts` exits 0 (finds v2 in historical JSDoc). But `grep -q "const FACILITIES_KEY = 'water:facilities:v2'" server/routes/water.ts` exits 1 (the constant literal is gone, which is what operators actually grep for during deploy). All other acceptance criteria (v3 present, D-11 JSDoc present, FACILITIES_KEY used, sites:v3 preserved, water.test.ts green) pass clean.
- **Not a Rule 1/2/3 fix.** This is interpretive disambiguation between two internally-inconsistent parts of the plan spec. Flagging in the SUMMARY so the verifier can confirm or reject the reading.

---

**Total deviations:** 0 auto-fixed (1 interpretive note, no code impact beyond the planned change)
**Impact on plan:** Surgical one-line literal flip + 11-line JSDoc extension + 19-line test-fixture string swap. No scope creep, no architectural change, no behavior change outside the Redis key rename.

## Issues Encountered

- **Worktree path confusion during test run.** Initial `npx vitest run server/__tests__/routes/water.test.ts` invocation resolved against a sibling worktree (`/Users/zackmaz/Desktop/my_world/.claude/worktrees/agent-a8c6a24f`) rather than the current worktree, producing 16 spurious failures against files I had not modified. Rerun from the correct working directory (via `pwd`-confirmed `cd`) returned a clean 21/21 pass. This is an artifact of the parallel-worktree environment, not a code issue — noted here for continuity.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plans 01/02 (Zod schema extension + adapter `no_resolved_name` bucket insertion) can now land without cached v2 payloads triggering `.strict()` parse failures: on first post-deploy request, the strict parse miss cascades through the existing water-route tier order (Redis → dev file cache → snapshot → Overpass), landing on the snapshot which carries the new shape.
- Sites key (`sites:v3`) is untouched, so site route tests and site cache behavior are unchanged.
- No blockers for downstream plans in this phase.

## Self-Check: PASSED

Artifacts verified on disk:
- `server/routes/water.ts` contains `const FACILITIES_KEY = 'water:facilities:v3';` (line 118)
- `server/routes/water.ts` contains `Phase 27.3.2 D-11` JSDoc reference
- `server/routes/sites.ts` still contains `sites:v3` (untouched)
- `server/__tests__/routes/water.test.ts` contains 16 occurrences of `water:facilities:v3` and 0 occurrences of `water:facilities:v2`
- Commit `67325b3` exists in `git log --oneline` (head of branch)
- `npx vitest run server/__tests__/routes/water.test.ts` exits 0, 21/21 tests pass

---
*Phase: 27.3.2-water-facility-admission-tightening-drop-city-coord-fallback*
*Plan: 03*
*Completed: 2026-04-20*

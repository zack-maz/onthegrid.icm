---
phase: 27.3.2
plan: 04
subsystem: water-admission
tags:
  - water
  - admission
  - filtering
  - latin-label
  - wave2
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
dependency-graph:
  requires:
    - 27.3.2-01 (WaterFilterStats.rejections.no_resolved_name bucket + lock-step seeds)
    - 27.3.2-02 (rejectionsSchema Zod extension with no_resolved_name)
    - 27.3.2-03 (Redis key v2→v3 bump so cold-fill writes the new shape)
    - 27.3.1 Plan 02 (hasName gate) + Plan 10 (G1 Latin-only downstream) + Plan 06 (computeAdmissionDecision consolidation)
  provides:
    - 'hasLatinLabel(tags): boolean exported helper (mirrors hasName / hasCapacityData shape)'
    - "computeAdmissionDecision step 3b — rejects non-desal facilities without Latin-script name to bucket 'no_resolved_name'"
    - 'D-02 river-rescue kill cemented (linkedRiver no longer consulted at admission)'
    - 'D-03 desalination exemption preserved (single sparse-OSM-coverage carve-out)'
  affects:
    - 27.3.2-05 (extractLabel desal synthesis — admission now guarantees non-desal admits carry Latin labels)
    - 27.3.2-06 (server test fixtures — new Persian/Arabic-name fixtures will exercise step 3b)
    - 27.3.2-07 (WaterFiltersSection UI — DevApiStatus will render the new bucket populated)
    - 27.3.2-09 (waterLabel.ts client collapse — client fallback chain becomes dead for non-desal)
    - 27.3.2-10 (water-facilities.json regeneration — non-Latin elements now dropped pre-snapshot)
tech-stack:
  added: []
  patterns:
    - 'Latin-label admission helper mirrors hasName/hasCapacityData export style (27.3.2-PATTERNS.md Pattern 1)'
    - 'First-match-wins ordering preserved — new branch inserted BETWEEN no_name (step 3) and not_notable (step 4)'
    - "Desalination exemption pattern (`facilityType !== 'desalination'` guard) mirrors Plan 04 Branch 2c (27.3.1)"
    - 'Admission gate pure-function (no new parameters — reuses existing `tags` arg, hoisted isLatin)'
key-files:
  created: []
  modified:
    - server/adapters/overpass-water.ts
decisions:
  - 'hasLatinLabel inserted immediately after hasCapacityData (line ~183) — established pattern from Phase 27.3.1 for admission-related helper exports; keeps every isLatin-adjacent helper clustered in the first ~200 lines of the adapter.'
  - "Plan 04 writes no_resolved_name via the existing typed AdmissionVerdict union — no new bucket literal added here; Plan 01 already extended `keyof WaterFilterStats['rejections']` to include no_resolved_name so this compiles without an intermediate widening cast."
  - 'All 151 existing adapter tests pass unchanged — prior fixtures happened to use Latin-script names (Ataturk Dam, Iraqi Reservoir, Jeddah Desal, Local Dam). Plan 06 will add Persian/Arabic-tagged fixtures specifically to exercise the new branch.'
metrics:
  duration: '~2m'
  completed: '2026-04-20T21:58:38Z'
  tasks: 2
  files_modified: 1
---

# Phase 27.3.2 Plan 04: Water Facility Admission Tightening — Latin-Label Gate Summary

**Non-desal water facilities without a Latin-script `name:en`/`name`/`operator` now reject to `no_resolved_name` at the admission gate instead of admitting with a bare-type label; desalination bypass and `linkedRiver` admission-kill documented inline per D-02 / D-03.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-20T21:56:34Z
- **Completed:** 2026-04-20T21:58:38Z
- **Tasks:** 2
- **Files modified:** 1 (`server/adapters/overpass-water.ts`, +31 insertions)

## Accomplishments

- **Task 1:** `hasLatinLabel(tags)` exported helper added immediately after `hasCapacityData` (line 192), mirroring established `hasName` / `hasCapacityData` export style. Reuses the hoisted `isLatin` regex (line 357) verbatim — no new script-check logic.
- **Task 2:** New `step 3b` branch inserted into `computeAdmissionDecision` (line 770) between step 3 (no_name) and step 4 (not_notable). Non-desal facilities whose OSM tags fail `hasLatinLabel` now return `{ verdict: 'reject', bucket: 'no_resolved_name' }`. Desalination bypasses the check unconditionally.
- **D-02 river-rescue kill:** Inline comment documents that `linkedRiver` is no longer an admission signal — it remains only as post-admission enrichment for the detail panel (normalizeWaterElement).
- **Zero signature change:** `computeAdmissionDecision` still takes 7 args. No `linkedRiver` parameter added; admission gate stays pure-function over `(tags, lat, lng, facilityType, inPriority, score, nearestCity)`.

## Task Commits

1. **Task 1: Add exported hasLatinLabel helper** — `83d5d98` (feat)
2. **Task 2: Insert no_resolved_name admission branch** — `2ad76bd` (feat)

## Files Created/Modified

- `server/adapters/overpass-water.ts` — +31 lines total
  - Lines 182–200: new exported `hasLatinLabel` helper with JSDoc citing D-01 / D-05 and the D-03 desalination bypass rationale.
  - Lines 769–778: new step 3b branch in `computeAdmissionDecision` with comments citing D-01 / D-02 / D-03 / D-05.

## Verification

**Acceptance greps (all pass):**

- `grep -q "export function hasLatinLabel" server/adapters/overpass-water.ts` → 0
- `grep -q "Phase 27.3.2 D-01 / D-05" server/adapters/overpass-water.ts` → 0
- `grep -q "bucket: 'no_resolved_name'" server/adapters/overpass-water.ts` → 0
- `grep -q "facilityType !== 'desalination' && !hasLatinLabel(tags)" server/adapters/overpass-water.ts` → 0
- `grep -q "D-02: linkedRiver is no longer an admission signal" server/adapters/overpass-water.ts` → 0

**Export ordering invariant:** `grep -n "^export function \\(hasName\\|hasCapacityData\\|hasLatinLabel\\)"` → 155, 173, 192 (in spec order).

**First-match-wins admission ordering:** `grep -n "bucket: 'no_name'\\|bucket: 'no_resolved_name'\\|bucket: 'not_notable'"` → 766, 777, 785. New bucket strictly between the two existing buckets.

**TypeScript:** `npx tsc -p tsconfig.server.json --noEmit` — 22 errors, matching pre-plan baseline exactly. All 22 pre-existing (llmEventExtractor.ts + events.ts, unrelated to water admission; logged in `deferred-items.md` per Plan 01). Zero new errors introduced.

**Adapter tests:** `npx vitest run server/__tests__/adapters/overpass-water.test.ts` → 151/151 pass (~345ms).

## Transient Test Breakage

**None.** The Plan's Task 2 `<action>` block anticipated possible fixture fallout — "any test that relies on a non-Latin name admitting through step 3 but should now reject at step 3b." In practice, every existing `computeAdmissionDecision(...)` call site in `server/__tests__/adapters/overpass-water.test.ts` (45+ direct invocations) constructs OSM tags with Latin-script `name` values (e.g. `'Ataturk Dam'`, `'Local Dam'`, `'Iraqi Reservoir'`, `'Jeddah Desal'`, `'Karkheh'`, literal `'X'`). None land on the new step 3b branch, so no fixture updates are required in Plan 06 for preservation.

**Plan 06 can proceed as planned** — its value is in **adding** new fixtures (Persian/Arabic names, river-proximity non-Latin reservoirs) to exercise the new gate. Its job is pure extension, not remediation. The Plan 06 test file does not need to patch any existing `it(...)` case.

## Decisions Made

1. **Helper insertion point:** After `hasCapacityData` (line ~183), not grouped with `isLatin` (line ~357). Rationale: all admission-decision inputs are exported helpers clustered in the first ~200 lines (`hasName`, `hasCapacityData`, now `hasLatinLabel`) while `isLatin` / `toTitleCase` / `extractLabel` live with the other label-synthesis primitives. Keeps the export surface visually coherent.

2. **No parameter-extension on `computeAdmissionDecision`:** Plan explicitly warned against adding `linkedRiver` as an argument (D-02 river-rescue kill). Helper is called with the existing `tags` parameter; signature stays at 7 args.

3. **Lock-step test fixture-audit deferred to Plan 06:** Plan's acceptance criterion "Running `npx vitest run` may fail — report the count" turned out to be `0`. No breakage to report; Plan 06 becomes a pure-extension plan rather than a remediation plan.

4. **Verification-first execution:** Ran full `vitest run` on the adapter file between Task 2 and commit — cheaper than paying the Plan 06 cost of discovering a regression only when that plan's new fixtures are added. Matches the plan's own "report the count" directive.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1 / 2 / 3 / 4 auto-fixes triggered.

## Authentication Gates

None.

## Issues Encountered

**tsconfig path:** Plan's `<verify>` block says `npx tsc -p server/tsconfig.json`. Actual path is `tsconfig.server.json` (root). Used the correct path. Same 22-error baseline as Plan 01 confirms — not an issue, just a path mismatch in the plan spec.

## Deferred Issues

**22 pre-existing TS errors** in `server/lib/llmEventExtractor.ts` and `server/routes/events.ts` (baseline at commit `4b94565`, unchanged through Plans 01–03 and Plan 04). Logged in `.planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/deferred-items.md`. Out of scope for the water-admission phase; candidates for Phase 28 or a dedicated tech-debt ticket.

## Known Stubs

None. `hasLatinLabel` is a functional helper with full body; no TODO comments, empty arrays, or placeholder returns added. The new admission branch returns a concrete bucket literal and is unconditionally reachable for non-desal non-Latin inputs.

## Threat Flags

None. Both changes touch only internal server admission logic — no new network endpoint, no new auth path, no new file-access pattern, no new schema at a trust boundary. Threat T-27.3.2-05 (Tampering via OSM tag spoofing) disposition `mitigate` is satisfied: rejection is the safe default, and `isLatin` is a Unicode-range regex not a content validator so a crafted tag can only push a facility TO the no_resolved_name bucket, never OUT of it.

## Self-Check: PASSED

- FOUND: `server/adapters/overpass-water.ts` (modified, +31 lines across 2 commits)
- FOUND commit `83d5d98`: `feat(27.3.2-04): add exported hasLatinLabel helper`
- FOUND commit `2ad76bd`: `feat(27.3.2-04): reject non-desal facilities with no Latin label`
- Acceptance criteria greps all exit 0 (see Verification section).
- Export ordering invariant verified via `grep -n` (155 → 173 → 192).
- First-match-wins admission ordering verified via `grep -n` (766 → 777 → 785).
- Zero new TypeScript errors introduced (22 → 22 at plan boundary).
- 151/151 adapter tests green.

## Next Phase Readiness

**Plan 05 (Wave 2 sibling) can start immediately on the same base** — commits `83d5d98` + `2ad76bd` both touch only `server/adapters/overpass-water.ts` in the admission section. Plan 05's work (extractLabel desal synthesis, lines ~411–419 and ~826) is structurally disjoint and sequential per the wave-context handoff note.

**Plan 06 test extension** now has a clean slate: no existing-fixture remediation needed, only new-fixture addition.

---
status: complete
phase: 26.4-documentation-external-presentation
source:
  [
    26.4-01-SUMMARY.md,
    26.4-02-SUMMARY.md,
    26.4-03-SUMMARY.md,
    26.4-04-SUMMARY.md,
    26.4-05-SUMMARY.md,
    26.4-06-SUMMARY.md,
  ]
started: 2026-04-08T00:00:00Z
updated: 2026-04-09T02:55:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test

expected: Kill any running dev server. Run `npm run dev`. Server boots without errors, /health returns 200, /api/flights returns a JSON envelope, and the SPA loads at http://localhost:5173 with the map visible.
result: pass
notes: Server ready in 1s. /health 200 {status:ok, redis:true, latencyMs:103}. /api/flights 200 with {data:[81 flights], stale, lastFresh} envelope. SPA loads at :5173 with `<title>Iran Conflict Monitor</title>` and #root. No errors in boot log.

### 2. All Verification Gates Green

expected: Run `npm run lint && npm run format:check && npm run typecheck && npm run knip && npm run check:env`. Every command exits 0. Lint shows 0 errors (warnings OK). Type-coverage stays ≥97%.
result: pass
reported: "`npm run format:check` fails on docs/runbook.md (committed drift from Plan 06, breaks CI format gate)"
severity: major
notes: INITIAL RUN — 4/5 gates green; format:check failed on docs/runbook.md line 497 (`\_wrong*` → should be `\_wrong\*`). FIXED via `npx prettier --write docs/runbook.md` (single-character change). RE-RUN — all 5 gates green: lint 0 errors, format:check all files clean, typecheck 97.05%, knip clean, check:env OK 18 keys.

### 3. Full Test Suite Passing

expected: Run `npx vitest run`. 1277 tests across 101 files all pass (no failures, no skips beyond intentional `it.todo` stubs). Includes the chaos test (10 tests under server/**tests**/resilience/redis-death.test.ts).
result: pass
notes: 101 test files passed, 1277/1277 tests passing in 38.9s. No failures. (Warnings about --localstorage-file and jsdom canvas are pre-existing noise.)

### 4. README Hero Block & Structure

expected: Open README.md in your editor or on GitHub. Hero GIF embed visible at top, tagline + 7 shields.io/CI badges, 12-section table of contents, all section anchors resolve. Live Demo URL still shows _TBD_ placeholder (user fills in at publish time).
result: pass
notes: 623 lines (grew from 564 claimed in plan 04). Hero GIF embed at line 5 (`![Hero](docs/hero.gif)`). 8 badges on lines 29-36 (CI, CodeQL, Coverage, Tests, Type Coverage, TypeScript, Node, OpenAPI). TOC at line 53. 13 top-level `## ` sections (plan said 12). Live demo placeholder `_TBD — Vercel URL coming soon_` at line 17. docs/hero.gif exists (1.39 MB).

### 5. Hero GIF Animation Quality

expected: Open `docs/hero.gif` in Finder/Preview. ~1.3 MB, 48 frames @ 7fps, 1280px wide. Shows a Strait of Hormuz zoom with all 6 visualization layers (geographic, climate, political, ethnic, water stress, threat density) active. Loops smoothly.
result: pass
notes: 1280x720, 48 frames, 150ms/frame (~6.67fps), loop=0, 1.39 MB. Sampled frames 0/24/47 — all 6 layer legends visible (Elevation, Temperature, Water Health, Threat Density, Factions, Ethnic Groups), threat clusters render with the 4-stop thermal palette, flight icons active, markets panel + live clock advancing (22:53:00Z → 22:53:29Z → 22:53:55Z). Early frames show a wider view, mid/late frames hold the Strait of Hormuz zoom.

### 6. Six Layer Screenshots Look Correct

expected: Open each PNG in `docs/screenshots/`: political-layer.png, ethnic-layer.png, water-stress.png, threat-density.png, detail-panel.png, search-modal.png. Each shows its distinct layer state without crosstalk (e.g. search-modal has no leftover threat selection).
result: pass
notes: All 6 present and rendered. political-layer shows faction fills + FACTIONS legend; ethnic-layer shows hatched GeoEPR zones + Pashtun tooltip; water-stress shows labeled Tigris/Euphrates + WATER HEALTH legend; threat-density shows orange/red thermal clusters + THREAT DENSITY legend; detail-panel shows ThreatCluster "1 events / Shelling" panel on right (background map visually busy from dense ethnic hatching); search-modal shows Cmd+K with `type:flight` autocomplete and no detail-panel crosstalk (reordering worked). Sizes 1.6–3.3 MB.

### 7. Architecture Mermaid Diagrams Render

expected: Open `docs/architecture/data-flows.md` on GitHub or in a Mermaid-aware preview. 9 sequence diagrams render (flights, ships, events, news, sites, water, markets, weather, geocode). Also check `docs/architecture/system-context.md` for the C4Context block.
result: pass
notes: All 10 architecture files exist. Diagram counts match plan 05: system-context 2, data-flows 9 (all sequenceDiagram, all with autonumber + well-formed participant declarations), frontend 3, deployment 2, state-machines 4, types 1 = 21 total. First data-flows block (flights) is syntactically valid sequenceDiagram with Vercel Edge → Express API → Upstash → OpenSky/adsb.lol path. Branch already pushed to origin so GitHub native mermaid renderer will handle display.

### 8. ADR-0005 Reads as Honest Retrospective

expected: Open `docs/adr/0005-phase-26-2-nlp-approach-scrapped.md`. ~300 lines. Names specific deleted files (nlpGeoValidator.ts, titleFetcher.ts, me-cities.json, extract-geonames.ts), the threshold revert (0.38 → 0.35), the 2-week time investment, and a "What I Learned" section with 4 lessons. Status: Superseded.
result: pass
notes: 300 lines exactly. Status: Superseded — reverted in Phase 26.3. Names all 6 deleted files (nlpGeoValidator.ts, titleFetcher.ts, me-cities.json, extract-geonames.ts, plus both test files). Threshold revert 0.38 → 0.35 cited on line 51. "Time invested before scrap: roughly two weeks" on line 88. Full Nygard structure: Context (9), Decision (58), Consequences (95), What I Learned (171), Alternatives Considered post-scrap (252), References (277).

### 9. Runbook Has 9 Real Failure Modes

expected: Open `docs/runbook.md`. ~676 lines, 9 failure mode sections with Symptom / Detection / Cause / Remediation / Prevention structure. Each grounds remediation in real code paths (e.g. cacheGetSafe + REDIS_OP_TIMEOUT_MS lines 19-42 of server/cache/redis.ts), not generic SRE advice.
result: pass
notes: 676 lines exactly. 9 numbered failure modes: Upstash Redis unreachable, GDELT lastupdate.txt stale, Overpass timeout, AISStream WebSocket disconnect, Yahoo Finance throttling, Vercel function timeout, Upstash command budget exhausted, CORS misconfiguration, Vercel cron failure. Plus TOC, common log patterns, see-also. 47 occurrences of Symptom/Detection/Cause/Remediation/Prevention keywords (~5 per section). 11 refs to real code paths (server/cache, server/adapters, server/routes, server/middleware).

### 10. Degradation Contract is Layered with Proofs

expected: Open `docs/degradation.md`. ~303 lines. Documents 6 layers (Cache, Data Source, Response, Frontend, Rate Limit, /health). Bottom summary table maps each layer to failure mode, user-facing outcome, and a proof column (test file or code line).
result: pass
notes: 303 lines exactly. 6 layer sections (Cache/Data Source/Response/Frontend/Rate Limit/health) + Summary Table at line 282. Proof references land on concrete artifacts: server/**tests**/resilience/redis-death.test.ts (chaos), validateResponse.test.ts (Zod dev/prod), useSelectedEntity (frontend lost contact), withTimeout 2000ms cap (cache). Summary table has 4 columns (Layer / Failure mode / User-facing outcome / Proof) with 7 rows matching the body sections.

### 11. Public Rate Limiter Returns 429 on 7th Request

expected: With dev server running, run `for i in 1 2 3 4 5 6 7 8; do curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3001/api/flights; done`. First 6 return 200 (or 304), 7th and 8th return 429 from `rateLimiters.public` (6 req/min per-IP).
result: pass
notes: Rate limiter is intentionally bypassed in local dev by design (server/middleware/rateLimit.ts:43-47 — `if (NODE_ENV !== 'production' && !VERCEL) return next()`). Verified curl against the dev server returned 8/8 HTTP 200 (expected). The tier is verified by `server/__tests__/rateLimitPublic.test.ts` — 6/6 tests passing in 253ms, covering rateLimiters.public existence, 6 req/min config, and the 7th-request 429 response under production mocking. Definition at line 119: `public: createRateLimiter(6, 60, 'ratelimit:public')`.

### 12. robots.txt Served at /robots.txt

expected: With dev server running, run `curl http://localhost:3001/robots.txt`. Response contains `User-agent: *`, `Disallow: /api/`, `Disallow: /health`, `Allow: /`. Or in production, `curl https://your-vercel-url/robots.txt`.
result: pass
notes: public/robots.txt file matches expected 4 lines verbatim (`User-agent: *`, `Disallow: /api/`, `Disallow: /health`, `Allow: /`). Served at http://localhost:5173/robots.txt (Vite public/ default handling) with HTTP 200 and identical body.

## Summary

total: 12
passed: 12
issues: 0
pending: 0
skipped: 0
gaps_closed: 1

## Gaps

- truth: '`npm run format:check` exits 0 across the whole repo'
  status: closed
  resolution: 'Fixed in-session via `npx prettier --write docs/runbook.md` — single-character change on line 497 (`\_wrong*` → `\_wrong\*`). Whole-repo `npx prettier --check .` now reports "All matched files use Prettier code style". All 5 verification gates re-run green.'
  reason: 'User reported: `npm run format:check` fails on docs/runbook.md (committed drift from Plan 06, breaks CI format gate)'
  severity: major
  test: 2
  root_cause: |
  docs/runbook.md line 497 has `\_wrong*` (leading underscore escaped, trailing asterisk not escaped). Prettier in --check mode rejects the mismatched markdown italic marker and flags the file as needing reformatting. Committed in 5b5a0e2 (Plan 06 "add operations runbook") in a single 676-line insertion; never modified since.

  Not caught because Plan 06's self-check only ran `npx prettier --check README.md`, not the runbook or the whole repo. The husky pre-commit hook was installed in f355e34 before this commit, so lint-staged's prettier --write SHOULD have rewritten the file at commit time, but either the hook was skipped (--no-verify) or prettier --write didn't normalize this specific escape. Either way, the current committed content drifts from prettier's expected output.

  Impact: `.github/workflows/ci.yml` line 25 runs `npm run format:check` which will fail on the next push, blocking CI. No runtime impact (docs-only file).
  artifacts:
  - path: 'docs/runbook.md'
    issue: 'Line 497 has `\_wrong*`; prettier --write rewrites it to `\_wrong\*`'
    missing:
  - 'Run `npx prettier --write docs/runbook.md` to apply the single-character fix (line 497)'
  - 'Re-run `npm run format:check` to confirm the whole repo is prettier-clean'
  - 'Commit as `docs(26.4-06): fix prettier format on runbook.md`'
    debug_session: ''
    fix_complexity: trivial — single character change, no planner needed

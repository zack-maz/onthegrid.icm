---
phase: 26.4-documentation-external-presentation
plan: 02
type: execute
wave: 2
depends_on:
  - "26.4-01"
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/codeql.yml
  - .husky/pre-commit
  - .gitleaks.toml
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - PRES-10
  - PRES-11
  - PRES-12
  - PRES-13
  - PRES-14
  - PRES-15
must_haves:
  truths:
    - "GitHub Actions CI workflow runs on every PR with lint, typecheck, test, audit, coverage upload"
    - "GitHub Actions CodeQL workflow runs on every PR and weekly schedule"
    - "Pre-commit hook runs lint-staged plus gitleaks under 2 seconds on staged files"
    - "Gitleaks blocks commits containing planted fake API keys via fixture-tested config"
    - "Vercel preview deploys are documented as a manual GitHub-integration step (no YAML required)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PR CI: lint-and-typecheck plus test (vitest --coverage + codecov upload) plus audit jobs"
      min_lines: 50
    - path: ".github/workflows/codeql.yml"
      provides: "CodeQL JS/TS analysis on PR plus Monday 06:00 UTC schedule"
      min_lines: 25
    - path: ".husky/pre-commit"
      provides: "lint-staged then gitleaks protect --staged --redact"
      min_lines: 10
    - path: ".gitleaks.toml"
      provides: "Allowlist for test fixtures and known non-secrets"
      min_lines: 15
    - path: "package.json"
      provides: "prepare script (husky), lint-staged config block, devDeps husky+lint-staged"
  key_links:
    - from: ".husky/pre-commit"
      to: "package.json lint-staged config"
      via: "npx lint-staged invocation"
      pattern: "lint-staged"
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "npm run lint, format:check, typecheck"
      pattern: "npm run (lint|typecheck|format:check)"
---

<objective>
Wire CI/CD infrastructure: GitHub Actions workflows for PR validation and CodeQL security analysis, husky+lint-staged pre-commit hook for fast format/lint, and gitleaks pre-commit secret scanning.

Purpose: Make every PR runnable, every commit safe, every secret leak caught before it touches the wire. This is the discipline-as-infrastructure layer that turns the cleanup work in Plan 01 into an enforced ratchet.

Output: Two GitHub Actions workflows, one husky pre-commit hook, gitleaks config, and a documented manual step for enabling Vercel preview deploys.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26.4-documentation-external-presentation/26.4-CONTEXT.md
@.planning/phases/26.4-documentation-external-presentation/26.4-RESEARCH.md

# Plan 01 must be complete (lint/format scripts wired)
@.planning/phases/26.4-documentation-external-presentation/26.4-01-SUMMARY.md

# Existing files this plan extends
@package.json
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions workflows (CI + CodeQL)</name>
  <files>.github/workflows/ci.yml, .github/workflows/codeql.yml</files>
  <action>
1. Verify the .github/workflows/ directory exists. If not, create it via Bash mkdir -p.

2. Create .github/workflows/ci.yml with the EXACT content from research §"Full GitHub Actions ci.yml" (Plan 02). Required jobs: lint-and-typecheck (runs npm run lint, npm run format:check, npm run typecheck, npm run knip with continue-on-error: true), test (runs npx vitest run --coverage and uploads coverage via codecov-action@v5 with files: ./coverage/lcov.info and fail_ci_if_error: false), audit (runs npm audit --audit-level=high). All three jobs use actions/checkout@v4 and actions/setup-node@v4 with node-version 22 and cache npm. Top of file: name: CI; on: pull_request and push to main; concurrency group ci-${{ github.ref }} with cancel-in-progress: true.

3. Create .github/workflows/codeql.yml with the EXACT content from research §"Full GitHub Actions codeql.yml" (Plan 02). Required: name: CodeQL; on: push to main, pull_request to main, schedule cron 0 6 * * 1; one analyze job with permissions actions:read, contents:read, security-events:write; matrix language: javascript-typescript; uses github/codeql-action/init@v3 then github/codeql-action/analyze@v3 with category /language:${{ matrix.language }}.

4. Visually verify both YAMLs use 2-space indent, no tabs, no trailing whitespace.

DO NOT add a Vercel deploy YAML — Vercel preview deploys come from the Vercel-GitHub integration, configured via Vercel dashboard (covered in Task 4 documentation).
DO NOT add Dependabot, commitlint, or PR template — user explicitly deferred.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; test -f .github/workflows/codeql.yml &amp;&amp; head -3 .github/workflows/ci.yml | grep -q "name: CI" &amp;&amp; head -3 .github/workflows/codeql.yml | grep -q "name: CodeQL"</automated>
  </verify>
  <done>
    .github/workflows/ci.yml exists with lint-and-typecheck, test, audit jobs.
    .github/workflows/codeql.yml exists with javascript-typescript matrix and weekly schedule.
    Both YAMLs are syntactically valid (no tab characters, proper 2-space indent).
  </done>
</task>

<task type="auto">
  <name>Task 2: Install husky + lint-staged, write pre-commit hook with gitleaks</name>
  <files>package.json, package-lock.json, .husky/pre-commit, .gitleaks.toml</files>
  <action>
1. Install dev dependencies: npm install -D husky@^9 lint-staged@^15. Verify both appear in package.json devDependencies.

2. Run npx husky init to create .husky/ directory and starter pre-commit hook. This adds "prepare": "husky" to package.json automatically. Confirm the prepare script is present.

3. Add to package.json a lint-staged block as a top-level sibling of scripts. The block should map *.{ts,tsx} to ["eslint --fix", "prettier --write"] and *.{json,md,css,yml,yaml} to ["prettier --write"]. Use the Edit tool to insert this block.

4. Overwrite .husky/pre-commit with a bash script that: (a) sets bash strict mode set -e, (b) runs npx lint-staged, (c) checks for gitleaks via command -v, (d) if gitleaks present runs gitleaks protect --staged --redact --verbose, (e) if absent prints a warning with brew/scoop install instructions and continues. Make it executable via Bash chmod +x .husky/pre-commit.

5. Create .gitleaks.toml at repo root with an [allowlist] section. paths array should include: server/__tests__/fixtures/.*, server/__tests__/.*\.test\.ts$, docs/screenshots/.*, docs/hero\.gif$, package-lock\.json$, \.planning/.*. regexes array should include placeholder patterns like KEY=your_key_here and TOKEN=replace_me. Use TOML triple-single-quote literal strings for the regexes.

6. Verify pre-commit hook: cat .husky/pre-commit shows the script; ls -la .husky/pre-commit shows execute permission; npm run prepare succeeds (creates .husky/_/ if missing).

7. Test runtime budget: stage a single TypeScript file via git add src/main.tsx (or any small file), run time npx lint-staged, must complete in under 2 seconds. Do NOT actually commit yet; just verify timing. Reset staged change via git reset HEAD src/main.tsx.

DO NOT install gitleaks via npm — it is a binary tool installed via brew/scoop/go install. The hook fails open with a warning if not installed (developer convenience). CI catches secrets via a separate gitleaks-action invocation handled in Task 3 below.
  </action>
  <verify>
    <automated>test -f .husky/pre-commit &amp;&amp; test -x .husky/pre-commit &amp;&amp; test -f .gitleaks.toml &amp;&amp; node -e "const p=require('./package.json'); if(!p.devDependencies.husky) process.exit(1); if(!p.devDependencies['lint-staged']) process.exit(1); if(!p.scripts.prepare) process.exit(1); if(!p['lint-staged']) process.exit(1);"</automated>
  </verify>
  <done>
    husky and lint-staged installed as devDeps.
    package.json has prepare script and lint-staged config block.
    .husky/pre-commit exists, is executable, runs lint-staged then gitleaks (or warns gracefully).
    .gitleaks.toml exists with fixture allowlist.
    npx lint-staged on a single file completes in under 2 seconds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Plant-and-remove gitleaks fixture test plus document Vercel preview deploys</name>
  <files>package.json</files>
  <behavior>
    - Test 1: A staged file containing a fake-but-realistic-shaped secret (e.g., "AKIA" prefix + 16 chars) causes gitleaks protect --staged to exit non-zero.
    - Test 2: With the fake secret removed, gitleaks protect --staged exits 0 (clean).
    - Test 3: Vercel preview deploy documentation step is captured for Plan 04 README work (not implemented here, but noted).
  </behavior>
  <action>
1. Verify gitleaks is installed locally: command -v gitleaks. If not available, install via brew install gitleaks (macOS).

2. Run the planted-secret smoke test:
   a. Create a temp test file with a fake AWS-shaped key: echo 'const k = "AKIAIOSFODNN7EXAMPLE";' > /tmp/leak-fixture.ts
   b. Stage it: cd /Users/zackmaz/Desktop/my_world && cp /tmp/leak-fixture.ts src/__leak-fixture.ts && git add src/__leak-fixture.ts
   c. Run gitleaks protect --staged --redact --verbose — MUST exit non-zero. Capture exit code.
   d. Unstage and delete: git reset HEAD src/__leak-fixture.ts && rm src/__leak-fixture.ts
   e. Run gitleaks protect --staged --redact --verbose with no staged files — MUST exit 0.

3. Capture the smoke-test commands as a comment block in package.json scripts (or document in Plan 04 SUMMARY) — NOT as a permanent script (we don't want it lingering as testable noise). The point is to prove gitleaks works once during this plan's execution, not to add it to CI.

4. Document Vercel preview deploys (handoff to Plan 04 README work):
   - Vercel preview deploys are enabled via Vercel dashboard, NOT via .github/workflows/ YAML.
   - Steps the user must do MANUALLY in Vercel dashboard:
     a. Visit https://vercel.com/[username]/[project]/settings/git
     b. Enable "Production Branch: main"
     c. Enable "Deploy Hooks" or confirm GitHub integration is connected
     d. Confirm "Preview Deployments: Pull Requests" is ON
   - Plan 04 README will reference this. This task does NOT call the Vercel API or modify vercel.json.

5. Run final verification suite:
   - npm run lint exits 0
   - npm run format:check exits 0
   - npm run typecheck exits 0
   - npx vitest run exits 0 (Plan 03 will add new test files in Wave 2; this plan touches no production source)
  </action>
  <verify>
    <automated>command -v gitleaks &amp;&amp; gitleaks protect --staged --redact 2>/dev/null; echo "gitleaks-check-done"</automated>
  </verify>
  <done>
    Gitleaks fixture smoke test executed and verified to catch a planted secret.
    Vercel preview deploy enablement documented for Plan 04 hand-off.
    No regressions: lint, format:check, typecheck, vitest all pass.
  </done>
</task>

</tasks>

<verification>
- .github/workflows/ci.yml and codeql.yml exist and parse as valid YAML
- .husky/pre-commit exists, is executable, runs lint-staged then gitleaks
- .gitleaks.toml allowlist is in place
- package.json has prepare script + lint-staged block + husky + lint-staged in devDependencies
- npx lint-staged on a single staged file completes in under 2 seconds
- Gitleaks blocks a planted fake AWS secret (smoke-tested manually during execution)
- npm run lint, format:check, typecheck, vitest all exit 0
</verification>

<success_criteria>
A future PR triggers CI workflow with all jobs green, every commit attempt runs the husky pre-commit hook (format + lint + secret scan) in under 2 seconds, and gitleaks blocks accidental secret commits before they enter git history.
</success_criteria>

<output>
After completion, create .planning/phases/26.4-documentation-external-presentation/26.4-02-SUMMARY.md documenting:
- CI workflow file paths and the jobs they define
- CodeQL workflow schedule and language matrix
- Husky pre-commit hook contents and runtime measured
- Gitleaks fixture smoke test result (exit codes captured)
- Vercel preview deploy enablement steps for the user to perform manually
- Manual hand-off items for Plan 04 README (gitleaks install instructions, Vercel preview steps)
</output>

---
phase: 27.4.3-free-claude-code-routing
plan: 05
status: partial
subsystem: infra
tags: [cutover, gate-A, gate-B, auto-rollback, deferred]
---

# Plan 27.4.3-05 — v3 Cutover (Partial — Cutover Deferred, Gate B FAIL)

## Outcome

**Plan completed 3 of 7 tasks: Tasks 1, 2, plus the Gate B run record.** Cutover (Task 6) was deliberately NOT executed because Gate B failed 3 of 4 sub-thresholds. Per plan, when Gate B fails the executor STOPS; operator decided to defer cutover rather than re-run with the runner-up model or accept threshold waivers.

## Tasks completed (3 of 7)

| Task | Name | Commit | Status |
|---|---|---|---|
| 1 | Wire D-17 auto-rollback triggers (watchdog + eval-drop) | `72b77bb` | ✅ Done |
| 2 | Gate A eval parity (resolver-only baseline) | `fa875cc` | ✅ Done |
| 3 | Gate A operator confirmation | (checkpoint) | ✅ Approved |
| 4 | Gate B production smoke run | `69ba7bc` | ⚠ FAIL recorded |
| 5 | Gate B operator confirmation | (checkpoint) | ❌ Not reached (Gate B failed) |
| 6 | Cutover POST + UAT close | — | ❌ Skipped per plan rule |
| 7 | SUMMARY.md (this doc) | (this commit) | ✅ Done |

## Gate A — PASSED

Resolver-only eval against 50-event GT corpus, vs locked `qwen/qwen3.5-397b-a17b`:

- within20km: **47/50 = 0.940** (D-16 stretch target met exactly)
- D-16 floor (≥ 0.890): PASS, +5.0pp margin
- Baseline written to `events:llm-eval-baseline:v3` (90d TTL)
- Auto-rollback Trigger 2 (eval-drop ≥ 5pp) is armed against this baseline

## Gate B — FAILED (3 of 4 thresholds breached)

Full production smoke (196 batches, 391 GDELT groups, 363 enriched events):

| Sub-threshold | Required | Actual | Verdict |
|---|---|---|---|
| watchdogTimeoutCount | 0 | 11 | FAIL |
| DLQ count | ≤ 5 | 28 | FAIL |
| Duration | ≤ 120 min | 218 min | FAIL |
| provenanceCounts populated | yes | yes | PASS |

Auto-rollback Trigger 1 (watchdog_recurrence) **fired correctly mid-run**:

```json
{from: "v3", to: "v2", trigger: "auto:watchdog_recurrence",
 reason: "watchdogTimeoutCount=11 (>= 3)"}
```

The pipeline override is now `v2` (Redis `events:llm-pipeline-override`).

### Failure mode

`qwen/qwen3.5-397b-a17b` showed long-tail latency at production scale that the 10-event Plan 03 bake-off didn't reveal:

- Plan 03 bake-off (10 events): mean 17.8s, 0 timeouts
- Gate B (196 batches): p50 23.9s, p95 389s, **p99 929s** (15+ min)

11 batches blew through the 90s watchdog hard-kill. **Accuracy was not the issue** — within20km eval still 0.940. Reliability and end-to-end runtime are the blockers.

## Auto-rollback validation

Plan 05 Task 1 wired D-17 Trigger 1 (watchdog_recurrence ≥ 3) and Trigger 2 (eval-drop ≥ 5pp) into `server/lib/llmEventExtractor.v3.ts` with 280 lines of regression tests in `server/__tests__/lib/llmAutoRollback.test.ts`.

Production validation: Trigger 1 fired exactly as designed during Gate B. The system safely demoted itself to v2 mid-run when it detected sustained reliability problems. This is the most important outcome of the entire phase — the safety net works in production.

## What ships in v3 (available under env override)

Even though v3 is not the default, the full v3 surface exists and can be exercised:

- Run with `LLM_PIPELINE_V3=true` to force v3 for inspection
- Topbar pipeline pill renders `v3` in blue
- DevApiStatus dashboard shows all 7 v3 observability blocks (RoutingTrace / Latency / RateLimit / SchemaFail / ErrorTaxonomy / PipelineFlips / CostShadow + DrillDown lineage)
- All 6 D-17 auto-rollback triggers + audit log entries work
- Per-candidate eval baselines persisted to `events:llm-eval-baseline:v3:<sanitized-model>` for any future re-comparison

## What was NOT done

- Task 6: cutover POST `/api/events/llm-pipeline {version: 'v3'}` — skipped per plan rule
- Task 6: parent UAT 27.4.2-HUMAN-UAT.md tests 1+2 status flip — skipped (depends on cutover)
- Auto-rollback Trigger 1 production confirmation in CUTOVER.md (technically already recorded — it fired during Gate B)

## Future-phase hand-off

The cutover is deferred. Possible paths for a follow-up phase:

1. **Newer NIM model** — NVIDIA's catalog drifts; a future evaluation may surface a model with both Gate A accuracy and Gate B reliability.
2. **Adaptive batching** — split batches that timeout, retry smaller. Reduces long-tail impact.
3. **Hybrid pipeline** — Cerebras/v2 as primary (low latency), v3 as background enrichment for high-value events. Trades coverage for latency.
4. **Provider diversification** — OpenRouter is wired but unused for the bake-off. A multi-model OpenRouter cascade could outperform single-model NIM.

## Verification

- TS clean across `server/`
- Server tests: 989+/989+ green (auto-rollback test suite passes)
- D-21 rollback safety preserved: v1 + v2 extractors + `llm-provider.ts` byte-identical
- Pipeline override safely set to v2 (post auto-rollback)

## Self-Check: PASSED (for the partial scope)

All 3 reachable tasks completed atomically. Stopping at Gate B FAIL is the correct behavior per plan rules — this SUMMARY accurately records the partial completion and the operator decision to defer cutover.

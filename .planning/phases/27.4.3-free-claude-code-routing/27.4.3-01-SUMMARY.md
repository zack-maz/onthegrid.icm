---
phase: 27.4.3-free-claude-code-routing
plan: 01
subsystem: infra
tags: [llm, routing, free-claude-code, nvidia-nim, openrouter, vendoring, security, tdd]
---

# Plan 27.4.3-01 — Free-Claude-Code Routing Foundation

## What was built

Vendored the free-claude-code routing primitives (NVIDIA NIM + OpenRouter) into TypeScript at `server/lib/freeClaudeRouter.ts`. Established v3 env-var + config foundations. Extended circuit-breaker + DLQ + token-budget + progress unions to accept the two new providers without breaking v2 rollback safety.

## Files created/modified

### Created
- `server/lib/freeClaudeRouter.ts` — 352-line vendored TS port (pinned upstream SHA `40951c145ad29d6dfe450e83fd2b91fc19b9a27f`, MIT license attribution). Exports `callLLM`, `stripReasoningBlocks`, `classifyError`, `FreeProvider`, `RoutingDecision`. Implements RollingWindow rate limiter (40 req/min), 429 backoff with exponential delays + ±250ms jitter, `<think>...</think>` stripper, fall-through cascade NVIDIA NIM → OpenRouter.
- `server/__tests__/lib/freeClaudeRouter.test.ts` — 15 vitest unit tests across all 5 D-23 test groups (rolling window, 429 backoff, `<think>` strip, fall-through cascade, error taxonomy).

### Modified
- `server/config.ts` — Added `NVIDIA_NIM_API_KEY`, `OPENROUTER_API_KEY`, `LLM_PIPELINE_V3` env Zod parsing. Added `isPipelineV3()`, `getPipelineVersion()`, tri-state `setPipelineOverride('v1' | 'v2' | 'v3' | null)`.
- `.env.example` — Documented all three new env vars.
- `server/lib/llmCircuitBreaker.ts` — `Provider` union widened to 4 entries (cerebras, groq, nvidia_nim, openrouter). All breaker maps initialized for new providers.
- `server/__tests__/lib/llmCircuitBreaker.test.ts` — Tests updated to cover new providers.
- `server/lib/llmDLQ.ts` — `reason` taxonomy widened with 4 v3-specific values for routing-failure classification.
- `server/lib/llmTokenBudget.ts` — Provider union widened (D-21 byte-identical preserved on `llm-provider.ts`).
- `server/lib/llmProgress.ts` — `LLMPipelineProgress.callHistory.provider` + `breakerState` widened.

## Commits (5)

- `677a977` feat(27.4.3-01): vendor free-claude-code routing primitives to TS
- `d799305` test(27.4.3-01): add 15 vitest unit tests for freeClaudeRouter (D-23)
- `460fc8a` feat(27.4.3-01): add NVIDIA_NIM/OpenRouter env + tri-state pipeline override
- `a9a1edd` feat(27.4.3-01): widen Provider union + DLQ taxonomy for v3 routing
- `0ff3387` docs(27.4.3-01): complete free-claude-code routing foundation plan

## Verification

- `server/__tests__/lib/freeClaudeRouter.test.ts` — 15/15 green (4.6s)
- `server/__tests__/lib/llmCircuitBreaker.test.ts` — 14/14 green
- Full server suite — 986/986 green
- TypeScript clean across `server/`
- `git diff <plan-base>..HEAD -- server/adapters/llm-provider.ts` empty (D-21 rollback safety preserved)

## Auto-fixed deviations (4)

1. Rule 1 — primary-skip routing reason fix (semantically incorrect previous value)
2. Rule 1 — unused-param cleanup (TS noUnusedParameters)
3. Rule 3 — widen `llmTokenBudget.Provider` to satisfy union compatibility (preserves D-21)
4. Rule 3 — widen `LLMPipelineProgress.callHistory.provider` + `breakerState` for new providers

All deviations documented in commit messages and necessary for either correctness or D-21 rollback safety.

## Self-Check: PASSED

All 4 plan tasks executed, all atomic commits in place, full test suite green, TS clean, D-21 preserved.

## Key outputs for downstream waves

- `freeClaudeRouter.callLLM(messages, schemaText)` ready for Plan 02b's v3 extractor consumption
- `isPipelineV3()` / `getPipelineVersion()` / `setPipelineOverride('v3')` available throughout server code
- All breaker/DLQ/budget/progress maps now accept the v3 providers without runtime errors

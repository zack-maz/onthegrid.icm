---
phase: 28.2.7-audit-tier-completeness
verified: 2026-05-07T22:25:00Z
status: human_needed
score: 3/3 must-haves verified at code level
human_verification:
  - test: 'After phase merges to main + Vercel auto-deploys, run prod-connectivity-audit.yml manually against https://otg-iran-monitor.vercel.app'
    expected: 'Workflow exits 0 AND audit:connectivity:last-result has allTiersGreen=true with tierStatus.{critical=healthy, nonCritical∈{healthy,degraded}, static∈{healthy,degraded}, probeOnly=healthy, cron∈{healthy,degraded}}'
    why_human: 'Requires operator-controlled deploy + manual workflow_dispatch trigger; cannot be exercised against the local working tree'
  - test: 'Force-trigger each cron post-deploy and verify the cron tier flips green'
    expected: 'After firing /api/cron/health, /api/cron/warm, and /api/cron/refresh-events?force=true once each, /api/health summary.cron === { healthy: 3, degraded: 0, unhealthy: 0, unknown: 0 }'
    why_human: 'Requires production Bearer + Vercel cron tick — unobservable from local code state alone'
  - test: 'After at least one refresh-events cron run, verify llmStatus stays healthy across cold-started instances'
    expected: 'Hit /api/health 10 times across an idle window; all responses show endpoints.llmStatus.status === "healthy" (Redis-backed read survives cold starts)'
    why_human: 'Vercel Fluid Compute cold-start is opportunistic; cannot reliably force a cold instance from outside'
  - test: 'Verify probe-only tier is healthy immediately at deploy (no cron run required)'
    expected: '/api/health summary.probeOnly === { healthy: 2, unhealthy: 0, unknown: 0 } on the first request after the new bundle goes live'
    why_human: 'Requires a real prod /api/health round-trip against the deployed bundle'
---

# Phase 28.2.7: Audit-tier Completeness Verification Report

**Phase Goal:** Make `prod-connectivity-audit.yml` exit 0 with `allTiersGreen=true` against a healthy prod by resolving the 3 pre-existing tier-unknown bugs surfaced when Phase 28.2.6 cleared the dominant `critical[llmEvents]` blocker.

**Verified:** 2026-05-07T22:25:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth (from SPEC R1/R2/R3 + Acceptance Criteria)                                                                         | Status                    | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | R1 — Each of 3 cron handlers writes `cron:lastTick:<name>` after body succeeds                                           | ✓ VERIFIED (code)         | `cron-health.ts:118` writes `cron:lastTick:health` with `Date.now()` + `CRON_LASTTICK_TTL_SEC` AFTER eval blocks complete, BEFORE `res.json`. `cron-warm.ts:85` writes `cron:lastTick:warm` inside `if (partialOrBetter)` guard (D-04). `refresh-events-cron.ts:64` writes `cron:lastTick:refresh-events` inside `try` block AFTER `runRefreshExtraction` resolves, before `res.status(200).json` (catch block does NOT write — D-03 honest-failure semantics). |
| 2   | R1 — `CRON_LASTTICK_TTL_SEC = 604_800` (7 days) constant exists in `healthSources.ts`                                    | ✓ VERIFIED                | `server/lib/healthSources.ts:65` — `export const CRON_LASTTICK_TTL_SEC = 7 * 24 * 60 * 60; // 604_800 — 7 days`. Doc cites CONTEXT D-06 seconds-based override of SPEC's `_TTL_MS`.                                                                                                                                                                                                                                                                             |
| 3   | R2 — `llm:lastProgress` is written to Redis on every `startedAt`-set transition (D-01)                                   | ✓ VERIFIED (code)         | `server/lib/llmProgress.ts:522-526` — `void cacheSetSafe(LLM_LASTPROGRESS_KEY, { startedAt, completedAt }, LLM_LASTPROGRESS_TTL_SEC)` inside `resetProgress()`. Test 1 in `llmProgress.persistence.test.ts` asserts the write and TTL.                                                                                                                                                                                                                          |
| 4   | R2 — Mid-run `updateProgress` calls do NOT write to Redis; only terminal transitions (D-02)                              | ✓ VERIFIED (code)         | `server/lib/llmProgress.ts:542-548` — `if (partial.completedAt !== undefined) { void cacheSetSafe(...) }`. Tests 2-3 in `llmProgress.persistence.test.ts` assert mid-run skip + terminal-write.                                                                                                                                                                                                                                                                 |
| 5   | R2 — `probeLlmStatus()` reads Redis first, falls back to in-memory singleton, returns `null` when both null (D-08, D-09) | ✓ VERIFIED (code)         | `server/routes/health.ts:141-171` — async `probeLlmStatus()` does `cacheGetSafe<{startedAt, completedAt}>(LLM_LASTPROGRESS_KEY, ...)` inside try/catch, then `latest = redisLatest ?? memLatest`. Tests 4-7 assert all four branches: SPEC R2 acceptance (Redis populated, fresh singleton), D-09 both-null, Redis-null fallback, Redis-throws fallback.                                                                                                        |
| 6   | R2 — Probe is read-only — no backfill of in-memory singleton (D-10)                                                      | ✓ VERIFIED (code)         | Inspection of `probeLlmStatus()` body — zero `llmProgress.<field> = ...` mutations. Test 4 explicitly asserts `singleton.startedAt + singleton.completedAt remain null` after probe call.                                                                                                                                                                                                                                                                       |
| 7   | R3 — `probeProbeOnly()` returns `freshnessMs: 0` (was `null`)                                                            | ✓ VERIFIED                | `server/routes/health.ts:211-219` — body returns `freshnessMs: 0`. JSDoc rewritten (lines 190-210) to cite Phase 28.2.7 R3 + the deriveStatus chain step-by-step. `freshnessMs: null` no longer present in `probeProbeOnly`.                                                                                                                                                                                                                                    |
| 8   | R3 — `deriveStatus(0, 0, false) === 'healthy'`                                                                           | ✓ VERIFIED (test)         | `server/lib/healthSources.ts:139-149` deriveStatus body — `0 <= 0` → `'healthy'`. Test 1 in `health.probeOnly.test.ts` asserts the invariant directly via pure-function unit (no mocks).                                                                                                                                                                                                                                                                        |
| 9   | Bundle regen — `api/vercel-entry.js` reflects R1+R2+R3 source state                                                      | ✓ VERIFIED                | `api/vercel-entry.js` (1.72 MB, 86*279 lines) contains: `cron:lastTick:health` (1), `cron:lastTick:warm` (1), `cron:lastTick:refresh-events` (1), `llm:lastProgress` (1 literal + 7 occurrences with identifier `LLM_LASTPROGRESS*\*`), `freshnessMs: 0`(2 — probeSources canon + probeProbeOnly fix). Commit`48a528a`is bundle-only (source-leak grep`^server/\|^src/\|^scripts/` returns 0).                                                                  |
| 10  | Vitest baseline 2174 stays green; new tests ADD to count                                                                 | ✓ VERIFIED (test run)     | Local `npx vitest run` exits 0 with **2192 passed, 19 skipped, 5 todo, 170 test files**. Net delta: +18 = baseline 2174 + 6 cron-lasttick + 7 llmProgress.persistence + 5 health.probeOnly. Matches Plan 03 self-check (2192/2192).                                                                                                                                                                                                                             |
| 11  | `tsc --noEmit` exits 0                                                                                                   | ✓ VERIFIED (run)          | `npx tsc --noEmit` produced no output and exit 0.                                                                                                                                                                                                                                                                                                                                                                                                               |
| 12  | Acceptance: `prod-connectivity-audit.yml` exits 0 + writes `allTiersGreen=true` against deployed prod                    | ? UNCERTAIN — needs human | Workflow file exists at `.github/workflows/prod-connectivity-audit.yml`. Operator must merge PR + Vercel auto-deploys + manual `workflow_dispatch` against `https://otg-iran-monitor.vercel.app`. Cannot be exercised against the local working tree.                                                                                                                                                                                                           |
| 13  | Acceptance: `/api/health summary.cron === {healthy:3, ...}` after all 3 crons fire                                       | ? UNCERTAIN — needs human | Requires Vercel cron ticks against the deployed bundle.                                                                                                                                                                                                                                                                                                                                                                                                         |
| 14  | Acceptance: `/api/health summary.probeOnly === {healthy:2, ...}` immediately at deploy                                   | ? UNCERTAIN — needs human | Mechanical at deploy time but requires real `/api/health` round-trip against deployed bundle to confirm.                                                                                                                                                                                                                                                                                                                                                        |
| 15  | Acceptance: `endpoints.llmStatus.status === "healthy"` survives cold starts                                              | ? UNCERTAIN — needs human | Vercel Fluid Compute cold start is opportunistic and unobservable from local code state.                                                                                                                                                                                                                                                                                                                                                                        |

**Score:** 11/11 code-level truths verified. 4 acceptance items require operator deploy + workflow run.

### Required Artifacts

| Artifact                                               | Expected                                                                                  | Status     | Details                                                                                                                                                                                       |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server/lib/healthSources.ts`                          | `CRON_LASTTICK_TTL_SEC` export                                                            | ✓ VERIFIED | Line 65 — `export const CRON_LASTTICK_TTL_SEC = 7 * 24 * 60 * 60`. Doc comment cites CONTEXT D-06.                                                                                            |
| `server/routes/cron-health.ts`                         | `cacheSetSafe('cron:lastTick:health', Date.now(), CRON_LASTTICK_TTL_SEC)` AFTER body      | ✓ VERIFIED | Line 118 — placement after both eval blocks (runEval + runAdversarialEval) + source-freshness loop, BEFORE `res.json`. D-03 honored.                                                          |
| `server/routes/cron-warm.ts`                           | Conditional `cacheSetSafe('cron:lastTick:warm', ...)` gated on partial-or-better          | ✓ VERIFIED | Line 85 — `const partialOrBetter = results.some(r => r.status === 'fulfilled')`; conditional `await cacheSetSafe(...)` inside the guard. D-04 honored.                                        |
| `server/routes/refresh-events-cron.ts`                 | `cacheSetSafe('cron:lastTick:refresh-events', ...)` AFTER `runRefreshExtraction` resolves | ✓ VERIFIED | Line 64 — placement inside `try` block, after `log.info`, before `res.status(200).json`. Catch block does NOT write. D-03 + D-05 honored.                                                     |
| `server/lib/llmProgress.ts`                            | `LLM_LASTPROGRESS_KEY`, `LLM_LASTPROGRESS_TTL_SEC`, write-through hooks                   | ✓ VERIFIED | Lines 491, 501 (constants); Lines 522-526 (resetProgress write-through, D-01); Lines 542-548 (updateProgress transition-only guard, D-02). All three present.                                 |
| `server/routes/health.ts §probeLlmStatus`              | Async, Redis-first, in-memory fallback                                                    | ✓ VERIFIED | Lines 141-171 — async signature, `cacheGetSafe<{ startedAt, completedAt }>` inside try/catch, `latest = redisLatest ?? memLatest`. Returns `freshnessMs: null` when both null (D-09 honored). |
| `server/routes/health.ts §probeProbeOnly`              | `freshnessMs: 0` (was `null`)                                                             | ✓ VERIFIED | Lines 211-219. JSDoc rewritten to cite Phase 28.2.7 R3 + deriveStatus chain.                                                                                                                  |
| `server/__tests__/routes/cron-lasttick.test.ts`        | 6 contract tests (D-12)                                                                   | ✓ VERIFIED | 6 `it` blocks. All pass.                                                                                                                                                                      |
| `server/__tests__/lib/llmProgress.persistence.test.ts` | 7 contract tests (D-12)                                                                   | ✓ VERIFIED | 7 `it` blocks. All pass.                                                                                                                                                                      |
| `server/__tests__/routes/health.probeOnly.test.ts`     | 5 contract tests (D-12)                                                                   | ✓ VERIFIED | 5 `it` blocks. All pass.                                                                                                                                                                      |
| `api/vercel-entry.js`                                  | Bundle reflects R1+R2+R3 source state                                                     | ✓ VERIFIED | 1.72 MB ESM bundle. All 5 substring contracts pass (3 cron tick keys, llm:lastProgress, 2× freshnessMs:0). Commit `48a528a` source-leak check returns 0.                                      |

### Key Link Verification

| From                            | To                                       | Via                                                                                         | Status  | Details                                                                                                                                                                       |
| ------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cron-health.ts`                | `cron:lastTick:health` Redis key         | `cacheSetSafe(...)` after eval body                                                         | ✓ WIRED | Imports `cacheSetSafe` from `../cache/redis.js` and `CRON_LASTTICK_TTL_SEC` from `../lib/healthSources.js`. Single call site at line 118.                                     |
| `cron-warm.ts`                  | `cron:lastTick:warm` Redis key           | Conditional `cacheSetSafe` inside `if (partialOrBetter)` guard                              | ✓ WIRED | Single call site at line 85. The original `allOk` const is preserved (drives log level + response status); new partial gate is independent.                                   |
| `refresh-events-cron.ts`        | `cron:lastTick:refresh-events` Redis key | `cacheSetSafe` inside try block AFTER `runRefreshExtraction` resolves                       | ✓ WIRED | Single call site at line 64. NOT inside helper `runRefreshExtraction` — D-05 helper-vs-route boundary preserved (verified by grep of helper file: `cron:lastTick` count = 0). |
| `health.ts probeCronTick`       | All three cron tick keys                 | `cacheGetSafe<number>(\`cron:lastTick:${name}\`, 999_999_999)` (reader unchanged)           | ✓ WIRED | Line 230 — reader is byte-unchanged from pre-phase state per CONTEXT (R1 fixes the upstream "no writers" gap; reader contract was already correct).                           |
| `llmProgress.ts resetProgress`  | `llm:lastProgress` Redis key             | `void cacheSetSafe(LLM_LASTPROGRESS_KEY, snapshot, LLM_LASTPROGRESS_TTL_SEC)`               | ✓ WIRED | Line 522. Fire-and-forget `void` per CONTEXT D-01.                                                                                                                            |
| `llmProgress.ts updateProgress` | `llm:lastProgress` Redis key             | Transition-only `void cacheSetSafe` inside `if (partial.completedAt !== undefined)` guard   | ✓ WIRED | Lines 542-548. Test 2 (mid-run skip) + Test 3 (terminal write) confirm semantics.                                                                                             |
| `health.ts probeLlmStatus`      | `llm:lastProgress` Redis key             | `cacheGetSafe<{startedAt, completedAt}>(LLM_LASTPROGRESS_KEY, ...)` with try/catch fallback | ✓ WIRED | Lines 148-158. Imports `LLM_LASTPROGRESS_KEY` from `../lib/llmProgress.js` (single source of truth — drift impossible).                                                       |
| `health.ts probeProbeOnly`      | `deriveStatus(0, 0, false) → 'healthy'`  | `freshnessMs: 0` literal                                                                    | ✓ WIRED | Lines 211-219. Mirrors `probeSources` (lines 177-188) — same shape, same canon.                                                                                               |

### Data-Flow Trace (Level 4)

| Artifact                              | Data Variable                          | Source                                          | Produces Real Data                                                                | Status                                          |
| ------------------------------------- | -------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------- | ----------------------------------------------- |
| `probeCronTick`                       | tickTs from `cacheGetSafe`             | Redis key `cron:lastTick:<name>`                | Yes — written by R1 cron handlers on body success                                 | ✓ FLOWING                                       |
| `probeLlmStatus` (Redis path)         | redisLatest from `cacheGetSafe`        | Redis key `llm:lastProgress`                    | Yes — written by R2 `resetProgress` + `updateProgress({completedAt})` transitions | ✓ FLOWING                                       |
| `probeLlmStatus` (in-memory fallback) | memLatest from `llmProgress` singleton | Module-level `llmProgress: LLMPipelineProgress` | Yes — `resetProgress` mutates `startedAt`; pipeline mutates `completedAt`         | ✓ FLOWING                                       |
| `probeProbeOnly`                      | `freshnessMs: 0` constant              | Static literal                                  | N/A (honest-stub by design — pure config introspection)                           | ✓ FLOWING (per design — mirrors `probeSources`) |

### Behavioral Spot-Checks

| Behavior                                                                    | Command                                                                                                                                                              | Result                                                                                                                   | Status |
| --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------ |
| Bundle contains R1 cron tick keys                                           | `grep -c "cron:lastTick:health" api/vercel-entry.js`                                                                                                                 | 1                                                                                                                        | ✓ PASS |
| Bundle contains R1 cron tick keys (warm)                                    | `grep -c "cron:lastTick:warm" api/vercel-entry.js`                                                                                                                   | 1                                                                                                                        | ✓ PASS |
| Bundle contains R1 cron tick keys (refresh-events)                          | `grep -c "cron:lastTick:refresh-events" api/vercel-entry.js`                                                                                                         | 1                                                                                                                        | ✓ PASS |
| Bundle contains R2 llm:lastProgress key                                     | `grep -c "llm:lastProgress" api/vercel-entry.js`                                                                                                                     | 1                                                                                                                        | ✓ PASS |
| Bundle contains R3 freshnessMs:0 (probeSources + probeProbeOnly)            | `grep -c "freshnessMs: 0" api/vercel-entry.js`                                                                                                                       | 2                                                                                                                        | ✓ PASS |
| 3 new test files exist + tests count                                        | `ls server/__tests__/{routes/cron-lasttick.test.ts,lib/llmProgress.persistence.test.ts,routes/health.probeOnly.test.ts}` + `grep -c "^[[:space:]]*it("`              | 3 files, 6+7+5 = 18 it blocks                                                                                            | ✓ PASS |
| New tests pass in isolation                                                 | `npx vitest run server/__tests__/routes/cron-lasttick.test.ts server/__tests__/lib/llmProgress.persistence.test.ts server/__tests__/routes/health.probeOnly.test.ts` | 18/18 passed                                                                                                             | ✓ PASS |
| Full vitest baseline + new tests                                            | `npx vitest run`                                                                                                                                                     | 2192 passed, 19 skipped, 5 todo (172 files)                                                                              | ✓ PASS |
| TypeScript clean                                                            | `npx tsc --noEmit`                                                                                                                                                   | exit 0, no output                                                                                                        | ✓ PASS |
| Helper boundary preserved (D-05) — runRefreshExtraction does NOT write tick | `grep -c "cron:lastTick" server/lib/llmExtractionPipeline.ts`                                                                                                        | 0                                                                                                                        | ✓ PASS |
| All 4 task commits present in feature branch                                | `git log --oneline main..HEAD`                                                                                                                                       | 6870259, 23ef108, 2ccc75f (R1), 67fbee8, 8ae9d9a (R2), f7bcadb (R3), 48a528a (bundle), + tests + summaries — all present | ✓ PASS |

### Requirements Coverage

| Requirement                                               | Source Plan(s) | Description                                                                  | Status             | Evidence                                                        |
| --------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------- |
| R1                                                        | 28.2.7-01      | `cron:lastTick:<name>` writers in all 3 cron handlers, on success only       | ✓ SATISFIED (code) | Truths #1, #2 above. 6 contract tests pass.                     |
| R2                                                        | 28.2.7-02      | `llm:lastProgress` write-through + Redis-first probe with in-memory fallback | ✓ SATISFIED (code) | Truths #3, #4, #5, #6 above. 7 contract tests pass.             |
| R3                                                        | 28.2.7-03      | `probeProbeOnly()` returns `freshnessMs: 0` honest-stub                      | ✓ SATISFIED (code) | Truths #7, #8 above. 5 contract tests pass.                     |
| bundle-regen                                              | 28.2.7-04      | `api/vercel-entry.js` reflects R1+R2+R3 source state                         | ✓ SATISFIED        | Truth #9 above. Commit `48a528a` is bundle-only.                |
| Acceptance: prod workflow exits 0 with allTiersGreen=true | ROADMAP / SPEC | Wave-close gate                                                              | ? NEEDS HUMAN      | Truth #12 — requires merge + Vercel deploy + workflow_dispatch. |

No orphaned requirements detected (REQUIREMENTS.md does not exist for this project; phase tracking is via plan frontmatter `requirements: [R1]` etc., all of which are accounted for above).

### Anti-Patterns Found

(Filtered to phase-modified files: `server/lib/healthSources.ts`, `server/lib/llmProgress.ts`, `server/routes/cron-health.ts`, `server/routes/cron-warm.ts`, `server/routes/health.ts`, `server/routes/refresh-events-cron.ts`, plus 3 new test files and `api/vercel-entry.js`.)

| File                                                           | Concern                                                                                                                                                                                                                            | Severity   | Impact                                                                                                                                                                                                                                                 |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `server/routes/health.ts` line 163                             | `latest = redisLatest ?? memLatest` (Redis-first wins always) — see REVIEW WR-01                                                                                                                                                   | ⚠️ Warning | Pathological sequence (crashed run leaves stale `startedAt` in Redis) can mask up to 7 days of subsequent in-memory completions on cold instances. Documented in REVIEW.md WR-01. NOT a phase blocker — matches CONTEXT D-08 read order specification. |
| `server/lib/llmProgress.ts` lines 542-548                      | `if (partial.completedAt !== undefined)` fires on both success AND error termination — see REVIEW WR-02                                                                                                                            | ⚠️ Warning | Errored terminal writes (`stage: 'error'` + `completedAt: Date.now()`) advance the freshness witness. Conceptual mismatch with R1's "tick fires only on success" invariant. Documented in REVIEW.md WR-02. NOT a phase blocker — matches CONTEXT D-02. |
| `server/__tests__/routes/cron-lasttick.test.ts`                | TTL not asserted in cron-warm "both succeed" case + missing `?force=true` Bearer-rejection coverage — see REVIEW WR-03                                                                                                             | ⚠️ Warning | TTL drift in cron-warm could slip through; auth-bypass regression in `?force=true` would not be caught. Documented as follow-up in REVIEW.md WR-03.                                                                                                    |
| `server/routes/cron-health.ts:29`, `refresh-events-cron.ts:41` | Bearer auth uses string `===` (not `timingSafeEqual`) — see REVIEW WR-04 (carried-forward, not introduced)                                                                                                                         | ⚠️ Warning | Timing-leaky in principle. Pre-existing pattern (cited "Mirrors eval-cron.ts:33-40"). Documented in REVIEW.md WR-04.                                                                                                                                   |
| 6 INFO items in REVIEW.md                                      | Cron-name string duplication (IN-01), `lastSuccessTs:Date.now()` semantic drift (IN-02), degrade-open asymmetry doc (IN-03), cron-warm no-auth (IN-04), STALE_THRESHOLD_MS outlier (IN-05), `Function` type in test helper (IN-06) | ℹ️ Info    | All carried-forward or minor maintainability concerns. Documented in REVIEW.md. Zero blockers.                                                                                                                                                         |

**Zero CRITICAL anti-patterns introduced. All 4 warnings are documented in 28.2.7-REVIEW.md as "issues_found, not phase blockers" — they describe correctness/observability concerns that align with the phase's own CONTEXT decisions or are carried-forward from earlier phases.**

### Human Verification Required

#### 1. Wave-close gate workflow exits 0 with allTiersGreen=true

**Test:** After phase merges to main and Vercel auto-deploys (~2-3 min), run `gh workflow run prod-connectivity-audit.yml -R zack-maz/onthegrid.icm` against the default target `https://otg-iran-monitor.vercel.app`.

**Expected:**

- Workflow exits 0
- Step 6 "Final status check" reads `steps.sidecar.outcome === 'success'`
- `audit:connectivity:last-result` Redis sidecar key holds:
  - `allTiersGreen: true`
  - `tierStatus: { critical: "healthy", nonCritical: "healthy" | "degraded", static: "healthy" | "degraded", probeOnly: "healthy", cron: "healthy" | "degraded" }`

**Why human:** Requires operator-controlled merge + Vercel deploy + manual `workflow_dispatch` trigger. Cannot be exercised against the local working tree. This is the SPEC's single binary acceptance signal.

#### 2. Cron tier flips green after each cron fires once

**Test:** Force-trigger each cron post-deploy:

```bash
curl -H "Authorization: Bearer $CRON_SECRET" 'https://otg-iran-monitor.vercel.app/api/cron/health'
curl -H "Authorization: Bearer $CRON_SECRET" 'https://otg-iran-monitor.vercel.app/api/cron/warm'
curl -H "Authorization: Bearer $CRON_SECRET" 'https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true'
```

Wait 5s, then `curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" https://otg-iran-monitor.vercel.app/api/health | jq '.summary.cron'`.

**Expected:** `{"healthy": 3, "degraded": 0, "unhealthy": 0, "unknown": 0}`.

**Why human:** Requires Vercel cron tick against deployed bundle. Local code state cannot reach Redis with prod credentials.

#### 3. llmStatus survives Vercel Fluid Compute cold starts

**Test:** After at least one refresh-events cron run completes, hit `/api/health` 10 times in quick succession after a 5-min idle window.

**Expected:** All 10 responses report `endpoints.llmStatus.status === "healthy"`. Pre-Plan-02, this would have shown a mix of `"healthy"` (warm instance) and `"unknown"` (cold instance from a different `x-vercel-id`).

**Why human:** Vercel Fluid Compute cold start is opportunistic and unobservable from outside; cannot reliably force a cold instance.

#### 4. probe-only tier flips green immediately at deploy

**Test:** First request after deploy: `curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" https://otg-iran-monitor.vercel.app/api/health | jq '.summary.probeOnly'`.

**Expected:** `{"healthy": 2, "unhealthy": 0, "unknown": 0}` — no cron run required.

**Why human:** Requires real `/api/health` round-trip against deployed bundle.

### Gaps Summary

**Code-level: zero gaps.** All 3 falsifiable requirements (R1, R2, R3) and the bundle-regen plan (28.2.7-04) deliver exactly what they promised at HEAD. All 18 new contract tests pass; vitest baseline 2174 → 2192 (+18, no regressions); `tsc --noEmit` exits 0; bundle (`api/vercel-entry.js`) regenerated and contains every expected R1+R2+R3 substring; commit `48a528a` is bundle-only with source-leak grep returning 0.

**Acceptance-level: 4 items require human verification.** The phase's terminal acceptance criterion ("`prod-connectivity-audit.yml` exits 0 with `allTiersGreen=true`") is mechanically achievable against the deployed bundle — every code path required to flip the cron, non-critical[llmStatus], and probe-only tiers green is present and tested. But the criterion itself can only be exercised via operator-triggered deploy + workflow run, which the verifier cannot perform from the local working tree. Per the orchestrator brief, items requiring deploy are routed to `human_needed`, not `gaps_found`.

**Known follow-ups (REVIEW.md):** 4 warnings (WR-01 through WR-04) and 6 info items (IN-01 through IN-06) are documented in `28.2.7-REVIEW.md`. None are phase blockers. WR-01 (Redis-first stale-startedAt mask) and WR-02 (errored terminal writes advance freshness witness) are correctness concerns that match CONTEXT D-02 / D-08 specifications — strengthening either is a follow-up phase, not a closure gap. WR-03 (test coverage gaps) and WR-04 (timing-safe Bearer compare) are quality-of-implementation improvements with no observable phase-goal impact.

**Recommendation:** Phase is code-complete and bundle-ready. Status `human_needed` reflects only the post-deploy workflow run that the SPEC explicitly designated as the acceptance signal. Operator can:

1. Merge `feature/28.2.7-audit-tier-completeness` to `main`.
2. Wait for Vercel auto-deploy (~2-3 min).
3. Run `gh workflow run prod-connectivity-audit.yml -R zack-maz/onthegrid.icm`.
4. Confirm exit 0 + sidecar `allTiersGreen=true`.
5. If green → phase 28.2.7 closes; phase 28.3 (k6 load test) is unblocked.
6. If any tier still red → re-verify bundle deploy via `/api/health` and trace the specific tier; the local code state guarantees the fix is in the bundle, so any prod-side failure points to deploy mechanics or external dependency state, not the source changes.

---

_Verified: 2026-05-07T22:25:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 28.2.7-audit-tier-completeness
plan: 03
subsystem: infra
tags: [observability, audit-tier, probe-only, vitest]

# Dependency graph
requires:
  - phase: 28.2.5-api-green-gate
    provides: deriveStatus four-state helper at server/lib/healthSources.ts:139, FRESHNESS_THRESHOLDS_MS for authCheck/geocode (0 ms threshold), TIER_BY_ENDPOINT 'probe-only' classifications, /api/health aggregate route + summary.probeOnly bucket
  - phase: 28.2.7-01
    provides: HEAD baseline (cron:lastTick writers landed)
  - phase: 28.2.7-02
    provides: HEAD baseline (probeLlmStatus async + Redis-first read landed) — Plan 03 modifies a DIFFERENT function (probeProbeOnly) in the same server/routes/health.ts file
provides:
  - probeProbeOnly() returns freshnessMs:0 (was null) so deriveStatus(0,0,false) flips authCheck + geocode from 'unknown' → 'healthy' immediately at deploy (no cron tick required)
  - JSDoc rewritten to document the honest-stub contract (the prior comment block described the bug behavior as the intended design — leaving it in place would mislead future readers)
  - 5 contract tests at server/__tests__/routes/health.probeOnly.test.ts pinning the SPEC R3 acceptance: deriveStatus invariant + per-endpoint freshnessMs:0 + status:'healthy' + summary.probeOnly bucket equality + null-shape regression guard
affects:
  [
    28.2.7-plan-04 (bundle regen + prod audit — depends on Plans 01/02/03 all landing first per CONTEXT D-13 atomic deploy boundary),
    28.3 (k6 load test depends on tier-green gate),
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Honest-stub contract for config-introspection probes: freshnessMs:0 + lastSuccessTs:Date.now() + hadError:false + freshnessThresholdMs:0 → deriveStatus 0<=0 → healthy. "Process running iff probe healthy" — same canon as probeSources()'
    - 'JSDoc-as-contract: when fixing a bug whose description in the prior JSDoc reads as intended design ("we report freshnessMs=null and rely on healthy/unhealthy via hadError"), rewrite the JSDoc in the same commit. Leaving stale "describes the bug" comments in place re-creates the bug on the next refactor'

key-files:
  created:
    - server/__tests__/routes/health.probeOnly.test.ts
  modified:
    - server/routes/health.ts

key-decisions:
  - "Single-character functional change: `freshnessMs: null` → `freshnessMs: 0`. Mirrors probeSources() (server/routes/health.ts:177-188) which is the established canon for 'pure config-introspection, no upstream call to fail' probes."
  - 'JSDoc fully rewritten (not patched) — the prior comment described the bug behavior as a feature ("we report freshnessMs=null and rely on healthy/unhealthy via hadError"); preserving any of it would mislead the next reader. Replacement cites Phase 28.2.7 R3, the deriveStatus chain step-by-step, and the SPEC interview decision (option-b honest-stub locked over option-a synthetic exercise).'
  - 'Test file uses /api/health round-trip pattern (probe functions are private to health.ts; round-trip is the established testability boundary per health.test.ts).'
  - 'Test 1 imports deriveStatus directly — pure-function invariant, no mocks. Fastest assertion of the SPEC R3 invariant.'
  - 'Tests 2-5 mock cacheGetSafe to return null for ALL keys so cache-backed probes flip to "unknown" but the probe-only + probeSources arms (which do not read Redis) are unaffected. The summary.probeOnly bucket is the only surface this file inspects.'
  - 'Test 5 is a narrow regression guard: asserts ONLY the negation of the original bug shape (freshnessMs !== null). Does not duplicate the positive assertions in Tests 2-3.'
  - 'Other probe functions (probeSources, probeLlmStatus, probeCronTick, probeCacheKey, runProbe, buildSummary) and FRESHNESS_THRESHOLDS_MS / TIER_BY_ENDPOINT in healthSources.ts are unchanged.'

patterns-established:
  - "JSDoc-as-contract refresh: a 1-line functional fix paired with a full JSDoc rewrite when the prior comment describes the bug as the design. The reader's mental model must match the post-fix runtime, not the pre-fix runtime."
  - 'Pure-function-first contract test layering: the fastest, lowest-coupling assertion (deriveStatus(0,0,false) === healthy) goes first; the round-trip assertions (which exercise the entire /api/health probe pipeline) come after. If the pure-function test fails, the round-trip tests are guaranteed to fail too — fast feedback during regression hunts.'

requirements-completed: [R3]

# Metrics
duration: 4min
completed: 2026-05-08
---

# Phase 28.2.7 Plan 03: probeProbeOnly honest-stub Summary

**One-line freshnessMs:null → freshnessMs:0 fix in `probeProbeOnly()` so `deriveStatus(0,0,false)` flips authCheck + geocode from perma-`unknown` to `healthy` at deploy time (no cron tick required), plus JSDoc rewrite + 5 contract tests pinning SPEC R3.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-05-08T04:51:50Z
- **Completed:** 2026-05-08T04:55:30Z
- **Tasks:** 2
- **Files modified:** 1 source + 1 new test file

## Accomplishments

- `probeProbeOnly()` in `server/routes/health.ts` now returns `freshnessMs: 0` (was `null`). Mirrors `probeSources()` (the established honest-stub canon for config-introspection probes with no upstream call to fail).
- The deriveStatus chain post-fix: `freshnessMs:0, threshold:0, hadError:false` → `hadError===false` (skip first guard) → `freshnessMs===null` is false (skip second guard) → `0 <= 0` evaluates true → return `'healthy'`. Verified by Test 1 (pure-function unit) at the lowest possible coupling.
- JSDoc fully rewritten — the prior comment block described the bug behavior as the intended design (`"we report freshnessMs=null and rely on healthy/unhealthy via hadError"`), which would mislead future readers and re-create the bug on the next refactor. Replacement cites Phase 28.2.7 R3, walks through the deriveStatus chain step-by-step, and notes the SPEC interview decision (option-b honest-stub locked over option-a synthetic middleware exercise).
- Probe-only tier (authCheck, geocode) flips from `summary.probeOnly: { healthy: 0, unhealthy: 0, unknown: 2 }` → `{ healthy: 2, unhealthy: 0, unknown: 0 }` immediately at deploy. No cron run required — the probe is synchronous; the flip is mechanical.
- 5 contract tests in `server/__tests__/routes/health.probeOnly.test.ts`:
  1. Pure-function invariant: `deriveStatus(0, 0, false) === 'healthy'`.
  2. `/api/health` round-trip — `endpoints.authCheck` has `freshnessMs:0` + `status:'healthy'` + `tier:'probe-only'`.
  3. `/api/health` round-trip — `endpoints.geocode` has `freshnessMs:0` + `status:'healthy'` + `tier:'probe-only'`.
  4. `/api/health` round-trip — `summary.probeOnly` deep-equals `{ healthy: 2, unhealthy: 0, unknown: 0 }`.
  5. Regression guard — neither `authCheck` nor `geocode` has `freshnessMs: null` (the original bug shape).
- Full vitest suite: **2192 passed** (baseline 2187 + 5 new), 19 skipped, 5 todo. `tsc --noEmit` exits 0. Zero regressions.

## Task Commits

Each task was committed atomically on `feature/28.2.7-audit-tier-completeness`:

1. **Task 1: Source change — probeProbeOnly returns freshnessMs:0 + JSDoc rewrite** — `f7bcadb` (feat)
2. **Task 2: Vitest contract tests for probeProbeOnly honest-stub** — `bc2903d` (test)

## Files Created/Modified

### Created

- `server/__tests__/routes/health.probeOnly.test.ts` — 5 contract tests in a single describe (`probeProbeOnly — Phase 28.2.7 R3 honest-stub contract`). Reuses the `createReqRes` / `extractHandler` `/api/health` round-trip harness from `routes/health.test.ts`; mocks `cacheGetSafe` to return null for ALL keys so cache-backed probes flip to 'unknown' but the probe-only + probeSources arms (which do not read Redis) are unaffected. Test 1 imports `deriveStatus` directly for the pure-function invariant — no mocks.

### Modified

- `server/routes/health.ts`:
  - `probeProbeOnly()` body — single functional change: `freshnessMs: null` → `freshnessMs: 0`.
  - JSDoc above `probeProbeOnly()` — fully rewritten. New text cites Phase 28.2.7 R3 + the deriveStatus chain step-by-step + the SPEC interview decision (option-b honest-stub locked).
  - All other functions (`probeSources`, `probeLlmStatus`, `probeCacheKey`, `probeCronTick`, `runProbe`, `buildSummary`) and the `healthRouter.get('/')` handler are byte-unchanged.

## Decisions Made

All key implementation decisions were locked in `28.2.7-CONTEXT.md` (R3 section: "No additional decisions beyond SPEC R3. Two-line change.") and SPEC.md R3. No new decisions were needed during execution; the plan's `<action>` block specified the exact replacement text for both the JSDoc block and the function body.

## Deviations from Plan

None — plan executed exactly as written. All grep contracts in `<verify>` and `<acceptance_criteria>` blocks of both tasks pass. tsc clean. vitest 2187 → 2192 (+5) on the first run.

One minor acceptance-criteria-spec drift to note: Task 2's acceptance criterion `grep "{ healthy: 2, unhealthy: 0, unknown: 0 }" server/__tests__/routes/health.probeOnly.test.ts returns exactly 1 match` returns 2 matches in the actual file — once in the test name string (`it('/api/health summary.probeOnly === { healthy: 2, unhealthy: 0, unknown: 0 }', ...)`) and once in the `toEqual()` assertion. Both occurrences are intentional: the title was specified verbatim by the plan's `<behavior>` block (Test 4: `body.summary.probeOnly === { healthy: 2, unhealthy: 0, unknown: 0 }`), and the assertion is what the plan's `<action>` block writes. The spec's "exactly 1" was an over-strict count — the intent was "the assertion is present", which it is. Functional outcome is correct; runtime test passes; this is a benign acceptance-criteria miscount in the plan template, not a correctness issue.

---

**Total deviations:** 0 (1 acceptance-criteria miscount noted, no source-of-truth change)
**Impact on plan:** None.

## Issues Encountered

- The `git commit` lint hook (eslint --fix + prettier --write) inserted non-null-assertion `!` operators on `endpoints.authCheck` / `endpoints.geocode` accesses I had originally written without them; the post-format file is functionally identical and still passes all 5 tests. Left as-is per "lint hooks are part of the project's commit protocol" convention — same posture as Plan 02 took for its lint-hook-driven dynamic-import collapse.
- The pre-existing `.planning/STATE.md` modification was already present in the working tree at plan start (orchestrator-owned per the spawn prompt: "Do NOT update STATE.md or ROADMAP.md — the orchestrator owns those writes after the wave completes"). Left untouched throughout this plan.

## User Setup Required

None — no external service configuration required, no env vars added, no new Redis keys created.

## Next Phase Readiness

- **Plan 04** (bundle regen + prod-connectivity-audit run) is now unblocked: all three source-fix plans (R1 cron writers in Plan 01, R2 llm:lastProgress write-through + Redis-first probe in Plan 02, R3 probeProbeOnly honest-stub in Plan 03) have landed on `feature/28.2.7-audit-tier-completeness`. Plan 04 is responsible for running `npm run build`, committing the regenerated `api/vercel-entry.js` + `dist-server/` bundle, and triggering the `prod-connectivity-audit.yml` workflow against the deployed prod URL. Per CONTEXT D-13, the bundle commit lands AFTER all 3 source-code commits so the bundle reflects the final source state at deploy time.
- **Phase 28.3** (k6 load test) remains gated on `prod-connectivity-audit.yml` exiting 0 with `allTiersGreen=true` — Plan 04's responsibility.

### End-to-end probe verification (post-Plan-04 deploy)

Per the plan's `<verification>` block, after all 4 plans ship and Vercel deploys:

1. Hit /api/health: `curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" https://otg-iran-monitor.vercel.app/api/health | jq '.endpoints.authCheck, .endpoints.geocode'`
2. Expect: BOTH endpoints show `"status": "healthy"`, `"freshnessMs": 0`, `"tier": "probe-only"`.
3. Verify summary: `curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" https://otg-iran-monitor.vercel.app/api/health | jq '.summary.probeOnly'`
4. Expect: `{"healthy": 2, "unhealthy": 0, "unknown": 0}`.

This probe is synchronous and requires no cron run — the flip is immediate at deploy time. Unlike R1 (cron tick writers, which require all 3 crons to fire at least once before the cron tier flips green) and R2 (llm:lastProgress write-through, which requires at least one pipeline run before the Redis-backed read returns non-null), R3 is mechanical: the post-deploy `/api/health` response will show `summary.probeOnly: { healthy: 2, unhealthy: 0, unknown: 0 }` on the first request after the bundle goes live.

## Self-Check: PASSED

- File `server/routes/health.ts`: FOUND — `grep -A 8 "function probeProbeOnly()"` shows `freshnessMs: 0,` (1 match), `freshnessMs: null,` (0 matches); `grep -B 18 "function probeProbeOnly()"` shows `Phase 28.2.7 R3` + `honest-stub` markers (2 matches total).
- File `server/__tests__/routes/health.probeOnly.test.ts`: FOUND — 5 `it` blocks; `import { deriveStatus }` (1 match); `endpoints.authCheck.freshnessMs` (2 matches — positive + regression guard); `endpoints.geocode.freshnessMs` (2 matches); `summary.probeOnly` (2 matches); `deriveStatus(0, 0, false)` (2 matches — assertion + JSDoc).
- File `server/routes/health.ts §probeSources` (lines 177-188): FOUND, byte-unchanged — `grep -A 8 "function probeSources()" | grep -c "freshnessMs: 0,"` returns 1 (the pre-existing canon, untouched).
- Commit `f7bcadb` (Task 1 source change): FOUND.
- Commit `bc2903d` (Task 2 contract tests): FOUND.
- vitest 2192/2192 PASS (baseline 2187 + 5 new from Task 2), tsc --noEmit exit 0, zero regressions in any of the 11 cases in `routes/health.test.ts` or the 7 cases in `lib/llmProgress.persistence.test.ts` from Plan 02.

---

_Phase: 28.2.7-audit-tier-completeness_
_Plan: 03_
_Completed: 2026-05-08_

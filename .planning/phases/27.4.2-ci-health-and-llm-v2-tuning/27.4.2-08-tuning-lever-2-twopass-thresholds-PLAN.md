---
phase: 27.4.2
plan: 08
type: execute
wave: 2
depends_on: [27.4.2-07]
files_modified:
  - server/lib/llmResolver.ts
autonomous: true
requirements_addressed: [D-11-lever-2, D-12, D-13, D-14, D-15]
nyquist_compliant_intent: true
user_setup: []
tags: [llm-tuning, lever-2, two-pass-verify, gate-threshold, wr-04, resolver]

must_haves:
  truths:
    - 'shouldTriggerTwoPassVerify() 250km gate (line ~356 in llmResolver.ts) tested at one or more lower thresholds (e.g., 150km, 200km) per D-11 lever 2A'
    - 'WR-04 single-candidate auto-accept (lines 392-405) tested with disable-toggle per D-11 lever 2B alternative'
    - 'After EACH sub-lever commit: npm run eval:replay shows within20km/total ≥ Plan 06 baseline (D-14 enforced; revert if regress)'
    - 'Each sub-lever atomic commit per D-15'
    - 'After all sub-levers: wave-boundary full production run; D-13 stop check applied'
    - 'If D-13 stop met → wave done; backlog P9/P10; phase close. If not → orchestrator routes to Plan 09 (System Prompt).'
  artifacts:
    - path: 'server/lib/llmResolver.ts'
      provides: 'Tuned 2-pass verify threshold (formerly 250km) AND/OR WR-04 single-candidate auto-accept policy change'
      contains: 'shouldTriggerTwoPassVerify'
  key_links:
    - from: 'server/lib/llmResolver.ts shouldTriggerTwoPassVerify'
      to: 'server/lib/llmResolver.ts resolveViaPoiAmenity / resolveViaNominatimDirect / resolveViaVerifiedTwoPass'
      via: 'branch dispatch threshold'
      pattern: 'shouldTriggerTwoPassVerify'
    - from: 'server/lib/llmResolver.ts WR-04 auto-accept (lines 392-405)'
      to: 'Branch 4 reranker LLM call'
      via: 'single-candidate skip OR force-rerank decision'
      pattern: "candidates\\.length === 1"
---

<objective>
Execute D-11 lever 2 (2-pass verify thresholds) — second-priority lever for `gdelt-actiongeo-fallback%` reduction. Two sub-lever surfaces in `server/lib/llmResolver.ts`:

**Lever 2A (250km gate):** `shouldTriggerTwoPassVerify()` (line ~349-359) currently returns true for `precision === 'region' || precision === 'city' || dist > 250`. The 250km threshold is too generous for ME theater (most ME countries are < 250km wide → trips trivially). Lower to 150km (CONTEXT D-11 explicit candidate) to force reranker on coarse hits earlier; pairs better with the Branch 4 candidate-set width.

**Lever 2B (WR-04 single-candidate auto-accept):** `resolveViaVerifiedTwoPass` lines 392-405 currently SHORT-CIRCUITS reranker on N=1 candidate (the WR-04 cache-miss-prevention rationale). CONTEXT D-11 names this as the alternative test: disable the auto-accept to force reranker on N=1, catching "Beirut, Oregon" false positives. Cost: ~50% more reranker tokens per CONTEXT — watch DevApiStatus token-counter delta on the next wave-boundary production run; if Cerebras pushes toward soft-cap (80%), scale back.

Per D-12 micro-iteration, each sub-lever is its own atomic commit with `npm run eval:replay` output captured. Per D-14 revert policy, regression at 20km → revert + closed-negative hypothesis.

Order of sub-levers: 2A first (one-character constant change, cheap to test); 2B second (more invasive, more token cost — only attempt if 2A alone doesn't close D-13).

Output: 1-3 atomic commits on `server/lib/llmResolver.ts`. After each, GT replay; after all, wave-boundary production run + D-13 check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-LLM-PIPELINE-ANALYSIS.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-06-SUMMARY.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-07-SUMMARY.md
@server/lib/llmResolver.ts

<interfaces>
shouldTriggerTwoPassVerify() current state (server/lib/llmResolver.ts:349-359):

```typescript
function shouldTriggerTwoPassVerify(
  hierarchy: LocationHierarchyV2,
  hit: SnapshotHit,
  ctx: ResolveContext,
): boolean {
  const precision = derivePrecision(hierarchy);
  if (precision === 'region' || precision === 'city') return true;
  const dKm = haversineKm(hit.lat, hit.lng, ctx.centroidLat, ctx.centroidLng);
  if (dKm > 250) return true; // ← Lever 2A target
  return false;
}
```

WR-04 single-candidate auto-accept current state (server/lib/llmResolver.ts:392-406):

```typescript
if (candidates.length === 1) {
  // WR-04: single unambiguous Nominatim result — accept without LLM
  // reranking. The 2-pass reranker exists to disambiguate; one
  // candidate needs no disambiguation. Caching a miss here would
  // wrongly route this query to the lower-quality GDELT fallback for
  // the next 30d.
  const only = candidates[0]!;
  const hit: SnapshotHit = {
    lat: only.lat,
    lng: only.lng,
    displayName: only.displayName,
  };
  await cacheSetSafe(key, hit, GEOCODE_CACHE_REDIS_TTL_SEC);
  return hit;
}
```

Pre-tuning baselines (read from .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-06-SUMMARY.md AND 27.4.2-07-SUMMARY.md):

- within20km/total = X.XXX (post-Lever-1 if Lever-1 landed)
- +5pp absolute target = (Plan 06 baseline) + 0.05
- gdelt-actiongeo-fallback% = NN.N% (target < 25%)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight — check whether to SKIP this plan per D-13</name>
  <files>(read-only check)</files>
  <read_first>
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-07-SUMMARY.md (Plan 07 wave-boundary outcome — primary reference for D-13 verdict)
  </read_first>
  <action>
1. Read Plan 07 SUMMARY (the immediately-prior wave-boundary). If P07 wrote no SUMMARY (e.g. it errored or was skipped), abort and request orchestrator re-route — P08 cannot proceed without P07's wave-boundary verdict.
2. Look for the D-13 stop check verdict. Two possible outcomes:
   - "STOP — both goals met (eval +Xpp ≥ +5pp at 20km AND fallback NN.N% < 25%)"
   - "CONTINUE — Goal A met / Goal B not met" (or similar partial)
3. If STOP:
   - Write a brief plan summary at `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-08-SUMMARY.md` saying:
     ```
     # Plan 08 — SKIPPED
     D-13 stop condition was met at Plan 07 (commit ABCDEF). Per CONTEXT
     D-13 ("As soon as eval score beats v1 by ≥5pp at 20km AND fallback
     drops below 25%, the tuning wave is done"), this plan is skipped.

     Backlog items filed via /gsd-add-todo for Lever 2 + Lever 3 + Lever 4
     sub-hypotheses:
       - "Phase 27.4.X future tuning — Lever 2A (250km → 150km gate) per
          CONTEXT D-11 lever 2 + Specifics lines 522-536"
       - "Phase 27.4.X future tuning — Lever 2B (WR-04 single-candidate
          auto-accept disable) per CONTEXT D-11 lever 2 alternative + §12 Q5"
       - "Phase 27.4.X future tuning — Lever 3 SYSTEM_PROMPT_V2 tightening
          per CONTEXT D-11 lever 3 + PATTERNS lines 647-689 candidates"
       - "Phase 27.4.X future tuning — Lever 4 NEWS/TEMPORAL/BELLINGCAT
          block tuning per CONTEXT D-11 lever 4 + §12 Q1/Q3/Q4/Q10"
     ```

- File the four backlog todos via `/gsd-add-todo` (one per sub-hypothesis category — they are independent levers).
- This task is the only task that runs; Tasks 1-3 below are skipped.
- Plan close.

4. If CONTINUE: proceed to Task 1.
   </action>
   <verify>
   <automated>test -f .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-08-SUMMARY.md</automated>
   </verify>
   <acceptance_criteria> - Plan 08 SUMMARY exists, declaring either "SKIPPED" with backlog SHA references OR "PROCEEDING" with the current eval/fallback state inherited from P07 - If SKIPPED: backlog todos filed via `/gsd-add-todo` for Lever 2A, Lever 2B, Lever 3, and Lever 4 - If PROCEEDING: Task 1 begins immediately
   </acceptance_criteria>
   <done>
   Plan 08 disposition set: skipped or proceeding.
   </done>
   </task>

<task type="auto" tdd="true">
  <name>Task 1: Lever 2A — lower 250km gate to test value (default 150km)</name>
  <files>server/lib/llmResolver.ts</files>
  <read_first>
    - server/lib/llmResolver.ts (lines 349-359 — shouldTriggerTwoPassVerify; read first per modify-in-place safety)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (Specifics lines 522-536 — proposed 150km value with rationale)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md (lines 597-611 — exact edit shape; one-character constant change)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-LLM-PIPELINE-ANALYSIS.md (§12 Q2 hypothesis — 2-pass verify threshold sensitivity)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-07-SUMMARY.md (current eval state from Lever 1)
    - **Eval baseline drift guard (T-27.4.2-04 mitigation):** before any source edit in this task, run `git diff HEAD .planning/eval/ground-truth-events.json` — output MUST be empty. If non-empty, ABORT this task — the ground-truth corpus has drifted from Plan 06's frozen baseline and any eval comparison is invalid. Surface the drift to the orchestrator and request reset to the P06 commit's tree state for that file before retrying.
  </read_first>
  <behavior>
    - Test 1: shouldTriggerTwoPassVerify returns true for hits where dist > 150km (was > 250km)
    - Test 2: hits where 150 < dist ≤ 250km that previously skipped reranker now invoke it
    - Test 3: npm run eval:replay shows within20km/total ≥ Plan 06 baseline (no regression — D-14)
    - Test 4: npm run eval:replay tracking 5km may move slightly — that's OK per D-14 (gate is 20km only)
    - Test 5: server suite still passes (no test mocks the 250 constant)
  </behavior>
  <action>
1. Open `server/lib/llmResolver.ts` at line 356.
2. Change `if (dKm > 250) return true;` to `if (dKm > 150) return true;`.
3. Add a one-line inline comment explaining the change:

```typescript
// Phase 27.4.2 P8 lever 2A: 250km → 150km. Original threshold was too
// generous for ME theater (most ME countries < 250km wide trip trivially);
// forcing reranker on coarser hits per D-11 lever 2 + §12 Q2 hypothesis.
if (dKm > 150) return true;
```

4. Run `npm run eval:replay 2>&1 | tee /tmp/_eval_lever2a.txt`. Capture per-distance counts.
5. Compare to Plan 06 baseline within20km/total (NOT Plan 07 — Plan 07's eval may have moved Lever 1's contribution; Plan 08 measures incremental delta but the gate is still vs the original Plan 06 fixed comparator).
6. If `within20km/total < Plan 06 baseline`: REGRESSION. Discard the edit (`git checkout server/lib/llmResolver.ts`) AND optionally try a less-aggressive value: 200km. If 200km also regresses, REVERT the lever entirely; document closed-negative §12 Q2 hypothesis in plan summary; proceed to Task 2.
7. If `within20km/total ≥ Plan 06 baseline` (PASS) AND `within20km/total ≥ Plan 06 baseline + 0.05` (D-13 goal A potentially met):
   - Run linting/format/test gates: `npm run lint --silent && npx prettier --check server/lib/llmResolver.ts && npx vitest run server/`
   - Commit:

```
feat(27.4.2): tune 2-pass verify gate — lower 250km to 150km

eval 5km/20km/100km: A→B / C→D / E→F

D-11 lever 2A: 250km too generous for ME theater (most countries
< 250km wide trip the gate trivially), forcing reranker on coarser
hits per §12 Q2 hypothesis. Pairs with Branch 4 candidate-set width.

Token-cost note: ~N% more reranker calls expected on next production
run; verify against soft-cap budget at wave-boundary checkpoint.
```

8. If you tested 200km as a fallback and it landed, the commit message reflects 200km instead of 150km.
9. If both 150km and 200km regress, revert both attempts; the gate value stays at 250km; move to Task 2 (WR-04 alternative).
   </action>
   <verify>
   <automated>grep -E "if \\(dKm > [0-9]+\\) return true" server/lib/llmResolver.ts | head -1 &amp;&amp; npm run eval:replay 2>&amp;1 | tail -3 | grep "within20km/total"</automated>
   </verify>
   <acceptance_criteria> - The numeric threshold in `shouldTriggerTwoPassVerify` is now < 250 (verify: `grep -E "if \\(dKm > (150|200)\\) return true" server/lib/llmResolver.ts` exits 0) OR the lever was REVERTED with closed-negative documentation - Commit message body contains `eval 5km/20km/100km: ...` line with concrete numbers - `npm run eval:replay` final ratio ≥ Plan 06 baseline within20km/total - All four D-02 gates still green
   </acceptance_criteria>
   <done>
   Lever 2A landed (or reverted with documentation); ready for Task 2 evaluation.
   </done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2: Lever 2B — disable WR-04 single-candidate auto-accept (alternative)</name>
  <files>server/lib/llmResolver.ts</files>
  <read_first>
    - server/lib/llmResolver.ts (lines 388-410 — read full block; the WR-04 explanation comment is critical context for the revert decision)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-11 lever 2 alternative; D-14 revert policy)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md (lines 615-636 — exact edit shape)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-LLM-PIPELINE-ANALYSIS.md (§12 Q5 / §11 lever 2 — WR-04 hypothesis: may accept POI false positives like "Beirut, Oregon")
  </read_first>
  <behavior>
    - Test 1: When candidates.length === 1, the resolver now invokes the reranker LLM call instead of short-circuiting
    - Test 2: npm run eval:replay shows within20km/total ≥ Plan 06 baseline (D-14)
    - Test 3: token cost increases — wave-boundary production run will show higher Cerebras tokens; expected ~50% more reranker calls per CONTEXT D-11 lever 2B
    - Test 4: WR-04 cache-miss prevention rationale is preserved by the alternative shape (cache misses still get cached so subsequent identical queries don't blow Nominatim throttle)
  </behavior>
  <action>
**Decision gate:** Only proceed to Task 2 if Task 1 outcome (Lever 2A) did NOT close D-13. If Plan 06 baseline + 0.05 is already crossed AND fallback% < 25% on Lever 2A's incremental, then this task is SKIPPED — go straight to Task 3 wave-boundary check.

If proceeding:

1. Open `server/lib/llmResolver.ts` lines 392-406.
2. The cleanest edit is to COMMENT OUT the entire `if (candidates.length === 1)` block (preserves context for future revisit) AND add an inline-explanation comment. Replace lines 392-406 with:

```typescript
// Phase 27.4.2 P8 lever 2B: WR-04 single-candidate auto-accept disabled.
// Hypothesis (§12 Q5): single-candidate path was accepting POI false positives
// ("Beirut, Oregon" for Beirut, Lebanon queries) without reranker disambiguation.
// Cost: ~50% more reranker tokens — watch token-counter at wave-boundary.
//
// PRESERVED FOR REVERT: original WR-04 block is the previous git commit; restore
// via `git revert HEAD` if eval regresses or token soft-cap is breached.
//
// Cache-miss-prevention rationale is preserved because the reranker path also
// calls cacheSetSafe on the chosen candidate (see resolveViaVerifiedTwoPass tail).
```

(Effectively: delete the `if (candidates.length === 1) { ... return hit; }` block, leaving the comment as the diff context.)

3. Run `npm run eval:replay 2>&1 | tee /tmp/_eval_lever2b.txt`. Capture per-distance counts.
4. Compare to Plan 06 baseline:
   - If `within20km/total < Plan 06 baseline`: REGRESSION. Revert (`git revert HEAD --no-edit`). Document closed-negative §12 Q5 sub-hypothesis ("WR-04 disable degrades 20km eval; cache-miss-prevention rationale validated"). Proceed to Task 3.
   - If `within20km/total ≥ Plan 06 baseline`: PASS. Proceed to commit.
5. Run lint/format/test gates.
6. Commit:

```
feat(27.4.2): tune WR-04 — disable single-candidate auto-accept

eval 5km/20km/100km: A→B / C→D / E→F

D-11 lever 2B: WR-04 was accepting Nominatim single-candidate
results without reranker disambiguation, plausibly admitting POI
false positives like "Beirut, Oregon" for "Beirut, Lebanon"
queries (§12 Q5 hypothesis).

Token-cost: expect ~50% more reranker calls; soft-cap monitoring
at wave-boundary. Revert via git revert if soft-cap breached.
```

  </action>
  <verify>
    <automated>(grep -c "candidates\\.length === 1" server/lib/llmResolver.ts | grep "^1$" &amp;&amp; echo "still has WR-04 — task skipped or reverted") || (! grep -q "if (candidates\\.length === 1)" server/lib/llmResolver.ts &amp;&amp; echo "WR-04 disabled — task landed")</automated>
  </verify>
  <acceptance_criteria>
    - One of three valid outcomes:
      (a) Task SKIPPED because Task 1 (Lever 2A) crossed D-13 — plan summary records "skipped: Lever 2A sufficient"
      (b) Task LANDED with WR-04 disabled — `grep -c "if (candidates\\.length === 1)" server/lib/llmResolver.ts` returns 0 AND eval ≥ baseline
      (c) Task REVERTED — closed-negative §12 Q5 hypothesis documented in plan summary
    - If landed: commit message body contains the eval before/after AND the token-cost warning AND the revert instructions
    - All four D-02 gates still green
  </acceptance_criteria>
  <done>
    Lever 2B evaluated (skipped, landed, or reverted with documentation); ready for Task 3 wave-boundary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave-boundary checkpoint — full production run + D-13 stop check</name>
  <files>(no source edits — wave-boundary measurement)</files>
  <read_first>
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-13 stop condition; D-06/D-07 hang-recurrence)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md (Pitfall 4 — cooldown-clear method, NOT `?force=true`)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-06-SUMMARY.md (Plan 06 baseline reference)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-07-SUMMARY.md (Plan 07 wave-boundary state)
  </read_first>
  <action>
1. Trigger a fresh full production run per Pitfall 4 cooldown-clear method:
   a. `redis-cli del events:llm-process-ts`
   b. `redis-cli del events:llm:v2`
   c. `curl -s 'http://localhost:5173/api/events' > /dev/null`
   d. Poll `/api/events/llm-status` until `progress.stage === 'idle'` (this can take 20-45 min for a full 184-batch v2 run)
2. Capture from `lastRun.summary`:
   - `evalScore.within20km / total` ratio
   - `provenanceCounts['gdelt-actiongeo-fallback'] / sum(provenanceCounts)` (fallback%)
   - `watchdogTimeoutCount`
   - `tokenCounters.cerebras` and `tokenCounters.groq` — compare to budget caps (1M/200K) — if Cerebras > 80% (soft-cap warning), note this for Task 4 decision-making in Plan 09 (system prompt could be tightened to reduce token spend)
3. Apply D-13 stop check:
   - Goal A: post-tuning within20km/total ≥ Plan 06 baseline + 0.05
   - Goal B: post-tuning fallback% < 0.25
   - **STOP if BOTH met:** wave is done. Capture untouched levers (P9/P10) as backlog via `/gsd-add-todo` (one todo per lever category — system-prompt tuning, NEWS/TEMPORAL/BELLINGCAT block tuning). Proceed to phase close.
   - **CONTINUE if either unmet:** orchestrator routes to Plan 09 (Lever 3 — system prompt tightening).
4. Apply D-07 hang-recurrence response if needed:
   - If `watchdogTimeoutCount ≥ 3`: invoke `setProviderOrderOverride(['groq', 'cerebras'])` via a one-off tsx script. Document the flip in plan summary. The flip persists for the rest of the wave (in-memory only).
5. Document the result. Important state for downstream:
   - If continuing: Plan 09 inherits this state as its baseline for measuring Lever 3 contribution
   - If stopping: phase close runbook (separate handoff or final plan) restores `setProviderOrderOverride(null)` if a flip happened
  </action>
  <verify>
    <automated>curl -s 'http://localhost:5173/api/events/llm-status' 2>/dev/null | jq -e '.lastRun.summary != null' || echo "Production run state captured manually for plan summary"</automated>
  </verify>
  <acceptance_criteria>
    - Plan summary documents post-Lever-2 production run results: within20km/total, fallback%, watchdogTimeoutCount, tokenCounters
    - D-13 stop check explicitly recorded ("STOP — both goals met" OR "CONTINUE — Goal A met / Goal B not met (NN.N%)" or similar)
    - D-07 hang-recurrence response applied if needed; commit/run-id of flip recorded
    - Soft-cap monitoring noted (Cerebras / Groq token usage vs daily caps)
    - Backlog todos filed if wave stops; handoff state recorded if wave continues
  </acceptance_criteria>
  <done>
    Wave-boundary state captured; orchestrator knows whether to continue (Plan 09) or close.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                          | Description                                                                                |
| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Resolver branch dispatch threshold → coordinate quality                           | Wrong threshold value can route correct hits to slower paths or skip needed disambiguation |
| Single-candidate auto-accept → cache miss prevention vs false-positive admittance | The WR-04 design embeds a tradeoff that Lever 2B re-evaluates                              |

## STRIDE Threat Register

| Threat ID          | Category                                                                                                                                                                        | Component                                            | Disposition | Mitigation Plan                                                                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-27.4.2-LEVER2-01 | T (Tampering — gate threshold lowered too far, paying token cost without lift)                                                                                                  | server/lib/llmResolver.ts shouldTriggerTwoPassVerify | mitigate    | Task 1 D-14 enforcement: revert if 20km regresses; soft-cap monitoring at Task 3 wave-boundary catches token over-spend                                                                                                                 |
| T-27.4.2-05        | D (Token budget exhaustion if Lever 2B lands and reranker calls double)                                                                                                         | server/lib/llmTokenBudget.ts soft-cap 0.8            | mitigate    | Task 3 explicitly checks tokenCounters vs daily caps; Plan 09 (next) inherits the soft-cap state                                                                                                                                        |
| T-27.4.2-LEVER2-02 | I (Information disclosure — WR-04 cache-miss-prevention rationale lost in revert chain)                                                                                         | server/lib/llmResolver.ts WR-04 block                | mitigate    | Task 2 commit message body documents the original WR-04 rationale + the revert instruction (`git revert HEAD`); the comment block is preserved as the diff context                                                                      |
| T-27.4.2-03        | T (Provider-flip leakage across phases)                                                                                                                                         | setProviderOrderOverride state                       | mitigate    | In-memory only; phase-close task explicitly resets to null                                                                                                                                                                              |
| T-27.4.2-04        | I (Information disclosure — eval baseline drift mid-wave: .planning/eval/ground-truth-events.json modified between P06 and P08 silently invalidates P08 acceptance comparisons) | .planning/eval/ground-truth-events.json              | mitigate    | Task 1 read_first explicitly runs `git diff HEAD .planning/eval/ground-truth-events.json` before any source edit; non-empty diff aborts the task and requests orchestrator reset; corpus is frozen for the entire phase per scope guard |

</threat_model>

<verification>
After Task 1 and Task 2, `npm run eval:replay` within20km/total ≥ Plan 06 baseline. After Task 3, capture wave-boundary state; D-13 stop check applied.
</verification>

<success_criteria>

- Lever 2A tested (250km → 150km or 200km, or REVERTED with documentation)
- Lever 2B tested (WR-04 disabled, SKIPPED if Lever 2A sufficient, or REVERTED with documentation)
- Per-sub-lever atomic commits; D-14 revert policy enforced
- Final eval ≥ Plan 06 baseline; wave-boundary fallback% recorded
- D-13 stop check applied; orchestrator routed to phase close OR Plan 09
  </success_criteria>

<output>
After completion, create `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-08-SUMMARY.md` documenting:
- Lever 2A outcome: threshold value (250 → 150/200/reverted) + eval before/after
- Lever 2B outcome: skipped|landed|reverted + eval before/after
- Wave-boundary production run: within20km/total, fallback%, watchdogTimeoutCount, tokenCounters
- D-13 stop check (STOP/CONTINUE) + reasoning
- Closed-negative §12 hypotheses (if any reverts)
- Hang-recurrence flip outcome (if any)
- If wave continues: handoff to Plan 09 with current state as the inherited baseline
- If wave stops: backlog `/gsd-add-todo` SHAs for P9 + P10
- Token soft-cap status (Cerebras + Groq daily usage)
- Commit SHAs for every kept commit
</output>

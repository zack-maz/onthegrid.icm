---
phase: 27.4.2
plan: 02
type: execute
wave: 1
depends_on: [27.4.2-01]
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements_addressed: [D-04]
nyquist_compliant_intent: true
user_setup: []
tags: [ci-health, npm-audit, dependencies, security]

must_haves:
  truths:
    - 'npm audit --audit-level=high reports zero high-severity advisories'
    - 'npm test still passes post-bump (Plan 01 fix preserved + no test regressions from dep bumps)'
    - 'npm run build still succeeds post-bump'
    - 'No major version jumps applied (no --force used)'
  artifacts:
    - path: 'package.json'
      provides: 'Updated minor/patch version pins for advisory-affected deps'
    - path: 'package-lock.json'
      provides: 'Regenerated lock with resolved transitive advisories'
  key_links:
    - from: 'package.json dependencies'
      to: 'npm audit advisory database'
      via: 'npm audit fix (no --force)'
      pattern: '(brace-expansion|fast-xml-parser|flatted|path-to-regexp|picomatch|postcss)'
---

<objective>
Apply `npm audit fix` (no `--force`) in a single atomic commit to clear all high-severity advisories that resolve cleanly via minor/patch bumps. Verify post-bump that `npm test` and `npm run build` still pass.

Purpose: D-04 CI gate — `npm audit --audit-level=high` must report zero advisories before tuning wave begins per D-02 strict gate. Isolated diff ensures dependency-bump review is clean (D-04 explicitly: "do not chain it to other CI fixes so the dependency-bump diff is reviewable in isolation").

Output: Updated `package.json` + `package-lock.json` with cleared high-severity advisories; one atomic commit; any residual moderate-severity advisories that would require `--force` documented in plan summary as backlog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot pre-bump audit state</name>
  <files>(read-only)</files>
  <read_first>
    - package.json (full file — confirm current dep pins; check `engines.node` is `>=20`)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-04 lines 115-126 — verbatim decision)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md (lines 76-89 — D-04 mechanical operation, no analog needed)
  </read_first>
  <action>
Run `npm audit --audit-level=high --json > /tmp/audit-before.json 2>&1 || true` (the `|| true` is intentional — `npm audit` exits non-zero when advisories exist; we want the JSON regardless).

Then run `npm audit --audit-level=high 2>&1 | tee /tmp/audit-before.txt || true` to capture the human-readable form.

Capture for the commit message body and plan summary:

- Total advisories listed (count)
- Per-package list (e.g., brace-expansion@1.x.x → fixable, fast-xml-parser@4.x.x → fixable, etc.)
- Any advisories marked "fix unavailable" or "requires --force" — these are residual

Do NOT commit at this step. This is information gathering.
</action>
<verify>
<automated>test -f /tmp/audit-before.txt && wc -l /tmp/audit-before.txt</automated>
</verify>
<acceptance_criteria> - `/tmp/audit-before.txt` exists and has non-zero size - `/tmp/audit-before.json` is parseable JSON (`jq . /tmp/audit-before.json` exits 0) OR documents "no advisories" if state is already clean - Pre-bump advisory count captured for plan summary
</acceptance_criteria>
<done>
Baseline audit state captured for delta comparison; ready to apply fix.
</done>
</task>

<task type="auto">
  <name>Task 2: Run npm audit fix (no --force) and verify suite still green</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - /tmp/audit-before.txt (output of Task 1)
    - package.json (confirm scripts.test, scripts.build are defined)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-04 explicit reject of --force)
  </read_first>
  <action>
1. Run `npm audit fix` (NO `--force`, NO `--production` — full dev tree).
2. Confirm `package.json` and/or `package-lock.json` were modified: `git diff --stat package.json package-lock.json`.
3. Run `npx vitest run src/__tests__/filters.test.ts` — must still pass (preserves Plan 01 work).
4. Run `npm run build` — must succeed (validates Vite frontend + tsup server bundle + tsc still compile after dep bumps).
5. Run `npm audit --audit-level=high 2>&1 | tee /tmp/audit-after.txt || true` to capture residual state.
6. If high-severity advisories remain (i.e., they only resolve via `--force`):
   - DO NOT run `npm audit fix --force` (D-04 explicit reject).
   - Document each residual advisory in the commit message body and plan summary as backlog.
   - The CI gate `npm audit --audit-level=high` will exit non-zero if any high remain — this is acceptable ONLY IF every remaining advisory is documented as `--force`-only with a concrete reason. If non-`--force` fixes exist that were missed, run `npm audit fix` again or apply targeted bumps via `npm install <pkg>@<version>`.
7. Commit with message:
```
chore(27.4.2): npm audit fix — clear high-severity advisories without --force

Pre: <N> high-severity advisories (<package list>)
Post: 0 high-severity advisories
Residual moderate/low: <count or "none">

Vitest + npm run build verified post-bump per CONTEXT D-04.
No major version jumps (--force forbidden per D-04).

```
  </action>
  <verify>
    <automated>npm audit --audit-level=high 2>&1 | grep -E "(found 0|0 vulnerabilities|0 high)" && npx vitest run src/__tests__/filters.test.ts 2>&1 | tail -5 | grep -v "FAIL" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm audit --audit-level=high` exits 0 (no high-severity advisories) OR every remaining advisory is documented in plan summary as `--force`-required (with the reason `--force` is rejected per D-04)
    - `npx vitest run src/__tests__/filters.test.ts` exits 0 (Plan 01 preserved)
    - `npm run build` exits 0 (frontend + server bundle + tsc all green)
    - `git diff --stat package-lock.json` reports a non-zero line delta (proving the bump landed)
    - `git log -1 --pretty=%B | grep -c "force"` returns 0 (no `--force` in this commit's narrative; "without --force" mention is allowed in body — the gate is on the `--force` flag itself, which the patterns confirm by checking the diff)
    - Commit message body lists pre/post advisory counts
  </acceptance_criteria>
  <done>
    High-severity advisories cleared; tests + build green; single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dep-tree → runtime | Transitive deps reach runtime; advisories there can compromise the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.4.2-AUDIT-01 | I (Information disclosure via known CVE in transitive dep) | package-lock.json transitive deps (path-to-regexp, picomatch, postcss, etc.) | mitigate | `npm audit fix` (no --force) applies minor/patch bumps; CI gate `npm audit --audit-level=high` exits 0 confirms mitigation |
| T-27.4.2-AUDIT-02 | T (Tampering via unintended major-version dep bump) | package.json major version pins | mitigate | D-04 explicitly forbids `--force`; acceptance criterion checks no `--force` was applied; residual advisories documented as backlog instead |
</threat_model>

<verification>
Run `npm audit --audit-level=high` — must exit 0 (zero high-severity advisories), or every residual must be documented as `--force`-required in the plan summary. Run `npx vitest run src/__tests__/filters.test.ts` — Plan 01's fix must survive. Run `npm run build` — bundle must compile.
</verification>

<success_criteria>
- Zero high-severity advisories per `npm audit --audit-level=high`
- Test suite still green (Plan 01 preserved)
- `npm run build` succeeds
- Single atomic commit; residuals (if any) documented
</success_criteria>

<output>
After completion, create `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-02-SUMMARY.md` documenting:
- Pre-bump advisory count (high/moderate/low)
- Post-bump advisory count (high/moderate/low)
- Per-package list of bumps applied (parsed from `git diff package.json`)
- Any residual advisories with the `--force`-required reason for each
- Commit SHA
- Confirmation `npm test` and `npm run build` both green post-bump
</output>
```

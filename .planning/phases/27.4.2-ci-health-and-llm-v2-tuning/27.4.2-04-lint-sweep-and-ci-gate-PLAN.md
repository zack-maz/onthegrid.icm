---
phase: 27.4.2
plan: 04
type: execute
wave: 1
depends_on: [27.4.2-03]
files_modified:
  - '(lint sweep — auto-fixable + targeted manual fixes for the 63 errors)'
  - eslint.config.js
autonomous: true
requirements_addressed: [D-02, D-05]
nyquist_compliant_intent: true
user_setup: []
tags: [ci-health, eslint, lint, mass-sweep, ci-gate]

must_haves:
  truths:
    - 'npm run lint exits 0 for ERRORS (zero errors remaining; was 63 pre-plan)'
    - 'react-refresh/only-export-components warnings are downgraded or scoped (interpretation per RESEARCH §1 / Pitfall 5)'
    - 'Plan 01-03 work preserved (filters tests green, prettier clean, npm run build succeeds)'
    - 'All four D-02 CI gates green at the end of this plan: vitest, npm audit (high), prettier --check, eslint (zero errors)'
  artifacts:
    - path: 'eslint.config.js'
      provides: 'Lint rule allowances for react-refresh/only-export-components per the plan-summary residuals interpretation'
    - path: '(every file with auto-fixable lint errors — likely .ts/.tsx in src/ and server/)'
      provides: 'Lint-fixed source code'
  key_links:
    - from: 'eslint.config.js'
      to: 'every .ts/.tsx file'
      via: 'npm run lint -- --fix'
      pattern: 'n/a — mechanical + targeted'
---

<objective>
Resolve the actual 412-problem lint state (NOT the CONTEXT-said "~20") by:
1. Running `npm run lint -- --fix` for the auto-fixable subset (mostly the 5 prefer-const errors, the 10 unused-vars errors, parts of the 102 react-hooks/exhaustive-deps warnings).
2. Targeted manual fixes for the residual 48 `@typescript-eslint/no-explicit-any` errors (cast-with-comment pattern from CLAUDE.md / 26.4-01 precedent for deck.gl v9 type-cast cleanup).
3. Negotiating the 238 `react-refresh/only-export-components` warnings per RESEARCH Pitfall 5 / D-02 interpretation: downgrade the rule via `eslint.config.js` to `'warn'` (already is — the rule produces warnings not errors) AND document residual warnings in plan summary; the D-02 four-gate is treated as "zero ERRORS, warnings categorized".
4. Verifying all four D-02 CI gates green at plan close — this plan is the **gate-keeper plan** for whether Wave 2 may begin.

Per RESEARCH §1 / Pitfall 5 / CONTEXT D-02 negotiation guidance:

- D-02 strict reading: "zero warnings" — NOT achievable in this plan's scope. The 238 `react-refresh/only-export-components` warnings each require a structural file split (move constants to a co-located `*.constants.ts`); that's a multi-file refactor scoped to a follow-up phase.
- D-02 negotiated reading (locked in this plan summary): "zero ERRORS, warnings categorized — `react-refresh/only-export-components` accepted as advisory; `react-hooks/exhaustive-deps` warnings auto-fixed where possible, residuals listed".

Output: Single atomic commit `chore(27.4.2): lint sweep — clear errors, downgrade react-refresh warnings`. Plan summary captures the per-rule warning count delta and explicitly records the D-02 negotiated interpretation for the planner-checker review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md
@eslint.config.js
@package.json

<interfaces>
<!-- Existing eslint config precedent (eslint.config.js:24-34) — the `_`-prefix unused-vars allowance shape -->

```javascript
'@typescript-eslint/no-unused-vars': [
  'error',
  {
    argsIgnorePattern: '^_',
    varsIgnorePattern: '^_',
    caughtErrorsIgnorePattern: '^_',
    destructuredArrayIgnorePattern: '^_',
  },
],
```

<!-- Existing react-refresh rule shape (eslint.config.js per CLAUDE.md / 26.4-01 precedent) -->

```javascript
'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot pre-sweep lint state and categorize</name>
  <files>(read-only)</files>
  <read_first>
    - eslint.config.js (full file — confirm current rule configurations)
    - package.json (confirm `lint` script and `lint:fix` if present)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-05 lint negotiation rationale)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md (lines 428-441 — Pitfall 5 412-problem breakdown: 238 react-refresh, 102 react-hooks, 48 no-explicit-any, 10 no-unused-vars, 5 prefer-const)
  </read_first>
  <action>
1. Run `npm run lint 2>&1 | tee /tmp/lint-before.txt; echo "EXIT: $?"`. Capture full output (do not truncate).
2. Parse the rule-by-rule breakdown (the eslint summary table at the end of the output):
   - errors total
   - warnings total
   - per-rule counts: `react-refresh/only-export-components`, `react-hooks/exhaustive-deps`, `@typescript-eslint/no-explicit-any`, `@typescript-eslint/no-unused-vars`, `prefer-const`, plus any rules not in CONTEXT
3. Confirm against RESEARCH §1 expectation (412 problems / 63 errors / 349 warnings; 238 react-refresh; 102 react-hooks; 48 no-explicit-any; 10 no-unused-vars; 5 prefer-const). If actual numbers differ materially (>20%), note the delta in the plan summary — it does not block; just record the actual numbers.
4. Capture the list of files with the highest error concentration (e.g., top 10 files by error count): `grep -E "^/.*(\\.ts|\\.tsx)$" /tmp/lint-before.txt | sort | uniq -c | sort -rn | head -10`.
  </action>
  <verify>
    <automated>test -f /tmp/lint-before.txt && wc -l /tmp/lint-before.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/lint-before.txt` exists with non-zero size
    - Pre-sweep error count captured (number)
    - Pre-sweep warning count captured (number)
    - Per-rule breakdown captured for plan summary
    - Top-10 high-error files identified
  </acceptance_criteria>
  <done>
    Baseline lint state captured; rule-level breakdown ready for the targeted manual pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Auto-fix pass + targeted manual fixes for all 63 errors</name>
  <files>(every file modified by `npm run lint -- --fix` plus targeted error fixes)</files>
  <read_first>
    - /tmp/lint-before.txt (Task 1 output)
    - eslint.config.js (rule definitions)
    - The top-10 high-error files from Task 1 (read each before editing)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md (Pitfall 5 strategy)
  </read_first>
  <action>
1. Run `npm run lint -- --fix 2>&1 | tee /tmp/lint-after-autofix.txt; echo "EXIT: $?"`. This applies prefer-const, simple unused-vars, and many react-hooks/exhaustive-deps warnings.
2. Re-run `npm run lint 2>&1 | tee /tmp/lint-after-autofix-check.txt; echo "EXIT: $?"`. Parse new error/warning counts. If errors > 0, identify which rule and which file (most likely all 48 no-explicit-any are still present; possibly residual unused-vars too).
3. For each remaining ERROR (NOT warning):
   - **`@typescript-eslint/no-explicit-any` (48 expected residual):** apply the `as unknown as <Type>` double-cast pattern from CLAUDE.md / 26.4-01 precedent for deck.gl v9 stricter type defs. For LLM pipeline `any` usages, prefer narrowing to the actual Zod-validated shape (e.g., `EnrichedEventV2`) where the call site already has it. For test files, prefer renaming the type alias rather than disabling the rule. Last resort (and ONLY last resort): inline `// eslint-disable-next-line @typescript-eslint/no-explicit-any -- <reason>` with a one-sentence reason.
   - **`@typescript-eslint/no-unused-vars` (residual after auto-fix):** apply the `_`-prefix convention per the eslint.config.js precedent (`argsIgnorePattern: '^_'`). Rename the variable to `_<name>`.
   - **`prefer-const` (residual after auto-fix, unlikely):** change `let` → `const` if reassignment is not present.
4. Re-run `npm run lint 2>&1 | tee /tmp/lint-after-manual.txt; echo "EXIT: $?"`. Parse counts. ERRORS must be 0.
5. **WARNING handling per the negotiated D-02 interpretation:**
   - The 238 `react-refresh/only-export-components` warnings: do NOT chase by splitting components. The rule is already `'warn'` not `'error'` (per CLAUDE.md / 26.4-01 precedent at eslint.config.js). Document the count in plan summary; tag as backlog for a follow-up component-split phase.
   - The remaining `react-hooks/exhaustive-deps` warnings: if the auto-fix pass left some, leave them as-is for this plan (they are not in scope per the lint sweep mass-sweep mandate; chasing them risks behavioral changes). Document the count in plan summary.
6. If at this point any ERROR remains that cannot be fixed without behavioral change or scope explosion, STOP and write a HANDOFF.md note in the phase dir. Do NOT add a blanket-disable comment at file top. Do NOT skip pre-commit hook with `--no-verify` per project security protocol.
7. Stage and commit:
```
chore(27.4.2): lint sweep — clear all errors, downgrade react-refresh warnings

Pre: 412 problems (63 errors, 349 warnings)
Post: <X> problems (0 errors, <Y> warnings)

Errors fixed:

- prefer-const: 5 → 0 (auto-fix)
- no-unused-vars: 10 → 0 (auto-fix + \_-prefix)
- no-explicit-any: 48 → 0 (as unknown as <Type> cast pattern, or scoped disable with reason)

Warnings handled:

- react-refresh/only-export-components: <N> → <N> (advisory; rule already 'warn'; structural file splits scoped to follow-up phase)
- react-hooks/exhaustive-deps: <M> → <M-K> (auto-fix only; manual chase out-of-scope)

Per CONTEXT D-02 negotiated interpretation locked in plan summary:
"zero ERRORS, warnings categorized — react-refresh/only-export-components
accepted as advisory; react-hooks/exhaustive-deps residuals captured as
backlog".

```
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 | grep -E "(0 errors|✖ 0 problems|0 problems)" || (npm run lint 2>&1 | grep -E "✖ [0-9]+ problems \\([0-9]+ errors" | grep -E "\\(0 errors")</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 OR exits non-zero only because of WARNING count (not errors). Disambiguate via output: must show `0 errors` in the summary line.
    - `npx vitest run src/__tests__/filters.test.ts` exits 0 (Plan 01 preserved)
    - `npx prettier --check .` exits 0 (Plan 03 preserved)
    - `npm run build` exits 0 (no behavioral regression from lint fixes)
    - Commit message body lists the per-rule pre/post error counts (the structured residuals table) — grep `git log -1 --pretty=%B | grep -c "no-explicit-any"` returns ≥1
    - No blanket `/* eslint-disable */` comments added at file top — search: `git diff HEAD~1 | grep -E "^\\+.*eslint-disable" | grep -v "next-line"` returns 0 matches (line-scoped is fine; file-scoped is not)
  </acceptance_criteria>
  <done>
    All 63 errors cleared; warnings categorized per the negotiated D-02 reading; commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-02 four-gate verification — final CI gate-keeper checkpoint</name>
  <what-built>
    Wave 1 is complete. Verify all four D-02 gates GREEN before opening Wave 2.
  </what-built>
  <how-to-verify>
1. Run `npx vitest run` (full suite, not just filters.test.ts). Confirm:
   - Test Files: all green
   - Tests: 0 failures (Plan 01 fix propagates; Plans 02/03/04 introduced no regressions)
2. Run `npm audit --audit-level=high`. Confirm exit 0 (zero high-severity advisories) — Plan 02 outcome.
3. Run `npx prettier --check .`. Confirm exit 0 (zero drift) — Plan 03 outcome.
4. Run `npm run lint`. Confirm `0 errors` in the summary line — this plan's outcome. WARNINGS are accepted per the negotiated interpretation captured in this plan's commit body.
5. Optionally run the entire `.github/workflows/ci.yml` job locally if `act` or equivalent is available (not required if all four gates above pass).

If any gate FAILS:
- Wave 2 (P6+) MUST NOT begin per D-02 strict CI gate.
- File a HANDOFF.md noting which gate failed, the exit code, and the diagnostic info.
- Either revert the offending plan and re-attempt, or open a follow-up plan in Wave 1.
  </how-to-verify>
  <resume-signal>Type "all four gates green" to proceed to Wave 2 / Plan 06; OR describe the failure for a follow-up plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint-rule-config → CI signal trustworthiness | Rule downgrades or blanket-disables erode the value of CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.4.2-LINT-01 | T (Tampering — silent rule disable that erodes CI value) | eslint.config.js + every file with `eslint-disable` comments | mitigate | Acceptance criterion #5: no FILE-scoped disables (only line-scoped); each line-scoped disable carries a one-sentence reason in the comment. |
| T-27.4.2-LINT-02 | D (Denial of CI signal — accepting too many warnings) | eslint.config.js | mitigate | Plan summary explicitly records the D-02 negotiated interpretation; the planner-checker review enforces that the reasoning is sound. |
| T-27.4.2-01 | E (Elevation via process bypass — `--no-verify`) | every commit in this plan | mitigate | Project security protocol forbids `--no-verify`; pre-commit hook (Plan 05) verifies its enforcement. |
</threat_model>

<verification>
Run all four D-02 gates: `npx vitest run`, `npm audit --audit-level=high`, `npx prettier --check .`, `npm run lint`. All four must report 0 errors. Plan 03 prettier preservation + Plan 02 audit preservation + Plan 01 filter fix preservation are all transitively verified by these commands.
</verification>

<success_criteria>
- Lint errors: 0 (negotiated 412→0 errors; warnings categorized in plan summary)
- All four D-02 CI gates green
- Wave 2 may begin (gate-keeper checkpoint passed)
- Single atomic commit `chore(27.4.2): lint sweep`
</success_criteria>

<output>
After completion, create `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-04-SUMMARY.md` documenting:
- Pre-sweep lint state (412 problems / 63 errors / 349 warnings, per-rule breakdown)
- Post-sweep lint state (0 errors / X warnings, per-rule breakdown)
- The D-02 NEGOTIATED INTERPRETATION (verbatim) — this is the contract Wave 2 reads:
  > "Per CONTEXT D-02, lint gate is interpreted as 'zero ERRORS, warnings categorized'. The 238 react-refresh/only-export-components warnings are accepted as advisory because each requires a structural file split. Rule downgrades scoped to a follow-up component-organization phase."
- Files modified (count by directory)
- Per-rule fix-strategy taken (auto-fix vs cast pattern vs `_`-prefix vs scoped disable with reason)
- Confirmation all four D-02 gates green
- Commit SHA
- Backlog items for `/gsd-add-todo`: react-refresh component-split phase, react-hooks/exhaustive-deps cleanup phase
</output>
```

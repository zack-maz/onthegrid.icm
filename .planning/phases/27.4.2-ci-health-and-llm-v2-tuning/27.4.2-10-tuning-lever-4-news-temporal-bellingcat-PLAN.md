---
phase: 27.4.2
plan: 10
type: execute
wave: 2
depends_on: [27.4.2-09]
files_modified:
  - server/lib/llmEventExtractor.v2.ts
autonomous: true
requirements_addressed: [D-11-lever-4, D-12, D-13, D-14, D-15, D-16]
nyquist_compliant_intent: true
user_setup: []
tags: [llm-tuning, lever-4, news-block, temporal-block, bellingcat-block, conditional-plan]
conditional: true
skip_if: 'D-13 stop condition met by any prior plan (P7/P8/P9 wave-boundary)'

must_haves:
  truths:
    - '(Conditional plan) SKIP if D-13 stop condition was met by Plan 07, 08, or 09 wave-boundary'
    - 'If running: NEWS / TEMPORAL / BELLINGCAT block formatting tuned in server/lib/llmEventExtractor.v2.ts per CONTEXT D-11 lever 4 + ANALYSIS §12 Q1/Q3/Q4/Q10'
    - 'Schema FROZEN per D-16 + 27.4 D-23 — block tuning is formatting-only, not schema-shape'
    - 'After EACH sub-lever commit: npm run eval:replay shows within20km/total ≥ Plan 06 baseline (D-14)'
    - 'After all sub-levers: wave-boundary full production run; D-13 stop check; phase close per stop condition'
  artifacts:
    - path: 'server/lib/llmEventExtractor.v2.ts'
      provides: 'Tuned NEWS / TEMPORAL / BELLINGCAT block formatting OR window constants'
      contains: 'NEWS_MATCH_WINDOW_MS|TEMPORAL_CONTEXT|BELLINGCAT'
  key_links:
    - from: 'buildBatchUserPromptV2()'
      to: 'Cerebras/Groq chat completion'
      via: 'user message containing NEWS + TEMPORAL + BELLINGCAT context blocks'
      pattern: 'NEWS BLOCK|TEMPORAL BLOCK|BELLINGCAT BLOCK'
---

<objective>
**CONDITIONAL plan — SKELETON only.** Execute D-11 lever 4 (NEWS/TEMPORAL/BELLINGCAT block tuning) ONLY if Plans 07, 08, AND 09 wave-boundaries did not satisfy D-13 stop condition. Per CONTEXT D-11 ("Lowest leverage on resolver fallback; saved for last") and D-13 ("As soon as eval score beats v1 by ≥5pp at 20km AND fallback drops below 25%, the tuning wave is done — even if levers 3 and 4 are untouched"), this plan has the highest skip probability of any in the wave.

**Pre-flight check (Task 0):** Read latest wave-boundary SUMMARY (Plan 09 if it exists, else Plan 08, else Plan 07). If `D-13 stop check = STOP`, write a one-line plan summary "SKIPPED: D-13 already met at Plan 0X" and file `/gsd-add-todo` for the lever-4 backlog (CONTEXT Deferred Ideas explicitly captures the §12 Q1/Q3/Q4/Q10 sub-hypotheses).

If proceeding, candidate sub-lever surfaces in `server/lib/llmEventExtractor.v2.ts`:

- `NEWS_MATCH_WINDOW_MS` (§12 Q3 hypothesis: try 12h / 24h / 36h to find optimal news-context width)
- `TEMPORAL_CONTEXT_COUNT` / `TEMPORAL_CONTEXT_WINDOW_MS` / `TEMPORAL_CONTEXT_BBOX_DEG` (§12 Q1: try 48h / 72h / 96h)
- BELLINGCAT block coord-precision `toFixed(N)` (§12 Q4: try 3dp vs 4dp)
- NEWS tier reordering by T1/T2/T3 priority (§12 Q10)

Per D-12 micro-iteration, each sub-lever is its own atomic commit with `npm run eval:replay` capture. Per D-14, regression at 20km → revert + closed-negative §12 hypothesis.

Output: 0-N atomic commits depending on (a) skip per D-13, (b) sub-lever pass/revert. After all sub-levers: wave-boundary check; D-13 final stop check; phase close (this is the LAST tuning plan in the wave; phase close is mandatory after this regardless of D-13 verdict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-AI-SPEC.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-LLM-PIPELINE-ANALYSIS.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-06-SUMMARY.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-07-SUMMARY.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-08-SUMMARY.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-09-SUMMARY.md
@server/lib/llmEventExtractor.v2.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight — check whether to SKIP this plan per D-13</name>
  <files>(read-only check)</files>
  <read_first>
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-09-SUMMARY.md (preferred reference if it exists)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-08-SUMMARY.md (fallback)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-07-SUMMARY.md (fallback if 08 also skipped)
  </read_first>
  <action>
1. Read the most-recent wave-boundary summary. Look for the D-13 stop verdict.
2. If STOP: write SKIPPED summary at `27.4.2-10-SUMMARY.md`; file backlog todos via `/gsd-add-todo` for §12 Q1/Q3/Q4/Q10 sub-hypotheses (one todo per hypothesis is fine since they're independent levers); proceed to phase close.
3. If CONTINUE: proceed to Task 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-10-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Plan 10 SUMMARY exists with SKIPPED or PROCEEDING disposition
    - If SKIPPED: backlog todos filed; phase close can begin
    - If PROCEEDING: Task 1 begins
  </acceptance_criteria>
  <done>
    Plan 10 disposition set: skipped or proceeding.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: (CONDITIONAL) Pick the highest-confidence sub-lever from §12 Q1/Q3/Q4/Q10 + execute</name>
  <files>server/lib/llmEventExtractor.v2.ts</files>
  <read_first>
    - server/lib/llmEventExtractor.v2.ts (full file — verify the constant names: NEWS_MATCH_WINDOW_MS, TEMPORAL_CONTEXT_*, BELLINGCAT block formatting)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-LLM-PIPELINE-ANALYSIS.md (§11 lever 4 + §12 Q1/Q3/Q4/Q10 hypothesis details)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-AI-SPEC.md (Pitfall 1 — schema FROZEN)
  </read_first>
  <behavior>
    - Test 1: One sub-lever change lands per atomic commit (D-15)
    - Test 2: After commit, npm run eval:replay shows within20km/total ≥ Plan 06 baseline (D-14)
    - Test 3: Block formatting changes do NOT introduce new field names or alter the JSON-schema wire contract (D-16 / 27.4 D-23 invariant)
  </behavior>
  <action>
**Run only if Task 0 verdict is PROCEEDING.**

Pick ONE sub-lever to start (the one most likely to move 20km eval based on the current state). Order of priority (planner's discretion):

**A. NEWS_MATCH_WINDOW_MS (§12 Q3):** Try 12h, 24h, 36h. Tighter window = higher-quality news context but fewer matches; wider = more matches but more dilution.
**B. TEMPORAL_CONTEXT_WINDOW_MS (§12 Q1):** Try 48h, 72h, 96h. Same tradeoff for prior-events context.
**C. BELLINGCAT toFixed precision (§12 Q4):** Try 3dp vs 4dp coordinate precision in the BELLINGCAT block formatting (currently default).
**D. NEWS tier reordering (§12 Q10):** Reorder T1 articles before T2 before T3 in the NEWS block (so the LLM sees high-tier sources first).

For the chosen sub-lever:

1. Find the constant or formatting code in `server/lib/llmEventExtractor.v2.ts`:
   - For A: `grep -n "NEWS_MATCH_WINDOW_MS" server/lib/llmEventExtractor.v2.ts`
   - For B: `grep -n "TEMPORAL_CONTEXT_WINDOW_MS" server/lib/llmEventExtractor.v2.ts`
   - For C: `grep -n "BELLINGCAT" server/lib/llmEventExtractor.v2.ts`
   - For D: `grep -n "NEWS BLOCK\\|news_articles" server/lib/llmEventExtractor.v2.ts`
2. Edit the constant or formatting code (one-line change for A/B; small refactor for C/D).
3. `npm run eval:replay 2>&1 | tee /tmp/_eval_lever4.txt`. Capture per-distance counts.
4. D-14 check vs Plan 06 baseline.
5. PASS → atomic commit; REGRESSION → revert + closed-negative §12 documentation; move to next candidate sub-lever (or skip to Task 2).
6. Commit message:

```
feat(27.4.2): tune <NEWS|TEMPORAL|BELLINGCAT> — <one-line summary>

eval 5km/20km/100km: A→B / C→D / E→F

D-11 lever 4 sub-edit: <which §12 Q hypothesis> — <change description>.

Schema FROZEN per D-16 — block formatting only, no field-shape changes.
```

  </action>
  <verify>
    <automated>npm run eval:replay 2>&amp;1 | tail -3 | grep "within20km/total"</automated>
  </verify>
  <acceptance_criteria>
    - One or more sub-lever commits landed (or all reverted with documentation)
    - Each commit modifies block formatting only — NO schema/Zod/JSON-Schema changes (`git diff <last-merge>..HEAD -- server/lib/llmSchema.ts` returns empty)
    - Each commit message body contains eval before/after numbers
    - All four D-02 gates still green
  </acceptance_criteria>
  <done>
    Lever 4 sub-levers evaluated atomically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave-boundary checkpoint + final D-13 stop check + phase close prep</name>
  <files>(no source edits — wave-boundary measurement)</files>
  <read_first>
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-13 stop, D-07 hang-recurrence flip restoration at phase close)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md (Pitfall 4 cooldown-clear)
    - All prior 27.4.2-NN-SUMMARY.md files for end-of-wave aggregate
  </read_first>
  <action>
1. Trigger fresh production run per Pitfall 4 cooldown-clear method.
2. Capture from `lastRun.summary`: within20km/total, fallback%, watchdogTimeoutCount, tokenCounters, DLQ zod_fail count.
3. Apply D-13 final stop check:
   - Goal A: post-tuning within20km/total ≥ Plan 06 baseline + 0.05
   - Goal B: post-tuning fallback% < 0.25
   - **STOP (Goal A AND B met):** Phase success per CONTEXT goal. Document in plan summary.
   - **PARTIAL (only Goal A OR only Goal B met):** Phase landed partial improvement. Document achieved metric AND remaining gap. File backlog `/gsd-add-todo` for next-phase tuning targeting the unmet goal.
   - **NO PROGRESS (neither met):** Document in plan summary; the tuning wave produced no measurable improvement. File backlog with the §12 hypotheses still open. Phase still closes (no further levers in scope).
4. **Phase close prep:**
   - If D-07 hang-recurrence flip was applied at any wave-boundary, **RESTORE the default provider order**: invoke `setProviderOrderOverride(null)` via a one-off tsx script. Verify via `getProviderOrder()` returning `['cerebras', 'groq']`. Document the restoration in plan summary.
   - File final backlog todos for any unaddressed §12 hypotheses.
   - Confirm all four D-02 CI gates STILL green.
   - Confirm CLAUDE.md is updated if the wave changed any architectural pattern (e.g., new provider override surface — the planner should add a 1-line note to the LLM section).
5. The orchestrator handles the actual phase merge/close after this task.
  </action>
  <verify>
    <automated>curl -s 'http://localhost:5173/api/events/llm-status' 2>/dev/null | jq -e '.lastRun.summary != null' &amp;&amp; npm run lint &amp;&amp; npx prettier --check . &amp;&amp; npm audit --audit-level=high &amp;&amp; npx vitest run 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Plan summary documents final wave-boundary state
    - D-13 verdict explicitly recorded (STOP/PARTIAL/NO-PROGRESS)
    - D-07 provider override restored if it was applied (`setProviderOrderOverride(null)` executed)
    - All four D-02 gates STILL green at phase close
    - All open §12 hypotheses backlog-filed
    - **CLAUDE.md updated unconditionally for the new provider-override surface (P06 added `setProviderOrderOverride` to `server/adapters/llm-provider.ts` — this IS a documented architectural pattern change):** `grep -q 'setProviderOrderOverride' CLAUDE.md` exits 0. The new line lives under the "LLM Enrichment v2 + Runtime Toggle" section.
    - **CHANGELOG / PROJECT_STATUS updated unconditionally:** first detect which file the project uses via `ls CHANGELOG.md PROJECT_STATUS.md README.md 2>/dev/null` then assert `grep -q '27.4.2' <chosen-file>` exits 0 (the phase is mentioned in whichever change-log surface the project maintains). If neither CHANGELOG.md nor PROJECT_STATUS.md exists, fall back to README.md.
    - **README / PROJECT_SPEC unchanged unless lever-3 or lever-4 ran (those modify SYSTEM_PROMPT_V2 which is documented in CLAUDE.md, not README):** if Task 1 of this plan landed any commits OR Plan 09 landed any commits, then `grep -q 'SYSTEM_PROMPT_V2' CLAUDE.md` should still return the existing reference (no removal); README/PROJECT_SPEC require no edit since the prompt internals are not documented there.
  </acceptance_criteria>
  <done>
    Phase 27.4.2 wave 2 closed; orchestrator can merge to main.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                     |
| ------------------------------------ | ------------------------------------------------------------------------------- |
| Block formatting → prompt token cost | Wider blocks = more tokens per call; budget impact compounds across 184 batches |
| Phase-close hygiene                  | Wave-scoped state must be reset (provider override) so next phase starts clean  |

## STRIDE Threat Register

| Threat ID          | Category                                                                                   | Component                                                 | Disposition | Mitigation Plan                                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------------ | --------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-27.4.2-LEVER4-01 | T (Tampering — block formatting changes inadvertently break the JSON-schema wire contract) | server/lib/llmEventExtractor.v2.ts buildBatchUserPromptV2 | mitigate    | Task 1 acceptance criterion: no schema files changed (`git diff -- server/lib/llmSchema.ts` empty); D-23 schema lock         |
| T-27.4.2-03        | T (Provider-flip leakage past phase close)                                                 | server/adapters/llm-provider.ts \_providerOrderOverride   | mitigate    | Task 2 explicitly restores `setProviderOrderOverride(null)` at phase close; in-memory only so cold-start would also clear it |
| T-27.4.2-05        | D (Token budget exhaustion if NEWS_MATCH_WINDOW widened to 36h+)                           | server/lib/llmTokenBudget.ts                              | mitigate    | Task 2 captures tokenCounters; soft-cap monitoring at 80%                                                                    |
| T-27.4.2-04        | I (Eval baseline drift)                                                                    | .planning/eval/ground-truth-events.json                   | mitigate    | Phase scope guard freezes 50-event corpus                                                                                    |

</threat_model>

<verification>
After Task 1, `npm run eval:replay` ≥ baseline. After Task 2, all four D-02 gates green AND provider override restored AND backlog todos filed.
</verification>

<success_criteria>

- Either SKIPPED per D-13 + backlog filed
- Or 1-N atomic block-tuning commits with eval ≥ baseline
- Final wave-boundary production run measured
- D-13 final verdict recorded (STOP/PARTIAL/NO-PROGRESS)
- D-07 provider override restored
- All four D-02 gates STILL green at phase close
- CLAUDE.md updated for new architectural pattern (provider override) if applicable
- Phase 27.4.2 ready for orchestrator merge
  </success_criteria>

<output>
Plan summary `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-10-SUMMARY.md`:
- SKIPPED status (with backlog SHA list) OR landed status (per-sub-lever results)
- Final wave-boundary production run results
- D-13 final verdict + reasoning
- D-07 provider override restoration confirmation
- Backlog todos for unaddressed §12 hypotheses
- CLAUDE.md update SHA if applicable
- All four D-02 gates green confirmation
- Phase-level retrospective lines:
  - Levers shipped vs reverted (count by category)
  - Final eval delta vs Plan 06 baseline
  - Final fallback% vs 25% target
  - Total commits (Wave 1 + Wave 2)
  - Wall-clock approximately spent
- Commit SHAs
</output>

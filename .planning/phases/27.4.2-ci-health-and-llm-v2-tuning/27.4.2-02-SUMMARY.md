---
phase: 27.4.2-ci-health-and-llm-v2-tuning
plan: 02-npm-audit-fix
subsystem: infra
tags: [npm-audit, dependencies, security, ci-health, lockfile]

# Dependency graph
requires:
  - phase: 27.4.2-01-filter-test-fixture
    provides: Green client+server test suite (1827/1827) — the post-bump regression-detection signal that proves the dep bumps are non-breaking
provides:
  - Zero high-severity advisories per `npm audit --audit-level=high` (D-04 CI gate prerequisite for D-02 strict gate)
  - Lockfile-only minor/patch bumps for 11 advisory-affected packages + 1 new transitive dep (no major-version jumps; --force not used)
  - Documented out-of-scope build failure baseline (vite.config.ts manualChunks references unresolved deck.gl packages — predates this plan)
affects:
  [
    27.4.2-03-prettier-mass-sweep,
    27.4.2-04-lint-sweep-and-ci-gate,
    27.4.2-05-pre-commit-guard-verification,
    27.4.2-06-tuning-baseline-and-helpers,
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Dep-bump isolation: `npm audit fix` (no --force) lands in its own atomic commit so the lockfile diff is reviewable independently of cosmetic CI fixes (D-04 verbatim)'
    - 'Pre-existing-baseline doctrine for build/test verification: when a verification step (here `npm run build`) fails identically before AND after the change, the change is net-zero on health and the failure is logged in `deferred-items.md` rather than auto-fixed under Rule 3 (SCOPE BOUNDARY)'

key-files:
  created:
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-02-SUMMARY.md
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md
  modified:
    - package-lock.json

key-decisions:
  - 'D-04 applied verbatim: `npm audit fix` with NO `--force`, NO `--production`. All 8 advisories (4 high + 4 moderate) resolved cleanly via lockfile-only minor/patch bumps. `package.json` was NOT touched — every bump landed transitively.'
  - 'Documented pre-existing `npm run build` break (vite.config.ts manualChunks references unresolved `@deck.gl/react` and `@deck.gl/aggregation-layers`) in deferred-items.md instead of auto-fixing under Rule 3, because the break predates this commit (verified via `git stash && npm run build` showing identical error).'
  - 'Treated single redis-death.test.ts timeout in the first full-suite run as a flake — re-running the file in isolation passed 10/10, and a subsequent full-suite run passed 1827/1827 across 127 files. Load-sensitive timing in the chaos test, not a bump regression.'

patterns-established:
  - 'Pattern: lockfile-only audit fix — `npm audit fix` may resolve all advisories transitively, leaving `package.json` byte-identical. Verify via `git diff --stat package.json` (zero lines) and document the bumps from the `package-lock.json` `version:` deltas.'
  - 'Pattern: build-verification baseline carve-out — when a planned `npm run build` verification step fails on a pre-existing issue unrelated to the plan, record the baseline in `deferred-items.md` and assert "no NEW regressions" rather than blocking on the pre-existing break (consistent with Phase 27.3.2 tsc-baseline precedent at .planning/phases/27.3.2-water-facility-admission-tightening-drop-city-coord-fallback/deferred-items.md)'

requirements-completed: [D-04]

# Metrics
duration: 7min
completed: 2026-04-25
---

# Phase 27.4.2 Plan 02: npm audit fix Summary

**Single-commit lockfile-only minor/patch sweep cleared all 8 npm audit advisories (4 high: flatted, path-to-regexp, picomatch, vite + 4 moderate: brace-expansion, fast-xml-parser, postcss, protocol-buffers-schema) with no `--force` and no `package.json` edits — `npm audit --audit-level=high` now exits 0, full test suite stays 1827/1827 green.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-25T08:26:25Z
- **Completed:** 2026-04-25T08:33:46Z
- **Tasks:** 2 of 2 (Task 1: snapshot baseline; Task 2: apply fix + verify + commit)
- **Files modified:** 1 (`package-lock.json`)
- **Files created:** 2 (this SUMMARY + deferred-items.md)

## Pre/Post Advisory Counts

| Severity | Pre   | Post  | Residual |
| -------- | ----- | ----- | -------- |
| High     | 4     | 0     | 0        |
| Moderate | 4     | 0     | 0        |
| Low      | 0     | 0     | 0        |
| **All**  | **8** | **0** | **0**    |

`npm audit --audit-level=high` exits 0 post-fix.

## Per-Package Bumps Applied

All bumps are **minor/patch** (no major-version jumps); all landed in `package-lock.json` only — `package.json` was NOT modified.

| Package                    | From   | To     | Severity-fixed | Notes                                |
| -------------------------- | ------ | ------ | -------------- | ------------------------------------ |
| `brace-expansion` (top)    | 1.1.12 | 1.1.14 | moderate       | top-level                            |
| `brace-expansion` (nested) | 5.0.4  | 5.0.5  | moderate       | under @typescript-eslint             |
| `fast-xml-builder`         | 1.1.4  | 1.1.5  | (transitive)   | dep of fast-xml-parser               |
| `fast-xml-parser`          | 5.5.8  | 5.7.2  | moderate       | top-level dep                        |
| `flatted`                  | 3.4.1  | 3.4.2  | high           | prototype-pollution CVE              |
| `path-expression-matcher`  | 1.2.0  | 1.5.0  | (transitive)   | dep of fast-xml-parser               |
| `path-to-regexp`           | 8.3.0  | 8.4.2  | high           | ReDoS CVEs (2 of them)               |
| `picomatch`                | 4.0.3  | 4.0.4  | high           | method-injection + ReDoS CVEs        |
| `postcss`                  | 8.5.8  | 8.5.10 | moderate       | XSS-via-Stringify CVE                |
| `protocol-buffers-schema`  | 3.6.0  | 3.6.1  | moderate       | prototype-pollution CVE              |
| `strnum`                   | 2.2.1  | 2.2.3  | (transitive)   | dep of fast-xml-parser               |
| `vite`                     | 6.4.1  | 6.4.2  | high           | path-traversal + arbitrary-read CVEs |
| `@nodable/entities`        | (new)  | 2.1.0  | (transitive)   | new dep added by fast-xml-parser 5.7 |

`git diff --stat package-lock.json` reported 52 insertions / 39 deletions across 1 file.

## Residual Advisories Requiring `--force`

**None.** Every advisory was cleanly resolvable via minor/patch transitive bumps. No `--force` candidates remain.

## Task Commits

1. **Task 1: Snapshot pre-bump audit state** — no commit (information gathering only; baseline saved to `/tmp/audit-before.txt` and `/tmp/audit-before.json`)
2. **Task 2: Run npm audit fix and verify suite still green** — `c662c49` (chore)

## Files Created/Modified

- `package-lock.json` — 52 insertions, 39 deletions across 11 advisory-affected packages + 1 new transitive dep (`@nodable/entities`). All minor/patch.
- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md` — created to log the pre-existing `npm run build` failure (vite.config.ts `manualChunks` references unresolved deck.gl packages).
- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-02-SUMMARY.md` — this file.

## Decisions Made

- **Followed D-04 verbatim:** `npm audit fix` with no `--force`, no `--production` — full dev tree, minor/patch only. Outcome was even cleaner than D-04 forecast: D-04 mentions "three high-severity packages (flatted, path-to-regexp, picomatch)" but the actual baseline included a fourth high (`vite`) plus 4 moderates. All 8 still resolved without `--force`.
- **Did NOT auto-fix the pre-existing build break under Rule 3.** The build failure (`vite.config.ts` references `@deck.gl/react` + `@deck.gl/aggregation-layers`, neither installed) was confirmed via `git stash && npm run build` to predate this commit. Per the SCOPE BOUNDARY rule, Rule 3 only auto-fixes blocking issues DIRECTLY caused by the current task's changes. Recommended fix is documented in `deferred-items.md` for a future plan to absorb (likely Plan 04 lint-sweep-and-ci-gate, since it owns the CI-gate work).
- **Did NOT clean up untracked `.claude/` directory.** That's worktree leftovers from a separate workstream, out-of-scope per Plan 01's note.

## Deviations from Plan

### Auto-fixed Issues

**None.** The plan executed exactly as written: D-04 applied verbatim, single atomic commit, lockfile-only diff.

### Documented as Deferred (not auto-fixed)

**1. [Out of scope per SCOPE BOUNDARY] Pre-existing `npm run build` failure**

- **Found during:** Task 2 verification of acceptance criterion `npm run build` exits 0
- **Issue:** Build fails with `Could not resolve entry module "@deck.gl/react"`. Both `@deck.gl/react` and `@deck.gl/aggregation-layers` are listed in `vite.config.ts` `manualChunks.vendor-deckgl` but neither is installed nor declared in `package.json`.
- **Why not auto-fixed:** Verified via `git stash && npm run build` that this exact error occurred on the pre-fix tree. The dep bumps did NOT cause it; per SCOPE BOUNDARY, Rule 3 only applies to issues directly caused by the current task. The plan's `npm run build` verification step is interpreted (consistent with Phase 27.3.2's tsc-baseline precedent) as "the bumps did not regress the build state" — satisfied: same failure mode before and after.
- **Mitigation:** Documented in `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md` with the recommended fix (strip the two unresolved names from the `vendor-deckgl` `manualChunks` array — they are bundling hints, not imports, so removing them makes Rollup happy without adding any package). Recommended owner: Plan 04 (lint-sweep-and-ci-gate) or a tiny Plan 02.5.

---

**Total deviations:** 0 auto-fixed; 1 documented as deferred.
**Impact on plan:** Production behavior unchanged. CI-gate criterion `npm audit --audit-level=high` now exits 0 — D-04's stated purpose. Test suite preserved (1827/1827).

## Issues Encountered

- **Single test timeout on the first full-suite run** (`server/__tests__/resilience/redis-death.test.ts > GET /api/news returns 200 (degraded)... under Redis death`). Re-running the file in isolation: 10/10 pass. Re-running the full suite: 1827/1827 pass. Confirmed flake (load-sensitive timing in a chaos test that intentionally sleeps under fake Redis death; ran during the 90th second of an already-warm suite). Not a bump regression.
- **Vitest 4 dropped the `basic` reporter.** First full-suite run with `--reporter=basic` errored with `Failed to load url basic`. Switched to default reporter. Cosmetic only; logged in `deferred-items.md` for downstream plans to avoid the same trap.
- **Engine-mismatch warning:** `npm audit fix` printed `npm warn EBADENGINE Unsupported engine ... required: { node: '22.x' }, current: { node: 'v25.6.1', npm: '11.9.0' }`. Warning only — npm proceeded. Pinning is on `22.x` per `package.json` `engines.node`; runtime mismatch is a developer-machine issue not a CI/build issue. Not in scope.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- ✅ **Plan 03 (prettier-mass-sweep) ready:** dep-bump diff is isolated and reviewable; the next commit's lockfile-only diff confirmed clean signal for the prettier mass-sweep that follows.
- ✅ **Plan 04 (lint-sweep-and-ci-gate) ready:** the D-02 strict-CI-gate criterion `npm audit --audit-level=high` exit 0 is now true. Plan 04 can wire `npm audit --audit-level=high` into the CI gate without immediately failing it. **Note for Plan 04:** the `deferred-items.md` `npm run build` issue should be evaluated — Plan 04 either owns the fix or explicitly excludes `npm run build` from the CI gate until a follow-up plan addresses it.
- ✅ **Plan 05 (pre-commit-guard-verification) ready:** husky + lint-staged + gitleaks ran successfully on this commit (prettier auto-formatted the lockfile, gitleaks scanned 0 commits / 3.37 KB / 0 leaks) — confirms the pre-commit pipeline is functional before plan 05 hardens it.
- ✅ **Tuning wave (Plans 06-10):** D-02 strict CI gate prerequisite `npm audit --audit-level=high` exit 0 now satisfied. The wave can begin under a clean security baseline.

### Note for downstream plans

- **`package.json` was NOT modified** — every bump landed transitively in `package-lock.json`. Reviewers expecting `package.json` bumps should look at `package-lock.json` instead.
- **`fast-xml-parser` 5.5.8 → 5.7.2** is the largest version delta (+0.2 minor). It introduced one new transitive dep (`@nodable/entities` 2.1.0). If downstream plans encounter unexpected XML parsing behavior, this is the most likely suspect to bisect.
- **The pre-existing `npm run build` break** is critical to know about before Plan 04 wires the CI gate. See `deferred-items.md` for the recommended fix.

## Self-Check: PASSED

- `package-lock.json` modified — FOUND in commit c662c49 (52+/39-)
- `package.json` byte-identical — VERIFIED (`git diff package.json` empty)
- Commit `c662c49` — FOUND in `git log` with full body listing all bumps
- `npm audit --audit-level=high` exits 0 — VERIFIED (output: `found 0 vulnerabilities`)
- `npx vitest run src/__tests__/filters.test.ts` — 49/49 passing post-commit (Plan 01 preserved)
- `npx vitest run` (full suite) — 1827/1827 across 127 files post-commit (no regressions)
- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/deferred-items.md` — FOUND
- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-02-SUMMARY.md` — FOUND
- `git diff` of commit shows no `--force` flag invocation — VERIFIED (lockfile diff only; "without --force" appears 2x in commit body as narrative, not as a flag)

---

_Phase: 27.4.2-ci-health-and-llm-v2-tuning_
_Plan: 02-npm-audit-fix_
_Completed: 2026-04-25_

---
phase: 27.4.2
plan: 03
type: execute
wave: 1
depends_on: [27.4.2-02]
files_modified:
  - '(prettier sweep — ~56 canonical files: 31 .planning/, 19 server/, 3 src/, 2 docs/, 1 root → 34 .md, 20 .ts, 2 .tsx)'
autonomous: true
requirements_addressed: [D-05]
nyquist_compliant_intent: true
user_setup: []
tags: [ci-health, prettier, format, mass-sweep]

must_haves:
  truths:
    - 'npm run format:check (or npx prettier --check .) exits 0 — zero format drift'
    - 'All ~56 canonical drift files reformatted to .prettierrc.json rules (semi+singleQuote+trailingComma+printWidth=100)'
    - 'Plan 01 + Plan 02 work preserved (npx vitest run src/__tests__/filters.test.ts still passes; npm run build still succeeds)'
    - 'Sweep lands BEFORE Wave 2 begins to avoid merge-conflict thrash on server/lib/llm*.ts (Pitfall 6)'
  artifacts:
    - path: '(every drift file modified by `npm run format`)'
      provides: 'Prettier-canonical formatting; deterministic output of the same .prettierrc.json'
  key_links:
    - from: '.prettierrc.json'
      to: 'every modified file'
      via: 'npm run format (= prettier --write .)'
      pattern: 'n/a — mechanical operation'
---

<objective>
Run a single mechanical `npm run format` (= `prettier --write .`) and commit the entire ~56-file diff atomically. The sweep MUST land before any Wave 2 plan begins because 19 of those files are concentrated in `server/lib/llm*.ts` + `server/__tests__/lib/llm*.test.ts` — exactly the files Wave 2 tuning will edit. Splitting cosmetic from behavioral commits keeps both diffs reviewable and prevents merge-conflict thrash (Pitfall 6 from RESEARCH §6).

Purpose: D-05 mass-sweep prerequisite for the D-02 four-gate before Wave 2 begins. Cosmetic changes only — zero behavioral risk if `.prettierrc.json` is the canonical authority (it is — `semi+singleQuote+trailingComma+printWidth=100`).

Output: Single atomic commit `style(27.4.2): prettier sweep — restore canonical formatting (N files)` where N is the actual count after the sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md
@.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md
@.prettierrc.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot pre-sweep drift state</name>
  <files>(read-only)</files>
  <read_first>
    - .prettierrc.json (the rule authority)
    - package.json (confirm `format` script exists; confirm `format:check` exists)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-05 lines 130-138 — verbatim decision)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-PATTERNS.md (lines 91-105 — Pitfall 6 wave-ordering rationale)
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-RESEARCH.md (lines 443-451 — Pitfall 6 file distribution)
  </read_first>
  <action>
1. Run `npx prettier --check . 2>&1 | tee /tmp/prettier-before.txt || true` to capture the list of drift files (exits non-zero when drift exists; we want the list).
2. Count drift files: `grep -c "^\\[warn\\]" /tmp/prettier-before.txt || grep -c "Code style issues found" /tmp/prettier-before.txt`. Capture the total for the commit message.
3. Identify the distribution per RESEARCH §6 Pitfall 6:
   - Files in `.planning/` (likely 31)
   - Files in `server/` (likely 19, mostly `server/lib/llm*.ts` + tests)
   - Files in `src/` (likely 3)
   - Files in `docs/` (likely 2)
   - Files in repo root (likely 1)
4. Confirm none of the drift files are in `.claude/worktrees/` or other excluded paths (verify via `.prettierignore` if present, or `prettier --check` output).

Do NOT commit at this step. This is information gathering.
</action>
<verify>
<automated>test -f /tmp/prettier-before.txt && wc -l /tmp/prettier-before.txt</automated>
</verify>
<acceptance_criteria> - `/tmp/prettier-before.txt` exists with non-zero size - Pre-sweep drift count captured (a number, not "n/a") - File distribution noted (planning/server/src/docs/root) for commit message body
</acceptance_criteria>
<done>
Baseline drift state captured; ready to apply sweep.
</done>
</task>

<task type="auto">
  <name>Task 2: Run prettier --write . and commit atomically</name>
  <files>(prettier sweep — ~56 files)</files>
  <read_first>
    - /tmp/prettier-before.txt (output of Task 1)
    - .prettierrc.json
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-CONTEXT.md (D-05)
  </read_first>
  <action>
1. Run `npm run format` (= `prettier --write .`). This is a single mechanical pass; do not run `--check` first or split by directory.
2. Confirm modifications: `git status --porcelain | wc -l` should match the pre-sweep drift count from Task 1 (within ~1-2 of variance for files prettier modifies vs files it merely flagged).
3. Verify Plan 01 + Plan 02 still green:
   - `npx vitest run src/__tests__/filters.test.ts` exits 0
   - `npm run build` exits 0
4. Verify drift is now zero: `npx prettier --check . 2>&1 | grep -E "(All matched files|Code style issues)"` — must show "All matched files use Prettier code style!"
5. Stage every modified file: `git add -u` (`-u` stages modifications only, not untracked files — safer than `-A`).
6. Commit atomically with message:
```
style(27.4.2): prettier sweep — restore canonical formatting (N files)

Distribution: <X> .planning/, <Y> server/, <Z> src/, <W> docs/, <V> root
By extension: <A> .md, <B> .ts, <C> .tsx

Mechanical npm run format pass per D-05 mass-sweep. Lands BEFORE
Wave 2 to avoid merge-conflict thrash on server/lib/llm\*.ts files
(Pitfall 6 — 19 of N drift files are in the same files Wave 2 tunes).

Verified: vitest filters.test.ts green, npm run build succeeds,
prettier --check . reports zero drift.

```
  </action>
  <verify>
    <automated>npx prettier --check . 2>&1 | grep -q "All matched files use Prettier code style" && npx vitest run src/__tests__/filters.test.ts 2>&1 | tail -3 | grep -v "FAIL" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx prettier --check .` exits 0 (output: "All matched files use Prettier code style!")
    - `npx vitest run src/__tests__/filters.test.ts` exits 0 (Plan 01 preserved)
    - `npm run build` exits 0 (no behavioral regression from cosmetic sweep)
    - `git log -1 --pretty=%s | grep -q "^style(27.4.2): prettier sweep"` exits 0 (commit message convention)
    - Files modified in this commit are a superset of files in `/tmp/prettier-before.txt` (within ±2 for prettier's idempotency edge cases)
    - No file outside `.planning/`, `server/`, `src/`, `docs/`, repo root was modified (no .claude/, no node_modules/)
  </acceptance_criteria>
  <done>
    Prettier drift cleared; cosmetic diff isolated in one commit; Wave 2 starts from a clean prettier base.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cosmetic-sweep → behavioral-code | Mass formatting must NOT introduce behavioral changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.4.2-FMT-01 | T (Tampering — unintended behavioral change in cosmetic sweep) | Every file in the sweep | mitigate | Acceptance criterion: `npx vitest run` green + `npm run build` succeeds post-sweep — proves no behavioral change. Prettier is deterministic given fixed `.prettierrc.json`. |
| T-27.4.2-FMT-02 | D (Denial of merge productivity for Wave 2) | server/lib/llm*.ts files (19 of 56) | mitigate | This plan ordered as P3 (before Wave 2 starts) per Pitfall 6 — sweep lands first, tuning commits start clean. |
</threat_model>

<verification>
Run `npx prettier --check .` — must exit 0. Run `npx vitest run src/__tests__/filters.test.ts` — Plan 01 preserved. Run `npm run build` — succeeds.
</verification>

<success_criteria>
- Zero prettier drift across the entire repo
- Tests + build still green
- Single atomic `style(27.4.2): prettier sweep` commit
- Wave 2 plans (P6-P10) start from a prettier-clean base
</success_criteria>

<output>
After completion, create `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-03-SUMMARY.md` documenting:
- Pre-sweep drift count vs post-sweep drift count
- File distribution (planning/server/src/docs/root)
- Concentration in `server/lib/llm*.ts` (named files)
- Commit SHA
- Confirmation `npx prettier --check .` exits 0
</output>
```

---
phase: 27.4.2-ci-health-and-llm-v2-tuning
plan: 01-filter-test-fixture
subsystem: testing
tags: [vitest, test-fixture, filter, ci-health, enabledPrecisions, worktrees]

# Dependency graph
requires:
  - phase: 27-llm-event-pipeline
    provides: enabledPrecisions filter field on FilterState (production contract at src/lib/filters.ts:147)
provides:
  - Green client test suite (1827/1827 passing across 127 files) — D-02 strict CI gate prerequisite
  - Vitest exclusion of legacy .claude/worktrees/agent-*/ — unblocks every future test run for the rest of the phase
  - FilterState test factory shape with enabledPrecisions backfilled per D-03
affects:
  [
    27.4.2-02-npm-audit-fix,
    27.4.2-03-prettier-mass-sweep,
    27.4.2-04-lint-sweep-and-ci-gate,
    27.4.2-05-pre-commit-guard-verification,
    27.4.2-06-tuning-baseline-and-helpers,
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Test fixture factory (makeDefaults) extension instead of softening production type signatures (D-03)
    - Worktree exclusion at vite.config.ts test.exclude — dev-tooling hygiene separate from project source

key-files:
  created:
    - .planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-01-SUMMARY.md
  modified:
    - src/__tests__/filters.test.ts
    - vite.config.ts

key-decisions:
  - 'D-03 applied verbatim: backfill the test fixture, do NOT soften filters.enabledPrecisions to optional in production code'
  - "Mirror enabledSiteTypes precedent at line 66: array-of-string-literal with `as import('@/stores/filterStore').PrecisionTier[]` cast"
  - "Rule-3 deviation: add '**/.claude/worktrees/**' to vite.config.ts test.exclude — 16 stale parallel-executor worktrees were inflating failure counts 17x and blocking the D-02 strict CI gate for the entire phase"

patterns-established:
  - "Pattern: Phase-N+1 fixture-shape backfill — when a new non-nullable filter field ships in Phase N's production code, the next CI plan extends test factories rather than weakening the production contract"
  - "Pattern: Vitest exclusion of dev-tool worktrees — .claude/worktrees/** must be excluded from glob discovery so leftover parallel-executor snapshots don't pollute the test signal"

requirements-completed: [D-03, D-17]

# Metrics
duration: 6min
completed: 2026-04-25
---

# Phase 27.4.2 Plan 01: Filter Test Fixture Summary

**One-line `enabledPrecisions: ['exact','neighborhood','city','region']` backfill in `makeDefaults()` greens 32 cascading filter tests, plus a Rule-3 vitest worktree-exclusion that lifted the project-wide signal from 16 failed test files to 1827/1827 passing across 127 files.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-25T08:18:16Z (first baseline run)
- **Completed:** 2026-04-25T08:22:22Z
- **Tasks:** 1 of 1 (TDD: RED baseline → GREEN fix → verification)
- **Files modified:** 2

## Pre/Post Failure Counts

| Stage                                      | filters.test.ts (canonical)                                          | Full client suite                                         |
| ------------------------------------------ | -------------------------------------------------------------------- | --------------------------------------------------------- |
| **Before**                                 | 32 failures (49 tests, 17 file copies × 32 = 544 reported by vitest) | 16 failed test files                                      |
| **After fixture fix only**                 | 0 failures in canonical file                                         | 16 failed test files (15 stale worktree copies still red) |
| **After fixture fix + worktree exclusion** | 49/49 passing                                                        | 127/127 files, 1827/1827 tests passing                    |

The plan's stated "544 → 0" target was the vitest-reported aggregate. The canonical file went 32 → 0; the remaining 512 phantom failures came from 16 stale `.claude/worktrees/agent-*/src/__tests__/filters.test.ts` snapshots that pre-dated the fix.

## Accomplishments

- 4-element `enabledPrecisions` literal added to the test fixture factory at the exact insertion point per PATTERNS.md (after `setShowLowSeverity` callback, before `enabledSiteTypes` site-type toggles), mirroring the `enabledSiteTypes` precedent shape
- Worktree-exclusion glob added to `vite.config.ts` test.exclude with an inline JSDoc explaining the failure mode (Rule-3 deviation; documented below)
- D-02 strict CI gate prerequisite met: client tests green, ready for plans 02-05 to land their dependency / format / lint / hook fixes against a clean baseline
- Production code at `src/lib/filters.ts:147` byte-identical to main — `git diff --stat src/lib/filters.ts` empty as required by acceptance criterion #4

## Task Commits

1. **Task 1: Add enabledPrecisions to makeDefaults() factory + Rule-3 worktree exclusion** — `9b5f61b` (fix)

## Files Created/Modified

- `src/__tests__/filters.test.ts` — backfilled `enabledPrecisions: ['exact','neighborhood','city','region'] as import('@/stores/filterStore').PrecisionTier[]` in the `makeDefaults()` FilterState factory. Prettier auto-reformatted the 4-element array to multi-line during pre-commit (semantically identical; 4 strings, same cast, same field name).
- `vite.config.ts` — extended `test.exclude` from inline 3-element array to 5-element array adding `'**/.claude/worktrees/**'` with a 6-line explanatory comment.

## Decisions Made

- **Followed D-03 verbatim:** the fix lives entirely in the test fixture, not in production code. `filters.enabledPrecisions` remains non-nullable. Acceptance criterion #4 (`git diff --stat src/lib/filters.ts` empty) passed.
- **Did not backfill the other 25 missing FilterState fields proactively** (per Pitfall 1 / RESEARCH §6) — only `enabledPrecisions` is read by `entityPassesFilters`. Adding others would have turned a one-line cosmetic into a 25-line behavioral diff.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Excluded legacy .claude/worktrees/agent-\*/ from vitest discovery**

- **Found during:** Task 1 verification (post-fix vitest run still showed 512 failures)
- **Issue:** Vitest was glob-matching `**/src/__tests__/filters.test.ts` and pulling in 16 stale snapshots from `.claude/worktrees/agent-*/` (leftover locked worktrees from prior parallel executor runs). Each contained the pre-fix copy of `filters.test.ts` (without `enabledPrecisions`), inflating the failure count from 32 (canonical file) to 512 (16 stale × 32) and making it impossible to verify the plan's success criterion ("0 failures") against the canonical file alone. Acceptance criterion #1 (`npx vitest run src/__tests__/filters.test.ts` exits 0) was technically blocked despite the canonical fix being correct.
- **Fix:** Added `'**/.claude/worktrees/**'` to `vite.config.ts` `test.exclude` array with a 6-line inline comment explaining the failure mode and the Phase 27.4.2 origin.
- **Files modified:** `vite.config.ts`
- **Verification:** Post-fix `npx vitest run src/__tests__/filters.test.ts` reports `Test Files 1 passed (1)`, `Tests 49 passed (49)`. Post-fix full-suite `npx vitest run` reports `Test Files 127 passed (127)`, `Tests 1827 passed (1827)`.
- **Committed in:** `9b5f61b` (bundled with the Task 1 fixture fix because both changes are required to make `npx vitest run src/__tests__/filters.test.ts` produce a green signal — the plan's primary acceptance criterion)
- **Rule cited:** Rule 3 (auto-fix blocking issues) — the test infrastructure prevented verification of the planned fix; the worktree leftovers are dev tooling, not project source code, so excluding them is hygiene not behavior change.

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The Rule-3 fix unblocked the plan's own verification path AND unblocked the D-02 strict CI gate for the rest of the phase. Plans 02-10 will now see real signal from `npm test`. Production behavior unchanged. Scope boundary respected: I did not delete the worktrees themselves (those are git-managed locked worktrees from a separate workstream — out of scope per the SCOPE BOUNDARY rule).

## Confirmation Required by Plan Output Spec

- ✅ **Pre/post failure count:** 32 → 0 in canonical filters.test.ts (544 → 1827/1827 across full suite when worktree exclusion is included)
- ✅ **File of insertion:** `src/__tests__/filters.test.ts` `makeDefaults()` (post-prettier-format: lines 65-73; semantic insertion point per PATTERNS.md = after `setShowLowSeverity` callback at line 64, before `// Site type toggles` comment)
- ✅ **Commit SHA:** `9b5f61b`
- ✅ **`src/lib/filters.ts` NOT modified:** `git diff --stat src/lib/filters.ts` empty; `grep -c "enabledPrecisions" src/lib/filters.ts` returns 2 (unchanged baseline; lines 147 + 151)
- ✅ **Sibling test files now passing as a side effect:** Yes — extensive. The canonical test for this plan was `filters.test.ts`, but the worktree exclusion (Rule-3 fix) also greens the full-suite signal: `Test Files 127 passed (127)`, `Tests 1827 passed (1827)`. Tracked here so plan-04's lint-sweep-and-ci-gate plan can verify against this baseline.

## Issues Encountered

- **Initial verification confusion:** First post-fix run still showed 512 failures, which looked like the fix hadn't worked. Investigation via `git worktree list` revealed 16 locked legacy worktrees under `.planning/.claude/worktrees/agent-*/` containing stale snapshots. The fix WAS correct in the canonical file (`grep -n "enabledPrecisions" /Users/zackmaz/Desktop/my_world/src/__tests__/filters.test.ts` confirmed lines 66-68); vitest was discovering 17 file copies. Resolved via Rule-3 worktree exclusion above.
- **Prettier auto-formatted the inline 4-element array to multi-line during pre-commit hook.** Acceptance criterion #5 said "the exact insertion uses `as import('@/stores/filterStore').PrecisionTier[]` (matching the `enabledSiteTypes` precedent at line 66)". The `as` cast and import path are unchanged; same 4 strings, same field name. The line count differs (multi-line vs single-line), and `enabledSiteTypes` was also reformatted to multi-line by the same prettier pass — keeping the precedent match intact in shape, just expanded vertically.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- ✅ **Plan 02 (npm-audit-fix) ready:** baseline test suite is green; `npm audit fix` outcome can be measured against a clean signal.
- ✅ **Plan 03 (prettier-mass-sweep) ready:** prettier already ran on this commit's diff (pre-commit hook); the broader `npm run format` sweep will land cleanly.
- ✅ **Plan 04 (lint-sweep-and-ci-gate) ready:** the D-02 strict-CI-gate verification step (`npm test` returning green) is now meaningful; pre-fix it would have shown 512+ failures regardless of any other lint/format work.
- ✅ **Plan 05 (pre-commit-guard-verification) ready:** husky + lint-staged ran successfully on this commit (eslint --fix + prettier --write + gitleaks all passed) — confirming the existing hook is functional before plan 05 hardens it.

### Note for downstream plans

The 16 locked worktrees under `.claude/worktrees/agent-*/` are NOT cleaned up by this plan — they are git-managed (`git worktree list` shows them as `locked`). If a future plan or operator wants to remove them, use `git worktree remove --force <path>` on each, but that is explicitly out-of-scope for plan 01 per the SCOPE BOUNDARY rule. The exclusion makes their presence harmless for vitest discovery.

## Self-Check: PASSED

- `src/__tests__/filters.test.ts` — FOUND
- `vite.config.ts` — FOUND
- `.planning/phases/27.4.2-ci-health-and-llm-v2-tuning/27.4.2-01-SUMMARY.md` — FOUND
- Commit `9b5f61b` — FOUND in `git log --all`
- `src/lib/filters.ts` diff vs main — 0 lines (production code byte-identical)
- `npx vitest run src/__tests__/filters.test.ts` — 49/49 passing post-commit
- `npx vitest run` (full suite) — 1827/1827 across 127 files post-commit

---

_Phase: 27.4.2-ci-health-and-llm-v2-tuning_
_Plan: 01-filter-test-fixture_
_Completed: 2026-04-25_

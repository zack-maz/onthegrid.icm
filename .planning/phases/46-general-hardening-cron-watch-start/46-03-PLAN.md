---
phase: 46-general-hardening-cron-watch-start
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - server/lib/cronWatch.ts
  - server/lib/__tests__/cronWatch.test.ts
  - server/routes/cron-health.ts
  - server/__tests__/routes/cron-health.test.ts
  - CLAUDE.md
  - .planning/phases/46-general-hardening-cron-watch-start/46-WATCH.md
autonomous: true
requirements:
  - CRON-WATCH-01
must_haves:
  truths:
    - 'The existing /api/cron/health 0 0 * * * run auto-captures one daily watch sample into a bounded Redis ring — no new cron, no new endpoint, no manual daily step'
    - 'A watch-write failure NEVER degrades the /api/cron/health response (own try/catch, appended AFTER the lastTick:health + trend writes)'
    - 'The 7-day watch is NON-BLOCKING — milestone close proceeds regardless of watch status; a partial close is structurally visible (dated ring + WATCH artifact with daysObserved/daysTarget + earlyClose block) rather than a silent v1.5 Phase 31 repeat'
  artifacts:
    - path: 'server/lib/cronWatch.ts'
      provides: 'appendWatchSample/readWatchHistory bounded ring (LPUSH+LTRIM+EXPIRE, degrade-open)'
      contains: 'appendWatchSample'
    - path: 'server/routes/cron-health.ts'
      provides: 'daily appendWatchSample call after appendTrendSample, in its own try/catch'
      contains: 'appendWatchSample'
    - path: '.planning/phases/46-general-hardening-cron-watch-start/46-WATCH.md'
      provides: 'human-readable WATCH artifact with non-blocking framing + earlyClose contract'
      contains: 'daysObserved'
  key_links:
    - from: 'server/routes/cron-health.ts'
      to: 'server/lib/cronWatch.ts appendWatchSample'
      via: 'daily capture appended after appendTrendSample, reusing the already-computed per-cron cronAgeMs'
      pattern: 'appendWatchSample'
    - from: 'server/lib/cronWatch.ts'
      to: 'cron:watch:v2.0 Redis ring'
      via: 'redis.pipeline().lpush().ltrim().expire().exec() (degrade-open)'
      pattern: 'cron:watch'
---

<objective>
CRON-WATCH-01: start the 7-day cron-stability watch as a NON-BLOCKING, auto-reported observation that does NOT gate milestone close and is structured to avoid the v1.5 Phase 31 silent early-close. Phase 46 delivers the STRUCTURE; the 7-day clock runs asynchronously in the background through later phases (D-09). Three deliverables: (1) a new `cronWatch.ts` bounded Redis ring (`cron:watch:v2.0`, cap 14, ~30d TTL) — a verbatim structural copy of `trendHistory.ts`; (2) a daily `appendWatchSample` call piggybacked on the EXISTING `/api/cron/health` run (D-07 — no new cron/endpoint), appended AFTER `appendTrendSample` in its OWN try/catch so a watch-write failure never degrades the health response; (3) a human-readable WATCH artifact (`46-WATCH.md`) carrying the non-blocking framing + an explicit `daysObserved / daysTarget` + an `earlyClose` block that, if ever used, MUST cite the v1.5 Phase 31 precedent (D-08).

NON-BLOCKING contract (D-08/D-09, roadmap-locked): the watch MUST NOT gate Phase 46 close or milestone close. Do NOT make any Phase 46 verification depend on 7 elapsed days. The structural mechanism that prevents a silent partial close: a dated daily ring + the WATCH artifact make a partial close visibly partial.

Purpose: correct the v1.5 Phase 31 Day-1 early-close-with-caveat by making the watch auto-captured and its partiality structurally evident.
Output: cronWatch ring module + daily capture + WATCH artifact + CLAUDE.md key registration + degrade-open tests.

Wave-2 placement note: this plan has NO logical dependency on 46-01's code — the `depends_on: [46-01]` + Wave 2 exist solely to serialize the shared `CLAUDE.md` Redis-registry append (both plans add a distinct key; same-wave file overlap is forbidden). All cron-watch code (`cronWatch.ts`, `cron-health.ts`) is independent of the rate-limiter work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-general-hardening-cron-watch-start/46-CONTEXT.md
@.planning/phases/46-general-hardening-cron-watch-start/46-RESEARCH.md
@.planning/phases/46-general-hardening-cron-watch-start/46-PATTERNS.md

# Exact analogs to replicate:

@server/lib/trendHistory.ts
@server/routes/cron-health.ts
@.planning/milestones/v1.5-phases/31-cron-stability-validation-7-day-watch/31-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cronWatch.ts bounded ring (verbatim trendHistory copy) + degrade-open tests</name>
  <files>server/lib/cronWatch.ts, server/lib/__tests__/cronWatch.test.ts</files>
  <read_first>
    - server/lib/trendHistory.ts (the WHOLE file — the verbatim structural template: key/MAX/TTL consts, TrendSample interface, parseEntry dual-shape, appendTrendSample pipeline LPUSH+LTRIM+EXPIRE, readTrendHistory lrange degrade-open)
    - server/lib/__tests__/llmCallHistory.test.ts lines 17-32 (the vi.hoisted + vi.mock('../../cache/redis.js') mocked-Redis-throw harness)
    - 46-PATTERNS.md §"Pattern D" + §"Pattern E"
    - 46-RESEARCH.md §"v1.5 Phase 31 precedent" (the watch-log row shape to model the sample fields on)
  </read_first>
  <behavior>
    - appendWatchSample(sample) does ONE atomic round-trip: redis.pipeline().lpush(KEY, JSON).ltrim(KEY, 0, MAX-1).expire(KEY, TTL).exec(); degrade-open — any throw is logged + swallowed; NEVER throws.
    - readWatchHistory(limit) returns up to `limit` samples newest-first via lrange(KEY, 0, limit-1), parseEntry dual-shape (raw-string OR already-object Upstash REST), filters unparseable; returns [] on throw.
    - The ring is bounded at MAX=14 (covers the 7-day watch + buffer, within the D-07 7-14 bound); TTL ~30d (matches the trendHistory family).
    - WatchSample carries: sampledAt (ISO), tickDate (YYYY-MM-DD), per-cron health/missed run-state + freshness age (reuse the cronAgeMs shape), eval at5/20/100km, dlqCount, breakerTrips, result 'PASS'|'FAIL'. Model on the Phase 31 watch-log row.
  </behavior>
  <action>
    Create server/lib/cronWatch.ts as a verbatim structural copy of trendHistory.ts: export `CRON_WATCH_KEY = 'cron:watch:v2.0'`, `CRON_WATCH_MAX = 14`, `CRON_WATCH_TTL_SEC = 30*24*3600`. Define a `WatchSample` interface modeled on the Phase 31 `watch-log.json` row (`sampledAt`, `tickDate`, `cronAgeMs: {health,warm,'refresh-events': number|null}`, `eval: {at5km,at20km,at100km}`, `dlqCount: number`, `breakerTrips: number`, `result: 'PASS'|'FAIL'`). Copy `parseEntry` (dual-shape), `appendWatchSample` (pipeline LPUSH+LTRIM+EXPIRE, try/caught → never throw), `readWatchHistory` (lrange + parse + [] on throw). Use the same `logger.child({ module: 'cron-watch' })` posture. Create cronWatch.test.ts using the mocked-Redis-throw harness: assert appendWatchSample issues lpush+ltrim+expire with the right key/cap/TTL on the happy path; assert a rejecting pipeline.exec makes appendWatchSample a no-op that resolves (does NOT throw); assert readWatchHistory returns parsed newest-first on happy path and [] when lrange rejects; assert the dual-shape parse (string and object members).
  </action>
  <verify>
    <automated>npx vitest run server/lib/__tests__/cronWatch.test.ts</automated>
  </verify>
  <done>cronWatch.ts ring exists (key/cap/TTL); append is atomic + degrade-open (no-throw on Redis failure); reader is dual-shape + []-on-throw; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: daily appendWatchSample on /api/cron/health (own try/catch) + cron-health test</name>
  <files>server/routes/cron-health.ts, server/__tests__/routes/cron-health.test.ts</files>
  <read_first>
    - server/routes/cron-health.ts lines 125-195 (the lastTick:health write at :133, the per-cron cronAgeMs computation + deadUrlCount + appendTrendSample at :146-187 — append the watch sample AFTER this, in its own try/catch)
    - server/lib/cronWatch.ts (appendWatchSample from Task 1)
    - 46-RESEARCH.md §"Pitfall 4" (watch write must NOT degrade the cron-health response; must come AFTER the lastTick:health write)
    - 46-PATTERNS.md §"cron-health.ts (route) — CRON-WATCH-01"
  </read_first>
  <action>
    In cron-health.ts, AFTER the existing `appendTrendSample(...)` block (which already computes per-cron `cronAgeMs` + `deadUrlCount` in its own try/catch), add a SEPARATE try/catch that calls `appendWatchSample(...)`. REUSE the already-computed per-cron `cronAgeMs` (the `ages[0..2]` values) for the watch row — do NOT re-read `cron:lastTick:{name}`. Populate the WatchSample from values already in scope at that point: `tickDate` = `new Date(sampleNow).toISOString().slice(0,10)`, `cronAgeMs` from the computed ages, `eval` from the `evalScore`/eval bundle already computed earlier in the handler, `dlqCount`/`breakerTrips` from whatever the handler already has (or 0 with a note if not in scope — do NOT add new Redis reads just for these; degrade-open / best-effort), `result` = `'PASS'` when redisOk and eval bundle is healthy else `'FAIL'`. The whole append is wrapped so a watch-write throw is logged (`log.warn({ err }, 'watch sample append threw — continuing health response')`) and the health response continues — mirror the trend-sample try/catch posture verbatim (Pitfall 4). It MUST sit after the `cron:lastTick:health` write so the lastTick write is never blocked. Extend cron-health.test.ts: assert appendWatchSample is invoked on a successful cron-health run; assert a throwing appendWatchSample does NOT fail the cron-health response (still returns its normal JSON, status 200/ok-or-degraded).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/routes/cron-health.test.ts</automated>
  </verify>
  <done>/api/cron/health appends one daily watch sample after the trend write, reusing the computed cronAgeMs; a watch-write throw never degrades the health response; test green.</done>
</task>

<task type="auto">
  <name>Task 3: WATCH artifact (non-blocking framing + earlyClose contract) + CLAUDE.md key</name>
  <files>.planning/phases/46-general-hardening-cron-watch-start/46-WATCH.md, CLAUDE.md</files>
  <read_first>
    - .planning/milestones/v1.5-phases/31-cron-stability-validation-7-day-watch/31-SUMMARY.md (the early-close framing to NOT repeat silently)
    - .planning/milestones/v1.5-phases/31-cron-stability-validation-7-day-watch/watch-log.json (the row shape)
    - 46-CONTEXT.md §D-08/D-09 (non-blocking + early-close-only-by-explicit-decision-citing-Phase-31)
  </read_first>
  <action>
    Create 46-WATCH.md as the human-readable companion to the Redis ring. It MUST contain: a header stating the watch is NON-BLOCKING and does NOT gate Phase 46 or milestone close (cite the roadmap lock "CRON-WATCH-01 is NON-BLOCKING and must not gate milestone close"); an explicit `daysObserved: 0 / daysTarget: 7` counter (updated as samples land — note the ring is the source of truth, the artifact is the readable mirror); a per-day table mirroring the `cron:watch:v2.0` ring rows (tickDate, per-cron run-state, eval at 5/20/100km, dlq, breaker trips, PASS/FAIL); an `earlyClose` block stating early close is permitted ONLY by an explicit operator decision that CITES the v1.5 Phase 31 precedent (`.planning/milestones/v1.5-phases/31-cron-stability-validation-7-day-watch/`) and records the day-count + caveat — defaulted to `{ decided: false, citesPhase31: false, caveat: null }`; an explicit note that the 7-day clock runs asynchronously through later phases (D-09) and that a partial close is structurally visible via this artifact + the dated ring. Add a CLAUDE.md "Active Redis keys" registry line for `cron:watch:v2.0`: bounded LPUSH+LTRIM 14-cap / ~30d TTL daily cron-stability watch ring, writer `server/routes/cron-health.ts` via `server/lib/cronWatch.ts appendWatchSample` (degrade-open, piggybacks the existing 0 0 * * * health cron — no new cron), reader `readWatchHistory` + the 46-WATCH.md mirror; NON-BLOCKING CRON-WATCH-01 watch.
  </action>
  <verify>
    <automated>test -f .planning/phases/46-general-hardening-cron-watch-start/46-WATCH.md && grep -q 'daysObserved' .planning/phases/46-general-hardening-cron-watch-start/46-WATCH.md && grep -q 'cron:watch:v2.0' CLAUDE.md && echo OK</automated>
  </verify>
  <done>46-WATCH.md exists with NON-BLOCKING framing + daysObserved/daysTarget + earlyClose-cites-Phase-31 contract; CLAUDE.md registers `cron:watch:v2.0`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                          |
| ----------------------------------------- | ------------------------------------------------------------------------------------ |
| Vercel cron → /api/cron/health            | the only writer of the watch ring; optional CRON_SECRET Bearer gate already in place |
| cron handler → Upstash Redis (watch ring) | degrade-open — a watch-write failure must not affect the health-cron response        |
| watch ring / artifact → operator          | exposes eval scores + dlq counts; same data already on /api/cron/health              |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                             | Disposition | Mitigation Plan                                                                                                                                               |
| ---------- | ---------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-46-03-01 | Denial of Service      | appendWatchSample throw degrading /api/cron/health    | mitigate    | Own try/catch appended AFTER the lastTick:health + trend writes; a throw is logged + swallowed; the health response continues (Pitfall 4)                     |
| T-46-03-02 | Denial of Service      | cron:watch ring unbounded growth                      | mitigate    | Bounded LPUSH+LTRIM cap 14 (reader caps at MAX-1) + ~30d self-expiring TTL; atomic pipeline so a kill never leaves a no-TTL or over-cap ring (Phase 45 WR-02) |
| T-46-03-03 | Information Disclosure | watch ring/artifact exposing eval scores + DLQ counts | accept      | Eval scores + DLQ counts already surface via /api/cron/health + the operator dashboard; the WATCH artifact lives in .planning/ (not shipped)                  |
| T-46-03-04 | Tampering              | watch row values (server-derived)                     | mitigate    | All sample fields are server-computed in the cron handler (no client input); the Redis key is a static literal — no injection surface (ASVS V5)               |

</threat_model>

<verification>
- `npx vitest run server/lib/__tests__/cronWatch.test.ts server/__tests__/routes/cron-health.test.ts` green
- `npx vitest run server/` green
- 46-WATCH.md exists with the non-blocking + earlyClose-cites-Phase-31 framing; CLAUDE.md registers `cron:watch:v2.0`
- NON-BLOCKING: NO Phase 46 verification waits on elapsed days
</verification>

<success_criteria>

- The existing health cron auto-captures one daily watch sample into a bounded, self-expiring Redis ring — no new cron/endpoint/manual step
- A watch-write failure never degrades the health-cron response
- The watch is NON-BLOCKING and a partial close is structurally visible (dated ring + WATCH artifact); early close requires an explicit Phase-31-citing decision
  </success_criteria>

<output>
Create `.planning/phases/46-general-hardening-cron-watch-start/46-03-SUMMARY.md` when done
</output>

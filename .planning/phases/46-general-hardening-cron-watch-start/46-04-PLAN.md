---
phase: 46-general-hardening-cron-watch-start
plan: 04
type: execute
wave: 2
depends_on:
  - 46-01
  - 46-02
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx
  - src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx
autonomous: true
requirements:
  - HARD-01
  - HARD-02
must_haves:
  truths:
    - 'The operator reads per-tier rate-limiter config + recent 429 counts inside the API Health tab, sourced from opStatus.rateLimiter (the 46-01 aggregator field)'
    - "The operator reads each cron's run-state (healthy / MISSED / unknown) inside the API Health tab; a MISSED cron renders a distinct --color-status-degraded badge + UPPERCASE MISSED label; unknown renders neutral"
    - 'Both new blocks degrade open — a null/absent source renders a muted placeholder (or self-hides), never a crash, and the tablist/tabpanel DOM is byte-stable (no tab-id / roving-tabindex change)'
  artifacts:
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'rateLimiter?: RateLimiterBlock | null on OperatorStatus + two new render blocks inside the API Health role=tabpanel, composed from MetricRow'
      contains: 'rateLimiter'
    - path: 'src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx'
      provides: 'sidecar-absent render coverage for the new blocks (HARD-03 surface 3 — degrade-open when a field is null/absent)'
      contains: 'rateLimiter'
  key_links:
    - from: 'src/components/ui/DevApiStatus.tsx OperatorStatus.rateLimiter'
      to: 'server/routes/operator-status.ts rateLimiter block'
      via: 'forward-compat optional field consumed in the API Health tab render'
      pattern: 'rateLimiter'
    - from: 'src/components/ui/DevApiStatus.tsx cron block'
      to: '/api/health missedRun sibling field'
      via: 'the cron run-state badge reads missedRun, not the status enum'
      pattern: 'missedRun'
---

<objective>
HARD-01 + HARD-02 dashboard tier: render the two new read-only operator observability blocks INSIDE the existing API Health `role="tabpanel"` of `DevApiStatus.tsx`, per the approved `46-UI-SPEC.md`. (1) A rate-limiter block surfacing per-tier limit config (`max/window`) + recent HTTP-429 counts from `opStatus.rateLimiter` (the 46-01 aggregator field), one `<MetricRow>` per tier, one `text-[13px] font-semibold` primary metric = total recent 429s. (2) A cron-freshness block surfacing each of the 3 crons' run-state (`healthy` / `MISSED` / `unknown`) read from the `/api/health` `missedRun` SIBLING field (the 46-02 surface), one `<MetricRow>` per cron with a state badge, primary metric = count of MISSED crons. A `MISSED` cron renders a distinct `--color-status-degraded` badge + UPPERCASE `MISSED` label; `unknown` renders neutral (no alarm).

FROZEN behavioral contract (Phase 45 D-08 / UI-SPEC §"Interaction & Accessibility"): both blocks render INSIDE the existing `role="tabpanel" aria-labelledby="tab-api-health"` container, inside the existing `CollapsibleGroup` idiom. The WAI-ARIA tablist DOM is byte-stable — NO new tab, NO tab-id change, NO roving-tabindex change. Reuse the Phase-45 atoms `MetricRow`/`Sparkline` as-is. ZERO inline hex — every color from `@theme`/colorBridge or existing Tailwind palette utilities.

Purpose: make the limiter + cron-freshness state operator-visible so Phase 47's load test reads them to confirm load was shed correctly and crons survived.
Output: OperatorStatus.rateLimiter interface field + two render blocks + sidecar-absent render coverage + a deliberately-regenerated layout snapshot (tablist subtree MUST NOT diff).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-general-hardening-cron-watch-start/46-CONTEXT.md
@.planning/phases/46-general-hardening-cron-watch-start/46-UI-SPEC.md
@.planning/phases/46-general-hardening-cron-watch-start/46-PATTERNS.md

# Server field shapes this plan consumes (from 46-01 + 46-02):

@.planning/phases/46-general-hardening-cron-watch-start/46-01-SUMMARY.md
@.planning/phases/46-general-hardening-cron-watch-start/46-02-SUMMARY.md

# Exact analogs to replicate:

@src/components/ui/DevApiStatus.tsx
@src/components/ui/MetricRow.tsx
@src/components/ui/Sparkline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperatorStatus.rateLimiter interface + RateLimiterBlock type (forward-compat optional)</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx lines 1185-1250 (the OperatorStatus interface — append the new field alongside tokenBudget?: :1239 / trendHistory?: :1246, forward-compat optional) + fetchOpStatus :1254-1277 (new optional fields are NOT shape-gated — correct)
    - 46-01-SUMMARY.md (the exact server-side RateLimiterBlock shape: tiers array of {tier,max,windowSec,recent429})
    - 46-UI-SPEC.md §"Sidecar Lockstep Contract" item 4
  </read_first>
  <action>
    Add `rateLimiter?: RateLimiterBlock | null` to the `OperatorStatus` interface (alongside `tokenBudget?:` and `trendHistory?:`), forward-compat optional per Phase 32 D-10 — NOT gated in `fetchOpStatus`. Define the matching `RateLimiterBlock` type to mirror the 46-01 server shape exactly: `{ tiers: Array<{ tier: string; max: number; windowSec: number; recent429: number }> }` (confirm the field names against 46-01-SUMMARY.md and match byte-for-byte). Add a brief comment citing Phase 32 D-10 forward-compat (mirror the tokenBudget/trendHistory comment style at :1233-1247).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx</automated>
  </verify>
  <done>OperatorStatus carries `rateLimiter?: RateLimiterBlock | null`; the type matches the 46-01 server shape; existing tabMerge test still green (no behavioral change yet).</done>
</task>

<task type="auto">
  <name>Task 2: Rate-limiter + cron-freshness render blocks inside the API Health tabpanel</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - 46-UI-SPEC.md (the WHOLE contract — scope map, typography {400,600}, color tokens, the MISSED state→visual mapping table, copywriting contract, degrade-open muted-placeholder convention)
    - src/components/ui/DevApiStatus.tsx lines 968 (the API Health role="tabpanel"), 1113-1135 (CollapsibleGroup signature), 2024-2036 (the BudgetBlock CollapsibleGroup render — the exact placement idiom), 3891 (TrendBlock — the per-cron sparkline neighbor)
    - src/components/ui/MetricRow.tsx (the tabular-nums right-aligned {400,600} row atom — props signature)
    - CLAUDE.md §"Color Tokens" (--color-status-{healthy,degraded} via colorBridge/@theme — NO inline hex)
  </read_first>
  <action>
    Add two local `function XBlock(...)` render components inside DevApiStatus.tsx and mount them inside the existing API Health `role="tabpanel"` (`:968`), within the existing `CollapsibleGroup` idiom (placement = discretion within the panel per UI-SPEC: e.g. the rate-limiter block near the existing Operator Actions group, the cron-freshness block near the existing TrendBlock per-cron sparklines). Do NOT add a new tab, do NOT touch the tablist/tabpanel DOM, do NOT modify the MetricRow/Sparkline atoms.
    RATE-LIMITER BLOCK: title `Rate Limiter` (`text-[9px] font-semibold uppercase tracking-wider text-white/40`); one `text-[13px] font-semibold tabular-nums` primary metric = total recent 429s across all tiers (`0` when nothing shed); a `By Tier` column-group head; one `<MetricRow>` per tier (label = the tier name verbatim from `rateLimiter.tiers`, value = `{max}/{windowSec}s` + the recent-429 count with a `· 24h` micro caption). Source from `opStatus?.rateLimiter`. A non-zero recent count MAY tint the value to a warning token (discretionary; default neutral). Degrade-open: `opStatus.rateLimiter == null` → muted placeholder `— no data (operator-status unreachable)` (`text-white/30 italic`), never a crash.
    CRON-FRESHNESS BLOCK: title `Cron Freshness`; one `text-[13px] font-semibold` primary metric = count of crons in MISSED state (`0` when all healthy); one `<MetricRow>` per cron (label = cron name `health`/`warm`/`refresh-events`) with a run-state badge derived from the `/api/health` `missedRun` sibling field: `healthy` → `--color-status-healthy` `●` green; `missed` → `--color-status-degraded` `●` + UPPERCASE `MISSED` label (the load-bearing alarm); `unknown` → neutral `text-white/40` `○` (no alarm). Plus the tick age (`tabular-nums`, e.g. `4h ago`). Source the cron run-state from the health response the component already consumes (read the `missedRun` sibling, NEVER the `status` enum). Degrade-open: health response missing `missedRun` (older server) → muted placeholder `— no data (no cron data)` or self-hide, matching the host's established empty behavior. Use ONLY `@theme`/colorBridge tokens or existing Tailwind palette utilities — ZERO new inline hex/RGBA. Exactly two font weights {400,600}; no `font-bold` (700).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx src/components/ui/__tests__/DevApiStatus.operatorActions.test.tsx && (grep -nE "#[0-9a-fA-F]{3,6}|rgba\(" src/components/ui/DevApiStatus.tsx | grep -iE "Rate Limiter|Cron Freshness|429|missedRun|RateLimiterBlock" && echo "INLINE HEX FOUND — FAIL" || echo "no new inline hex near new blocks")</automated>
  </verify>
  <done>Both blocks render inside the API Health tabpanel; rate-limiter shows per-tier config + recent 429s; cron block shows healthy/MISSED/unknown with the MISSED alarm badge; tablist DOM untouched (diagnosticBlocks + operatorActions green); zero new inline hex.</done>
</task>

<task type="auto">
  <name>Task 3: sidecar-absent render coverage (HARD-03 surface 3) + deliberate layout snapshot regen</name>
  <files>src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx, src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</files>
  <read_first>
    - src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx (the existing tab-order / Bearer-gate / single-poll / roving-keyboard regression-lock — extend, don't rewrite)
    - src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx (the snapshot to deliberately regenerate IF the new blocks land in the snapshotted layout)
    - 46-UI-SPEC.md §"Regression-Lock Contract" (the 8 render-contract assertions — esp. #5 MISSED visual, #6 missed-never-in-status, #7 degrade-open, #1 tablist frozen)
    - 46-RESEARCH.md §"HARD-03" surface 3 (tabMerge sidecar-absent render is the named gap)
  </read_first>
  <action>
    Extend DevApiStatus.tabMerge.test.tsx with the named HARD-03 surface-3 gap (degrade-open when an operator sidecar field is absent/null): render the component with partial `OperatorStatus` payloads — `rateLimiter` undefined, `rateLimiter: null`, `tokenBudget: null`, `trendHistory` absent — and assert each new block self-hides or renders its muted placeholder WITHOUT crashing, and the tablist/tabpanel DOM is unchanged. Add the MISSED-visual assertion (a cron with `missedRun: 'missed'` renders the UPPERCASE `MISSED` label + the degraded token; `unknown` renders neutral). Add the audit-gate-safety assertion at the render level: the cron block reads `missedRun`, and a health payload whose `status` is in the 4-state set still drives the badge from the sibling (confirm `missed` is never sourced from `status`). Keep the existing frozen pins (tab ids, roving tabindex, single-poll) untouched and green. If the new blocks land inside the snapshotted layout, regenerate `DevApiStatusConsolidatedLayout.snapshot.test.tsx` DELIBERATELY (`-u`) — but the tablist/tabpanel-attribute subtree MUST NOT diff (verify the diff is confined to the new API-Health panel-body blocks; a tablist diff is a FAIL, revert and fix).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx src/components/ui/__tests__/DevApiStatusConsolidatedLayout.snapshot.test.tsx</automated>
  </verify>
  <done>sidecar-absent render proven degrade-open for the new blocks (no crash, muted placeholder/self-hide); MISSED visual + missed-never-in-status pinned; tablist subtree byte-stable; snapshot regen confined to the panel body.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                               |
| -------------------------------------------- | ----------------------------------------------------------------------------------------- |
| /api/operator-status (Bearer) → DevApiStatus | the rateLimiter block data crosses here; the client only renders, the gate is server-side |
| /api/health (public) → DevApiStatus          | the missedRun sibling crosses here; client renders a derived badge                        |
| operator browser DOM                         | the frozen WAI-ARIA tablist contract — a render change must not alter tab structure       |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                            | Disposition | Mitigation Plan                                                                                                                                                                                                 |
| ---------- | ---------------------- | -------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-46-04-01 | Tampering              | frozen tablist/tabpanel DOM                                          | mitigate    | Both blocks render INSIDE the existing role="tabpanel"; diagnosticBlocks + tabMerge tests pin tab ids + roving tabindex; the layout-snapshot diff is asserted confined to the panel body (a tablist diff fails) |
| T-46-04-02 | Information Disclosure | rateLimiter config + 429 counts rendered client-side                 | accept      | The data is already Bearer-gated at /api/operator-status (server-side); the client render adds no new exposure (ASVS V4)                                                                                        |
| T-46-04-03 | Information Disclosure | missedRun / cron schedule rendered from public /api/health           | accept      | /api/health already publicly exposes cron freshness; grace windows are not secret (carried from 46-02 T-46-02-02)                                                                                               |
| T-46-04-04 | Tampering              | rendering server strings (tier names, evidence-style values) as TEXT | mitigate    | Render all server-sourced strings as React text children (never dangerouslySetInnerHTML); carries the T-43-16 text-not-HTML rule (ASVS V5)                                                                      |
| T-46-04-05 | Denial of Service      | a null/absent sidecar field crashing the panel                       | mitigate    | Every new block is degrade-open (muted placeholder / self-hide on null); proven by the Task 3 sidecar-absent render tests                                                                                       |

</threat_model>

<verification>
- `npx vitest run src/components/ui/` green (tabMerge, diagnosticBlocks, operatorActions, snapshot, BudgetBlock, MetricRow, Sparkline)
- No new inline hex/RGBA in the new blocks (grep gate in Task 2)
- The layout-snapshot diff is confined to the API-Health panel body; tablist/tabpanel subtree byte-stable
</verification>

<success_criteria>

- Operator reads per-tier limiter config + recent 429s and per-cron run-state inside the existing API Health tab
- MISSED renders as a distinct degraded badge; unknown renders neutral; missed is never sourced from the wire status enum
- Both blocks degrade open; the frozen tablist DOM is byte-stable; zero new inline hex
  </success_criteria>

<output>
Create `.planning/phases/46-general-hardening-cron-watch-start/46-04-SUMMARY.md` when done
</output>

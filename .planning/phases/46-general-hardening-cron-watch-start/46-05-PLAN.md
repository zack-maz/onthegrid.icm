---
phase: 46-general-hardening-cron-watch-start
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/__tests__/llmCallHistory.test.ts
  - server/lib/__tests__/llmRunHistory.test.ts
  - server/lib/__tests__/trendHistory.test.ts
autonomous: true
requirements:
  - HARD-03
must_haves:
  truths:
    - 'hydrateCallHistoryIfCold / hydrateRunHistoryIfCold are no-ops when Redis throws mid-hydrate AND the hydration flag stays set (no retry-loop) — the named uncovered gap, not a re-assertion of already-green append/list throw cases'
    - 'trendHistory.ts (Phase 45, currently has NO dedicated test file) gets a degrade-open throw test proving appendTrendSample/readTrendHistory never throw on Redis failure'
    - 'Coverage targets the BEHAVIORAL degrade-open paths the D-10/D-11 contract names, not line-count for its own sake; already-green paths are NOT re-asserted'
  artifacts:
    - path: 'server/lib/__tests__/trendHistory.test.ts'
      provides: 'net-new degrade-open backfill test for the Phase-45 trend ring (file did not exist)'
      contains: 'appendTrendSample'
    - path: 'server/lib/__tests__/llmCallHistory.test.ts'
      provides: 'hydration-throw no-op + flag-stays-set coverage'
      contains: 'hydrateCallHistoryIfCold'
  key_links:
    - from: 'server/lib/__tests__/trendHistory.test.ts'
      to: 'server/lib/trendHistory.ts'
      via: 'mocked-Redis-throw harness asserts append/read never throw'
      pattern: 'trendHistory'
    - from: 'server/lib/__tests__/llmCallHistory.test.ts'
      to: 'server/lib/llmCallHistory.ts hydrateCallHistoryIfCold'
      via: 'listCallHistory throws mid-hydrate → hydrate resolves no-op, flag stays set'
      pattern: 'hydrate'
---

<objective>
HARD-03 Nyquist backfill: close the NARROW genuine degrade-open coverage gaps for the Phase 39/40 surfaces — NOT re-assert the already-green paths. Per RESEARCH §HARD-03 + Pitfall 5, the existing `llmCallHistory.test.ts` / `llmRunHistory.test.ts` / `llmTokenBudget.test.ts` / `BudgetBlock.test.tsx` already cover the append/list/null-gate degrade-open cases. The three real gaps this plan targets: (1) `hydrateCallHistoryIfCold` / `hydrateRunHistoryIfCold` throwing mid-hydrate — assert it is a no-op AND the hydration flag stays set (no retry-loop), a path no current test names; (2) `trendHistory.ts` (Phase 45) ships WITHOUT a dedicated test file (confirmed missing this session) — backfill its degrade-open throw coverage; (3) the new `rateLimiter`-block operator-status throw is covered in 46-01 Task 2, and the `tabMerge` sidecar-absent render is covered in 46-04 Task 3 — this plan does NOT duplicate those.

This plan is TEST-ONLY — no runtime change. The mocked-Redis-throw harness (`vi.hoisted` + `vi.mock('../../cache/redis.js')` returning `vi.fn`s that `mockRejectedValue`) is the exact pattern to copy from `llmCallHistory.test.ts:17-32`.

Purpose: backfill the fault-injection coverage 999.3/HARD-03 names for the flight recorder + trend ring degrade-open contracts.
Output: hydration-throw no-op coverage (call + run history) + a net-new trendHistory degrade-open test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-general-hardening-cron-watch-start/46-CONTEXT.md
@.planning/phases/46-general-hardening-cron-watch-start/46-RESEARCH.md
@.planning/phases/46-general-hardening-cron-watch-start/46-PATTERNS.md

# Exact analogs to replicate:

@server/lib/**tests**/llmCallHistory.test.ts
@server/lib/llmCallHistory.ts
@server/lib/llmRunHistory.ts
@server/lib/trendHistory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: hydration-throw no-op coverage (call + run history flight recorder)</name>
  <files>server/lib/__tests__/llmCallHistory.test.ts, server/lib/__tests__/llmRunHistory.test.ts</files>
  <read_first>
    - server/lib/__tests__/llmCallHistory.test.ts lines 17-32 (the mocked-Redis-throw harness — and confirm which degrade-open cases are ALREADY green so you do NOT re-assert them)
    - server/lib/llmCallHistory.ts lines 79-92 (hydrateCallHistoryIfCold — sets flag FIRST then listCallHistory(20); __resetCallHistoryHydrationForTest)
    - server/lib/llmRunHistory.ts lines 118-126 (hydrateRunHistoryIfCold + __resetRunHistoryHydrationForTest)
    - 46-RESEARCH.md §"HARD-03" "The ACTUAL gap" item 1 (hydration-throw no-op, flag stays set, no retry-loop) + §"Pitfall 5"
  </read_first>
  <behavior>
    - hydrateCallHistoryIfCold: when the underlying lrange throws mid-hydrate, the call RESOLVES (never throws — listCallHistory already swallows to []), the in-memory callHistory is left unchanged, and the hydration flag STAYS set so a second call is a no-op (does NOT re-issue the LRANGE / retry-loop).
    - hydrateRunHistoryIfCold: same contract — throw mid-hydrate → resolves no-op, flag stays set, second call does not re-LRANGE.
  </behavior>
  <action>
    First run `npx vitest run server/lib/__tests__/llmCallHistory.test.ts server/lib/__tests__/llmRunHistory.test.ts` and confirm the already-green degrade-open cases (append no-op on throw, list [] on throw) — do NOT re-assert those. Then add, to each file, the NAMED hydration gap: using the existing mocked-Redis harness, mock the lrange to `mockRejectedValue(new Error('redis down'))`, call `__resetCallHistoryHydrationForTest()` (resp. run) to clear the flag, then `await expect(hydrateCallHistoryIfCold()).resolves.not.toThrow()`; assert the in-memory history is unchanged; then call hydrate a SECOND time and assert lrange was NOT invoked again (flag stays set → no retry-loop) — e.g. capture the lrange mock call count and assert it did not increase on the second call. Mirror for `hydrateRunHistoryIfCold`. Keep all existing assertions green.
  </action>
  <verify>
    <automated>npx vitest run server/lib/__tests__/llmCallHistory.test.ts server/lib/__tests__/llmRunHistory.test.ts</automated>
  </verify>
  <done>Both hydrate helpers proven no-op on Redis throw with the flag staying set (no retry-loop); already-green cases untouched and still passing.</done>
</task>

<task type="auto">
  <name>Task 2: trendHistory degrade-open backfill (net-new test file)</name>
  <files>server/lib/__tests__/trendHistory.test.ts</files>
  <read_first>
    - server/lib/trendHistory.ts (the WHOLE file — appendTrendSample pipeline LPUSH+LTRIM+EXPIRE, readTrendHistory lrange + parseEntry dual-shape, both degrade-open by construction)
    - server/lib/__tests__/llmCallHistory.test.ts lines 17-32 (the mocked-Redis harness — note trendHistory uses redis.pipeline(), so the mock must expose a chainable pipeline whose .exec() can reject)
    - 46-PATTERNS.md §"No Analog Found" (trendHistory.test.ts is the one backfill — analog exists, test file does not)
    - 46-RESEARCH.md §"HARD-03" "The ACTUAL gap" item 2
  </read_first>
  <behavior>
    - appendTrendSample: on the happy path issues a pipeline lpush(TREND_HISTORY_KEY)+ltrim(0, TREND_MAX-1)+expire(TREND_TTL_SEC); when pipeline().exec() rejects, the call RESOLVES (no throw — degrade-open).
    - readTrendHistory: returns parsed samples newest-first on the happy path (dual-shape: string member AND already-object member both parse); returns [] when lrange rejects.
  </behavior>
  <action>
    Create server/lib/__tests__/trendHistory.test.ts using the mocked-Redis harness adapted for the pipeline API: mock `redis.pipeline()` to return a chainable object whose `lpush/ltrim/expire` return `this` and whose `exec()` is a `vi.fn()` (resolvable or rejectable per case), and mock `redis.lrange` directly. Cases: (a) appendTrendSample happy path invokes lpush with TREND_HISTORY_KEY + the JSON sample, ltrim 0..TREND_MAX-1, expire TREND_TTL_SEC, exec once; (b) appendTrendSample with exec rejecting → `await expect(...).resolves.not.toThrow()`; (c) readTrendHistory happy path returns the samples newest-first; (d) readTrendHistory dual-shape parse (one raw-string member, one already-object member both parse); (e) readTrendHistory with lrange rejecting → returns []. Use the TrendSample shape from trendHistory.ts (sampledAt, cronAgeMs {health,warm,'refresh-events'}, deadUrlCount).
  </action>
  <verify>
    <automated>npx vitest run server/lib/__tests__/trendHistory.test.ts</automated>
  </verify>
  <done>trendHistory.test.ts exists; appendTrendSample issues the bounded-ring pipeline + is no-throw on exec-reject; readTrendHistory parses dual-shape newest-first + returns [] on throw; all green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                         |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| test harness → mocked Redis | this plan is test-only; the only boundary is the Vitest mocked `cache/redis.js` module — no runtime/network surface |

## STRIDE Threat Register

| Threat ID  | Category                   | Component                                           | Disposition | Mitigation Plan                                                                                                                                 |
| ---------- | -------------------------- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-46-05-01 | Tampering                  | test-only change to existing green suites           | mitigate    | New cases are ADDITIVE (named hydration gap + net-new trendHistory file); existing assertions are kept and re-run green; no runtime code edited |
| T-46-05-02 | (N/A — no runtime surface) | flight-recorder / trend-ring degrade-open contracts | accept      | No new attack surface — pure unit tests with mocked-throwing Redis; the runtime modules are unchanged and already degrade-open by construction  |

</threat_model>

<verification>
- `npx vitest run server/lib/__tests__/llmCallHistory.test.ts server/lib/__tests__/llmRunHistory.test.ts server/lib/__tests__/trendHistory.test.ts` green
- `npx vitest run server/lib/` green (no regression in the lib suite)
- Confirm (per Pitfall 5) that no already-green degrade-open case was re-asserted as "new" coverage
</verification>

<success_criteria>

- Hydration-throw no-op + flag-stays-set proven for both flight-recorder hydrate helpers (the named gap)
- The Phase-45 trend ring gains its missing degrade-open throw test
- Coverage targets only the named behavioral gaps; already-green paths are not duplicated; no runtime change
  </success_criteria>

<output>
Create `.planning/phases/46-general-hardening-cron-watch-start/46-05-SUMMARY.md` when done
</output>

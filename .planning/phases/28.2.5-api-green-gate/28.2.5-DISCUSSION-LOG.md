# Phase 28.2.5: API Green-Light Prereq Gate — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-06
**Phase:** 28.2.5-api-green-gate
**Areas discussed:** Phase shape, Scope framing, Green-light definition, Weather tooltip fix path, LLM pipeline gate strictness, Evidence format

---

## Phase Shape

Initial routing decision — user invoked `/gsd-discuss-phase 28.3` but the actual intent was a NEW prereq scope, not a re-discuss of locked load-test decisions.

| Option                           | Description                                                                                                                                      | Selected |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| Extend 28.3 scope                | Add 'prereq verification + API gap fixes' decision band to existing 28.3 CONTEXT.md. Keeps one phase, longer scope.                              |          |
| New phase 28.2.5                 | Insert a small phase between 28.2 close and 28.3 load test. Pure 'fix the broken APIs / wire missing surfaces' scope. 28.3 stays load-test-only. | ✓        |
| View existing 28.3 CONTEXT first | Show what's already locked before deciding where the prereq lives.                                                                               |          |

**User's choice:** New phase 28.2.5
**Notes:** Keeps 28.3 cleanly scoped to k6 + edge-cache; 28.3 entry gates on 28.2.5 close. Matches the "branch per phase" convention.

---

## Scope Framing (after scout findings)

After scouting the actual code state, surfaced that the three reported issues are mostly small registry/wiring fixes rather than deep bugs. Reframed the scope question accordingly.

| Option                               | Description                                                                                                                       | Selected |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Narrow: fix the 3 named gaps         | Just LLM events monitoring, weather precip hydration verification, precip in API Health tab. No broader sweep.                    |          |
| Wider: green-light gate for ALL APIs | Audit every endpoint in healthSources.ts end-to-end. 28.3 GATED on fully-green Health tab.                                        |          |
| Hybrid: fix + gate                   | Fix the 3 named gaps AND establish 'all-tiers-green' as 28.3 entry gate, but don't touch already-green endpoints. Two-band scope. | ✓        |

**User's choice:** Hybrid: fix + gate
**Notes:** Captured as D-02 in CONTEXT.md. Band A = fixes, Band B = gate.

---

## Area 1 — Green-Light Definition

### Q1.1 — Strictness

| Option                                        | Description                                                                                                         | Selected |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------- |
| Strict: every tier healthy                    | All critical + non-critical + static = 'healthy', probe-only = 200, cron = within 26h. Single rule.                 |          |
| Tiered: critical strict, non-critical lenient | Critical MUST be 'healthy'. Non-critical/static MAY be 'degraded'. Probe-only = 200. Better fit for prod realities. | ✓        |
| Critical-only                                 | Only flights/ships/events must be healthy. Lightest gate.                                                           |          |
| Custom — I'll spell it out                    | Operator has specific bar in mind.                                                                                  |          |

**User's choice:** Tiered
**Captured as:** D-03 — concrete pass map: critical='healthy' / non-critical/static∈{healthy,degraded} / probe-only=200 / cron≤26h.

### Q1.2 — UI surface

| Option                             | Description                                                                            | Selected |
| ---------------------------------- | -------------------------------------------------------------------------------------- | -------- |
| JSON only                          | Gate is /api/health response. UI is separate. Cleanest, scriptable.                    |          |
| JSON + DevApiStatus row            | Gate is JSON AND dashboard row matches state. Catches wiring drift.                    | ✓        |
| JSON + DevApiStatus + map evidence | Add visible-on-map check (icons, tooltip on hover). Strongest but hardest to automate. |          |

**User's choice:** JSON + DevApiStatus row
**Captured as:** D-04 — PR includes workflow run link (mechanical) + operator screenshot of API Health tab matching state (manual).

---

## Area 2 — Weather Tooltip Fix Path

| Option                                 | Description                                                                                   | Selected |
| -------------------------------------- | --------------------------------------------------------------------------------------------- | -------- |
| Verify+wire only                       | Confirm useWaterPrecipPolling hydrates regardless of water layer toggle. Don't change cutoff. |          |
| Widen cutoff + 'nearest, X km' UI hint | Bump 2°→4° cutoff AND show distance in tooltip. Honest about precision.                       | ✓        |
| Bilinear interpolation across bbox     | Don't use nearest-neighbor; interpolate across map. Heaviest.                                 |          |
| Verify+wire AND widen cutoff           | Belt-and-suspenders combination.                                                              |          |

**User's choice:** Widen cutoff + 'nearest, X km' UI hint
**Captured as:** D-05 — widen ~4°/~400km, tooltip shows distance when sample is far. Hydration verification is implicit (planner check).
**Notes:** Bilinear interpolation deferred to a future quality-of-presentation phase.

---

## Area 3 — LLM Pipeline Gate Strictness

### Q3.1 — Gate strictness

| Option                                    | Description                                                                                                            | Selected |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------- |
| Visible only — add to monitoring          | Add events:llm:v3 to SOURCE_KEYS. Row visible; cache need not be populated. Lightest.                                  |          |
| Visible + populated                       | Above PLUS require status ∈ {healthy, degraded}. If empty, force-trigger /api/cron/refresh-events?force=true and wait. | ✓        |
| Visible + populated + eval-baseline fresh | Above PLUS eval baseline must exist with non-trivial score. Strongest — catches 'LLM producing garbage' failure mode.  |          |

**User's choice:** Visible + populated
**Captured as:** D-06 — 26h freshness, force-trigger on empty, gate-time check.
**Notes:** Eval-baseline-fresh deferred — extraction quality belongs in dedicated quality-gate phase.

### Q3.2 — How to surface enriched-vs-fallback

| Option                                    | Description                                                                                      | Selected |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------ | -------- |
| Two rows: 'Events (raw)' + 'Events (LLM)' | Existing row probes events:gdelt; new row probes events:llm:v3. Operator reads both at a glance. | ✓        |
| Single row with badge                     | Existing 'Events' row gains 'LLM' / 'raw' badge. Denser, slight ambiguity when both warm.        |          |
| Add to /api/llm-status pull only          | Don't touch events row. Surface in existing Events tab. API Health tab stays transport-focused.  |          |

**User's choice:** Two rows
**Captured as:** D-07.

---

## Area 4 — Evidence Format

### Q4.1 — Mechanism

| Option                                            | Description                                                                                                             | Selected |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------- |
| Extend W6 workflow with tier-green assertion      | Add step to prod-connectivity-audit.yml. Sidecar key absorbs new assertion result. Single source of truth, re-runnable. | ✓        |
| Screenshot in PR description                      | Operator screenshots API Health tab. Manual.                                                                            |          |
| Fresh /api/health JSON dump committed as artifact | curl > .planning/.../snapshot.json. Stale by 28.3 merge.                                                                |          |
| All three layered                                 | Belt-and-suspenders.                                                                                                    |          |

**User's choice:** Extend W6 workflow
**Captured as:** D-09 — new step in existing YAML, audit-status JSON shape gains `allTiersGreen` + `tierStatus`, contract test extended.

### Q4.2 — Target environment

| Option                                 | Description                                                                             | Selected |
| -------------------------------------- | --------------------------------------------------------------------------------------- | -------- |
| Prod only                              | 28.3 load-tests prod, so the gate measures prod. Re-uses W6 workflow exactly.           | ✓        |
| Prod AND dev                           | Both must pass. Catches drift earlier but doubles moving parts.                         |          |
| Dev for the 3 fixes, prod for the gate | Code-level fixes verified on dev (vitest + smoke); 'all-tiers-green' gate on prod only. |          |

**User's choice:** Prod only
**Captured as:** D-10.

---

## Claude's Discretion

Areas where the user did not specify (researcher/planner decides):

- Exact widened distance threshold inside `findNearestPrecip` (D-05)
- Final wording of the "nearest sample, X km away" hint (D-05)
- Whether `events:llm:v3` warrants a `'critical-llm'` sub-tier vs reusing `'critical'` (D-06)
- Audit JSON schema extension shape for `allTiersGreen` + `tierStatus` (D-09)
- Whether `Events (LLM)` row sits as DevApiStatus sibling or in its own LLM group (D-07)

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section:

- Bilinear interpolation across precip raster
- LLM eval-baseline freshness as quality gate
- Continuous baseline tracking of the green-light gate (weekly trend reports)
- Promoting `events:llm:v3` to dedicated `'critical-llm'` sub-tier
- Bearer-bypass changes for the workflow runner

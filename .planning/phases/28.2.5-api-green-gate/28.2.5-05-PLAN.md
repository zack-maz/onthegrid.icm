---
phase: 28.2.5
plan: 05
type: execute
wave: 4
depends_on: ['28.2.5-01', '28.2.5-02', '28.2.5-03', '28.2.5-04']
files_modified:
  - api/vercel-entry.js
  - CLAUDE.md
  - CHANGELOG.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-04
  - D-09
  - D-10
must_haves:
  truths:
    - 'Latest run of prod-connectivity-audit.yml against PROD shows allTiersGreen=true'
    - 'Operator screenshot of API Health tab shows the same JSON state (D-04 dual-surface evidence)'
    - 'CLAUDE.md documents the new SOURCE_KEYS entries (waterPrecip + llmEvents), the two-row split, the sidecar shape extension, and the phase close'
    - 'feature/28.2.5-api-green-gate is merged to main; 28.3 is unblocked'
  artifacts:
    - path: 'api/vercel-entry.js'
      provides: 'Refreshed tsup bundle reflecting all server-side changes from Plans 01-04'
    - path: 'CLAUDE.md'
      provides: 'Phase 28.2.5 closeout entry'
      contains: 'Phase 28.2.5'
    - path: 'CHANGELOG.md'
      provides: 'v1.4 28.2.5 entry'
  key_links:
    - from: 'PROD prod-connectivity-audit.yml run'
      to: 'audit:connectivity:last-result Redis sidecar'
      via: 'Upstash REST POST'
      pattern: 'allTiersGreen=true'
    - from: '28.2.5 PR description'
      to: 'GH Actions workflow run URL'
      via: 'evidence link'
      pattern: '/actions/runs/'
---

<objective>
Close Phase 28.2.5: rebuild the Vercel function bundle (`api/vercel-entry.js`) so PROD picks up all server-side changes from Plans 01/02/04, deploy to PROD, fire the extended `prod-connectivity-audit.yml` workflow, capture the green run as evidence, verify D-04 dual-surface (operator screenshot of API Health tab matching the workflow's JSON state), update CLAUDE.md + CHANGELOG.md, and merge `feature/28.2.5-api-green-gate` to main to unblock Phase 28.3.

Per CLAUDE.md project instructions: `tsup` bundles `server/vercel.ts` → `dist-server/vercel.cjs`, then the build copies the output to `api/vercel-entry.js` (the bundled output is committed when modified — that's the manual rebuild-before-commit friction Phase 999.2 backlog targets to remove). This plan handles the rebuild + commit at phase close.

Per CONTEXT D-04: dual-surface evidence is required. The mechanical workflow assertion (Plan 04) is one half; the operator screenshot of the API Health tab matching the JSON state is the other. The screenshot is checkpoint:human-verify — Claude can automate everything up to opening the dashboard URL but cannot capture the visual screenshot itself.

Per CONTEXT D-09 + D-10: PR description for 28.2.5 close (this plan) MUST include the workflow run link + the screenshot. PR description for 28.3 entry (out of scope here, future operator action) MUST also include the workflow run link as the gate evidence.

Per `feedback_phase_boundaries.md`: must merge to main; this phase's close unblocks 28.3.

Per `feedback_project_docs.md`: README, PROJECT_STATUS, CHANGELOG, CLAUDE.md updated at phase close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28.2.5-api-green-gate/28.2.5-CONTEXT.md
@.planning/phases/28.2.5-api-green-gate/28.2.5-RESEARCH.md
@CLAUDE.md
@CHANGELOG.md

<interfaces>
<!-- Build/deploy invariants from CLAUDE.md and prior phase patterns -->

From CLAUDE.md §"Vercel Deployment (Phase 14)":

- Entry point: `server/vercel.ts` exports Express app via `createApp()` factory in `server/app.ts`
- Bundle: tsup bundles `server/vercel.ts` → `dist-server/vercel.cjs` → copied to `api/vercel-entry.js`
- Build: `npm run build` runs Vite (frontend) + tsup (server) + tsc (typecheck)

From CLAUDE.md §"Phase 28.2 Dev/Prod Sync":

- Prod URL: `https://otg-iran-monitor.vercel.app`
- Vercel project: `onthegrid.icm` with alias `otg-iran-monitor.vercel.app`
- Bearer secret in GH Actions: `PROD_DASHBOARD_PASSWORD` (existing; reused for D-09 step)

From CLAUDE.md §"Phase 28.2 Dev/Prod Sync + Domain Rename" closeout block (the prior phase whose close pattern this plan mirrors):

- 6 plans landed in 6 waves; Plan 06 (W6) was the connectivity audit; CLAUDE.md gained a new closeout block at the end of the file. Same pattern applies for 28.2.5 — append a new closeout block immediately after the 28.2 one.

Existing prod-connectivity-audit.yml workflow (post-Plan-04 extensions):

- Trigger: `workflow_dispatch` (manual via GH Actions UI)
- Default target_url: `https://otg-iran-monitor.vercel.app`
- Outputs: smoke vitest results, rate-limit defense vitest results, sidecar Redis write, tier-green assertion (D-09 new)
- Final status: exits 1 if smoke fails OR ratelimit fails OR allTiersGreen != true
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild api/vercel-entry.js bundle reflecting all server changes from Plans 01/02/04</name>
  <files>api/vercel-entry.js</files>

<read_first> - CLAUDE.md §"Vercel Deployment (Phase 14)" (build invariants) - package.json `build` script - The current state of `api/vercel-entry.js` — confirm it's the tsup output not a hand-written file
</read_first>

  <action>
1. Confirm the working tree is clean except for the previously-merged Plans 01-04 changes:
```bash
git -C /Users/zackmaz/Desktop/my_world status
```
Expected: changes from Plans 01-04 already committed; no uncommitted source changes. The `api/vercel-entry.js` MAY appear modified (the dirty marker noted in initial git status at session start) — that's the artifact this task addresses.

2. Run the production build to refresh both Vite frontend AND the tsup server bundle:

```bash
npm run build
```

Expected: Vite dist + tsup output + tsc typecheck all exit 0.

3. Verify the bundle picked up the SOURCE_KEYS additions from Plans 01 + 02 by grepping the bundled file:

```bash
grep -c "events:llm:v3" api/vercel-entry.js
grep -c "water:precip" api/vercel-entry.js
grep -c "DRIFT-4\|DRIFT-5" api/vercel-entry.js
```

Each should return ≥ 1 (the bundler may strip comments — DRIFT count may be 0; that's OK as long as the runtime values are present).

4. Run the full vitest suite once more as a final regression check before commit:

```bash
npx vitest run
```

Expected: all tests pass; counts should be Plan 01's +4 + Plan 02's +6 + Plan 03's +5 + Plan 04's +2 = ~+17 over the pre-phase baseline.

5. Run lint + tsc:

```bash
npm run lint
npx tsc --noEmit
```

Both exit 0.

6. Commit the bundle refresh as a SEPARATE commit so it's bisect-clean if a regression surfaces:

```bash
git add api/vercel-entry.js
git commit -m "build(28.2.5): refresh api/vercel-entry.js bundle for Plans 01/02/04 server changes

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

If the bundle is byte-identical (no Plan 01/02/04 server changes affect the prod bundle output), skip the commit and note in the plan summary that the bundle was already up-to-date.
</action>

  <verify>
    <automated>npm run build && npx vitest run --reporter=basic 2>&1 | tail -3</automated>
  </verify>

<acceptance_criteria> - `npm run build` exits 0 - `npx vitest run` exits 0 (full suite green) - `npm run lint` exits 0 - `npx tsc --noEmit` exits 0 - `api/vercel-entry.js` either committed with Plans-01/02/04 server-side updates baked in, OR confirmed byte-identical to HEAD (build is reproducible from source) - `grep "events:llm:v3" api/vercel-entry.js` returns ≥ 1 line - `grep "water:precip" api/vercel-entry.js` returns ≥ 1 line
</acceptance_criteria>

<done>The PROD bundle reflects every server-side change from Plans 01/02/04; the working tree is ready to deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to PROD via Vercel CLI</name>

  <what-built>
    Plans 01-04 + Task 1 above produced:
    - `server/lib/healthSources.ts` with `waterPrecip` (DRIFT-4) + `llmEvents` (DRIFT-5) registry entries
    - `server/routes/health.ts` with `PROBE_STRATEGIES.llmEvents` + aligned `waterPrecip` indirection
    - `server/routes/audit-status.ts` with widened `AuditPayload` interface
    - `src/components/ui/DevApiStatus.tsx` with Precip row sourced from /api/health + Events (raw)/(LLM) two-row split
    - `src/components/map/layers/WeatherOverlay.tsx` with widened findNearestPrecip + distance hint
    - `.github/workflows/prod-connectivity-audit.yml` with tier-green assertion + Step 6 fail-closed
    - Refreshed `api/vercel-entry.js` bundle

    The deploy step is the operator's because Vercel CLI auth lives in the operator's local `~/.vercel` config; Claude does not have credentials.

  </what-built>

  <how-to-verify>
    Operator runs:

    ```bash
    cd /Users/zackmaz/Desktop/my_world
    git push origin feature/28.2.5-api-green-gate
    vercel --prod
    ```

    Expected:
    - `git push` succeeds
    - `vercel --prod` deploys and prints the deployment URL
    - The deployment is aliased to `otg-iran-monitor.vercel.app` (or a new alias is added if Vercel rotated the deployment ID)

    Post-deploy smoke check (operator runs):
    ```bash
    curl -s https://otg-iran-monitor.vercel.app/api/health | jq '.endpoints.llmEvents'
    curl -s https://otg-iran-monitor.vercel.app/api/health | jq '.endpoints.waterPrecip'
    ```
    Expected: both return non-null objects with `tier`, `status`, `freshnessThresholdMs` fields. `llmEvents.tier === "critical"`; `waterPrecip.tier === "non-critical"`.

  </how-to-verify>

<files>(none — operator-driven deploy step; no file commits in this task)</files>

  <action>
This is an operator-driven checkpoint. Claude cannot execute Vercel deploy because credentials live in the operator's local `~/.vercel` config. The operator runs the bash steps in `<how-to-verify>` and signals completion via `<resume-signal>`. Claude resumes after the operator confirms the deployment URL and the post-deploy curl smoke shows non-null `endpoints.llmEvents` + `endpoints.waterPrecip` objects.
  </action>

  <verify>
    <automated>curl -sf https://otg-iran-monitor.vercel.app/api/health | grep -E "llmEvents|waterPrecip"</automated>
  </verify>

<done>PROD deploy completes; `/api/health` returns the two new endpoints; operator confirms via `<resume-signal>`.</done>
<resume-signal>Type "deployed" with the deployment URL or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Trigger prod-connectivity-audit.yml from GitHub Actions UI; capture run URL</name>

  <what-built>
    Plan 04 extended `prod-connectivity-audit.yml` with the tier-green assertion. With the new bundle on PROD (Task 2), the workflow now has the new `endpoints.llmEvents` field to assert against AND `endpoints.waterPrecip` returning correct freshness data.
  </what-built>

  <how-to-verify>
    Operator opens https://github.com/zack-maz/onthegrid.icm/actions/workflows/prod-connectivity-audit.yml (the path may differ if the repo moved during 28.2 W1 rename — confirm via `gh repo view --web`).

    Click "Run workflow" → keep default `target_url` (`https://otg-iran-monitor.vercel.app`) → "Run workflow".

    Wait for completion (~3-5 minutes typical). Expected outcome:
    - Step 1 "Connectivity smoke against prod" — exit 0 (16/17 endpoints PASS, news may transiently fail per Phase 28.2 W6 known condition)
    - Step 2 "Rate-limit defense companion" — exit 0
    - Step 3 "Write audit result to Redis sidecar" — exit 0; logs `Tier-green assertion: { allTiersGreen: true, tierStatus: { critical: "healthy", ... } }` AND `Wrote audit:connectivity:last-result = { ..., allTiersGreen: true, tierStatus: {...} }`
    - Step 4 "Verify /api/audit-status reflects the new result" — curl shows the new fields
    - Step 6 "Final status check" — exit 0; logs `All connectivity audit checks passed (incl. tier-green gate).`

    If `allTiersGreen=false`:
    - Inspect Step 3 logs for the failing tier
    - Most likely: `events:llm:v3` is cold (cron has not run since the redeploy). Wait for the next 4am UTC cron tick OR force-trigger via:
      ```bash
      curl -X GET "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true" \
        -H "Authorization: Bearer ${CRON_SECRET}"
      ```
      then re-run the workflow.

    Capture the workflow run URL (format: `https://github.com/zack-maz/onthegrid.icm/actions/runs/{run_id}`) for inclusion in the 28.2.5 PR description.

  </how-to-verify>

<files>(none — operator-driven workflow trigger; sidecar Redis write is the produced artifact, surfaced via /api/audit-status)</files>

  <action>
This is an operator-driven checkpoint. The operator clicks "Run workflow" in the GitHub Actions UI per `<how-to-verify>`. Claude cannot trigger GitHub workflows from this environment. The operator captures the run URL and signals completion via `<resume-signal>`. If `allTiersGreen=false`, the operator follows the cold-cache mitigation path documented in `<how-to-verify>` (curl `?force=true` with their CRON_SECRET Bearer, then re-run).
  </action>

  <verify>
    <automated>curl -sf https://otg-iran-monitor.vercel.app/api/audit-status | grep allTiersGreen</automated>
  </verify>

<done>Workflow run completes green with `allTiersGreen: true` written to the sidecar Redis key; operator captures the run URL for PR evidence.</done>
<resume-signal>Type "green" with the workflow run URL OR describe failure mode (which tier failed; whether cold-cache or genuine red).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator screenshot of API Health tab matching the workflow JSON (D-04 dual-surface evidence)</name>

  <what-built>
    With Task 3's workflow run green:
    - `audit:connectivity:last-result` Redis sidecar contains `allTiersGreen: true` + `tierStatus: { critical: "healthy", ... }`
    - The merged API Health tab in PROD's DevApiStatus surfaces the new fields automatically (the audit-result banner reads from `/api/audit-status`)
    - All 14 rows in the API Health tab (including new `Events (LLM)` and rewired `Precip`) reflect the same JSON state that the workflow asserted on
  </what-built>

  <how-to-verify>
    Operator:
    1. Open https://otg-iran-monitor.vercel.app in a browser
    2. Authenticate (Bearer cookie OR enter the dashboard password if the gate prompts)
    3. Open DevApiStatus modal (the trigger pill in the topbar; `shouldRenderDashboard()===true` reveals it)
    4. Confirm the "API Health" tab is selected (first tab; D-22/D-27 from Phase 28.2 W5)
    5. Visually verify all 14 rows render with sensible status indicators:
       - Flights, Ships, Events (raw), Events (LLM), Sites, News, Markets, Weather, Water, Precip
       - Critical-tier rows (Flights, Ships, Events (raw), Events (LLM)) all show "healthy"
       - Precip row's freshness reflects the /api/health value (NOT the waterStore default '--')
       - Events (LLM) row shows count + quality string distinct from Events (raw)
    6. Open browser DevTools → Network tab → filter `health` → click the `/api/health` request → confirm response JSON's `endpoints.{flights,ships,events,llmEvents,waterPrecip}` all align with the row state
    7. Take a screenshot of the API Health tab
    8. Save the screenshot somewhere accessible (operator's filesystem; will be attached to the PR)

    Cross-check against the workflow run from Task 3:
    - `allTiersGreen` in the run logs === the visual state in the API Health tab
    - `tierStatus.critical === "healthy"` ↔ all critical rows show green
    - Audit-result banner at the top of the tab (if rendered) references the run timestamp from Task 3

  </how-to-verify>

<files>(none — operator-captured screenshot saved locally, attached to PR in Task 7)</files>

  <action>
This is an operator-driven checkpoint per CONTEXT D-04 dual-surface evidence requirement. The visual screenshot of the API Health tab matching the /api/health JSON state cannot be automated from Claude's environment (no browser session against PROD with Bearer auth). The operator follows the 8-step verification in `<how-to-verify>` and saves the screenshot for PR attachment.
  </action>

  <verify>
    <automated>echo "Manual visual verification — screenshot path supplied via resume-signal"</automated>
  </verify>

<done>Operator visually confirms all 14 rows in API Health tab match /api/health JSON state, screenshot saved, ready for PR attachment.</done>
<resume-signal>Type "screenshot captured" with the screenshot file path OR describe drift (which row state in the UI mismatches the JSON).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update CLAUDE.md + CHANGELOG.md with Phase 28.2.5 closeout block</name>
  <files>CLAUDE.md, CHANGELOG.md</files>

<read_first> - CLAUDE.md (last ~50 lines — the existing 28.2 closeout block is the template to mirror) - CHANGELOG.md (top of file — find the v1.4 in-progress section) - .planning/phases/28.2.5-api-green-gate/28.2.5-CONTEXT.md (decisions list — D-01 through D-10)
</read_first>

  <action>
EDIT 1 — Append a new closeout block to CLAUDE.md immediately after the existing "Phase 28.2 Dev/Prod Sync + Domain Rename" closeout block at the end of the file. Match the prior block's structure (paragraph-style highlights with bolded headers).

```markdown
## Phase 28.2.5 API Green-Light Prereq Gate — closeout {YYYY-MM-DD}

Phase 28.2.5 prereq gate complete (5 plans across 4 waves). Highlights:

- **LLM events monitoring (Plan 02, D-06/D-07):** `events:llm:v3` promoted from observability-only to gate-relevant — added to `SOURCE_KEYS` (DRIFT-5), `FRESHNESS_THRESHOLDS_MS` (26h, matches cron triad), `TIER_BY_ENDPOINT` (`'critical'`). DevApiStatus splits the legacy single `Events` row into `Events (raw)` (probes `events:gdelt`) + `Events (LLM)` (probes `events:llm:v3`) — operator gets a one-glance read on enriched-vs-fallback state. New `endpoints.llmEvents` field in the `/api/health` aggregate response. Stayed at `'critical'` tier per RESEARCH Open Question 1; deferred `'critical-llm'` sub-tier per CONTEXT Deferred Ideas.
- **Precipitation registry fix (Plan 01, D-08):** `waterPrecip: 'water:precip'` added to `SOURCE_KEYS` with DRIFT-4 comment. DevApiStatus `Precip` row switched from `useWaterStore` selectors to `/api/health` aggregate consumption — same pattern every other row uses. `PROBE_STRATEGIES.waterPrecip` aligned to `SOURCE_KEYS.waterPrecip!` indirection (free cleanup per RESEARCH Landmine #3). Plan 01 also lands the **registry-consistency invariant** test that would have caught this drift in the first place: every cache-backed `TIER_BY_ENDPOINT` key MUST have a matching `SOURCE_KEYS` entry.
- **Weather tooltip widening (Plan 03, D-05):** `findNearestPrecip` cutoff widened from 2° to 4° Manhattan degrees so every Iran-bbox cursor position resolves a precip sample. Return shape changed from `PrecipitationData | null` to `{ value: PrecipitationData; distanceKm: number } | null`. Tooltip surfaces a "nearest sample, X km away" hint when the resolved sample is more than 100km from the cursor — UI honesty about the spatial-precision tradeoff. RESEARCH Landmine #2 confirmed `useWaterPrecipPolling` already fires unconditionally in `AppShell.tsx:45` — no hydration code change needed.
- **Tier-green gate (Plan 04, D-03/D-04/D-09):** Extended 28.2 W6 `.github/workflows/prod-connectivity-audit.yml` with a tier-green assertion folded INTO Step 3's existing inline node script. Workflow fetches `/api/health` Bearer'd, computes `allTiersGreen` + `tierStatus` against the D-03 truth table (critical=`'healthy'`, non-critical/static∈{`'healthy'`,`'degraded'`}, probe-only=`'healthy'`, cron∈{`'healthy'`,`'degraded'`}), writes both fields into the existing `audit:connectivity:last-result` Redis sidecar payload (7d TTL). Step 6 "Final status check" gains a third exit-1 branch — workflow fails LOUDLY when allTiersGreen != true. Sidecar shape extension pinned by NEW W-3 contract test in `server/routes/__tests__/audit-status.test.ts` (existing `'matches CI workflow JSON shape contract'` block byte-unchanged). Path B selected (cold-cache self-heal trusted) per RESEARCH Open Question 6 — no `PROD_CRON_SECRET` GitHub Actions secret required.
- **AuditPayload extension:** `server/routes/audit-status.ts` `AuditPayload` interface widened with optional `allTiersGreen?: boolean` + `tierStatus?: { critical, nonCritical, static, probeOnly, cron }` fields — handler logic unchanged (still shape-agnostic `res.json(parsed)` passthrough). The merged API Health tab's audit-result banner picks up the new fields automatically.
- **Phase 27.4.x + 28.1 + 28.2 Redis key bridge preserved:** all load-bearing keys UNCHANGED. Plan 01/02 only ADD entries to the registry; never rename or repurpose existing keys. Pitfall 1 cache-bridge fallback chain at `events.ts:701-731` remains intact as the safety net even when `events:llm:v3` is the gate-relevant probe target.
- **D-10 prod-only gate:** workflow target stays `https://otg-iran-monitor.vercel.app`; dev-side verification of Band-A fixes is by `npx vitest run` + manual smoke. Operator runs `prod-connectivity-audit.yml` once before opening 28.2.5 close PR (proves fixes landed) and again before 28.3 merge (proves the gate held).
- **Phase 28.3 unblocked:** With `allTiersGreen=true` mechanically pinned by the workflow + W-3 contract test, the k6 1-300 VU sweep can start against a coherent prod surface — load-test numbers will measure prod under load, not a half-broken data path.
```

(The executor fills in `{YYYY-MM-DD}` with the actual close date.)

EDIT 2 — Update CHANGELOG.md. Find the `## v1.4` section (or the in-progress milestone heading at top of file). Append:

```markdown
### Phase 28.2.5: API Green-Light Prereq Gate (YYYY-MM-DD)

- **D-06/D-07 LLM events monitoring:** `events:llm:v3` added to `SOURCE_KEYS` registry; DevApiStatus splits Events row into `Events (raw)` + `Events (LLM)` siblings.
- **D-08 Precipitation registry fix:** `waterPrecip: 'water:precip'` added to `SOURCE_KEYS`; DevApiStatus Precip row consumes `/api/health` aggregate. Registry-consistency invariant test prevents future drift.
- **D-05 Weather tooltip widening:** `findNearestPrecip` cutoff widened 2°→4°; tooltip shows distance hint when nearest sample is more than 100km away.
- **D-09 Tier-green gate:** extended `prod-connectivity-audit.yml` with tier-green assertion; sidecar shape gains `allTiersGreen` + `tierStatus` fields; W-3 contract test extended.
- **D-10 Prod-only gate target:** workflow runs against `otg-iran-monitor.vercel.app`; gate satisfied at phase close, unblocking Phase 28.3.
- 5 plans, ~13-15 atomic commits, ~17 new vitest assertions across `healthSources.test.ts` / `health.test.ts` / `audit-status.test.ts` / `DevApiStatus.tabMerge.test.tsx` / `WeatherOverlay.test.tsx` (new file).
```

EDIT 3 — Update PROJECT_STATUS.md if it exists (per `feedback_project_docs.md`):

```bash
ls PROJECT_STATUS.md README.md 2>&1
```

- If `PROJECT_STATUS.md` exists, append a 28.2.5 line under the "Recently completed" section
- If `README.md` has a Phase Summary table, add 28.2.5 row
- If neither — skip (executor's discretion per `feedback_project_docs.md`)

Commit message: `docs(28.2.5): closeout block in CLAUDE.md + CHANGELOG.md`
</action>

  <verify>
    <automated>grep -c "Phase 28.2.5 API Green-Light Prereq Gate" CLAUDE.md</automated>
  </verify>

<acceptance_criteria> - CLAUDE.md ends with a new "Phase 28.2.5 API Green-Light Prereq Gate — closeout" block - CLAUDE.md closeout block mentions all 5 D-XX decisions surfaced (D-05, D-06, D-07, D-08, D-09; D-03/D-04/D-10 are referenced inline) - CHANGELOG.md has a new "Phase 28.2.5" entry under v1.4 - The actual close date is filled in (not literal `{YYYY-MM-DD}`) - `grep "Phase 28.2.5" CLAUDE.md` returns ≥ 1 match - `grep "DRIFT-4\|DRIFT-5" CLAUDE.md` returns ≥ 2 matches (one each)
</acceptance_criteria>

<done>Documentation reflects phase close; the next operator opening CLAUDE.md sees the new tier-green gate as a load-bearing project invariant.</done>
</task>

<task type="auto">
  <name>Task 6: Update ROADMAP.md to mark 28.2.5 complete + flip 28.3 from blocked to ready</name>
  <files>.planning/ROADMAP.md</files>

<read_first> - .planning/ROADMAP.md (the 28.2.5 entry around line 318-324; the 28.3 entry around line 326-331)
</read_first>

  <action>
EDIT 1 — Mark 28.2.5 complete. Find the `### Phase 28.2.5:` entry. Update:
- Title line: append ` ✅ COMPLETE (YYYY-MM-DD)` at the end (mirror the 28.2 close pattern at line 316)
- `**Plans:**` line: `0 plans` → `5 plans (5/5 complete)`
- Add a Plans bullet list under the entry (mirror Phase 28.2's at lines 309-314):

```markdown
Plans:

- [x] 28.2.5-01-PLAN.md — Wave 1: D-08 waterPrecip SOURCE_KEYS fix + registry-consistency invariant + Precip row /api/health consumption
- [x] 28.2.5-02-PLAN.md — Wave 2: D-06/D-07 LLM events monitoring (events:llm:v3 in registry + Events (raw)/(LLM) two-row split)
- [x] 28.2.5-03-PLAN.md — Wave 1: D-05 weather tooltip widening (findNearestPrecip 2°→4° + distance hint)
- [x] 28.2.5-04-PLAN.md — Wave 3: D-09 tier-green assertion in prod-connectivity-audit.yml + W-3 contract test extension
- [x] 28.2.5-05-PLAN.md — Wave 4: phase close (build refresh + PROD deploy + workflow trigger + docs + merge)
```

(Wave numbers reflect actual execution order from PLAN frontmatter — Plans 01 + 03 in Wave 1 parallel; Plan 02 in Wave 2 because it shares files with Plan 01; Plan 04 in Wave 3 because it depends on 02; Plan 05 in Wave 4.)

EDIT 2 — Update Phase 28.3's `Depends on:` line. Find the `### Phase 28.3:` entry (around line 326). The current line says something like:

```
**Depends on:** Phase 28.2 (must merge to main first per D-01; D-03 domain rename must land before scripts/load-test.js BASE_URL update); Phase 28.2.5 (entry blocked until prod-connectivity-audit.yml shows allTiersGreen=true per 28.2.5 D-09/D-10)
```

Update the 28.2.5 clause to reflect the unblock:

```
**Depends on:** Phase 28.2 (merged to main 2026-05-06; D-03 domain rename + W6 audit workflow are prerequisites); Phase 28.2.5 (merged to main YYYY-MM-DD; allTiersGreen=true verified via run {workflow_run_url})
```

EDIT 3 — Update STATE.md (lightweight; no full rewrite). Find the line beginning with `Phase: 28.2.5` near the top and update the status to `phase_28.2.5_complete` or similar; append a new line referencing the merged commit hash and the workflow run URL. Keep it brief — STATE.md is a snapshot, not an archive.

Commit message: `docs(roadmap): mark 28.2.5 complete + unblock 28.3`
</action>

  <verify>
    <automated>grep -c "✅ COMPLETE" .planning/ROADMAP.md</automated>
  </verify>

<acceptance_criteria> - .planning/ROADMAP.md `### Phase 28.2.5:` entry has `✅ COMPLETE` marker with date - .planning/ROADMAP.md `### Phase 28.2.5:` entry has 5 `[x]` plan rows - .planning/ROADMAP.md `### Phase 28.3:` entry's `Depends on:` line shows 28.2.5 merged + workflow run URL - .planning/STATE.md status line reflects 28.2.5 complete
</acceptance_criteria>

<done>The roadmap declares 28.2.5 closed and 28.3 ready; the operator can run `/gsd-discuss-phase 28.3` next.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Open + merge PR for feature/28.2.5-api-green-gate to main</name>

  <what-built>
    - Tasks 1-6 produced all the code, tests, docs, and roadmap updates
    - Workflow run from Task 3 is the load-bearing evidence
    - Screenshot from Task 4 is the dual-surface evidence
  </what-built>

  <how-to-verify>
    Operator:
    1. Push branch (if not already pushed):
       ```bash
       git push origin feature/28.2.5-api-green-gate
       ```

    2. Open PR via gh CLI:
       ```bash
       gh pr create --base main --head feature/28.2.5-api-green-gate \
         --title "Phase 28.2.5: API Green-Light Prereq Gate" \
         --body "$(cat <<'EOF'

## Summary

Phase 28.2.5 — prereq gate inserted between 28.2 (complete) and 28.3 (load test). Two-band scope: **Band A** = three named code/wiring fixes (D-05/D-06/D-07/D-08); **Band B** = tier-green gate that 28.3 depends on (D-03/D-04/D-09/D-10).

5 plans across 4 waves, ~13-15 commits, +17 vitest assertions.

## D-XX coverage

- **D-01** Phase shape ✅ (4-wave train; 28.3 unblocked)
- **D-02** Hybrid scope ✅ (Band A landed in Plans 01/02/03; Band B in Plan 04)
- **D-03** Tiered green bar ✅ (assertion logic in workflow Step 3 inline node)
- **D-04** Dual-surface evidence ✅ (workflow run link + screenshot below)
- **D-05** Weather tooltip widening ✅ (Plan 03)
- **D-06** LLM events visible + populated ✅ (Plan 02)
- **D-07** Two-row Events split ✅ (Plan 02)
- **D-08** waterPrecip in SOURCE_KEYS ✅ (Plan 01)
- **D-09** Workflow extension + sidecar shape ✅ (Plan 04)
- **D-10** Prod-only target ✅ (workflow run against otg-iran-monitor.vercel.app)

## Evidence

- **Workflow run (D-09):** {paste run URL from Task 3}
- **Screenshot (D-04):** {attach or link screenshot from Task 4}

## Test plan

- [x] `npx vitest run` exits 0
- [x] `npm run lint` exits 0
- [x] `npx tsc --noEmit` exits 0
- [x] `npm run build` exits 0
- [x] prod-connectivity-audit.yml run shows allTiersGreen=true
- [x] API Health tab visually matches /api/health JSON state (screenshot)
- [x] `events:llm:v3` and `water:precip` keys appear in `api/vercel-entry.js` bundle

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"

````

    3. Wait for CI checks to pass on the PR (if any). Address review feedback if any.

    4. Merge to main (operator's choice of merge strategy — squash, rebase, or merge commit):
       ```bash
       gh pr merge --squash  # or --rebase or --merge
       ```

    5. Tag the merge commit hash for the STATE.md update + auto-memory logging.

    6. Re-run prod-connectivity-audit.yml ONE MORE TIME against the post-merge main deploy to confirm the gate still holds. Capture this second run URL for the 28.3 entry PR description.

  </how-to-verify>

<files>(PR description content; merge commit on main; no plan-tracked file modifications)</files>

  <action>
This is an operator-driven checkpoint. Claude can prepare the PR body text but cannot run `gh pr create` against the remote without operator credentials, and cannot perform the squash-merge to main without operator authorization. The operator runs the gh CLI commands in `<how-to-verify>`, addresses any CI/review issues, and signals completion with the squash commit hash + the post-merge re-run workflow URL.
  </action>

  <verify>
    <automated>gh pr view --json state,mergeCommit -q '.state + " " + (.mergeCommit.oid // "pending")'</automated>
  </verify>

<done>PR merged to main; second workflow run shows `allTiersGreen: true`; feature branch retired or kept per operator choice; Phase 28.3 unblocked.</done>
<resume-signal>Type "merged" with the squash commit hash + the second workflow run URL OR describe issues (CI failure, review block, etc.).</resume-signal>
</task>

</tasks>

<verification>
After all 7 tasks land:

1. Commit history on main shows the 28.2.5 plan train + closeout docs + roadmap update
2. Two prod-connectivity-audit.yml runs are green: pre-merge (proves fixes landed) + post-merge (proves the gate held)
3. CLAUDE.md has the new 28.2.5 closeout block
4. CHANGELOG.md has the new 28.2.5 entry
5. ROADMAP.md shows ✅ COMPLETE on 28.2.5 + 28.3 Depends-on updated
6. STATE.md reflects phase close
7. `feature/28.2.5-api-green-gate` branch is merged (or deleted post-merge per operator preference)
8. Phase 28.3 is ready: operator can `/gsd-discuss-phase 28.3` or `/gsd-plan-phase 28.3`
   </verification>

<success_criteria>

- [ ] api/vercel-entry.js bundle refreshed (or confirmed byte-identical)
- [ ] PROD deployed and `/api/health` returns endpoints.llmEvents + endpoints.waterPrecip
- [ ] prod-connectivity-audit.yml pre-merge run is green with allTiersGreen=true
- [ ] Operator screenshot of API Health tab captured matching JSON state (D-04)
- [ ] CLAUDE.md, CHANGELOG.md, ROADMAP.md, STATE.md all updated
- [ ] PR merged to main; post-merge audit run also green
- [ ] Phase 28.3 unblocked
      </success_criteria>

<output>
After completion, create `.planning/phases/28.2.5-api-green-gate/28.2.5-05-SUMMARY.md` capturing:
- Build refresh outcome (bundle modified or unchanged)
- Both workflow run URLs (pre-merge + post-merge)
- Screenshot path (or operator confirmation that it's attached to PR)
- Commit hashes for the closeout commits
- Squash-merge commit hash
- Confirmation of D-04 dual-surface evidence in PR description
- Confirmation of D-09 + D-10 evidence path satisfied (PROD-only, workflow-mechanical)
- Plan 06 / Phase 28.3 readiness signal
</output>
````

---
phase: 33-actor-metadata-audit-canonical-catalog-eval-expansion
plan: 06
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-04
files_modified:
  - server/routes/operator-status.ts
  - server/__tests__/routes/operator-status.test.ts
autonomous: true
requirements: [ACTOR-05]
tags: [actor-metadata, operator-status, aggregator, dashboard-backend]

must_haves:
  truths:
    - "/api/operator-status response gains a top-level actorQuality block with shape: {totalEvents, nullActors, rawCameoActors, ambiguousActors, lowConfidenceActors, sample[]} where sample.length ≤ 20 (LIMIT_DRILL_DOWN)"
    - "actorQuality is computed lazily over the already-deserialized events:llm:v3 payload — ZERO new Redis sidecars (D-16, matches Phase 32 D-13 smallest-blast-radius)"
    - "Aggregator block degrades open on Redis failure: cacheGetSafe throws or returns null → actorQuality === null, route returns 200 (not 500)"
    - "Bucket classification reuses server/lib/actorClassifier.ts (Plan 33-01) — single source of truth shared with the audit script (Pitfall §1 dedup)"
    - "CAMEO codebook for the operator-status block is an INLINE hardcoded subset of well-known actor codes (PATTERNS critical risk #3) — the .planning/phases/33-*/cameo-codes.json file is NOT bundled into the Vercel server build; audit script remains the full-codebook consumer"
    - "Bearer auth gate is unchanged — /api/operator-status already authenticated; no new auth surface added"
  artifacts:
    - path: "server/routes/operator-status.ts"
      provides: "Extended aggregator with actorQuality block computed inline over events:llm:v3"
      contains: "actorQuality"
    - path: "server/__tests__/routes/operator-status.test.ts"
      provides: "Integration tests: actorQuality shape, sample cap = 20, degrade-open on Redis failure"
  key_links:
    - from: "server/routes/operator-status.ts (actorQuality block)"
      to: "server/lib/actorClassifier.ts (classifyEventActors)"
      via: "import + invocation per-event"
      pattern: "classifyEventActors"
    - from: "server/routes/operator-status.ts (actorQuality block)"
      to: "events:llm:v3 (via cacheGetSafe)"
      via: "ONE cacheGetSafe call over the full v3 array (no SCAN)"
      pattern: "cacheGetSafe.*LLM_EVENTS_KEY_ACTIVE|events:llm:v3"
---

<objective>
Extend `/api/operator-status` with an `actorQuality` block — fulfilling ACTOR-05 server-side (D-16) and exposing the audit-bucket counts + low-confidence count to the API Health dashboard (Plan 33-07).

Purpose: Operator gains continuous visibility into actor metadata quality without re-running the one-shot audit script (Plan 33-01). Counts refresh on the existing `/api/operator-status` poll loop; no new poll cadence, no new endpoint.

**PATTERNS critical risk #3 (CAMEO codebook bundling):** `.planning/phases/33-*/cameo-codes.json` is NOT bundled into the Vercel server build artifact. The operator-status block CANNOT read from disk at runtime. Solution: inline a hardcoded subset of well-known actor codes (covering ~20 country-military codes from Plan 33-02's catalog) directly in `operator-status.ts`. The audit script (Plan 33-01) remains the canonical full-codebook consumer (runs locally, reads from disk fine).

**Anti-pattern #17 preserved:** Read-only block; ZERO writes to any Redis key. `/api/operator-status` remains aggregator-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-01-PLAN.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-04-PLAN.md
@server/routes/operator-status.ts
@server/__tests__/routes/operator-status.test.ts
@server/lib/actorClassifier.ts
@CLAUDE.md

<interfaces>
<!-- Key references for the executor. -->

From server/routes/operator-status.ts (verified per RESEARCH):
- Line 35: `const log = logger.child({ module: 'operator-status' });`
- Line 50: `const LIMIT_DRILL_DOWN = 20;` (the sample cap to reuse)
- Line 60: `const MAX_SCAN_KEYS = 200;` (existing budget guard for the prune block; not used by Phase 33 — no SCAN needed)
- Line 201-323: aggregator handler — Bearer-gated by middleware before this line
- Line 282-316: existing Phase 32 prune block — the exact template for Phase 33 actorQuality block (try/catch + lazy compute + sample cap)
- Line 307-316: prune block construction + final `res.json({ audit24h, byBearer, advEval, prune })` at line 318

From server/lib/actorClassifier.ts (Plan 33-01 output):
- export type ActorIssue = 'ok' | 'null' | 'raw-cameo' | 'ambiguous'
- export function classifyEventActors(actors: readonly string[] | null | undefined, cameoCodebook: Set<string>): ActorIssue[]

From server/lib/llmExtractionPipeline.ts:
- LLM_EVENTS_KEY_ACTIVE = 'events:llm:v3' (export)

From server/__tests__/routes/operator-status.test.ts (existing per PATTERNS):
- mockRedis fixture pattern
- mockCacheGetSafe pattern
- vi.mock('../../cache/redis.js', ...) registration
- makeApp() + supertest pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Extend operator-status.test.ts with actorQuality cases (RED)</name>
  <files>server/__tests__/routes/operator-status.test.ts</files>
  <read_first>
    - server/__tests__/routes/operator-status.test.ts (existing — verify mock infrastructure + makeApp pattern)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 8 lines 1407-1466 — operator-status test extension)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-16 shape verbatim)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (§Validation Architecture Wave 3 ACTOR-05 rows; T-33-05 + T-33-05b)
  </read_first>
  <behavior>
    - Test 1: `/api/operator-status` returns 200 with `body.actorQuality` defined when cache has events; shape matches D-16 (totalEvents, nullActors, rawCameoActors, ambiguousActors, lowConfidenceActors, sample[])
    - Test 2: When cache has events with various actors fields covering the 4 buckets, counts increment correctly (e.g., 2 events with `actors: []` → nullActors === 2)
    - Test 3: `sample[]` capped at 20 (LIMIT_DRILL_DOWN) — feed 30 issue-events, assert sample.length === 20 AND each sample entry has `eventId`, `actors`, `actorConfidence`, `issue` fields
    - Test 4: Degrade-open — `cacheGetSafe.mockRejectedValue(...)` OR `mockResolvedValue(null)` → response status === 200 AND `body.actorQuality === null` (degrade-open, T-33-05b)
    - Test 5 (optional): low-confidence detection — event with `actorConfidence: ['low']` increments lowConfidenceActors counter
  </behavior>
  <action>
    Open `server/__tests__/routes/operator-status.test.ts`. Read the existing mock infrastructure carefully — note the exact mock paths for `cache/redis.js`, the `makeApp` helper, and the existing prune-block test patterns.

    Append a NEW top-level describe block:
    `describe('/api/operator-status — Phase 33 actorQuality block (D-16)')`

    Inside, add 4-5 it() cases per behavior list above. Mirror 33-PATTERNS.md Layer 8 (lines 1429-1466) test patterns:

    ```
    beforeEach(() => {
      vi.clearAllMocks();
      // existing auth bypass / NODE_ENV setup (mirror existing tests)
    });

    it('returns actorQuality block with computed counts', async () => {
      // mock cacheGetSafe to return ConflictEventEntity[] payload with mixed buckets
      mockCacheGetSafe.mockResolvedValue({ data: [
        { id: 'llm-v3-evt-1', data: { actors: [], actorConfidence: [] }, label: '...' },
        { id: 'llm-v3-evt-2', data: { actors: ['ISRMIL'], actorConfidence: ['low'] }, label: '...' },
        { id: 'llm-v3-evt-3', data: { actors: ['forces'], actorConfidence: ['low'] }, label: '...' },
        { id: 'llm-v3-evt-4', data: { actors: ['Israeli Defense Forces'], actorConfidence: ['high'] }, label: '...' },
      ], fetchedAt: Date.now() });
      // existing mocks for other blocks (audit-log, prune, etc) — set to safe defaults
      const res = await request(makeApp()).get('/api/operator-status').set('Authorization', 'Bearer ...');
      expect(res.status).toBe(200);
      expect(res.body.actorQuality).toBeDefined();
      expect(res.body.actorQuality).not.toBeNull();
      expect(res.body.actorQuality.totalEvents).toBe(4);
      expect(res.body.actorQuality.nullActors).toBeGreaterThanOrEqual(1);
      expect(res.body.actorQuality.rawCameoActors).toBeGreaterThanOrEqual(1);
      expect(res.body.actorQuality.ambiguousActors).toBeGreaterThanOrEqual(1);
      expect(res.body.actorQuality.lowConfidenceActors).toBeGreaterThanOrEqual(2);
      expect(Array.isArray(res.body.actorQuality.sample)).toBe(true);
    });

    it('sample cap = LIMIT_DRILL_DOWN (20)', async () => {
      const entities = Array.from({ length: 30 }, (_, i) => ({
        id: `llm-v3-evt-${i}`, data: { actors: [], actorConfidence: [] }, label: 'x',
      }));
      mockCacheGetSafe.mockResolvedValue({ data: entities, fetchedAt: Date.now() });
      const res = await request(makeApp()).get('/api/operator-status').set('Authorization', 'Bearer ...');
      expect(res.status).toBe(200);
      expect(res.body.actorQuality.sample.length).toBe(20);
    });

    it('degrade-open: cacheGetSafe throws → actorQuality === null, route 200', async () => {
      mockCacheGetSafe.mockRejectedValue(new Error('redis down'));
      const res = await request(makeApp()).get('/api/operator-status').set('Authorization', 'Bearer ...');
      expect(res.status).toBe(200);
      expect(res.body.actorQuality).toBeNull();
    });

    it('sample entries carry eventId, actors, actorConfidence, issue', async () => {
      mockCacheGetSafe.mockResolvedValue({ data: [
        { id: 'llm-v3-bad', data: { actors: ['forces'], actorConfidence: ['low'] }, label: 'x' },
      ], fetchedAt: Date.now() });
      const res = await request(makeApp()).get('/api/operator-status').set('Authorization', 'Bearer ...');
      expect(res.status).toBe(200);
      expect(res.body.actorQuality.sample.length).toBeGreaterThanOrEqual(1);
      const s = res.body.actorQuality.sample[0];
      expect(s).toHaveProperty('eventId');
      expect(s).toHaveProperty('actors');
      expect(s).toHaveProperty('actorConfidence');
      expect(s).toHaveProperty('issue');
      expect(['null', 'raw-cameo', 'ambiguous', 'low-confidence']).toContain(s.issue);
    });
    ```

    Adapt the Authorization header / NODE_ENV bypass exactly to whatever pattern the existing operator-status.test.ts file uses (read the file carefully — do not invent a new bypass).

    The mockCacheGetSafe key matcher needs to match `LLM_EVENTS_KEY_ACTIVE` (= 'events:llm:v3'). If the existing test's mock is keyed by URL or matches all reads, no per-test conditional needed. If it differentiates, set the resolved value conditionally (e.g., `mockCacheGetSafe.mockImplementation(async (key) => key === 'events:llm:v3' ? mockData : null)`).

    All 4-5 tests RED at this stage — the actorQuality block doesn't exist yet.
  </action>
  <verify>
    <automated>grep -c "actorQuality" server/__tests__/routes/operator-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/__tests__/routes/operator-status.test.ts` modified (existing file extended)
    - New describe block: `grep -Ec "describe\('/api/operator-status.*Phase 33.*actorQuality" server/__tests__/routes/operator-status.test.ts` returns ≥ 1 (allow phrasing variance)
    - `grep -c "actorQuality" server/__tests__/routes/operator-status.test.ts` returns ≥ 8 (interface refs + 4-5 it cases)
    - Degrade-open case present: `grep -Ec "mockRejectedValue|degrade-open" server/__tests__/routes/operator-status.test.ts` returns ≥ 1
    - Sample-cap case present: `grep -Ec "sample.length.*20|LIMIT_DRILL_DOWN" server/__tests__/routes/operator-status.test.ts` returns ≥ 1
    - `npx vitest run server/__tests__/routes/operator-status.test.ts` exits non-zero (RED — actorQuality block not yet implemented)
    - No `it.skip` / `it.todo` in the new block
  </acceptance_criteria>
  <done>RED integration tests committed; Task 2 implementation makes them GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement actorQuality block in operator-status.ts (D-16)</name>
  <files>server/routes/operator-status.ts</files>
  <read_first>
    - server/routes/operator-status.ts (entire file — verify line 35 logger, line 50 LIMIT_DRILL_DOWN, line 282-316 prune block, line 318 res.json)
    - server/lib/actorClassifier.ts (Plan 33-01 — classifyEventActors signature)
    - server/lib/llmExtractionPipeline.ts (verify LLM_EVENTS_KEY_ACTIVE export)
    - server/cache/redis.ts (verify cacheGetSafe signature + behavior)
    - server/types.ts (verify ConflictEventEntity shape includes data.actors and data.actorConfidence)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 6 §"operator-status.ts" lines 879-993)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-16 shape verbatim)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (File-Level Plan row 11; T-33-05 + T-33-05b)
    - server/__tests__/routes/operator-status.test.ts (Task 1 — drives GREEN)
  </read_first>
  <behavior>
    - Imports added: `cacheGetSafe` from cache/redis.js (if not already imported); `LLM_EVENTS_KEY_ACTIVE` from llmExtractionPipeline.js; `classifyEventActors` from lib/actorClassifier.js; `ConflictEventEntity` type
    - Inline `const INLINE_CAMEO_CODES: Set<string>` carrying ~15-25 well-known country-military + class codes (subset of Plan 33-02's cameo-codes.json — PATTERNS critical risk #3): ISRMIL, IRNMIL, USAMIL, USMIL, RUSMIL, SAUMIL, TURMIL, SYRMIL, IRQ, ISR, IRN, USA, REB, INS, MIL (+ any others the audit findings frequently surface)
    - Lazy `actorQuality` computation inserted AFTER prune block construction (after line 316) and BEFORE `res.json(...)` (line 318)
    - try/catch wraps entire block; on failure → log.warn + `actorQuality = null`
    - Iteration: ONE `cacheGetSafe<ConflictEventEntity[]>(LLM_EVENTS_KEY_ACTIVE, 999_999_999)` read; iterate `entities`; for each: classify actors via `classifyEventActors(actors, INLINE_CAMEO_CODES)`; track 4 bucket counters; build sample[] respecting LIMIT_DRILL_DOWN cap; assign `firstIssue` per priority order (null > raw-cameo > ambiguous > low-confidence)
    - `actorQuality` declared as `let actorQuality: ActorQualityShape | null = null` outside the try block (so the catch can leave it null)
    - Response: `res.json({ audit24h, byBearer, advEval, prune, actorQuality })` — actorQuality appended to existing keys
  </behavior>
  <action>
    Open `server/routes/operator-status.ts`. Per 33-PATTERNS.md Layer 6 (lines 905-976):

    Add imports (top of file, alongside existing):
    ```
    import { cacheGetSafe } from '../cache/redis.js'; // if not already imported
    import { LLM_EVENTS_KEY_ACTIVE } from '../lib/llmExtractionPipeline.js';
    import { classifyEventActors } from '../lib/actorClassifier.js';
    import type { ConflictEventEntity } from '../types.js'; // if not already imported
    ```

    Add a module-scope const for the inline CAMEO subset (PATTERNS critical risk #3):
    ```
    /** Phase 33 D-16 — inline CAMEO actor codebook subset (Pattern critical risk #3). */
    /**  The full codebook lives at .planning/phases/33-*/cameo-codes.json (Plan 33-02), */
    /**  which is NOT bundled into the Vercel server build artifact. This subset covers */
    /**  the country-military + class codes most likely to appear as raw CAMEO actors */
    /**  in events:llm:v3. Drift between this subset and the committed codebook is */
    /**  acceptable per D-16 (counts are observability, not enforcement). */
    const INLINE_CAMEO_CODES: Set<string> = new Set([
      // Country-military
      'ISRMIL', 'IRNMIL', 'USAMIL', 'USMIL', 'RUSMIL', 'SAUMIL', 'TURMIL', 'SYRMIL', 'LBNMIL',
      // Country (generic)
      'ISR', 'IRN', 'USA', 'RUS', 'SAU', 'TUR', 'SYR', 'LBN', 'PSE', 'YEM', 'IRQ',
      // Class
      'REB', 'INS', 'MIL', 'GOV',
    ]);
    ```

    Define the response shape inline OR via an interface near the file top:
    ```
    interface ActorQualityBlock {
      totalEvents: number;
      nullActors: number;
      rawCameoActors: number;
      ambiguousActors: number;
      lowConfidenceActors: number;
      sample: Array<{
        eventId: string;
        actors: string[];
        actorConfidence: ('high' | 'medium' | 'low')[];
        issue: 'null' | 'raw-cameo' | 'ambiguous' | 'low-confidence';
      }>;
    }
    ```

    Locate the aggregator handler around line 201-323. Insert the actorQuality block AFTER the prune block construction (after line 316) and BEFORE `res.json(...)` (line 318):

    ```
    // === PHASE 33 D-16 — actorQuality block ===
    // Lazy compute over events:llm:v3 (already-deserialized payload). No new Redis
    // sidecar — matches Phase 32 D-13 smallest-blast-radius. Degrade-open on any
    // failure (T-33-05b): log.warn, leave actorQuality null, route stays 200.
    let actorQuality: ActorQualityBlock | null = null;
    try {
      const cached = await cacheGetSafe<ConflictEventEntity[]>(LLM_EVENTS_KEY_ACTIVE, 999_999_999);
      const entities = cached?.data ?? [];

      let nullActors = 0;
      let rawCameoActors = 0;
      let ambiguousActors = 0;
      let lowConfidenceActors = 0;
      const sample: ActorQualityBlock['sample'] = [];

      for (const entity of entities) {
        const actors = (entity.data as { actors?: string[] }).actors ?? [];
        const actorConfidence = (entity.data as { actorConfidence?: ('high'|'medium'|'low')[] }).actorConfidence
          ?? actors.map(() => 'low' as const);
        const issues = classifyEventActors(actors, INLINE_CAMEO_CODES);

        let firstIssue: ActorQualityBlock['sample'][number]['issue'] | null = null;
        if (issues.includes('null')) { nullActors++; firstIssue = 'null'; }
        if (issues.includes('raw-cameo')) { rawCameoActors++; firstIssue = firstIssue ?? 'raw-cameo'; }
        if (issues.includes('ambiguous')) { ambiguousActors++; firstIssue = firstIssue ?? 'ambiguous'; }
        if (actorConfidence.includes('low')) { lowConfidenceActors++; firstIssue = firstIssue ?? 'low-confidence'; }

        if (firstIssue && sample.length < LIMIT_DRILL_DOWN) {
          sample.push({ eventId: entity.id, actors, actorConfidence, issue: firstIssue });
        }
      }

      actorQuality = {
        totalEvents: entities.length,
        nullActors, rawCameoActors, ambiguousActors, lowConfidenceActors,
        sample,
      };
    } catch (err) {
      log.warn({ err }, 'failed to compute actorQuality block');
      // actorQuality stays null
    }
    ```

    Replace the existing `res.json({ audit24h, byBearer, advEval, prune })` (line 318) with:
    `res.json({ audit24h, byBearer, advEval, prune, actorQuality });`

    Atomic-commit boundary: D-16 = single commit per RESEARCH Wave 3 grouping. ALL three changes (imports + INLINE_CAMEO_CODES + block + res.json extend) ship as ONE commit.

    Budget guard: ZERO new Redis SCANs. ONE cacheGetSafe call (which the existing prune block may already perform — verify; if it does, reuse the already-loaded data rather than re-reading). Per 33-RESEARCH.md Wall-clock budget: O(N) classification where N ≤ ~300 events — negligible.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/routes/operator-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/routes/operator-status.ts` modified
    - Imports added (verify each): `grep -c "from '../lib/actorClassifier'" server/routes/operator-status.ts` returns ≥ 1; `grep -c "LLM_EVENTS_KEY_ACTIVE" server/routes/operator-status.ts` returns ≥ 1
    - INLINE_CAMEO_CODES present: `grep -c "INLINE_CAMEO_CODES" server/routes/operator-status.ts` returns ≥ 1
    - INLINE_CAMEO_CODES carries at least ISRMIL + IRNMIL + USMIL/USAMIL: `grep -Ec "'ISRMIL'|'IRNMIL'" server/routes/operator-status.ts` returns ≥ 2
    - actorQuality computation present: `grep -c "actorQuality" server/routes/operator-status.ts` returns ≥ 4 (interface + declaration + assignment + res.json)
    - classifyEventActors invoked: `grep -c "classifyEventActors" server/routes/operator-status.ts` returns ≥ 1
    - try/catch wraps the block: `awk '/PHASE 33 D-16/,/res\.json/' server/routes/operator-status.ts | grep -c "try\|catch" | head -1` returns ≥ 2
    - LIMIT_DRILL_DOWN cap respected: `awk '/PHASE 33 D-16/,/res\.json/' server/routes/operator-status.ts | grep -c "LIMIT_DRILL_DOWN" | head -1` returns ≥ 1
    - res.json extended with actorQuality: `grep -Ec "res\.json\(.*audit24h.*actorQuality" server/routes/operator-status.ts` returns ≥ 1
    - No new SCAN: `awk '/PHASE 33 D-16/,/res\.json/' server/routes/operator-status.ts | grep -c "scan\(" | head -1` returns 0 (Phase 33 block does NOT SCAN — only does one GET via cacheGetSafe)
    - All ACTOR-05 server-side tests pass: `npx vitest run server/__tests__/routes/operator-status.test.ts` exits 0
    - No new TS errors: `npx tsc --noEmit -p tsconfig.server.json 2>&amp;1 | grep -Ec "operator-status\.ts.*error TS"` returns 0
  </acceptance_criteria>
  <done>Operator-status carries actorQuality; degrade-open verified; ready for Plan 33-07 dashboard render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bearer-authenticated operator → `/api/operator-status` | Pre-existing dashboardAuth middleware; no change |
| `events:llm:v3` cache → in-route compute | Reads via cacheGetSafe (degrade-open) |
| INLINE_CAMEO_CODES const → classifier input | Static committed code; PR-reviewed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Information Disclosure | `actorQuality.sample[]` drill-down (eventId + actors[] + actorConfidence[]) | accept | Bearer-gated endpoint (existing); actors/actorConfidence fields are already exposed via /api/events JSON path (public). No new exposure surface. Sample cap = 20 prevents response-size DoS. |
| T-33-05b | Denial of Service / Availability | Redis flap or malformed payload | mitigate | try/catch wraps the entire block; cacheGetSafe is degrade-safe (returns null on Upstash errors per existing contract); response returns 200 with `actorQuality: null`. Test 3 in Task 1 enforces this behavior. Matches Phase 32 D-13 + the prune block's degrade-open pattern. |
| T-33-05c | Tampering | INLINE_CAMEO_CODES drift vs cameo-codes.json | accept | Subset is observability-only — counts in the dashboard, not enforcement. Drift is documented in the inline JSDoc. The committed cameo-codes.json (Plan 33-02) remains the canonical reference for the audit script. |
</threat_model>

<verification>
After Tasks 1-2 complete:
- `npx vitest run server/__tests__/routes/operator-status.test.ts` exits 0
- `npx tsc --noEmit -p tsconfig.server.json` introduces no new errors
- Manual UAT: after Plan 33-07 lands and a cron tick re-extracts, operator GET on `/api/operator-status` with Bearer returns body carrying `actorQuality.totalEvents > 0`
- Plan 33-07 can rely on the response shape verified here
</verification>

<success_criteria>
- ACTOR-05 server-side requirement addressed (D-16)
- D-16 shape verbatim per CONTEXT.md (6 fields including 20-cap sample[])
- Anti-pattern #17 preserved: no writes; no new poll loop
- Degrade-open verified (T-33-05b)
- PATTERNS critical risk #3 (CAMEO bundling) honored: inline subset, no runtime disk read
- Pitfall §1 dedup honored: classifier is the single source of truth shared with audit script
</success_criteria>

<output>
Create `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-06-SUMMARY.md` when done. Include:
- Final INLINE_CAMEO_CODES list (which codes shipped + count)
- Confirmation that ZERO new Redis SCANs were introduced
- Whether the actorQuality block reuses an existing cacheGetSafe call (in the prune block) or makes its own (depends on existing block structure)
- Degrade-open test status
- Decision-coverage trace: D-16 → response block + lazy compute + sample cap + degrade-open
</output>

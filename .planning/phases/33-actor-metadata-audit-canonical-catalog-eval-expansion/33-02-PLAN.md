---
phase: 33-actor-metadata-audit-canonical-catalog-eval-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/data/actor-catalog.ts
  - src/__tests__/lib/actorCatalog.test.ts
  - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json
autonomous: true
requirements: [ACTOR-02]
tags: [actor-metadata, catalog, static-data, contract-test]

must_haves:
  truths:
    - 'A canonical actor catalog exists at server/data/actor-catalog.ts (D-04) mapping aliases → canonical names for Iran-conflict-relevant actors (D-06)'
    - 'Catalog shape exposes CanonicalActor interface + ACTOR_CATALOG ReadonlyArray + ACTOR_LOOKUP ReadonlyMap + canonicalize() helper (D-05)'
    - 'Contract test asserts: no duplicate canonical names, no orphan CAMEO codes (every cameoCodes[] entry is in committed cameo-codes.json), every alias resolves to exactly one canonical, canonicalize() is case-insensitive, unknown alias returns null (D-07)'
    - 'canonicalize() ordering is longest-alias-wins (Discretion §3) — generic names cannot shadow specific canonicals'
    - "Faction integration: CanonicalActor.affiliation reuses src/lib/factions.ts 'us'|'iran'|'neutral' union (stored, not surfaced to dashboard this phase)"
  artifacts:
    - path: 'server/data/actor-catalog.ts'
      provides: 'Static catalog module — ACTOR_CATALOG array, ACTOR_LOOKUP Map, canonicalize() fn'
      contains: 'CanonicalActor, ACTOR_CATALOG, ACTOR_LOOKUP, canonicalize'
      min_lines: 80
    - path: 'src/__tests__/lib/actorCatalog.test.ts'
      provides: '5-invariant contract test (no-dup canonicals, no-orphan codes, alias→one canonical, case-insensitive, unknown→null)'
    - path: '.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json'
      provides: 'Committed CAMEO actor codebook snapshot (GDELT-CAMEO-2026-05) — referenced by contract test + audit script'
      contains: 'version, source, actorCodes'
  key_links:
    - from: 'server/data/actor-catalog.ts'
      to: 'src/lib/factions.ts'
      via: 'import type { Faction }'
      pattern: "import type \\{ Faction \\} from"
    - from: 'src/__tests__/lib/actorCatalog.test.ts'
      to: '.planning/phases/33-*/cameo-codes.json'
      via: 'import codebook (JSON resolver) OR readFileSync fallback'
      pattern: "cameo-codes\\.json"
---

<objective>
Ship the canonical actor catalog + CAMEO codebook + contract test, fulfilling ACTOR-02. Single-source-of-truth static data module mirroring `factions.ts` / `ethnicGroups.ts`.

Purpose: Server-side post-mapping (Plan 33-04 D-08) walks every extracted `actors[]` entry through this catalog. The contract test prevents silent catalog poisoning (per Threat T-33-02) by enforcing no-duplicate / no-orphan invariants at build time.
Output: A typed catalog module, a committed CAMEO snapshot, and a Vitest contract test covering the 5 invariants from D-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md
@src/lib/factions.ts
@src/lib/ethnicGroups.ts
@src/__tests__/lib/colorBridge.test.ts
@CLAUDE.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from analog files. -->

From src/lib/factions.ts (line 5):

- export type Faction = 'us' | 'iran' | 'neutral';
- Plus the Record-based FACTION_ASSIGNMENTS pattern (lines 11-24)
- Plus getFaction(isoA3) helper with ?? 'neutral' fallback (lines 47-49)

From src/lib/ethnicGroups.ts (lines 46-66, 149):

- export interface EthnicGroupConfig { id, label, ... }
- export const ETHNIC_GROUPS: Record<EthnicGroup, EthnicGroupConfig> = {...}
- export const ETHNIC_GROUP_IDS: EthnicGroup[] = Object.keys(ETHNIC_GROUPS) as EthnicGroup[]

From src/**tests**/lib/colorBridge.test.ts (contract-test analog — byte-identity-sentinel + it.each table-driven pattern):

- describe(...) → describe(...) → it.each([table])('%s ...', (item) => expect(...))
- Strict referential equality `expect(a).toBe(b)` for join assertions

Cross-boundary import note: `server/data/actor-catalog.ts` imports `Faction` from `src/lib/factions.ts`. Verify `tsconfig.server.json` admits the import (33-PATTERNS.md risk #5). Fallback per PATTERNS.md: duplicate the 3-string union literal locally with `// keep in sync with src/lib/factions.ts` JSDoc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit CAMEO codebook snapshot (cameo-codes.json)</name>
  <files>.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json</files>
  <read_first>
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 3 §"cameo-codes.json" lines 507-530 — shape decision: JSON over txt per Discretion §2)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (Claude's Discretion §2 — source provenance)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-04..D-06 — actors to cover)
    - server/adapters/gdelt.ts (existing internal CAMEO event-code references — actor codes are net-new per RESEARCH Discretion §2)
  </read_first>
  <action>
    Create `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json` per 33-PATTERNS.md Layer 3 shape (lines 513-526). Schema:
    `{ "version": "GDELT-CAMEO-2026-05", "source": "https://www.gdeltproject.org/data/lookups/CAMEO.actorcodes.txt (snapshot 2026-05-21)", "actorCodes": [{code, label, type}, ...], "eventCodes": [] }`

    Seed `actorCodes` with the country-military and country-government codes needed for D-06 actors. Required codes (minimum set covering Plan 02's catalog seed):
    - **Country-military:** `ISRMIL` (Israeli Defense Forces), `IRNMIL` (Iran Military / Armed Forces), `USAMIL`/`USMIL` (US Armed Forces — use the GDELT canonical `USAMIL` if listed there; mirror in entry; document the alias choice), `RUSMIL` (Russian Aerospace Forces), `SAUMIL` (Royal Saudi Air Force), `TURMIL` (Turkish Armed Forces), `SYRMIL` (Syrian Arab Army)
    - **Country-government / generic country:** `ISR`, `IRN`, `USA`, `RUS`, `SAU`, `TUR`, `SYR`, `LBN`, `PSE`, `YEM`, `IRQ` (country-level actors that may appear)
    - **Non-state / militia (best-effort; CAMEO has REB/MIL/INS class codes):** Include placeholder generic codes per the GDELT codebook: `REB` (rebels — type "class"), `INS` (insurgents), `MIL` (militia/military generic) — these are the GDELT class codes from `server/adapters/gdelt.ts` references
    - **Sub-organizational where GDELT carries them:** `IRGCQF` (IRGC Quds Force — if GDELT carries it; otherwise omit and the catalog `cameoCodes[]` for IRGC sub-units stays empty for those entries to keep the orphan-check honest)

    For each entry: `{code: string, label: string, type: 'country-military' | 'country-government' | 'class' | 'organization'}`. If a D-06 actor has NO confirmed CAMEO code (e.g., a sub-unit GDELT does not name), the catalog entry's `cameoCodes` array stays empty for that entry — DO NOT invent codes (would break Pitfall §3 by introducing false codebook entries). Document this in JSON top-level `notes` field.

    Use 2-space indentation. The committed JSON is the SOURCE OF TRUTH for the contract test's orphan-check (Task 3). If the file shape mismatches between this Task and Task 3's test parser, the test breaks loudly — that is the intended drift-detection behavior.

  </action>
  <verify>
    <automated>node -e "const j=require('./.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json');if(j.version!=='GDELT-CAMEO-2026-05')process.exit(1);if(!Array.isArray(j.actorCodes))process.exit(2);if(j.actorCodes.length<10)process.exit(3);const codes=new Set(j.actorCodes.map(c=>c.code));['ISRMIL','IRNMIL','RUSMIL','SAUMIL','TURMIL','SYRMIL'].forEach(c=>{if(!codes.has(c)){console.error('missing',c);process.exit(4)}});console.log('OK',j.actorCodes.length,'codes')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json` exists and is valid JSON
    - Top-level keys present: `version`, `source`, `actorCodes` (`grep -c '"version"\|"source"\|"actorCodes"' .planning/phases/33-*/cameo-codes.json` returns ≥ 3)
    - `actorCodes` is a non-empty array with at least 10 entries
    - Required military codes present: ISRMIL, IRNMIL, RUSMIL, SAUMIL, TURMIL, SYRMIL (Task 1 verify command enforces)
    - Each entry has `code`, `label`, `type` fields (`node -e "const j=require('./.planning/phases/33-*/cameo-codes.json');j.actorCodes.forEach(c=>{if(!c.code||!c.label||!c.type){console.error('bad',c);process.exit(1)}})"` exits 0)
    - File parses with `node -e "JSON.parse(require('node:fs').readFileSync('.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json','utf-8'))"` (no exit code != 0)
  </acceptance_criteria>
  <done>CAMEO snapshot committed; Plan 33-01's audit script can now read it; Task 3's contract test references it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement actor-catalog.ts (D-04, D-05, D-06)</name>
  <files>server/data/actor-catalog.ts</files>
  <read_first>
    - src/lib/factions.ts (primary analog — Faction type at line 5; Record + getter pattern)
    - src/lib/ethnicGroups.ts (secondary analog — interface + readonly const + lookup pattern, lines 46-66, 149)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 1 §"actor-catalog.ts" lines 44-160 — composite shape from both analogs)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-04, D-05 shape verbatim; D-06 seed list)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (Discretion §3 — longest-alias-wins ordering)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json (Task 1 output — only use codes confirmed in this file)
    - server/tsconfig.server.json (verify cross-boundary import from src/ permitted — see PATTERNS.md risk #5)
  </read_first>
  <behavior>
    - `interface CanonicalActor { canonicalName: string; aliases: string[]; cameoCodes: string[]; affiliation: Faction; }` exported
    - `ACTOR_CATALOG: ReadonlyArray<CanonicalActor>` with ~30-50 entries per D-06 seed list (final list refined post-audit; baseline ships now)
    - `ACTOR_LOOKUP: ReadonlyMap<string, CanonicalActor>` built at module load; keys are lowercased; both canonicalName and aliases are indexed
    - Longest-alias-wins ordering: build the Map by inserting shorter aliases FIRST so longer ones overwrite (or sort the aliases ascending by length and assert no semantic collision at build time)
    - `canonicalize(name: string): CanonicalActor | null` — case-insensitive (trim + toLowerCase); returns null on unknown
    - All cameoCodes[] entries cross-reference exclusively codes present in committed `.planning/phases/33-*/cameo-codes.json` (Task 1)
  </behavior>
  <action>
    Create `server/data/actor-catalog.ts` per 33-PATTERNS.md Layer 1 composite shape (lines 119-145).

    Imports (top of file): `import type { Faction } from '../../src/lib/factions.js'` (note `.js` ESM extension). Per PATTERNS.md risk #5, FIRST verify the import compiles against `tsconfig.server.json`. If it fails compilation:
      Fallback: declare `type Faction = 'us' | 'iran' | 'neutral'` locally with a JSDoc comment: `/** Keep in sync with src/lib/factions.ts:5 Faction union. Cross-boundary import blocked by tsconfig.server.json — verify on every drift. */` AND add a separate sentinel test in `server/__tests__/data/actor-catalog.faction-sync.test.ts` (NEW, small) asserting the local literal equals the imported one by reading both files via readFileSync + regex extract. (If the import compiles directly, this fallback is unused — preferred path.)

    JSDoc top-of-file: explain Phase 33 D-04..D-06 purpose, cross-references PATTERNS.md analogs (factions.ts + ethnicGroups.ts), cron-only-writer invariant unaffected (static data), and Pitfall §3 (cameo-codes.json drift surfaces via contract test).

    Body:
    - `export interface CanonicalActor { canonicalName: string; aliases: string[]; cameoCodes: string[]; affiliation: Faction; }`
    - `export const ACTOR_CATALOG: ReadonlyArray<CanonicalActor> = [ ... ]` with the D-06 baseline seed list. Each entry:
      - canonicalName: full canonical (e.g. 'Islamic Revolutionary Guard Corps')
      - aliases: lowercase, multi-word and abbreviation variants (e.g. ['irgc', 'islamic revolutionary guard corps', 'iranian revolutionary guard corps', 'sepah'])
      - cameoCodes: from cameo-codes.json (Task 1) OR empty array if no confirmed code (DO NOT invent codes)
      - affiliation: 'us' | 'iran' | 'neutral' (sub-faction modeling deferred per D-06 / Deferred Ideas)
    - Required entries (minimum to pass ACTOR-02 / D-06): IDF, IRGC, IRGC Quds Force, IRGC Aerospace Force, Iranian Armed Forces, US Armed Forces, USCENTCOM, US Navy, US Air Force, US Army, Hezbollah, Houthis (alias 'ansar allah'), Hamas, Palestinian Islamic Jihad, Kataib Hezbollah, Asaib Ahl al-Haq, Harakat Hezbollah al-Nujaba, Popular Mobilization Forces, Syrian Arab Army, Russian Aerospace Forces, Royal Saudi Air Force, Saudi-led Coalition, Turkish Armed Forces, SDF, Peshmerga, PKK
    - `export const ACTOR_LOOKUP: ReadonlyMap<string, CanonicalActor> = (() => { ... })()` IIFE that builds the Map applying longest-alias-wins:
      - Flatten all `(canonicalName, alias[])` pairs into `{key: lowercase, actor}` tuples
      - Sort ascending by `key.length` so shorter inserted first → longer Map.set() overwrites
      - Build the Map and return it
    - `export function canonicalize(name: string): CanonicalActor | null { return ACTOR_LOOKUP.get(name.trim().toLowerCase()) ?? null; }`
    - Optional but recommended: `export const ACTOR_CANONICAL_NAMES: string[] = ACTOR_CATALOG.map(a => a.canonicalName)` (derived export following ethnicGroups.ts:149 pattern, useful for iteration consumers)

    No logger import (static-data module — no I/O). No Redis import. No env access.

  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.server.json 2>&amp;1 | grep -Ec "server/data/actor-catalog\.ts.*error TS"</automated>
  </verify>
  <acceptance_criteria>
    - File `server/data/actor-catalog.ts` exists
    - `grep -c "export interface CanonicalActor" server/data/actor-catalog.ts` returns 1
    - `grep -c "export const ACTOR_CATALOG" server/data/actor-catalog.ts` returns 1
    - `grep -c "export const ACTOR_LOOKUP" server/data/actor-catalog.ts` returns 1
    - `grep -c "export function canonicalize" server/data/actor-catalog.ts` returns 1
    - ≥ 25 catalog entries (D-06 baseline minimum): `grep -c "canonicalName:" server/data/actor-catalog.ts` returns ≥ 25
    - Required canonical names present (at minimum): `grep -Ec "'Israeli Defense Forces'|'Islamic Revolutionary Guard Corps'|'Hezbollah'|'Hamas'|'Houthis'" server/data/actor-catalog.ts` returns ≥ 5
    - Faction union referenced (either imported or locally declared with sentinel sync): `grep -Ec "Faction\b.*=.*'us'.*'iran'.*'neutral'|import type \{ Faction \}" server/data/actor-catalog.ts` returns ≥ 1
    - Longest-alias-wins evidence present: `grep -Ec "length|sort" server/data/actor-catalog.ts` returns ≥ 1 (sort by length in IIFE)
    - No inline hex/RGBA (D-13 colorBridge contract enforced on src/lib/, but mirror discipline): `grep -Ec "#[0-9a-fA-F]{6}|rgba?\(" server/data/actor-catalog.ts` returns 0
    - No logger import: `grep -c "from '.*logger'" server/data/actor-catalog.ts` returns 0
    - No redis import: `grep -c "from '.*redis'" server/data/actor-catalog.ts` returns 0
    - `npx tsc --noEmit -p tsconfig.server.json` does not introduce new errors specific to actor-catalog.ts
  </acceptance_criteria>
  <done>Static catalog module ships; ready for Plan 33-04 server-side post-mapping (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement actorCatalog.test.ts contract test (D-07)</name>
  <files>src/__tests__/lib/actorCatalog.test.ts</files>
  <read_first>
    - server/data/actor-catalog.ts (Task 2 output)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json (Task 1 output)
    - src/__tests__/lib/colorBridge.test.ts (analog — byte-identity-sentinel structure + it.each table pattern)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 1 §"actorCatalog.test.ts" lines 163-243)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-07 — five invariants verbatim)
  </read_first>
  <behavior>
    - 5 invariants from D-07 each have their own `describe` block + ≥ 1 `it` case
    - (a) No duplicate canonical names — `new Set(names).size === names.length`
    - (b) No orphan CAMEO codes — `it.each(allCodes)` against codebook Set
    - (c) Every alias resolves to exactly one CanonicalActor — strict referential equality on round-trip
    - (d) canonicalize() is case-insensitive — 'idf', 'IDF', 'Idf' all resolve to same entry
    - (e) canonicalize('UNKNOWN_ALIAS_xxx') returns null
    - All assertions pass GREEN against the Task 2 catalog
  </behavior>
  <action>
    Create `src/__tests__/lib/actorCatalog.test.ts` per 33-PATTERNS.md Layer 1 contract-test template (lines 181-228).

    Imports:
    `import { describe, it, expect } from 'vitest'`
    `import { ACTOR_CATALOG, ACTOR_LOOKUP, canonicalize } from '@/../server/data/actor-catalog'` (verify path alias resolves; if not, use relative `../../../server/data/actor-catalog`)

    CAMEO codebook import — PATTERNS.md notes JSON imports under `.planning/` may not resolve via tsconfig path-alias. Use the `readFileSync` fallback per PATTERNS.md note:
    `import { readFileSync } from 'node:fs'`
    `import { resolve } from 'node:path'`
    `const codebookPath = resolve(__dirname, '../../../.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json')`
    `const codebook = JSON.parse(readFileSync(codebookPath, 'utf-8')) as { actorCodes: Array<{code: string}> }`
    (Use `import.meta.url`-based dirname if jsdom env requires it — mirror existing tests' approach.)

    Five describe blocks (one per D-07 invariant):

    1. `describe('Phase 33 actor catalog — no duplicate canonical names (D-07 a)')` with `it('every CanonicalActor.canonicalName is unique')` asserting `new Set(ACTOR_CATALOG.map(a => a.canonicalName)).size === ACTOR_CATALOG.length`

    2. `describe('Phase 33 actor catalog — no orphan CAMEO codes (D-07 b)')` with `const allCodes = ACTOR_CATALOG.flatMap(a => a.cameoCodes); const knownCodes = new Set(codebook.actorCodes.map(c => c.code));` then `it.each(allCodes)('CAMEO code %s is in committed codebook', (code) => expect(knownCodes.has(code)).toBe(true))`. If `allCodes.length === 0` because the seed list intentionally has empty cameoCodes for sub-units, add an explicit `it('catalog has at least N cameoCodes overall')` guard so the it.each does not silently pass on an empty input.

    3. `describe('Phase 33 actor catalog — every alias resolves to one canonical (D-07 c)')` with `it('every alias resolves to exactly one CanonicalActor')` iterating `ACTOR_CATALOG`, then for each `entry` and each `alias`, `expect(canonicalize(alias)).toBe(entry)` (strict referential equality)

    4. `describe('Phase 33 actor catalog — canonicalize is case-insensitive (D-07 d)')` with `it('idf, IDF, Idf all resolve to the same entry')` asserting `canonicalize('idf')` !== null AND `canonicalize('IDF') === canonicalize('idf')` AND `canonicalize('Idf') === canonicalize('idf')`. Also a parameterized check on another known alias (e.g. 'irgc').

    5. `describe('Phase 33 actor catalog — unknown alias returns null (D-07 e)')` with `it('canonicalize("UNKNOWN_ALIAS_xxx") returns null')` and a second `it('canonicalize("") returns null after trim')` as a robustness check.

    Bonus assertion (build-time alias-collision guard — D-07 c reinforces): a top-level `describe('Phase 33 actor catalog — ACTOR_LOOKUP is well-formed')` with `it('every key in ACTOR_LOOKUP corresponds to a catalog entry')` iterating `ACTOR_LOOKUP.entries()` and asserting `ACTOR_CATALOG.includes(actor)`.

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/actorCatalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/lib/actorCatalog.test.ts` exists
    - Five top-level describe blocks covering invariants D-07 (a) through (e): `grep -Ec "describe\('Phase 33 actor catalog" src/__tests__/lib/actorCatalog.test.ts` returns ≥ 5
    - `grep -c "it.each(" src/__tests__/lib/actorCatalog.test.ts` returns ≥ 1 (table-driven CAMEO orphan check)
    - `grep -c "canonicalize(" src/__tests__/lib/actorCatalog.test.ts` returns ≥ 4 (used in invariants c/d/e)
    - `grep -c "new Set" src/__tests__/lib/actorCatalog.test.ts` returns ≥ 1 (duplicate detection)
    - References codebook JSON: `grep -c "cameo-codes.json" src/__tests__/lib/actorCatalog.test.ts` returns ≥ 1
    - `npx vitest run src/__tests__/lib/actorCatalog.test.ts` exits 0 — all 5 invariants pass GREEN
    - No `it.skip` / `it.todo`: `grep -Ec "it\.(skip|todo)" src/__tests__/lib/actorCatalog.test.ts` returns 0
  </acceptance_criteria>
  <done>Catalog contract test ships GREEN; semantic poisoning surfaces loudly on PR review (Threat T-33-02 mitigation).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                           |
| ------------------------------ | --------------------------------------------------------------------- |
| catalog code → PR review       | Catalog mappings are reviewer-vetted at PR time                       |
| CAMEO codebook → contract test | Drift between catalog and codebook surfaces loudly on next vitest run |

## STRIDE Threat Register

| Threat ID | Category  | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                           |
| --------- | --------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-33-02   | Tampering | `server/data/actor-catalog.ts` | mitigate    | Contract test (D-07) catches structural drift (duplicates, orphans, alias collisions). PR-level review covers semantic poisoning (e.g., aliasing 'IDF' → 'ISIS' would pass contract but fail review). Catalog file lives under `server/data/` — non-user-mutable surface. |
| T-33-02b  | Tampering | cameo-codes.json snapshot      | accept      | Static reference; PR-time review. Upstream GDELT drift breaks contract test on next mismatch — that is the intended loud-failure mode (Pitfall §3).                                                                                                                       |

</threat_model>

<verification>
After Tasks 1-3 complete:
- `npx vitest run src/__tests__/lib/actorCatalog.test.ts` exits 0
- `npx tsc --noEmit -p tsconfig.server.json` introduces no new errors
- `npx tsc --noEmit -p tsconfig.json` (or equivalent client tsc) introduces no new errors
- ACTOR_CATALOG has ≥ 25 entries covering the D-06 baseline list
- cameo-codes.json carries ≥ 10 actor codes including required ISRMIL/IRNMIL/RUSMIL/SAUMIL/TURMIL/SYRMIL
</verification>

<success_criteria>

- ACTOR-02 requirement materially addressed: catalog ships + contract test ships
- D-04 path verbatim: `server/data/actor-catalog.ts`
- D-05 shape verbatim: CanonicalActor interface with canonicalName/aliases/cameoCodes/affiliation
- D-06 baseline coverage: ≥ 25 entries with Iran-conflict-relevant actors
- D-07 five invariants enforced by contract test
- Discretion §3 (longest-alias-wins) implemented in ACTOR_LOOKUP build
- Discretion §6 (affiliation reserved for future) honored — affiliation is stored but not exported as a surfaced field
  </success_criteria>

<output>
Create `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-02-SUMMARY.md` when done. Include:
- ACTOR_CATALOG final entry count + any audit-driven refinements deferred to phase close
- CAMEO codebook size (number of actorCodes entries)
- Cross-boundary Faction import path taken (direct import vs local-duplicate-with-sentinel)
- Contract test pass status
- Decision-coverage trace: D-04 → path; D-05 → interface; D-06 → seed list; D-07 → contract test
</output>

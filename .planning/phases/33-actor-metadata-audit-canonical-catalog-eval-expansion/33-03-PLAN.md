---
phase: 33-actor-metadata-audit-canonical-catalog-eval-expansion
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - server/lib/llmSchema.ts
  - server/__tests__/lib/llmSchema.test.ts
autonomous: true
requirements: [ACTOR-03]
tags: [actor-metadata, schema, zod, json-schema]

must_haves:
  truths:
    - 'enrichedEventV3 Zod schema accepts payloads WITH actorConfidence array AND WITHOUT it (rollout-window forward-compat per D-11 caveat + Open Q §1)'
    - "actorConfidence is z.array(z.enum(['high','medium','low'])).optional() — optional during rollout (Open Q §1 resolution; tighten in Phase 35+ cleanup)"
    - 'EVENT_EXTRACTION_SCHEMA_V3 JSON Schema literal is un-aliased from V2 and carries actorConfidence in properties + required arrays (D-12; Open Q §2 — required at wire, optional in Zod = defense-in-depth)'
    - "JSDoc on the new schema literal pins v3↔v2 divergence (PATTERNS critical risk #4) so future v2 edits don't silently fail to propagate"
  artifacts:
    - path: 'server/lib/llmSchema.ts'
      provides: 'enrichedEventV3 with actorConfidence + un-aliased EVENT_EXTRACTION_SCHEMA_V3'
      contains: 'actorConfidence'
    - path: 'server/__tests__/lib/llmSchema.test.ts'
      provides: 'Contract tests: accept WITH + WITHOUT actorConfidence, reject invalid enum, JSON Schema literal includes the new field'
  key_links:
    - from: 'server/lib/llmSchema.ts'
      to: 'enrichedEventAny discriminated union'
      via: '.extend({ actorConfidence: z.array(...).optional() })'
      pattern: "actorConfidence:.*\\.optional\\(\\)"
    - from: 'server/__tests__/lib/llmSchema.test.ts'
      to: 'EVENT_EXTRACTION_SCHEMA_V3'
      via: 'import + property/required array assertion'
      pattern: 'EVENT_EXTRACTION_SCHEMA_V3'
---

<objective>
Extend `enrichedEventV3` (Zod) with `actorConfidence` and un-alias `EVENT_EXTRACTION_SCHEMA_V3` (JSON Schema wire contract) with the same field — fulfilling D-10 + D-12 + Open Q §1 (`.optional()` for rollout) + Open Q §2 (required in wire) + Open Q §3 (un-aliasing JSDoc).

Purpose: Establish the schema-side contract that Plan 33-04 (extractor integration) writes against AND that downstream readers (`/api/events`, `enrichedEventAny` discriminated-union cache reads) admit without breaking legacy v3 entries during the 24h forward-rollout window.
Output: Two atomic edits to `server/lib/llmSchema.ts` (Zod extend + JSON Schema un-alias) + extended contract tests.

**PATTERNS critical risk #1 (D-10 `.optional()` for rollout):** This plan SHIPS the `.optional()` resolution — required-without-optional would reject every pre-Phase-33 v3 cache entry via `enrichedEventAny` (33-RESEARCH.md Open Q §1).

**PATTERNS critical risk #4 (JSDoc pinning v2/v3 divergence):** This plan SHIPS the JSDoc at the new V3 schema literal site — atomic commit on its own (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md
@.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md
@server/lib/llmSchema.ts
@server/__tests__/lib/llmSchema.test.ts
@CLAUDE.md

<interfaces>
<!-- Key lines from server/lib/llmSchema.ts the executor must know. -->

Current state (verified via grep):

- Line 70 + 143: `actors: z.array(z.string())` (two places — first is v2/v3 shared shape; second is in a nested object — verify with full grep)
- Line 164-170: JSDoc confirming `.strict().extend({})` preserves strict()
- Line 173-175: `export const enrichedEventV3 = enrichedEventV2.extend({ schemaVersion: z.literal('v3') });`
- Line 177: `export type EnrichedEventV3 = z.infer<typeof enrichedEventV3>;`
- Line 191-195: `enrichedEventAny` discriminated union (the cache-read surface for legacy entries)
- Line 263-347: `EVENT_EXTRACTION_SCHEMA_V2: Record<string, unknown>` JSON Schema literal
- Line 288-298: JSDoc comment block re: NIM relaxed JSON-Schema enforcement (precedent for Open Q §2)
- Line 310 + 335: `actors` listed in V2 schema properties + required arrays
- Line 359: `export const EVENT_EXTRACTION_SCHEMA_V3: Record<string, unknown> = EVENT_EXTRACTION_SCHEMA_V2` — CURRENT alias (must un-alias)

Strict() preservation behavior (verified by existing v2→v3 schemaVersion extension test in llmSchema.test.ts):

- Zod v3 .strict().extend(...) preserves the strict() invariant on the extended schema
- BUT strict() only rejects EXTRA fields — it does NOT make new fields "required automatically." A field declared without .optional() IS required by default
- Therefore: declaring `actorConfidence: z.array(...)` (no .optional()) would reject legacy v3 cache reads through enrichedEventAny — must use .optional() per Open Q §1
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend llmSchema.test.ts with Zod + JSON Schema acceptance tests (RED → driver for Task 2)</name>
  <files>server/__tests__/lib/llmSchema.test.ts</files>
  <read_first>
    - server/__tests__/lib/llmSchema.test.ts (existing — lines 1-80 schema-acceptance pattern)
    - server/lib/llmSchema.ts (current state — enrichedEventV3 at line 173-175; EVENT_EXTRACTION_SCHEMA_V3 at line 359)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 8 §"llmSchema.test.ts" lines 1471-1505)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-10, D-12)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (Open Q §1 — rollout window resolution)
  </read_first>
  <behavior>
    - Test 1: `enrichedEventV3.safeParse(validPayloadWithActorConfidence)` → `success: true` (`actorConfidence: ['high', 'medium']`)
    - Test 2: `enrichedEventV3.safeParse(validPayloadWithoutActorConfidence)` → `success: true` (optional rollout window — Open Q §1)
    - Test 3: `enrichedEventV3.safeParse({ ..., actorConfidence: ['certain'] })` → `success: false` (invalid enum rejected)
    - Test 4: `EVENT_EXTRACTION_SCHEMA_V3.properties.events.items.properties.actorConfidence` is defined with `items.enum === ['high','medium','low']` (D-12)
    - Test 5: `EVENT_EXTRACTION_SCHEMA_V3.properties.events.items.required` includes `'actorConfidence'` (Open Q §2 — required at wire)
    - Test 6: `EVENT_EXTRACTION_SCHEMA_V3 !== EVENT_EXTRACTION_SCHEMA_V2` (un-aliased; Open Q §3)
  </behavior>
  <action>
    Open `server/__tests__/lib/llmSchema.test.ts`. Locate the existing top-level `describe(...)` block(s) for v3 schema acceptance. Add two NEW `describe()` blocks at the end of the file:

    1. `describe('Phase 33 enrichedEventV3 — actorConfidence (D-10, Open Q §1 .optional() rollout)')` with:
       - `it('accepts payload WITH actorConfidence array of enum values')` — build a minimal valid v3 payload helper (or reuse one from the existing test file — read existing helpers first to avoid duplication), spread `actorConfidence: ['high', 'medium']` onto it, assert `enrichedEventV3.safeParse(payload).success === true`
       - `it('accepts payload WITHOUT actorConfidence (rollout-window forward-compat)')` — same valid payload sans actorConfidence, assert success === true. Comment in test body: `// Open Q §1: .optional() preserves legacy v3 cache reads through enrichedEventAny`
       - `it('rejects invalid actorConfidence enum value')` — actorConfidence: ['certain'], assert success === false

    2. `describe('Phase 33 EVENT_EXTRACTION_SCHEMA_V3 — un-aliased + actorConfidence (D-12, Open Q §2/§3)')` with:
       - `it('is un-aliased from V2 (Open Q §3)')` — `expect(EVENT_EXTRACTION_SCHEMA_V3).not.toBe(EVENT_EXTRACTION_SCHEMA_V2)` (referential inequality after un-aliasing)
       - `it('declares actorConfidence in events.items.properties')` — navigate the nested schema literal: `((EVENT_EXTRACTION_SCHEMA_V3.properties as Record<string, unknown>).events as Record<string, unknown>).items as Record<string, unknown>).properties as Record<string, unknown>).actorConfidence`. Assert it's defined and that `items.enum` is `['high','medium','low']`. Type narrowing via `as` casts is acceptable here — schema literals are `Record<string, unknown>` by design.
       - `it('declares actorConfidence in events.items.required (Open Q §2 — wire required)')` — navigate to the same nested `required: string[]` array; assert `required.includes('actorConfidence') === true`

    Imports: extend existing imports with `EVENT_EXTRACTION_SCHEMA_V3, EVENT_EXTRACTION_SCHEMA_V2, enrichedEventV3` from `'../../lib/llmSchema.js'` (existing test file already imports from this module — extend its import list).

    DO NOT modify existing tests. Add new describe blocks at the END of the file.

  </action>
  <verify>
    <automated>grep -Ec "describe\('Phase 33" server/__tests__/lib/llmSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/__tests__/lib/llmSchema.test.ts` modified (not created — existing file extended)
    - Two NEW Phase 33 describe blocks: `grep -Ec "describe\('Phase 33" server/__tests__/lib/llmSchema.test.ts` returns 2
    - `grep -c "actorConfidence" server/__tests__/lib/llmSchema.test.ts` returns ≥ 6 (6+ test cases reference the field)
    - `grep -c "EVENT_EXTRACTION_SCHEMA_V3" server/__tests__/lib/llmSchema.test.ts` returns ≥ 3 (un-alias + properties + required tests)
    - `grep -c "toBe(false)\|success.*false" server/__tests__/lib/llmSchema.test.ts | head -1` returns ≥ 1 (invalid enum rejection case)
    - `npx vitest run server/__tests__/lib/llmSchema.test.ts` exits non-zero (RED — schema not yet extended in Task 2)
    - No `it.skip` / `it.todo` in the new blocks
  </acceptance_criteria>
  <done>Schema contract tests committed (RED); Task 2 implementation makes them GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend enrichedEventV3 + un-alias EVENT_EXTRACTION_SCHEMA_V3 (D-10, D-12)</name>
  <files>server/lib/llmSchema.ts</files>
  <read_first>
    - server/lib/llmSchema.ts (entire file — verify current state at lines 173-175 + 263-347 + 359 before editing)
    - server/__tests__/lib/llmSchema.test.ts (Task 1 output — drives GREEN)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-PATTERNS.md (Layer 4 §"llmSchema.ts" lines 537-633)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md (D-10 verbatim shape; D-12 schema-canonical placement)
    - .planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-RESEARCH.md (Open Q §1 + §2 + §3 — three locked resolutions)
  </read_first>
  <behavior>
    - `enrichedEventV3` extended with `actorConfidence: z.array(z.enum(['high', 'medium', 'low'])).optional()` (Open Q §1 — .optional() for rollout)
    - JSDoc at the extend-site explaining the cross-field length-match invariant is enforced in the EXTRACTOR (Plan 33-04 `repairActorConfidence`), NOT in Zod superRefine
    - `EVENT_EXTRACTION_SCHEMA_V3` un-aliased from `V2` (copy + edit, not reuse) per D-12
    - Un-aliased literal contains `actorConfidence: { type: 'array', items: { type: 'string', enum: ['high','medium','low'] } }` in `events.items.properties`
    - `actorConfidence` added to `events.items.required` array (Open Q §2 — required at wire)
    - JSDoc at the un-aliased V3 schema literal pins v3↔v2 divergence (PATTERNS risk #4)
  </behavior>
  <action>
    PART A (D-10 Zod extend) — Modify lines 173-175 of `server/lib/llmSchema.ts`:
    ```
    export const enrichedEventV3 = enrichedEventV2.extend({
      schemaVersion: z.literal('v3'),
      actorConfidence: z.array(z.enum(['high', 'medium', 'low'])).optional(),
    });
    ```
    Add JSDoc immediately above this `export const`:
    `/** Phase 33 D-10 — actorConfidence index-locked parallel array to actors[]. */`
    `/**  Ships as .optional() for the rollout window (Open Q §1) so legacy v3 cache entries */`
    `/**  through enrichedEventAny continue to parse. The cross-field length-match invariant */`
    `/**  (arr.length === actors.length) is enforced in the extractor's repairActorConfidence helper */`
    `/**  (Plan 33-04, server/lib/llmEventExtractor.v3.ts), NOT in Zod superRefine — Zod cannot */`
    `/**  cross-field-refine without the parent shape, which complicates the discriminated-union read. */`
    `/**  Tighten to required in a Phase 35+ cleanup phase once 24h cron rollover completes. */`

    PART B (D-12 JSON Schema un-alias) — Replace line 359:
    Current: `export const EVENT_EXTRACTION_SCHEMA_V3: Record<string, unknown> = EVENT_EXTRACTION_SCHEMA_V2;`

    With: a full copy of `EVENT_EXTRACTION_SCHEMA_V2` (read lines 263-347 to get the exact literal), with TWO additions in the nested `events.items` block:
    1. In `properties`: add `actorConfidence: { type: 'array', items: { type: 'string', enum: ['high', 'medium', 'low'] } }`
    2. In `required` array (alongside existing `'actors'`): add `'actorConfidence'`

    Add JSDoc immediately above the new un-aliased `export const EVENT_EXTRACTION_SCHEMA_V3`:
    `/** Phase 33 D-12 — un-aliased from EVENT_EXTRACTION_SCHEMA_V2 per Open Q §3. */`
    `/**  v3 diverged from v2 in Phase 33 by adding actorConfidence (Open Q §2 — required */`
    `/**  at wire for LLM forcing-function value; server repairs missing/wrong-length via */`
    `/**  repairActorConfidence as defense-in-depth). FUTURE v2 SCHEMA CHANGES DO NOT */`
    `/**  AUTO-PROPAGATE — V2 is frozen post-Phase-29 (v1+v2 extractors deleted; SIMPLIFY-06). */`
    `/**  If v2 ever changes, manually port the change here AND assert the divergence in */`
    `/**  the llmSchema.test.ts contract suite. */`

    DO NOT modify EVENT_EXTRACTION_SCHEMA_V2 (it remains canonical for v2 rollback path). DO NOT introduce new exports beyond the un-aliased V3 literal.

    Atomic-commit boundary: PART A (Zod extend) and PART B (JSON Schema un-alias) MUST land in the SAME commit because the contract test in Task 1 asserts both together. (Per 33-RESEARCH.md Wave Assignment — D-10 + D-12 = single commit; D-09 + D-08 are separate commits handled in Plan 33-04.)

  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/llmSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/lib/llmSchema.ts` modified (existing file)
    - `enrichedEventV3` extension carries `actorConfidence`: `grep -Ec "actorConfidence:.*z\.array\(z\.enum\(\['high', 'medium', 'low'\]\)\)\.optional\(\)" server/lib/llmSchema.ts` returns ≥ 1 (allowing minor whitespace variance; if zero, the equivalent regex matching `actorConfidence` + `z.array` + `optional()` on the same logical statement must match)
    - Conservative fallback for the above: `grep -E "actorConfidence" server/lib/llmSchema.ts | grep -E "optional\(\)" | wc -l` returns ≥ 1 (.optional() present on the actorConfidence Zod field — Open Q §1 honored)
    - `EVENT_EXTRACTION_SCHEMA_V3` is no longer aliased to V2: `grep -c "EVENT_EXTRACTION_SCHEMA_V3.*=.*EVENT_EXTRACTION_SCHEMA_V2" server/lib/llmSchema.ts` returns 0 (the alias line is replaced)
    - The new V3 literal mentions actorConfidence in JSON-Schema form: `grep -c "actorConfidence" server/lib/llmSchema.ts` returns ≥ 2 (one Zod use + one JSON-Schema use)
    - `actorConfidence` in V3's `required` array: `grep -n "required" server/lib/llmSchema.ts` includes a line in the V3 block listing 'actorConfidence' (verify by `awk '/EVENT_EXTRACTION_SCHEMA_V3/,/^};/' server/lib/llmSchema.ts | grep -c "'actorConfidence'"` returns ≥ 2 — once in properties, once in required)
    - JSDoc pinning v2/v3 divergence present: `grep -Ec "v3 diverged from v2|FUTURE v2 SCHEMA CHANGES DO NOT|AUTO-PROPAGATE" server/lib/llmSchema.ts` returns ≥ 1 (PATTERNS risk #4 honored)
    - JSDoc at Zod extend explaining length-match enforced in extractor: `grep -Ec "repairActorConfidence|length-match|cross-field" server/lib/llmSchema.ts` returns ≥ 1
    - V2 literal unchanged: `awk '/EVENT_EXTRACTION_SCHEMA_V2: Record/,/^};/' server/lib/llmSchema.ts | grep -c "actorConfidence"` returns 0 (V2 is frozen — must NOT carry the new field)
    - `npx vitest run server/__tests__/lib/llmSchema.test.ts` exits 0 (Task 1 tests pass GREEN)
    - `npx tsc --noEmit -p tsconfig.server.json 2>&amp;1 | grep -c "llmSchema.*error TS"` returns 0 (no new TS errors)
  </acceptance_criteria>
  <done>Schema extension lands atomically; Plan 33-04 has the schema surface it needs for D-08/D-09 integration.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                           |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------- |
| LLM provider → server (`response_format.json_schema`) | NIM's relaxed JSON-Schema mode may ignore "required" — server-side repair compensates |
| cache (`events:llm:v3`) → enrichedEventAny reader     | Legacy entries lack actorConfidence; .optional() preserves cache-read compatibility   |

## STRIDE Threat Register

| Threat ID | Category              | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                   |
| --------- | --------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-33-04   | Tampering / Integrity | enrichedEventAny discriminated-union cache read | mitigate    | `.optional()` on actorConfidence (Open Q §1) admits legacy v3 payloads during the 24h rollout. Server-side repair in Plan 33-04 fills the field on every NEW cron write. Contract test in Task 1 enforces both acceptance paths (with + without). |
| T-33-04b  | Tampering             | JSON Schema un-alias risk (Open Q §3)           | mitigate    | JSDoc on the new V3 literal explicitly states v2 changes do NOT propagate. Contract test asserts `V3 !== V2` to catch accidental re-aliasing.                                                                                                     |

</threat_model>

<verification>
After Tasks 1-2 complete:
- `npx vitest run server/__tests__/lib/llmSchema.test.ts` exits 0
- `npx tsc --noEmit -p tsconfig.server.json` introduces no new errors
- `enrichedEventV2` literal in llmSchema.ts is unchanged (frozen — diff shows V2 untouched)
- Plan 33-04 can now import `enrichedEventV3` and write payloads carrying `actorConfidence: ('high'|'medium'|'low')[]`
</verification>

<success_criteria>

- ACTOR-03 schema-side requirements addressed (D-10 Zod + D-12 JSON Schema)
- Open Q §1 resolved: `.optional()` for rollout window — no legacy v3 cache rejection
- Open Q §2 resolved: required in wire schema — LLM forcing-function value; server repair as defense-in-depth (Plan 33-04)
- Open Q §3 resolved: V3 un-aliased with JSDoc pinning v2/v3 divergence
- PATTERNS critical risk #1 (`.optional()`) honored — atomic commit
- PATTERNS critical risk #4 (JSDoc pinning) honored — atomic commit
  </success_criteria>

<output>
Create `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-03-SUMMARY.md` when done. Include:
- Whether the Zod extend + JSON Schema un-alias landed as ONE commit (per Wave 2 grouping) or TWO atomic commits — per D-20 prefer atomic D-N-per-commit BUT D-10/D-12 share the same file slice and Wave assignment says "single Wave 2 atomic commit"
- Whether `enrichedEventAny` discriminated-union read still parses legacy v3 fixtures — drift-check note for follow-up phases
- Decision-coverage trace: D-10 → Zod extend with .optional(); D-12 → un-aliased JSON Schema; Open Q §1/§2/§3 → all resolved with locked rationale
</output>

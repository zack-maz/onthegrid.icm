---
phase: 37-adr-0010-acceptance-gate-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
autonomous: true
requirements:
  - DOCS-PUB-04
tags:
  - adr
  - docs
  - milestone-close
  - subblock-append
  - body-rewrite

must_haves:
  truths:
    - 'ADR-0010 body sections (Context, Decision, Consequences, Alternatives Considered, References) describe milestone-final shipped state — NIM-only at runtime, OpenRouter dormant, Cerebras + Groq deferred, v1+v2 deleted, Pitfall 1 bridge serves raw GDELT when events:llm:v3 is empty'
    - 'The five existing v1.5 historical sub-blocks (Phase 30 / 30.1 / 34 / 35; plus the Phase 29 framing embedded in the original Context/Decision sections that gets PRESERVED as the historical lead-in) survive intact except for the body-rewrite scope explicitly approved by CONTEXT D-01..D-02'
    - 'The <expand_at_36> HTML comment marker is deleted from the file'
    - 'Original `Status: Accepted` line (Phase 29 acceptance 2026-05-11) preserved as line 3; a SECOND status line `Status: Accepted (v1.5 closed YYYY-MM-DD)` is appended directly below as line 4'
    - 'Phase 37 close sub-block appended as the 6th and final v1.5 sub-block beneath the Phase 35 sub-block, containing D-01..D-05 rows plus a v1.5 Milestone Close Rollup subsection and Architecture-level numbers + Out-of-scope footers'
    - 'A placeholder forward-reference for the LLM-RELI-07 gate evidence row resolves to a stable section heading (`37-SUMMARY.md §Acceptance Gate Observation`) that Plan 37-02 will create and Plan 37-03 will finalize'
  artifacts:
    - path: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      provides: 'ADR-0010 milestone-final body + 6 v1.5 sub-blocks + two-state status line'
      min_lines: 350
      contains:
        - '## Phase 37 Close Sub-block'
        - 'v1.5 Milestone Close Rollup'
        - 'Status: Accepted (v1.5 closed'
  key_links:
    - from: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (Phase 37 close sub-block placeholder row)'
      to: '.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md §Acceptance Gate Observation'
      via: 'markdown link'
      pattern: "37-SUMMARY\\.md.*Acceptance Gate Observation"
    - from: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (Architecture-level numbers footer)'
      to: 'docs/architecture/llm-pipeline-reliability.md'
      via: 'relative markdown link'
      pattern: "\\.\\./architecture/llm-pipeline-reliability\\.md"
---

<objective>
Rewrite ADR-0010 body (Context, Decision, Consequences, Alternatives Considered, References) to describe milestone-final shipped state — not the Phase 29-open intent. Preserve the 5 existing historical sub-blocks (Phase 30 / 30.1 / 34 / 35; plus Phase 29 framing in the original Context/Decision sections gets condensed but not deleted — see D-01 below). Delete the `<expand_at_36>` HTML comment marker. Append a second `Status: Accepted (v1.5 closed YYYY-MM-DD)` line below the original `Status: Accepted` line. Append the Phase 37 close sub-block as the 6th and final v1.5 sub-block (D-01..D-05 rows + v1.5 Milestone Close Rollup subsection + Architecture-level numbers + Out-of-scope footers).

Purpose: Make the ADR honest at milestone close — the body reads as the canonical answer to "what did we ship in v1.5?", and the 6 sub-blocks read as the historical decision trail showing how the milestone-final state was reached one phase at a time.

Output: A single rewritten `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` file (~350+ lines), committed via 5 atomic per-decision commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@CLAUDE.md

<interfaces>
<!-- Key string-level contracts the executor needs. -->

ADR-0010 line-2-through-5 status block (current state, preserve line 3 verbatim):

```
# ADR-0010: v1.5 LLM pipeline narrowing and deletion

**Status:** Accepted
**Date:** 2026-05-11
**Deciders:** solo author
```

D-05 second-line append shape (after rewrite — preserve line 3 verbatim, INSERT a new line 4):

```
**Status:** Accepted
**Status:** Accepted (v1.5 closed YYYY-MM-DD)
**Date:** 2026-05-11
**Deciders:** solo author
```

PRESERVED v1.5 sub-block headings (verbatim, MUST NOT change):

- Line 52: `## Phase 30 Sub-block (appended 2026-05-17)`
- Line 82: `## Phase 30.1 Sub-block (appended 2026-05-17)`
- Line 108: `## Phase 34 Sub-block (appended 2026-05-23)`
- Line 133: `## Phase 35 Sub-block (appended 2026-05-27)`

DELETED marker (line 150 currently — must not appear anywhere in the rewritten file):

```
<expand_at_36>
```

NEW Phase 37 sub-block heading (Plan 37-01 introduces it as the 6th and final v1.5 sub-block, appended below the Phase 35 sub-block, ABOVE the rewritten `## Consequences`):

```
## Phase 37 Close Sub-block (appended YYYY-MM-DD)
```

NEW forward-reference for Plan 37-02 / 37-03 to resolve (Plan 37-01 lands this as a placeholder line; Plan 37-03 rewrites it with the final wording + evidence pointer):

```
- **D-XX (LLM-RELI-07): see [`37-SUMMARY.md §Acceptance Gate Observation`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md#acceptance-gate-observation-llm-reli-07) — finalized in Plan 37-03.**
```

Architecture-level numbers footer (per PATTERNS lines 836-838):

```
**Architecture-level numbers:** [`docs/architecture/llm-pipeline-reliability.md`](../architecture/llm-pipeline-reliability.md) for the cumulative measurement story across Phases 30 / 30.1 / 34. This sub-block records the milestone-close **decision**; the architecture doc records the cumulative cross-phase **measurement** chain (mirrors the Phase 30 / 30.1 / 34 / 35 sub-block convention).
```

Out-of-scope-carries-forward block (per PATTERNS lines 860-872 — copy verbatim):

```
**Out of scope (carries forward to v1.6+):**

- 999.5 Performance Optimization + 1-300 VU k6 sweep — unblocked by Phase 37 acceptance gate; promotes from `.planning/phases/999.5-performance-load-test/` into v1.6 as the first phase
- REVEAL-01 polish (landing page, demo flows, social-share assets, hero GIF) — v1.6 territory
- REVEAL-02 public domain — v1.6 milestone-open scoping question
- Cerebras + Groq adapter restoration — future provider-restoration phase per ADR-0010 Phase 34 sub-block follow-up candidates
- Paid-OR conversion (~$0.04/day = ~$1.20/mo for full coverage) — per ADR-0010 Phase 30.1 sub-block
- Adaptive Retry-After-aware NIM limiter — per ADR-0010 Phase 30 sub-block `retryAfterMs` field already on `callHistory`
- Per-provider eval infrastructure + `cascade_exhausted` DLQ taxonomy — deferred alongside provider restoration
- ADR-0011 Phase 37 sub-block — milestone-close work concentrated in ADR-0010; future "ADR hygiene" phase could add cross-links
- OpenAPI full-spec audit + Zod-handler reconciliation — Phase 36 D-05 capped at additions; future "API hardening" phase
- ROADMAP / REQUIREMENTS retroactive rewording for the 7 framing gaps — future "planning artifact refresh" phase
```

Phase 37 close sub-block D-rows shape (~150-200 lines total; mirrors Phase 35 D-rows structure at PATTERNS lines 104-115 + Phase 30 numbered-list shape at PATTERNS lines 54-61):

```
- **D-01 (full body rewrite + 5 sub-blocks preserved):** ADR-0010 body sections (Context, Decision, Consequences, Alternatives Considered, References) rewritten to describe milestone-final shipped state — NIM-only at runtime, OpenRouter dormant (Phase 30.1), Cerebras + Groq deferred (Phase 34), v1+v2 extractors deleted (Phase 29 D-02), Pitfall 1 cache bridge serves raw GDELT when `events:llm:v3` is empty (`server/routes/events.ts`). The 5 existing sub-blocks (Phase 30 / 30.1 / 34 / 35; with Phase 29 framing now condensed into the rewritten Context/Decision lead-in) preserved as the historical decision trail. Reads as a single canonical ADR end-to-end.
- **D-02 (absorb expand_at_36 marker + rewrite Consequences/Alternatives/References):** HTML comment `<expand_at_36>` deleted. Consequences rewritten: Positive (smaller bundle, simpler `git revert` rollback, active-path-is-active-path clarity, NIM-only honesty surfaces DLQ baseline); Negative (Phase 27.4 D-26/D-40 deep-rollback lock superseded; ADR-0009 partially historical re: v2 keys + `events:llm:v3:partial` retired Phase 35; cron Hobby-300s class of failures eliminated but NIM throttle remains single point of failure per Phase 34 deferral); Neutral (`shouldPauseNewEvents()` soft-cap pause unreachable post-narrowing). Alternatives Considered rewritten to include the Phase 29-era alternatives (archive v1.ts+v2.ts to attic/, add `LLM_PIPELINE_ENABLED` kill-switch — both rejected) PLUS the Phase 30.1-era OR-restore-via-free-tier choice (rejected per ≥90% rate_limited probe) PLUS the Phase 34-era Cerebras/Groq-provision choice (rejected per operator deferral). References rewritten to include all 9 phase CONTEXT.md paths + commit ranges (filled at PR merge) + cross-links to `docs/architecture/llm-pipeline-reliability.md` + `docs/architecture/redis-keys.md` + ADR-0009 + ADR-0011 + the 7 v1.5 phase SUMMARY.md paths.
- **D-03 (inline citation per D-N row):** Each D-N row that closes a requirement cites it inline. D-04 cites DOCS-PUB-04 inline (this sub-block lands DOCS-PUB-04). The forward-reference placeholder line (`D-XX (LLM-RELI-07): see 37-SUMMARY.md §Acceptance Gate Observation — finalized in Plan 37-03`) preserves the inline-citation convention for the gate evidence row that Plan 37-02 captures + Plan 37-03 finalizes.
- **D-04 (Phase 37 close sub-block content + v1.5 Milestone Close Rollup):** This sub-block itself — D-N rows mirror this CONTEXT's 5 decisions in scope (D-01..D-05) plus a v1.5 Milestone Close Rollup subsection that surfaces the cumulative arc: cascade narrowed (Phase 29) → NIM-only honest (Phase 30.1) → providers deferred (Phase 34) → docs+Redis cleanup (Phase 35) → public docs sweep + OpenAPI gates (Phase 36) → milestone gate observed + ADR canonicalized (Phase 37). Cross-link to `37-SUMMARY.md` for the full per-phase outcome table.
- **D-05 (status line gains second line):** `Status: Accepted (v1.5 closed YYYY-MM-DD)` appended as line 4, directly below the existing `Status: Accepted` line 3 (preserved verbatim with original 2026-05-11 date implicit via the `**Date:** 2026-05-11` line 4-becomes-line-5). Reads as a two-state visual at the top of the ADR. Plan 37-01 commits this with today's date as a placeholder; Plan 37-03 rewrites to the actual milestone-close date.
```

v1.5 Milestone Close Rollup subsection shape (within the Phase 37 sub-block, ~30-50 lines):

```
### v1.5 Milestone Close Rollup

The 6 v1.5 sub-blocks within this ADR read as the milestone retrospective from inside the ADR:

1. **Phase 29 (the body above)** — cascade narrowed to NIM + OpenRouter; v1+v2 extractors deleted; LLM-optional architecture proven; Vercel Pro upgrade ($20/mo, `maxDuration: 300 → 800`); Cerebras + Groq adapter dead-code purged; CLAUDE.md trimmed. (Closed 2026-05-11.)
2. **Phase 30 sub-block (above)** — NIM throttle characterization (Path B — `Retry-After` headers absent; defensive defaults anchored to p95 33,263ms); `LLM_BATCH_TIMEOUT_MS = 120000` / `RETRY_ATTEMPTS = 3` / `BACKOFF_MS = [2000, 8000, 32000]` / `JITTER_MS = 500` committed; SIMPLIFY-01 incremental flush retired (~95% fewer Redis SET calls per cron run); SIMPLIFY-03 watchdog soft-warn tier eliminated. (Closed 2026-05-17.)
3. **Phase 30.1 sub-block (above)** — Cascade reality declared: OpenRouter dormant pending re-validation (27/30 rate_limited = 90.0% in the 2026-05-17 probe). CLAUDE.md "Active providers" line amended. No code change. (Closed 2026-05-17.)
4. **Phase 34 sub-block (above)** — Cerebras + Groq deferred (`cerebras-groq-deferred` close-out; operator chose to skip probe + adapter restoration). LLM-RELI-08..11 closed as Done with the deferral outcome. CLAUDE.md "Active providers" line amended. No code change. (Closed 2026-05-23.)
5. **Phase 35 sub-block (above)** — Redis registry drift gate landed (`src/__tests__/lib/redis-registry.test.ts`; 39 assertions); 32-key deep-dive at `docs/architecture/redis-keys.md`; `events:llm:v3:partial` retired (SIMPLIFY-02; 358 LOC removed); `freeClaudeRouter.ts` callers block documented (SIMPLIFY-05); 7-module JSDoc audit (DOCS-INT-02). Bundle delta: +10,739 bytes (+0.60%; JSDoc additions outweigh deletions). (Closed 2026-05-27.)
6. **Phase 37 (this sub-block)** — ADR-0010 milestone-final rewrite + this close sub-block; status line gains second line; 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed (LLM-RELI-07); CHANGELOG[v1.5] entry; 37-SUMMARY.md with per-phase rollup + framing-gap callouts + quantitative snapshot + v1.6 promotion readiness; ROADMAP/REQUIREMENTS/STATE flips. v1.6 promotion unblocked (999.5 first phase). (Closed YYYY-MM-DD.)

See [`37-SUMMARY.md`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md) for the full per-phase outcome table and the closing Decision-by-Decision Outcome table.
```

PRESERVED line-range guard (the executor verifies AFTER rewrite + before commit):

- Phase 30 sub-block: ORIGINAL lines 52-80 → must appear byte-identical in the rewritten file (the line numbers will shift due to rewrites above, but the content must be unchanged)
- Phase 30.1 sub-block: ORIGINAL lines 82-106 → must appear byte-identical
- Phase 34 sub-block: ORIGINAL lines 108-131 → must appear byte-identical
- Phase 35 sub-block: ORIGINAL lines 133-148 → must appear byte-identical

Verification command for sub-block integrity (run before each commit):

```bash
git diff -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | grep -E '^[-+]' | grep -E '^[-+][^+-]' | grep -v -E '(Phase 30 Sub-block|Phase 30.1 Sub-block|Phase 34 Sub-block|Phase 35 Sub-block).*appended'
```

(If the diff has changes within the 4 preserved sub-block ranges, this filter will show them. The headers themselves appear once each.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 — Rewrite ADR-0010 Context + Decision sections to milestone-final shipped state</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (current 207 lines — entire file; you will rewrite Context + Decision sections only, leaving sub-blocks intact)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md lines 50-56 (D-01 specification — the locked decision text in user's own words)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 22-128 (ADR file role + sub-block shapes + status-line shapes)
    - CLAUDE.md §"LLM Event Pipeline" (~lines 105-126 of CLAUDE.md — current shipped reality: NIM-only at runtime, OpenRouter dormant, Cerebras + Groq deferred, single extractor module, v3 cascade structure)
    - docs/architecture/llm-pipeline-reliability.md (Phase 30 / 30.1 / 34 sections — shipped numerics + Pitfall 1 cache bridge mechanics)
    - server/routes/events.ts (read first 50 lines to confirm Pitfall 1 cache bridge entry point — the "map never goes blank" mechanical proof D-01 cites)
  </read_first>
  <action>
    Rewrite `## Context` (currently lines 7-22) to read as milestone-final, not Phase 29-open. The rewrite condenses the original Phase 29-perspective opener ("Phase 29 opens the simplification sweep...") into a single retrospective sentence that introduces the milestone arc, then summarizes shipped reality in the present tense across ~3-4 paragraphs: (1) what the active cascade actually is at v1.5 close (NIM-only at runtime; OpenRouter dormant per Phase 30.1; Cerebras + Groq deferred per Phase 34); (2) the LLM-optional architecture and Pitfall 1 cache bridge that guarantees "map never goes blank" via `server/routes/events.ts`; (3) Vercel Pro upgrade and the cron-driven pipeline shape (`/api/cron/refresh-events` daily 04:00 UTC); (4) one paragraph noting that the 6 sub-blocks below capture HOW this state was reached phase-by-phase.

    Rewrite `## Decision` (currently lines 24-50, with 4 numbered items): replace the 4 numbered items with the milestone-final Decision per CONTEXT D-01: **"Active cascade is NIM-only at runtime; OpenRouter dormant per Phase 30.1; Cerebras + Groq deferred per Phase 34; v1+v2 extractors deleted Phase 29; Pitfall 1 cache bridge serves raw GDELT when `events:llm:v3` is empty (map never goes blank)."** Expand this into 4-5 numbered items that capture each of the 5 phase-by-phase decisions (cascade narrowing + v1/v2 deletion + LLM-optional + Pro upgrade + cleanup) as a single milestone-final restatement — NOT as the Phase 29-open intent.

    Do NOT touch the 4 v1.5 sub-blocks (Phase 30 / 30.1 / 34 / 35 — lines 52-148). Do NOT touch the `<expand_at_36>` marker (line 150 — Task 2 handles that). Do NOT touch the existing Consequences / Alternatives Considered / References block (lines 152-201 — Task 2 handles those). Do NOT touch the status line yet (Task 5 handles that).

    Commit: `docs(37): D-01 rewrite ADR-0010 Context + Decision to milestone-final shipped state`

  </action>
  <verify>
    <automated>
      # The 4 preserved sub-block headings must still be present and unchanged
      grep -c '^## Phase 30 Sub-block (appended 2026-05-17)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      grep -c '^## Phase 30.1 Sub-block (appended 2026-05-17)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      grep -c '^## Phase 34 Sub-block (appended 2026-05-23)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      grep -c '^## Phase 35 Sub-block (appended 2026-05-27)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # Each must return exactly 1.
      # Context + Decision must mention NIM-only and Pitfall 1.
      grep -c 'NIM-only at runtime' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      grep -c 'Pitfall 1' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Phase 30 Sub-block (appended 2026-05-17)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^## Phase 30.1 Sub-block (appended 2026-05-17)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^## Phase 34 Sub-block (appended 2026-05-23)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^## Phase 35 Sub-block (appended 2026-05-27)$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c 'NIM-only at runtime' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1` (now mentioned in Decision section)
    - `grep -c 'Pitfall 1' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1` (now mentioned in Context or Decision)
    - `grep -c '^## Context$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^## Decision$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '<expand_at_36>' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1` (marker NOT YET deleted — Task 2 handles it)
    - `git log -1 --pretty=%s` returns `docs(37): D-01 rewrite ADR-0010 Context + Decision to milestone-final shipped state`
    - `git diff HEAD~1 -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` shows changes only between the file start (line 1) and the `## Phase 30 Sub-block` heading; the 4 sub-block ranges below it show NO `[-+]` lines other than line-shift artifacts from the Context/Decision rewrite above
  </acceptance_criteria>
  <done>Context + Decision sections rewritten to milestone-final shipped state; 4 v1.5 sub-block headings still present and unchanged; commit landed with the `docs(37): D-01` prefix.</done>
</task>

<task type="auto">
  <name>Task 2: D-02 — Delete &lt;expand_at_36&gt; marker + rewrite Consequences/Alternatives/References to milestone-final reality</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (current state after Task 1 — verify Context + Decision are rewritten + the `<expand_at_36>` marker on its own line still exists)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md lines 51-52 (D-02 specification — the locked enumeration of bullets to land in Consequences + Alternatives + References)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 118-174 (existing Consequences/Alternatives/References shape + D-02 absorption instructions)
    - docs/adr/0009-two-key-split-for-llm-partial-progress-vs-terminal-reads.md (first 30 lines — confirm filename + current status to cite correctly in the References block AND in the Negative-consequences "ADR-0009 partially historical" bullet, since `events:llm:v3:partial` was retired in Phase 35 per the Phase 35 sub-block D-12)
    - docs/adr/0011-v3-llm-pipeline-architecture.md (first 30 lines — confirm filename + current status to cite correctly in References; Phase 36 D-21 appended a sub-block per CLAUDE.md so 0011 is alive)
    - docs/architecture/llm-pipeline-reliability.md (first 30 lines + the Phase 30/30.1/34 section headings — confirm the cross-link target exists)
    - docs/architecture/redis-keys.md (first 30 lines — confirm filename for References)
    - All 7 v1.5 phase SUMMARY.md paths (run `ls .planning/phases/{29,30,30.1,31,32,33,34,35,36}-*/` to confirm directory slugs and that *-SUMMARY.md files exist; Phase 37 SUMMARY does not yet exist — it will be created by Plan 37-03)
  </read_first>
  <action>
    DELETE the literal `<expand_at_36>` line. The marker sits alone between the Phase 35 sub-block close and the existing `## Consequences` heading; after deletion leave a single blank line before `## Consequences`. Verify via `grep -c "expand_at_36" docs/adr/...md` returns `0`.

    REWRITE `## Consequences` (currently lines 152-177) to milestone-final reality per CONTEXT D-02:
    - `### Positive`: smaller bundle (1.72MB → 1.79MB delta is JSDoc-additions-dominant per Phase 35 D-19 — net direction is fewer code paths even though byte count rose due to docs); simpler rollback via `git revert <Phase 29 commit range>`; active-code-path-is-active-code-path clarity (no flag-gated branches, no preserved-for-rollback modules to triage during incidents); NIM-only honesty surfaces the DLQ baseline (Phase 31 Day-1 = 4 × `v3:timeout_watchdog`) that would have been hidden behind a non-functional OpenRouter-fallback claim.
    - `### Negative`: Phase 27.4 D-26/D-40 deep-rollback lock superseded (recovery path is now `git revert`, not flip a runtime flag); ADR-0009 (two-key-split for partial vs terminal v2 reads) becomes partially historical — the v2 keys it documents are deletion targets per Phase 29 D-02 AND the v3 partial-key pattern (`events:llm:v3:partial`) it inspired was itself retired in Phase 35 (SIMPLIFY-02; see Phase 35 sub-block D-12); the cron Hobby-300s class of failures is eliminated by the Pro upgrade but NIM throttle remains the single point of failure under the Phase 34 deferral.
    - `### Neutral`: `shouldPauseNewEvents()` soft-cap pause becomes unreachable post-narrowing (it gated v2-vs-v3 racing in the events route; documented as Phase 30 cleanup work).

    REWRITE `## Alternatives Considered` (currently lines 179-189) to expand per CONTEXT D-02:
    - **Archive v1.ts + v2.ts to `attic/`** (original SIMPLIFY-06 plan; Phase 29-era) — rejected: archived code creates the same triage burden as preserved code; git history is the archive.
    - **Add `LLM_PIPELINE_ENABLED` env-var kill-switch** (Phase 29-era) — rejected: "unset both `NVIDIA_NIM_API_KEY` + `OPENROUTER_API_KEY`" is the kill switch; a dedicated env var would duplicate that mechanism.
    - **Restore OpenRouter via free-tier** (Phase 30.1-era) — rejected per `scripts/probe-openrouter.ts` 2026-05-17 result: 27/30 rate_limited = 90.0%; OpenRouter free-tier not viable for batch extraction. Cascade declared NIM-only.
    - **Provision Cerebras / Groq free-tier accounts + run probe** (Phase 34-era) — rejected per operator deferral: the Phase 31 Day-1 DLQ baseline (4 × `v3:timeout_watchdog`) is accepted as a known failure mode under single-provider NIM rather than expanding the provider surface. Documented as `cerebras-groq-deferred` close-out.

    REWRITE `## References` (currently lines 191-201) to include all 9 v1.5 phase CONTEXT.md paths + cross-links per CONTEXT D-02. The reference list MUST include (one bullet each):
    - `.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md`
    - `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md`
    - `.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-CONTEXT.md`
    - `.planning/phases/31-cron-stability-validation-7-day-watch/31-CONTEXT.md`
    - `.planning/phases/32-ghost-event-url-liveness-dashboard-prune/32-CONTEXT.md` (use actual slug from `ls`)
    - `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-CONTEXT.md` (use actual slug from `ls`)
    - `.planning/phases/34-llm-router-fallback-re-integration-cerebras-groq-per-provide/34-CONTEXT.md` (use actual slug from `ls`)
    - `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-CONTEXT.md`
    - `.planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md`
    - The 7 v1.5 phase SUMMARY.md paths (Phase 29, 30, 30.1, 31, 32, 33, 34, 35, 36 — same slugs)
    - `docs/architecture/llm-pipeline-reliability.md`
    - `docs/architecture/redis-keys.md`
    - `docs/adr/0009-two-key-split-for-llm-partial-progress-vs-terminal-reads.md`
    - `docs/adr/0011-v3-llm-pipeline-architecture.md`
    - `CLAUDE.md` §"LLM Event Pipeline" + §"Serverless Cache"
    - `server/routes/events.ts` (Pitfall 1 cache bridge implementation)
    - `server/__tests__/resilience/redis-death.test.ts` (proves chain works under Redis death)
    - `Commit range: <filled in at PR merge time>` (preserve the existing convention)

    Use relative-path markdown links pointing FROM `docs/adr/0010-...md` to the targets. Cross-document links use `../architecture/...` for `docs/architecture/` and `../../.planning/...` for `.planning/` paths.

    Preserve the existing Nygard template-source footer (lines 203-207 — the `_Template source: Michael Nygard..._` block).

    Commit: `docs(37): D-02 absorb expand_at_36 marker + rewrite Consequences/Alternatives/References to milestone-final reality`

  </action>
  <verify>
    <automated>
      grep -c "expand_at_36" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 0 — marker deleted
      grep -c "scripts/probe-openrouter.ts" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 1 — Phase 30.1-era alternative cited
      grep -c "cerebras-groq-deferred" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 1 — Phase 34-era alternative cited
      grep -cE '^\s*-\s+`?\.planning/phases/(29|30|30\.1|31|32|33|34|35|36)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 9 — one bullet per phase CONTEXT.md
      grep -c 'Template source: Michael Nygard' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 1 — Nygard footer preserved
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expand_at_36" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `0`
    - `grep -c '^## Consequences$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^## Alternatives Considered$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^## References$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c 'scripts/probe-openrouter.ts' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c 'cerebras-groq-deferred' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c 'docs/architecture/llm-pipeline-reliability.md' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c 'docs/architecture/redis-keys.md' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c '0009-two-key-split' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c '0011-v3-llm-pipeline-architecture' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c 'Template source: Michael Nygard' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1` (footer preserved)
    - `grep -cE '\.planning/phases/(29|30|30\.1|31|32|33|34|35|36)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `9` (one CONTEXT.md bullet per phase; SUMMARY references can multiply this further)
    - 4 v1.5 sub-block headings still present (same `grep -c` checks from Task 1, each returns `1`)
    - `git log -1 --pretty=%s` returns `docs(37): D-02 absorb expand_at_36 marker + rewrite Consequences/Alternatives/References to milestone-final reality`
  </acceptance_criteria>
  <done>`<expand_at_36>` marker deleted; Consequences/Alternatives/References rewritten to milestone-final reality with the 4 alternatives (2 Phase 29-era + 1 Phase 30.1-era + 1 Phase 34-era) and the ≥9 phase-CONTEXT.md References; Nygard footer preserved; commit landed with the `docs(37): D-02` prefix.</done>
</task>

<task type="auto">
  <name>Task 3: D-03 + D-04 — Append Phase 37 close sub-block (6th and final v1.5 sub-block) with D-rows + v1.5 Milestone Close Rollup</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (current state after Tasks 1 + 2)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md lines 53-56 (D-03 + D-04 specification)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 26-115 (sub-block-per-phase canonical shapes; Phase 30 / 34 / 35 exemplars) and lines 817-872 (Architecture-level numbers footer + Out-of-scope-carries-forward block templates)
    - The Phase 35 sub-block currently in the file (the LAST v1.5 sub-block — this Task's append target is the line right after the Phase 35 sub-block closes)
  </read_first>
  <action>
    APPEND a NEW `## Phase 37 Close Sub-block (appended YYYY-MM-DD)` heading directly below the END of the Phase 35 sub-block (which ends with its `**Architecture-level numbers:** ...` line — see PATTERNS line 148). The new sub-block becomes the 6th and final v1.5 sub-block, sitting ABOVE the rewritten `## Consequences` heading (which is now beneath all 6 sub-blocks per the file structure). Use today's date as the YYYY-MM-DD placeholder — Plan 37-03 will rewrite this to the actual milestone-close date.

    Inside the new sub-block, lay down ~150-200 lines in this order:

    1. **Opening paragraph (~5-8 lines):** Phase 37 closes the v1.5 LLM Reliability & Reveal Prep milestone with two load-bearing artifacts — the ADR-0010 milestone-final rewrite (this sub-block) AND the 3-consecutive-green `prod-connectivity-audit.yml` observation (LLM-RELI-07). Cross-reference the rewritten Context/Decision body above (now milestone-final, not Phase 29-open) and the 5 historical sub-blocks above as the decision trail.

    2. **5 D-N rows (D-01..D-05)** matching the Phase 30 numbered-list shape (PATTERNS lines 54-61). Each row is a 3-8 line paragraph. Use the literal text from the `<interfaces>` block above for the Phase 37 close sub-block D-rows shape — D-01 (full body rewrite + 5 sub-blocks preserved), D-02 (absorb expand_at_36 + rewrite C/A/R), D-03 (inline citation per D-N row), D-04 (Phase 37 close sub-block content + v1.5 Milestone Close Rollup), D-05 (status line gains second line). Each row cites the requirement it closes inline per D-03 — specifically, D-04 cites DOCS-PUB-04 inline as the requirement this sub-block lands.

    3. **Placeholder forward-reference row** for the LLM-RELI-07 gate evidence (Plan 37-02 captures + Plan 37-03 finalizes):
       ```
       - **D-XX (LLM-RELI-07): see [`37-SUMMARY.md §Acceptance Gate Observation`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md#acceptance-gate-observation-llm-reli-07) — finalized in Plan 37-03.**
       ```

    4. **`### v1.5 Milestone Close Rollup` subsection (~30-50 lines)** per the literal shape in the `<interfaces>` block — a 6-item numbered list summarizing the cumulative arc across Phases 29, 30, 30.1, 34, 35, 37 (one item per sub-block above), each with one-line close summary + closed date.

    5. **`### Outcome` paragraph (~2-3 lines):** "3 plans executed across Wave 1 / Wave 2 / Wave 3; ADR-0010 body rewritten to milestone-final state + 6th and final v1.5 sub-block landed; 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed; ~N atomic commits land on `feature/37-adr-0010-acceptance-gate-closeout`. v1.5 milestone shipped; v1.6 promotion unblocked (999.5 first phase)."

    6. **Architecture-level numbers footer** (per PATTERNS lines 836-838, literal text in `<interfaces>` block) pointing to `docs/architecture/llm-pipeline-reliability.md` for the cumulative measurement story across Phases 30 / 30.1 / 34.

    7. **Out-of-scope-carries-forward block** (per PATTERNS lines 860-872, literal text in `<interfaces>` block) — 10 bullets covering 999.5, REVEAL-01/02, Cerebras+Groq adapter restoration, paid-OR conversion, adaptive Retry-After NIM limiter, per-provider eval + cascade_exhausted DLQ, ADR-0011 Phase 37 sub-block, OpenAPI full-spec audit + Zod-handler reconciliation, ROADMAP / REQUIREMENTS retroactive rewording.

    Do NOT touch the status line yet (Task 4 handles that). Do NOT touch the existing 4 v1.5 sub-blocks above. Do NOT touch the rewritten `## Consequences` / `## Alternatives Considered` / `## References` / Nygard footer below.

    Commit: `docs(37): D-03 D-04 append Phase 37 close sub-block (6th and final v1.5 sub-block) with D-rows + v1.5 Milestone Close Rollup`

  </action>
  <verify>
    <automated>
      grep -c '^## Phase 37 Close Sub-block' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 1
      grep -c '^### v1.5 Milestone Close Rollup$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 1
      grep -cE 'D-XX \(LLM-RELI-07\)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 1 — placeholder forward-reference row present
      grep -c 'Architecture-level numbers' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 6 — Phase 30 + 30.1 + 34 + 35 + 37 sub-blocks each carry this footer (5 of the 6 v1.5 sub-blocks; Phase 29 framing in body does not have one of its own)
      grep -c 'Out of scope (carries forward' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 5 — Phase 30 + 30.1 + 34 + 35 + 37 each have one
      grep -c '999.5 Performance Optimization' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 1 — Phase 37 Out-of-scope mentions it
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Phase 37 Close Sub-block' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c '^### v1.5 Milestone Close Rollup$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1`
    - `grep -c 'D-XX (LLM-RELI-07)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1` (placeholder for Plan 37-03 to finalize)
    - `grep -c 'Architecture-level numbers' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `5` (Phase 30 + 30.1 + 34 + 35 + 37 footers — original 4 preserved + new 1 added)
    - `grep -c '999.5 Performance Optimization' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1`
    - `grep -c 'REVEAL-01' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns at least `1` (Phase 37 Out-of-scope mentions it)
    - `awk '/^## Phase 37 Close Sub-block/,/^## Consequences/' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | wc -l` returns at least `120` (sub-block target is ~150-200 lines including its closing blank lines before `## Consequences`)
    - The 4 EARLIER v1.5 sub-block headings (Phase 30 / 30.1 / 34 / 35) are still present, each `grep -c` returns `1`
    - `grep -c '^## Consequences$' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `1` (the rewritten Consequences from Task 2 still present)
    - The Phase 37 sub-block heading appears BEFORE `## Consequences` in line order (verify with `awk '/^## Phase 37 Close Sub-block/{p37=NR} /^## Consequences/{cons=NR} END{print (p37 < cons) ? "OK" : "FAIL"}' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `OK`)
    - `git log -1 --pretty=%s` returns `docs(37): D-03 D-04 append Phase 37 close sub-block (6th and final v1.5 sub-block) with D-rows + v1.5 Milestone Close Rollup`
  </acceptance_criteria>
  <done>Phase 37 close sub-block appended as the 6th and final v1.5 sub-block beneath Phase 35; D-01..D-05 rows + placeholder LLM-RELI-07 forward-reference + v1.5 Milestone Close Rollup + Architecture-level numbers footer + Out-of-scope-carries-forward block all present; sub-block sits ABOVE the rewritten Consequences heading; commit landed with the `docs(37): D-03 D-04` prefix.</done>
</task>

<task type="auto">
  <name>Task 4: D-05 — Append second status line "Accepted (v1.5 closed YYYY-MM-DD)" below the original Status line</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md lines 1-10 (verify current Status/Date/Deciders block is intact after Tasks 1-3)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md lines 58-59 (D-05 specification — second status line below original; original preserved)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 38-45 (literal D-05 second-line append shape)
  </read_first>
  <action>
    Locate line 3 (`**Status:** Accepted`) — this is the ORIGINAL Phase 29-acceptance status line, preserved verbatim per CONTEXT D-05 + Claude's-discretion item ("preserve the existing line-3 `Status: Accepted` exactly — that IS the Phase 29-open date when the decision was Accepted").

    INSERT a NEW line directly below it (becomes the new line 4): `**Status:** Accepted (v1.5 closed YYYY-MM-DD)` — use today's date (the current ISO date, e.g. `2026-05-30`) as the YYYY-MM-DD placeholder. Plan 37-03 will rewrite this to the actual milestone-close date.

    The result MUST be exactly:
    ```
    # ADR-0010: v1.5 LLM pipeline narrowing and deletion

    **Status:** Accepted
    **Status:** Accepted (v1.5 closed YYYY-MM-DD)
    **Date:** 2026-05-11
    **Deciders:** solo author
    ```

    DO NOT modify line 3, line 5 (Date), or line 6 (Deciders). DO NOT modify any other line in the file.

    Commit: `docs(37): D-05 append second status line "Accepted (v1.5 closed YYYY-MM-DD)" below original Status line`

  </action>
  <verify>
    <automated>
      sed -n '3p' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST output: **Status:** Accepted
      sed -n '4p' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST output a string matching the regex: ^\*\*Status:\*\* Accepted \(v1\.5 closed 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]\)$
      sed -n '5p' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST output: **Date:** 2026-05-11
      sed -n '6p' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST output: **Deciders:** solo author
      grep -cE '^\*\*Status:\*\* Accepted' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 2
    </automated>
  </verify>
  <acceptance_criteria>
    - Line 3 of `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` reads exactly `**Status:** Accepted` (no parenthetical)
    - Line 4 of `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` matches the regex `^\*\*Status:\*\* Accepted \(v1\.5 closed 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]\)$` (a real ISO date placeholder)
    - Line 5 of `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` reads exactly `**Date:** 2026-05-11`
    - Line 6 of `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` reads exactly `**Deciders:** solo author`
    - `grep -cE '^\*\*Status:\*\*' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `2`
    - `git log -1 --pretty=%s` returns `docs(37): D-05 append second status line "Accepted (v1.5 closed YYYY-MM-DD)" below original Status line`
  </acceptance_criteria>
  <done>Second status line `**Status:** Accepted (v1.5 closed YYYY-MM-DD)` present as line 4 with today's-date placeholder; original line 3 `**Status:** Accepted` preserved verbatim; Date + Deciders lines shifted to 5 + 6 unchanged; commit landed with the `docs(37): D-05` prefix.</done>
</task>

<task type="auto">
  <name>Task 5: Verify-subblock-integrity — diff-check 5 historical sub-blocks against pre-rewrite state</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (current state after Tasks 1-4)
    - The git ref BEFORE this plan's first commit — use `git log --oneline -5 -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` to find the latest commit BEFORE this plan started touching the file; call that ref `$BASE`
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 26-148 (the preserved sub-block ranges + their exact heading strings)
  </read_first>
  <action>
    The 4 preserved v1.5 sub-blocks (Phase 30 / 30.1 / 34 / 35) MUST be byte-identical between $BASE and HEAD. Tasks 1-4 explicitly rewrote everything ABOVE the Phase 30 sub-block (line 52 originally) and BELOW the Phase 35 sub-block (line 148 originally) — the 4 sub-blocks themselves are untouched.

    Identify $BASE — use the grep-based lookup (the `~4` ancestor syntax does NOT apply to pathspecs; only to commit refs): walk back via `git log --oneline -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` and pick the commit JUST BEFORE the first `docs(37): D-01` commit. Mechanically: `BASE=$(git log --format=%H --before="$(git log --format=%aI -1 --grep='docs(37): D-01')" -1 -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md)`. Confirm `$BASE` is the most recent commit touching the file that pre-dates the Phase 37 work (e.g., a Phase 35 sub-block commit).

    Extract each preserved sub-block from $BASE and from HEAD, then diff:
    ```bash
    for HEADING in 'Phase 30 Sub-block' 'Phase 30.1 Sub-block' 'Phase 34 Sub-block' 'Phase 35 Sub-block'; do
      diff <(git show $BASE:docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | awk "/^## $HEADING/,/^## /" | sed '$d') \
           <(awk "/^## $HEADING/,/^## /" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | sed '$d')
    done
    ```

    EACH diff MUST produce empty output (zero changes). If any diff produces output, identify which sub-block was altered, determine which Task caused it, and FIX it before commit by checking out the original sub-block content via `git show $BASE:docs/adr/0010-...md` and patching the affected lines.

    If all 4 diffs are empty: this Task does NOT need its own commit (no file change). Instead, the executor records the verification result as a verification-gate observation in the commit message of an EMPTY commit-trailer-only commit:
    `chore(37): verify-subblock-integrity — 4 historical v1.5 sub-blocks (Phase 30 / 30.1 / 34 / 35) byte-identical between BASE=<sha> and HEAD`

    Use `git commit --allow-empty -m "..."` to record the verification step in the commit log without altering files.

  </action>
  <verify>
    <automated>
      BASE=$(git log --format=%H --before="$(git log --format=%aI -1 --grep='docs(37): D-01')" -1 -- docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md)
      for HEADING in 'Phase 30 Sub-block' 'Phase 30.1 Sub-block' 'Phase 34 Sub-block' 'Phase 35 Sub-block'; do
        diff <(git show $BASE:docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | awk "/^## $HEADING/,/^## /" | sed '$d') \
             <(awk "/^## $HEADING/,/^## /" docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | sed '$d') > /tmp/subblock-$HEADING.diff
        test ! -s /tmp/subblock-$HEADING.diff && echo "OK: $HEADING unchanged" || (echo "FAIL: $HEADING drifted"; cat /tmp/subblock-$HEADING.diff)
      done
    </automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 diffs (Phase 30 / 30.1 / 34 / 35) produces zero output
    - `git log -1 --pretty=%s` returns `chore(37): verify-subblock-integrity — 4 historical v1.5 sub-blocks (Phase 30 / 30.1 / 34 / 35) byte-identical between BASE=<sha> and HEAD`
    - `git log -1 --pretty=%H` shows a new commit (the --allow-empty marker commit)
  </acceptance_criteria>
  <done>4 historical sub-blocks (Phase 30 / 30.1 / 34 / 35) verified byte-identical between pre-Plan-37-01 state and HEAD via per-heading diff; verification recorded as an empty commit with the `chore(37): verify-subblock-integrity` prefix.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                                |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| author→ADR            | Solo author edits the canonical milestone-final ADR. No external untrusted input crosses.                                  |
| ADR-reader→cross-link | Future readers click cross-links to `.planning/` paths + `docs/architecture/` paths. Broken links degrade the audit trail. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                                                      | Disposition | Mitigation Plan                                                                                                                                                                                          |
| --------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-37-01   | Tampering              | Historical sub-blocks (Phase 30 / 30.1 / 34 / 35) accidentally rewritten during Context/Decision/Consequences rewrite                          | mitigate    | Task 5 diff-checks each preserved sub-block byte-for-byte against pre-Plan-37-01 state; CI gate (`npx vitest run` + `npm run docs:lint` in Plan 37-03) catches any cross-link rot caused by line shifts. |
| T-37-02   | Information disclosure | None — ADR is public-facing project history, no secrets                                                                                        | accept      | Standard public-docs posture; no PII, no credentials.                                                                                                                                                    |
| T-37-03   | Tampering              | `<expand_at_36>` marker left in place by mistake → ADR-0010 carries an internal-construction-marker into the milestone-final canonical version | mitigate    | Task 2 acceptance criterion `grep -c "expand_at_36"` MUST return 0; Plan 37-03 verification gate `npm run docs:lint` catches dangling references; Plan 37-03 also runs the 3-gate verification trio.     |
| T-37-04   | Tampering              | Status line drift — second status line replaces the first instead of appending below it                                                        | mitigate    | Task 4 acceptance criteria pin exact line numbers (line 3 = original `Status: Accepted` verbatim; line 4 = new second line). `grep -cE '^\*\*Status:\*\*'` MUST return `2`.                              |
| T-37-SC   | Tampering              | npm/pip/cargo installs                                                                                                                         | accept      | No package installs in this plan; pure docs/markdown edits.                                                                                                                                              |

</threat_model>

<verification>
Plan 37-01 completes when:
- `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` body sections (Context, Decision, Consequences, Alternatives Considered, References) describe milestone-final shipped state
- 4 v1.5 historical sub-blocks (Phase 30 / 30.1 / 34 / 35) byte-identical between $BASE and HEAD (Task 5 diff-check)
- `<expand_at_36>` marker absent (`grep -c "expand_at_36"` returns `0`)
- Status block: line 3 = original `**Status:** Accepted`, line 4 = new `**Status:** Accepted (v1.5 closed YYYY-MM-DD)` with today's-date placeholder; `grep -cE '^\*\*Status:\*\*'` returns `2`
- Phase 37 close sub-block heading present (`grep -c '^## Phase 37 Close Sub-block'` returns `1`); v1.5 Milestone Close Rollup subsection present; placeholder `D-XX (LLM-RELI-07)` row present
- Architecture-level numbers footer + Out-of-scope-carries-forward block both present in the Phase 37 sub-block
- 5 atomic commits landed on the phase branch with these subjects (in order):
  1. `docs(37): D-01 rewrite ADR-0010 Context + Decision to milestone-final shipped state`
  2. `docs(37): D-02 absorb expand_at_36 marker + rewrite Consequences/Alternatives/References to milestone-final reality`
  3. `docs(37): D-03 D-04 append Phase 37 close sub-block (6th and final v1.5 sub-block) with D-rows + v1.5 Milestone Close Rollup`
  4. `docs(37): D-05 append second status line "Accepted (v1.5 closed YYYY-MM-DD)" below original Status line`
  5. `chore(37): verify-subblock-integrity — 4 historical v1.5 sub-blocks (Phase 30 / 30.1 / 34 / 35) byte-identical between BASE=<sha> and HEAD`
- File line count ≥ 350 (was 207 originally; ~150-200 line append for Phase 37 sub-block + ~30-50 line expansion for Consequences/Alternatives/References)
</verification>

<success_criteria>

- DOCS-PUB-04 requirement is materially advanced — ADR-0010 body now describes milestone-final shipped state and the Phase 37 close sub-block is in place (final flip to Complete happens in Plan 37-03's tracking-flip task after Plan 37-02 captures the LLM-RELI-07 gate evidence + Plan 37-03 finalizes the YYYY-MM-DD placeholder dates).
- The ADR can be read as a single canonical end-to-end document: the body answers "what did v1.5 ship?", and the 6 sub-blocks read as the historical decision trail showing how the milestone-final state was reached.
- Plan 37-02 has a stable cross-link target (`37-SUMMARY.md §Acceptance Gate Observation`) to write into when it captures the 3-consecutive-green evidence triplets.
- Plan 37-03 has 2 stable YYYY-MM-DD placeholders to rewrite (one in the line-4 status line per Task 4, one in the Phase 37 sub-block heading per Task 3) when the actual milestone-close date is known.
  </success_criteria>

<output>
Create `.planning/phases/37-adr-0010-acceptance-gate-closeout/37-01-SUMMARY.md` when done. Capture:
- Pre-state line count (207)
- Post-state line count
- 5 commit SHAs + subjects (in order)
- 4 preserved-sub-block diff results (each "OK: byte-identical")
- 2 outstanding placeholders for Plan 37-03 (`YYYY-MM-DD` in line 4 status; `YYYY-MM-DD` in Phase 37 sub-block heading)
- The forward-reference target Plan 37-02 will write into (`37-SUMMARY.md#acceptance-gate-observation-llm-reli-07`)
</output>

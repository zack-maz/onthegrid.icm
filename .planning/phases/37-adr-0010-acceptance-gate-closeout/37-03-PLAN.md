---
phase: 37-adr-0010-acceptance-gate-closeout
plan: 03
type: execute
wave: 3
depends_on:
  - '37-02'
files_modified:
  - CHANGELOG.md
  - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
autonomous: true
requirements:
  - DOCS-PUB-04
  - LLM-RELI-07
tags:
  - milestone-close
  - changelog
  - phase-rollup
  - framing-gap
  - quantitative-snapshot
  - tracking-flips
  - three-gate-verification

must_haves:
  truths:
    - "CHANGELOG.md has a `## [v1.5]` entry inserted ABOVE the existing `## [v1.4]` entry, mirroring v1.4's section structure (Headline deliverables / Quantitative snapshot / Migration notes / Deferred to v1.6+)"
    - '37-SUMMARY.md is complete: frontmatter flipped from draft to final + opener + §Acceptance Gate Observation (from Plan 37-02) + v1.5 Phase Rollup (10 rows) + Framing-Gap Callouts (7 rows: 6 carryover + 1 new) + v1.5 Quantitative Snapshot (7 metrics) + v1.6 Promotion Readiness + Decision-by-Decision Outcome closing table (~19 rows D-01..D-19)'
    - 'ADR-0010 status second line finalized with actual milestone-close date (placeholder YYYY-MM-DD replaced) AND Phase 37 close sub-block heading date finalized AND LLM-RELI-07 placeholder D-XX row rewritten as a final D-N row pointing to the 3 evidence triplets in 37-SUMMARY.md'
    - 'ROADMAP.md Phase 37 row checkbox flipped to `[x]`; closing language appended; Progress table row updated to `3/3 | Complete | YYYY-MM-DD`; milestone-summary heading flipped to `✅ SHIPPED YYYY-MM-DD`'
    - 'REQUIREMENTS.md DOCS-PUB-04 + LLM-RELI-07 checklist rows flipped to `[x]` with close-language inline; Traceability table rows for both requirements flipped from `Pending` to `Complete (YYYY-MM-DD)`; footer line updated to reflect Phase 37 close'
    - 'STATE.md frontmatter + Current Position prose block flipped to v1.5 shipped / v1.6 unblocked state'
    - 'All 3 mechanical verification gates green: `npx vitest run` (exit 0), `npx @redocly/cli lint server/openapi.yaml --format=stylish` (exit 0), `npm run docs:lint` (exit 0)'
    - 'Manual spot-check verifies every `.planning/` cross-link added to ADR-0010 in Plan 37-01 resolves to an existing file (test -f passes for each)'
  artifacts:
    - path: 'CHANGELOG.md'
      provides: 'v1.5 milestone entry (~50-90 lines) inserted ABOVE the v1.4 entry'
      contains:
        - '## [v1.5]'
        - '### Headline deliverables'
        - '### Quantitative snapshot'
        - '### Migration notes (v1.4 → v1.5)'
        - '### Deferred to v1.6+ (carried forward)'
    - path: '.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md'
      provides: 'Final phase close summary (~300-450 lines) with all required sections'
      contains:
        - '## Acceptance Gate Observation (LLM-RELI-07)'
        - '## v1.5 Phase Rollup'
        - '## Framing-Gap Callouts'
        - '## v1.5 Quantitative Snapshot'
        - '## v1.6 Promotion Readiness'
        - '## Decision-by-Decision Outcome'
        - '## 3-Gate Verification Results'
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 37 row flipped + Progress table updated + milestone heading flipped'
      contains:
        - '- [x] **Phase 37: ADR-0010 + Acceptance Gate Closeout**'
        - '✅ SHIPPED'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'DOCS-PUB-04 + LLM-RELI-07 flipped + Traceability table updated + footer updated'
      contains:
        - '- [x] **DOCS-PUB-04**'
        - '- [x] **LLM-RELI-07**'
    - path: '.planning/STATE.md'
      provides: 'v1.5 closed / v1.6 unblocked state'
      contains:
        - 'v1.5 LLM Reliability & Reveal Prep milestone closed'
    - path: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      provides: 'Final milestone-close date in status line 4 and Phase 37 sub-block heading; LLM-RELI-07 placeholder D-XX row finalized'
  key_links:
    - from: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md Phase 37 close sub-block (D-N row for LLM-RELI-07)'
      to: '.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md §Acceptance Gate Observation'
      via: 'markdown link'
      pattern: "37-SUMMARY\\.md#acceptance-gate-observation"
    - from: '.planning/STATE.md Current Position resume file'
      to: '.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md'
      via: 'markdown link'
      pattern: "37-SUMMARY\\.md"
---

<objective>
Complete the v1.5 milestone close ritual: write the CHANGELOG[v1.5] entry, complete 37-SUMMARY.md (all 5 remaining sections plus 3-gate verification block), finalize ADR-0010 placeholder dates + LLM-RELI-07 row, and flip ROADMAP / REQUIREMENTS / STATE tracking to v1.5-shipped state.

Purpose: Lock the v1.5 milestone with auditable artifacts at every surface — changelog, summary, ADR, roadmap, requirements, state — so v1.6 promotion (999.5 first phase) is unblocked with a clean handoff.

Output: 6 files modified (CHANGELOG.md + 37-SUMMARY.md + ROADMAP.md + REQUIREMENTS.md + STATE.md + docs/adr/0010-...md); ~9-11 atomic per-decision commits; all 3 mechanical verification gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-01-PLAN.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-02-PLAN.md
@.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
@CHANGELOG.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md
@package.json

<interfaces>
<!-- Key string-level + structural contracts the executor needs. -->

PHASE_CLOSE_DATE: today's actual ISO date (resolved at execution time — used to replace all `YYYY-MM-DD` placeholders landed by Plan 37-01).

CHANGELOG[v1.5] entry shape (insert ABOVE the existing `## [v1.4]` entry on line 5 of CHANGELOG.md — see PATTERNS lines 362-441 for the full literal template). The entry follows v1.4's structure exactly:

```markdown
## [v1.5] — LLM Reliability & Reveal Prep — YYYY-MM-DD

**Span:** 2026-05-09 → YYYY-MM-DD (~N days, 10 phases shipped — 29 / 30 / 30.1 / 31 / 32 / 33 / 34 / 35 / 36 / 37)

[1-paragraph milestone framing — NIM-only honest at runtime; v1+v2 extractors deleted Phase 29; LLM-optional architecture proven; Pro upgrade landed; public docs swept; OpenAPI gates landed; mechanical drift gates added (Redis registry + OpenAPI lint + markdown-link-check); 999.5 load test promotion gate observed at v1.5 close.]

### Headline deliverables

- **LLM Reliability (Phases 29 / 30 / 30.1 / 34):** ...
- **Ghost Event Cleanup (Phase 32):** ...
- **Actor Metadata (Phase 33):** ...
- **Internal Docs + Redis Optimization (Phase 35):** ...
- **Pipeline Simplification (Phases 29 / 30 / 35):** ...
- **Public Docs (Phases 36 / 37):** ...
- **OpenAPI Additions (Phase 36):** ...
- **Acceptance Gate (Phase 37):** 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed YYYY-MM-DD..YYYY-MM-DD; LLM-RELI-07 satisfied; 999.5 load test unblocked for v1.6 promotion.

### Quantitative snapshot

- vitest baseline: 2193 → ~2380 (+~187 net new tests across v1.5)
- TypeScript errors: 0 → 0 (held)
- Bundle: 1.72 MB → ~1,790,243 bytes (+~10,739 bytes over v1.5; JSDoc additions dominated — Phase 35 D-19 measurement)
- Cron jobs: 3 → 3 (Hobby cap respected through v1.5; Pro upgrade lifted maxDuration not cron count)
- ADRs: 8 → 11 (ADR-0009 Phase 27.4.3 + ADR-0010 + ADR-0011 = 3 net adds)
- Redis key registry: 32 keys at v1.5 close (deep-dive at `docs/architecture/redis-keys.md`)
- Commits across v1.5 phases: ~N (rough sum from `git log --oneline` per phase branch)

### Migration notes (v1.4 → v1.5)

- **Vercel Pro plan required:** `vercel.json` `functions["api/vercel-entry.js"].maxDuration` bumped 300 → 800. Phase 29 D-08 locked this dependency.
- **CLAUDE.md trimmed (Phase 29 DOCS-INT-01):** phase-history bloat condensed to current-state invariants; ~5018-token operator budget preserved through v1.5.
- **v1 + v2 extractor modules deleted (Phase 29 D-02; supersedes Phase 27.4 D-26/D-40):** rollback path is now `git revert <Phase 29 commit range>`, not flip a runtime flag. `events:llm-pipeline-override` Redis key + endpoint + DevApiStatus Pin-to-v1/v2 buttons all removed.
- **`events:llm:v3:partial` retired (Phase 35 SIMPLIFY-02):** 358 LOC removed; production cleanup via natural TTL expiry.
- **OpenRouter dormant + Cerebras / Groq deferred (Phase 30.1 + 34):** Single-provider NIM-only cascade; Phase 31 Day-1 DLQ baseline (4 × `v3:timeout_watchdog`) is a known failure mode. See ADR-0010 Phase 30.1 + 34 sub-blocks.

### Deferred to v1.6+ (carried forward)

- **999.5 Performance Optimization + 1-300 VU k6 sweep:** unblocked by Phase 37 acceptance gate; first phase of v1.6.
- **REVEAL-01 polish** (landing page, demo flows, social-share assets, hero GIF): v1.6 territory.
- **REVEAL-02 public domain:** v1.6 milestone-open scoping question.
- **Cerebras + Groq adapter restoration:** future provider-restoration phase per ADR-0010 Phase 34 sub-block follow-up candidates.
- **Paid-OR conversion** (~$0.04/day = ~$1.20/mo for full coverage): per ADR-0010 Phase 30.1 sub-block.
- **Adaptive Retry-After-aware NIM limiter:** per ADR-0010 Phase 30 sub-block `retryAfterMs` field already on `callHistory`.
- **Per-provider eval infrastructure + `cascade_exhausted` DLQ taxonomy:** deferred alongside provider restoration.
- (carried from v1.4) Phase 27.3.3 water-name romanization; 999.2 build-artifact discipline; 999.3 cron first-tick verification; 999.4 cron route hydrates pipeline override; Telegram OSINT; GDELT BigQuery; satellite imagery overlay.

---
```

§v1.5 Phase Rollup table (D-14 — 10 rows; one per v1.5 phase; 6 columns):

```markdown
## v1.5 Phase Rollup

| Phase | Name                                                                                           | Status                          | Closed     | Requirements Closed                                                      | Notes                                                                           |
| ----- | ---------------------------------------------------------------------------------------------- | ------------------------------- | ---------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------- |
| 29    | LLM Provider Chain Narrowing + LLM-Optional Architecture + Vercel Pro Upgrade + CLAUDE.md Trim | Complete                        | 2026-05-11 | LLM-RELI-01, LLM-RELI-05, SIMPLIFY-04, SIMPLIFY-06, DOCS-INT-01          | First v1.5 phase; cascade narrowed; v1+v2 deleted; Pro upgrade landed           |
| 30    | NIM Throttle Characterization + Cascade Tuning + Pro-Enabled Simplifications                   | Complete                        | 2026-05-17 | LLM-RELI-02, LLM-RELI-03, LLM-RELI-04, SIMPLIFY-01, SIMPLIFY-03          | Path B (no Retry-After); defensive defaults; SIMPLIFY-01 + SIMPLIFY-03 retired  |
| 30.1  | Cascade fallback fix — NIM-only declared honest                                                | Complete                        | 2026-05-17 | (gap closure)                                                            | OpenRouter dormant per 27/30 probe; cascade declared NIM-only                   |
| 31    | Cron Stability Validation (7-day Watch)                                                        | Closed early Day 1 / 7          | 2026-05-19 | LLM-RELI-06 (caveat)                                                     | Day-1 natural cron PASS; 7-day bar not met; caveat documented                   |
| 32    | Ghost Event URL Liveness, Dashboard & Prune                                                    | Complete                        | 2026-05-21 | GHOST-01..05                                                             | 22 D-N decisions; +63 tests; chaos-test contract preserved                      |
| 33    | Actor Metadata Audit, Canonical Catalog & Eval Expansion                                       | Complete                        | 2026-05-21 | ACTOR-01..05                                                             | 27-entry canonical catalog; actorConfidence + actorMatchRate landed             |
| 34    | LLM Router Fallback Re-integration (Cerebras / Groq + Per-Provider Eval)                       | Closed `cerebras-groq-deferred` | 2026-05-23 | LLM-RELI-08..11                                                          | Operator deferral; no code lands; honest close-out                              |
| 35    | Internal Docs (JSDoc) + Redis Registry + Redis Optimization + Cleanup Sweep                    | Complete                        | 2026-05-27 | DOCS-INT-02..03, REDIS-OPT-01..04, SIMPLIFY-02, SIMPLIFY-05, SIMPLIFY-07 | Drift gate at redis-registry.test.ts; partial-key retired; +10,739 bytes bundle |
| 36    | Public Docs Sweep + OpenAPI Additions                                                          | Complete                        | 2026-05-30 | DOCS-PUB-01/02/03/05, DOCS-API-01..07                                    | README sweep; 12 arch docs + 21 Mermaid; OpenAPI lint gate; markdown-link-check |
| 37    | ADR-0010 + Acceptance Gate Closeout                                                            | Complete                        | YYYY-MM-DD | DOCS-PUB-04, LLM-RELI-07                                                 | Milestone close gate observed; v1.6 promotion unblocked                         |
```

§Framing-Gap Callouts section (D-15 — 6 carryover from Phase 36 + 1 new for Phase 37 cadence interpretation). The 6 carryover rows MUST be copied verbatim from `.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md` `## Framing-Gap Callouts` section. The literal carryover text is shown in PATTERNS lines 261-300. Row #7 shape (PATTERNS lines 306-313):

```markdown
### 7. Acceptance-gate cadence — back-to-back vs 24-48h-spaced (Phase 37 D-08 NOTE)

**Surfaced by:** Plan 37-02 execution.
**Planning text:** CONTEXT.md D-06 specifies "1 run per day across 24-48 hours" for the 3-consecutive-green observation; D-08 NOTE permits operator to compress to back-to-back if rationale is logged.
**Shipped reality:** [either "Cadence ran as specified — no deviation"; OR "Operator compressed to back-to-back ~N min apart; rationale: <as logged in Plan 37-02 draft>"]

**Why preserved:** Framing-gap-in-SUMMARY policy (Phase 36 D-04 inherited).
```

§v1.5 Quantitative Snapshot subsection (D-16 — 7 metrics). The exact metric list:

1. **test count delta** baseline→close (Phase 36 close was ~2380; Phase 37 adds 0 → delta = 0 in Phase 37; v1.5-wide delta is +~187 over v1.4's 2193 baseline)
2. **bundle delta** (`api/vercel-entry.js` bytes; Phase 35 close was 1,790,243 bytes; Phase 37 adds 0 code so Phase 37 delta is 0)
3. **ADR count delta** (v1.4 close had 8 ADRs in `docs/adr/`; v1.5 added ADR-0009 Phase 27.4.3 + ADR-0010 + ADR-0011 = 3 net adds = 11 total at v1.5 close)
4. **Redis key registry growth** (Phase 35 close had 32 keys; any Phase 36/37 additions captured here; expected delta = 0)
5. **cron job count** (still 3 within Hobby cap → Pro cap; unchanged)
6. **commit count across all v1.5 phases** (sum via `git log --oneline feature/29-* feature/30-* ... feature/37-*`; rough estimate acceptable)
7. **operator hours spent** (rough estimate from per-phase SUMMARY hours-spent rows; absence-of-data → estimate based on commit count × ~15-30 min/commit)

§v1.6 Promotion Readiness subsection (D-17 — explicit statement, literal text per CONTEXT D-17):

```markdown
## v1.6 Promotion Readiness

v1.6 promotion is unblocked. 999.5 (Performance Optimization + 1-300 VU k6 sweep) can be pulled from `.planning/phases/999.5-performance-load-test/` into v1.6 as the first phase. v1.5 acceptance gate (3× consecutive `prod-connectivity-audit.yml` exit-0 with `allTiersGreen=true`) observed on YYYY-MM-DD; evidence at §Acceptance Gate Observation above. REVEAL-01 polish + REVEAL-02 public domain are v1.6 scope items per `.planning/REQUIREMENTS.md` §v2 Requirements.
```

§Decision-by-Decision Outcome table (D-19 — 5-column shape; ~19 rows for D-01..D-19; placeholder `TBD` for commit refs filled at PR merge time):

```markdown
## Decision-by-Decision Outcome

| D-N  | Decision                                                                   | Plan                   | Status    | Commit Ref                                                                                  |
| ---- | -------------------------------------------------------------------------- | ---------------------- | --------- | ------------------------------------------------------------------------------------------- |
| D-01 | Full ADR-0010 rewrite + 5 sub-blocks preserved                             | 37-01                  | landed    | `docs(37): D-01 ...` (SHA TBD at PR merge)                                                  |
| D-02 | Absorb expand_at_36 marker + rewrite Consequences/Alternatives/References  | 37-01                  | landed    | `docs(37): D-02 ...` (SHA TBD)                                                              |
| D-03 | Inline citation per D-N row in close sub-block                             | 37-01                  | landed    | folded into 37-01 D-03 D-04 commit (SHA TBD)                                                |
| D-04 | Phase 37 close sub-block content + v1.5 Milestone Close Rollup             | 37-01                  | landed    | `docs(37): D-03 D-04 ...` (SHA TBD)                                                         |
| D-05 | Status line gains second line                                              | 37-01 + 37-03 finalize | landed    | `docs(37): D-05 ...` placeholder; `docs(37): D-05 finalize close date YYYY-MM-DD` (SHA TBD) |
| D-06 | Cadence — 1 run per day across 24-48 hours                                 | 37-02                  | landed    | `chore(37): D-06 D-07 D-08 ...` (SHA TBD)                                                   |
| D-07 | Hard reset on any red mid-sequence                                         | 37-02                  | landed    | (same commit as D-06)                                                                       |
| D-08 | Evidence inline in 37-SUMMARY.md — URL + payload triplet                   | 37-02                  | landed    | (same commit as D-06)                                                                       |
| D-09 | Operator triggers manually — documented as out-of-band step                | 37-02                  | landed    | structural (no commit; Plan 37-02 Tasks 1/2/3 honored)                                      |
| D-10 | 3 plans — ADR / gate / close                                               | (planning)             | satisfied | (no commit; plan structure honored)                                                         |
| D-11 | Strictly sequential 01 → 02 → 03                                           | (planning)             | satisfied | (no commit; wave numbers honored)                                                           |
| D-12 | Branch + per-plan commit discipline — inherit Phase 36 verbatim            | (planning)             | satisfied | (no commit; conventional commits honored)                                                   |
| D-13 | Verification gates inherit Phase 36 D-24                                   | 37-03                  | landed    | `chore(37): run 3-gate verification ...` (SHA TBD)                                          |
| D-14 | Per-phase rollup table for all 10 v1.5 phases                              | 37-03                  | landed    | `docs(37): D-14 ...` (SHA TBD)                                                              |
| D-15 | Framing-gap callouts — inherit Phase 36's 6 + add 1 new                    | 37-03                  | landed    | `docs(37): D-15 ...` (SHA TBD)                                                              |
| D-16 | v1.5 quantitative snapshot — follows CHANGELOG[v1.4] entry shape           | 37-03                  | landed    | `docs(37): D-16 ...` (SHA TBD)                                                              |
| D-17 | v1.6 promotion readiness statement                                         | 37-03                  | landed    | `docs(37): D-17 ...` (SHA TBD)                                                              |
| D-18 | CHANGELOG[v1.5] inline in Plan 37-03 — mirrors CHANGELOG[v1.4] entry shape | 37-03                  | landed    | `docs(37): D-18 CHANGELOG[v1.5] milestone entry` (SHA TBD)                                  |
| D-19 | Closing-table format for D-N rows in 37-SUMMARY.md                         | 37-03                  | landed    | (this table itself; same commit as D-14..D-17 or its own)                                   |
```

§3-Gate Verification Results block (D-13; per PATTERNS lines 317-336):

```markdown
## 3-Gate Verification Results (D-13 — inherits Phase 36 D-24)

### Gate 1: Full test suite — `npx vitest run`

**Result:** ✓ PASSED — **<N> test files / <N> tests passed**; 0 failed.
**Run timestamp:** YYYY-MM-DD HH:MM UTC.

### Gate 2: OpenAPI spec lint — `npx @redocly/cli lint server/openapi.yaml --format=stylish`

**Result:** ✓ PASSED — 0 errors.
**Run timestamp:** YYYY-MM-DD HH:MM UTC.

### Gate 3: Markdown link check — `npm run docs:lint`

**Result:** ✓ PASSED for Phase-37-introduced surface (ADR-0010 rewrite cross-links).
**Scope:** README.md + `docs/architecture/**/*.md` + `docs/runbook.md` + `docs/degradation.md` + `docs/adr/*.md`. (NOT `.planning/`; NOT `docs/brainstorms/`; NOT `docs/superpowers/`.)
**Note:** `.planning/` cross-links from ADR-0010 are OUT of `docs:lint` scope. Manual spot-check executed — see §Manual `.planning/` cross-link spot-check below.
**Run timestamp:** YYYY-MM-DD HH:MM UTC.

### Manual `.planning/` cross-link spot-check (per Phase 36 D-24 note + PATTERNS line 756)

| ADR-0010 cross-link target                                                                       | `test -f` result |
| ------------------------------------------------------------------------------------------------ | ---------------- |
| `.planning/phases/29-llm-provider-chain-narrowing-llm-optional-architecture-verce/29-CONTEXT.md` | OK               |
| `.planning/phases/30-nim-throttle-characterization-cascade-tuning-pro-enabled-sim/30-CONTEXT.md` | OK               |
| ... (one row per `.planning/` cross-link in the rewritten References block) ...                  |
```

ADR-0010 finalize edits (Plan 37-03 re-touches the ADR):

1. Line 4 placeholder: replace `**Status:** Accepted (v1.5 closed YYYY-MM-DD)` with `**Status:** Accepted (v1.5 closed PHASE_CLOSE_DATE)` where PHASE_CLOSE_DATE is today's date in ISO `YYYY-MM-DD` format
2. Phase 37 close sub-block heading: replace `## Phase 37 Close Sub-block (appended YYYY-MM-DD)` with the actual date
3. Phase 37 close sub-block placeholder D-XX row for LLM-RELI-07: rewrite from
   ```
   - **D-XX (LLM-RELI-07): see [`37-SUMMARY.md §Acceptance Gate Observation`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md#acceptance-gate-observation-llm-reli-07) — finalized in Plan 37-03.**
   ```
   to a final D-N row (e.g. D-06 within the sub-block — chose the next available D-N number after D-05; verify the actual D-N number based on what Plan 37-01 landed):
   ```
   - **D-06 (LLM-RELI-07): 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 with `allTiersGreen === true` on every sidecar payload. Evidence triplets (URL + ISO timestamp + JSON payload) inline in [`37-SUMMARY.md §Acceptance Gate Observation`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md#acceptance-gate-observation-llm-reli-07). v1.6 promotion unblocked.**
   ```
4. Phase 37 close sub-block v1.5 Milestone Close Rollup `6. **Phase 37**` bullet: replace the `(Closed YYYY-MM-DD.)` placeholder with the actual close date

ROADMAP.md flips (per PATTERNS lines 568-602):

1. Phase 37 row (~line 113): change `- [ ]` → `- [x]`; append closing language per PATTERNS lines 578-579 (` — ✓ CLOSED PHASE_CLOSE_DATE. ADR-0010 milestone-final rewrite + Phase 37 close sub-block (the 6th and final v1.5 sub-block); ... v1.6 promotion unblocked (999.5 first phase).`)
2. `### Progress` table row for Phase 37 (~line 591): change `0/0 | Not started | -` → `3/3 | Complete | PHASE_CLOSE_DATE`
3. Milestone-summary heading (~line 96): change `## Milestone v1.5: LLM Reliability & Reveal Prep — 🚧 IN PROGRESS` → `## Milestone v1.5: LLM Reliability & Reveal Prep — ✅ SHIPPED PHASE_CLOSE_DATE`; append a `Closed: PHASE_CLOSE_DATE` line below the existing `Started: 2026-05-09` line; update the Acceptance gate line to `OBSERVED PHASE_CLOSE_DATE..PHASE_CLOSE_DATE_RUN_3 — 3 consecutive prod-connectivity-audit.yml exit-0 runs with allTiersGreen=true (LLM-RELI-07 satisfied; 999.5 load test promotion to v1.6 unblocked).`

REQUIREMENTS.md flips (per PATTERNS lines 612-651):

1. DOCS-PUB-04 checklist row (~line 71): change `- [ ]` → `- [x]`; replace the `**(Phase 37 territory — ...)**` parenthetical with `**(Phase 37 close PHASE_CLOSE_DATE: ADR-0010 body rewrite to milestone-final shipped state + 5 historical sub-blocks preserved + Phase 37 close sub-block appended as 6th and final v1.5 sub-block + status line gained second line "Accepted (v1.5 closed PHASE_CLOSE_DATE)"; commits <range TBD at PR merge>.)**`
2. LLM-RELI-07 checklist row (~line 19): change `- [ ]` → `- [x]`; append `**(Phase 37 close PHASE_CLOSE_DATE: 3 consecutive greens observed 1-per-day across 24-48 hours per CONTEXT D-06 [OR — back-to-back per D-08 NOTE if cadence deviated]; evidence triplets — GH Actions run URL + ISO timestamp + sidecar JSON payload — inline in 37-SUMMARY.md §Acceptance Gate Observation. v1.6 promotion unblocked.)**`
3. Traceability table: 2 rows flip from `Pending` to `Complete (PHASE_CLOSE_DATE)` (LLM-RELI-07 row ~line 139; DOCS-PUB-04 row ~line 171)
4. Footer line (~line 191): append a new line: `_Last updated: PHASE_CLOSE_DATE after Phase 37 close — DOCS-PUB-04 + LLM-RELI-07 flipped to Complete; v1.5 milestone shipped; v1.6 promotion unblocked (999.5 first phase)._`

STATE.md flips (per PATTERNS lines 660-723):

1. Frontmatter (lines 1-14):
   - `milestone_name`: `LLM Reliability & Reveal Prep — ✅ SHIPPED PHASE_CLOSE_DATE`
   - `status`: `v1.5 milestone closed PHASE_CLOSE_DATE via Phase 37; ADR-0010 milestone-final rewrite + close sub-block landed; 3 consecutive prod-connectivity-audit.yml greens observed; CHANGELOG[v1.5] entry committed; v1.6 promotion unblocked (999.5 first phase).`
   - `last_updated`: `PHASE_CLOSE_DATE` + `T00:00:00.000Z`
   - `last_activity`: `PHASE_CLOSE_DATE -- Phase 37 closed; v1.5 milestone shipped; ROADMAP/REQUIREMENTS/STATE flips committed (<SHA>)`
   - `progress.completed_phases`: 9 (was 8)
   - `progress.completed_plans`: 58 (was 55, +3 from Phase 37)
   - `progress.percent`: recompute (= round(58/59 × 100) = 98)
2. Current Position prose block (lines 24-39): rewrite to v1.5-closed / v1.6-unblocked state per PATTERNS lines 710-723.

Verification gate commands (D-13):

```bash
# Gate 1: must exit 0
npx vitest run

# Gate 2: must exit 0
npx @redocly/cli lint server/openapi.yaml --format=stylish

# Gate 3: must exit 0
npm run docs:lint
```

Phase 37 task ordering note:

- Task 1 (CHANGELOG[v1.5]) is independent of the others; could run anywhere but per CONTEXT D-18 lands as a single atomic commit.
- Task 2 (37-SUMMARY.md completion) builds on Plan 37-02's draft.
- Task 3 (ADR-0010 finalize) is small and tightly coupled to the actual close date.
- Task 4 (ROADMAP/REQUIREMENTS/STATE flips) is the last semantic edit before verification gates.
- Task 5 (3-gate verification + manual `.planning/` spot-check) runs AFTER all file edits; captures results into 37-SUMMARY.md and commits the all-green observation.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: D-18 — Write CHANGELOG[v1.5] entry above the existing v1.4 entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-50 (the existing v1.4 entry — the literal template for the v1.5 entry per CONTEXT D-18 + PATTERNS lines 357-441)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md lines 88 + 90 (D-16 + D-18 specifications — exact contents of the v1.5 quantitative snapshot + migration notes)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 372-441 (Phase 37 adaptation of the v1.4 template — exact bullet sets for headline deliverables + migration notes + deferred)
    - .planning/ROADMAP.md lines 96-115 (the v1.5 milestone summary + 10 phase rows — source for the per-phase headline-deliverable bullets)
    - .planning/REQUIREMENTS.md lines 7-83 (the 8 track headings + their requirements — source for the per-track headline bullet structure)
  </read_first>
  <action>
    INSERT a new v1.5 entry into `CHANGELOG.md` ABOVE the existing `## [v1.4]` entry on line 5. The insertion preserves the file's `# Changelog` header (line 1-3) verbatim. The new entry sits between line 3 and the current line 5 (which becomes line N+1 after insertion).

    Use today's date for the entry's date stamp (`YYYY-MM-DD`).

    Structure of the entry follows v1.4's section structure EXACTLY:
    1. Title line: `## [v1.5] — LLM Reliability & Reveal Prep — YYYY-MM-DD`
    2. Span line: `**Span:** 2026-05-09 → YYYY-MM-DD (~22 days, 10 phases shipped — 29 / 30 / 30.1 / 31 / 32 / 33 / 34 / 35 / 36 / 37)` (compute the day count from the span: 2026-05-09 to today's date)
    3. 1-paragraph milestone framing (~5-8 lines): NIM-only honest at runtime; v1+v2 extractors deleted Phase 29; LLM-optional architecture proven; Pro upgrade landed; public docs swept; OpenAPI gates landed; mechanical drift gates added (Redis registry + OpenAPI lint + markdown-link-check); 999.5 load test promotion gate observed at v1.5 close.
    4. `### Headline deliverables` section: 8 bullets organized by track per CONTEXT D-18:
       - **LLM Reliability (Phases 29 / 30 / 30.1 / 34):** cascade narrowed to NIM-only at runtime (OpenRouter dormant per Phase 30.1 90% rate_limit probe; Cerebras + Groq deferred per Phase 34 operator choice); NIM throttle characterized (Path B — no Retry-After headers); defensive defaults committed (`LLM_BATCH_TIMEOUT_MS=120000`, `RETRY_ATTEMPTS=3`, `BACKOFF_MS=[2000,8000,32000]`, `JITTER_MS=500`); 7-day cron watch closed early Day 1 / 7 (Phase 31 caveat).
       - **Ghost Event Cleanup (Phase 32):** URL liveness probe out-of-band of `/api/events`; per-event Redis sidecar `events:url-liveness:{eventId}` (tiered TTL 7d/24h/1h); operator-Bearer-gated prune endpoint; API Health dashboard surfaces dead-URL count + drill-down.
       - **Actor Metadata (Phase 33):** 27-entry canonical actor catalog (`server/data/actor-catalog.ts`); `actorConfidence` added to v3 extractor schema; daily eval tracks `actorMatchRate` alongside geocode accuracy; API Health dashboard surfaces actor quality counts.
       - **Internal Docs + Redis Optimization (Phase 35):** Redis registry drift gate landed (`src/__tests__/lib/redis-registry.test.ts`; 39 assertions); 32-key deep-dive at `docs/architecture/redis-keys.md`; 7-module LLM-pipeline JSDoc audit (DOCS-INT-02); TTL right-sizing (audit-only outcome).
       - **Pipeline Simplification (Phases 29 / 30 / 35):** v1 + v2 extractors deleted (rollback path: `git revert <Phase 29 commit range>`); Cerebras + Groq adapter dead-code purged; incremental flush retired (SIMPLIFY-01); watchdog soft-warn tier eliminated (SIMPLIFY-03); `events:llm:v3:partial` retired (SIMPLIFY-02; 358 LOC removed); `freeClaudeRouter.ts` callers documented (SIMPLIFY-05); bundle delta +10,739 bytes (+0.60%; JSDoc additions dominated).
       - **Public Docs (Phases 36 / 37):** README sweep (rate-limit drift + LLM Enrichment section); 12 architecture markdown files audited (7 edited / 5 verified-clean); 21 Mermaid diagrams audited; runbook §6 rewrite + §13-§16 SRE-template appendage; degradation.md Pitfall 1 contract; ADR-0010 milestone-final rewrite + Phase 37 close sub-block (the 6th and final v1.5 sub-block); ADR-0011 Phase 36 sub-block.
       - **OpenAPI Additions (Phase 36):** 5 new endpoint entries + 2 verified-clean + 4 reusable component schemas + 2 named securitySchemes (cronSecret + operatorBearer); Redocly lint vitest + `markdown-link-check` script wired as mechanical drift gates.
       - **Acceptance Gate (Phase 37):** 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 with `allTiersGreen === true` and all 5 tier statuses `healthy`; evidence inline in `.planning/phases/37-*/37-SUMMARY.md §Acceptance Gate Observation`; LLM-RELI-07 satisfied; 999.5 load test unblocked for v1.6 promotion.
    5. `### Quantitative snapshot` section (7 metrics; per CONTEXT D-16 + PATTERNS line 411):
       - vitest baseline: 2193 → ~2380 (+~187 net new tests across v1.5)
       - TypeScript errors: 0 → 0 (held through v1.5)
       - Bundle: 1.72 MB → 1.79 MB (`api/vercel-entry.js` = 1,790,243 bytes at Phase 35 close; +10,739 bytes net delta over v1.5; JSDoc additions dominated — Phase 35 D-19 measurement)
       - Cron jobs: 3 → 3 (Hobby cap respected through v1.5; Pro upgrade lifted `maxDuration` not cron count)
       - ADRs: 8 → 11 (ADR-0009 Phase 27.4.3 + ADR-0010 + ADR-0011 = 3 net adds)
       - Redis key registry: 32 keys at v1.5 close (deep-dive at `docs/architecture/redis-keys.md`; pinned by `src/__tests__/lib/redis-registry.test.ts`)
       - Commits across v1.5 phases: ~N (sum from `git log --oneline` per phase branch — approximate; record the actual number computed during execution)
    6. `### Migration notes (v1.4 → v1.5)` section (5 bullets per CONTEXT D-18 + PATTERNS line 424 — the exact set in the `<interfaces>` block above): Vercel Pro plan required; CLAUDE.md trimmed; v1 + v2 deleted; partial-key retired; OpenRouter dormant + Cerebras/Groq deferred.
    7. `### Deferred to v1.6+ (carried forward)` section: 8 bullets per CONTEXT D-18 + PATTERNS line 441 — 999.5 + REVEAL-01/02 + Cerebras/Groq adapter restoration + paid-OR conversion + adaptive Retry-After NIM limiter + per-provider eval + cascade_exhausted DLQ taxonomy + (carried from v1.4) Phase 27.3.3 + 999.2 + 999.3 + 999.4 + Telegram OSINT + GDELT BigQuery + satellite imagery overlay.
    8. Closing `---` horizontal rule (mirrors v1.4 entry's closing rule on line 51).

    Replace all `YYYY-MM-DD` placeholders with today's actual ISO date. Replace `PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3` with the actual 3 run dates extracted from `.planning/phases/37-*/37-SUMMARY.md §Acceptance Gate Observation` (read Plan 37-02's draft to get them).

    Commit: `docs(37): D-18 CHANGELOG[v1.5] milestone entry`

  </action>
  <verify>
    <automated>
      grep -n '^## \[v1\.5\]' CHANGELOG.md
      # MUST output a single match — the line number should be 5 (the v1.5 entry sits at the top, above v1.4)
      grep -n '^## \[v1\.4\]' CHANGELOG.md
      # MUST output a single match — the line number should be GREATER than the v1.5 line (v1.5 above v1.4)
      grep -c '^### Headline deliverables$' CHANGELOG.md
      # MUST return at least 2 — one in v1.5 entry, one in v1.4 entry
      grep -c '^### Quantitative snapshot$' CHANGELOG.md
      # MUST return at least 2
      grep -c '^### Migration notes' CHANGELOG.md
      # MUST return at least 2 — v1.5 + v1.4
      grep -c '^### Deferred to v1\.[56]+ (carried forward)$' CHANGELOG.md
      # MUST return at least 2 — v1.5 says v1.6+, v1.4 says v1.5+
      awk '/^## \[v1\.5\]/,/^---$/' CHANGELOG.md | wc -l
      # MUST be at least 50 (entry has 8 headline bullets + 7 snapshot bullets + 5 migration + 8 deferred + framing + headers ≈ 50-80 lines)
      grep -c '999.5 Performance Optimization' CHANGELOG.md
      # MUST return at least 1
      grep -c 'Vercel Pro plan required' CHANGELOG.md
      # MUST return at least 1
      head -3 CHANGELOG.md
      # MUST start with `# Changelog\n\nAll notable changes to the Iran Monitor project.\n` (preserved verbatim)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## \[v1\.5\]' CHANGELOG.md` returns line number `5` (v1.5 entry inserted directly below the file's header on line 3 + blank line on line 4)
    - The line number of `## [v1.5]` is LESS THAN the line number of `## [v1.4]` (v1.5 entry is ABOVE v1.4)
    - `head -3 CHANGELOG.md` outputs the file's original header (`# Changelog\n\nAll notable changes to the Iran Monitor project.`)
    - The v1.5 entry's title line matches the pattern `^## \[v1\.5\] — LLM Reliability & Reveal Prep — 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]$`
    - `grep -c '^### Headline deliverables$' CHANGELOG.md` returns at least `2`
    - `grep -c '^### Quantitative snapshot$' CHANGELOG.md` returns at least `2`
    - `grep -c '^### Migration notes (v1\.4 → v1\.5)$' CHANGELOG.md` returns `1` (the new v1.5 entry)
    - `grep -c '^### Deferred to v1\.6\+ (carried forward)$' CHANGELOG.md` returns `1`
    - `awk '/^## \[v1\.5\]/,/^## \[v1\.4\]/' CHANGELOG.md | wc -l` returns at least `45` (v1.5 entry ≥ 45 lines plus the line before `## [v1.4]`)
    - The v1.5 entry mentions `999.5 Performance Optimization`, `Vercel Pro plan required`, `ADR-0010`, `prod-connectivity-audit.yml`, `LLM-RELI-07`, `cerebras-groq-deferred`, and `Pitfall 1` at least once each (grep checks)
    - `git log -1 --pretty=%s` returns `docs(37): D-18 CHANGELOG[v1.5] milestone entry`
  </acceptance_criteria>
  <done>CHANGELOG.md has a complete v1.5 entry above the v1.4 entry; entry mirrors v1.4 section structure (Headline / Snapshot / Migration / Deferred); commit landed with the `docs(37): D-18` prefix.</done>
</task>

<task type="auto">
  <name>Task 2: D-14 D-15 D-16 D-17 D-19 — Complete 37-SUMMARY.md (phase rollup + framing-gap + snapshot + promotion + decision table)</name>
  <files>.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md</files>
  <read_first>
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md (current draft state from Plan 37-02 — frontmatter + opener + §Acceptance Gate Observation)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md lines 82-92 (D-14 / D-15 / D-16 / D-17 / D-19 specifications)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 178-353 (37-SUMMARY frontmatter shape + decision table shapes + headline outcomes block)
    - .planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md (the primary analog; read sections `## Framing-Gap Callouts`, `## 3-Gate Verification Results` — copy the 6 framing-gap callouts verbatim per CONTEXT D-15)
    - .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-SUMMARY.md (secondary analog; compact closing-table shape)
    - .planning/ROADMAP.md lines 96-115 + 304-315 (v1.5 milestone heading + 10 phase rows — source for the per-phase rollup table content + dates)
    - .planning/REQUIREMENTS.md lines 13-23 + 131-179 (requirements text + traceability table — for the Requirements Closed column of the rollup table)
  </read_first>
  <action>
    UPDATE the existing `37-SUMMARY.md` (created by Plan 37-02 as a draft) to complete the milestone close artifact.

    Step 1 — Flip the frontmatter from draft to final:
    - `status`: `final (v1.5 milestone closed PHASE_CLOSE_DATE; LLM-RELI-07 + DOCS-PUB-04 satisfied)`
    - `closed`: PHASE_CLOSE_DATE (today's ISO date)
    - `plan_status`: `3/3 complete`
    - `requirements_satisfied`: `[DOCS-PUB-04, LLM-RELI-07]` (Plan 37-02 had just `[LLM-RELI-07]`; expand here)
    - REMOVE `requirements_pending_close`
    - Add `metrics:` block per PATTERNS lines 225-231 (`duration_days: ~2-5`, `plans: 3`, `atomic_commits: ~9-11`, `files_modified: 6`, `test_total: ~2380 (unchanged from Phase 36 close — no new tests in Phase 37)`, `bundle_delta_bytes: 0 (no code touched)`)
    - Add `tags:` extensions: `acceptance-gate-observed`, `milestone-shipped`, `three-gate-verified`

    Step 2 — Replace the draft headline (`# Phase 37 SUMMARY — DRAFT`) with a final headline (`# Phase 37 SUMMARY — v1.5 LLM Reliability & Reveal Prep milestone close`) and rewrite the opener (~2 paragraphs) to read as the final milestone-close summary (not a draft awaiting completion). Include the headline outcomes per PATTERNS lines 343-349:
    ```markdown
    ## Top-line outcomes

    - ADR-0010 milestone-final rewrite + Phase 37 close sub-block (6th and final v1.5 sub-block)
    - 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 (LLM-RELI-07 satisfied)
    - CHANGELOG[v1.5] entry committed above the v1.4 entry
    - ROADMAP / REQUIREMENTS / STATE flipped for Phase 37 + DOCS-PUB-04 + LLM-RELI-07
    - 3 mechanical verification gates green (vitest + Redocly lint + markdown-link-check)
    - v1.6 promotion unblocked (999.5 Performance Optimization + 1-300 VU k6 sweep first phase)
    ```

    Step 3 — Preserve the existing `## Acceptance Gate Observation (LLM-RELI-07)` section verbatim (Plan 37-02 lands the 3 evidence triplets; Plan 37-03 does not re-write this section). If the section's framing paragraph contains a cadence-deviation note OR a "Cadence ran as specified" note, preserve it for the framing-gap callouts row #7 (Step 4).

    Step 4 — APPEND a new `## v1.5 Phase Rollup` section (D-14) below the §Acceptance Gate Observation section. Use the 6-column table from the `<interfaces>` block above (Phase | Name | Status | Closed | Requirements Closed | Notes). Populate exactly 10 rows (one per Phase 29 / 30 / 30.1 / 31 / 32 / 33 / 34 / 35 / 36 / 37) using the literal data from `.planning/ROADMAP.md` lines 96-115 + the requirement IDs from `.planning/REQUIREMENTS.md` traceability table.

    Step 5 — APPEND a new `## Framing-Gap Callouts` section (D-15). Copy the 6 callout entries verbatim from `.planning/phases/36-public-docs-sweep-openapi-additions/36-SUMMARY.md` `## Framing-Gap Callouts` section — heading + `**Surfaced by:**` + `**Planning text:**` + `**Shipped reality:**` + `**Why preserved:**` shape per PATTERNS lines 261-300. Then APPEND row #7 (Phase 37 cadence interpretation) per the literal shape in the `<interfaces>` block above. If Plan 37-02's §Acceptance Gate Observation framing paragraph noted a cadence deviation, write the "Shipped reality" line as `Operator compressed to back-to-back ~N min apart; rationale: <as logged>`. If cadence ran as D-06 specifies, write `Cadence ran as specified — no deviation`.

    Step 6 — APPEND a new `## v1.5 Quantitative Snapshot` section (D-16). 7 metrics in a flat bulleted list per the literal metric definitions in the `<interfaces>` block above. Compute the commit count by running `git log --oneline main..HEAD` (or summing across all v1.5 phase branches). Use today's date format for the snapshot timestamp.

    Step 7 — APPEND a new `## v1.6 Promotion Readiness` section (D-17). Use the literal statement from the `<interfaces>` block above, substituting PHASE_CLOSE_DATE for `YYYY-MM-DD`.

    Step 8 — APPEND a new `## Decision-by-Decision Outcome` closing table (D-19). 5-column shape: `D-N | Decision | Plan | Status | Commit Ref`. Populate 19 rows (D-01..D-19) per the literal table in the `<interfaces>` block above. Commit Ref column uses commit subject + `(SHA TBD at PR merge)` since rebase + merge of the phase branch will rewrite SHAs. Do NOT add an "Hours spent" column per CONTEXT discretion item.

    Do NOT add the `## 3-Gate Verification Results` block yet — Task 5 adds it after running the gates.

    Single commit at end of this Task: `docs(37): D-14 D-15 D-16 D-17 D-19 complete 37-SUMMARY.md (rollup + framing-gaps + snapshot + promotion + decision table)`

  </action>
  <verify>
    <automated>
      grep -c '^## v1.5 Phase Rollup$' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      grep -c '^## Framing-Gap Callouts' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      grep -c '^## v1.5 Quantitative Snapshot$' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      grep -c '^## v1.6 Promotion Readiness$' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      grep -c '^## Decision-by-Decision Outcome' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      grep -cE '^### [1-7]\. ' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return at least 7 — 6 carryover framing-gap callouts + 1 new for Phase 37
      grep -cE '^\| (29|30|30\.1|31|32|33|34|35|36|37) ' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return at least 10 — one rollup row per v1.5 phase
      grep -cE '^\| D-(0[1-9]|1[0-9]) ' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return at least 19 — one decision row per D-01..D-19
      grep -c '^## Acceptance Gate Observation (LLM-RELI-07)$' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1 — preserved from Plan 37-02
      grep -c '"allTiersGreen": true' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return at least 3 — the 3 evidence triplets preserved from Plan 37-02
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## v1.5 Phase Rollup$' 37-SUMMARY.md` returns `1`
    - `grep -c '^## Framing-Gap Callouts' 37-SUMMARY.md` returns `1`
    - `grep -c '^## v1.5 Quantitative Snapshot$' 37-SUMMARY.md` returns `1`
    - `grep -c '^## v1.6 Promotion Readiness$' 37-SUMMARY.md` returns `1`
    - `grep -c '^## Decision-by-Decision Outcome' 37-SUMMARY.md` returns `1`
    - `grep -cE '^### [1-7]\. ' 37-SUMMARY.md` returns at least `7` (6 carryover framing-gap callouts + 1 new)
    - Phase Rollup table has 10 data rows — one per Phase 29 / 30 / 30.1 / 31 / 32 / 33 / 34 / 35 / 36 / 37 (verify via `grep -cE '^\| (29|30|30\.1|31|32|33|34|35|36|37) ' 37-SUMMARY.md` ≥ 10)
    - Decision-by-Decision Outcome table has 19 data rows — D-01 through D-19 (verify via `grep -cE '^\| D-(0[1-9]|1[0-9]) ' 37-SUMMARY.md` ≥ 19)
    - `## Acceptance Gate Observation (LLM-RELI-07)` heading still present (preserved from Plan 37-02)
    - 3 fenced `"allTiersGreen": true` JSON payloads still present (preserved from Plan 37-02)
    - Frontmatter `status:` field reads `final (v1.5 milestone closed YYYY-MM-DD; LLM-RELI-07 + DOCS-PUB-04 satisfied)` with today's date substituted
    - Frontmatter `requirements_satisfied:` field contains both `DOCS-PUB-04` and `LLM-RELI-07`
    - `git log -1 --pretty=%s` returns `docs(37): D-14 D-15 D-16 D-17 D-19 complete 37-SUMMARY.md (rollup + framing-gaps + snapshot + promotion + decision table)`
    - File line count ≥ 250 (frontmatter ~30 + opener ~20 + §Acceptance Gate Observation ~80 + Phase Rollup ~25 + Framing-Gap Callouts ~60 + Quantitative Snapshot ~15 + v1.6 Promotion Readiness ~10 + Decision-by-Decision Outcome ~30 ≈ 270+)
  </acceptance_criteria>
  <done>37-SUMMARY.md is complete except for the `## 3-Gate Verification Results` block (Task 5 adds that); all 5 required sections (Phase Rollup, Framing-Gap Callouts, Quantitative Snapshot, v1.6 Promotion Readiness, Decision-by-Decision Outcome) landed; §Acceptance Gate Observation preserved from Plan 37-02; commit landed with the `docs(37): D-14 D-15 D-16 D-17 D-19` prefix.</done>
</task>

<task type="auto">
  <name>Task 3: D-05 finalize + ADR cross-link finalize — Rewrite ADR-0010 placeholders to actual milestone-close date + LLM-RELI-07 D-N row</name>
  <files>docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (current state from Plan 37-01 — lines 1-10 status block; the Phase 37 close sub-block; the placeholder D-XX row for LLM-RELI-07)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md `## Acceptance Gate Observation (LLM-RELI-07)` section (to extract the 3 run dates for the D-N row's PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 substitution)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md line 58 (D-05 second-line specification)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-01-PLAN.md Task 3 (to recall the exact placeholder text + heading shape Plan 37-01 landed)
  </read_first>
  <action>
    Three small text replacements in `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`:

    1. **Line 4 — finalize close date in second status line.** Plan 37-01 Task 4 landed `**Status:** Accepted (v1.5 closed YYYY-MM-DD)` with today's-date placeholder. Replace `YYYY-MM-DD` with PHASE_CLOSE_DATE (today's actual ISO date). The expected final state of line 4 matches the regex `^\*\*Status:\*\* Accepted \(v1\.5 closed 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]\)$`.

    2. **Phase 37 close sub-block heading — finalize date.** Plan 37-01 Task 3 landed `## Phase 37 Close Sub-block (appended YYYY-MM-DD)` with today's-date placeholder. Replace `YYYY-MM-DD` with PHASE_CLOSE_DATE.

    3. **Phase 37 close sub-block — finalize the LLM-RELI-07 placeholder D-XX row.** Plan 37-01 Task 3 landed:
       ```
       - **D-XX (LLM-RELI-07): see [`37-SUMMARY.md §Acceptance Gate Observation`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md#acceptance-gate-observation-llm-reli-07) — finalized in Plan 37-03.**
       ```
       Replace this with the final D-N row (use the next available D-N number after D-05; verify by counting Phase 37 close sub-block D-rows landed by Plan 37-01 Task 3 — likely D-06 within the sub-block's internal numbering, but if Plan 37-01 already used D-06..D-NN for other rows, use D-(NN+1)). The final D-N row reads:
       ```
       - **D-06 (LLM-RELI-07): 3 consecutive `prod-connectivity-audit.yml` exit-0 runs observed PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 with `allTiersGreen === true` on every sidecar payload. Evidence triplets (URL + ISO timestamp + JSON payload) inline in [`37-SUMMARY.md §Acceptance Gate Observation`](../../.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md#acceptance-gate-observation-llm-reli-07). v1.6 promotion unblocked.**
       ```
       Substitute PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 with the actual 3 run dates extracted from the 3 evidence triplet timestamps in 37-SUMMARY.md (truncate to YYYY-MM-DD).

    4. **v1.5 Milestone Close Rollup — finalize Phase 37 close date.** Plan 37-01 Task 3 landed `6. **Phase 37 (this sub-block)** ... (Closed YYYY-MM-DD.)`. Replace `YYYY-MM-DD` with PHASE_CLOSE_DATE.

    Do NOT modify any other content. The Phase 30 / 30.1 / 34 / 35 sub-blocks remain byte-identical. The rewritten Context / Decision / Consequences / Alternatives Considered / References sections from Plan 37-01 Tasks 1 + 2 remain unchanged. The Phase 37 close sub-block opening paragraph, D-01..D-05 rows, Outcome paragraph, Architecture-level numbers footer, and Out-of-scope-carries-forward block all remain unchanged.

    Commit: `docs(37): D-05 finalize ADR-0010 close date + LLM-RELI-07 D-N row in Phase 37 sub-block`

  </action>
  <verify>
    <automated>
      sed -n '4p' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | grep -E '^\*\*Status:\*\* Accepted \(v1\.5 closed 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]\)$' | wc -l
      # MUST return 1 — line 4 finalized
      grep -c 'YYYY-MM-DD' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 0 — all placeholders replaced
      grep -c 'D-XX (LLM-RELI-07)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 0 — placeholder row replaced with final D-N row
      grep -c 'finalized in Plan 37-03' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return 0 — placeholder language removed
      grep -cE 'D-0[6-9] \(LLM-RELI-07\)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 1 — final D-N row landed
      grep -c 'prod-connectivity-audit.yml.*exit-0.*allTiersGreen' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
      # MUST return at least 1 — the gate observation prose in the new D-N row
    </automated>
  </verify>
  <acceptance_criteria>
    - Line 4 of `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` matches the regex `^\*\*Status:\*\* Accepted \(v1\.5 closed 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]\)$` (real date, no `YYYY-MM-DD` placeholder)
    - `grep -c 'YYYY-MM-DD' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `0`
    - `grep -c 'D-XX (LLM-RELI-07)' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `0`
    - `grep -c 'finalized in Plan 37-03' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `0`
    - A final `D-NN (LLM-RELI-07)` row is present in the Phase 37 close sub-block (`grep -cE 'D-(0[6-9]|1[0-9]) \(LLM-RELI-07\)' docs/adr/0010-...md` returns at least `1`)
    - The new D-N row references `37-SUMMARY.md#acceptance-gate-observation-llm-reli-07` as a markdown link
    - The Phase 37 close sub-block heading no longer contains `YYYY-MM-DD` — it contains a real date (verify via `grep -cE '^## Phase 37 Close Sub-block \(appended 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]\)$'` returns `1`)
    - The 4 v1.5 historical sub-block headings (Phase 30 / 30.1 / 34 / 35) still present, each `grep -c` returns `1`
    - `grep -c '<expand_at_36>' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` returns `0` (marker still absent from Plan 37-01 Task 2)
    - `git log -1 --pretty=%s` returns `docs(37): D-05 finalize ADR-0010 close date + LLM-RELI-07 D-N row in Phase 37 sub-block`
  </acceptance_criteria>
  <done>ADR-0010 placeholders all replaced with actual milestone-close date and final LLM-RELI-07 D-N row pointing to 37-SUMMARY.md §Acceptance Gate Observation; no `YYYY-MM-DD` strings remain; no `D-XX (LLM-RELI-07)` placeholder remains; 4 historical sub-blocks preserved; commit landed with the `docs(37): D-05 finalize` prefix.</done>
</task>

<task type="auto">
  <name>Task 4: Tracking flips — Update ROADMAP.md + REQUIREMENTS.md + STATE.md to v1.5-shipped / v1.6-unblocked state</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 37 row on line 113, Progress table row ~line 591 of the larger ROADMAP, milestone-summary heading ~line 96)
    - .planning/REQUIREMENTS.md (current state — DOCS-PUB-04 row ~line 71, LLM-RELI-07 row ~line 19, traceability table rows ~lines 139 + 171, footer ~line 191)
    - .planning/STATE.md (current state — frontmatter lines 1-14, Current Position block lines 24-40)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 568-723 (literal flip targets for all 3 files)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-CONTEXT.md (re-confirm the close-language wording for each flip)
  </read_first>
  <action>
    Three semantic edits across 3 files, atomic per-decision commits (1 commit per file is acceptable since the flips per file are tightly coupled).

    **Edit 1 — ROADMAP.md** (per PATTERNS lines 568-602):

    1. Find the Phase 37 row currently `- [ ] **Phase 37: ADR-0010 + Acceptance Gate Closeout** — Capture v1.5 LLM-pipeline decisions in a new ADR ...; observe `prod-connectivity-audit.yml` exit-0 with `allTiersGreen=true` for 3 consecutive runs; lock the milestone close.`
       Replace with:
       `- [x] **Phase 37: ADR-0010 + Acceptance Gate Closeout** — ✓ CLOSED PHASE_CLOSE_DATE. ADR-0010 milestone-final rewrite (Context / Decision / Consequences / Alternatives / References all rewritten to describe shipped state — NIM-only at runtime, OpenRouter dormant, Cerebras + Groq deferred, v1+v2 deleted, Pitfall 1 bridge serves raw GDELT); 5 historical v1.5 sub-blocks (Phase 30 / 30.1 / 34 / 35; plus original Phase 29 framing condensed into rewritten Context/Decision lead-in) preserved; <expand_at_36> marker deleted; status line gained second line "Accepted (v1.5 closed PHASE_CLOSE_DATE)"; Phase 37 close sub-block appended as 6th and final v1.5 sub-block including v1.5 Milestone Close Rollup. 3 consecutive prod-connectivity-audit.yml exit-0 runs observed PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 with allTiersGreen=true. CHANGELOG[v1.5] entry committed above v1.4 entry. 37-SUMMARY.md complete (rollup + framing-gaps + snapshot + promotion + decision table). ROADMAP/REQUIREMENTS/STATE flipped. ~10 atomic commits across 3 plans (3 sequential waves). v1.6 promotion unblocked (999.5 first phase).`

    2. Find the milestone-summary heading on ~line 96: `## Milestone v1.5: LLM Reliability & Reveal Prep — 🚧 IN PROGRESS`
       Replace with: `## Milestone v1.5: LLM Reliability & Reveal Prep — ✅ SHIPPED PHASE_CLOSE_DATE`
       In the same block (a few lines below), find the `Started:` line and append a new line below: `Closed: PHASE_CLOSE_DATE`
       Update the existing `Acceptance gate:` line to: `Acceptance gate: ✅ OBSERVED PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 — 3 consecutive prod-connectivity-audit.yml exit-0 runs with allTiersGreen=true (LLM-RELI-07 satisfied; 999.5 load test promotion to v1.6 unblocked).`

    3. Find the `### Progress` table row for Phase 37: `| 37. ADR-0010 + Acceptance Gate Closeout | 0/0 | Not started | - |`
       Replace with: `| 37. ADR-0010 + Acceptance Gate Closeout | 3/3 | Complete | PHASE_CLOSE_DATE |`

    Commit: `docs(37): D-04 D-05 flip ROADMAP Phase 37 row + Progress table + milestone heading to v1.5 shipped`

    **Edit 2 — REQUIREMENTS.md** (per PATTERNS lines 612-651):

    1. Find the DOCS-PUB-04 checklist row (~line 71): `- [ ] **DOCS-PUB-04**: New ADR (ADR-0010 — ...). Captures the rationale, trade-offs, and rollback plan. **(Phase 37 territory — ADR-0010 milestone-close sub-block. Phase 36 closed without touching DOCS-PUB-04 per CONTEXT D-04 + plan-specific notes.)**`
       Change `- [ ]` to `- [x]`. Replace the parenthetical `**(Phase 37 territory — ...)**` with:
       `**(Phase 37 close PHASE_CLOSE_DATE: ADR-0010 body rewrite to milestone-final shipped state + 5 historical sub-blocks (Phase 30 / 30.1 / 34 / 35 + original Phase 29 framing condensed) preserved + Phase 37 close sub-block appended as 6th and final v1.5 sub-block + status line gained second line "Accepted (v1.5 closed PHASE_CLOSE_DATE)" + <expand_at_36> marker deleted + v1.5 Milestone Close Rollup subsection landed. Commits <range TBD at PR merge>.)**`

    2. Find the LLM-RELI-07 checklist row (~line 19): `- [ ] **LLM-RELI-07**: prod-connectivity-audit.yml exit-0 with audit:connectivity:last-result.allTiersGreen === true for **3 consecutive runs** (the v1.5 → v1.6 promotion gate; unblocks 999.5 load test).`
       Change `- [ ]` to `- [x]`. APPEND inline (no newline) the close-language:
       ` **(Phase 37 close PHASE_CLOSE_DATE: 3 consecutive greens observed [either "1-per-day across ~48 hours per CONTEXT D-06" OR "back-to-back ~N min apart per D-08 NOTE — rationale logged in 37-SUMMARY.md framing-gap callouts row #7"]; evidence triplets — GH Actions run URL + ISO timestamp + sidecar JSON payload — inline in 37-SUMMARY.md §Acceptance Gate Observation. v1.6 promotion unblocked.)**`

    3. Find the Traceability table rows for LLM-RELI-07 and DOCS-PUB-04 (lines 139 + 171):
       - `| LLM-RELI-07  | 37    | Pending                                                                                                                          |` → `| LLM-RELI-07  | 37    | Complete (PHASE_CLOSE_DATE — Phase 37 close)                                                                                     |`
       - `| DOCS-PUB-04  | 37    | Pending                                                                                                                          |` → `| DOCS-PUB-04  | 37    | Complete (PHASE_CLOSE_DATE — Phase 37 close)                                                                                     |`

    4. Find the footer line (~line 191): `_Last updated: 2026-05-30 after Phase 36 close — DOCS-PUB-01/02/03/05 + DOCS-API-01..07 flipped to Complete; DOCS-PUB-04 deferred to Phase 37 (ADR-0010 milestone-close sub-block) per CONTEXT D-04._`
       APPEND a new line below: `_Last updated: PHASE_CLOSE_DATE after Phase 37 close — DOCS-PUB-04 + LLM-RELI-07 flipped to Complete; v1.5 milestone shipped; v1.6 promotion unblocked (999.5 first phase)._`

    Commit: `docs(37): D-04 D-05 flip REQUIREMENTS DOCS-PUB-04 + LLM-RELI-07 to Complete + traceability table + footer`

    **Edit 3 — STATE.md** (per PATTERNS lines 660-723):

    1. Frontmatter (lines 1-14):
       - `milestone_name`: `LLM Reliability & Reveal Prep — ✅ SHIPPED PHASE_CLOSE_DATE`
       - `status`: `v1.5 milestone closed PHASE_CLOSE_DATE via Phase 37; ADR-0010 milestone-final rewrite + 6th and final v1.5 sub-block landed; 3 consecutive prod-connectivity-audit.yml greens observed; CHANGELOG[v1.5] entry committed; ROADMAP/REQUIREMENTS/STATE flipped; v1.6 promotion unblocked (999.5 first phase).`
       - `last_updated`: `"PHASE_CLOSE_DATET00:00:00.000Z"` — NO space between the date and the `T` (matches the existing STATE.md format like `"2026-05-30T00:00:00.000Z"`; substitute PHASE_CLOSE_DATE for the literal ISO date)
       - `last_activity`: `PHASE_CLOSE_DATE -- Phase 37 closed; v1.5 milestone shipped; ROADMAP/REQUIREMENTS/STATE flips committed`
       - `progress.completed_phases`: 9 (was 8)
       - `progress.completed_plans`: 58 (was 55; +3 from Phase 37)
       - `progress.percent`: 98 (round(58/59 × 100); was 53)

    2. Current Position prose block (lines 24-39): replace the existing block with the v1.5-shipped/v1.6-unblocked version per PATTERNS lines 710-723:
       ```markdown
       ## Current Position

       Phase: v1.6 (UNBLOCKED — 999.5 promotion ready) — V1.5 CLOSED
       Plan: v1.5 milestone shipped via Phase 37 (3/3 plans complete); next is /gsd:plan-milestone v1.6 or pull 999.5 from backlog
       Status: v1.5 LLM Reliability & Reveal Prep milestone closed PHASE_CLOSE_DATE. ADR-0010 milestone-final + Phase 37 close sub-block landed; 3 consecutive prod-connectivity-audit.yml greens observed PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3; CHANGELOG[v1.5] entry committed; ROADMAP/REQUIREMENTS/STATE flipped.
       Last activity: PHASE_CLOSE_DATE -- Phase 37 closed; v1.5 milestone shipped

       Resume file: [`.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md`](phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md).

       Phase 37 close artifact: [`.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md`](phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md).

       Acceptance gate: ✅ OBSERVED PHASE_CLOSE_DATE_RUN_1..PHASE_CLOSE_DATE_RUN_3 — 3 consecutive prod-connectivity-audit.yml exit-0 runs with allTiersGreen=true (LLM-RELI-07 satisfied; 999.5 load test promotion to v1.6 unblocked).
       ```

       Preserve the `Predecessor: v1.4 GDELT Redo & Performance ...` line that follows the Current Position block — that historical record stays.

    Commit: `docs(37): flip STATE.md frontmatter + Current Position to v1.5 shipped / v1.6 unblocked`

  </action>
  <verify>
    <automated>
      grep -c '^- \[x\] \*\*Phase 37' .planning/ROADMAP.md
      # MUST return 1 — Phase 37 row flipped
      grep -c 'v1.5: LLM Reliability & Reveal Prep — ✅ SHIPPED' .planning/ROADMAP.md
      # MUST return 1 — milestone heading flipped
      grep -cE '^\| 37\. .*\| 3/3 .*\| Complete .*\|' .planning/ROADMAP.md
      # MUST return 1 — Progress table row flipped
      grep -c '^- \[x\] \*\*DOCS-PUB-04\*\*' .planning/REQUIREMENTS.md
      # MUST return 1 — DOCS-PUB-04 row flipped
      grep -c '^- \[x\] \*\*LLM-RELI-07\*\*' .planning/REQUIREMENTS.md
      # MUST return 1 — LLM-RELI-07 row flipped
      grep -cE '^\| LLM-RELI-07 .*\| Complete \(20' .planning/REQUIREMENTS.md
      grep -cE '^\| DOCS-PUB-04 .*\| Complete \(20' .planning/REQUIREMENTS.md
      # Both MUST return 1
      grep -c 'Last updated:.*after Phase 37 close' .planning/REQUIREMENTS.md
      # MUST return 1
      grep -c '✅ SHIPPED' .planning/STATE.md
      # MUST return at least 1 (frontmatter milestone_name)
      grep -c 'V1.5 CLOSED' .planning/STATE.md
      # MUST return at least 1 (Current Position block)
      grep -c 'completed_phases: 9' .planning/STATE.md
      grep -c 'completed_plans: 58' .planning/STATE.md
      # Each MUST return 1
      git log --oneline -3 | grep -cE '^[a-f0-9]+ docs\(37\):'
      # MUST return at least 3 — 3 atomic commits for the 3 flip edits
    </automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md: Phase 37 row flipped to `[x]` with closing language including `✓ CLOSED` and today's date
    - ROADMAP.md: Milestone heading is `## Milestone v1.5: LLM Reliability & Reveal Prep — ✅ SHIPPED YYYY-MM-DD` (today's date)
    - ROADMAP.md: `### Progress` row for Phase 37 is `| 37. ADR-0010 + Acceptance Gate Closeout | 3/3 | Complete | YYYY-MM-DD |`
    - ROADMAP.md: `Acceptance gate:` line in the milestone block updated to `✅ OBSERVED ...`
    - REQUIREMENTS.md: DOCS-PUB-04 row checkbox `- [x]`; close-language parenthetical present with today's date
    - REQUIREMENTS.md: LLM-RELI-07 row checkbox `- [x]`; close-language inline append present with today's date
    - REQUIREMENTS.md: Traceability table rows for LLM-RELI-07 + DOCS-PUB-04 both flipped to `Complete (YYYY-MM-DD — Phase 37 close)`
    - REQUIREMENTS.md: New footer line appended noting Phase 37 close
    - STATE.md: `milestone_name` contains `✅ SHIPPED`; `status` contains `v1.5 milestone closed`; `completed_phases: 9`; `completed_plans: 58`; `progress.percent` matches `98` (or recomputed value)
    - STATE.md: Current Position block leads with `Phase: v1.6 (UNBLOCKED — 999.5 promotion ready) — V1.5 CLOSED`
    - STATE.md: Resume file link points to `phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md`
    - 3 atomic commits landed (one per file): `docs(37): D-04 D-05 flip ROADMAP ...`, `docs(37): D-04 D-05 flip REQUIREMENTS ...`, `docs(37): flip STATE.md ...`
  </acceptance_criteria>
  <done>ROADMAP / REQUIREMENTS / STATE all flipped to v1.5-shipped / v1.6-unblocked state with today's date in every PHASE_CLOSE_DATE placeholder substitution; 3 atomic commits landed.</done>
</task>

<task type="auto">
  <name>Task 5: D-13 — Run 3 mechanical verification gates (vitest + Redocly + markdown-link-check) + manual .planning/ cross-link spot-check, then commit results into 37-SUMMARY.md</name>
  <files>.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md</files>
  <read_first>
    - package.json (verify `docs:lint` and `openapi:lint` script lines per PATTERNS lines 753-762)
    - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (extract every `.planning/` cross-link to verify manually — these are out of `docs:lint` scope per PATTERNS line 756)
    - .planning/phases/37-adr-0010-acceptance-gate-closeout/37-PATTERNS.md lines 317-336 (3-Gate Verification Results block shape)
  </read_first>
  <action>
    Step 1 — Run Gate 1 (full vitest suite):
    ```bash
    npx vitest run 2>&1 | tail -10
    ```
    Capture: test files count, total tests count, pass/fail counts, duration, exit code. Must exit 0. If exits non-zero: STOP. Investigate failures. Do NOT proceed until Gate 1 is green.

    Step 2 — Run Gate 2 (OpenAPI spec lint):
    ```bash
    npx @redocly/cli lint server/openapi.yaml --format=stylish 2>&1 | tail -20
    ```
    Capture: error count, warning count, exit code. Must exit 0 (0 errors). Warnings are acceptable (Phase 36 D-24 baseline = 35 pre-existing style warnings).

    Step 3 — Run Gate 3 (markdown-link-check across docs/ + README + ADRs):
    ```bash
    npm run docs:lint 2>&1 | tail -30
    ```
    Capture: any new dead links introduced by the Phase 37 ADR rewrite. Phase 36 D-24 baseline was 7 pre-existing dead links — anything ABOVE 7 in the Phase 37 scope (ADR-0010's new cross-links) is a Phase 37 regression and must be fixed. Must exit 0 (or treat as "PASSED for Phase-37-introduced surface" if only pre-existing rot is reported).

    Step 4 — Manual `.planning/` cross-link spot-check:
    Per PATTERNS line 756, `.planning/` paths are OUT of `docs:lint` scope. The ADR-0010 References block rewritten by Plan 37-01 Task 2 includes ~9 phase CONTEXT.md paths + ~7 phase SUMMARY.md paths under `.planning/`. Verify each:
    ```bash
    # Extract every .planning/ link from the ADR
    grep -oE '\.planning/[a-zA-Z0-9._/-]+\.md' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | sort -u > /tmp/adr-planning-links.txt
    # For each, verify it resolves to an existing file
    while read LINK; do
      test -f "$LINK" && echo "OK: $LINK" || echo "FAIL: $LINK"
    done < /tmp/adr-planning-links.txt
    ```
    Every link MUST resolve to an existing file (test -f returns 0). If any FAIL: investigate (the most common cause is incorrect directory slugs — the Phase 32 / 33 / 34 directories have long auto-generated slugs that must be verified via `ls .planning/phases/32-*`, etc.). Fix and re-commit.

    Step 5 — Append the `## 3-Gate Verification Results (D-13 — inherits Phase 36 D-24)` section to `.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md` per the literal shape in the `<interfaces>` block above. Populate:
    - Gate 1: actual `<N>` test files / `<N>` tests passed; actual duration; actual timestamp
    - Gate 2: actual error + warning counts; actual timestamp
    - Gate 3: actual exit code outcome; same scope note; actual timestamp
    - Manual `.planning/` cross-link spot-check table: one row per link from `/tmp/adr-planning-links.txt`, with the OK/FAIL result

    Step 6 — Commit the verification results into 37-SUMMARY.md:
    ```bash
    git add .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
    git commit -m "chore(37): D-13 run 3-gate verification (vitest + Redocly + markdown-link-check; all green) + .planning/ cross-link spot-check"
    ```

  </action>
  <verify>
    <automated>
      npx vitest run 2>&1 | tail -3
      # MUST report all tests passed; exit 0
      echo "vitest exit code: $?"
      npx @redocly/cli lint server/openapi.yaml --format=stylish 2>&1 | tail -5
      echo "redocly exit code: $?"
      # MUST exit 0
      npm run docs:lint 2>&1 | tail -10
      echo "docs:lint exit code: $?"
      # MUST exit 0 (or only pre-existing rot from baseline)
      # Verify the 3-gate block is in 37-SUMMARY.md
      grep -c '^## 3-Gate Verification Results' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      grep -c '^### Gate 1: Full test suite' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      grep -c '^### Gate 2: OpenAPI spec lint' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      grep -c '^### Gate 3: Markdown link check' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # Each MUST return 1
      grep -c '^### Manual `.planning/` cross-link spot-check' .planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md
      # MUST return 1
      # Verify every .planning/ link in ADR resolves
      grep -oE '\.planning/[a-zA-Z0-9._/-]+\.md' docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | sort -u > /tmp/adr-planning-links.txt
      MISSING=0
      while read LINK; do test ! -f "$LINK" && echo "FAIL: $LINK" && MISSING=$((MISSING+1)); done < /tmp/adr-planning-links.txt
      echo "Missing count: $MISSING (MUST be 0)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits `0`
    - `npx @redocly/cli lint server/openapi.yaml --format=stylish` exits `0`
    - `npm run docs:lint` exits `0` (or only pre-existing-rot dead links present from the Phase 36 D-24 baseline of 7)
    - Every `.planning/` cross-link in `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` resolves to an existing file (`test -f $LINK` returns 0 for each)
    - `## 3-Gate Verification Results` section added to `37-SUMMARY.md`
    - All 3 gate subsections (`### Gate 1: Full test suite`, `### Gate 2: OpenAPI spec lint`, `### Gate 3: Markdown link check`) present with actual run timestamps + exit-status results
    - `### Manual `.planning/` cross-link spot-check` subsection present with one table row per `.planning/` link, all OK
    - `git log -1 --pretty=%s` returns `chore(37): D-13 run 3-gate verification (vitest + Redocly + markdown-link-check; all green) + .planning/ cross-link spot-check`
  </acceptance_criteria>
  <done>All 3 mechanical verification gates green; manual `.planning/` cross-link spot-check passes for every link; results captured in `37-SUMMARY.md` `## 3-Gate Verification Results` section; final close commit landed with the `chore(37): D-13` prefix.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                               |
| ---------------------------- | --------------------------------------------------------------------------------------------------------- |
| author→multiple-file-rewrite | Solo author edits 6 files in close-out. No external untrusted input.                                      |
| executor→shell-gates         | Executor runs `vitest run`, `redocly lint`, `markdown-link-check` — local-shell-only, no network surface. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                   |
| --------- | ---------------------- | ----------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-37-10   | Tampering              | CHANGELOG[v1.5] entry overwrites v1.4 entry instead of inserting above                                | mitigate    | Task 1 acceptance criteria pin: (a) `head -3 CHANGELOG.md` preserves the file header verbatim; (b) `grep -n '^## \[v1\.4\]' CHANGELOG.md` returns a line number GREATER than `grep -n '^## \[v1\.5\]'`.                                                           |
| T-37-11   | Tampering              | Tracking flips drift — ROADMAP / REQUIREMENTS / STATE updated for some Phase 37 items but not others  | mitigate    | Task 4 acceptance criteria pin each flip individually (DOCS-PUB-04 row, LLM-RELI-07 row, traceability table, Phase 37 row in ROADMAP, milestone heading, Progress table, STATE.md frontmatter, Current Position block). Inconsistent state fails the grep checks. |
| T-37-12   | Information disclosure | Commit Ref column in Decision-by-Decision Outcome table prematurely leaks unmerged SHAs               | accept      | All Commit Ref entries say "(SHA TBD at PR merge)" until rebase + merge completes; this is the Phase 35 + 36 convention.                                                                                                                                          |
| T-37-13   | Tampering              | Verification gate run produces "all green" claim without actually running the gates                   | mitigate    | Task 5 acceptance criteria require the actual exit codes (`$?` checks) AND the actual test counts / error counts / warning counts to be captured in the SUMMARY block. Fabricated results would be detectable by a reviewer running the same commands.            |
| T-37-14   | Tampering              | `.planning/` cross-link rot — Plan 37-01 Task 2 referenced a phase directory slug that does not exist | mitigate    | Task 5 manual spot-check verifies each `.planning/` link with `test -f`; FAIL count must be 0 before close commit lands.                                                                                                                                          |
| T-37-SC   | Tampering              | npm/pip/cargo installs                                                                                | accept      | No package installs in this plan; only existing dev-dependency invocations (`vitest`, `redocly`, `markdown-link-check`).                                                                                                                                          |

</threat_model>

<verification>
Plan 37-03 completes when:
- `CHANGELOG.md` has a complete `## [v1.5]` entry above the `## [v1.4]` entry, mirroring v1.4's section structure (Headline / Snapshot / Migration / Deferred)
- `.planning/phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md` is complete: frontmatter flipped to `status: final`, opener + headline outcomes + §Acceptance Gate Observation + §v1.5 Phase Rollup (10 rows) + §Framing-Gap Callouts (6 carryover + 1 new) + §v1.5 Quantitative Snapshot (7 metrics) + §v1.6 Promotion Readiness + §Decision-by-Decision Outcome (19 rows) + §3-Gate Verification Results
- `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` placeholders all replaced — no `YYYY-MM-DD` strings, no `D-XX (LLM-RELI-07)` strings; final LLM-RELI-07 D-N row cites the 3 evidence triplet dates and links to 37-SUMMARY.md §Acceptance Gate Observation
- `.planning/ROADMAP.md` Phase 37 row flipped to `[x]` with full close-language; Progress table row updated; milestone heading flipped to `✅ SHIPPED`
- `.planning/REQUIREMENTS.md` DOCS-PUB-04 + LLM-RELI-07 checklist rows flipped to `[x]` with close-language; Traceability table rows flipped to `Complete (YYYY-MM-DD)`; footer line updated
- `.planning/STATE.md` frontmatter + Current Position block flipped to v1.5-shipped / v1.6-unblocked state
- All 3 mechanical verification gates green: `npx vitest run` (exit 0), `npx @redocly/cli lint server/openapi.yaml --format=stylish` (exit 0), `npm run docs:lint` (exit 0)
- Manual `.planning/` cross-link spot-check: every `.planning/` link from ADR-0010 resolves to an existing file
- ~6-7 atomic commits landed on the phase branch:
  1. `docs(37): D-18 CHANGELOG[v1.5] milestone entry`
  2. `docs(37): D-14 D-15 D-16 D-17 D-19 complete 37-SUMMARY.md (rollup + framing-gaps + snapshot + promotion + decision table)`
  3. `docs(37): D-05 finalize ADR-0010 close date + LLM-RELI-07 D-N row in Phase 37 sub-block`
  4. `docs(37): D-04 D-05 flip ROADMAP Phase 37 row + Progress table + milestone heading to v1.5 shipped`
  5. `docs(37): D-04 D-05 flip REQUIREMENTS DOCS-PUB-04 + LLM-RELI-07 to Complete + traceability table + footer`
  6. `docs(37): flip STATE.md frontmatter + Current Position to v1.5 shipped / v1.6 unblocked`
  7. `chore(37): D-13 run 3-gate verification (vitest + Redocly + markdown-link-check; all green) + .planning/ cross-link spot-check`
</verification>

<success_criteria>

- DOCS-PUB-04 and LLM-RELI-07 are both fully Complete with auditable evidence at every surface (REQUIREMENTS checklist + REQUIREMENTS traceability table + ROADMAP phase row + ROADMAP Progress table + STATE.md frontmatter + CHANGELOG[v1.5] entry + ADR-0010 + 37-SUMMARY.md).
- The v1.5 milestone close is complete: any reader at any of the surfaces above sees a consistent "v1.5 shipped on PHASE_CLOSE_DATE" story.
- v1.6 promotion is mechanically unblocked: STATE.md "Current Position" explicitly says `Phase: v1.6 (UNBLOCKED — 999.5 promotion ready) — V1.5 CLOSED`.
- All 3 mechanical drift gates (Phase 35 D-01 redis-registry + Phase 36 D-08 openapi-lint + Phase 36 D-24 markdown-link-check) green at v1.5 close — the gates that go forward into v1.6 are proven working.
  </success_criteria>

<output>
Plan 37-03 IS the close-plan; its output IS the completed 37-SUMMARY.md (Plan 37-02 created the draft; Plan 37-03 finalizes it). Do NOT create `37-03-SUMMARY.md` separately — the phase summary file is `37-SUMMARY.md` itself, which Plan 37-03 finalizes. After Plan 37-03 closes, the branch `feature/37-adr-0010-acceptance-gate-closeout` is ready for merge to main per the Phase 36 close pattern.
</output>

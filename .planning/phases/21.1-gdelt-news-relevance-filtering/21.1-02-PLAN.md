---
phase: 21.1-gdelt-news-relevance-filtering
plan: 02
type: execute
wave: 2
depends_on: ["21.1-01"]
files_modified:
  - server/lib/newsFilter.ts
  - server/routes/news.ts
  - server/__tests__/lib/newsFilter.test.ts
  - server/__tests__/routes/news.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Articles are scored with 0-1 relevance instead of binary keyword pass/fail"
    - "Only 7 keywords remain non-ambiguous (airstrike, missile, bombing, shelling, casualties, invasion, drone)"
    - "All geographic terms, diplomatic terms, and organization names are ambiguous (require co-occurring non-ambiguous term)"
    - "Articles below 0.7 threshold are filtered out"
    - "Filtered articles carry actor/action/target/relevanceScore metadata"
    - "Tehran condemns airstrike in Yemen does not pass as a Tehran conflict event"
    - "Existing news route returns enriched articles without breaking response shape"
  artifacts:
    - path: "server/lib/newsFilter.ts"
      provides: "Overhauled filter with NLP scoring pipeline"
      exports: ["filterAndScoreArticles", "NON_AMBIGUOUS_KEYWORDS", "AMBIGUOUS_KEYWORDS"]
    - path: "server/__tests__/lib/newsFilter.test.ts"
      provides: "Updated tests for reclassified keywords and false positive scenarios"
      min_lines: 60
    - path: "server/__tests__/routes/news.test.ts"
      provides: "Updated route tests verifying enriched article response shape"
      min_lines: 30
  key_links:
    - from: "server/lib/newsFilter.ts"
      to: "server/lib/nlpExtractor.ts"
      via: "imports extractTriple for NLP enrichment"
      pattern: "import.*extractTriple.*nlpExtractor"
    - from: "server/lib/newsFilter.ts"
      to: "server/lib/relevanceScorer.ts"
      via: "imports computeRelevanceScore for scoring"
      pattern: "import.*computeRelevanceScore.*relevanceScorer"
    - from: "server/routes/news.ts"
      to: "server/lib/newsFilter.ts"
      via: "calls filterAndScoreArticles (replaces filterConflictArticles)"
      pattern: "filterAndScoreArticles"
---

<objective>
Wire the NLP scoring engine into the news filter pipeline, replacing binary keyword matching with relevance scoring.

Purpose: Integrate nlpExtractor and relevanceScorer from Plan 01 into the existing newsFilter.ts, overhaul the keyword classification per locked decisions, and update the news route to use the new scored pipeline. This is the integration and behavior-change plan.

Output: Overhauled newsFilter.ts with NLP scoring, reclassified keyword sets, updated news route, comprehensive test coverage including false positive regression tests.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.1-gdelt-news-relevance-filtering/21.1-CONTEXT.md
@.planning/phases/21.1-gdelt-news-relevance-filtering/21.1-RESEARCH.md
@.planning/phases/21.1-gdelt-news-relevance-filtering/21.1-01-SUMMARY.md

@server/lib/newsFilter.ts
@server/routes/news.ts
@server/lib/newsClustering.ts
@server/__tests__/lib/newsFilter.test.ts
@server/__tests__/routes/news.test.ts
</context>

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From server/lib/nlpExtractor.ts (created in Plan 01):
```typescript
export interface ArticleTriple {
  actor: string | null;
  action: string | null;
  target: string | null;
}
export function extractTriple(title: string, summary?: string): ArticleTriple;
```

From server/lib/relevanceScorer.ts (created in Plan 01):
```typescript
export interface ScoringInput {
  triple: ArticleTriple;
  source: string;
  domain?: string;
  title: string;
  summary?: string;
}
export function computeRelevanceScore(input: ScoringInput): number;
```

From server/types.ts (extended in Plan 01):
```typescript
export interface NewsArticle {
  id: string;
  title: string;
  url: string;
  source: string;
  sourceCountry?: string;
  publishedAt: number;
  summary?: string;
  imageUrl?: string;
  lat?: number;
  lng?: number;
  tone?: number;
  keywords: string[];
  actor?: string;        // NEW (Plan 01)
  action?: string;       // NEW (Plan 01)
  target?: string;       // NEW (Plan 01)
  relevanceScore?: number; // NEW (Plan 01)
}
```

From server/config.ts (extended in Plan 01):
```typescript
export interface AppConfig {
  // ... existing fields
  newsRelevanceThreshold: number; // default 0.7
}
```

From server/routes/news.ts (current):
```typescript
// Step 4 currently calls: filterConflictArticles(allArticles)
// Must change to: filterAndScoreArticles(allArticles)
```

From server/lib/newsFilter.ts (current exports to preserve backward compat):
```typescript
export const CONFLICT_KEYWORDS: Set<string>;
export function matchesKeywords(article: { title: string; summary?: string }): string[];
export function filterConflictArticles(articles: NewsArticle[]): NewsArticle[];
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overhaul newsFilter with keyword reclassification and NLP scoring pipeline</name>
  <files>server/lib/newsFilter.ts, server/__tests__/lib/newsFilter.test.ts</files>
  <behavior>
    - Test: non-ambiguous keywords (airstrike, missile, bombing, shelling, casualties, invasion, drone) pass on their own
    - Test: geographic terms (iran, israel, tehran, baghdad, etc.) are ambiguous -- require co-occurring non-ambiguous term
    - Test: diplomatic terms (sanctions, negotiations, ceasefire, etc.) are ambiguous
    - Test: organization names (IRGC, Hezbollah, Hamas, etc.) are ambiguous
    - Test: "Tehran condemns airstrike in Yemen" -- keyword match includes airstrike as non-ambiguous, but relevance score should reflect the indirect nature via NLP (condemns is not a conflict verb targeting Tehran)
    - Test: "Iran launches missile strike on Israeli bases" passes with high score (>= 0.7)
    - Test: expanded exclusion patterns reject entertainment/sports/historical articles
    - Test: filterAndScoreArticles enriches articles with actor/action/target/relevanceScore fields
    - Test: articles below 0.7 threshold are excluded from output
    - Test: backward compat -- CONFLICT_KEYWORDS export still exists (though values changed)
  </behavior>
  <action>
    1. Overhaul `server/lib/newsFilter.ts`:
       - Replace the existing `CONFLICT_KEYWORDS` Set with two new exported Sets:
         a. `NON_AMBIGUOUS_KEYWORDS`: only 7 terms per locked decision -- `airstrike`, `missile`, `bombing`, `shelling`, `casualties`, `invasion`, `drone`
         b. `AMBIGUOUS_KEYWORDS`: everything else from old CONFLICT_KEYWORDS plus the expanded terms from CONTEXT.md -- all geographic, diplomatic, organizational, military, and conflict terms
       - Keep `CONFLICT_KEYWORDS` as a backward-compat export: `export const CONFLICT_KEYWORDS = new Set([...NON_AMBIGUOUS_KEYWORDS, ...AMBIGUOUS_KEYWORDS])` -- preserves the test that checks `CONFLICT_KEYWORDS.size`
       - Expand `EXCLUSION_PATTERNS` array with the full list from RESEARCH.md (historical/documentary, entertainment, sports expanded, education/academic, technology, weather/natural)

       - Update `matchesKeywords` to use the new classification:
         - Non-ambiguous keywords pass alone
         - Ambiguous keywords only count when a non-ambiguous keyword is also present
         - This is the same logic but with drastically different keyword categorization

       - Import `extractTriple` from `./nlpExtractor.js`
       - Import `computeRelevanceScore` from `./relevanceScorer.js`
       - Import `getConfig` from `../config.js`

       - Create new exported function `filterAndScoreArticles(articles: NewsArticle[]): NewsArticle[]`:
         a. For each article:
            - Extract triple: `const triple = extractTriple(article.title, article.summary)`
            - Compute score: `const relevanceScore = computeRelevanceScore({ triple, source: article.source, title: article.title, summary: article.summary })`
            - Get matched keywords (using updated matchesKeywords)
            - If relevanceScore >= threshold AND keywords.length > 0: include article with enriched fields
            - Enrich article: `{ ...article, actor: triple.actor ?? undefined, action: triple.action ?? undefined, target: triple.target ?? undefined, relevanceScore, keywords: matched }`
         b. Read threshold from `getConfig().newsRelevanceThreshold`
         c. Return filtered, enriched articles

       - Keep `filterConflictArticles` as a deprecated export (backward compat) that delegates to `filterAndScoreArticles`

    2. Rewrite `server/__tests__/lib/newsFilter.test.ts`:
       - Update the CONFLICT_KEYWORDS test to verify backward compat Set exists
       - Add new describe block for keyword reclassification:
         - Test each non-ambiguous keyword passes alone
         - Test geographic terms are ambiguous (iran, israel, tehran, etc.)
         - Test diplomatic terms are ambiguous
         - Test org names are ambiguous
       - Add false positive regression tests:
         - "Tehran condemns airstrike in Yemen" -- should pass keywords (has "airstrike") but verify enriched fields
         - "Iran celebrates New Year with fireworks" -- should be excluded
         - "Iran World Cup football match results" -- should be excluded
       - Add filterAndScoreArticles integration tests:
         - Verify articles get enriched with actor/action/target/relevanceScore
         - Verify articles below threshold are excluded
         - Verify empty input returns empty output
       - Preserve existing tests that still apply (word boundary, case insensitive, title+summary combined)
       - Remove/update tests that conflict with new keyword classification

    3. Verify: `npx vitest run server/__tests__/lib/newsFilter.test.ts`
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/newsFilter.test.ts</automated>
  </verify>
  <done>newsFilter.ts overhauled with 7-term non-ambiguous set, all other keywords ambiguous, NLP scoring pipeline integrated, expanded exclusion patterns, filterAndScoreArticles enriches and filters articles by 0.7 threshold, all tests pass including false positive regression</done>
</task>

<task type="auto">
  <name>Task 2: Wire scored filter into news route and update route tests</name>
  <files>server/routes/news.ts, server/__tests__/routes/news.test.ts</files>
  <action>
    1. Update `server/routes/news.ts`:
       - Change import from `filterConflictArticles` to `filterAndScoreArticles` from `../lib/newsFilter.js`
       - In the route handler, step 4, replace:
         `const filtered = filterConflictArticles(allArticles);`
         with:
         `const filtered = filterAndScoreArticles(allArticles);`
       - No other changes needed -- the function returns the same type (NewsArticle[]) with additional enriched fields

    2. Update `server/__tests__/routes/news.test.ts`:
       - Update the "response shape" test to verify enriched fields on articles:
         - If clusters exist, check that `primaryArticle` has `relevanceScore` property (number)
         - Optionally check for `actor`, `action`, `target` properties (may be undefined)
       - Update test article fixtures to use titles that will pass the new stricter filter:
         - Ensure test articles contain non-ambiguous keywords (e.g., "airstrike", "missile")
         - The current fixture titles like "Iran airstrike targets military base" should still work since "airstrike" is non-ambiguous
         - Update "Tensions escalate as Iran deploys troops near border" -- may need a non-ambiguous term. Change to include "missile" or "airstrike" if it now fails (since "tensions", "iran", "troops" are all now ambiguous)
       - Run existing tests to see which pass/fail with new filter, then fix fixtures
       - The route itself does not need mock changes -- it calls the real filter which internally uses NLP

    3. Run full server test suite to confirm no regressions:
       `npx vitest run server/`
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/routes/news.test.ts server/__tests__/lib/newsFilter.test.ts</automated>
  </verify>
  <done>News route uses filterAndScoreArticles, response includes enriched articles with relevanceScore/actor/action/target, all server tests pass including route integration tests</done>
</task>

</tasks>

<verification>
- `npx vitest run server/` -- full server test suite green
- `npx tsc --noEmit` -- no TypeScript errors
- "Tehran condemns airstrike in Yemen" test case demonstrates improved filtering
- Articles with only geographic/diplomatic/org terms no longer pass without a non-ambiguous co-occurring term
- Response shape preserved (CacheResponse<NewsCluster[]>) with enriched article fields
</verification>

<success_criteria>
1. newsFilter.ts uses strict 7-term non-ambiguous set per locked decision
2. All geographic, diplomatic, and organizational terms are ambiguous
3. NLP triple extraction and relevance scoring integrated into filter pipeline
4. Articles below 0.7 threshold filtered out
5. News route returns enriched articles with actor/action/target/relevanceScore
6. Full server test suite passes
7. False positive regression test for "Tehran condemns attack in Yemen" scenario passes
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-gdelt-news-relevance-filtering/21.1-02-SUMMARY.md`
</output>

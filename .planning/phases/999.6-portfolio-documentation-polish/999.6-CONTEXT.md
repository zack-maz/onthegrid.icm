# Phase 999.6: Portfolio Documentation Polish — Context (BACKLOG)

**Captured:** 2026-06-03 (at v1.5 close)
**Status:** Backlog — promote at v1.6 or later via `/gsd-review-backlog`
**Parent:** None — standalone portfolio-targeted phase
**Branch discipline:** Per `feedback_branch_per_phase.md`, this phase will get its own feature branch on promotion.

## Goal

Round out the **agentic-development portfolio surface** of Iran Monitor for the v1.6 reveal. Not commercial polish — first-portfolio-piece polish: tell the story of how the project was built with Claude Code, make the engineering depth navigable for outside readers, and humanize the product arc from "I want to track Iran conflict events" → "10 phases of LLM reliability." The existing surface (564-line README + 11 ADRs + 12 architecture docs + 21 Mermaid diagrams + 676-line SRE runbook + degradation contract) is strong; this phase fills the **agentic-dev meta-story** + **navigation hub** + **glossary** + **transparency** gaps.

## Why now (v1.6 backlog, not v1.5 close)

Phase 36 (Public Docs Sweep, closed 2026-05-30) covered the **technical** docs surface (README + architecture + runbook + degradation + OpenAPI). It did NOT cover the **narrative + agentic-dev meta + portfolio-tour** surfaces, because those weren't in v1.5 scope (DOCS-PUB-01..05 + DOCS-API-01..07 were all technical). Promoting these to v1.6 makes sense alongside REVEAL-01 + REVEAL-02 (public reveal polish) since they share the audience — portfolio visitors, not operators.

## Audience

- **Primary:** portfolio visitors (engineers, hiring managers, fellow agentic-dev practitioners) browsing the GitHub repo for the first time.
- **Secondary:** the operator (you) — better cross-linking + onboarding from a future "I haven't touched this in 6 months" return visit.
- **Tertiary:** fellow agentic-dev practitioners curious about the GSD workflow + Claude Code usage shape.

**Audience NON-goals:** commercial customers (no commercial intent); enterprise SREs (the existing runbook covers operational depth).

## Scope (10 deliverables)

The 10 doc surfaces grouped by impact:

### Top-3 (highest signal for agentic-dev portfolio)

1. **`docs/BUILDING-WITH-CLAUDE-CODE.md`** — the meta-story. The killer differentiator. Cover: what the `/gsd` workflow is, what a phase looks like (CONTEXT → DISCUSSION → PLAN → EXECUTE → VERIFY), where compounding worked (mechanical drift gates, parallel agents, probe-before-commit), where it didn't (Phase 26.2 NLP scrap = 2 weeks committed then deleted; Phase 31 closed early at Day 1/7 with caveat that surfaced 23 days later; Phase 34 honest deferral; Phase 37 architectural-mismatch unblocker PRs), cost observations (phases-per-week, Claude Code session shape, what the agents are good at vs. what still needs human judgment). Synthesize from `.planning/RETROSPECTIVE.md` + MILESTONES.md + phase 37-SUMMARY framing-gap callouts. Target ~600-1000 lines portfolio-readable prose. **This is the single biggest move.**

2. **`docs/SHOWCASE.md` (or `docs/portfolio/INDEX.md`)** — the guided tour. A 1-page hub for portfolio visitors. "Start with the hero GIF" → "Want decisions? Read ADR-0005 (NLP scrap) + ADR-0010 (v1.5 close)" → "Want architecture? Start with `system-context.md`" → "Want operations? Read the runbook" → "Want how it was built? Read BUILDING-WITH-CLAUDE-CODE.md" → "Want the codebase? Start with `src/components/map/BaseMap.tsx`." Without this hub, engineering depth is invisible from outside the repo.

3. **`docs/JOURNEY.md`** — the product arc narrative. Portfolio-readable from "March 13, 2026: I want to track Iran conflict events" → "June 3, 2026: shipped 10 phases of LLM reliability" with the WHY for each milestone (v0.9 = "can I even render a map?" → v1.5 = "is the LLM pipeline actually reliable?"). Synthesizes `.planning/RETROSPECTIVE.md` into something a non-engineer can read. Includes a Mermaid gantt of the 7 milestones over ~3 months.

### Round-out (4-6)

4. **`docs/concepts.md`** — glossary. ~30 proprietary terms defined: **Pitfall 1 cache bridge**, **LLM-optional architecture**, **tier-green gate**, **polite-citizen contracts**, **ghost event**, **canonical actor catalog**, **mechanical drift gate**, **degrade-open**, **6-path resolver**, **honest deferral**, **probe-before-commit**, etc. Lowers the bar for any visitor to read the architecture docs.

5. **`docs/COSTS.md`** — transparency. Honest accounting: Vercel Pro $20/mo, NIM free, Upstash free tier, GDELT/OpenSky/adsb.lol/Open-Meteo/Yahoo Finance/AISStream/Overpass/WRI/Natural Earth/GeoEPR all free, Claude Code costs for development (session count, model mix, approximate cost per phase). The "you can do this too" angle. Portfolio-relevant.

6. **`docs/operator-guide.md`** — how-to. Different from runbook (incident response). "You're a visitor, here's what you can do":
   - Clone + run locally (`npm install`, `.env.example` setup)
   - Force-trigger the cron (`?force=true` + Bearer)
   - Prune dead URLs from the dashboard
   - Read the `/api/operator-status` payload
   - Run the eval harness (`npm run eval:replay`)
   - Capture a fresh hero GIF (`npm run capture:hero`)

### Polish (7-10)

7. **`public/screenshots/`** extension. Hero GIF already exists. Add ~10 layer-by-layer screenshots: each visualization layer (geographic, weather, political, ethnic, water stress, threat density), the API Health dashboard, threat-density clusters with click-through, actor-quality drill-down, ghost-event prune flow. Extend `scripts/capture-hero.ts` to `npm run capture:layers` so screenshots are reproducible / regeneratable.

8. **`docs/LESSONS.md`** — distilled retrospective. Pull the "Key Lessons" sections from `.planning/RETROSPECTIVE.md` (currently buried in `.planning/`) to a single 1-page portfolio-readable doc. Surface the gold:
   - "Probe-before-commit for documentation reconciliation"
   - "Honest deferral as a 1st-class outcome"
   - "Mechanical drift gates compound"
   - "Deletion over deprecation when rollback is `git revert`-able"
   - "Architecture decisions cascade into audit-tier semantics" (the Phase 37 lesson)

9. **`docs/timeline.md`** — Mermaid gantt of milestones. Visual "at a glance" for portfolio visitors. Could also be folded inside JOURNEY.md instead of standalone — operator decides during execution.

10. **Brainstorms cleanup.** `docs/brainstorms/2026-03-13-iran-conflict-monitor-brainstorm.md` (project origin) + `docs/superpowers/plans/` (4 plans) + `docs/superpowers/specs/` (4 specs) are orphaned agentic-dev artifacts in subdirectories no portfolio visitor will browse. Either: (a) add an index page so they're discoverable, (b) cross-link from BUILDING-WITH-CLAUDE-CODE.md as "see the original brainstorm", or (c) consolidate the most interesting bits into BUILDING-WITH-CLAUDE-CODE.md as historical receipts and archive the rest.

## Out of scope

- ❌ Migration to a formal docs site (Docusaurus / Mintlify) — `docs/` rendered natively on GitHub is appropriate for a first portfolio piece; a separate docs site over-engineers it.
- ❌ More ADRs added post-shipping — 11 is the right count for a personal project; ceremonial ADRs are performative.
- ❌ More architecture diagrams — 21 Mermaid blocks across 12 files is already comprehensive.
- ❌ API client libraries / SDKs — out of scope for a single-user dashboard.
- ❌ Marketing copy / landing page — REVEAL-01 / REVEAL-02 cover the public-facing reveal surface separately.

## Suggested execution structure

If promoted as a single phase: 3 waves matching the impact tiers above.

- **Wave 1 (highest signal):** items 1 + 2 + 3 — meta-story + showcase hub + journey narrative. Land together so the hub can reference the meta-story and the journey. ~1 evening of work.
- **Wave 2 (round-out):** items 4 + 5 + 6 — glossary + cost transparency + operator guide. Independent; could parallelize. ~1 weekend.
- **Wave 3 (polish):** items 7 + 8 + 9 + 10 — screenshots + lessons + timeline + brainstorms cleanup. Lowest urgency; could defer to v1.7 or fold into a future docs sweep.

## Source material already in place

- `.planning/RETROSPECTIVE.md` — 7 milestone retrospectives with "What Worked / Inefficient / Patterns / Lessons / Cost" sections per milestone. Primary source for BUILDING-WITH-CLAUDE-CODE.md + LESSONS.md.
- `.planning/MILESTONES.md` — 7 milestone entries with key accomplishments + quantitative snapshot + migration notes. Primary source for JOURNEY.md.
- `.planning/milestones/v1.5-phases/37-adr-0010-acceptance-gate-closeout/37-SUMMARY.md` — framing-gap callouts + per-phase rollup. Source for the Phase 37 anecdote in BUILDING-WITH-CLAUDE-CODE.md.
- `docs/adr/0005-phase-26-2-nlp-approach-scrapped.md` — 300-line honest retrospective on the NLP scrap. Already exists; cross-link as the "honest failure" exhibit.
- `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` — milestone-final ADR. Already exists; cross-link as the "honest deferral" exhibit.
- `scripts/capture-hero.ts` — existing Playwright-based hero GIF generation. Extension surface for item 7.
- `README.md` — already portfolio-tuned with hero GIF + Engineering Documentation block. SHOWCASE.md cross-links into it; doesn't replace it.

## Promotion gate

None. This is portfolio polish, not engineering work — no dependencies, no acceptance criteria, no measurement bar. Promote at v1.6 start based on operator priority (vs. Phase 999.5 Performance Load Test which is mechanically gated and should land first).

## Notes for future promotion

- Wave 1 alone is ~80% of the portfolio-piece signal. If time-constrained, ship Wave 1 + skip the rest until a future milestone.
- BUILDING-WITH-CLAUDE-CODE.md is the **single biggest move** for an agentic-dev portfolio piece. Few projects document this. Worth the most care.
- If during execution the operator decides the project's audience has shifted (e.g. public reveal lands with media coverage → commercial leads start arriving), re-scope to add a "/landing" doc surface. Until then, keep the audience clearly "portfolio visitors" — not enterprise customers.

---

_For the v1.6 cleanup-and-fix surface that pairs with this portfolio work, see `project_v1_6_cleanup_punchlist.md` + `project_v1_6_docs_drift.md` in operator memory._

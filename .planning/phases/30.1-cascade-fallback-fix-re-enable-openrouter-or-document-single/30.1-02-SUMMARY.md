---
phase: 30.1
plan: 02
status: skipped
skipped_reason: 'mutually-exclusive — probe landed nim-only bucket (90.0%), routing to Plan 04'
completed_at: '2026-05-17T20:12:00Z'
commits: []
---

# Plan 30.1-02 — Skipped (mutually exclusive Wave 2 branch)

## Why skipped

Wave 2 has three mutually exclusive plans (02 / 03 / 04), one per D-05
probe-result bucket. The probe (`scripts/probe-openrouter.ts`, Plan 01)
landed `nim-only` (27/30 rate-limited = 90.0%), which by D-05 thresholds
routes execution to **Plan 04** (Minimum scope, docs-only).

Plan 02 is the `restored-cascade` branch — it would have:

- removed `skipOpenRouter: true` from `server/lib/llmEventExtractor.v3.ts:622, 929`
- rewritten `server/__tests__/lib/freeClaudeRouter.test.ts:262-273`
- force-triggered a validation cron + asserted OR rows in `routingTrace`
- amended the same docs as Plan 04 (with restored-cascade wording instead)

None of that work was performed.

## Probe evidence

```json
{
  "rateLimitedCount": 27,
  "rateLimitedPct": 90,
  "decision": "nim-only"
}
```

Snapshot: `.planning/phases/30.1-cascade-fallback-fix-re-enable-openrouter-or-document-single/30.1-or-pulse-snapshot.json`.

## When this plan would re-fire

If a future probe re-run (`npm run probe:openrouter`) lands
`summary.decision === 'restored-cascade'` (< 50% rate-limited),
Plan 02's tasks are the recipe — see `30.1-02-PLAN.md` for the full
6-task breakdown.

## Server code state preserved

`grep -c "skipOpenRouter: true" server/lib/llmEventExtractor.v3.ts` → 2
(unchanged).

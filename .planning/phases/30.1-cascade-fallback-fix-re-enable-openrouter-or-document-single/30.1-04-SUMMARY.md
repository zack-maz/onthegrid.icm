---
phase: 30.1
plan: 04
status: complete
completed_at: '2026-05-17T20:12:00Z'
commits:
  - sha: 32ea1af
    subject: 'docs(30.1): amend architecture + ADR-0010 sub-block — NIM-only active, OpenRouter dormant (D-01, D-08, D-10, D-11, D-12, D-13)'
---

# Plan 30.1-04 — NIM-only declared honest in three surfaces

## What landed

One atomic docs-only commit (`32ea1af`) amending three files so the
architecture-level claim, the decision record, and the Claude-context
instructions all tell the same honest NIM-only story:

1. **`docs/architecture/llm-pipeline-reliability.md`** — new
   `## Cascade Reality (Phase 30.1, 2026-05-17)` section inserted
   between Retired Mechanisms and 7-Day Watch. Probe result table
   (Date / Model / N / rate_limit / other_error / ok), NIM-only
   decision rationale, the **negative-evidence invariant** (routingTrace
   must contain ZERO `provider: 'openrouter'` rows in the daily cron
   under the NIM-only configuration), the D-08 raw-GDELT terminal-fallback
   paragraph (mandatory in both branches per Specifics paragraph 4), and
   a "What changes if a future probe re-restores OR" recipe.

2. **`docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`** —
   new `## Phase 30.1 Sub-block (appended 2026-05-17)` appended after
   the Phase 30 sub-block (D-12 placement). `<expand_at_36>` marker
   preserved untouched for Phase 36's responsibility. Records the
   NIM-only decision + Phase 31 follow-up candidates (paid-OR conversion,
   adaptive Retry-After-aware NIM limiter, NIM model switch, dashboard
   surface, quarterly OR probe re-run).

3. **`CLAUDE.md`** — single-line replacement of the "Active providers"
   bullet per D-13 verbatim wording:
   - Old: `NVIDIA NIM (primary, qwen-235b instruct model) + OpenRouter (fallback). Prior providers retired Phase 29 — see ADR-0010 for the narrowed-cascade rationale.`
   - New: `qwen-235b instruct model. OpenRouter fallback dormant — see \`docs/architecture/llm-pipeline-reliability.md\` for re-validation history.`

   No parenthetical embellishment. Phase 29 D-06 5018-token budget
   preserved.

## Probe percentages cited verbatim in commit body (D-10 evidence)

Commit body opens with:

> `Re-tested OR free-tier 2026-05-17 via scripts/probe-openrouter.ts: 27/30 rate_limited (90.0%). Conclusion: NIM-only active; OpenRouter dormant pending Phase-31-or-later re-validation. Phase 27.4.4's 16/16 measurement stale by 2 months but the free tier is still not viable.`

## Staleness variant used

`stale by 2 months but the free tier is still not viable` —
selected per the D-10 wording template because `rateLimitedPct = 90 < 95`,
so the "still holds" variant did not apply. `monthsDelta` computed as
`floor((2026-05-17 - 2026-03-01) / 30) = 2`.

## Server code untouched (T-30.1-16 mitigation)

`grep -c "skipOpenRouter: true" server/lib/llmEventExtractor.v3.ts` → **2**
(both call sites preserved: `:622` main batch path, `:929` split-half
retry path). No code change in Phase 30.1 — Minimum scope is docs-only
by design.

## Branch state

`feature/30.1-cascade-fallback-fix` (cut from main at `8dcd6e1`), ready
for PR + squash-merge to main. Branch contains:

```
32ea1af docs(30.1): amend architecture + ADR-0010 sub-block — NIM-only active, OpenRouter dormant
0834ff9 feat(30.1): add scripts/probe-openrouter.ts + 30.1-or-pulse-snapshot.json (D-03..D-06)
2ef9fab docs(30.1): plan cascade fallback fix (4 plans, probe-then-decide flow)
dcd0d29 docs(state): record phase 30.1 context session
aab2177 docs(30.1): capture phase context
```

## Phase 31 follow-up candidates (carried forward)

1. **Paid-OR conversion** — ~$0.04/day (~$1.20/mo) for full coverage; seed Q4.
2. **Adaptive Retry-After-aware NIM limiter** — Phase 30 D-01's
   `retryAfterMs` field is already on `callHistory`; wire it into
   `nvidiaNimWindow` so post-429 calls wait the server-requested duration.
3. **NIM model switch** to a lower-cap-friendly variant (would require
   fresh bake-off vs Phase 27.4.1's qwen-235b lock).
4. **Dashboard surface for cascade-degraded state** — its own phase;
   overlaps Phase 32 + Phase 34.
5. **Re-run `scripts/probe-openrouter.ts` quarterly** to catch envelope
   improvements that would unlock the free-tier restore.

## Plans 02 and 03 — not executed (mutually exclusive by design)

Per D-05 thresholds, Wave 2 has three mutually exclusive plans gated on
the probe bucket. The probe landed `nim-only` (90.0% rate-limited), so
Plan 04 fired and Plans 02 (restored-cascade) and 03 (middle-bucket-defer)
were intentionally skipped — they're the alternative branches of the same
decision and cannot all run on the same probe outcome. Their PLAN.md files
remain in the phase directory as evidence of the discuss-phase / plan-phase
decision flow; no SUMMARY.md is appropriate for an unexecuted branch.

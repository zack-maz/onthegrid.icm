---
phase: 22.1-fixing-dispersion
verified: 2026-04-02T19:06:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 22.1: Fixing Dispersion & Camera Fly-To Verification Report

**Phase Goal:** Fix dispersion pipeline so events visually spread instead of stacking, and fix camera fly-to to fire once per unique near: pin instead of continuously re-centering
**Verified:** 2026-04-02T19:06:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                                       | Status     | Evidence                                                                                                             |
|----|-----------------------------------------------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------------|
| 1  | City-centroid events are visually spread into concentric rings instead of stacking                                           | VERIFIED   | `disperseEvents()` called on merged set in `events.ts` line 112; test "applies disperseEvents to merged result" passes |
| 2  | Dispersion operates on the full merged event set (cached + backfill + fresh), not per-batch                                  | VERIFIED   | `const dispersed = disperseEvents(merged)` at line 112, after all three sources merged into `eventMap`              |
| 3  | `parseAndFilter` returns undispersed events so audit script gets raw coordinates                                             | VERIFIED   | Line 342 `return results;` with comment "Dispersion is applied downstream"; gdelt.test.ts asserts `originalLat` is undefined |
| 4  | Redis cache stores dispersed coordinates so dispersion runs only on cache write, not every read                              | VERIFIED   | `cacheSetSafe(EVENTS_KEY, dispersed, ...)` at line 114 caches the dispersed output                                  |
| 5  | Camera flies to near: location once when proximity pin coordinates change                                                    | VERIFIED   | `pinChanged` guard at lines 506-516; test "calls setFlyToTarget when near: pin is first set" passes                 |
| 6  | Camera does NOT re-fly when other search terms change while near: pin stays the same                                         | VERIFIED   | `lastFlownPinRef` ref comparison prevents duplicate fly; test "does NOT call setFlyToTarget when pin coordinates unchanged" passes |
| 7  | Clearing near: resets fly-to state so re-entering near:Tehran flies again                                                    | VERIFIED   | `else if (lastFlownPinRef.current !== null)` branch at line 522 resets ref; test "resets fly-to state when near: is cleared" passes |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact                                              | Expected                                      | Status     | Details                                                                            |
|-------------------------------------------------------|-----------------------------------------------|------------|------------------------------------------------------------------------------------|
| `server/routes/events.ts`                             | Post-merge `disperseEvents()` call before cache write | VERIFIED | Lines 109-114: comment + `disperseEvents(merged)` + `cacheSetSafe(EVENTS_KEY, dispersed, ...)` |
| `server/adapters/gdelt.ts`                            | `parseAndFilter` returns raw undispersed events | VERIFIED  | Line 342: `return results;` — no `disperseEvents` import anywhere in file          |
| `server/lib/geoValidation.ts`                         | Exports `CENTROID_TOLERANCE` shared constant  | VERIFIED   | Lines 172-176: `export const CENTROID_TOLERANCE = 0.01;` with JSDoc               |
| `server/lib/dispersion.ts`                            | Imports `CENTROID_TOLERANCE` from geoValidation | VERIFIED  | Line 5: `import { detectCentroid, CITY_CENTROIDS, CENTROID_TOLERANCE } from './geoValidation.js'` |
| `server/__tests__/routes/events.test.ts`              | Test verifying dispersion applied after merge | VERIFIED   | Lines 411-480: "Post-merge dispersion" describe block, full centroid events spread test |
| `src/hooks/useQuerySync.ts`                           | Ref-based fly-to deduplication guard          | VERIFIED   | Line 368: `lastFlownPinRef`; lines 504-525: `pinChanged` guard + reset logic      |
| `src/hooks/useQuerySync.test.ts`                      | Tests for fly-to dedup behavior               | VERIFIED   | Lines 308-408: 4 tests in "fly-to deduplication" describe block, all pass         |

---

### Key Link Verification

| From                          | To                               | Via                                                   | Status   | Details                                                                        |
|-------------------------------|----------------------------------|-------------------------------------------------------|----------|--------------------------------------------------------------------------------|
| `server/routes/events.ts`     | `server/lib/dispersion.ts`       | `import disperseEvents`, called after merge before cache write | WIRED  | Line 5 import + line 112 `disperseEvents(merged)` + line 114 caches `dispersed` |
| `server/lib/dispersion.ts`    | `server/lib/geoValidation.ts`    | `import CENTROID_TOLERANCE` shared constant           | WIRED    | Line 5 import; `CENTROID_TOLERANCE` used in `findCentroidKey()` at line 109   |
| `src/hooks/useQuerySync.ts`   | `notificationStore.setFlyToTarget` | `pinChanged` guard gates the call                   | WIRED    | Lines 506-516: `if (pinChanged) { ... setFlyToTarget(...); lastFlownPinRef.current = ... }` |

---

### Requirements Coverage

| Requirement | Source Plan    | Description                                                                            | Status        | Evidence                                                                                          |
|-------------|----------------|----------------------------------------------------------------------------------------|---------------|---------------------------------------------------------------------------------------------------|
| FIX-01      | 22.1-01-PLAN   | Dispersion runs post-merge in events route (single-pass, no slot collisions)           | SATISFIED     | `disperseEvents(merged)` in `events.ts` lines 109-114                                            |
| FIX-02      | 22.1-01-PLAN   | `parseAndFilter` returns raw undispersed coordinates                                   | SATISFIED     | `return results;` with comment; gdelt.test.ts asserts `originalLat` undefined post-parse         |
| FIX-03      | 22.1-01-PLAN   | `parseAndFilterWithTrace` returns raw undispersed coordinates for audit                | SATISFIED     | Lines 658-661 in gdelt.ts: comment explains audit records have raw coordinates; no disperseEvents call |
| FIX-04      | 22.1-02-PLAN   | Camera fly-to fires once per unique near: pin coordinate change                        | SATISFIED     | `lastFlownPinRef` + `pinChanged` guard; all 4 dedup tests pass                                   |
| FIX-05      | 22.1-02-PLAN   | Editing other query terms with same near: pin does NOT trigger re-fly                  | SATISFIED     | `pinChanged` is false when `prev.lat === pin.lat && prev.lng === pin.lng`; test passes            |
| FIX-06      | 22.1-01-PLAN   | `CENTROID_TOLERANCE` shared constant between `dispersion.ts` and `geoValidation.ts`   | SATISFIED     | Exported from `geoValidation.ts` line 176; imported in `dispersion.ts` line 5                    |

**Note on requirements documentation:** FIX-01 through FIX-06 are referenced in `ROADMAP.md` (line 69) and both PLAN files, but are NOT formally defined in `REQUIREMENTS.md` traceability table. This is an informational gap (the requirements are an inserted sub-phase fix, not part of the v1.3 milestoned requirements). No action required for phase sign-off but worth a note for documentation housekeeping.

---

### Anti-Patterns Found

None. All scanned files are clean:
- No TODO/FIXME/HACK/PLACEHOLDER comments in modified files
- No stub return patterns (`return null`, `return {}`, `return []` are all valid guard-clause returns)
- No console.log-only implementations

---

### Human Verification Required

**1. Visual Dispersion on Map**

**Test:** Open the app, let events load, zoom to Tehran (lat 35.69, lng 51.39). Multiple GDELT events geocoded to Tehran should visually spread into a ring pattern around the city center instead of stacking on a single point.
**Expected:** Events visible as a ring around Tehran at approximately 3km, 6km, and 9km radii with no overlap.
**Why human:** Visual spacing of DeckGL ScatterplotLayer points cannot be verified programmatically.

**2. Near: Camera Dedup in Live UI**

**Test:** Open search (Cmd+K), type `near:Tehran`, verify map flies to Tehran. Then add `type:airstrike` to the query without removing the near: tag. Map should NOT re-fly. Then clear the entire query and retype `near:Tehran` — map should fly again.
**Expected:** Single fly on first near:Tehran, no fly on term addition, fly again on re-entry after clear.
**Why human:** useRef state during a live React render cycle cannot be verified from test harness at integration level.

---

### Test Suite Results

All automated checks passed:

- `npx vitest run server/__tests__/lib/dispersion.test.ts server/__tests__/routes/events.test.ts server/__tests__/gdelt.test.ts` — **99 tests, 3 files, all pass**
- `npx vitest run src/hooks/useQuerySync.test.ts` — **35 tests, all pass** (4 new fly-to dedup tests included)

Commits verified in git log:
- `f645df7` — test(22.1-01): failing tests for dispersion pipeline relocation
- `ac4fde0` — feat(22.1-01): relocate disperseEvents to events route with shared tolerance constant
- `9770284` — test(22.1-02): failing test for fly-to deduplication guard
- `ee0c253` — feat(22.1-02): add ref-based fly-to deduplication guard to useQuerySync

---

_Verified: 2026-04-02T19:06:00Z_
_Verifier: Claude (gsd-verifier)_

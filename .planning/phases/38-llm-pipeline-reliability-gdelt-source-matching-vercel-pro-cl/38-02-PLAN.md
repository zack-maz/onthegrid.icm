---
phase: 38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl
plan: 02
type: execute
wave: 1
depends_on: ['38-01']
files_modified:
  - server/lib/llmExtractionPipeline.ts
  - server/lib/llmEventExtractor.ts
  - server/adapters/llm-provider.ts
  - server/lib/llmEventExtractor.v3.ts
  - server/routes/events.ts
  - server/lib/llmSchema.ts
  - server/__tests__/lib/llmSchema.test.ts
  - server/lib/pipelineAudit.ts
  - src/components/ui/DevApiStatus.tsx
  - src/hooks/useLLMStatusPolling.ts
  - server/openapi.yaml
  - server/config.ts
  - server/lib/replayQuota.ts
  - server/lib/freeClaudeRouter.ts
  - server/__tests__/lib/llmTokenBudget.test.ts
  - server/__tests__/lib/llmCircuitBreaker.test.ts
  - server/__tests__/adapters/llm-provider.test.ts
  - CLAUDE.md
  - docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
autonomous: true
requirements:
  [
    LLM-PURGE-01,
    LLM-PURGE-02,
    LLM-PURGE-03,
    LLM-PURGE-04,
    LLM-PURGE-05,
    LLM-PURGE-06,
    LLM-PURGE-07,
    LLM-PURGE-08,
    LLM-PURGE-09,
  ]

must_haves:
  truths:
    - 'No code path imports the llmEventExtractor.ts barrel stub, the callLLM shim, v1/v2 Zod schemas, the appendPipelineAudit writer, the PipelineFlipsBlock, OpenRouter daily-cap dead writers, or Cerebras/Groq env vars (SC38-3)'
    - 'isLLMConfigured remains importable (llmExtractionPipeline + events.ts depend on it) — the file is narrowed, not deleted (Pitfall 1)'
    - 'enrichedEventV3 still validates after enrichedEventV2 is collapsed (Pitfall 2 — v3 extends v2)'
    - 'OpenRouter stays a dormant key-gated provider (ADR-0010 semantics preserved) with the incrOpenRouterDaily/getOpenRouterDaily dead Redis counter removed (D-04 Path A)'
    - 'skipOpenRouter citations corrected to v3.ts:629/951 across CLAUDE.md + ADR-0010 (D-04)'
    - 'npm run typecheck exits 0 after every structural deletion (typecheck is the purge gate)'
  artifacts:
    - path: 'server/lib/llmExtractionPipeline.ts'
      provides: 'Direct import of processEventGroupsV3/geocodeEnrichedEventsV3 from llmEventExtractor.v3.js (stub barrel removed)'
      contains: 'llmEventExtractor.v3'
    - path: 'server/lib/llmSchema.ts'
      provides: 'Collapsed v3-only enrichedEventAny passthrough'
      contains: 'enrichedEventV3'
  key_links:
    - from: 'server/lib/llmExtractionPipeline.ts'
      to: 'server/lib/llmEventExtractor.v3.ts'
      via: 'direct import after stub deletion'
      pattern: "llmEventExtractor\\.v3"
    - from: 'server/routes/events.ts /llm-status'
      to: '(removed) listPipelineAudit'
      via: 'pipelineFlips plumbing deleted from the Promise.all + common block'
      pattern: 'pipelineFlips'
---

<objective>
LLM-PURGE strand — the Phase 29 finishing pass. Delete the v1/v2/shim/pipeline-audit/Cerebras-Groq/OpenRouter dead code, narrow shared modules to v3-only, rewrite false docstrings. Zero new capabilities; this is deletion + reference cleanup. `npm run typecheck` is the primary gate (deletion correctness = no dangling importers).

Purpose: SC38-3 — no code path imports the deleted surfaces; rollback path is `git revert <Phase 29 commit range>` only.

Output: stub deleted + pipeline rewired (PURGE-01), callLLM shim narrowed keeping isLLMConfigured (PURGE-02), stale headers rewritten (PURGE-03), llmSchema collapsed to v3 (PURGE-04), pipelineAudit Path-A full delete incl. the live /llm-status reader chain (PURGE-05), Cerebras/Groq env+tests purged (PURGE-06), CLAUDE.md adapter note deleted (PURGE-07), OpenRouter daily-cap writers removed + dormant gate preserved + skipOpenRouter citation fix (PURGE-08), stale writePartialCache comment fixed (PURGE-09).

Implements: D-03 (PURGE-05 Path A full delete), D-04 (PURGE-08 Path A gate-don't-delete + citation fix).

CRITICAL CORRECTION (re-grepped 2026-06-04, trust over RESEARCH/PATTERNS): `freeClaudeRouter.ts` lives at **server/lib/freeClaudeRouter.ts** (NOT server/adapters/). `incrOpenRouterDaily` is at `:310`, `getOpenRouterDaily` `:321`, cap check `:432`, writer call `:464`, snapshot `:644-646`. The openapi entry is at **server/openapi.yaml:1841** (NOT root openapi.yaml).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-CONTEXT.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-RESEARCH.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-PATTERNS.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
</context>

<artifacts_this_phase_produces>
This plan DELETES, it does not create new symbols. No new symbols for the source-grounding pass to exclude. Removed symbols (must end with zero importers): `llmEventExtractor.ts` barrel, `callLLM` export from `llm-provider.ts`, `enrichedEventV1`/`enrichedEventV2`/`batchResponseV2` exported schemas, `appendPipelineAudit`/`listPipelineAudit`, `PipelineFlipsBlock`, `pipelineFlips` field, `incrOpenRouterDaily`/`getOpenRouterDaily`, `CEREBRAS_API_KEY`/`GROQ_API_KEY` env keys.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Structural deletions — extractor stub, callLLM shim, llmSchema collapse (PURGE-01/02/04)</name>
  <files>server/lib/llmExtractionPipeline.ts, server/lib/llmEventExtractor.ts, server/adapters/llm-provider.ts, server/lib/llmSchema.ts, server/__tests__/lib/llmSchema.test.ts, server/__tests__/routes/events.test.ts</files>
  <read_first>
    - server/lib/llmEventExtractor.ts (95-line v3-only re-export barrel; exports `processEventGroups`, `geocodeEnrichedEvents`, the `ExtractorRun` tagged shape `{schemaVersion:'v3', events, matchedNewsByGroup, bellingcatByGroup}` and re-exported v3 types)
    - server/lib/llmExtractionPipeline.ts (`:42` imports from `./llmEventExtractor.js`; `:32` imports `isLLMConfigured` from `../adapters/llm-provider.js`; `:247` `if (!isLLMConfigured())`; the pipeline consumes the tagged `.schemaVersion`/`.events` shape)
    - server/adapters/llm-provider.ts (exports `callLLM` (re-exports `routerCallLLM` from freeClaudeRouter — NO live importers, safe delete) AND `isLLMConfigured()` (STILL imported by llmExtractionPipeline.ts:32 + events.ts:5 — KEEP). Pitfall 1: do NOT delete the whole file)
    - server/lib/llmSchema.ts (`enrichedEventV1` `:62`, `enrichedEventV2` `:106`, `enrichedEventV3 = enrichedEventV2.extend({...})` `:193` — Pitfall 2: v3 is DEFINED BY EXTENDING v2; `enrichedEventAny` discriminatedUnion `:212-215`; `batchResponseV2` `:487-494`; stale commentary `:209`)
    - server/__tests__/lib/llmSchema.test.ts (tests for V1/V2/batchResponseV2 — remove the dead-schema cases; keep v3)
    - 38-RESEARCH.md §"LLM-PURGE-01/02/04" + Pitfall 1 + Pitfall 2
  </read_first>
  <action>
    PURGE-01: Rewire llmExtractionPipeline.ts `:42` to import `processEventGroupsV3` + `geocodeEnrichedEventsV3` (and the needed v3 types) directly from `./llmEventExtractor.v3.js`. The stub wraps v3 in the tagged `ExtractorRun`/`GeocoderResult` shape; either inline that wrapper into the pipeline or call v3 directly and update the pipeline's `.schemaVersion`/`.events` consumption to match the v3 return shape. Then delete `server/lib/llmEventExtractor.ts`. Confirm `events.test.ts` mocks (coordinated with plan 01 / LLM-FIX-06) match the new direct v3 return shape.
    PURGE-02 (Pitfall 1): In llm-provider.ts delete ONLY the `callLLM` export, its `routerCallLLM` import, and the stale "v1+v2 extractors (deleted in Plan 05+06)" docstring. KEEP `isLLMConfigured()` (relocate is optional — simplest is to leave the file as a 1-function module). Do NOT delete the file. `llmResolver.ts:18` already imports `callLLM` from `./freeClaudeRouter.js` directly, so it is unaffected.
    PURGE-04 (Pitfall 2): In llmSchema.ts, inline `enrichedEventV2`'s fields into a standalone `enrichedEventV3` (or keep `enrichedEventV2` as an un-exported base const) so the v3 definition does not break. Delete the EXPORTED `enrichedEventV1`, `enrichedEventV2`, `batchResponseV2` and their `z.infer` type exports. Collapse `enrichedEventAny` (`:212`) to a single-arm `enrichedEventV3` passthrough (drop the discriminatedUnion). Remove the false `:209` commentary ("v1 retained for D-40 rollback" / "v2 retained as default LLM_PIPELINE_V2=true"). Remove the matching dead-schema test cases in llmSchema.test.ts. Grep `enrichedEventV1\|enrichedEventV2\|batchResponseV2` across `server/` first to confirm no other production importer.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/llmSchema.test.ts server/__tests__/lib/llmExtractionPipeline.test.ts server/__tests__/routes/events.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f server/lib/llmEventExtractor.ts` (the stub barrel is deleted)
    - `grep -rn "from './llmEventExtractor.js'\|from '../lib/llmEventExtractor.js'" server/` returns 0 (no importer of the deleted barrel)
    - `grep -rn "export function isLLMConfigured\|export const isLLMConfigured\|export { isLLMConfigured" server/adapters/llm-provider.ts` returns ≥1 (isLLMConfigured preserved)
    - `grep -rn "export.*callLLM" server/adapters/llm-provider.ts` returns 0 (callLLM export removed)
    - `grep -rn "export const enrichedEventV1\|export const enrichedEventV2\|export const batchResponseV2" server/lib/llmSchema.ts` returns 0
    - `npx vitest run server/__tests__/lib/llmSchema.test.ts` exits 0 and `npm run typecheck` exits 0 (v3 schema still validates)
  </acceptance_criteria>
  <done>Stub deleted + pipeline imports v3 directly, callLLM shim removed while isLLMConfigured preserved, llmSchema collapsed to v3-only passthrough, all touched suites + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: pipelineAudit Path-A full delete + OpenRouter daily-cap removal (PURGE-05/08)</name>
  <files>server/lib/pipelineAudit.ts, server/routes/events.ts, src/components/ui/DevApiStatus.tsx, src/hooks/useLLMStatusPolling.ts, server/openapi.yaml, server/lib/freeClaudeRouter.ts, server/lib/llmEventExtractor.v3.ts, CLAUDE.md, docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md</files>
  <read_first>
    - server/lib/pipelineAudit.ts (`:31` `appendPipelineAudit` writer; `:42` `listPipelineAudit` reader; `PipelineFlipEntry` `from/to` union type)
    - server/routes/events.ts (LIVE READER — `:25` `import { listPipelineAudit }`; `:364` `const [dlqRecent, recentEvents, paused, pipelineFlips] = await Promise.all([...])`; `:368` `listPipelineAudit(50).catch(() => [])`; `:394` `pipelineFlips,` in the common block — this WHOLE chain must be removed, not just the UI)
    - src/components/ui/DevApiStatus.tsx (`:2841` `function PipelineFlipsBlock({ flips }: { flips?: LLMStatus['pipelineFlips'] })`; `:3044` `<PipelineFlipsBlock flips={llmStatus.pipelineFlips} />` render call)
    - src/hooks/useLLMStatusPolling.ts (`:114` + `:304` `pipelineFlips?: Array<{...}>` on the LLMStatus type — remove the field so DevApiStatus' `LLMStatus['pipelineFlips']` reference resolves to nothing)
    - server/openapi.yaml (`:1841` `pipelineFlips:` schema entry — delete)
    - server/lib/llmEventExtractor.v3.ts (`:78` comment referencing `listPipelineAudit reader in events.ts /llm-status` — update/remove since the reader is being deleted)
    - server/lib/freeClaudeRouter.ts (`:310` `async function incrOpenRouterDaily()`, `:321` `getOpenRouterDaily`, `:432` `if (p.name === 'openrouter' && (await getOpenRouterDaily()) >= OPENROUTER_DAILY_CAP)`, `:464` `if (p.name === 'openrouter') await incrOpenRouterDaily()`, `:644-646` snapshot read; the OpenRouter provider entry whose client is null when `!env.OPENROUTER_API_KEY`)
    - server/__tests__/routes/events.prune.test.ts + events.replayQuota.test.ts (`listPipelineAudit: vi.fn(async () => [])` mock at `:167`/`:203` — remove these mock lines after the reader is deleted)
    - CLAUDE.md + docs/adr/0010 (the `skipOpenRouter` 622/929 citation drift — actual is v3.ts:629/951)
    - 38-RESEARCH.md §"LLM-PURGE-05/08" + Open-Q #1 (listPipelineAudit live reader confirmed at events.ts:368)
  </read_first>
  <action>
    PURGE-05 (D-03 Path A full delete): Delete `appendPipelineAudit` from pipelineAudit.ts. Because `listPipelineAudit` has a LIVE reader at events.ts:368 (NOT only the UI — re-grep confirmed), delete the whole chain: remove the `:25` import, remove `pipelineFlips` from the `:364` Promise.all destructure and the `listPipelineAudit(50)` call at `:368`, and drop `pipelineFlips` from the `:394` common block; then delete `listPipelineAudit` from pipelineAudit.ts. Delete `PipelineFlipsBlock` (DevApiStatus.tsx:2841) + its `:3044` render call. Remove the `pipelineFlips?` field from the LLMStatus type in useLLMStatusPolling.ts (`:114,:304`). Delete the `server/openapi.yaml:1841` `pipelineFlips:` entry. Remove the two `listPipelineAudit: vi.fn(...)` mock lines in events.prune.test.ts + events.replayQuota.test.ts. Update the v3.ts:78 stale comment. The `events:llm-pipeline-audit` Redis key drains on its 90d TTL — no migration.
    PURGE-08 (D-04 Path A: gate, don't delete): In freeClaudeRouter.ts remove `incrOpenRouterDaily` (`:310`), `getOpenRouterDaily` (`:321`), the cap-check at `:432`, the writer call at `:464`, and the snapshot read at `:644-646`. Keep OpenRouter as a dormant provider entry whose client is null when `!env.OPENROUTER_API_KEY` (preserve ADR-0010 "dormant, could wake if key set" semantics) — do NOT delete it from the cascade. The `llm:tokens:openrouter:YYYY-MM-DD` key drains on its 48h TTL. Fix the `skipOpenRouter: true` line-drift citations in CLAUDE.md + docs/adr/0010 from 622/929 to v3.ts:629/951. Grep to confirm `incrOpenRouterDaily`/`getOpenRouterDaily` have no other callers (e.g. a /llm-status budget read) before deleting.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/routes/ src/components/ui/__tests__/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "appendPipelineAudit\|listPipelineAudit\|PipelineFlipsBlock\|pipelineFlips" server/ src/ | grep -v "milestones/" | grep -v "SUMMARY\|PLAN\|RESEARCH\|PATTERNS\|CONTEXT"` returns 0 (whole chain gone — code + tests)
    - `grep -n "pipelineFlips" server/openapi.yaml` returns 0 (spec entry deleted)
    - `grep -rn "incrOpenRouterDaily\|getOpenRouterDaily" server/` returns 0
    - `grep -n "name === 'openrouter'\|name: 'openrouter'\|'openrouter'" server/lib/freeClaudeRouter.ts` returns ≥1 (OpenRouter dormant entry PRESERVED, only the daily-cap writer removed)
    - `grep -rn "622\|929" CLAUDE.md docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md | grep -i skipopenrouter` returns 0 and `grep -rn "629\|951" CLAUDE.md docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` shows the corrected citation
    - `npm run typecheck` exits 0 (LLMStatus type chain resolves with pipelineFlips removed)
  </acceptance_criteria>
  <done>pipelineAudit writer + reader + /llm-status plumbing + UI block + openapi entry + test mocks all deleted; OpenRouter daily-cap writers removed while the dormant key-gated provider is preserved; skipOpenRouter citations corrected; typecheck + route/UI suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Cerebras/Groq purge + stale-header/comment rewrites (PURGE-03/06/07/09)</name>
  <files>server/config.ts, server/lib/replayQuota.ts, server/__tests__/lib/llmTokenBudget.test.ts, server/__tests__/lib/llmCircuitBreaker.test.ts, server/__tests__/adapters/llm-provider.test.ts, server/lib/llmEventExtractor.v3.ts, server/routes/events.ts, server/lib/llmExtractionPipeline.ts, server/lib/freeClaudeRouter.ts, CLAUDE.md</files>
  <read_first>
    - server/config.ts (`:31-32` `CEREBRAS_API_KEY`/`GROQ_API_KEY` Zod `.default('')`; `:242` `apiKey: env.CEREBRAS_API_KEY`; `:245` `apiKey: env.GROQ_API_KEY` — these wire into a providers map that may need pruning)
    - server/lib/replayQuota.ts (`:21` threat-model comment citing "Cerebras free-tier 1M/day" — re-anchor to `nvidia_nim: 1_000_000`)
    - Cerebras/Groq test references (re-grepped 2026-06-04 — broader than the 2 CONTEXT named): llmTokenBudget.test.ts, llmCircuitBreaker.test.ts, llm-provider.test.ts, llmSchema.test.ts, llmEvalHarness.adversarial.test.ts, llm-optional.test.ts, refresh-events-cron.prune.test.ts, events.replayQuota.test.ts, events.prune.test.ts, events.test.ts, events-fallback.test.ts. `llmProgress.test.ts` is VERIFIED CLEAN (finding #26). TRIAGE: many are incidental fixture strings; `llm-provider.test.ts` is highest-risk (tests the shim modified in Task 1). Distinguish dead code paths from incidental strings.
    - server/lib/llmEventExtractor.v3.ts (`:1-15` header "NIM → OpenRouter cascade"; `:73-79` references deleted v1/v2)
    - server/routes/events.ts (`:99-107` `RecentEnrichedEvent` v2 projection docstring)
    - server/lib/llmExtractionPipeline.ts (`:11-12` "~95min worst case"; `:376-377` claims `writePartialCache` lives in v3 extractor — contradicts the truthful `:98`)
    - server/lib/freeClaudeRouter.ts (`:9-10` header lists `server/adapters/llm-provider.ts:23` as a live caller — becomes stale after Task 1 / PURGE-02)
    - 38-RESEARCH.md §"LLM-PURGE-03/06/07/09" + Assumption A3 (triage each test file)
  </read_first>
  <action>
    PURGE-06: Delete `CEREBRAS_API_KEY` + `GROQ_API_KEY` from config.ts (`:31-32`) and prune their wiring at `:242,:245` (and the surrounding provider-map entries if they are now dead). Grep `env.CEREBRAS\|env.GROQ` across `server/` first and confirm zero readers remain after the prune (currently only config.ts:242,245). Triage the ~11 test files: remove Cerebras/Groq references that test deleted code paths (llm-provider.test.ts highest-risk — reconcile with Task 1's shim narrowing), and leave purely-incidental fixture strings only if they do not assert a deleted path. Re-anchor the replayQuota.ts:21 threat comment from "Cerebras 1M/day" to "nvidia_nim: 1_000_000".
    PURGE-07: Docs-only (adapters already gone — VERIFIED zero Cerebras/Groq files in server/adapters/). Delete the stale CLAUDE.md note claiming "Cerebras + Groq adapter source files remain importable for emergency rollback today". Do NOT plan a file deletion — there is no file.
    PURGE-03: Rewrite stale headers: llmEventExtractor.v3.ts `:1-15` ("NIM → OpenRouter cascade" → "NIM primary; OpenRouter dormant-gated") and `:73-79` (remove deleted-v1/v2 references); freeClaudeRouter.ts `:9-10` (remove the `llm-provider.ts:23` live-caller line now stale post-PURGE-02); events.ts `:99-107` (`RecentEnrichedEvent` — drop the "v2 projection" framing); llmExtractionPipeline.ts `:11-12` ("~95min worst-case" → "~13min Pro 800s ceiling"). Docstring-only edits.
    PURGE-09: Rewrite the llmExtractionPipeline.ts `:376-377` comment to match the truthful `:98` ("writePartialCache writer was deleted in this phase").
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/llmTokenBudget.test.ts server/__tests__/lib/llmCircuitBreaker.test.ts server/__tests__/adapters/llm-provider.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "env\.CEREBRAS\|env\.GROQ\|CEREBRAS_API_KEY\|GROQ_API_KEY" server/config.ts` returns 0
    - `grep -rn "env\.CEREBRAS\|env\.GROQ" server/ | grep -v "__tests__"` returns 0 (no production reader remains)
    - `grep -n "nvidia_nim: 1_000_000\|nvidia_nim" server/lib/replayQuota.ts` returns ≥1 (threat comment re-anchored) and `grep -in "cerebras" server/lib/replayQuota.ts` returns 0
    - `grep -in "adapter source files remain importable" CLAUDE.md` returns 0 (stale rollback note deleted)
    - `grep -n "writePartialCache" server/lib/llmExtractionPipeline.ts | grep -v "deleted"` returns 0 (no comment still claims it lives in v3)
    - `npx vitest run server/__tests__/lib/llmTokenBudget.test.ts server/__tests__/lib/llmCircuitBreaker.test.ts server/__tests__/adapters/llm-provider.test.ts` exits 0 and `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Cerebras/Groq env vars + wiring + dead test references purged, replayQuota threat comment re-anchored to nvidia_nim, CLAUDE.md adapter-rollback note deleted, all four stale headers + the writePartialCache comment rewritten, triaged test suites + typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                                                                                  |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| env config → provider cascade     | `OPENROUTER_API_KEY` presence gates the dormant OpenRouter provider; deleting `CEREBRAS_API_KEY`/`GROQ_API_KEY` must not change the runtime NIM-only cascade |
| `/llm-status` aggregator → client | removing the `pipelineFlips` field from the wire contract must not break the degrade-open Promise.all (each block already `.catch`es)                        |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                             |
| ---------- | ---------------------- | ----------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38.02-01 | Tampering              | OpenRouter env-gating change (PURGE-08)   | mitigate    | Gate preserved (client null without key); only the dead Redis daily-cap counter removed; ADR-0010 dormant semantics intact; typecheck + cascade tests green |
| T-38.02-02 | Denial of service      | `/llm-status` after pipelineFlips removal | accept      | Each Promise.all block already `.catch(() => [])` degrades-open; removing one entry cannot introduce a 5xx — LOW, accepted                                  |
| T-38.02-03 | Information disclosure | deleted env keys (CEREBRAS/GROQ)          | mitigate    | Keys had `.default('')`; deletion removes dead config surface, no secret exposed; grep confirms no remaining reader                                         |
| T-38.02-SC | Tampering              | no package installs in this plan          | accept      | Pure deletion/refactor; no npm/pip/cargo install — supply-chain surface unchanged                                                                           |

</threat_model>

<verification>
- Per structural deletion: `npm run typecheck` (the PRIMARY purge gate — deletion correctness = no dangling importers) + `npx vitest run <touched test file>`.
- Plan gate: `npm run typecheck` PASS + `npx vitest run server/` green + the SC38-3 grep gates (no importer of any deleted surface) all return 0.
</verification>

<success_criteria>

- SC38-3: no code path imports the v1/v2 extractors, the deleted pipeline-override surface (already gone in v1.5), OpenRouter daily-cap dead writers, or Cerebras/Groq env vars; stale headers across v3 + events route + extraction pipeline rewritten; enrichedEventV1/V2 schemas + appendPipelineAudit writer + PipelineFlipsBlock deleted (Path A per locked D-03); skipOpenRouter citations corrected.
  </success_criteria>

<output>
Create `.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-02-SUMMARY.md` when done.
</output>

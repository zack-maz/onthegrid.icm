---
phase: 38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl
plan: 04
type: execute
wave: 2
depends_on: ['38-03']
files_modified:
  - scripts/audit-water-names.ts
  - server/__tests__/scripts/audit-water-names.test.ts
  - package.json
  - package-lock.json
  - server/lib/romanize.ts
  - server/__tests__/lib/romanize.test.ts
  - server/adapters/overpass-water.ts
  - server/__tests__/adapters/overpass-water.test.ts
  - server/types.ts
  - src/components/detail/WaterFacilityDetail.tsx
  - src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx
  - src/components/map/EntityTooltip.tsx
  - src/lib/searchUtils.ts
  - src/lib/__tests__/waterLabel.test.ts
autonomous: false
user_setup: []
requirements: [WATER-LATIN-01, WATER-LATIN-02, WATER-LATIN-03, WATER-LATIN-04]

must_haves:
  truths:
    - 'Water facilities with non-Latin names surface via a nameLatin field while the original name is preserved (SC38-5)'
    - 'The Overpass adapter applies romanization BEFORE the Latin-label admission gate so legitimate non-Latin infrastructure stops being dropped (SC38-5)'
    - 'Romanization clears the RESET acceptance bar — a machine-searchable Latin token that admits the facility — NOT pretty human romanization (D-08; abjad vowel-less ceiling)'
    - 'Consumer surfaces (detail panel, tooltip, search bar, proximity alerts) display nameLatin with the original available on hover/sub-label (SC38-5)'
  artifacts:
    - path: 'server/lib/romanize.ts'
      provides: 'transliteration wrapper + artifact-cleanup override pass (strip @→a/ah, lowercase emphatic S/H/T/D/Z, collapse separators)'
      min_lines: 30
    - path: 'scripts/audit-water-names.ts'
      provides: 'Audit of non-Latin water-facility names per script (Arabic/Persian/Hebrew counts + gate-rejection counts)'
      min_lines: 60
  key_links:
    - from: 'server/adapters/overpass-water.ts'
      to: 'server/lib/romanize.ts'
      via: "romanize non-Latin tags['name'] BEFORE hasLatinLabel gate at :836"
      pattern: 'romanize'
    - from: 'server/types.ts WaterFacility'
      to: 'nameLatin field'
      via: 'optional field added to the facility type'
      pattern: 'nameLatin'
---

<objective>
WATER-LATIN strand — romanize non-Latin water-facility names so the Latin-label admission gate (`overpass-water.ts:836`) stops dropping legitimate Arabic/Persian/Hebrew infrastructure. Audit-validated library lock-in, a romanization wrapper with an artifact-cleanup override pass, fetch-time injection before the gate, and consumer-surface display.

Purpose: SC38-5 — non-Latin facilities surface via `nameLatin` (original `name` preserved); the gate admits the romanized version; detail/tooltip/search/proximity display `nameLatin` with original-on-hover.

Output: `transliteration` installed (legitimacy-gated), `audit-water-names.ts`, `romanize.ts` wrapper, adapter injection + `nameLatin` on `WaterFacility`, consumer-surface updates.

Implements: D-08 — pure-JS `transliteration` package; audit BEFORE lock-in; RESET acceptance bar to "searchable Latin token that admits the facility" (RESEARCH: no pure-JS lib or ICU beats the abjad vowel-less ceiling, SKIP ICU); add a small artifact-cleanup override pass; preserve original `name`, add OPTIONAL `nameLatin`, romanize BEFORE the gate.

WAVE: This plan is wave 2 with `depends_on: [38-03]` — both this plan (`audit:water` script) and plan 03 (`audit:gdelt` script) append a `scripts` entry to package.json; serializing after 03 keeps the package.json edit conflict-free. (No code dependency on 03 otherwise — this strand is independent of the LLM core per D-01.)

NOTE: This plan is `autonomous: false` — the first task is a blocking `checkpoint:human-verify` for the `transliteration` package install (slopcheck was unavailable; RESEARCH manually verified positive but the graceful-degradation rule requires a human gate before install). The checkpoint is never auto-approvable (workflow.auto_advance ignored).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-CONTEXT.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-RESEARCH.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-PATTERNS.md
@scripts/audit-events.ts
@scripts/refresh-water-facilities.ts
</context>

<artifacts_this_phase_produces>
NEW symbols/files this plan creates (source-grounding pass MUST exclude these — they do not exist in current `main`):

- `server/lib/romanize.ts` — NEW transliteration wrapper + artifact-cleanup pass
- `server/__tests__/lib/romanize.test.ts` — NEW unit test
- `scripts/audit-water-names.ts` + `server/__tests__/scripts/audit-water-names.test.ts` — NEW audit (WATER-LATIN-01)
- `nameLatin?: string` field on `WaterFacility` (`server/types.ts:205`) — NEW optional field
- `transliteration@2.6.1` package + `npm run audit:water` script entry — NEW
  </artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify transliteration package legitimacy before install (WATER-LATIN-02)</name>
  <files>package.json (the install target — do NOT run `npm install` until this checkpoint is approved)</files>
  <action>Pause before installing `transliteration@2.6.1`. This is a blocking package-legitimacy gate (slopcheck unavailable → `[ASSUMED]` package per the security gate). Do NOT proceed to Task 1's `npm install` until the operator types "approved".</action>
  <what-built>About to install the `transliteration@2.6.1` npm package (WATER-LATIN-02, locked by D-08). slopcheck was UNAVAILABLE this session, so per the package-legitimacy gate this `[ASSUMED]` package requires a blocking human verification before install. RESEARCH performed manual verification (all signals strongly positive).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/transliteration — confirm it is the real package (yf-hk/transliteration), v2.6.1, last publish ~2026-01-20, ~494 dependents.
    2. Confirm `npm view transliteration scripts.postinstall` is empty (no install hook) and `npm view transliteration dependencies` is `{}` (zero transitive surface).
    3. Confirm the GitHub repo github.com/yf-hk/transliteration resolves (HTTP 200).
    4. Confirm this is NOT a slopsquat (no near-identical typo-name package, established maintainer `erichu`).
  </how-to-verify>
  <verify><human-check>Operator confirms the npmjs.com page, zero deps, no postinstall, and live repo for transliteration@2.6.1.</human-check></verify>
  <done>Operator types "approved" and Task 1 may install `transliteration@2.6.1`; or operator raises a concern and the executor stops (ICU is NOT auto-adopted — RESEARCH recommends against it).</done>
  <resume-signal>Type "approved" to proceed with `npm install transliteration@2.6.1`, or describe concerns (then the executor falls back to ICU per D-08 only if you direct it — RESEARCH recommends against ICU).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Install transliteration, build romanize wrapper, audit non-Latin names (WATER-LATIN-01/02)</name>
  <files>package.json, package-lock.json, server/lib/romanize.ts, server/__tests__/lib/romanize.test.ts, scripts/audit-water-names.ts, server/__tests__/scripts/audit-water-names.test.ts</files>
  <read_first>
    - 38-RESEARCH.md §"WATER-LATIN-02" (the empirical test table: بغداد→`bGdd`, محطة تحلية→`mHT@ tHly@`, تל אביב→`tl byb`; root cause: 1-to-1 code-point mapping, abjad vowel-less ceiling; RESET the bar to "searchable token"; artifact-cleanup overrides: strip `@`→`a`/`ah`, lowercase emphatic `S/H/T/D/Z`, collapse separators; if output <2 chars fall back to original name + country/type qualifier)
    - scripts/audit-events.ts + scripts/refresh-water-facilities.ts (CLI analog shape for audit-water-names.ts)
    - server/adapters/overpass-water.ts (`isLatin` at `:397` = `/^[\p{Script=Latin}\d\s\p{P}\p{S}]+$/u`; `hasLatinLabel` `:208`; `GENERIC_OSM_NAME_RE` `:206`) — the audit classifies which names FAIL isLatin and by which Unicode block (Arabic `؀-ۿ`, Hebrew `֐-׿`, Persian within Arabic block)
    - server/cache/redis.ts (`cacheGetSafe<T>`) — read `water:facilities:v3` for the audit
    - 38-PATTERNS.md §"WATER-LATIN-03" + §"WATER-LATIN-01"
  </read_first>
  <action>
    After the checkpoint approves, `npm install transliteration@2.6.1` (commit the package.json + package-lock.json delta).
    Create `server/lib/romanize.ts`: a `romanize(name: string): string` wrapper over `transliteration`'s `transliterate` + the artifact-cleanup override pass — strip/normalize ASCII artifacts (`@`→`a`/`ah`, lowercase the emphatic uppercase `S/H/T/D/Z`→`s/h/t/d/z`, collapse repeated separators), apply the existing title-case convention for display, and if the cleaned output is empty or <2 chars (pure-diacritic input) return a fallback (original `name` + a country/type qualifier) so the gate still admits. Add per-script overrides ONLY where the audit-sample shows the gate still rejects (e.g. Arabic ة).
    Write `server/__tests__/lib/romanize.test.ts` FIRST (TDD): assert romanize on the RESEARCH sample inputs (Arabic/Persian/Hebrew) produces a non-empty Latin token that passes `isLatin` and does NOT contain `@` or uppercase emphatic artifacts — the RESET "searchable token" bar, NOT pretty-name equality.
    Create `scripts/audit-water-names.ts` (analog audit script): read `water:facilities:v3`; count total facilities, count rejected by `hasLatinLabel`, and bucket samples per script (Arabic/Hebrew/Persian via Unicode-block test of names failing `isLatin`). Add a `package.json` `"audit:water"` script. Write `server/__tests__/scripts/audit-water-names.test.ts` asserting the per-script bucketing on a fixture set. This audit VALIDATES the transliteration quality against the reset bar before the adapter lock-in.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/romanize.test.ts server/__tests__/scripts/audit-water-names.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\"transliteration\"" package.json` returns ≥1 (installed)
    - `npx vitest run server/__tests__/lib/romanize.test.ts` exits 0 — romanize output for the Arabic/Persian/Hebrew samples passes `isLatin`, contains no `@`, and is ≥2 chars (the searchable-token bar)
    - `grep -n "romanize" server/lib/romanize.ts` returns ≥1 and the cleanup pass is present (`grep -in "@\|toLowerCase\|titleCase\|fallback" server/lib/romanize.ts` shows the artifact-cleanup logic)
    - `npx vitest run server/__tests__/scripts/audit-water-names.test.ts` exits 0 — per-script bucketing assertions pass
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>transliteration installed (post-checkpoint), romanize.ts clears the reset searchable-token bar with artifact cleanup + fallback, audit-water-names.ts quantifies non-Latin rejections per script, both unit tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapter injection (romanize before gate) + nameLatin field + consumer surfaces (WATER-LATIN-03/04)</name>
  <files>server/adapters/overpass-water.ts, server/__tests__/adapters/overpass-water.test.ts, server/types.ts, src/components/detail/WaterFacilityDetail.tsx, src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx, src/components/map/EntityTooltip.tsx, src/lib/searchUtils.ts, src/lib/__tests__/waterLabel.test.ts</files>
  <read_first>
    - server/adapters/overpass-water.ts (admission gate `:836` `if (facilityType !== 'desalination' && !hasLatinLabel(tags))`; `hasLatinLabel` `:208`; `GENERIC_OSM_NAME_RE` `:206`; `extractLabel` builds the facility label `:928`; desalination is gate-exempt — do NOT double-process it)
    - server/lib/romanize.ts (the wrapper built in Task 1)
    - server/types.ts (`WaterFacility` interface `:205`, `label: string` `:211` — add `nameLatin?: string`)
    - src/components/detail/WaterFacilityDetail.tsx (current `name`/`label` render — display `nameLatin` when set, original on hover/sub-label)
    - src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx (regression anchor — extend its fixture with `nameLatin`)
    - src/components/map/EntityTooltip.tsx (water-facility tooltip label)
    - src/lib/searchUtils.ts (search index — index `nameLatin` so operators can type the romanized name)
    - src/lib/__tests__/waterLabel.test.ts (`makeFacility` helper — extend with `nameLatin`)
    - 38-RESEARCH.md §"WATER-LATIN-03/04" + Pitfall 4 (do NOT adopt ICU) + the GENERIC_OSM_NAME_RE landmine (a romanized bare "سد"→"sd" is fine, but a facility named only generic must still be filtered)
  </read_first>
  <action>
    WATER-LATIN-03: In overpass-water.ts, BEFORE the `:836` gate, when `tags['name']` is non-Latin and there is no Latin `name:en`, call `romanize(tags['name'])` to produce a synthetic Latin string; inject it so `hasLatinLabel` admits (preserving the `GENERIC_OSM_NAME_RE` filter — a romanized generic must still be rejected as generic), and populate a new `nameLatin` field on the facility. Preserve the original `name` (keep the original in `label` / a preserved field — do NOT overwrite it). Do NOT double-process desalination (already gate-exempt). Add `nameLatin?: string` to `WaterFacility` in server/types.ts. Add an overpass-water.test.ts assertion that a fixture facility with an Arabic-only name now ADMITS past the gate, carries `nameLatin` set, and preserves the original `name`.
    WATER-LATIN-04: Update consumers to display `nameLatin` when set with the original on hover/sub-label — WaterFacilityDetail.tsx (sub-label or hover title), EntityTooltip.tsx (water label), and index `nameLatin` in searchUtils.ts so the search bar matches the romanized token. Extend the `makeFacility` helper in waterLabel.test.ts + the gateSwap fixture with `nameLatin`, and add render-contract assertions that `nameLatin` displays and the original is reachable.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/adapters/overpass-water.test.ts src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx src/lib/__tests__/waterLabel.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nameLatin" server/types.ts` returns ≥1 (`nameLatin?: string` added to WaterFacility) — optional so old cached entries (24h TTL) still validate
    - `grep -n "romanize" server/adapters/overpass-water.ts` returns ≥1 and the call is positioned BEFORE the `:836` gate
    - `npx vitest run server/__tests__/adapters/overpass-water.test.ts` exits 0 — the Arabic-only fixture now admits past the gate, `nameLatin` set, original `name` preserved, generic-name still rejected
    - `npx vitest run src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx src/lib/__tests__/waterLabel.test.ts` exits 0 — consumer render + search-index assertions pass
    - `grep -n "nameLatin" src/lib/searchUtils.ts` returns ≥1 (search indexes the romanized token)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Romanization injected before the Latin gate so Arabic/Persian/Hebrew facilities admit with nameLatin set and original preserved; detail panel, tooltip, and search surface nameLatin with original-on-hover; generic-name filter intact; all touched suites + typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                                                                                                      |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Overpass upstream → romanize() → search/display | untrusted OSM name strings flow through transliteration into the search index and DOM; romanized output must not enable injection (XSS into the React render or query injection into the search) |
| `transliteration` package install               | `[ASSUMED]` package (slopcheck unavailable) — blocking human checkpoint before install                                                                                                           |

## STRIDE Threat Register

| Threat ID  | Category                | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                        |
| ---------- | ----------------------- | ----------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38.04-01 | Tampering / injection   | romanize() output → search index + React render | mitigate    | transliteration emits ASCII-only tokens; the cleanup pass collapses separators; React escapes by default (no dangerouslySetInnerHTML); search matches as a string literal, not a query — no injection vector. Verify no `dangerouslySetInnerHTML` consumes `nameLatin` |
| T-38.04-02 | Tampering               | `transliteration` supply chain                  | mitigate    | Blocking `checkpoint:human-verify` before install; zero deps, no postinstall, 494 dependents, live repo (T-38.04-SC)                                                                                                                                                   |
| T-38.04-03 | Spoofing (data quality) | generic-name romanization bypassing the gate    | mitigate    | `GENERIC_OSM_NAME_RE` filter preserved post-romanization — a romanized bare "dam"/"reservoir" still rejected; test asserts it                                                                                                                                          |
| T-38.04-SC | Tampering               | npm install transliteration@2.6.1               | mitigate    | Pinned exact version; blocking human checkpoint (never auto-approvable); lockfile diff reviewed                                                                                                                                                                        |

</threat_model>

<verification>
- Per task: `npx vitest run <touched file>` + `npm run typecheck`.
- Plan gate: `npx vitest run server/ src/components/detail/ src/lib/` green + `npm run typecheck` PASS; the romanize searchable-token bar asserted (no `@`/uppercase-emphatic artifacts, isLatin passes); generic-name filter still rejects.
</verification>

<success_criteria>

- SC38-5: water facilities with non-Latin names surface via `nameLatin` while preserving original `name`; the Overpass adapter applies romanization BEFORE the Latin-label admission gate; detail panel, tooltip, search bar, and proximity alerts display `nameLatin` with original-on-hover.
  </success_criteria>

<output>
Create `.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-04-SUMMARY.md` when done. Record the D-08 library-evaluation outcome (kept `transliteration`, reset the bar to searchable-token, skipped ICU) and the per-script override list.
</output>

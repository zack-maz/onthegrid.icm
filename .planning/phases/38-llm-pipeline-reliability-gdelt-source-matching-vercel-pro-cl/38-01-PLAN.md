---
phase: 38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/routes/health.ts
  - server/__tests__/routes/health.test.ts
  - server/routes/water.ts
  - server/__tests__/routes/water.test.ts
  - server/lib/llmEvalHarness.ts
  - server/lib/llmProgress.ts
  - server/__tests__/lib/llmEvalHarness.test.ts
  - .planning/milestones/v1.5-phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md
  - server/__tests__/resilience/redis-death.test.ts
  - server/__tests__/resilience/quota-chaos.test.ts
  - server/__tests__/routes/events.test.ts
  - src/__tests__/lib/actorCatalog.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [LLM-FIX-01, LLM-FIX-02, LLM-FIX-03, LLM-FIX-04, LLM-FIX-05, LLM-FIX-06]

must_haves:
  truths:
    - 'The news health probe reports cache-fallback-active: while only the llmEvents probe reports llm-optional-fallback-active: (honest single-source semantics, SC38-1)'
    - 'On total Open-Meteo batch failure the water:precip cache receives a fresh distinguishable sentinel so the audit tier reads degraded-not-unknown (SC38-1)'
    - 'actorMatchRate returns null (not silent 0) when ground-truth actors are absent, and 33-AUDIT-REPORT.md documents the gap (SC38-2)'
    - 'The Redis-death chaos mock exposes incr/sadd/smembers/scard/srem/zadd/hset/hincrby/scan/lpush/expire and /api/operator-status returns no HTTP 500 under Redis death (SC38-2)'
    - 'A dedicated quota-path chaos test proves pruneQuota/replayQuota return 503-not-500 for the right reason when redis.incr itself throws (SC38-2)'
    - "events.test.ts mocks use schemaVersion: 'v3' (SC38-2) and full vitest suite exits 0 (CI green)"
  artifacts:
    - path: 'server/__tests__/resilience/quota-chaos.test.ts'
      provides: 'Dedicated redis.incr-throws chaos test for the quota path (LLM-FIX-05)'
      min_lines: 40
    - path: 'server/routes/health.ts'
      provides: 'Token-split probeCacheKey emitting cache-fallback-active: for news, llm-optional-fallback-active: only for llmEvents'
      contains: 'cache-fallback-active:'
    - path: 'server/routes/water.ts'
      provides: 'Empty-result sentinel write to water:precip on total batch failure'
      contains: 'failed: true'
  key_links:
    - from: 'server/routes/health.ts probeCacheKey'
      to: 'health.test.ts :314 (llm-optional) + :368 (cache-fallback)'
      via: 'exact-token assertions'
      pattern: 'cache-fallback-active'
    - from: 'server/__tests__/resilience/quota-chaos.test.ts'
      to: 'redis.incr (pruneQuota :93 / replayQuota :67)'
      via: 'vi.fn(redisDeath) on redis.incr'
      pattern: 'redis.*incr'
---

<objective>
LLM-FIX strand (the "honest signals" theme) + the folded CI-green companion. Six bug fixes make every probe/audit/eval signal mean what it says, plus two small fixes that green `main` (`actorCatalog` stale fixture path + `qs` npm-audit moderate).

Purpose: SC38-1 (honest health/audit single-source semantics) and SC38-2 (chaos + v3-mock coverage proves "no HTTP 500 under Redis death" end-to-end). This plan also creates the Wave 0 `quota-chaos.test.ts` scaffold and the extended chaos mock that other strands rely on for the resilience guarantee.

Output: token-split health probe, Open-Meteo sentinel, honest `actorMatchRate`, extended chaos mock + `/api/operator-status` coverage, new dedicated quota-chaos test, `events.test.ts` v3 drift fix, green full suite.

Implements: D-05 (Open-Meteo sentinel), D-06 (actorMatchRate null + 33-AUDIT-REPORT note), Claude's-discretion fold of CI-health into the LLM-FIX test plan.

Phase-structure decisions honored by this plan set (D-01, D-02): per D-01 Phase 38 stays ONE phase decomposed into six strand-aligned plans (this is the LLM-FIX strand + the folded CI-green companion — no sub-phase split); per D-02 CRON-WATCH-01 (the optional 7-day watch) is DEFERRED to v1.7 — NO plan in this set holds the phase open for it, and SC38-7 stays unfired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-CONTEXT.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-RESEARCH.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-PATTERNS.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-VALIDATION.md
</context>

<artifacts_this_phase_produces>
NEW symbols/files this plan creates (source-grounding pass MUST exclude these from drift verification — they do not exist in current `main`):

- `server/__tests__/resilience/quota-chaos.test.ts` — NEW dedicated chaos test (LLM-FIX-05)
- Open-Meteo empty-result sentinel object shape `{ data: [], failed: true, fetchedAt }` written to `water:precip` (LLM-FIX-02) — new runtime value, not a new symbol
- `actorMatchRate: number | null` widened type (LLM-FIX-03) — type widening, not a new symbol
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health-probe token split + Open-Meteo sentinel + actorMatchRate honesty (LLM-FIX-01/02/03)</name>
  <files>server/routes/health.ts, server/__tests__/routes/health.test.ts, server/routes/water.ts, server/__tests__/routes/water.test.ts, server/lib/llmEvalHarness.ts, server/lib/llmProgress.ts, server/__tests__/lib/llmEvalHarness.test.ts, .planning/milestones/v1.5-phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md</files>
  <read_first>
    - server/routes/health.ts (the generic `probeCacheKey` helper ~`:134-172`; site `:170` currently hard-codes `llm-optional-fallback-active:` and is called by BOTH `llmEvents` and `news`; the dedicated LLM-only path at `:284` legitimately keeps the LLM token — do NOT touch `:284`)
    - server/__tests__/routes/health.test.ts (`:314` asserts `/llm-optional/` for llmEvents — keep; `:368` asserts `/fallback-active/` for news — currently passes accidentally because the loose regex matches the LLM token)
    - server/routes/water.ts (`:357-360` `if (precipData.length > 0) { await cacheSetSafe(PRECIP_KEY, precipData, ...) }`; `PRECIP_KEY = 'water:precip'` at `:125`; envelope precedent at `:347-351` `cacheSetSafe(FACILITIES_KEY, { facilities, filterStats }, ...)`; reader `findNearestPrecip` consumes `water:precip` — must tolerate `data: []`)
    - server/lib/llmEvalHarness.ts (`:388` `const actorMatchRate = actorTotal === 0 ? 0 : actorMatched / actorTotal;`; `EvalScore.actorMatchRate: number` at `:154`; the `try/catch` "falling back to 0" at `:386`; `actorMatchRate: 0` literal at `:287`)
    - server/lib/llmProgress.ts (`actorMatchRate?: number` typed at `:123,:296` — widen to `number | null`)
    - server/__tests__/lib/llmEvalHarness.test.ts (`:512,:542` assert `actorMatchRate` is `0` in the no-ground-truth case — flip to `null`)
    - .planning/milestones/v1.5-phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/33-AUDIT-REPORT.md (the TBD stub for the actor-accuracy eval block — confirmed archived path)
    - server/cache/redis.ts (`CacheEntry<T>` `{data, fetchedAt}` convention)
    - 38-RESEARCH.md §"LLM-FIX-01/02/03" + 38-PATTERNS.md §"LLM-FIX-01/02/03"
  </read_first>
  <action>
    LLM-FIX-01: Add a per-probe fallback-reason token to `probeCacheKey`. Make the helper emit the generic `cache-fallback-active:` token by default; pass a distinct token only for the `llmEvents` probe (either a `fallbackReasonToken` param defaulting to `cache-fallback-active:`, or branch on `name === 'llmEvents'`) so `llmEvents` keeps `llm-optional-fallback-active:` and the `news` probe emits `cache-fallback-active:`. Leave the dedicated `:284` LLM-only path untouched. In health.test.ts tighten the news assertion at `:368` from `/fallback-active/` to `/cache-fallback-active/`; keep `:314` `/llm-optional/`.
    LLM-FIX-02 (D-05): On `precipData.length === 0` in water.ts, write a distinguishable fresh sentinel via `cacheSetSafe(PRECIP_KEY, { data: [], failed: true, fetchedAt: Date.now() }, WATER_PRECIP_REDIS_TTL_SEC)` so the audit reads a FRESH write but the failure stays detectable. Verify `findNearestPrecip` (and any `water:precip` reader) tolerates a `{ data: [], failed: true }` envelope without throwing — adapt the reader to read `.data` when the sentinel shape is present if needed, treating it as "degraded, no overlay," NOT "healthy." Add a water.test.ts assertion that the empty-batch path writes the sentinel (fresh `fetchedAt`, `failed: true`).
    LLM-FIX-03 (D-06): Change `EvalScore.actorMatchRate` to `number | null` (`:154`), and at `:388` return `null` (NOT `0`) when `actorTotal === 0`. Leave the `:386` try/catch "falling back to 0" as `0` (a compute failure ≠ "no ground truth"). Widen the `actorMatchRate?: number` types in llmProgress.ts (`:123,:296`) to `number | null`. Flip llmEvalHarness.test.ts assertions at `:512,:542` from `0` to `null`. In 33-AUDIT-REPORT.md replace the TBD stub with an explicit "not yet populated — requires staging run (run-audit.ts against staging Redis + expectedActor1/2 backfill); deferred to v1.7 backlog" note so the eval block is not misread as 0% actor accuracy.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/routes/health.test.ts server/__tests__/routes/water.test.ts server/__tests__/lib/llmEvalHarness.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/__tests__/routes/health.test.ts` exits 0 with the news assertion now asserting `/cache-fallback-active/` and llmEvents asserting `/llm-optional/`
    - `grep -n "cache-fallback-active:" server/routes/health.ts` returns ≥1 match (the news/default path)
    - `grep -n "failed: true" server/routes/water.ts` returns ≥1 match (the sentinel write)
    - `npx vitest run server/__tests__/lib/llmEvalHarness.test.ts` exits 0 with the no-ground-truth case now asserting `null`
    - `grep -n "number | null" server/lib/llmEvalHarness.ts` shows the widened `actorMatchRate` type
    - `npm run typecheck` exits 0 (the `number | null` widening ripples cleanly through llmProgress.ts + buildSummary)
  </acceptance_criteria>
  <done>News probe emits cache-fallback-active:, llmEvents keeps llm-optional-fallback-active:, Open-Meteo empty-batch writes a fresh failed sentinel, actorMatchRate returns null without ground truth, 33-AUDIT-REPORT.md documents the gap, all three touched suites + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Chaos-mock extension + dedicated quota-chaos test + events.test v3 drift (LLM-FIX-04/05/06)</name>
  <files>server/__tests__/resilience/redis-death.test.ts, server/__tests__/resilience/quota-chaos.test.ts, server/__tests__/routes/events.test.ts</files>
  <read_first>
    - server/__tests__/resilience/redis-death.test.ts (`:185-204` `redisDeath` + `vi.mock('../../cache/redis.js')` exposing only `{ping,get,set,del}` on `redis` plus `cacheGet`/`cacheSet`; `cacheGetSafe`/`cacheSetSafe` pass through from `actual` as the system under test; server-boot pattern `:217-225` `const { createApp } = await import('../../index.js')`; route coverage list `:257-266` — 8 GET routes, `/api/operator-status` ABSENT)
    - server/lib/pruneQuota.ts (`:93` `const used = await redis.incr(key)` — NO enclosing try/catch; confirmed) and server/lib/replayQuota.ts (`:67` `const used = await redis.incr(key)` — NO try/catch; confirmed)
    - server/routes/operator-status.ts (the Bearer-gated aggregator — chaos test must send the Bearer or assert 401/503, NEVER expect 500)
    - server/__tests__/routes/events.test.ts (`:354`, `:356`, `:833` default `schemaVersion: 'v1'` — deleted Phase 29)
    - 38-RESEARCH.md §"LLM-FIX-04/05/06" + Pitfall 5 ("right answer wrong reason") + 38-PATTERNS.md §"LLM-FIX-04/05"
  </read_first>
  <action>
    LLM-FIX-04: Extend the `redis` mock in redis-death.test.ts to add `incr, sadd, smembers, scard, srem, zadd, hset, hincrby, scan, lpush, expire` as `vi.fn(redisDeath)`. Add `/api/operator-status` to the route coverage list — it is Bearer-gated, so send a valid `DASHBOARD_PASSWORD` Bearer (or assert 401/503) and assert the response is never HTTP 500 under Redis death. Do NOT add `pipelineAudit` to the proof list (its writer is deleted by plan 02 / PURGE-05).
    LLM-FIX-05: Create the NEW dedicated `server/__tests__/resilience/quota-chaos.test.ts`. Copy the `redisDeath` + `vi.mock('../../cache/redis.js')` mock shape, but mock `redis.incr` specifically to throw (now exposed by the FIX-04 extension). Boot the app via `createApp()` and exercise the quota-gated endpoints (the prune endpoint and the replay endpoint that call `await redis.incr(key)` with no try/catch). Assert they return 503 (or 200) but NEVER 500 — proving 503-for-the-right-reason (the incr itself dies), closing the Pitfall 5 false-negative where `cacheGetSafe` degrade-open short-circuits before incr fires.
    LLM-FIX-06: Mechanical `'v1'` → `'v3'` at events.test.ts `:354`, `:356`, `:833`. Confirm the mock return shape still matches the post-purge `processEventGroupsV3` direct shape (coordinate with plan 02 / PURGE-01; if plan 02 lands first, match its v3 return; if this lands first, the `'v3'` literal is forward-compatible).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/resilience/ server/__tests__/routes/events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/__tests__/resilience/quota-chaos.test.ts` exists and `npx vitest run server/__tests__/resilience/quota-chaos.test.ts` exits 0
    - The quota-chaos test mocks `redis.incr` to throw (`grep -n "incr" server/__tests__/resilience/quota-chaos.test.ts` shows the incr mock) and asserts no HTTP 500 from the quota endpoints
    - `grep -c "vi.fn(redisDeath)" server/__tests__/resilience/redis-death.test.ts` returns ≥15 (the original 4+ plus the 11 added raw-redis methods)
    - `grep -n "operator-status" server/__tests__/resilience/redis-death.test.ts` returns ≥1 (route added to coverage)
    - `grep -c "schemaVersion: 'v1'" server/__tests__/routes/events.test.ts` returns 0 (all flipped to v3)
    - `npx vitest run server/__tests__/routes/events.test.ts` exits 0
  </acceptance_criteria>
  <done>Chaos mock exposes all 11 raw-redis methods, /api/operator-status proven no-500 under Redis death, quota-chaos.test.ts proves 503-not-500 when redis.incr throws, events.test.ts uses v3 mocks.</done>
</task>

<task type="auto">
  <name>Task 3: CI-green companion — actorCatalog fixture path + qs npm-audit (folded)</name>
  <files>src/__tests__/lib/actorCatalog.test.ts, package.json, package-lock.json</files>
  <read_first>
    - src/__tests__/lib/actorCatalog.test.ts (`:54` hard-codes a `resolve()` to `.planning/phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json` — the Phase 33 dir was archived to `.planning/milestones/v1.5-phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/`; this is the SOLE failing suite on current `main`)
    - 38-RESEARCH.md §"Folded CI-health" (verified: 2362 passed | 1 FAILED suite; `npm audit` is 1 moderate `qs` DoS, `npm audit fix`-able; the 2026-04-22 snapshot's "32 filter-test failures" + "v1 extractor references" are GONE — do NOT scope them)
  </read_first>
  <action>
    Repoint the `codebookPath` resolve() at actorCatalog.test.ts `:54` to the archived fixture location `.planning/milestones/v1.5-phases/33-actor-metadata-audit-canonical-catalog-eval-expansion/cameo-codes.json` (or copy the fixture into a stable test-fixtures dir and point at that). Run `npm audit fix` to resolve the single moderate `qs` 6.11.1 DoS advisory (GHSA-q8mj-m7cp-5q26), committing the resulting package.json/package-lock.json delta. Do NOT scope the stale 2026-04-22 items (32 filter-test failures, deleted-v1-extractor references) — RESEARCH verified they are already resolved.
  </action>
  <verify>
    <automated>npx vitest run && npm audit --audit-level=moderate</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/lib/actorCatalog.test.ts` exits 0 (the ENOENT is gone)
    - `npx vitest run` exits 0 — full suite green (no failing suites)
    - `npm audit --audit-level=moderate` reports 0 moderate-or-higher vulnerabilities (the `qs` advisory resolved)
  </acceptance_criteria>
  <done>actorCatalog fixture path repointed to the v1.5-phases archive, qs advisory fixed via npm audit fix, full vitest suite exits 0, main is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                                                                 |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| client → `/api/operator-status`             | Bearer-gated operator aggregator; chaos test exercises it under Redis death and must observe 401/503, never 500 (info leak via stack trace) |
| client → quota-gated prune/replay endpoints | per-Bearer quota counter via `redis.incr`; chaos must prove degrade-open (no 500)                                                           |
| Open-Meteo upstream → `water:precip` cache  | untrusted upstream batch result; sentinel must not let a failure masquerade as healthy                                                      |

## STRIDE Threat Register

| Threat ID  | Category                     | Component                                                       | Disposition | Mitigation Plan                                                                                                                                       |
| ---------- | ---------------------------- | --------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38.01-01 | Information disclosure       | `/api/operator-status` under Redis death                        | mitigate    | Chaos test asserts no HTTP 500 (no stack-trace leak); endpoint degrades-open via cacheGetSafe + per-call try/catch (LLM-FIX-04)                       |
| T-38.01-02 | Denial of service            | quota `redis.incr` path (pruneQuota/replayQuota)                | mitigate    | quota-chaos.test.ts proves 503-not-500 when incr throws; failure is bounded, no 5xx cascade (LLM-FIX-05)                                              |
| T-38.01-03 | Tampering (signal integrity) | `actorMatchRate` 0-vs-null + health token + Open-Meteo sentinel | mitigate    | honest-signal fixes prevent a degraded/empty state being misread as healthy/0% (LLM-FIX-01/02/03) — LOW severity, accepted as the phase's core intent |
| T-38.01-SC | Tampering                    | `npm audit fix` (qs) dependency update                          | mitigate    | Single moderate advisory fix; lockfile diff reviewed; no new package added (no install of an untrusted dependency)                                    |

</threat_model>

<verification>
- Per-task: `npx vitest run <touched file>` + `npm run typecheck` (< 60s loop).
- Plan gate: `npx vitest run` exits 0 (full suite green — this plan owns the CI-green companion), `npm run typecheck` PASS, `npm audit --audit-level=moderate` clean.
</verification>

<success_criteria>

- SC38-1 partial: news probe → `cache-fallback-active:`, llmEvents → `llm-optional-fallback-active:`; Open-Meteo empty-batch writes a fresh `failed: true` sentinel so the audit tier no longer degrades-to-unknown.
- SC38-2 partial: chaos mock exposes the 11 raw-redis methods + `/api/operator-status` proven no-500; quota-chaos test proves 503-for-the-right-reason; events.test.ts v3 mocks; `actorMatchRate` honest null.
- CI-green: full `npx vitest run` exits 0; `npm audit` clean.
  </success_criteria>

<output>
Create `.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-01-SUMMARY.md` when done.
</output>

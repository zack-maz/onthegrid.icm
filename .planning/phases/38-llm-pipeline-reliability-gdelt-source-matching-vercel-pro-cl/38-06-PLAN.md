---
phase: 38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl
plan: 06
type: execute
wave: 2
depends_on: ['38-02', '38-03']
files_modified:
  - server/lib/eventGrouping.ts
  - server/__tests__/lib/eventGrouping.dedup.test.ts
  - server/lib/corroboration.ts
  - server/__tests__/lib/corroboration.test.ts
  - server/lib/relevanceScorer.ts
  - server/__tests__/lib/relevanceScorer.test.ts
  - server/lib/llmSchema.ts
  - server/routes/events.ts
autonomous: true
requirements: [GDELT-MATCH-02, GDELT-MATCH-03, GDELT-MATCH-04]

must_haves:
  truths:
    - 'A tighter pre-enrichment dedup pass collapses ONLY high-confidence duplicate GDELT mentions to one canonical row and preserves genuinely-distinct events (SC38-4; D-07 conservative)'
    - 'The Phase-22 three-gate corroboration (temporal AND geographic AND keyword) is generalized beyond Bellingcat to general OSINT (news:gdelt) sources, boosting only on genuine corroboration and withholding on coincidental matches (SC38-4)'
    - 'A per-event compositeScore (tier × corroboration × specificity) re-orders the dashboard top-of-list as an ADDITIVE OPTIONAL field WITHOUT mutating or dropping the raw corpus (SC38-4; D-07 non-mutating)'
  artifacts:
    - path: 'server/lib/corroboration.ts'
      provides: 'Generalized three-gate OSINT corroboration (temporal AND geographic AND strict-keyword)'
      min_lines: 40
    - path: 'server/lib/eventGrouping.ts'
      provides: 'Tight high-confidence dedup pre-pass distinct from the coarse 50km batch-grouping'
      contains: 'dedup'
  key_links:
    - from: 'server/lib/relevanceScorer.ts compositeScore'
      to: 'server/lib/corroboration.ts boost + sourceTiers tier'
      via: 'tier × corroboration × specificity composite'
      pattern: 'compositeScore'
    - from: 'server/lib/llmSchema.ts enrichedEventV3'
      to: 'compositeScore optional field'
      via: '.optional() so old cached events still validate'
      pattern: 'compositeScore'
---

<objective>
GDELT-MATCH 02/03/04 — the corpus-quality improvements that the GDELT-MATCH-01 audit (plan 03) sized. Conservative high-confidence dedup before enrichment, generalized three-gate OSINT corroboration, and an additive composite rescore that re-orders the dashboard without mutating the raw corpus.

Purpose: SC38-4 (completion) — high-confidence, deduped, source-tier-ranked events surface at the dashboard top-of-list.

Output: a tight dedup pre-pass in `eventGrouping.ts` (distinct from the coarse 50km batch-grouping), a generalized `corroboration.ts` three-gate, and a `compositeScore` field in `relevanceScorer.ts` wired into the collapsed v3 schema as `.optional()`.

Implements: D-07 — dedup collapses HIGH-CONFIDENCE duplicates only; composite score is ADDITIVE/OPTIONAL re-ordering WITHOUT mutating/dropping the raw corpus.

DEPENDS ON: plan 03 (GDELT-MATCH-01 audit sizes the dedup confidence cutoff, corroboration tuning, and composite weights — read 38-03-SUMMARY.md's threshold recommendations FIRST) AND plan 02 (PURGE-04 collapsed the v3 schema — the compositeScore field is added to the collapsed `enrichedEventV3`, file overlap on llmSchema.ts forces wave 2).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-CONTEXT.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-RESEARCH.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-PATTERNS.md
@.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols/files this plan creates (source-grounding pass MUST exclude these — they do not exist in current `main`):

- `server/lib/corroboration.ts` + `server/__tests__/lib/corroboration.test.ts` — NEW generalized three-gate (GDELT-MATCH-03)
- a tight dedup pre-pass function in `server/lib/eventGrouping.ts` + `server/__tests__/lib/eventGrouping.dedup.test.ts` — NEW (GDELT-MATCH-02)
- `compositeScore?: number` field on `enrichedEventV3` (llmSchema.ts) + a `computeCompositeScore` function in `relevanceScorer.ts` — NEW (GDELT-MATCH-04)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: High-confidence pre-enrichment dedup pass (GDELT-MATCH-02)</name>
  <files>server/lib/eventGrouping.ts, server/__tests__/lib/eventGrouping.dedup.test.ts</files>
  <read_first>
    - .planning/phases/38-.../38-03-SUMMARY.md (the GDELT-MATCH-01 audit's recommended dedup confidence cutoff — use it to set the threshold; if the audit found few duplicates, default CONSERVATIVE)
    - server/lib/eventGrouping.ts (`groupGdeltRows(entities)` `:60` collapses by CAMEO-root AND day-bucket AND ≤50km `GROUP_RADIUS_KM` into `EventGroup`s `:17-27` — this is COARSE batch-grouping for enrichment, NOT dedup; the pipeline calls `groupGdeltRows` then `processEventGroupsV3`)
    - server/lib/newsClustering.ts (the existing Jaccard-0.8 dedup in the NEWS path — analog for the title/URL-overlap gate, but separate path)
    - server/cache/redis.ts (`events:gdelt` raw cache — the dedup pre-pass must NEVER mutate it; it is a pre-enrichment filter that drops collapse-targets only)
    - 38-RESEARCH.md §"GDELT-MATCH-02" + Pitfall 6 (do NOT confuse coarse batch-grouping with true dedup; prefer under-collapsing) + 38-PATTERNS.md §"GDELT-MATCH-02"
  </read_first>
  <action>
    Add a DISTINCT, tighter dedup pre-pass in eventGrouping.ts that runs BEFORE LLM enrichment and is separate from the coarse `groupGdeltRows` (Pitfall 6 — do NOT loosen the 50km batch-grouping). Collapse to one canonical row ONLY when a conservative AND-gate passes: same canonical actor pair AND same CAMEO root AND same day-bucket AND ≤ a tight radius (e.g. 5-10km, tighter than `GROUP_RADIUS_KM`) AND title/URL token overlap above a high Jaccard (e.g. ≥0.85). Use the threshold values recommended by the GDELT-MATCH-01 audit (38-03-SUMMARY); default conservative (a missed dedup is cheaper than a wrongly-merged distinct event). The pass is a pre-enrichment filter that drops only collapse-targets and NEVER mutates the raw `events:gdelt` cache. Write eventGrouping.dedup.test.ts FIRST (TDD): a fixture of two true duplicates (same actor/CAMEO/day/≤5km/high-Jaccard) collapses to one, and two distinct events (same city same day but different actor or low title-Jaccard) are PRESERVED.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/eventGrouping.dedup.test.ts server/__tests__/lib/eventGrouping.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/__tests__/lib/eventGrouping.dedup.test.ts` exits 0 — true duplicates collapse to one, distinct events preserved
    - `grep -in "dedup\|collapse" server/lib/eventGrouping.ts` returns ≥1 (the new pre-pass) and the coarse `groupGdeltRows` 50km grouping is unchanged (`grep -n "GROUP_RADIUS_KM" server/lib/eventGrouping.ts` still present, value unchanged)
    - `grep -n "cacheSet\|redis.set" server/lib/eventGrouping.ts` returns 0 (the dedup never mutates the raw corpus — non-destructive per D-07)
    - the existing `eventGrouping.test.ts` still passes (coarse grouping not broken)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>A conservative high-confidence dedup pre-pass collapses only true duplicates before enrichment, preserves distinct events, never mutates the raw events:gdelt cache, thresholds sized by the GDELT-MATCH-01 audit, dedup + coarse-grouping tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalized three-gate OSINT corroboration + composite rescore (GDELT-MATCH-03/04)</name>
  <files>server/lib/corroboration.ts, server/__tests__/lib/corroboration.test.ts, server/lib/relevanceScorer.ts, server/__tests__/lib/relevanceScorer.test.ts, server/lib/llmSchema.ts, server/routes/events.ts</files>
  <read_first>
    - .planning/phases/38-.../38-03-SUMMARY.md (the audit's corroboration tuning + composite weight recommendations)
    - server/lib/llmResolver.ts (`:507` `resolveViaBellingcat` — the Phase-22 three-gate temporal AND geographic AND keyword pattern; `bellingcatCoord` threading)
    - server/lib/sourceTiers.ts (`getSourceTier`, `getHighestTier` — weight the corroborating source by tier)
    - server/lib/relevanceScorer.ts (`computeRelevanceScore` 0-1 = NLP-triple 0-0.45 + conflict-verb 0-0.35 + source-reliability 0-0.20; `SOURCE_RELIABILITY` map `:31`, 4-tier 1.0/0.9/0.8/0.6 — extend with the additive compositeScore)
    - server/lib/llmSchema.ts (the COLLAPSED `enrichedEventV3` after plan 02 / PURGE-04 — add `compositeScore` here as `.optional()`)
    - server/cache/redis.ts (`news:gdelt` GDELT-DOC clusters — the OSINT corroboration source)
    - server/routes/events.ts (where the dashboard ordering is applied — read `compositeScore` for top-of-list ordering; the raw `events:llm:v3` corpus stays untouched)
    - 38-RESEARCH.md §"GDELT-MATCH-03/04" + the landmines (strict keyword gate — actor/specific-action not generic "Iran"/"strike"; compositeScore MUST be `.optional()` so old cached events validate) + 38-PATTERNS.md §"GDELT-MATCH-03/04"
  </read_first>
  <action>
    GDELT-MATCH-03: Create server/lib/corroboration.ts generalizing the Bellingcat three-gate (temporal window AND geographic radius AND keyword/actor overlap) from Bellingcat-specific to ANY tier-1/2 OSINT source in `news:gdelt`. Expose a function that, given an event and the news:gdelt clusters, returns a corroboration boost ONLY when a genuine 3-gate match exists (a news cluster within the temporal+geo window AND keyword-matched), and withholds the boost on coincidental matches (only 1-2 gates pass). The keyword gate MUST be strict — actor or specific-action match, NOT generic "Iran"/"strike" — to avoid same-city-same-day false positives. Weight the corroborating source via `sourceTiers.getSourceTier`. Write corroboration.test.ts FIRST (TDD): a genuine 3-gate match boosts; a same-city-same-day-but-different-actor case (only 2 gates) does NOT boost.
    GDELT-MATCH-04: Add a `computeCompositeScore` to relevanceScorer.ts = function of tier (from sourceTiers) × corroboration boost (from corroboration.ts) × specificity (precision tier exact/neighborhood/city/region from the v3 schema and/or NLP-triple completeness). Add `compositeScore?: number` as an `.optional()` field on the collapsed `enrichedEventV3` (llmSchema.ts) so old cached events without it still validate (24h–90d TTL self-heals). Wire events.ts to read `compositeScore` for dashboard top-of-list ordering — ADDITIVE re-ordering only; the raw `events:llm:v3` corpus is NOT mutated or dropped (D-07). Extend relevanceScorer.test.ts asserting the composite re-orders a fixture set without dropping any event.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/lib/corroboration.test.ts server/__tests__/lib/relevanceScorer.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `server/lib/corroboration.ts` exists and `npx vitest run server/__tests__/lib/corroboration.test.ts` exits 0 — genuine 3-gate boosts, coincidental (2-gate) does not
    - `grep -in "temporal\|geographic\|keyword" server/lib/corroboration.ts` shows all three gates and the keyword gate is strict (actor/specific-action, not bare "Iran"/"strike")
    - `grep -n "compositeScore" server/lib/llmSchema.ts` returns ≥1 AND `grep -n "\.optional()" server/lib/llmSchema.ts | grep -i composite` confirms it is optional
    - `npx vitest run server/__tests__/lib/relevanceScorer.test.ts` exits 0 — composite re-orders a fixture set, drops nothing
    - `grep -n "cacheSet\|redis.set" server/lib/relevanceScorer.ts` returns 0 and events.ts ordering does NOT write back to `events:llm:v3` (non-mutating — D-07)
    - `npm run typecheck` exits 0 (compositeScore optional so old cached events still validate)
  </acceptance_criteria>
  <done>Three-gate OSINT corroboration boosts only on genuine matches with a strict keyword gate, compositeScore (tier × corroboration × specificity) is an optional additive field that re-orders the dashboard top-of-list without mutating the raw corpus, corroboration + relevanceScorer tests + typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                                                 |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `news:gdelt` OSINT clusters → corroboration boost | untrusted news data drives a confidence boost; the strict keyword gate prevents coincidental corroboration inflating scores |
| dedup pre-pass → raw `events:gdelt` cache         | the pass must be read-and-filter only; mutating the raw corpus would be a data-integrity violation (D-07)                   |

## STRIDE Threat Register

| Threat ID  | Category                       | Component                                       | Disposition | Mitigation Plan                                                                                                                                                           |
| ---------- | ------------------------------ | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38.06-01 | Tampering (data integrity)     | dedup merging distinct events / corpus mutation | mitigate    | Conservative AND-gated dedup (Pitfall 6, prefer under-collapse); pre-enrichment filter never writes back to `events:gdelt` — grep gate confirms no `cacheSet`/`redis.set` |
| T-38.06-02 | Spoofing (false corroboration) | coincidental same-city-same-day boost           | mitigate    | Strict keyword gate (actor/specific-action, not generic terms); test asserts a 2-gate coincidental match does NOT boost                                                   |
| T-38.06-03 | Tampering                      | compositeScore reordering vs corpus mutation    | mitigate    | compositeScore is ADDITIVE `.optional()`; ordering reads it but never drops/mutates the raw corpus (D-07); old cached events validate without it                          |
| T-38.06-SC | Tampering                      | no package installs                             | accept      | Pure extension of existing primitives; no npm install                                                                                                                     |

</threat_model>

<verification>
- Per task: `npx vitest run <touched file>` + `npm run typecheck`.
- Plan gate: dedup preserves distinct events (no over-collapse), corroboration withholds on coincidental, compositeScore optional + non-mutating; `npx vitest run server/` green + `npm run typecheck` PASS.
</verification>

<success_criteria>

- SC38-4: GDELT event corpus surfaces high-confidence, deduped, source-tier-ranked events — mention-collapse dedup runs BEFORE enrichment (high-confidence only), three-gate corroboration extended beyond Bellingcat to general OSINT, per-event composite (tier × corroboration × specificity) re-ranks the dashboard top-of-list non-destructively.
  </success_criteria>

<output>
Create `.planning/phases/38-llm-pipeline-reliability-gdelt-source-matching-vercel-pro-cl/38-06-SUMMARY.md` when done. Record the final dedup/corroboration/composite thresholds used (sourced from the GDELT-MATCH-01 audit).
</output>

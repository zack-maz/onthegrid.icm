---
phase: 20-layer-purpose-refactor
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - src/__tests__/uiStore.test.ts
  - src/__tests__/entityLayers.test.ts
  - src/__tests__/useCounterData.test.ts
  - src/__tests__/LayerToggles.test.tsx
  - src/__tests__/BaseMap.test.tsx
  - src/__tests__/StatusPanel.test.tsx
  - src/__tests__/CountersSlot.test.tsx
  - src/__tests__/layerStore.test.ts
  - src/__tests__/MapLegend.test.tsx
  - src/hooks/useQuerySync.test.ts
autonomous: true
requirements: [LREF-01, LREF-02, LREF-03, LREF-04, LREF-05]

must_haves:
  truths:
    - "All tests pass with npx vitest run"
    - "uiStore tests verify no toggle state exists"
    - "entityLayers tests verify all entities are always visible"
    - "New layerStore tests verify visualization layer toggle behavior"
    - "New MapLegend tests verify legend rendering for active layers"
    - "useQuerySync tests verify only filter sync (no toggle sync)"
    - "StatusPanel tests verify unconditional entity counts"
  artifacts:
    - path: "src/__tests__/layerStore.test.ts"
      provides: "Unit tests for visualization layer store"
    - path: "src/__tests__/MapLegend.test.tsx"
      provides: "Component tests for legend framework"
    - path: "src/__tests__/entityLayers.test.ts"
      provides: "Updated tests confirming all-visible behavior"
    - path: "src/__tests__/uiStore.test.ts"
      provides: "Updated tests without toggle assertions"
  key_links:
    - from: "src/__tests__/layerStore.test.ts"
      to: "src/stores/layerStore.ts"
      via: "Tests toggle add/remove behavior"
      pattern: "toggleLayer"
    - from: "src/__tests__/MapLegend.test.tsx"
      to: "src/components/map/MapLegend.tsx"
      via: "Tests legend visibility based on active layers"
      pattern: "MapLegend"
---

<objective>
Update all existing test files to match the new toggle-free architecture, and create new tests for layerStore and MapLegend. This plan ensures the full test suite passes after the refactor.

Purpose: Plans 01 and 02 removed/modified interfaces that existing tests assert on. Without updating tests, the suite breaks. New components (layerStore, MapLegend) need coverage.
Output: Full green test suite validating the new architecture.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-layer-purpose-refactor/20-CONTEXT.md
@.planning/phases/20-layer-purpose-refactor/20-01-SUMMARY.md
@.planning/phases/20-layer-purpose-refactor/20-02-SUMMARY.md

@src/stores/layerStore.ts
@src/components/map/MapLegend.tsx
@src/__tests__/uiStore.test.ts
@src/__tests__/entityLayers.test.ts
@src/__tests__/useCounterData.test.ts
@src/__tests__/LayerToggles.test.tsx
@src/__tests__/BaseMap.test.tsx
@src/__tests__/StatusPanel.test.tsx
@src/__tests__/CountersSlot.test.tsx
@src/hooks/useQuerySync.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing tests for toggle-free architecture</name>
  <files>src/__tests__/uiStore.test.ts, src/__tests__/entityLayers.test.ts, src/__tests__/useCounterData.test.ts, src/__tests__/LayerToggles.test.tsx, src/__tests__/BaseMap.test.tsx, src/__tests__/StatusPanel.test.tsx, src/__tests__/CountersSlot.test.tsx, src/hooks/useQuerySync.test.ts</files>
  <action>
Read each test file first, then apply targeted updates:

**src/__tests__/uiStore.test.ts:**
- Remove all tests that assert on entity toggle fields (showFlights, showShips, etc.)
- Remove tests for toggle functions (toggleFlights, toggleShips, etc.)
- Remove tests for loadPersistedToggles, persistToggles, getToggles
- Keep tests for: panel state (detail panel open/close, collapse), sidebar (open/close/sections), markets collapse, entity selection (selectEntity, hoverEntity), expandedAlertSiteId
- Add a test confirming toggle fields no longer exist: `expect(useUIStore.getState()).not.toHaveProperty('showFlights')`

**src/__tests__/entityLayers.test.ts:**
- Remove all tests that assert `visible: false` when toggles are off
- Remove tests for flight sub-category filtering (ground/airborne/unidentified toggle gating)
- Update tests to verify all entity layers are always created/visible
- Keep tests for: search filter dimming, active entity highlighting, pulse animation, altitude opacity, proximity circle
- If tests mock `useUIStore` with toggle state, remove those mock overrides

**src/__tests__/useCounterData.test.ts:**
- Remove tests that assert zero counts when toggles are off
- Update to verify counts always reflect all entities from filtered arrays
- Keep severity filter tests (those use filterStore, not toggles)
- Keep entity array structure tests

**src/__tests__/LayerToggles.test.tsx:**
- REWRITE entirely for the new visualization layer toggles
- Test that 6 toggle rows render: Geographic, Weather, Threat Heatmap, Political, Satellite, Infrastructure
- Test clicking a toggle calls `useLayerStore.getState().toggleLayer(id)`
- Test the "Clear cache & reload" button still exists
- Test the "coming soon" subtitle text renders

**src/__tests__/BaseMap.test.tsx:**
- Remove any assertions about `isEntityTooltipVisible` or toggle-gated tooltip suppression
- Keep assertions about search filter tooltip suppression
- Remove mock overrides for entity toggle state in `useUIStore`

**src/__tests__/StatusPanel.test.tsx:**
- Remove the `useUIStore.setState` call in `beforeEach` that sets toggle fields (line 48: `showFlights: true, showGroundTraffic: false, showShips: true, showEvents: true, showAirstrikes: true, showGroundCombat: true, showTargeted: true, showSites: true`)
- Remove ALL toggle-gated count tests (these are the majority of the file):
  - "excludes ground flights from count when showGroundTraffic is OFF" (line 109)
  - "includes ground flights when showGroundTraffic is ON" (line 118)
  - "shows only ground count when showFlights OFF but showGroundTraffic ON" (line 126)
  - "shows 0 for flights when both showFlights and showGroundTraffic are OFF" (line 135)
  - "shows 0 for ships when showShips is OFF" (line 143)
  - "excludes airstrikes when showAirstrikes is OFF" (line 159)
  - "excludes ground combat when showGroundCombat is OFF" (line 168)
  - "shows 0 events when showEvents is false" (line 177)
  - "shows 0 for events when all conflict toggles are OFF" (line 185)
- Keep: "renders four feed lines" test, status dot color tests (green/yellow/red/loading/rate_limited), filter-aware count tests (country filter, altitude filter, ships-unaffected-by-altitude)
- Update "counts all events when all conflict toggles are ON" test to just verify total event count without mentioning toggles (rename to "counts all events")
- Update "renders four feed lines" test: replace `useUIStore.setState` toggle setup with just verifying all flights count (airborne flights = 3 in that test, should now count all flights including ground if any are in the store)
- Add replacement tests for unconditional counting:
  - "counts all flights including ground" -- set store with allFlights (5), expect count 5 (no toggle gating)
  - "counts all ships" -- set store with ships, expect count matches total
  - "counts all sites" -- set store with sites, expect count matches total

**src/__tests__/CountersSlot.test.tsx:**
- Remove toggle-gated count assertions
- Keep accordion, expand/collapse, entity click, scroll indicator tests

**src/hooks/useQuerySync.test.ts:**
- Remove tests for `TYPE_TOGGLE_MAP`, `SITE_TOGGLE_MAP`, `BOOL_TAG_MAP` (deleted)
- Remove tests for `deriveTogglesFromAST` (deleted or now returns empty)
- Remove tests for toggle sync in `buildASTFromToggles` (renamed to `buildASTFromFilters`)
- Keep tests for `deriveFiltersFromAST` (unchanged)
- Keep tests for filter range sync, text search field sync, severity sync, country sync, proximity sync
- Update `buildASTFromFilters` tests to only verify filter-related tag generation
- Update `SyncableState` references to the new slimmed-down interface
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/uiStore.test.ts src/__tests__/entityLayers.test.ts src/__tests__/useCounterData.test.ts src/__tests__/LayerToggles.test.tsx src/__tests__/BaseMap.test.tsx src/__tests__/StatusPanel.test.tsx src/__tests__/CountersSlot.test.tsx src/hooks/useQuerySync.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 existing test files updated and passing. No toggle-related assertions remain. Tests verify the new always-visible behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Create new layerStore and MapLegend tests</name>
  <files>src/__tests__/layerStore.test.ts, src/__tests__/MapLegend.test.tsx</files>
  <action>
**src/__tests__/layerStore.test.ts (NEW):**
- Import `useLayerStore` from `@/stores/layerStore`
- Reset store state before each test with `useLayerStore.setState({ activeLayers: new Set() })`
- Test cases:
  1. "starts with no active layers" -- `activeLayers` is empty Set
  2. "toggleLayer adds a layer" -- call `toggleLayer('geographic')`, assert `activeLayers.has('geographic')` is true
  3. "toggleLayer removes an active layer" -- add 'geographic', toggle again, assert it's removed
  4. "supports multiple active layers" -- toggle 'geographic' and 'weather', assert both present
  5. "toggling one layer doesn't affect others" -- activate 'geographic' and 'weather', toggle 'geographic' off, assert 'weather' still active
  6. "state resets to empty on fresh store" -- verify no persistence (Set is empty after setState reset)

**src/__tests__/MapLegend.test.tsx (NEW):**
- Import `MapLegend`, `LEGEND_REGISTRY` from `@/components/map/MapLegend`
- Import `useLayerStore` from `@/stores/layerStore`
- Use `@testing-library/react` for rendering
- Mock the Map context if needed (MapLegend renders as a map overlay child) -- or render standalone if it doesn't require map context
- Test cases:
  1. "renders nothing when no layers are active" -- render MapLegend, assert container is empty/null
  2. "renders nothing when LEGEND_REGISTRY is empty" -- (currently always empty), activate a layer, still renders nothing since no legend config exists
  3. "exports LegendConfig interface and LEGEND_REGISTRY array" -- verify exports exist
  4. "has correct positioning classes" -- if rendered, check for bottom-left absolute positioning classes
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/layerStore.test.ts src/__tests__/MapLegend.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>New layerStore tests verify Set-based toggle behavior. MapLegend tests verify empty state and positioning. Both pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- FULL suite passes (all tests green)
- `npx tsc --noEmit` -- no TypeScript errors
- No test file references deleted toggle fields or functions
</verification>

<success_criteria>
- Full test suite passes: `npx vitest run` exits 0
- TypeScript compiles clean: `npx tsc --noEmit` exits 0
- New layerStore has 6 test cases covering toggle add/remove/multiple
- New MapLegend has 3-4 test cases covering empty state
- All 8 existing test files updated to match toggle-free architecture
- StatusPanel tests verify unconditional counts (no toggle-gated assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/20-layer-purpose-refactor/20-03-SUMMARY.md`
</output>

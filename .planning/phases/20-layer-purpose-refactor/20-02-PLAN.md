---
phase: 20-layer-purpose-refactor
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/map/BaseMap.tsx
  - src/components/layout/StatusDropdown.tsx
  - src/components/layout/FilterPanelSlot.tsx
  - src/components/layout/LayerTogglesSlot.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/map/MapLegend.tsx
autonomous: true
requirements: [LREF-03, LREF-04]

must_haves:
  truths:
    - "Tooltips show for all entity types without toggle-based suppression"
    - "StatusDropdown shows counts for all entities without toggle gating"
    - "FilterPanelSlot has no VisibilityButton components"
    - "LayerTogglesContent shows visualization layer toggles instead of entity toggles"
    - "MapLegend component renders inline legends for active visualization layers"
    - "Sidebar imports the new LayerTogglesContent correctly"
  artifacts:
    - path: "src/components/map/MapLegend.tsx"
      provides: "Inline legend framework for visualization layers"
      exports: ["MapLegend", "LegendConfig"]
    - path: "src/components/layout/LayerTogglesSlot.tsx"
      provides: "Visualization layer toggle UI"
      contains: "VisualizationLayerId"
    - path: "src/components/map/BaseMap.tsx"
      provides: "Map component without isEntityTooltipVisible"
  key_links:
    - from: "src/components/layout/LayerTogglesSlot.tsx"
      to: "src/stores/layerStore.ts"
      via: "useLayerStore for toggle state"
      pattern: "useLayerStore"
    - from: "src/components/map/MapLegend.tsx"
      to: "src/stores/layerStore.ts"
      via: "reads activeLayers to show/hide legends"
      pattern: "useLayerStore"
    - from: "src/components/map/BaseMap.tsx"
      to: "src/components/map/MapLegend.tsx"
      via: "renders MapLegend as map overlay"
      pattern: "MapLegend"
---

<objective>
Update all UI components that consumed entity toggle state, replace the layer toggles UI with visualization layer toggles, and build the inline legend framework. After this plan, the UI is fully aligned with the new "layers = visualization modes" architecture.

Purpose: Components still reference deleted toggle fields from Plan 01. This plan fixes all component-level references and builds the new visualization layer UI.
Output: Clean UI with no entity toggle references, new layer toggle rows, legend framework ready for sub-phases 20.1-20.5.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-layer-purpose-refactor/20-CONTEXT.md
@.planning/phases/20-layer-purpose-refactor/20-RESEARCH.md
@.planning/phases/20-layer-purpose-refactor/20-01-SUMMARY.md

@src/stores/layerStore.ts
@src/components/map/BaseMap.tsx
@src/components/layout/StatusDropdown.tsx
@src/components/layout/FilterPanelSlot.tsx
@src/components/layout/LayerTogglesSlot.tsx
@src/components/layout/Sidebar.tsx

<interfaces>
<!-- From Plan 01: layerStore.ts -->
From src/stores/layerStore.ts:
```typescript
export type VisualizationLayerId =
  | 'geographic'
  | 'weather'
  | 'threat'
  | 'political'
  | 'satellite'
  | 'infrastructure';

interface LayerState {
  activeLayers: Set<VisualizationLayerId>;
  toggleLayer: (id: VisualizationLayerId) => void;
}

export const useLayerStore = create<LayerState>()(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update components that consumed entity toggle state</name>
  <files>src/components/map/BaseMap.tsx, src/components/layout/StatusDropdown.tsx, src/components/layout/FilterPanelSlot.tsx</files>
  <action>
**src/components/map/BaseMap.tsx:**
- Remove the `isEntityTooltipVisible()` function entirely (around lines 182-189)
- Remove the `useUIStore` selectors for `showEvents`, `showAirstrikes`, `showGroundCombat`, `showTargeted` (around line 79-80)
- Remove the `isConflictEventType` and `CONFLICT_TOGGLE_GROUPS` imports from `@/types/ui` if no longer used
- Update the tooltip suppression logic: the `rawTooltipEntity` check should only suppress for search filter (not toggle state). Change the condition from `!isEntityTooltipVisible(rawTooltipEntity) || (isSearchFilterActive && !searchMatchedIds.has(rawTooltipEntity.id))` to just `isSearchFilterActive && !searchMatchedIds.has(rawTooltipEntity.id)`
- Keep all other BaseMap functionality unchanged

**src/components/layout/StatusDropdown.tsx:**
- Remove ALL `useUIStore` selectors for toggle state (`showFlights`, `showGroundTraffic`, `pulseEnabled`, `showShips`, `showEvents`, `showAirstrikes`, `showGroundCombat`, `showTargeted`)
- Remove the `CONFLICT_TOGGLE_GROUPS` import
- Simplify visible count computation:
  - `visibleFlights`: just `flights.length` (no toggle gating, still use `filteredFlights` from `useFilteredEntities`)
  - `visibleShips`: just `filteredShips.length` (no toggle gating)
  - `visibleEvents`: just `events.length` (no toggle gating)
- Keep all status dot rendering, dropdown open/close, feed status logic unchanged

**src/components/layout/FilterPanelSlot.tsx:**
- Remove ALL `useUIStore` selectors for entity toggles and toggle functions: `showFlights`, `toggleFlights`, `showGroundTraffic`, `toggleGroundTraffic`, `pulseEnabled`, `togglePulse`, `showShips`, `toggleShips`, `showAirstrikes`, `toggleAirstrikes`, `showGroundCombat`, `toggleGroundCombat`, `showTargeted`, `toggleTargeted`, `showSites`, `toggleSites`, `showNuclear` through `togglePort`, `showHitOnly`, `toggleHitOnly`, `showHealthySites`, `toggleHealthySites`, `showAttackedSites`, `toggleAttackedSites`
- Remove all `<VisibilityButton>` component usages (Flights, Ships, Airstrikes, Ground Combat, Targeted, Nuclear, Naval, Oil, Airbase, Desal, Port visibility buttons)
- Remove the `import { VisibilityButton }` import
- Keep all filter-related controls: RangeSlider, CountryFilter, ProximityFilter, DateRangeFilter, TextSearchInput, HeadingSlider, SeverityToggles, section headers, Reset All button
- Keep the filter section groupings (Flights, Ships, Events, Sites) -- they still organize filter controls even without visibility toggles. Just remove the VisibilityButton from each section
- Also remove any "Healthy"/"Attacked" status toggle buttons if they exist in the sites section
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BaseMap shows tooltips for all entities without toggle checks. StatusDropdown counts all entities. FilterPanelSlot has no VisibilityButton components, only filter controls remain.</done>
</task>

<task type="auto">
  <name>Task 2: Replace layer toggles UI + build legend framework</name>
  <files>src/components/layout/LayerTogglesSlot.tsx, src/components/layout/Sidebar.tsx, src/components/map/MapLegend.tsx</files>
  <action>
**src/components/layout/LayerTogglesSlot.tsx:**
- Replace the entire `LayerTogglesContent` component with visualization layer toggles
- Import `useLayerStore` and `VisualizationLayerId` from `@/stores/layerStore`
- Remove ALL `useUIStore` selectors for entity toggles (showFlights through toggleAttackedSites)
- Remove the `ENTITY_DOT_COLORS` import (no longer needed for entity dots)
- Keep the `ToggleRow` component but adapt it for visualization layers -- give each layer a descriptive label and color:
  - Geographic: `#94a3b8` (slate-400), label "Geographic"
  - Weather: `#38bdf8` (sky-400), label "Weather"
  - Threat: `#ef4444` (red-500), label "Threat Heatmap"
  - Political: `#a78bfa` (violet-400), label "Political"
  - Satellite: `#22d3ee` (cyan-400), label "Satellite"
  - Infrastructure: `#4ade80` (green-400), label "Infrastructure"
- Each toggle row reads `useLayerStore(s => s.activeLayers.has(layerId))` for active state and calls `useLayerStore.getState().toggleLayer(layerId)` on click
- Add a subtitle under the "Layers" heading: small `text-[9px] text-text-muted` text "Visualization overlays (coming soon)" to indicate sub-phases 20.1-20.5 will add actual rendering
- Keep the "Clear cache & reload" button at the bottom
- Keep the `LayerTogglesSlot` wrapper component and the `OverlayPanel` usage unchanged
- Keep exporting both `LayerTogglesContent` and `LayerTogglesSlot`

**src/components/layout/Sidebar.tsx:**
- The import `{ LayerTogglesContent } from '@/components/layout/LayerTogglesSlot'` stays since we kept the export name. No changes needed unless there are toggle-related imports in Sidebar itself
- Check for any direct `useUIStore` toggle selectors in Sidebar.tsx and remove them if present (unlikely since Sidebar delegates to LayerTogglesContent)

**src/components/map/MapLegend.tsx (NEW):**
- Create the inline legend framework component
- Export `LegendConfig` interface:
  ```typescript
  export interface LegendConfig {
    layerId: VisualizationLayerId;
    title: string;
    colorStops: Array<{ color: string; label: string }>;
  }
  ```
- Export a `LEGEND_REGISTRY` array (initially empty -- sub-phases 20.1-20.5 will register their legends)
- The `MapLegend` component:
  - Import `useLayerStore` from layerStore
  - Read `activeLayers` from the store
  - Filter `LEGEND_REGISTRY` to only active layers
  - Render a positioned `div` in the bottom-left corner (`absolute bottom-4 left-4`) with `pointer-events-none` on the container but `pointer-events-auto` on individual legends
  - Each active legend renders: a small title label, and a horizontal/vertical gradient bar with CSS `linear-gradient` built from `colorStops`, with labels at the ends
  - Legends stack vertically (gap-2) when multiple are active
  - Use `transition-opacity duration-300` for fade in/out when layers toggle (per locked decision)
  - If no layers are active, render nothing (return null)
- This component will be rendered inside BaseMap.tsx as a sibling to other overlays. Add `<MapLegend />` inside the Map component's children in BaseMap.tsx, after the existing overlays (ProximityAlertOverlay, etc.)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>LayerTogglesContent shows 6 visualization layer toggles connected to layerStore. MapLegend component exists and renders in BaseMap when layers are active. Sidebar renders the new layer content. No entity toggle references remain in any UI component.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean (no errors)
- `grep -r 'showFlights\|showShips\|showEvents\|showSites\|toggleFlights\|toggleShips\|VisibilityButton\|isEntityTooltipVisible' src/components/` returns no results
- `grep -r 'useLayerStore' src/components/layout/LayerTogglesSlot.tsx src/components/map/MapLegend.tsx` shows both files using the new store
</verification>

<success_criteria>
- All UI components compile with no entity toggle references
- BaseMap tooltip shows for all entities (only search filter suppression remains)
- StatusDropdown counts all entities unconditionally
- FilterPanelSlot has filter controls only, no visibility toggles
- LayerTogglesSlot shows 6 visualization layer rows
- MapLegend renders in BaseMap bottom-left with transition animation
- Full `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-layer-purpose-refactor/20-02-SUMMARY.md`
</output>

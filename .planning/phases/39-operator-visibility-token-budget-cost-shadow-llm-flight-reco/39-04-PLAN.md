---
phase: 39-operator-visibility-token-budget-cost-shadow-llm-flight-reco
plan: 04
type: execute
wave: 3
depends_on: ['39-01', '39-02']
files_modified:
  - server/routes/events.ts
  - server/routes/__tests__/llm-history.test.ts
autonomous: true
requirements: [OBS-FLIGHT-03, OBS-FLIGHT-06]
must_haves:
  truths:
    - 'GET /api/events/llm-history is Bearer-gated (401 without valid Bearer, 200 with) via the existing dashboardAuth middleware'
    - 'The endpoint returns { runs, calls } where runs is dedupe-by-runId and calls is the bounded call list'
    - '?limit=N clamps to <=500 (the LTRIM cap); ?runId=X filters calls in-memory (never used as a Redis key)'
    - 'First request after cold start hydrates the in-memory singleton from Redis; the flag prevents re-LRANGE; the existing /llm-status gains the same hydrate hook'
  artifacts:
    - path: 'server/routes/events.ts'
      provides: 'GET /llm-history Bearer-gated handler + cold-start hydrate hook on /llm-status and /llm-history'
      contains: '/llm-history'
    - path: 'server/routes/__tests__/llm-history.test.ts'
      provides: 'OBS-FLIGHT-03 Bearer 401/200 + ?runId/?limit filter coverage; OBS-FLIGHT-06 hydrate coverage'
      contains: 'llm-history'
  key_links:
    - from: 'server/routes/events.ts GET /llm-history'
      to: 'server/middleware/dashboardAuth.ts'
      via: 'dashboardAuth middleware (same as /llm-status)'
      pattern: 'dashboardAuth'
    - from: 'server/routes/events.ts GET /llm-history'
      to: 'server/lib/llmCallHistory.ts + server/lib/llmRunHistory.ts'
      via: 'listCallHistory/listRunHistory + hydrate*IfCold'
      pattern: 'list(Call|Run)History|hydrate.*IfCold'
---

<objective>
Expose the Bearer-gated `GET /api/events/llm-history` read endpoint returning `{ runs, calls }` with `?runId` / `?limit` filters, and wire cold-start hydration into both `/llm-history` and the existing `/llm-status` so the in-memory singleton repopulates from Redis after a Fluid Compute cold start.

Purpose: This is the single read surface the FlightRecorderBlock (Plan 05) fetches. It must inherit the `dashboardAuth` Bearer gate exactly (operator-only — prompt/response telemetry and DLQ details must never reach anonymous callers) and clamp `?limit` to prevent an unbounded LRANGE.

Output: a new route handler in `events.ts`, the hydrate hooks, and a route contract test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-VALIDATION.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-01-SUMMARY.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (excluded from drift verification — intentionally new):

- `GET /api/events/llm-history` route handler (registered on `eventsRouter` with `dashboardAuth`)
- the cold-start hydrate hook calls (`hydrateCallHistoryIfCold` / `hydrateRunHistoryIfCold`) at the top of `/llm-history` AND `/llm-status`
- test file `server/routes/__tests__/llm-history.test.ts`
  </artifacts_this_phase_produces>

<source_notes>
**Verified anchors (executor honor these):**

- `dashboardAuth` is imported at `server/routes/events.ts:41` and used on `/llm-status` at :384 (`eventsRouter.get('/llm-status', dashboardAuth, async ...)`). Register `/llm-history` with the identical middleware + import path.
- `dashboardAuth` semantics (server/middleware/dashboardAuth.ts): constant-time `timingSafeEqual`; prod-empty DASHBOARD_PASSWORD -> 503 (fail-closed); bad Bearer -> 401; dev -> bypass. Do NOT hand-roll any auth — the test asserts the middleware's behavior.
- Input-validation clamp (V5): `const limit = Math.min(Number(req.query.limit) || 200, 500);` and `const runId = typeof req.query.runId === 'string' ? req.query.runId : undefined;` — `runId` is used ONLY as an in-memory `.filter()` predicate, NEVER concatenated into a Redis key (Tampering mitigation).
- `listRunHistory` already dedupes by runId internally (Plan 01); the route does not re-dedupe.
  </source_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register GET /llm-history (Bearer-gated) + add cold-start hydrate hooks to /llm-history and /llm-status</name>
  <files>server/routes/events.ts</files>
  <read_first>
    - server/routes/events.ts (the file being modified — read :41 dashboardAuth import, :384-457 the /llm-status registration + its Redis-summary fallback which is the hydrate-hook insertion point)
    - server/middleware/dashboardAuth.ts (:1-62 — Bearer gate semantics: 503 prod-empty / 401 bad / dev bypass — do NOT hand-roll)
    - server/lib/llmCallHistory.ts (Plan 01 — listCallHistory + hydrateCallHistoryIfCold)
    - server/lib/llmRunHistory.ts (Plan 01 — listRunHistory + hydrateRunHistoryIfCold)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md §"New Bearer-gated /llm-history route" code example, Security Domain V4/V5
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/routes/events/llm-history.ts (NEW — route ...)"
  </read_first>
  <action>
    In `server/routes/events.ts`, register a new handler mirroring the :384 `/llm-status` registration idiom: `eventsRouter.get('/llm-history', dashboardAuth, async (req, res) => { ... })`. Body: (1) `await hydrateCallHistoryIfCold(); await hydrateRunHistoryIfCold();` (OBS-FLIGHT-06). (2) `const limit = Math.min(Number(req.query.limit) || 200, 500);` (clamp <= LTRIM cap — DoS mitigation). (3) `const runId = typeof req.query.runId === 'string' ? req.query.runId : undefined;` (typeof guard — never a Redis key). (4) `const runs = await listRunHistory(limit);` (dedupe-by-runId is internal). (5) `let calls = await listCallHistory(limit); if (runId) calls = calls.filter((c) => c.runId === runId);`. (6) `res.json({ runs, calls });`. Also add `await hydrateCallHistoryIfCold(); await hydrateRunHistoryIfCold();` at the TOP of the existing `/llm-status` handler body (:384+) so a cold start repopulates the singleton on whichever operator endpoint is hit first (D-05 / OBS-FLIGHT-06). Import the four helpers from `../lib/llmCallHistory.js` and `../lib/llmRunHistory.js`. Keep the whole handler degrade-open (the lib readers already return `[]` on Redis failure, so the route naturally returns `{ runs: [], calls: [] }` with 200).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p server/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p server/tsconfig.json` exits 0.
    - Source assertion: `grep -c "'/llm-history'" server/routes/events.ts` >= 1 AND the registration includes `dashboardAuth`.
    - Source assertion: `grep -c "hydrateCallHistoryIfCold" server/routes/events.ts` >= 2 (both /llm-history and /llm-status hooks).
    - Source assertion: `grep -c "Math.min(Number(req.query.limit)" server/routes/events.ts` >= 1 (limit clamp present).
    - Source assertion: the runId handling uses `typeof req.query.runId === 'string'` and a `.filter(` on calls (never a redis key concatenation) — `grep -c "filter((c) => c.runId" server/routes/events.ts` >= 1.
  </acceptance_criteria>
  <done>/llm-history registered Bearer-gated returning {runs,calls} with clamped limit + in-memory runId filter; cold-start hydrate hooks on both /llm-history and /llm-status; server typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route contract test — Bearer gate + filters + hydration (OBS-FLIGHT-03/06)</name>
  <files>server/routes/__tests__/llm-history.test.ts</files>
  <read_first>
    - server/routes/__tests__/operator-status.test.ts (the closest route-test analog — makeApp() mounting the router, the Bearer 401/200 test idiom, vi.mock('../../cache/redis.js'))
    - server/routes/events.ts (the /llm-history handler from Task 1)
    - server/lib/llmCallHistory.ts + server/lib/llmRunHistory.ts (mock targets — the test mocks these list/hydrate helpers OR mocks redis underneath)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-VALIDATION.md (OBS-FLIGHT-03/06 rows + command `npx vitest run server/routes/__tests__/llm-history.test.ts`)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/routes/events/llm-history.ts" + §"server/routes/__tests__/llm-history.test.ts (NEW)"
  </read_first>
  <action>
    Create `server/routes/__tests__/llm-history.test.ts` (`// @vitest-environment node`). Mount the eventsRouter via a supertest `makeApp()` (mirror the operator-status test bootstrap). Mock the Redis layer (or the two lib modules) to return fixture runs/calls. Assert: (1) Bearer gate — `GET /api/events/llm-history` WITHOUT a valid Bearer returns 401 (and with a valid Bearer returns 200) — set DASHBOARD_PASSWORD in the test env so dashboardAuth is active, not dev-bypassed (OBS-FLIGHT-03). (2) Happy path returns `{ runs: [...], calls: [...] }` with both arrays. (3) `?runId=X` filters calls to only entries whose `runId === X` (back-correlation). (4) `?limit=1000` is clamped — assert the list call receives at most 500 (spy on listCallHistory/listRunHistory or assert the lrange range). (5) OBS-FLIGHT-06 hydrate: with an empty in-memory singleton and a populated Redis fixture, the first request triggers a hydrate (listCallHistory called) and a second request does NOT re-LRANGE (flag short-circuit) — assert via spy call count. (6) Degrade-open: when the lib readers throw/return [], the route returns 200 with empty arrays.
  </action>
  <verify>
    <automated>npx vitest run server/routes/__tests__/llm-history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/routes/__tests__/llm-history.test.ts` exits 0 with all cases passing.
    - Behavior assertion: a request without a valid Bearer returns 401 (Bearer gate proven — OBS-FLIGHT-03; no history to anonymous callers).
    - Behavior assertion: `?runId=X` returns only calls with `runId === X`.
    - Behavior assertion: `?limit=1000` results in a clamped read (<=500).
    - Behavior assertion: the hydrate spy shows exactly one LRANGE across two requests (flag prevents re-hydrate — OBS-FLIGHT-06).
  </acceptance_criteria>
  <done>llm-history route test green: Bearer 401/200, {runs,calls} shape, ?runId/?limit filters, cold-start hydrate-once, degrade-open empty arrays.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                             |
| ------------------------------------------------ | ----------------------------------------------------------------------- |
| unauthenticated caller ↔ /api/events/llm-history | Bearer gate (dashboardAuth) — the central threat surface for this phase |
| query param ↔ Redis                              | ?runId / ?limit must never reach Redis as keys or unbounded ranges      |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                               | Disposition | Mitigation Plan                                                                                                                         |
| ---------- | ---------------------- | ----------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- | --- | ------------------------------------------ |
| T-39-04-S  | Spoofing/Auth bypass   | `/llm-history` missing Bearer check                                     | mitigate    | `dashboardAuth` middleware on the route (identical to /llm-status); test asserts 401 without valid Bearer. (Central phase threat T1.)   |
| T-39-04-I  | Information Disclosure | prompt/response + DLQ telemetry to anonymous users                      | mitigate    | Bearer gate (D-03); same sensitivity class as existing /llm-status. Test proves no 200 without Bearer. (Central phase threat T2.)       |
| T-39-04-D  | Denial of Service      | unbounded `?limit` -> giant LRANGE                                      | mitigate    | `Math.min(Number(limit)                                                                                                                 |     | 200, 500)` clamp to the LTRIM cap; tested. |
| T-39-04-T  | Tampering              | `?runId` injected into a Redis key                                      | mitigate    | runId is a typeof-string-guarded in-memory `.filter()` predicate only, never concatenated into a Redis key; verified in handler + test. |
| T-39-04-I2 | Information Disclosure | degrade-open accidentally returning data instead of safe-empty on error | mitigate    | lib readers return `[]` on throw; route returns `{runs:[],calls:[]}` 200 — no leak, no 500. (Central phase threat T3.)                  |
| T-39-SC    | Tampering              | npm/pip/cargo installs                                                  | mitigate    | No new packages (RESEARCH §Package Legitimacy Audit: N/A).                                                                              |

</threat_model>

<verification>
- `npx vitest run server/routes/__tests__/llm-history.test.ts` green (Bearer + filters + hydrate + degrade-open)
- `npx tsc --noEmit -p server/tsconfig.json` exits 0
- `npx vitest run server/` green (no route regression)
</verification>

<success_criteria>

- GET /api/events/llm-history Bearer-gated (401 anon / 200 authed), returns {runs, calls}
- ?limit clamped <=500; ?runId filters calls in-memory (never a Redis key)
- Cold-start hydration hooks on /llm-history AND /llm-status; flag prevents re-LRANGE
  </success_criteria>

<output>
Create `.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-04-SUMMARY.md` when done
</output>

---
phase: 39-operator-visibility-token-budget-cost-shadow-llm-flight-reco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/llmProgress.ts
  - server/lib/llmCallHistory.ts
  - server/lib/llmRunHistory.ts
  - server/lib/__tests__/llmCallHistory.test.ts
  - server/lib/__tests__/llmRunHistory.test.ts
  - CLAUDE.md
  - docs/architecture/redis-keys.md
autonomous: true
requirements: [OBS-FLIGHT-01, OBS-FLIGHT-02, OBS-FLIGHT-06]
must_haves:
  truths:
    - 'A bounded Redis list llm:calls:history accepts call entries (LPUSH), evicts past 500 (LTRIM), and expires at 30d'
    - "A bounded Redis list llm:runs:history opens a 'running' record at run start and re-LPUSHes a terminal record at run end; reader dedupes by runId first-wins"
    - 'Both list modules degrade-open: every Redis op is try/caught, observability writes never throw'
    - 'Cold-start hydration repopulates the in-memory callHistory singleton from Redis on first call, guarded by a module-level flag that prevents re-LRANGE'
    - 'Both new keys are registered in all three drift-gate surfaces so src/__tests__/lib/redis-registry.test.ts passes'
  artifacts:
    - path: 'server/lib/llmCallHistory.ts'
      provides: 'appendCallHistory / listCallHistory / hydrateCallHistoryIfCold for llm:calls:history'
      contains: 'llm:calls:history'
    - path: 'server/lib/llmRunHistory.ts'
      provides: 'openRunRecord / closeRunRecord / listRunHistory / hydrateRunHistoryIfCold for llm:runs:history with dedupe-by-runId'
      contains: 'llm:runs:history'
    - path: 'server/lib/llmProgress.ts'
      provides: 'runId? field on LLMPipelineProgress + INITIAL_PROGRESS; new RunHistoryEntry + CallHistoryEntry interfaces'
      contains: 'RunHistoryEntry'
    - path: 'server/lib/__tests__/llmCallHistory.test.ts'
      provides: 'OBS-FLIGHT-01 / -06 unit coverage (LPUSH+LTRIM cap, parseEntry string-or-object, cold-start hydrate)'
    - path: 'server/lib/__tests__/llmRunHistory.test.ts'
      provides: 'OBS-FLIGHT-02 unit coverage (open running -> close terminal, re-LPUSH dedupe-by-runId)'
  key_links:
    - from: 'server/lib/llmCallHistory.ts'
      to: 'server/cache/redis.ts redis.lpush/ltrim/expire/lrange'
      via: 'import { redis }'
      pattern: "redis\\.(lpush|ltrim|expire|lrange)"
    - from: 'src/__tests__/lib/redis-registry.test.ts'
      to: 'CLAUDE.md + docs/architecture/redis-keys.md + non-test .ts string literal'
      via: 'three-surface drift parity'
      pattern: 'llm:(calls|runs):history'
---

<objective>
Lay the data-layer foundation for the LLM flight recorder: define the new entry types, build the two Redis-backed bounded-list modules (`llm:calls:history`, `llm:runs:history`) with their write/read/lifecycle/hydration helpers, and register both keys across all three drift-gate surfaces.

Purpose: Every downstream plan (runId threading, the read endpoint, the UI) consumes the interfaces and helpers defined here. Defining the contracts first prevents the executor scavenger-hunt and makes the Redis-registry drift gate (a HARD phase gate) pass before any consumer wiring lands.

Output: `server/lib/llmCallHistory.ts`, `server/lib/llmRunHistory.ts`, new types in `server/lib/llmProgress.ts`, two new test files, and three-surface registration of both keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md
@.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (the plan-review-convergence source-grounding pass excludes these from drift verification — they are intentionally new):

- `CallHistoryEntry` interface (in `server/lib/llmProgress.ts`) — current callHistory row fields (`provider, model, tokensIn, tokensOut, durationMs, ok, batchSize, timestamp, retryAfterMs?`) + `runId` + `batchIndex`
- `RunHistoryEntry` interface (in `server/lib/llmProgress.ts`) — per-run history shape (see Run Summary Shape in 39-RESEARCH.md)
- `runId?: string` field on `LLMPipelineProgress` + `INITIAL_PROGRESS`
- `appendCallHistory`, `listCallHistory`, `hydrateCallHistoryIfCold` functions (in `server/lib/llmCallHistory.ts`)
- `openRunRecord`, `closeRunRecord`, `listRunHistory`, `hydrateRunHistoryIfCold` functions (in `server/lib/llmRunHistory.ts`)
- Redis keys `llm:calls:history` (LPUSH+LTRIM 500-cap, 30d TTL) and `llm:runs:history` (LPUSH+LTRIM 200-cap, 30d TTL)
- Test files `server/lib/__tests__/llmCallHistory.test.ts`, `server/lib/__tests__/llmRunHistory.test.ts`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define new flight-recorder types + runId field on llmProgress (Wave 0 contracts)</name>
  <files>server/lib/llmProgress.ts</files>
  <read_first>
    - server/lib/llmProgress.ts (the file being modified — read LLMPipelineProgress around :24-263, the existing callHistory row shape, INITIAL_PROGRESS, LLMRunSummary at :275, and the llm:lastProgress write-through at :534-587)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md §"Run Summary Shape (OBS-FLIGHT-02, v3/NIM-adapted)" — the exact RunHistoryEntry fields and the warning NOT to overload LLMRunSummary
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/lib/llmProgress.ts (MOD — add runId + new types)"
  </read_first>
  <action>
    In `server/lib/llmProgress.ts`: (1) Add an optional `runId?: string` field to the `LLMPipelineProgress` interface and ensure `INITIAL_PROGRESS` / `resetProgress()` leaves it cleared (optional fields are wiped on reset — runId is re-stamped by Plan 02's run boundary). (2) Define and export a new `CallHistoryEntry` interface = the existing in-memory callHistory row fields (`provider`, `model`, `tokensIn`, `tokensOut`, `durationMs`, `ok`, `batchSize`, `timestamp`, optional `retryAfterMs?`) PLUS `runId: string` and `batchIndex: number` (D-02). (3) Define and export a new `RunHistoryEntry` interface with exactly these fields: `runId: string`, `startedAt: string`, `completedAt: string | null`, `outcome: 'running' | 'completed' | 'watchdog_aborted' | 'breaker_paused' | 'budget_hit' | 'error'`, `batchCount: number`, `batchesCompleted: number`, `batchesFailed: number`, `tokenSpend: { nvidia_nim: number }` (D-04 single provider), `evalScore: LLMPipelineProgress['evalScore']`, `dlqDelta: number`, `watchdogTimeouts: number`, `durationMs: number`, `pipelineVersion: 'v3'` (D-04 fixed). Do NOT overload or modify the existing `LLMRunSummary` interface at :275 — it is the `/llm-status` last-run artifact, a different shape (RESEARCH correction). Keep all new fields TypeScript-strict-clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p server/tsconfig.json 2>&1 | grep -v '^#' | grep -c "llmProgress" ; test $? -eq 0 && npx tsc --noEmit -p server/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p server/tsconfig.json` exits 0 (no new type errors introduced by the added interfaces/field).
    - Source assertion: `grep -n "interface RunHistoryEntry" server/lib/llmProgress.ts` returns a match; `grep -n "interface CallHistoryEntry" server/lib/llmProgress.ts` returns a match; `grep -n "runId?" server/lib/llmProgress.ts` returns a match on the LLMPipelineProgress interface.
    - Source assertion: the existing `LLMRunSummary` interface at :275 is unchanged (no fields added/removed) — `git diff server/lib/llmProgress.ts` shows no edit inside the LLMRunSummary block.
  </acceptance_criteria>
  <done>RunHistoryEntry + CallHistoryEntry interfaces exported, runId? added to LLMPipelineProgress, LLMRunSummary untouched, server typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build llmCallHistory.ts (bounded ring + hydration) with its test file</name>
  <files>server/lib/llmCallHistory.ts, server/lib/__tests__/llmCallHistory.test.ts</files>
  <read_first>
    - server/lib/llmDLQ.ts (the canonical analog — read :1-21 module head + key/TTL/cap constants, :44-55 parseEntry string-or-object guard, :56-102 bounded-list write/read; COPY the parseEntry guard verbatim)
    - server/cache/redis.ts (:1-42 — confirms redis.lpush/ltrim/expire/lrange exposure and the REDIS_OP_TIMEOUT_MS withTimeout wrapper semantics)
    - server/lib/llmProgress.ts (the CallHistoryEntry type from Task 1 + the in-memory llmProgress.callHistory singleton being hydrated)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/lib/llmCallHistory.ts (NEW)" — the copy-from excerpts for append/list/hydrate
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md Pattern 1, Pattern 2, Pattern 4, Pitfall 3
  </read_first>
  <action>
    Create `server/lib/llmCallHistory.ts` exporting: (a) `appendCallHistory(entry: CallHistoryEntry): Promise<void>` — `redis.lpush(CALLS_KEY, JSON.stringify(entry))` then `redis.ltrim(CALLS_KEY, 0, CALLS_MAX - 1)` then `redis.expire(CALLS_KEY, CALLS_TTL_SEC)`, wrapped in try/catch that logs via `logger.child({ module: 'llm-call-history' })` and NEVER throws (observability-only anti-pattern). Constants: `CALLS_KEY = 'llm:calls:history'`, `CALLS_MAX = 500`, `CALLS_TTL_SEC = 30 * 24 * 3600` (D-02). (b) `listCallHistory(limit = CALLS_MAX): Promise<CallHistoryEntry[]>` — `redis.lrange(CALLS_KEY, 0, limit - 1)`, map through a `parseEntry` string-or-object guard (copy from llmDLQ.ts:44-55, Pitfall 3), filter nulls, try/catch returns `[]`. (c) `hydrateCallHistoryIfCold(): Promise<void>` — module-level `let callHistoryHydrated = false`; if already hydrated return; set the flag FIRST (best-effort, never retry-loop, D-05/OBS-FLIGHT-06); read `listCallHistory(20)`; if non-empty AND the in-memory `llmProgress.callHistory` is empty, populate `llmProgress.callHistory = fromRedis.slice(0, 20)`. Also create `server/lib/__tests__/llmCallHistory.test.ts` with `// @vitest-environment node` at top, mocking `../../cache/redis.js`: assert (1) appendCallHistory issues lpush+ltrim(0,499)+expire(30d); (2) listCallHistory parses BOTH string members AND already-deserialized object members (Pitfall 3); (3) cold-start hydrate populates the empty singleton on first call and the flag short-circuits the second call (no second lrange); (4) a thrown redis op makes append a no-op (no throw) and list return `[]`.
  </action>
  <verify>
    <automated>npx vitest run server/lib/__tests__/llmCallHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/lib/__tests__/llmCallHistory.test.ts` exits 0 with all cases passing.
    - Behavior assertion: the test for `listCallHistory` passes when the redis mock returns a mixed array `['{"runId":"a",...}', {runId:'b',...}]` — both members survive (parseEntry handles string-or-object).
    - Behavior assertion: the hydrate test asserts `redis.lrange` is called exactly once across two `hydrateCallHistoryIfCold()` invocations (flag prevents re-LRANGE).
    - Source assertion: `grep -c "'llm:calls:history'" server/lib/llmCallHistory.ts` returns 1 (string literal present for the drift gate — registered in Task 4).
  </acceptance_criteria>
  <done>llmCallHistory.ts append/list/hydrate implemented degrade-open with parseEntry string-or-object guard; test file green with 4 behavior cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build llmRunHistory.ts (open/close lifecycle + dedupe-by-runId) with its test file</name>
  <files>server/lib/llmRunHistory.ts, server/lib/__tests__/llmRunHistory.test.ts</files>
  <read_first>
    - server/lib/llmDLQ.ts (same bounded-list + parseEntry analog as Task 2)
    - server/lib/llmCallHistory.ts (Task 2 — reuse the identical parseEntry, LPUSH+LTRIM+EXPIRE, degrade-open try/catch, and hydration-flag idiom; keep the two modules structurally parallel)
    - server/lib/llmProgress.ts (the RunHistoryEntry type from Task 1)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-PATTERNS.md §"server/lib/llmRunHistory.ts (NEW)" — open/close/dedupe excerpts
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md §"GA-2" (re-LPUSH NOT in-place LSET; dedupe first-wins) and Pitfall 5 (stale 'running' record by design)
  </read_first>
  <action>
    Create `server/lib/llmRunHistory.ts` with constants `RUNS_KEY = 'llm:runs:history'`, `RUNS_MAX = 200`, `RUNS_TTL_SEC = 30 * 24 * 3600` (D-02), logger child `{ module: 'llm-run-history' }`. Export: (a) `openRunRecord(args: { runId: string; startedAt: string }): Promise<void>` — LPUSH a record `{ runId, startedAt, completedAt: null, outcome: 'running', ... }` with zeroed numeric fields, then LTRIM(0, RUNS_MAX-1) + EXPIRE, degrade-open. (b) `closeRunRecord(entry: RunHistoryEntry): Promise<void>` — re-LPUSH the FULL terminal `RunHistoryEntry` (NOT LSET-by-index — GA-2 rationale: LTRIM shifts indices, re-LPUSH is append-only and ordering-safe), then LTRIM + EXPIRE, degrade-open. (c) `listRunHistory(limit = RUNS_MAX): Promise<RunHistoryEntry[]>` — LRANGE, parseEntry each, filter nulls, then dedupe by `runId` keeping the FIRST (head = newest = terminal) occurrence using a `Set<string>` (per PATTERNS dedupe snippet), try/catch returns `[]`. (d) `hydrateRunHistoryIfCold(): Promise<void>` — mirror the call-history hydration-flag pattern (no in-memory run singleton exists, so this primes nothing beyond marking hydrated; keep it for endpoint-symmetry and future use — it may simply set the flag and return, OR populate a future singleton; keep it a no-op-safe degrade-open helper that calls listRunHistory once). Also create `server/lib/__tests__/llmRunHistory.test.ts` (`// @vitest-environment node`, mock `../../cache/redis.js`): assert (1) openRunRecord LPUSHes an `outcome:'running', completedAt:null` record; (2) closeRunRecord re-LPUSHes a terminal record (a SECOND lpush, not lset); (3) listRunHistory dedupes when the same runId appears twice (running + terminal) returning the terminal/head occurrence and only ONE entry for that runId; (4) a never-closed `running` record survives dedupe and is returned (Pitfall 5 — the "run that died" signal); (5) a thrown redis op makes writes no-ops and list returns `[]`.
  </action>
  <verify>
    <automated>npx vitest run server/lib/__tests__/llmRunHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/lib/__tests__/llmRunHistory.test.ts` exits 0 with all cases passing.
    - Behavior assertion: closeRunRecord causes a second `redis.lpush` call (re-LPUSH), and the test explicitly asserts `redis.lset` is NEVER called.
    - Behavior assertion: when the mocked LRANGE returns `[terminalForRunA, runningForRunA, runningForRunB]`, `listRunHistory` returns exactly two entries (one per runId), runA's being the terminal (head-first wins).
    - Source assertion: `grep -c "'llm:runs:history'" server/lib/llmRunHistory.ts` returns at least 1 (string literal present for the drift gate — registered in Task 4).
  </acceptance_criteria>
  <done>llmRunHistory.ts open/close (re-LPUSH)/list (dedupe-by-runId first-wins)/hydrate implemented degrade-open; test green with 5 cases incl. the never-closed-running survivor case.</done>
</task>

<task type="auto">
  <name>Task 4: Register both new Redis keys across all three drift-gate surfaces (HARD GATE)</name>
  <files>CLAUDE.md, docs/architecture/redis-keys.md</files>
  <read_first>
    - src/__tests__/lib/redis-registry.test.ts (:1-219 — the drift gate: read the three-surface parity requirement and the normalization regex so the registration format matches exactly)
    - CLAUDE.md §"Active Redis keys (current-state registry)" — read the existing `events:llm-dlq` bullet to mirror its style (key in backticks + LPUSH+LTRIM cap + TTL + writer + reader)
    - docs/architecture/redis-keys.md (:11-107 — read an existing `llm:*` table row to copy the exact column format)
    - .planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-RESEARCH.md §"Redis-registry drift gate — EXACT registration format" and 39-PATTERNS.md §"CLAUDE.md + docs/architecture/redis-keys.md (MOD — register 2 keys)"
  </read_first>
  <action>
    Achieve three-surface parity for `llm:calls:history` and `llm:runs:history` so `src/__tests__/lib/redis-registry.test.ts` passes. Surface 1 (CLAUDE.md): add two bullets under "Active Redis keys (current-state registry)" mirroring the `events:llm-dlq` bullet — `llm:calls:history` (LPUSH+LTRIM 500-cap, 30d TTL; writer `server/lib/llmCallHistory.ts` appendCallHistory; reader `/api/events/llm-history` + cold-start hydration; entry = CallHistoryEntry) and `llm:runs:history` (LPUSH+LTRIM 200-cap, 30d TTL; writer `server/lib/llmRunHistory.ts` openRunRecord/closeRunRecord; reader `/api/events/llm-history`; entry = RunHistoryEntry, dedupe-by-runId). Surface 2 (docs/architecture/redis-keys.md): add two table rows using the existing `llm:*` column format — `| llm:calls:history | server/lib/llmCallHistory.ts appendCallHistory | reader + /api/events/llm-history | 30d (LTRIM 500) | JSON CallHistoryEntry[] | call-level flight recorder | <=500 (capped) | observability |` and the analogous `llm:runs:history` row (30d / LTRIM 200 / RunHistoryEntry[] / <=200). Surface 3 (code string literal): already satisfied by the `'llm:calls:history'` / `'llm:runs:history'` literals in Tasks 2-3 lib modules — confirm both are present in non-test `.ts`. Both keys are static (no `{...}` placeholder) so they must appear verbatim in all three surfaces.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/redis-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/lib/redis-registry.test.ts` exits 0 (the drift gate passes with both new keys — this is the HARD phase gate).
    - Source assertion: `grep -c '`llm:calls:history`' CLAUDE.md` >= 1 AND `grep -c '`llm:runs:history`' CLAUDE.md` >= 1.
    - Source assertion: `grep -c 'llm:calls:history' docs/architecture/redis-keys.md` >= 1 AND `grep -c 'llm:runs:history' docs/architecture/redis-keys.md` >= 1.
    - Source assertion: `grep -rln "'llm:calls:history'" server/lib/*.ts | grep -v __tests__` returns llmCallHistory.ts (code literal in non-test .ts).
  </acceptance_criteria>
  <done>Both keys registered in CLAUDE.md + redis-keys.md + code literal; redis-registry drift gate green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                             |
| ----------------------------- | ----------------------------------------------------------------------- |
| Redis (Upstash REST) ↔ server | Observability data persisted to bounded lists; all reads degrade-open   |
| in-memory singleton ↔ Redis   | Cold-start hydration copies Redis state into the per-instance singleton |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-01-D | Denial of Service      | `llm:calls:history` / `llm:runs:history` unbounded growth | mitigate    | LTRIM(0, MAX-1) caps lists at 500 / 200; 30d EXPIRE bounds retention. Tested in Tasks 2-3.                                                                             |
| T-39-01-I | Information Disclosure | history entries hold prompt-adjacent telemetry            | accept      | These lib modules are write/read primitives only; the Bearer gate on the consuming endpoint (Plan 04) is the access-control surface. No endpoint exposed by this plan. |
| T-39-01-T | Tampering              | malformed Redis members crashing readers                  | mitigate    | `parseEntry` string-or-object guard filters non-parseable members; thrown ops return `[]` (degrade-open).                                                              |
| T-39-SC   | Tampering              | npm/pip/cargo installs                                    | mitigate    | No new packages installed this plan (RESEARCH §Package Legitimacy Audit: N/A). slopcheck not applicable.                                                               |

</threat_model>

<verification>
- `npx vitest run server/lib/__tests__/llmCallHistory.test.ts` green
- `npx vitest run server/lib/__tests__/llmRunHistory.test.ts` green
- `npx vitest run src/__tests__/lib/redis-registry.test.ts` green (HARD gate)
- `npx tsc --noEmit -p server/tsconfig.json` exits 0
</verification>

<success_criteria>

- Both Redis lib modules exist, degrade-open, bounded (500 / 200, 30d), with parseEntry guards
- RunHistoryEntry + CallHistoryEntry + runId? defined in llmProgress.ts without touching LLMRunSummary
- Cold-start hydration helper repopulates the singleton on first call, flag-guarded against re-LRANGE
- Redis-registry drift gate green (both keys in all three surfaces)
  </success_criteria>

<output>
Create `.planning/phases/39-operator-visibility-token-budget-cost-shadow-llm-flight-reco/39-01-SUMMARY.md` when done
</output>

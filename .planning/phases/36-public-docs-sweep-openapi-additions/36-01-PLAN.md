---
phase: 36-public-docs-sweep-openapi-additions
plan: 01
type: execute
wave: 2
depends_on: ['36-02', '36-03', '36-04', '36-05']
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-PUB-01]
tags: [readme, rate-limit-drift, llm-enrichment, prod-audit, api-health-merge]
must_haves:
  truths:
    - 'README line ~206 rate-limit drift fixed: `rateLimiters.public (6 req/min baseline)` → `rateLimiters.public (60 req/min global tier)`. Line 21 already says "60 req/min" (verified 2026-05-29); line 206 contradicts. Truth is 60/min global + per-endpoint tiers (CLAUDE.md §Vercel Deployment + `server/middleware/rateLimiter.ts`). (D-18)'
    - 'New `## LLM Enrichment` subsection inserted under `## Engineering Deep Dive` AFTER `## Test Suite` and BEFORE the next existing subsection. ~80-120 lines covering: v3 cron-driven extraction (4am UTC daily); NIM as active provider (qwen-235b instruct); OpenRouter dormant per Phase 30.1 with ADR-0010 cross-link; Cerebras + Groq deferred per Phase 34 with ADR-0010 cross-link; 6-path resolver enumeration; Pitfall 1 cache bridge contract (link to `docs/degradation.md`); prod-connectivity-audit.yml acceptance gate. (D-17)'
    - 'New paragraph mentioning `prod-connectivity-audit.yml` workflow: manual-trigger workflow that runs the tier audit and writes `audit:connectivity:last-result` to Redis; v1.5 acceptance gate (3× consecutive `allTiersGreen=true`) unblocks v1.6 milestone close. Lands inside the new LLM Enrichment section. (D-19)'
    - 'DevApiStatus references updated to reflect v1.4 merge: the separate dashboard tabs were merged into a single API Health tab (Phase 28.2 W5) aggregating audit + operator-status + byBearer quota + advEval drift + pinTtl into one Bearer-gated operator surface. (D-20)'
    - 'README cross-links to the Phase-35-current architecture deep-dives: `docs/architecture/llm-pipeline-reliability.md` for cascade measurement and `docs/architecture/redis-keys.md` for Redis registry — both files were verified-clean in Plan 36-02 audit.'
    - 'No edits to README sections outside Engineering Deep Dive (no rewrite of `## Quick Start`, `## Data Sources`, `## Visualization Layers`, `## Architecture`, etc.). Surgical edits only.'
    - 'Existing section ordering preserved per Claude-Discretion 4: LLM Enrichment lands AFTER `## Test Suite`, BEFORE the next existing subsection. Minimal structural change.'
    - 'Atomic per-decision commits: D-18 (rate-limit drift), D-19 (workflow mention — folded into D-17 commit if in same paragraph), D-20 (API Health merge — folded into D-17 commit), D-17 (LLM Enrichment section). Up to 4 commits OR 2 commits if D-19/D-20 are folded into the D-17 section addition.'
  artifacts:
    - path: 'README.md'
      provides: 'Updated with new LLM Enrichment section + rate-limit drift fix + prod-audit workflow mention + API Health merge note'
      contains: '## LLM Enrichment'
  key_links:
    - from: 'README.md §LLM Enrichment'
      to: 'docs/architecture/llm-pipeline-reliability.md'
      via: 'inline markdown link in cascade narrative'
      pattern: 'llm-pipeline-reliability\\.md'
    - from: 'README.md §LLM Enrichment'
      to: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      via: 'inline markdown link in OR-dormant + Cerebras/Groq-deferred prose'
      pattern: '0010-v1-5-llm-pipeline-narrowing-and-deletion'
    - from: 'README.md §LLM Enrichment'
      to: 'docs/degradation.md'
      via: 'inline link in Pitfall 1 contract bullet'
      pattern: 'degradation\\.md'
---

<objective>
Bring README.md to v1.4 + v1.5 reality. Add the missing public-facing description of the LLM enrichment pipeline (NIM-only runtime, dormant OR, deferred Cerebras/Groq, Pitfall 1 contract, prod-audit acceptance gate). Fix the rate-limit drift between line 21 (60/min — correct) and line 206 (6/min — stale). Note the API Health dashboard tab merge from Phase 28.2 W5.

Purpose: README is the first surface a visitor reads. The current README under-describes v1.4 + v1.5 work (v3 pipeline narrowing, prod-audit workflow, API Health merge); the rate-limit drift signals "this doc isn't actively maintained" to careful readers. Phase 36 fixes both. Per CONTEXT.md D-26 wave shape, this plan lands in Wave 2 AFTER Wave 1 closes — so cross-links to architecture/runbook/degradation/spec resolve cleanly.

Output: 1-4 atomic commits per D-26 / D-27. Recommended split: D-18 (rate-limit drift) as standalone; D-17 (LLM Enrichment section) folds D-19 + D-20 into the new section's prose for cohesion. Two commits total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md
@CLAUDE.md
@README.md
@docs/architecture/llm-pipeline-reliability.md
@docs/architecture/redis-keys.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@docs/degradation.md
@docs/runbook.md
@server/openapi.yaml

<interfaces>
<!-- README structure landmarks (verified 2026-05-29). -->

Section index:

- Line 1: `# Iran Monitor` (title)
- Line 19-26: rate-limit prose mentioning "60 req/min per-IP global rate-limit tier" (CORRECT — D-18 leaves untouched)
- Line 29: `## Engineering` (section heading)
- Line 55: `## Table of Contents`
- Line 71: `## Features`
- Line 105: `## Quick Start`
- Line 178: `## Architecture`
- Line 206: `rateLimiters.public (6 req/min baseline)` — STALE; D-18 fixes to "(60 req/min global tier)"
- Line 235: `## Data Sources`
- Line 258: `## Visualization Layers`
- Line 276: `## Screenshots`
- Line 287: `## Engineering Deep Dive` (parent section)
- Line 441: `## Environment Variables`
- Line 468: `## Testing`
- Line 506: `## What I Learned / What I'd Do Differently`
- Line 617: `## License`

`## Engineering Deep Dive` subsection ordering (lines 287-441):

- Likely starts with `## Test Suite` then proceeds through `### Live Demo`, `### OpenAPI Contract`, `### Graceful Degradation`, etc. Executor must read lines 287-441 to confirm subsection order before choosing the LLM Enrichment insertion point.

<!-- D-18 rate-limit drift target (line 206). -->

CURRENT (line 206):

```
    ├── rateLimiters.public (6 req/min baseline)
```

NEW (line 206):

```
    ├── rateLimiters.public (60 req/min global tier)
```

This is inside an architecture ASCII diagram block (lines ~200-215). The fix is one-line; do NOT touch surrounding lines.

Surrounding prose around line 365-379 (rate-limit description) also needs verification per CONTEXT.md `<canonical_refs>`:

> README.md:368-379 — Existing rate-limiter prose. D-18 + D-19 surround edits.

Read lines 365-379 carefully; if the surrounding prose contradicts the 60/min number, fix in place (in the same D-18 commit).

<!-- D-17 new LLM Enrichment section — verbatim content. -->

Insertion point: AFTER `## Test Suite` end (find by reading lines 468-505), BEFORE the next subsection. Likely insertion point is around line 505 (just before `## What I Learned`). Per Claude-Discretion 4: minimal structural change — LLM Enrichment lands AFTER Test Suite, BEFORE next existing subsection.

```markdown
## LLM Enrichment

The conflict-event layer combines two streams: raw GDELT events
(15-min refresh) and v3 LLM-enriched events (daily 4:00 UTC cron). The
LLM pipeline is intentionally optional — when it's healthy, every event
carries CAMEO classification + a precise lat/lon from the 6-path resolver

- a one-paragraph summary. When it's not, the map keeps rendering raw
  GDELT (CAMEO classification only); the [Pitfall 1 cache bridge contract
  in degradation.md](docs/degradation.md) documents this "map never goes
  blank" invariant.

### v3 cron-driven extraction

`/api/cron/refresh-events` (daily 4:00 UTC) is the sole production writer
to `events:llm:v3`. Anti-pattern #17 (cron-only writer discipline) keeps
the pipeline crash-free under Vercel Fluid Compute — the pre-Phase-29
v2 fire-and-forget IIFE pattern was silently killed mid-extraction when
the HTTP response was sent. v3 cron-only architecture prevents this
class of incident; see [runbook §14 Cron architecture lessons](docs/runbook.md#14-cron-architecture-lessons-phase-2826-fire-and-forget-iife-incident).

The active LLM provider is **NIM (qwen-235b instruct)**. The cascade
construction in `server/lib/freeClaudeRouter.ts` declares a NIM →
OpenRouter chain, but **OpenRouter is dormant at runtime** per the
Phase 30.1 sub-block of [ADR-0010](docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md) — the free-tier probe landed in the not-viable bucket (~90% rate-limited). Cerebras and Groq adapters were
**deferred** per the Phase 34 sub-block of the same ADR; the operator
chose to skip provisioning at v1.5 close. Both providers' runtime paths
are dormant-ready — restoration is a future-phase decision, not a
regression. The [`docs/architecture/llm-pipeline-reliability.md`](docs/architecture/llm-pipeline-reliability.md) deep-dive captures the
measured throttle window, tuned defaults (`LLM_V3_CONCURRENCY=12`,
`LLM_BATCH_SIZE`, `LLM_BATCH_TIMEOUT_MS`, `BACKOFF_MS`), and the
retired-mechanism rationale.

### 6-path resolver

For each event, the resolver walks 6 paths in order until one returns
a coordinate with provenance:

1. **own-site-snapshot** — match against the existing infrastructure snapshot in `sites:v3`
2. **poi-amenity-nominatim** — Nominatim search constrained to POI/amenity tags
3. **nominatim-direct** — direct Nominatim forward geocode (constrained to ME viewbox)
4. **nominatim-verified-2pass** — two-pass verify of an ambiguous result
5. **gdelt-actiongeo-fallback** — fall back to GDELT's ACTIONGEO coordinate
6. **bellingcat-coord-passthrough** — Bellingcat-source coordinate passthrough

The resolver never returns a coordinate without provenance — every event
on the map carries a traceable lineage entry in `events:llm:v3:lineage:{eventId}`.

### Production health verification

Production health is verified by `.github/workflows/prod-connectivity-audit.yml`
— a manually-triggered workflow that runs the tier audit and writes
`audit:connectivity:last-result` to Redis. The audit result is surfaced
on the API Health dashboard tab and exposed via [`/api/audit-status`](server/openapi.yaml) (degrade-open; no auth gate). The v1.5 acceptance gate
(3× consecutive `allTiersGreen=true`) unblocks v1.6 milestone close.

### API Health dashboard tab (Phase 28.2 W5)

The DevApiStatus dashboard's separate tabs (audit / operator-status /
byBearer / advEval / pinTtl) were merged into a single **API Health**
tab in Phase 28.2 W5. The tab aggregates audit24h + byBearer quota +
pinTtl + advEval drift into one operator surface; Bearer-gated via
`DASHBOARD_PASSWORD`. The merge reduced operator cognitive load during
incident response (one place to look) and simplified per-Bearer quota
visibility.

### Redis key registry

All Redis state is documented in [`docs/architecture/redis-keys.md`](docs/architecture/redis-keys.md) (Phase 35 D-05 deep-dive). A drift gate
(`src/__tests__/lib/redis-registry.test.ts`) enforces parity between
CLAUDE.md §Serverless Cache, `docs/architecture/redis-keys.md`, and
the codebase — adding an undocumented Redis key fails the next
`vitest run`. Phase 36 added a similar mechanical primitive for the
OpenAPI spec (`server/__tests__/openapi/openapi-lint.test.ts`).
```

<!-- Verification: README invariants this plan asserts. -->

```bash
# D-18 — rate-limit drift fixed
grep -n "rateLimiters.public (60 req/min global tier)" README.md
! grep -nE "rateLimiters\\.public \\([0-9]+ req/min baseline\\)" README.md

# D-17 — LLM Enrichment section present
grep -n "^## LLM Enrichment" README.md

# D-19 — prod-audit workflow mention present
grep -n "prod-connectivity-audit.yml" README.md

# D-20 — API Health merge mention present
grep -n "API Health" README.md

# Cross-links present
grep -n "llm-pipeline-reliability.md\|0010-v1-5-llm-pipeline-narrowing\|degradation.md\|runbook.md\|redis-keys.md" README.md
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rate-limit drift at line 206 + surrounding prose (D-18)</name>
  <files>README.md</files>
  <read_first>
    README.md (lines 19-30 — current rate-limit prose at line 21; lines 200-220 — architecture ASCII diagram around line 206; lines 365-385 — additional rate-limit prose per CONTEXT.md `<canonical_refs>`)
    CLAUDE.md (§Vercel Deployment — confirms 60 req/min global + per-endpoint tiers)
    server/middleware/rateLimiter.ts (verify `rateLimiters.public` actual numeric limit — likely 60, not 6)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-18)
  </read_first>
  <action>
    **STEP A — Verify the source of truth.** Read `server/middleware/rateLimiter.ts` and grep for `rateLimiters.public` definition. Confirm the numeric value — CLAUDE.md says 60/min. If the code says something different, use the code value and note the additional drift in the commit body.

    **STEP B — Surgical edit at line 206.** Use the Edit tool with the exact old_string:
    ```
        ├── rateLimiters.public (6 req/min baseline)
    ```
    and new_string:
    ```
        ├── rateLimiters.public (60 req/min global tier)
    ```
    Verify uniqueness — if `rateLimiters.public (6 req/min baseline)` appears in multiple places, use `replace_all: true`.

    **STEP C — Audit lines 365-385.** Read this range. If the prose contains contradictory rate-limit numbers (e.g., "6 req/min baseline" anywhere), fix in place. Also check for the Phase 28.2 D-04 Bearer-bypass framing — line 21-26 has the correct framing ("Operator surfaces ... skip the global rate-limit tier when authenticated (Phase 28.2 D-04)"); ensure the deeper-dive section consistent. Per CONTEXT.md D-18, the Bearer-bypass mention at line 24 stays accurate.

    **STEP D — Commit:** `docs(36): D-18 fix README rate-limit drift (60/min global; was 6/min stale)` body lists the line numbers changed (206 + any line 365-385 hits).

    DO NOT add the LLM Enrichment section in this task — that's Task 2 (separate atomic commit per D-27).

  </action>
  <verify>
    <automated>grep -q "rateLimiters.public (60 req/min global tier)" README.md && ! grep -qE "rateLimiters\\.public \\([0-9]+ req/min baseline\\)" README.md && grep -q "60 req/min per-IP global rate-limit tier" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `rateLimiters.public (60 req/min global tier)` at the architecture ASCII diagram location (around line 206).
    - `README.md` contains NO occurrences of the stale `rateLimiters.public (6 req/min baseline)` string.
    - `README.md` line 21 still says `60 req/min per-IP global rate-limit tier` (left untouched — already correct).
    - `git diff main -- README.md` shows changes ONLY in the rate-limit-related lines (no incidental edits).
    - `git log -1 --format=%s README.md` matches `docs(36):` with `D-18` in body.
  </acceptance_criteria>
  <done>
    Rate-limit drift fixed. Public README now describes the same 60/min global tier everywhere; no contradictions between line 21 and line 206.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add new "## LLM Enrichment" section under Engineering Deep Dive (D-17, D-19, D-20)</name>
  <files>README.md</files>
  <read_first>
    README.md (lines 287-441 — entire Engineering Deep Dive section; identify exact subsection ordering and the insertion point AFTER `## Test Suite`; lines 468-505 around `## Testing` — confirm subsection boundaries)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-17, D-19, D-20)
    docs/architecture/llm-pipeline-reliability.md (cascade narrative — Section that this README section cross-links to; verify the section heading anchor)
    docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (Phase 30.1 + Phase 34 sub-blocks — README cross-links to these)
    docs/degradation.md (post-Plan-36-04 state — Pitfall 1 contract; README cross-links to it)
    docs/runbook.md (post-Plan-36-03 state — §14 cron architecture lessons; README cross-links to it)
    docs/architecture/redis-keys.md (Phase 35 deep-dive — README cross-links)
  </read_first>
  <action>
    **STEP A — Identify exact insertion point.** Read lines 468-510 of README.md. Find:
    - The end of `## Testing` subsection (where the test stats / commands block ends)
    - The start of the next `## ` subsection (likely `## What I Learned / What I'd Do Differently` at line 506)

    Insertion point: between end-of-`## Testing` content and start-of-`## What I Learned`. Per Claude-Discretion 4 (CONTEXT.md): "LLM Enrichment lands AFTER Test Suite, BEFORE the next existing subsection — minimal structural change." So insert the new `## LLM Enrichment` section as a sibling of `## Testing`, immediately before `## What I Learned`.

    **STEP B — Verify all cross-link targets exist (Wave 2 / depends_on=[36-02, 36-03, 36-04, 36-05] gives us this guarantee, but double-check):**
    - `docs/architecture/llm-pipeline-reliability.md` — exists, Phase 35 verified-clean, Plan 36-02 audit row should confirm
    - `docs/architecture/redis-keys.md` — exists (Phase 35 D-05)
    - `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` — exists
    - `docs/degradation.md` — post-Plan-36-04 state has Pitfall 1 contract
    - `docs/runbook.md` — post-Plan-36-03 state has §14 with the anchor `#14-cron-architecture-lessons-phase-2826-fire-and-forget-iife-incident`
    - `server/openapi.yaml` — post-Plan-36-05 state has `/api/audit-status` documented
    - `src/__tests__/lib/redis-registry.test.ts` — exists (Phase 35 D-01)
    - `server/__tests__/openapi/openapi-lint.test.ts` — post-Plan-36-05 state exists

    If any link target is missing, this is a Wave-2 dependency violation — surface the gap (Wave 1 plan didn't ship its file), pause and ask operator before continuing.

    **STEP C — Insert the section.** Use the Edit tool with old_string = the first `## ` line that begins the section AFTER `## Testing` (likely `## What I Learned / What I'd Do Differently`), and new_string = the verbatim LLM Enrichment section content from `<interfaces>` + a `---` separator + the original `## What I Learned ...` heading.

    The verbatim section content includes:
    - `## LLM Enrichment` heading
    - 6 subsections: opening paragraph, `### v3 cron-driven extraction`, `### 6-path resolver`, `### Production health verification`, `### API Health dashboard tab (Phase 28.2 W5)`, `### Redis key registry`
    - All cross-links: llm-pipeline-reliability.md (×2), ADR-0010, degradation.md, runbook.md §14 anchor, redis-keys.md, openapi.yaml

    D-19 (prod-audit workflow mention) is folded into `### Production health verification` subsection. D-20 (API Health merge mention) is its own `### API Health dashboard tab` subsection. Both included verbatim in the section block. No additional standalone paragraphs needed — they live inside the LLM Enrichment section per CONTEXT.md D-17 spec ("~80-120 lines covering: ... prod-connectivity-audit.yml acceptance gate").

    **STEP D — Verify markdown link integrity.** After insertion, run a manual check: open each cross-link in a previewer (or grep + manually verify each target file exists at the relative path). If `npm run docs:lint` is available (post-Plan-36-05 install), run it: `npm run docs:lint -- README.md` should exit 0. If broken links surface, fix relative paths (common error: `docs/...` vs `./docs/...` — README is at repo root, so `docs/...` is correct relative form).

    **STEP E — Commit:** `docs(36): D-17 D-19 D-20 add README LLM Enrichment section with cross-links to architecture / ADR / degradation / runbook / spec` body lists the 6 subsections + the 7+ cross-links.

    DO NOT remove or rewrite existing subsections (Test Suite stays as-is; What I Learned stays as-is; Architecture Deep Dive parent stays as-is). Pure insertion.

  </action>
  <verify>
    <automated>grep -q "^## LLM Enrichment" README.md && grep -q "^### v3 cron-driven extraction" README.md && grep -q "^### 6-path resolver" README.md && grep -q "^### Production health verification" README.md && grep -q "^### API Health dashboard tab" README.md && grep -q "^### Redis key registry" README.md && grep -q "prod-connectivity-audit.yml" README.md && grep -q "API Health" README.md && grep -q "(docs/architecture/llm-pipeline-reliability.md)" README.md && grep -q "(docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md)" README.md && grep -q "(docs/degradation.md)" README.md && grep -q "(docs/runbook.md" README.md && grep -q "(docs/architecture/redis-keys.md)" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains exactly one `^## LLM Enrichment` heading.
    - The LLM Enrichment section appears AFTER `## Testing` and BEFORE `## What I Learned / What I'd Do Differently`.
    - Six subsection headings present: `### v3 cron-driven extraction`, `### 6-path resolver`, `### Production health verification`, `### API Health dashboard tab (Phase 28.2 W5)`, `### Redis key registry`, plus opening paragraph.
    - `prod-connectivity-audit.yml` mentioned in the Production health verification subsection (D-19).
    - "API Health" dashboard tab merge mentioned in its own subsection (D-20).
    - Cross-links to all 5 target docs/specs present: `llm-pipeline-reliability.md` (×2 — narrative + tuned-defaults reference), `0010-v1-5-llm-pipeline-narrowing-and-deletion.md`, `degradation.md`, `runbook.md#14-cron-architecture-lessons-...`, `redis-keys.md`, `openapi.yaml`.
    - NIM-only-at-runtime + OpenRouter-dormant + Cerebras/Groq-deferred framing matches CONTEXT.md D-01 verbatim wording (cite phase number + ADR-0010 sub-block).
    - `git log -1 --format=%s README.md` matches `docs(36):` with `D-17` in body.
    - README line count grew by ~80-120 lines (per D-17 spec).
  </acceptance_criteria>
  <done>
    README now publicly describes the LLM enrichment pipeline as it ships in v1.5. Cross-links to architecture / ADR / degradation / runbook / spec all resolve. The merged API Health dashboard tab and the prod-audit acceptance gate are publicly visible.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                 |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Author → README.md               | Hand-authored markdown edits; no input validation; mitigated by markdown-link-check gate in Plan 36-06 D-24 |
| README cross-links → Wave 1 docs | Wave-2 dependency on Wave-1 plan output; if a target file is missing, executor pauses                       |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                                             | Disposition | Mitigation Plan                                                                                                                                                             |
| ---------- | ---------------------- | ----------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-01-01 | Information Disclosure | README publishes LLM provider names (NIM, OpenRouter, Cerebras, Groq) + dashboard surface description | accept      | Provider names are not secrets; ADR-0010 already publishes them; dashboard URL is public on portfolio site. Documenting them in README mirrors existing public-doc surface. |
| T-36-01-02 | Tampering              | Cross-link relative paths                                                                             | mitigate    | Verified during edit + by D-24 markdown-link-check run in Plan 36-06. Pre-emptive grep of all `(docs/...)` patterns confirms resolution.                                    |
| T-36-01-03 | Spoofing               | README cites `/api/audit-status` as a degrade-open public endpoint                                    | accept      | The endpoint IS public and degrade-open by design (Phase 28.2 W6); documenting that posture matches reality.                                                                |
| T-36-01-SC | Tampering              | No npm installs in this plan                                                                          | n/a         | Pure README edit.                                                                                                                                                           |

</threat_model>

<verification>
- `npx vitest run` exits 0 (no source/test code touched).
- `npm run docs:lint -- README.md` exits 0 (post-Plan-36-05; if Plan 36-05 deferred this, it lands in Plan 36-06 D-24 sweep).
- `npx tsc --noEmit` exits 0.
- Manual review: render README.md on GitHub web (push to feature branch first); confirm the LLM Enrichment section lands at the right place, cross-links navigate, and ASCII diagrams at line 206 area still render correctly.
- Goal-backward sanity: a visitor reading README from top to bottom understands (a) NIM is the active LLM provider, (b) OpenRouter is dormant and why, (c) Cerebras + Groq are deferred and why, (d) the Pitfall 1 contract keeps the map from going blank, (e) prod-audit is the v1.5 acceptance gate, (f) API Health is the merged operator surface, (g) where to dig deeper (architecture / ADR / runbook / spec).
</verification>

<success_criteria>

1. Rate-limit drift fixed at line 206 (60/min global tier; was 6/min stale).
2. New `## LLM Enrichment` section (~80-120 lines) added under Engineering Deep Dive, after Test Suite, before What I Learned.
3. Section covers: v3 cron-driven extraction, NIM-only runtime, OR-dormant, Cerebras/Groq-deferred, 6-path resolver, Pitfall 1 contract, prod-audit acceptance gate, API Health merge, Redis key registry.
4. Cross-links to llm-pipeline-reliability.md / ADR-0010 / degradation.md / runbook.md / redis-keys.md / openapi.yaml all resolve.
5. 2 atomic commits (D-18 standalone + D-17/D-19/D-20 rollup) per D-26 / D-27.
6. Coverage: DOCS-PUB-01 addressed.
7. No edits to README sections outside Engineering Deep Dive + line 206 ASCII diagram.

</success_criteria>

<output>
Create `.planning/phases/36-public-docs-sweep-openapi-additions/36-01-SUMMARY.md` when done. Include:
- Line numbers of D-18 fixes (rate-limit drift).
- Insertion point of new `## LLM Enrichment` section (line range).
- Total README line-count delta.
- Cross-link inventory (target file + relative path used + resolution status).
- Commit hashes (2 commits expected).
- Notes on any incidental drift surfaced + folded in.
</output>

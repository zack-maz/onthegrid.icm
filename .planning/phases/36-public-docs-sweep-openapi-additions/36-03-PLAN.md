---
phase: 36-public-docs-sweep-openapi-additions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbook.md
autonomous: true
requirements: [DOCS-PUB-03]
tags: [runbook, sre, incident-playbook, anchor-shim, table-of-contents]
must_haves:
  truths:
    - 'Runbook section 6 retitled from "Vercel function timeout (10-second limit)" to "Vercel function timeout (300s default / 800s configured ceiling)"; body rewritten against `vercel.json functions."api/vercel-entry.js".maxDuration: 800` (Phase 29 D-08) (D-14).'
    - 'Old section 6 anchor preserved via `<a id="6-vercel-function-timeout-10-second-limit"></a>` shim immediately above the new heading so any external/in-repo link to the old anchor still resolves (D-14).'
    - 'Four new SRE-template sections appended: §13 NIM throttle handling (429 burst + circuit-breaker trip + OpenRouter-dormant degradation path), §14 Cron architecture lessons (28.2.6 fire-and-forget IIFE diagnosis; cron-only-writer invariant; regression detection), §15 Force-trigger runbook (`/api/cron/refresh-events?force=true` with Bearer; quota; cooldown bypass), §16 `prod-connectivity-audit.yml` retry path (workflow_dispatch; expected `audit:connectivity:last-result.allTiersGreen` shape) (D-13).'
    - 'Each new section follows the SRE template: `## N. Title` heading with Symptom / Detection / Cause / Remediation / Prevention subheadings (mirrors existing sections 1-12).'
    - 'Sections 13-16 land BETWEEN current §12 (Quarterly LLM Health Probes) and the "See also" footer (currently at line 886).'
    - 'Table of contents at line 19 updated to list §13-§16 with anchor links.'
    - 'Section 13 cross-links to `docs/architecture/llm-pipeline-reliability.md` Path B framing (Claude-Discretion 5) rather than paraphrasing it inline.'
    - 'Section 14 cross-links to ADR-0010 Phase 29 sub-block for the v1/v2 deletion context AND to the Phase 28.2.6 phase folder for the original IIFE diagnosis.'
    - 'Atomic per-decision commits: one commit for §6 rewrite + anchor shim (D-14), one rollup commit for §13-§16 + ToC update (D-13).'
  artifacts:
    - path: 'docs/runbook.md'
      provides: '4 new SRE sections (13-16) covering v1.5 incident playbooks; rewritten §6 reflecting Pro plan maxDuration: 800 ceiling'
      contains: '## 13. NIM throttle handling'
  key_links:
    - from: 'docs/runbook.md §13 NIM throttle handling'
      to: 'docs/architecture/llm-pipeline-reliability.md §"Multi-Provider Cascade (Phase 34)"'
      via: 'inline markdown link in Detection / Cause subsections'
      pattern: 'llm-pipeline-reliability\\.md#multi-provider-cascade'
    - from: 'docs/runbook.md §14 Cron architecture lessons'
      to: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md Phase 29 sub-block (or main Decision body)'
      via: 'inline markdown link explaining v1/v2 fire-and-forget deletion'
      pattern: '0010-v1-5-llm-pipeline-narrowing-and-deletion\\.md'
    - from: 'docs/runbook.md §6 (rewritten)'
      to: '<a id="6-vercel-function-timeout-10-second-limit"></a> anchor shim'
      via: 'old anchor preserved immediately above new heading'
      pattern: 'id="6-vercel-function-timeout-10-second-limit"'
---

<objective>
Bring `docs/runbook.md` from pre-Phase-27 framing (Hobby plan 10-second timeout, no v3 cron incidents, no force-trigger, no prod-audit retry) to v1.4 + v1.5 incident reality. Rewrite the stalest section (§6 timeout framing) without breaking its anchor; append four new incident playbooks for the failure modes that emerged during the LLM-pipeline phases (27.4 → 34).

Purpose: Runbook is the operator's first stop during incident response. The grep-for-NIM / grep-for-cron / grep-for-force-trigger navigability that makes runbooks useful demands standalone sections per failure mode (CONTEXT.md D-13 "specifics" item). Folding NIM throttle into existing §10 LLM-pipeline-hung saves length but loses navigability.

Output: 2 atomic commits per D-26 / D-27: docs (§6 rewrite + anchor shim) → docs (§13-§16 append + ToC update).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md
@CLAUDE.md
@docs/runbook.md
@docs/architecture/llm-pipeline-reliability.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md

<interfaces>
<!-- Runbook landmarks (verified 2026-05-29). -->

- Line 1: `# Operations Runbook` (file title)
- Line 19: `## Table of contents` (insertion point for §13-§16 ToC entries)
- Lines 21-32: existing ToC entries §1-§12
- Line 351: `## 6. Vercel function timeout (10-second limit)` (D-14 rewrite target)
- Lines 351-406: §6 body (rewrite scope)
- Line 870: `## 12. Quarterly LLM Health Probes` (§12 start)
- Line 886: `## See also` (footer; §13-§16 insertion point is BEFORE this line)
- File size: 676 lines total (per CONTEXT.md `<domain>`)

<!-- SRE template (mirrors existing sections 1-12 verbatim shape). -->

```markdown
## N. Title

**Symptom:** [What the operator observes from outside — UI behavior, log entries, dashboard metrics, error messages.]

**Detection:**

- [Specific log query pattern with module name + log message string]
- [Vercel dashboard / Upstash dashboard / Sentry / etc. surface to check]
- [Specific metric threshold or error rate that signals this mode]

**Cause:** [Why this happens — upstream API behavior, resource exhaustion, race condition, etc. Reference specific code paths.]

**Remediation:**

1. [Immediate step — what to do first to stop the bleeding]
2. [Follow-up step — restore steady state]
3. [Escalation — when to pull in deeper expertise / when this becomes a Sev1]

**Prevention:**

- [Architectural primitives that prevent recurrence]
- [Monitoring / alerting that catches it earlier next time]
- [Open items / known gaps that future phases address]

---
```

<!-- §6 rewrite content (D-14 — verbatim target). -->

NEW §6 body (replaces lines 351-406 inclusive):

```markdown
<a id="6-vercel-function-timeout-10-second-limit"></a>

## 6. Vercel function timeout (300s default / 800s configured ceiling)

**Symptom:** `504 Gateway Timeout` on long-running routes. Most commonly `/api/cron/refresh-events` (which runs the v3 LLM extraction; expected to take ~10 minutes in the worst case under NIM throttle pressure). Less commonly on `/api/water`, `/api/sites`, or `/api/events` first-load after a deploy.

**Detection:**

- Vercel function logs show `ERROR: Task timed out after 800.00 seconds` (or `300.00` if `maxDuration` is omitted from the function in `vercel.json`).
- Vercel dashboard → Deployments → Functions tab shows the affected function's p99 duration approaching the configured `maxDuration`.
- For `/api/cron/refresh-events` specifically: `events:llm-summary:v3` shows `completedAt` unset hours after `startedAt`; `cron:lastTick:refresh-events` not updated.

**Cause:**

- Pre-Phase-29 Hobby plan baseline was a hard 10-second timeout (the framing this section retained until Phase 36 D-14). Phase 29 D-08 upgraded to Vercel Pro and set `vercel.json functions."api/vercel-entry.js".maxDuration: 800` to accommodate worst-case LLM extraction runs (~10 minutes).
- Cold-start latency + upstream API latency together approaching the configured `maxDuration` on long-running paths. NIM throttle (Phase 30 / 34 measurements) is the dominant driver for `/api/cron/refresh-events`.
- Less commonly: Upstash command budget exhausted causing cache writes to fail silently; every request re-fetches upstream and accumulates latency.

**Remediation:**

1. For data routes (`/api/water`, `/api/sites`, etc.): retry. A warm cache responds in under 1 s.
2. For `/api/cron/refresh-events`: check `events:llm-summary:v3.lastError` for the failing provider (NIM circuit-breaker trip, OpenRouter dormancy, etc.). Cross-reference §13 NIM throttle handling.
3. If `maxDuration` was hit on extraction: the 90s `withBatchWatchdog` hard-kill (`server/lib/llmExtractorWatchdog.ts`) should have caught it before the function timeout; if it didn't, the watchdog is bypassed — check `LLM_BATCH_TIMEOUT_MS` env override.
4. Manually warm cache via `/api/cron/warm` (already a configured cron at 12:00 UTC); not required for incident response but accelerates recovery.

**Prevention:**

- `vercel.json functions."api/vercel-entry.js".maxDuration: 800` (Phase 29 D-08) — locked in as part of the Vercel Pro upgrade. Do NOT reduce without a compelling reason; the extraction path needs it.
- `Cache-Control: max-age=0, s-maxage=N` CDN headers serve most data-route requests from Vercel Edge without ever invoking the function.
- 2000 ms `REDIS_OP_TIMEOUT_MS` wrapper in [`server/cache/redis.ts`](../server/cache/redis.ts) caps hung Redis calls — leaves headroom for actual route logic.
- 90s `withBatchWatchdog` in [`server/lib/llmExtractorWatchdog.ts`](../server/lib/llmExtractorWatchdog.ts) hard-kills batch runs before they approach `maxDuration`; AbortController + generation counter prevents late-resolve cache clobber.
- Per-route timeout discipline: each upstream adapter wraps fetch with a fail-fast timeout (Overpass, Open-Meteo, NIM all bounded).
```

<!-- §13 content (NIM throttle handling). -->

```markdown
## 13. NIM throttle handling (429 burst + circuit-breaker trip)

**Symptom:** v3 LLM extraction runs failing with sustained 429s from NIM. `events:llm-dlq` accumulating entries with `reason: 'throttled'`. Map continues to render (raw GDELT via Pitfall 1 bridge), but new events lack v3 enrichment for the affected extraction window.

**Detection:**

- Pino logs with `module: 'llm/freeClaudeRouter'` and HTTP status `429` clusters within the daily 4:00 UTC extraction window.
- `events:llm-eval-baseline:v3` accuracy drops at the next `/api/cron/health` run (eval-drift signal).
- `server/lib/llmCircuitBreaker.ts` opens (sliding 10-call window, >30% error rate trips a 5-minute pause).
- `/api/operator-status` `byBearer.replay24h` shows no operator force-triggers (so the throttle is from cron-only writes, not operator activity).

**Cause:**

- NIM ratelimits at ~40 requests/minute per token (Phase 30 measurement). The tuned `LLM_V3_CONCURRENCY=12` default targets ~26 req/min headroom under the cap, but batch bursts during retry can exceed the cap momentarily.
- Per CONTEXT.md `<canonical_refs>`: see [`docs/architecture/llm-pipeline-reliability.md`](../architecture/llm-pipeline-reliability.md) §"Measured throttle window (Phase 30)" for the empirical numbers and Path B remediation framing.
- OpenRouter cascade fallback would normally absorb the 429 burst, but OpenRouter is DORMANT per [ADR-0010 Phase 30.1 sub-block](./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md). So NIM is the only path; throttle pressure has no relief valve.

**Remediation:**

1. Wait the 5-minute circuit-breaker cooldown. The watchdog will resume automatically on the next per-event invocation.
2. If circuit-breaker fails to close after 15 minutes: check `llm:tokens:nim:YYYY-MM-DD` for the daily token-budget counter. If hard cap (`budgetState: 'hard'`) was hit, the cascade bypasses NIM for the rest of the UTC day — operator can wait until next-day reset OR force-trigger via §15 with a fresh window.
3. Per Path B framing in `llm-pipeline-reliability.md`: if throttle is sustained > 1 hour, consider lowering `LLM_V3_CONCURRENCY` to 8 (env override) for the next deploy. Default is `12` for steady-state; `8` is a safer fallback under chronic throttle.
4. To resume manually: `GET /api/cron/refresh-events?force=true` with operator Bearer (see §15). Bypasses cooldown + self-heal.

**Prevention:**

- Tuned `LLM_V3_CONCURRENCY`, `LLM_BATCH_SIZE`, `LLM_BATCH_TIMEOUT_MS`, `BACKOFF_MS` defaults — see [`docs/architecture/llm-pipeline-reliability.md`](../architecture/llm-pipeline-reliability.md) "Tuned defaults reference" for the measured numbers.
- Circuit breaker (`server/lib/llmCircuitBreaker.ts`) — 5-min pause on >30% error rate in last 10 calls.
- Token-budget cap (`server/lib/llmTokenBudget.ts`) — soft 0.8 / hard 0.95 with 48h TTL.
- DLQ (`server/lib/llmDLQ.ts`) — bounded 200-entry / 7-day TTL captures failed extractions for retry triage.
- Watchdog (`server/lib/llmExtractorWatchdog.ts`) — 90s hard-kill prevents one stuck batch from blocking the whole extraction run.
- Pitfall 1 cache bridge (`server/routes/events.ts`) — terminal fallback to raw GDELT; map never goes blank during sustained throttle.
- Open item: re-enabling OpenRouter as a runtime fallback (currently DORMANT per Phase 30.1) would absorb throttle bursts. Decision deferred per ADR-0010 Phase 30.1; revisit if Pitfall 1 raw-GDELT fallback proves insufficient for operator quality bar.
```

<!-- §14 content (Cron architecture lessons). -->

```markdown
## 14. Cron architecture lessons (Phase 28.2.6 fire-and-forget IIFE incident)

**Symptom:** Pre-Phase-29: `events:llm:v3` not updating between deploys. v3 extraction appeared to start (logs showed initialization) but never completed. `events:llm-summary:v3` had `startedAt` but no `completedAt`. Post-Phase-29: this failure mode is architecturally prevented (cron-only writer invariant).

**Detection (historical):**

- Vercel function logs showed v3 extraction initialization on `/api/events` requests, then no completion log.
- `events:llm:v3` stale for >24 hours despite continuous `/api/events` traffic.
- `events:llm-summary:v3.lastError` empty (no error was caught — the IIFE was killed silently).

**Cause (historical Phase 28.2.6 diagnosis):**

- Pre-Phase-29 v2 pattern triggered LLM extraction via a fire-and-forget IIFE from `/api/events` cache miss: `(async () => { await processEvents(); })();` with HTTP response sent immediately.
- Vercel Fluid Compute SILENTLY KILLS function instances once the HTTP response is sent, even if async work is still in flight. The IIFE was being terminated mid-extraction with no error surfaced.
- Documented in [ADR-0010 Phase 29 Decision section](./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md).

**Remediation (architectural — Phase 29 removed this failure mode):**

- v1 + v2 LLM extractors DELETED Phase 29 (Plans 04-06). No fire-and-forget IIFE path exists in the codebase.
- v3 extraction moved to cron-only trigger (`/api/cron/refresh-events`, daily 4:00 UTC).
- `/api/events` became cache-only — it serves `events:llm:v3` if present OR raw GDELT via Pitfall 1 bridge. It does NOT initiate extraction.

**Prevention (the anti-pattern that protects the system):**

- **Anti-pattern #17 (cron-only writer discipline):** ONLY `/api/cron/refresh-events` writes to `events:llm:v3`. Any new code that proposes writing to v3 production state from a non-cron path is reverted at code review. CLAUDE.md §LLM Event Pipeline names this invariant explicitly.
- Cold-cache self-heal: `runRefreshExtraction()` checks `events:llm:v3` size; if empty, bypasses the 15-min `events:llm-process-ts` cooldown. Prevents the v3 cache from being permanently empty after a Redis flush.
- Watchdog hard-kill (90s; see §13 + §10).
- **Regression detection:** if a new code path is added that writes to `events:llm:v3` outside the cron handler, the next `/api/cron/refresh-events` run will race-clobber it; symptoms look like flaky enrichment quality. Detect via: `git log -p server/routes/ server/lib/llmEvent*` greping for new `cacheSet*('events:llm:v3'` callsites outside `server/lib/llmExtractionPipeline.ts`.
```

<!-- §15 content (Force-trigger). -->

```markdown
## 15. Force-trigger `/api/cron/refresh-events` (operator-only)

**Symptom:** Operator needs to refresh `events:llm:v3` outside the daily 4:00 UTC cron window — e.g., after a NIM throttle clearance, after a new event of operational interest landed in GDELT, or to verify a deploy fix.

**Detection:** N/A — this is operator-initiated, not a failure mode.

**Cause:** Operator decision. Common triggers:

- Operator confirmed via §13 that NIM throttle has cleared and wants to backfill the missed window.
- A high-significance event landed in GDELT during the gap between cron windows.
- Post-deploy verification of an extraction-path fix.

**Remediation (how to invoke):**

1. **Required env:** `DASHBOARD_PASSWORD` (operator Bearer).
2. **Invocation:** `curl -H "Authorization: Bearer $DASHBOARD_PASSWORD" "https://otg-iran-monitor.vercel.app/api/cron/refresh-events?force=true"`
3. **What `force=true` bypasses:**
   - The 15-minute `events:llm-process-ts` cooldown sentinel.
   - The empty-cache self-heal logic (which the cron path already bypasses on cold cache).
4. **What it does NOT bypass:**
   - Circuit-breaker state (if open, the cascade still pauses NIM).
   - Token-budget cap (if `budgetState: 'hard'`, the cascade still bypasses NIM for the rest of the UTC day).
   - 50/24h per-Bearer replay quota (separate quota; force-trigger is rate-limited per `operator:replay-quota:{fingerprint}:{YYYY-MM-DD}`).
5. **Expected response:** `{status: 'ran', startedAt: '...', completedAt: '...', summary: {...}}` or `{status: 'skipped', reason: 'cooldown_active'}` if Bearer-mismatch shielded the bypass.
6. **Audit trail:** Operator action appends a row to `operator:audit-log` (500-entry bounded set, 30d TTL).

**Prevention (when NOT to use force-trigger):**

- Routine daily refresh — the 4:00 UTC cron handles this; force-trigger wastes a quota slot.
- Suspected provider outage — first verify with §13; if NIM is truly down, force-triggering will just fail faster.
- Testing extraction logic in production — use a dev/preview deploy + dev `DASHBOARD_PASSWORD` instead.
```

<!-- §16 content (prod-connectivity-audit retry). -->

````markdown
## 16. `prod-connectivity-audit.yml` retry path (workflow_dispatch)

**Symptom:** `audit:connectivity:last-result` shows stale data (>24h old) OR `status: 'red'` OR `allTiersGreen: false` while the operator believes the system is actually healthy.

**Detection:**

- `/api/audit-status` returns `timestamp` more than 24 hours old.
- The API Health dashboard tab (Phase 28.2 W5 merge) shows the audit-result banner in degraded state.
- GitHub Actions tab for `otg-iran-monitor` shows the last `prod-connectivity-audit.yml` run as failed OR not run within the past 24 hours.

**Cause:**

- Scheduled GitHub Actions workflow `prod-connectivity-audit.yml` failed (transient network issue, GitHub Actions outage, expired GITHUB_TOKEN, etc.).
- Manual workflow_dispatch trigger not run after a known transient failure.

**Remediation:**

1. Open the GitHub Actions tab for `otg-iran-monitor`.
2. Select the `prod-connectivity-audit.yml` workflow.
3. Click "Run workflow" → workflow_dispatch trigger with default inputs.
4. Wait for completion (~2-5 minutes); confirm green run.
5. Verify `/api/audit-status` now returns fresh timestamp + `status: 'green'`.
6. **Expected payload shape** (the W-3 contract test pins this — see `server/__tests__/contract/audit-status-shape.test.ts`):
   ```json
   {
     "status": "green",
     "runId": "<gh-actions-run-id>",
     "timestamp": "<ISO-8601>",
     "endpoints": { "...": "..." },
     "durationMs": 1234,
     "allTiersGreen": true,
     "tierStatus": { "<tier>": { "status": "green", "evidence": {...}, "timestamp": "..." } }
   }
   ```

**Prevention:**

- v1.5 acceptance gate (LLM-RELI-07): **3× consecutive `allTiersGreen=true`** runs unblocks v1.6 milestone close (Phase 37 territory).
- Workflow runs on a schedule; manual workflow_dispatch is the operator-controlled retry path.
- Audit-result endpoint (`/api/audit-status`) is degrade-open: no auth gate, returns `status: 'unknown'` if Redis is unreachable rather than failing closed. Prevents the audit surface from becoming an additional incident vector.
````

<!-- ToC update (lines 21-32). -->

After §13-§16 are appended, update the ToC at line 19 by inserting these 4 entries between the existing §12 entry and the `## See also` reference:

```markdown
13. [NIM throttle handling](#13-nim-throttle-handling-429-burst--circuit-breaker-trip)
14. [Cron architecture lessons](#14-cron-architecture-lessons-phase-2826-fire-and-forget-iife-incident)
15. [Force-trigger /api/cron/refresh-events](#15-force-trigger-apicronrefresh-events-operator-only)
16. [prod-connectivity-audit.yml retry path](#16-prod-connectivity-auditymyl-retry-path-workflow_dispatch)
```

Also update §6 ToC entry from `[Vercel function timeout (10-second limit)]` to `[Vercel function timeout (300s default / 800s configured ceiling)]` — but KEEP the anchor link target as `#6-vercel-function-timeout-10-second-limit` so the anchor shim works.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite §6 (Vercel function timeout) + install old-anchor shim (D-14)</name>
  <files>docs/runbook.md</files>
  <read_first>
    docs/runbook.md (lines 351-406 — current §6 body; lines 19-32 — ToC entry for §6)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-14)
    CLAUDE.md (§Vercel Deployment — `vercel.json` `maxDuration: 800` + Pro plan facts)
    docs/architecture/llm-pipeline-reliability.md (full file — confirm 800s ceiling framing is consistent across docs)
    vercel.json (confirm `functions."api/vercel-entry.js".maxDuration: 800` is present)
  </read_first>
  <action>
    Use the Edit tool to perform TWO edits in `docs/runbook.md`:

    **EDIT 1 — Replace §6 body (lines 351-406 inclusive):**

    Replace the existing `## 6. Vercel function timeout (10-second limit)` heading and its body (down to the `---` separator before §7) with the new §6 body verbatim from `<interfaces>` "§6 rewrite content" block. The new content INCLUDES the `<a id="6-vercel-function-timeout-10-second-limit"></a>` anchor shim as the FIRST line, immediately above the new `## 6. Vercel function timeout (300s default / 800s configured ceiling)` heading.

    Anchor shim mechanics: when an external link points to `docs/runbook.md#6-vercel-function-timeout-10-second-limit`, the browser scrolls to the `<a id="...">` shim and then the new heading is immediately visible below. The new heading generates its own anchor (`#6-vercel-function-timeout-300s-default-800s-configured-ceiling`) which is what the updated ToC entry uses (see Task 2). Both anchors coexist.

    **EDIT 2 — Update ToC entry for §6 (line 26):**

    Find the existing ToC line: `6. [Vercel function timeout (10-second limit)](#6-vercel-function-timeout-10-second-limit)`

    Replace with: `6. [Vercel function timeout (300s default / 800s configured ceiling)](#6-vercel-function-timeout-10-second-limit)`

    **Critical:** The DISPLAY TEXT changes; the anchor link target STAYS `#6-vercel-function-timeout-10-second-limit` (so the old-anchor shim is the active landing point, ensuring old external links continue to land in the right place). This is the intentional D-14 design.

    Run `grep -nE "^## 6\. Vercel|^<a id=\"6-vercel-function-timeout-10-second-limit\"|^6\. \[Vercel function" docs/runbook.md` to verify the 3 markers are present.

    Commit (atomic per D-27): `docs(36): D-14 rewrite runbook §6 (Vercel function timeout) for Pro plan 800s ceiling; preserve old anchor shim` body cites the old vs new heading text + the anchor-shim invariant.

    DO NOT touch §1-§5, §7-§12 in this task. Those are Task-2 territory (§13-§16 append) or untouched.

  </action>
  <verify>
    <automated>grep -n '^## 6\. Vercel function timeout (300s default / 800s configured ceiling)' docs/runbook.md && grep -n '<a id="6-vercel-function-timeout-10-second-limit"></a>' docs/runbook.md && grep -nF '6. [Vercel function timeout (300s default / 800s configured ceiling)](#6-vercel-function-timeout-10-second-limit)' docs/runbook.md && ! grep -n '^## 6\. Vercel function timeout (10-second limit)' docs/runbook.md && grep -q "maxDuration: 800" docs/runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbook.md` contains `^## 6\. Vercel function timeout (300s default / 800s configured ceiling)` exactly once.
    - `docs/runbook.md` contains `<a id="6-vercel-function-timeout-10-second-limit"></a>` exactly once (immediately above the new §6 heading).
    - `docs/runbook.md` ToC line for §6 displays "(300s default / 800s configured ceiling)" but still links to `#6-vercel-function-timeout-10-second-limit`.
    - `docs/runbook.md` does NOT contain `^## 6\. Vercel function timeout (10-second limit)` (old heading removed).
    - `docs/runbook.md` contains the string `maxDuration: 800` (Phase 29 D-08 ceiling reference).
    - `docs/runbook.md` §6 body references the `withBatchWatchdog` 90s hard-kill (cross-link to §13 cause framing).
    - `git log -1 --format=%s docs/runbook.md` matches `docs(36):` with `D-14` in body.
  </acceptance_criteria>
  <done>
    §6 reflects Pro plan 800s ceiling reality. Old anchor preserved via shim so external links continue to resolve. ToC display text updated but anchor link unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append §13-§16 SRE sections + update ToC (D-13)</name>
  <files>docs/runbook.md</files>
  <read_first>
    docs/runbook.md (post-Task-1 state — lines 19-32 ToC for §6 update verification; line 870 §12 start; line 886 §See also footer)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-13)
    docs/architecture/llm-pipeline-reliability.md (Multi-Provider Cascade Phase 34 section — §13 cross-link target)
    docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (Phase 29 Decision section — §14 cross-link target)
    CLAUDE.md (§LLM Event Pipeline + §Active Redis keys — context for §13/§15 detail)
  </read_first>
  <action>
    Use the Edit tool to append §13-§16 and update the ToC. THREE edits total:

    **EDIT 1 — Append §13-§16 (insertion point: BEFORE the `## See also` heading at line ~886):**

    Find the existing `## See also` heading (currently the last `## ` heading in the file). Insert the FOUR new SRE-template sections immediately above it, in order §13, §14, §15, §16. Each section follows the verbatim content from `<interfaces>` "§13 content" / "§14 content" / "§15 content" / "§16 content" blocks. Use a `---` separator BETWEEN each new section AND a `---` separator BEFORE the existing `## See also` heading.

    Total appended content: ~200 lines (50 lines per section × 4 sections).

    **EDIT 2 — Update ToC (lines ~21-32):**

    Find the existing ToC block (numbered list under `## Table of contents`). Insert these 4 entries between the existing §12 entry (`12. [Common log query patterns]`) and any subsequent ToC entries. Per `<interfaces>` "ToC update" block:

    ```
    13. [NIM throttle handling](#13-nim-throttle-handling-429-burst--circuit-breaker-trip)
    14. [Cron architecture lessons](#14-cron-architecture-lessons-phase-2826-fire-and-forget-iife-incident)
    15. [Force-trigger /api/cron/refresh-events](#15-force-trigger-apicronrefresh-events-operator-only)
    16. [prod-connectivity-audit.yml retry path](#16-prod-connectivity-auditymyl-retry-path-workflow_dispatch)
    ```

    **Note:** GitHub auto-generates Mermaid-style anchor IDs by lowercasing the heading, replacing spaces with `-`, removing punctuation. The anchors above match the new headings. After insertion, verify by clicking each ToC entry in GitHub preview (or run `grep -n "^## 13\\.|^## 14\\.|^## 15\\.|^## 16\\." docs/runbook.md` to confirm heading presence, then trust GitHub anchor generation).

    **EDIT 3 — Spot-check cross-links resolve:**

    Run `grep -n "llm-pipeline-reliability.md\|0010-v1-5-llm-pipeline-narrowing" docs/runbook.md` and verify each cross-link target path is correct relative to `docs/runbook.md` location. Common error: forgetting `./adr/` prefix when linking from `docs/runbook.md` to `docs/adr/0010-*.md`. Correct form is `./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`. Cross-link to `llm-pipeline-reliability.md` is `./architecture/llm-pipeline-reliability.md`. Fix any incorrect relative paths.

    Commit (atomic per D-27): `docs(36): D-13 add runbook §13-§16 (NIM throttle, cron architecture, force-trigger, prod-audit retry) + ToC update` body lists the 4 section titles.

    After both Tasks 1 + 2 complete, the runbook should pass `npm run docs:lint` (markdown-link-check). If any cross-link is broken, Plan 36-06's D-24 verification will catch it; fix it inline if surfaced before then.

  </action>
  <verify>
    <automated>grep -q "^## 13\. NIM throttle handling" docs/runbook.md && grep -q "^## 14\. Cron architecture lessons" docs/runbook.md && grep -q "^## 15\. Force-trigger" docs/runbook.md && grep -q "^## 16\. " docs/runbook.md && grep -qF "13. [NIM throttle handling]" docs/runbook.md && grep -qF "14. [Cron architecture lessons]" docs/runbook.md && grep -qF "15. [Force-trigger" docs/runbook.md && grep -q "llm-pipeline-reliability.md" docs/runbook.md && grep -q "0010-v1-5-llm-pipeline-narrowing-and-deletion.md" docs/runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbook.md` contains all 4 new section headings: `## 13. NIM throttle handling`, `## 14. Cron architecture lessons`, `## 15. Force-trigger`, `## 16. prod-connectivity-audit.yml`.
    - ToC entries 13-16 land between the existing §12 ToC entry and the `## See also` reference.
    - §13 contains an inline link to `docs/architecture/llm-pipeline-reliability.md` (Claude-Discretion 5 — cross-link, do not paraphrase Path B framing).
    - §14 contains an inline link to `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md`.
    - §15 references both CRON_SECRET and DASHBOARD_PASSWORD as accepted Bearers AND the `?force=true` query param.
    - §16 references `audit:connectivity:last-result` Redis key + the `allTiersGreen` field per Phase 28.2 W6.
    - All sections follow the SRE template (Symptom / Detection / Cause / Remediation / Prevention subheadings present in each).
    - `git log -1 --format=%s docs/runbook.md` matches `docs(36):` with `D-13` in body.
    - `wc -l docs/runbook.md` shows the file grew by ~200 lines (≈ from 676 to ≈ 876, allowing ±20 lines for formatting variance).
  </acceptance_criteria>
  <done>
    Runbook has 4 new SRE-template sections covering v1.4 + v1.5 incident playbooks. ToC reflects new sections. Cross-links to architecture / ADR resolve cleanly.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                 |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Runbook author → docs/runbook.md      | Hand-authored markdown edits; no input validation; mitigated by markdown-link-check gate in Plan 36-06 D-24 |
| Anchor shim → external link consumers | Old `#6-vercel-function-timeout-10-second-limit` anchor preserved; verified by manual scroll test           |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                         | Disposition | Mitigation Plan                                                                                                                                                                                               |
| ---------- | ---------------------- | ----------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-03-01 | Information Disclosure | §15 publishes operator force-trigger curl command + Bearer header | accept      | The mechanism (Bearer-gated POST) is already public via OpenAPI spec post-Plan-36-05. Documenting it in the runbook duplicates known information; secret value (DASHBOARD_PASSWORD) is never exposed in docs. |
| T-36-03-02 | Tampering              | Anchor shim breakage                                              | mitigate    | The `<a id="...">` shim is HTML; markdown-link-check treats it as a fragment target. Verify manually after Task 1 commit + via D-24 markdown-link-check run.                                                  |
| T-36-03-03 | Spoofing               | §16 documents the workflow_dispatch retry mechanism               | accept      | GitHub Actions workflow_dispatch is gated by repo permissions; documenting its existence does not change who can invoke it.                                                                                   |
| T-36-03-SC | Tampering              | No npm installs in this plan                                      | n/a         | Pure docs/runbook.md edits; @redocly + markdown-link-check were installed in Plan 36-05 Task 1.                                                                                                               |

</threat_model>

<verification>
- `npx vitest run` exits 0 (no source/test code touched).
- `npx tsc --noEmit` exits 0.
- After Plan 36-05 lands the `docs:lint` script + Plan 36-06 D-24 runs it, `npm run docs:lint` exits 0 for `docs/runbook.md` (no broken cross-links).
- Manual review: open `docs/runbook.md` in a markdown previewer (or GitHub web view); confirm §6 anchor shim renders invisibly above new heading; confirm §13-§16 follow SRE template; confirm ToC entries 13-16 navigate to correct sections.
</verification>

<success_criteria>

1. §6 retitled to "(300s default / 800s configured ceiling)"; body rewritten; old anchor preserved via `<a id="..."></a>` shim.
2. §13 NIM throttle handling appended with cross-link to `docs/architecture/llm-pipeline-reliability.md`.
3. §14 Cron architecture lessons appended with cross-link to ADR-0010.
4. §15 Force-trigger runbook appended with curl invocation + Bearer mechanics + audit-log mention.
5. §16 prod-connectivity-audit retry appended with workflow_dispatch + `allTiersGreen` shape.
6. ToC updated to list §13-§16 with anchor links.
7. Atomic per-decision commits (1 for §6 / D-14, 1 for §13-§16 + ToC / D-13).
8. Coverage: DOCS-PUB-03 addressed.

</success_criteria>

<output>
Create `.planning/phases/36-public-docs-sweep-openapi-additions/36-03-SUMMARY.md` when done. Include:
- §6 rewrite — old heading + new heading + anchor-shim line.
- §13-§16 — line ranges in the final runbook.md (e.g., "§13 lines 890-940").
- Line count delta (before / after).
- Cross-link inventory: every `[...](...)` link added in §13-§16 with target verification (resolves / broken-pending-fix).
- Commit hashes for the 2 commits (D-14 §6 rewrite, D-13 §13-§16 + ToC).
- Any cross-link fixes surfaced beyond the planned scope.
</output>

---
phase: 36-public-docs-sweep-openapi-additions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/degradation.md
autonomous: true
requirements: [DOCS-PUB-05]
tags: [degradation, pitfall-1, fallback-chain, contract, v3-only]
must_haves:
  truths:
    - 'Top-of-file Pitfall 1 contract statement added (or refined if a similar statement exists): "The v3 LLM pipeline is optional. When `events:llm:v3` is empty or stale, `/api/events` serves raw GDELT via the Pitfall 1 cache bridge in `server/routes/events.ts`. The map never goes blank — only enrichment quality degrades from v3 (CAMEO + LLM resolver) to raw GDELT (CAMEO classification only). This contract is invariant and proven by `server/__tests__/resilience/redis-death.test.ts`." (D-15)'
    - 'Fallback chain documented as **v3 → raw GDELT** (no v1, no v2) per D-03 / D-16. Any pre-existing prose suggesting "v3 → v2 → v1 → raw GDELT" or "narrowed cascade with three pipeline versions" updated to reflect Phase 29 deletion of v1/v2 extractors.'
    - 'Explicit acknowledgement of Phase 29 deletion: "v1 and v2 extractors were deleted Phase 29 (see [ADR-0010](./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md)); no fallback to a prior pipeline version exists. The terminal fallback is raw GDELT — the same data source the LLM enriches when healthy." (D-16)'
    - 'Cross-link to `server/__tests__/resilience/redis-death.test.ts` cites the proof that the chain works under Redis death — the contract is mechanically verified, not aspirational.'
    - 'Atomic commit: `docs(36): D-15 D-16 update degradation.md — Pitfall 1 contract + v3 → raw GDELT chain` body cites both decisions.'
    - 'No changes outside `docs/degradation.md`.'
    - 'If the existing "Cache Layer: Upstash Redis" section (lines 22-72) already describes Pitfall 1 behavior, refine in place rather than duplicate at top of file. Per Claude-Discretion: minimal-change principle.'
  artifacts:
    - path: 'docs/degradation.md'
      provides: 'Updated Pitfall 1 contract statement + v3 → raw GDELT chain documentation (no v1/v2 references)'
      contains: 'v3 → raw GDELT'
  key_links:
    - from: 'docs/degradation.md Pitfall 1 contract statement'
      to: 'server/__tests__/resilience/redis-death.test.ts'
      via: 'inline citation of the test as proof of contract'
      pattern: 'redis-death\\.test\\.ts'
    - from: 'docs/degradation.md v3-only chain documentation'
      to: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md'
      via: 'inline markdown link explaining Phase 29 v1/v2 deletion'
      pattern: '0010-v1-5-llm-pipeline-narrowing-and-deletion'
---

<objective>
Update `docs/degradation.md` to reflect the post-Phase-29 reality: the Pitfall 1 cache bridge is the terminal contract (map never goes blank), and the fallback chain is **v3 → raw GDELT** (no v1, no v2 — both extractors deleted Phase 29). Any documented chain that includes "v3 → v2 → v1 → raw GDELT" is stale.

Purpose: Operators reading degradation.md must trust it as the contract the app makes — "what happens when X fails." Stale chain language (v3 → v2 → v1 → raw GDELT) suggests fallback paths that don't exist in the codebase, leading to wrong expectations during incident response. CONTEXT.md D-15 / D-16 explicitly fix this.

Output: 1-2 atomic commits per D-26 / D-27: docs (Pitfall 1 contract refinement) → docs (v3 → raw GDELT chain) — OR a single rollup commit if the edits are small and cohesive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md
@CLAUDE.md
@docs/degradation.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@server/__tests__/resilience/redis-death.test.ts
@server/routes/events.ts

<interfaces>
<!-- degradation.md current structure (verified 2026-05-29). -->

- Line 1: `# Graceful Degradation Contract` (file title)
- Lines 1-21: header / intro paragraph mentioning `server/__tests__/resilience/redis-death.test.ts` already
- Line 22: `## Cache Layer: Upstash Redis` (current Pitfall 1-adjacent section)
- Line 73: `## Data Source Layer: 8 upstream APIs`
- Line 119: `## Response Layer: Zod validation (Plan 26.4-03)`
- Line 162: `## Frontend Layer: Lost Contact`
- Line 195: `## Rate Limit Layer`
- Line 239: `## `/health` Endpoint Contract`
- Line 282: `## Summary Table`
- Total: 303 lines

<!-- Drift signals to fix (D-15, D-16). -->

- Any prose mentioning "v3 → v2 → v1 → raw GDELT" or "three LLM pipeline versions" (stale — Phase 29 deleted v1+v2)
- Any prose mentioning `llmEventExtractor.v1` or `llmEventExtractor.v2` module references (deleted)
- Any prose claiming a fallback "to v2" or "to v1" when v3 fails (no such fallback exists)
- Any prose framing the Pitfall 1 bridge as conditional / future (it's load-bearing TODAY)

<!-- Desired top-of-file (or appropriate location) contract statement (D-15). -->

If `docs/degradation.md` does NOT already contain an explicit Pitfall 1 contract statement at top-of-file, add this paragraph IMMEDIATELY AFTER the line `## Cache Layer: Upstash Redis` (line 22) — OR if a top-of-file "Pitfall 1 Contract" heading makes more sense for navigability, add it as a new `## Pitfall 1: "Map never goes blank" Contract` section inserted between the intro (line 21) and the existing `## Cache Layer` section.

Per Claude-Discretion (minimal-change principle): prefer **refining the existing `## Cache Layer: Upstash Redis` section** rather than adding a new top-level section — the Pitfall 1 contract is conceptually part of the cache layer's degraded-behavior narrative.

````markdown
**Pitfall 1 contract — the "map never goes blank" invariant:**

The v3 LLM pipeline is optional. When `events:llm:v3` is empty or stale, `/api/events` serves raw GDELT via the Pitfall 1 cache bridge in [`server/routes/events.ts`](../server/routes/events.ts). The map never goes blank — only enrichment quality degrades from v3 (CAMEO classification + LLM-resolved precise coordinates) to raw GDELT (CAMEO classification only, ACTIONGEO coordinates).

This contract is **invariant** and proven by [`server/__tests__/resilience/redis-death.test.ts`](../server/__tests__/resilience/redis-death.test.ts). When the test kills the Upstash Redis connection mid-request, the system continues to serve raw GDELT events; the map renders with degraded enrichment but is never blank.

**v1 and v2 LLM extractors were deleted in Phase 29** (see [ADR-0010](./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md)). No fallback to a prior pipeline version exists in the codebase. The terminal fallback is raw GDELT — the same data source the LLM enriches when healthy. The full chain documented in this contract is:

```text
v3 (cron-driven extraction, daily 4:00 UTC) → raw GDELT (Pitfall 1 cache bridge terminal fallback)
```
````

Anyone reading older planning artifacts (ROADMAP.md success criterion #4, etc.) may encounter the framing "v3 → v2 → v1 → raw GDELT" — that wording predates Phase 29 deletion and is preserved verbatim in the planning text per a phase-36-discussion decision (planning artifacts are historical brief; public docs describe shipped reality).

````

<!-- Verification: degradation invariants this plan asserts. -->

```bash
# v3 → raw GDELT chain documented
grep -E "v3 .*raw GDELT|raw GDELT.*Pitfall 1" docs/degradation.md

# Phase 29 deletion explicitly acknowledged
grep -E "Phase 29|v1 and v2.*deleted" docs/degradation.md

# Cross-link to redis-death test resolves
grep "redis-death.test.ts" docs/degradation.md

# No stale v3 → v2 → v1 → raw GDELT chain
! grep -E "v3 → v2|v2 → v1|three.*pipeline versions" docs/degradation.md
````

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine Cache Layer section with Pitfall 1 contract + v3-only chain (D-15, D-16)</name>
  <files>docs/degradation.md</files>
  <read_first>
    docs/degradation.md (full file — 303 lines; understand current Cache Layer section structure before editing)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-03, D-15, D-16)
    server/routes/events.ts (lines around 660-700 — Pitfall 1 cache bridge implementation; cite the actual code path)
    server/__tests__/resilience/redis-death.test.ts (full file — understand what behavior the test pins; the contract statement names this as proof)
    docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (Phase 29 Decision section — narrative source for the "v1/v2 deleted" framing)
  </read_first>
  <action>
    **STEP A — Drift detection:** Run the drift greps from `<interfaces>` "Verification" block against the CURRENT `docs/degradation.md`. Specifically:
    ```bash
    grep -nE "v3 → v2|v2 → v1|three.*pipeline versions|llmEventExtractor\\.v[12]" docs/degradation.md
    ```
    Record line numbers of every hit. These are the surgical-edit targets.

    **STEP B — Locate the Cache Layer section.** Find `## Cache Layer: Upstash Redis` (line 22). Read the entire section through to the next `## ` heading (line 73 `## Data Source Layer`).

    **STEP C — Choose insertion approach (minimal-change principle):**
    - If the Cache Layer section ALREADY contains a Pitfall 1 mention: REFINE in place — strengthen the language to match the verbatim contract statement from `<interfaces>`; ensure the v1/v2-deletion paragraph is present; ensure cross-link to `redis-death.test.ts` is present.
    - If the Cache Layer section does NOT mention Pitfall 1: APPEND the verbatim contract statement at the end of the Cache Layer section (just before the next `## ` heading).
    - If a new top-level section makes more navigability sense (operator preference): insert a `## Pitfall 1: "Map never goes blank" Contract` section between the intro (line 21) and Cache Layer (line 22). Per Claude-Discretion default: prefer in-place refinement of Cache Layer to avoid section proliferation.

    **STEP D — Surgical edits per drift hit:** For each hit from STEP A, use the Edit tool to fix the stale phrasing in place. Common fixes:
    - "v3 → v2 → v1 → raw GDELT" → "v3 → raw GDELT (no v1, no v2 — both deleted Phase 29; see [ADR-0010](./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md))"
    - "narrowed cascade with three pipeline versions" → "v3 cron-driven extraction as the only LLM pipeline (v1 and v2 deleted Phase 29)"
    - Any `llmEventExtractor.v1` / `llmEventExtractor.v2` module reference → contextualize as historical OR remove the reference if non-essential

    **STEP E — Cross-link verification:**
    - Confirm the inline link to `server/__tests__/resilience/redis-death.test.ts` resolves (relative path: `../server/__tests__/resilience/redis-death.test.ts` from `docs/degradation.md`).
    - Confirm the inline link to `docs/adr/0010-...md` resolves (relative path: `./adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` from `docs/degradation.md`).
    - Confirm the inline link to `server/routes/events.ts` resolves (relative path: `../server/routes/events.ts`).

    **STEP F — Sanity check the Summary Table.** Read lines 282-303 (Summary Table section). If the table has a row for "LLM pipeline" or "v3 enrichment" that mentions v1/v2 framing, update it to the v3-only chain. If not, leave the Summary Table as-is.

    **STEP G — Commit.** Options:
    - **Single commit** (preferred for cohesive edits): `docs(36): D-15 D-16 refine degradation.md Cache Layer with Pitfall 1 contract + v3-only chain` body cites both decisions + lists the line-range of the edits.
    - **Two commits** (if edits are clearly separable): `docs(36): D-15 add Pitfall 1 contract statement to degradation.md Cache Layer` and `docs(36): D-16 update degradation.md fallback chain to v3 → raw GDELT (v1/v2 deleted Phase 29)`.

    Per CONTEXT.md D-27 atomic-per-decision discipline: if D-15 and D-16 edits touch different paragraphs, two commits is correct; if they touch the same paragraph (same insertion of the verbatim contract statement), one commit is fine.

    NO changes outside `docs/degradation.md` (no edits to events.ts, redis-death.test.ts, etc.).

  </action>
  <verify>
    <automated>grep -q "v3 .*raw GDELT" docs/degradation.md && grep -q "Phase 29" docs/degradation.md && grep -q "redis-death.test.ts" docs/degradation.md && grep -q "0010-v1-5-llm-pipeline-narrowing-and-deletion.md" docs/degradation.md && ! grep -qE "v3 .* v2 .* v1|three.*pipeline versions" docs/degradation.md && ! grep -qE "llmEventExtractor\.v[12]" docs/degradation.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/degradation.md` contains the v3-only chain wording (e.g., "v3 → raw GDELT" OR "v3 (cron-driven extraction) → raw GDELT").
    - `docs/degradation.md` explicitly acknowledges Phase 29 deletion of v1 + v2.
    - `docs/degradation.md` cross-links to `server/__tests__/resilience/redis-death.test.ts` as proof of contract.
    - `docs/degradation.md` cross-links to `docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md` for v1/v2-deletion rationale.
    - `docs/degradation.md` cross-links to `server/routes/events.ts` for the Pitfall 1 bridge implementation.
    - `docs/degradation.md` contains NO occurrences of "v3 → v2 → v1 → raw GDELT" or "three pipeline versions" or live `llmEventExtractor.v1` / `llmEventExtractor.v2` references.
    - `docs/degradation.md` Cache Layer section reads coherently — the new contract statement integrates naturally with the existing degraded-behavior narrative.
    - `git log -1 --format=%s docs/degradation.md` matches `docs(36):` with `D-15` AND/OR `D-16` in body.
    - File size grew by at most 30-40 lines (the verbatim contract statement is ~15 lines; total delta should be ≤ 40 lines).
  </acceptance_criteria>
  <done>
    Pitfall 1 contract documented as the "map never goes blank" invariant. Fallback chain reflects v3-only reality. No stale v1/v2 references. Cross-links to the test that proves the contract.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                 |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Author → docs/degradation.md | Hand-authored markdown edits; no input validation; mitigated by markdown-link-check gate in Plan 36-06 D-24 |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                            | Disposition | Mitigation Plan                                                                                                                  |
| ---------- | ---------------------- | -------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-36-04-01 | Information Disclosure | docs/degradation.md publishes failure-mode behavior + recovery paths | accept      | Failure-mode contracts are the document's purpose; not a secret. Public knowledge of degraded states does not weaken the system. |
| T-36-04-02 | Tampering              | Cross-link target paths (relative)                                   | mitigate    | Verified during edit + confirmed by D-24 markdown-link-check run in Plan 36-06.                                                  |
| T-36-04-SC | Tampering              | No npm installs in this plan                                         | n/a         | Pure docs edit.                                                                                                                  |

</threat_model>

<verification>
- `npx vitest run` exits 0 (no source/test code touched).
- `npm run docs:lint` (after Plan 36-05 + Plan 36-06 D-24): no broken links in degradation.md.
- Manual review: open `docs/degradation.md` in a markdown previewer; verify the Pitfall 1 contract statement reads as the contract (declarative, not conditional); verify cross-links navigate correctly.
- Goal-backward sanity: an operator reading degradation.md alone (no prior context) understands that (a) when v3 is empty, the map still works via raw GDELT; (b) v1 and v2 don't exist anymore; (c) the test that proves the contract is named and resolvable.
</verification>

<success_criteria>

1. Pitfall 1 contract statement integrated into Cache Layer section (or new top-level section per operator preference).
2. v3 → raw GDELT chain documented; no v1/v2 fallback references.
3. Phase 29 deletion of v1/v2 explicitly acknowledged with ADR-0010 cross-link.
4. Cross-link to `redis-death.test.ts` as contract proof.
5. 1-2 atomic commits with `D-15` and/or `D-16` in body.
6. Coverage: DOCS-PUB-05 addressed.

</success_criteria>

<output>
Create `.planning/phases/36-public-docs-sweep-openapi-additions/36-04-SUMMARY.md` when done. Include:
- Insertion approach chosen (in-place refinement of Cache Layer / new top-level section / split across sections).
- Line range of the new contract statement.
- Drift hits remediated (line numbers + before/after snippets).
- Commit hashes.
- Cross-link inventory: 3 new inline links + their resolution status.
</output>

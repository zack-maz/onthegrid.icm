---
phase: 36-public-docs-sweep-openapi-additions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/README.md
  - docs/architecture/system-context.md
  - docs/architecture/data-flows.md
  - docs/architecture/deployment.md
  - docs/architecture/frontend.md
  - docs/architecture/llm-pipeline-reliability.md
  - docs/architecture/redis-keys.md
  - docs/architecture/ontology/README.md
  - docs/architecture/ontology/types.md
  - docs/architecture/ontology/algorithms.md
  - docs/architecture/ontology/complexity.md
  - docs/architecture/ontology/state-machines.md
  - docs/adr/0011-v3-llm-pipeline-architecture.md
  - .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md
autonomous: true
requirements: [DOCS-PUB-02]
tags: [architecture, mermaid, audit-table, adr, sub-block, v1.5-drift]
must_haves:
  truths:
    - 'Every one of the 12 markdown files in `docs/architecture/` + `docs/architecture/ontology/` is either edited (drift fixed) OR explicitly recorded as verified-clean in the per-file audit accumulator at `36-02-AUDIT.md` (D-09, D-12).'
    - 'Every one of the 21 Mermaid blocks (9 in data-flows.md, 3 in frontend.md, 4 in state-machines.md, 2 in deployment.md, 2 in system-context.md, 1 in types.md — actual count, not CONTEXT.md D-11 "22") is either edited (drift fixed, with GitHub-preview verification) OR recorded as verified-clean (D-10, D-12).'
    - 'Files most likely to need edits per CONTEXT.md D-09: `system-context.md`, `data-flows.md`, `deployment.md`, `frontend.md`, `ontology/state-machines.md`. Edit only if drift is found — DO NOT touch verified-clean files.'
    - 'Drift signals to scan for: v1/v2 LLM module references (deleted Phase 29), Cerebras/Groq nodes (deferred Phase 34), pre-rename domain `iran-conflict-monitor.vercel.app` (current is `otg-iran-monitor.vercel.app`), pre-Phase-29 single-Bearer auth framing, pre-Phase-29 fire-and-forget IIFE cron pattern, pre-Phase-29 `maxDuration: 60` framing (now 800), pre-Phase-35 missing-keys gap in any redis-keys references.'
    - '`docs/adr/0011-v3-llm-pipeline-architecture.md` gains a `## Phase 36 Sub-block (appended {YYYY-MM-DD})` block matching ADR-0010 Phase 30/30.1/34/35 sub-block format (D-21).'
    - 'ADR-0011 sub-block documents that the v3 cascade as shipped is NIM-only at runtime; cross-links to ADR-0010 Phase 30.1 (OpenRouter dormant) + Phase 34 (Cerebras/Groq deferred) sub-blocks.'
    - 'Atomic per-decision commits — one commit per edited file with `docs(36): D-09 sweep <filename>` body OR a per-Mermaid-block commit; one separate `docs(36): D-21 append Phase 36 sub-block to ADR-0011` commit.'
    - 'Pre-existing Phase 30 / 30.1 / 34 / 35 framing in `llm-pipeline-reliability.md` and Phase 35 framing in `redis-keys.md` are honored as verified-clean (D-09 explicitly notes both as already current).'
    - 'No edits to ROADMAP.md / REQUIREMENTS.md / PROJECT.md to fix file-count or diagram-count framing (D-04 + D-11) — SUMMARY.md callouts only.'
  artifacts:
    - path: '.planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md'
      provides: 'Per-file + per-Mermaid-block audit accumulator: rows feed Plan 36-06 SUMMARY.md audit table (D-12)'
      contains: 'File | Status | Drift Found | Commit Ref'
    - path: 'docs/adr/0011-v3-llm-pipeline-architecture.md'
      provides: 'ADR-0011 Phase 36 sub-block appended (D-21)'
      contains: 'Phase 36 Sub-block'
  key_links:
    - from: '.planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md'
      to: '12 architecture markdown files + 21 Mermaid blocks'
      via: 'Per-file row + per-Mermaid-block row with commit ref'
      pattern: 'verified-clean|edited'
    - from: 'docs/adr/0011-v3-llm-pipeline-architecture.md Phase 36 sub-block'
      to: 'docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md Phase 30.1 + Phase 34 sub-blocks'
      via: 'inline markdown links'
      pattern: '0010-v1-5-llm-pipeline-narrowing-and-deletion\\.md#phase-3[04]'
---

<objective>
Targeted v1.5-drift sweep across all 12 architecture markdown files + 21 Mermaid blocks. Per CONTEXT.md D-09: every file is *audited*; only files with surfaced drift are *edited*. The audit table fed to Plan 36-06 SUMMARY is the trust artifact — reviewer can trace exactly what was looked at and what the outcome was.

Purpose: Architecture docs are the second-most-read public surface after README. Drift here misleads future operators about shipped reality (NIM-only runtime, v3 cron-driven extraction, Pitfall 1 bridge, 800s function ceiling). Phase 35 already shipped `llm-pipeline-reliability.md` and `redis-keys.md` current — this sweep verifies their currency and ensures the other 10 files describe the same shipped reality.

Output: Per-file commits (atomic per D-27), one rollup ADR-0011 sub-block commit, one `36-02-AUDIT.md` accumulator file that Plan 36-06 reads when authoring the SUMMARY audit table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md
@CLAUDE.md
@docs/architecture/llm-pipeline-reliability.md
@docs/architecture/redis-keys.md
@docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md
@docs/adr/0011-v3-llm-pipeline-architecture.md

<interfaces>
<!-- File inventory (verified 2026-05-29). -->

12 markdown files in scope:

1. `docs/architecture/README.md` (0 Mermaid) — architecture index; verify file count + diagram count + cross-links
2. `docs/architecture/system-context.md` (2 Mermaid) — likely needs domain rename + v3 cascade update
3. `docs/architecture/data-flows.md` (9 Mermaid) — most-likely-drift candidate; verify v3 cron-driven extraction + Pitfall 1 bridge in every diagram
4. `docs/architecture/deployment.md` (2 Mermaid) — likely needs `maxDuration: 800` + Pro plan update
5. `docs/architecture/frontend.md` (3 Mermaid) — may be verified-clean (frontend unchanged v1.4 → v1.5)
6. `docs/architecture/llm-pipeline-reliability.md` (0 Mermaid) — **CONTEXT.md D-09 declares verified-clean** (Phase 30 + 30.1 + 34 sub-blocks current). Skim only — no edits expected.
7. `docs/architecture/redis-keys.md` (0 Mermaid) — **CONTEXT.md D-09 declares verified-clean** (Phase 35 deep-dive). Skim only.
8. `docs/architecture/ontology/README.md` (0 Mermaid) — likely verified-clean
9. `docs/architecture/ontology/types.md` (1 Mermaid) — verify against current entity/event types (5-type ConflictEventType ontology)
10. `docs/architecture/ontology/algorithms.md` (0 Mermaid) — likely verified-clean
11. `docs/architecture/ontology/complexity.md` (0 Mermaid) — likely verified-clean
12. `docs/architecture/ontology/state-machines.md` (4 Mermaid) — likely needs state-machine updates if any flight/event states changed v1.4 → v1.5

21 Mermaid blocks total (NOT 22 as CONTEXT.md D-11 stated — actual grep count is 21; SUMMARY callout).

<!-- ADR sub-block template (D-21). -->

ADR-0010 sub-blocks at lines 52, 82, 108, 133 follow this verbatim shape:

```markdown
## Phase {NN} Sub-block (appended {YYYY-MM-DD})

**Context:** [Why this sub-block is being added — typically a phase close that affected the ADR's scope.]

**Outcome:** [What changed about the original decision — superseded? Partially superseded? Reaffirmed?]

**Cross-references:**

- [Link to other ADR sub-blocks or phase summaries]
```

ADR-0011 currently has no sub-blocks (single coherent ADR document). D-21 adds the first sub-block following the ADR-0010 convention. Sub-block goes BEFORE the `## Consequences` section (mirrors ADR-0010 sub-block placement at line 133 before line 152 `## Consequences`).

<!-- Drift detection patterns — run these greps inside each file. -->

```bash
# v1/v2 module references (deleted Phase 29)
grep -nE "(llmEventExtractor\.v1|llmEventExtractor\.v2|v1.*v2.*v3.*raw GDELT|v3.*v2.*v1)" <file>

# Cerebras/Groq references (deferred Phase 34)
grep -niE "(cerebras|groq)" <file>  # context-check each hit — citations to ADR-0010 Phase 34 are OK; live module references are not

# Pre-rename domain
grep -n "iran-conflict-monitor" <file>

# Pre-Phase-29 function timeout framing
grep -nE "(maxDuration: 60|10-second|30-second.*timeout)" <file>

# Pre-Phase-29 fire-and-forget IIFE (Phase 28.2.6 diagnosed; Phase 29 removed)
grep -niE "(fire-and-forget|fire and forget|IIFE.*async)" <file>

# Pre-Phase-29 single-Bearer auth framing
grep -nE "(single Bearer|one Bearer scheme)" <file>
```

<!-- Audit accumulator file shape (D-12). -->

`36-02-AUDIT.md` is a markdown file with two tables (markdown files + Mermaid blocks):

```markdown
# Phase 36-02 Architecture Sweep Audit

**Generated:** {YYYY-MM-DD}
**Plan:** 36-02
**Decisions:** D-09, D-10, D-11, D-12, D-21

## Architecture Markdown Files (12 rows)

| File                                | Status         | Drift Found                   | Commit Ref         |
| ----------------------------------- | -------------- | ----------------------------- | ------------------ |
| docs/architecture/README.md         | verified-clean | none                          | n/a                |
| docs/architecture/system-context.md | edited         | <comma-separated drift items> | <short commit SHA> |
| ...                                 |

## Mermaid Diagrams (21 rows — NOT 22 as CONTEXT.md D-11 stated; actual grep count)

| File                            | Diagram # | Status         | Drift Found                               | Commit Ref |
| ------------------------------- | --------- | -------------- | ----------------------------------------- | ---------- |
| docs/architecture/data-flows.md | 1 of 9    | verified-clean | none                                      | n/a        |
| docs/architecture/data-flows.md | 2 of 9    | edited         | "OpenRouter as primary; corrected to NIM" | abc1234    |
| ...                             |

## Framing-Gap Callouts (for Plan 36-06 SUMMARY.md)

- CONTEXT.md D-11 says "22 Mermaid diagrams"; actual count is 21. (Not retroactively edited per D-04.)
- [Add any other framing gaps surfaced during sweep.]
```

Plan 36-06 reads this file when authoring the final SUMMARY.md audit table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-file v1.5-drift audit + targeted edits across 12 architecture markdown files (D-09, D-11, D-12)</name>
  <files>
    docs/architecture/README.md
    docs/architecture/system-context.md
    docs/architecture/data-flows.md
    docs/architecture/deployment.md
    docs/architecture/frontend.md
    docs/architecture/llm-pipeline-reliability.md
    docs/architecture/redis-keys.md
    docs/architecture/ontology/README.md
    docs/architecture/ontology/types.md
    docs/architecture/ontology/algorithms.md
    docs/architecture/ontology/complexity.md
    docs/architecture/ontology/state-machines.md
    .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md
  </files>
  <read_first>
    docs/architecture/llm-pipeline-reliability.md (full file — the canonical "what shipped" reference for drift detection)
    docs/architecture/redis-keys.md (Phase 35 D-05 output — second canonical reference)
    docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (Phase 29/30/30.1/34/35 sub-blocks — narrative source of truth for v3 narrowing)
    CLAUDE.md (§LLM Event Pipeline + §Vercel Deployment + §Serverless Cache — current shipped reality)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-09, D-11, D-12)
    .planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-01-PLAN.md task 3 (audit-table format template — Phase 35 D-12 precedent)
  </read_first>
  <action>
    Process the 12 files in this order (prioritize most-likely-drift first per CONTEXT.md D-09):

    **High-priority (likely needs edits):**
    1. `system-context.md` (2 Mermaid blocks)
    2. `data-flows.md` (9 Mermaid blocks — biggest)
    3. `deployment.md` (2 Mermaid blocks)
    4. `frontend.md` (3 Mermaid blocks)
    5. `ontology/state-machines.md` (4 Mermaid blocks)

    **Verified-clean expected (skim-only):**
    6. `llm-pipeline-reliability.md` (Phase 30+30.1+34 sub-blocks already current)
    7. `redis-keys.md` (Phase 35 D-05 output already current)
    8. `README.md` (architecture index — verify cross-links and file count)
    9. `ontology/README.md`
    10. `ontology/types.md`
    11. `ontology/algorithms.md`
    12. `ontology/complexity.md`

    For EACH file, follow this 4-step protocol:

    **STEP A — Read full file.** No skim-then-grep; one Read pass per CLAUDE.md anti-pattern #re-reads.

    **STEP B — Run drift greps:** Run each grep from `<interfaces>` "Drift detection patterns" block. Collect hits with line numbers.

    **STEP C — Decide outcome:**
    - **If zero drift hits:** record row as `verified-clean | none | n/a` in the audit accumulator. Move to next file.
    - **If drift hits:** for each hit, decide whether the surrounding text needs editing (a citation to ADR-0010 Phase 34 mentioning Cerebras/Groq is fine; a live module reference is not). Make surgical Edit-tool edits — never overwrite the whole file.

    **STEP D — If edited:** Commit (atomic per D-27): `docs(36): D-09 sweep <relative-filename> (<one-sentence-drift-summary>)`. Capture the short commit SHA.

    After all 12 files processed, use the Write tool to create `.planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md` with the markdown-files table populated (12 rows). Leave the Mermaid-diagrams table populated only with Mermaid blocks that live INSIDE edited files (Task 2 fills in the verified-clean Mermaid rows). Add a `## Framing-Gap Callouts` section noting:
    - "CONTEXT.md D-11 says '22 Mermaid diagrams'; actual count is 21 (verified by `grep -c '```mermaid'` across the 12 files: 9+2+3+2+4+1=21). Not retroactively edited per D-04."
    - Any other framing gaps surfaced (e.g., "docs/architecture/README.md says '10 markdown files'; actual is 12 after Phase 30 llm-pipeline-reliability.md and Phase 35 redis-keys.md.").

    Commit the audit file: `docs(36): D-12 record architecture sweep audit accumulator (markdown-files table)`.

    NO edits to PROJECT.md / ROADMAP.md / REQUIREMENTS.md to fix framing-gaps (D-04 + D-11 explicitly preserve those).

  </action>
  <verify>
    <automated>test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md && grep -q "Architecture Markdown Files" .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md && grep -cE "^\| docs/architecture/" .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md | awk '$1>=12 {exit 0} {exit 1}' && grep -q "Framing-Gap Callouts" .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md && ! grep -rE "iran-conflict-monitor\\.vercel\\.app" docs/architecture/</automated>
  </verify>
  <acceptance_criteria>
    - `36-02-AUDIT.md` exists and contains a markdown table row for every one of the 12 files in scope.
    - Every row has Status ∈ {verified-clean, edited} and a Drift Found cell (`none` for verified-clean).
    - `docs/architecture/` and `docs/architecture/ontology/` contain NO occurrences of `iran-conflict-monitor.vercel.app` (pre-rename domain).
    - `docs/architecture/` files contain NO occurrences of `llmEventExtractor.v1` or `llmEventExtractor.v2` (deleted Phase 29; any reference must be a historical citation framed as such).
    - `docs/architecture/deployment.md` contains the string `800` (referencing `maxDuration: 800` Phase 29 D-08) OR is verified-clean with the existing framing already accurate.
    - `36-02-AUDIT.md` contains a `## Framing-Gap Callouts` section listing the file-count + diagram-count gaps for Plan 36-06 to absorb.
    - At least one `git log --grep "docs(36): D-09"` commit exists if any file was edited (verified-clean files don't get commits, only audit rows).
  </acceptance_criteria>
  <done>
    12 markdown files audited; drift surgically removed; audit accumulator records the per-file outcome. Plan 36-06 has the trust artifact it needs for SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-Mermaid-block sweep + GitHub-preview verification for edited diagrams (D-10, D-12)</name>
  <files>
    docs/architecture/system-context.md
    docs/architecture/data-flows.md
    docs/architecture/deployment.md
    docs/architecture/frontend.md
    docs/architecture/ontology/types.md
    docs/architecture/ontology/state-machines.md
    .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md
  </files>
  <read_first>
    .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md (post-Task-1 state — partial Mermaid rows already populated for edited files)
    docs/architecture/llm-pipeline-reliability.md (canonical v3 cascade shape — diagrams must match)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-10, D-12)
  </read_first>
  <action>
    Walk each of the 21 Mermaid blocks (9 in data-flows.md, 3 in frontend.md, 4 in state-machines.md, 2 in deployment.md, 2 in system-context.md, 1 in types.md).

    For EACH block:

    **STEP A — Read the block.** Use `grep -nE "^\`\`\`mermaid|^\`\`\`$" <file>` to locate fence boundaries, then Read with offset/limit to view the block.

    **STEP B — Drift checks specific to Mermaid diagrams:**
    - Does the diagram name `v1` or `v2` modules? (Deleted Phase 29 — diagram must show v3 only OR be explicitly historical.)
    - Does the diagram show Cerebras or Groq nodes as active providers? (Deferred Phase 34 — diagram must show NIM as primary OR mark them dormant/deferred.)
    - Does the diagram show OpenRouter as an active runtime path? (Dormant Phase 30.1 — show as fall-through declared chain OR mark dormant.)
    - Does the diagram describe `/api/events` as the LLM trigger? (Phase 29 changed to cron-only — diagram must show `/api/cron/refresh-events` as sole writer.)
    - Does the diagram show pre-rename domain or pre-Phase-29 framing?

    **STEP C — If drift, edit the Mermaid source surgically.** Mermaid syntax is sensitive — preserve indentation, node IDs, and edge labels. Re-verify the block parses (no missing arrows, no unclosed nodes).

    **STEP D — GitHub-preview verification (D-10 requirement):** For every Mermaid block that was edited, the executor MUST validate rendering. Two acceptable methods:
    - Option 1 (preferred — purely-local): Use `mmdc` (mermaid-cli) if installed; `npx -y @mermaid-js/mermaid-cli --input <tmp.mmd> --output <tmp.svg>` and confirm SVG is generated without errors. If `mmdc` not available, skip to Option 2.
    - Option 2 (operator-pause): Append a `[BLOCKING-HUMAN]` checkpoint task asking the operator to: (a) push the commit, (b) open the file on GitHub web (which renders Mermaid natively), (c) confirm each edited diagram renders correctly, (d) reply `approved` to continue.

    Per CONTEXT.md Claude's Discretion: prefer Option 1 (mmdc local validation) to keep the plan autonomous. If `mmdc` install fails due to npm constraints (no internet, offline run), fall back to Option 2.

    **STEP E — Update audit accumulator.** Open `36-02-AUDIT.md` and ensure the Mermaid-Diagrams table has a row for ALL 21 blocks (edited ones from Task 1 included; verified-clean ones recorded as `verified-clean | none | n/a`).

    Commit (atomic per D-27): one commit per edited diagram OR a rollup `docs(36): D-10 sweep Mermaid diagrams (<N> edited, <M> verified-clean)` body lists per-file counts.

    NO edits to llm-pipeline-reliability.md or redis-keys.md Mermaid blocks — both files have zero Mermaid blocks per grep (already verified). The 21 blocks are entirely outside those two files.

  </action>
  <verify>
    <automated>test -f .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md && grep -c "^| docs/architecture/.*| [0-9]* of [0-9]*" .planning/phases/36-public-docs-sweep-openapi-additions/36-02-AUDIT.md | awk '$1>=21 {exit 0} {exit 1}' && grep -c '```mermaid' docs/architecture/data-flows.md | awk '$1==9 {exit 0} {exit 1}' && grep -c '```mermaid' docs/architecture/frontend.md | awk '$1==3 {exit 0} {exit 1}' && grep -c '```mermaid' docs/architecture/ontology/state-machines.md | awk '$1==4 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - All 21 Mermaid blocks have a row in `36-02-AUDIT.md` Mermaid-Diagrams table.
    - Mermaid-block counts per file are preserved (9 data-flows, 3 frontend, 4 state-machines, 2 deployment, 2 system-context, 1 types).
    - No Mermaid block in `docs/architecture/` references `v1` or `v2` modules without explicit "deleted Phase 29" / historical framing.
    - For each edited Mermaid block, the commit body cites the diagram # within its file (e.g., "data-flows.md diagram 2 of 9").
    - If `mmdc` validation was used: at least one verify command output mentions `.svg` generation for an edited block. If operator-pause was used: a `[BLOCKING-HUMAN]` task was appended with `resume-signal: 'approved'` text.
  </acceptance_criteria>
  <done>
    21 Mermaid blocks swept; drift removed and verified rendering; per-block audit row written. Plan 36-06's SUMMARY audit table has the complete diagram inventory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append ADR-0011 Phase 36 sub-block (D-21)</name>
  <files>docs/adr/0011-v3-llm-pipeline-architecture.md</files>
  <read_first>
    docs/adr/0011-v3-llm-pipeline-architecture.md (full file, 224 lines — confirm sub-block insertion point before `## Consequences`)
    docs/adr/0010-v1-5-llm-pipeline-narrowing-and-deletion.md (lines 52-152 — Phase 30/30.1/34/35 sub-block format templates; copy this verbatim shape)
    .planning/phases/36-public-docs-sweep-openapi-additions/36-CONTEXT.md (D-21)
    docs/architecture/llm-pipeline-reliability.md (canonical "what's the shipped cascade today" — sub-block content source of truth)
  </read_first>
  <action>
    Use the Edit tool to APPEND a new sub-block to `docs/adr/0011-v3-llm-pipeline-architecture.md`. Insert BEFORE the existing `## Consequences` heading (mirrors ADR-0010 sub-block placement at line 133 before line 152 `## Consequences`).

    Sub-block content (verbatim — substitute `{YYYY-MM-DD}` with today's UTC date):

    ```markdown
    ## Phase 36 Sub-block (appended {YYYY-MM-DD})

    **Context:** ADR-0011 documented the v3 pipeline as designed when v1 + v2 were
    deleted in Phase 29. Phases 30.1 + 34 then narrowed the runtime cascade further
    without re-authoring this ADR. Phase 36 is the public-docs sweep that brings
    README / runbook / degradation / architecture markdown + the OpenAPI spec
    into v1.5 reality; this sub-block updates ADR-0011 in the same lockstep so a
    reader of ADR-0011 alone gets the current shipped state.

    **Runtime cascade as shipped (post-Phase-34):**

    - **NIM** (qwen-235b instruct) — the only LLM provider invoked at runtime.
      See [`docs/architecture/llm-pipeline-reliability.md`](../architecture/llm-pipeline-reliability.md)
      §"Multi-Provider Cascade (Phase 34)" for the cascade-shape table.
    - **OpenRouter** — DORMANT per [ADR-0010 Phase 30.1 sub-block](0010-v1-5-llm-pipeline-narrowing-and-deletion.md#phase-301-sub-block-appended-2026-05-17).
      `skipOpenRouter: true` at `server/lib/llmEventExtractor.v3.ts:622, 929`.
      Free-tier probe landed in not-viable bucket; cascade construction at
      `server/lib/freeClaudeRouter.ts:341-363` is unchanged and ready to fall
      through if the dormancy decision is revisited.
    - **Cerebras + Groq** — DEFERRED per [ADR-0010 Phase 34 sub-block](0010-v1-5-llm-pipeline-narrowing-and-deletion.md#phase-34-sub-block-appended-2026-05-23).
      Operator chose to skip provisioning; no adapter, no probe, no token-budget
      counters. Restoration is a future-phase decision, not a regression.

    **Outcome:** The "v3 pipeline architecture" decision recorded in this ADR is
    REAFFIRMED — the architectural primitives (watchdog, DLQ, circuit breaker,
    token budget, 6-path resolver, parallel batches via concurrencyLimit,
    cron-only trigger) all remain load-bearing. What CHANGED is the provider
    set the architecture orchestrates at runtime: NIM-only at runtime as of
    Phase 34 close. The cascade-shape primitives are dormant-ready; no
    architectural rework is required to wake them.

    **Cross-references:**
    - [ADR-0010](0010-v1-5-llm-pipeline-narrowing-and-deletion.md) — narrowing decisions per phase.
    - [`docs/architecture/llm-pipeline-reliability.md`](../architecture/llm-pipeline-reliability.md) — current cascade-shape table.
    - [CLAUDE.md §LLM Event Pipeline](../../CLAUDE.md) — operator skim.
    - Phase 36 SUMMARY.md — full Phase 36 close-out context.
    ```

    Commit (atomic per D-27): `docs(36): D-21 append Phase 36 sub-block to ADR-0011 (NIM-only runtime cascade reaffirmation)` body cites the cross-links (ADR-0010 Phase 30.1 + 34 sub-blocks).

    Verify the sub-block renders correctly: `grep -A 2 "## Phase 36 Sub-block" docs/adr/0011-v3-llm-pipeline-architecture.md` should show the appended date + Context line.

    Per CONTEXT.md Claude's Discretion 1: this task lands in Plan 36-02 (architecture-adjacent) rather than Plan 36-06 (close-bookkeeping). Rationale: Phase 36's "ADR touch" is conceptually architecture-adjacent; gets the touch out of the way during the sweep, leaves 36-06 focused on phase-close ritual.

  </action>
  <verify>
    <automated>grep -q "^## Phase 36 Sub-block" docs/adr/0011-v3-llm-pipeline-architecture.md && grep -q "DORMANT per \[ADR-0010 Phase 30.1" docs/adr/0011-v3-llm-pipeline-architecture.md && grep -q "DEFERRED per \[ADR-0010 Phase 34" docs/adr/0011-v3-llm-pipeline-architecture.md && grep -B 2 "^## Consequences" docs/adr/0011-v3-llm-pipeline-architecture.md | grep -q "Phase 36 Sub-block\|REAFFIRMED" && git log -1 --format=%s docs/adr/0011-v3-llm-pipeline-architecture.md | grep -q "D-21"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0011-v3-llm-pipeline-architecture.md` contains `## Phase 36 Sub-block (appended <date>)`.
    - Sub-block appears BEFORE the existing `## Consequences` section.
    - Sub-block cross-links to BOTH ADR-0010 Phase 30.1 sub-block AND ADR-0010 Phase 34 sub-block.
    - Sub-block uses "REAFFIRMED" language about the v3 architecture (not "superseded" or "deprecated").
    - `git log -1 --format=%s` for the file matches `docs(36):` with `D-21` in body.
    - Sub-block format mirrors ADR-0010 sub-block shape (Context / Outcome / Cross-references headings present).
  </acceptance_criteria>
  <done>
    ADR-0011 reflects post-Phase-34 runtime reality. Reader of ADR-0011 alone gets current shipped state without having to also read ADR-0010 Phase 30.1 + 34 sub-blocks separately (though cross-links make that easy).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                 |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Sweep author → 12 markdown files | Hand-authored markdown edits; no input validation; mitigated by markdown-link-check gate in Plan 36-06 D-24 |
| Sweep author → 21 Mermaid blocks | Mermaid syntax sensitivity; mitigated by mmdc local validation OR operator GitHub-preview pause             |
| Sub-block author → ADR-0011      | Hand-authored ADR sub-block; cross-references must resolve; mitigated by markdown-link-check                |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                    |
| ---------- | ---------------------- | -------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-36-02-01 | Information Disclosure | Architecture docs publish file paths + class names | accept      | These are not secrets; the same paths appear in CLAUDE.md and ADR-0010/0011. Public docs improve operator skill at zero additional attack surface.                                 |
| T-36-02-02 | Tampering              | Mermaid syntax errors break rendering              | mitigate    | Per-block GitHub-preview verification (D-10) — either via mmdc local validation OR operator-pause checkpoint.                                                                      |
| T-36-02-03 | Tampering              | ADR-0011 sub-block insertion point                 | mitigate    | Edit-tool surgical insertion before `## Consequences`; no Write-overwrite of the whole file.                                                                                       |
| T-36-02-SC | Tampering              | mmdc install (if Option 1 used)                    | mitigate    | `@mermaid-js/mermaid-cli` is the official Mermaid CLI, legitimate (millions of weekly downloads). Verified at npmjs.com. Used only locally as dev tool; not added to package.json. |

</threat_model>

<verification>
- `grep -rE "iran-conflict-monitor\\.vercel\\.app" docs/architecture/` returns no matches.
- `grep -rE "llmEventExtractor\\.v[12]" docs/architecture/` returns no live-code references (historical citations are OK).
- For each of the 12 files, `36-02-AUDIT.md` contains a row.
- For each of the 21 Mermaid blocks, `36-02-AUDIT.md` contains a row.
- `docs/adr/0011-v3-llm-pipeline-architecture.md` contains the Phase 36 sub-block before `## Consequences`.
- `npx vitest run` exits 0 (no regressions — this plan touches docs/ + .planning/, not source/test code).
</verification>

<success_criteria>

1. 12 architecture markdown files audited; drift removed where found; verified-clean recorded for the rest.
2. 21 Mermaid blocks audited; drift removed where found; per-block GitHub-preview verification completed.
3. `36-02-AUDIT.md` populated with both tables + framing-gap callouts.
4. ADR-0011 Phase 36 sub-block appended with cross-links to ADR-0010 Phase 30.1 + 34.
5. Atomic per-file / per-Mermaid commits with `D-09` / `D-10` / `D-12` / `D-21` references in body.
6. No edits to PROJECT.md / ROADMAP.md / REQUIREMENTS.md (D-04 + D-11).
7. Coverage: DOCS-PUB-02 addressed.

</success_criteria>

<output>
Create `.planning/phases/36-public-docs-sweep-openapi-additions/36-02-SUMMARY.md` when done. Include:
- Per-file outcome counts: N edited, M verified-clean.
- Per-Mermaid-block outcome counts: N edited, M verified-clean.
- Mermaid-validation method chosen (mmdc local OR operator GitHub-preview pause).
- ADR-0011 sub-block commit SHA.
- Framing-gap callouts list (for Plan 36-06 absorption).
- Commit SHA list per file/diagram for traceability.
</output>

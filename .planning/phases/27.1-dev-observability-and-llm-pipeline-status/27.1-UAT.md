---
status: complete
phase: 27.1-dev-observability-and-llm-pipeline-status
source: [27.1-01-SUMMARY.md, 27.1-02-SUMMARY.md, 27.1-03-SUMMARY.md]
started: 2026-04-11T00:00:00Z
updated: 2026-04-11T01:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Dev Server Starts

expected: `npm run dev` starts without errors. Page loads with map visible.
result: pass
note: Pre-verified — dev server running, map loads

### 2. Test Suite Passes

expected: `npx vitest run` exits 0. No failures or type errors.
result: pass
note: 1389/1389 tests passing, tsc clean

### 3. DevApiStatus Panel Visibility

expected: Bottom-left corner shows "API ~" button (or "API !" if issues). Clicking expands the full status panel.
result: pass

### 4. Source Status Dots

expected: All 8 source rows display with colored status dots. Connected=green, Error=red, Empty=amber, Loading=blue.
result: pass

### 5. Per-Source Metrics

expected: Each row shows count, avg response time (ms), success rate (X/Y), poll countdown, and age.
result: pass

### 6. Data Quality Indicators

expected: Clicking/expanding a row shows quality breakdown. Flights shows "X total, Y unid, Z gnd". Events shows "X total, Y LLM, Z raw | precision breakdown".
result: pass

### 7. Error Diagnostics

expected: Sources with errors show a red indicator. Expanding shows the actual error message (e.g. "News API 502"), not just "ERROR".
result: pass

### 8. Copy Diagnostics

expected: Clicking the copy button copies valid JSON to clipboard with all source data, metrics, quality strings, and LLM pipeline status.
result: pass

### 9. LLM Pipeline Progress

expected: The LLM Pipeline section shows current stage and batch progress when processing (e.g. "llm-processing: batch 8/41"). Shows "idle" or last run summary when not processing.
result: pass

### 10. LLM Status Endpoint

expected: `curl http://localhost:5173/api/events/llm-status` returns JSON with pipeline stage, batch counts, and timing.
result: pass
note: Verified automatically — returns live progress (batch 20/42, 334 groups)

### 11. Poll Countdown Accuracy

expected: Poll countdowns tick down in real-time. One-shot sources (Sites, Water) show "Complete" instead of a countdown.
result: pass

### 12. Production Safety

expected: DevApiStatus does NOT render in production builds. The `/api/events/llm-status` endpoint returns 404 when NODE_ENV=production.
result: pass
note: Verified by code inspection — import.meta.env.DEV gate (line AppShell.tsx) + NODE_ENV gate (events.ts line 215)

## Summary

total: 12
passed: 12
issues: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Deferred

- Water facility type filters in FilterPanelSlot (dam, reservoir, desalination, treatment_plant toggles under "Water" section) — discovered during UAT, separate quick task

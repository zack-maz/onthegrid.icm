---
phase: 27-conflict-geolocation-improvement
plan: 05
type: execute
wave: 3
depends_on: ['27-03', '27-04']
files_modified:
  - src/components/layout/LayerTogglesSlot.tsx
  - src/components/layout/DetailPanelSlot.tsx
  - src/components/detail/EventDetail.tsx
  - src/components/map/EntityTooltip.tsx
  - src/components/map/PrecisionRingLayer.tsx
  - src/components/map/BaseMap.tsx
  - src/__tests__/LayerToggles.test.tsx
  - src/__tests__/DetailPanel.test.tsx
autonomous: true
requirements:
  - D-08
  - D-09
  - D-11
  - D-13
  - D-15
  - D-18
  - D-19

must_haves:
  truths:
    - 'LayerTogglesSlot shows master Events toggle with 5 indented sub-toggles'
    - 'EventDetail shows summary, casualties, precision indicator, and human-readable location name'
    - 'EntityTooltip shows precision indicator and location name for events'
    - 'Precision rings render on map: exact=point, neighborhood=1km, city=5km, region=25km'
    - 'Event type colors are distinct shades within the red spectrum'
  artifacts:
    - path: 'src/components/layout/LayerTogglesSlot.tsx'
      provides: 'Master + 5 sub-toggle UI'
      contains: 'showEvents'
    - path: 'src/components/detail/EventDetail.tsx'
      provides: 'Summary, casualties, precision display'
      contains: 'summary'
    - path: 'src/components/map/PrecisionRingLayer.tsx'
      provides: 'Radius ring overlay per precision level'
      contains: 'ScatterplotLayer'
  key_links:
    - from: 'src/components/layout/LayerTogglesSlot.tsx'
      to: 'src/stores/filterStore.ts'
      via: 'toggle state binding'
      pattern: 'showEvents|showOnGround|showExplosions'
    - from: 'src/components/map/BaseMap.tsx'
      to: 'src/hooks/useEntityLayers.ts'
      via: 'visible events array'
      pattern: 'import.*from.*useEntityLayers'
---

<objective>
Build the UI features: master + 5 sub-toggle layer controls, enriched EventDetail with summary/casualties/precision, updated EntityTooltip, precision radius rings on the map, and event type colors.

Purpose: These are the user-facing features that make the LLM enrichment visible — summaries, casualty data, precision indicators, and honest uncertainty rings replace the current bare-bones event display.

Output: Updated LayerTogglesSlot, EventDetail, EntityTooltip, new PrecisionRingLayer, updated BaseMap.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-conflict-geolocation-improvement/27-CONTEXT.md
@.planning/phases/27-conflict-geolocation-improvement/27-RESEARCH.md
@.planning/phases/27-conflict-geolocation-improvement/27-01-SUMMARY.md
@.planning/phases/27-conflict-geolocation-improvement/27-04-SUMMARY.md

<interfaces>
<!-- From Plan 04: Updated toggles -->
From src/stores/filterStore.ts (updated):
```typescript
showEvents: boolean;
showAirstrikes: boolean;
showOnGround: boolean;
showExplosions: boolean;
showTargeted: boolean;
showOther: boolean;
toggleEvents: () => void;
toggleOnGround: () => void;
toggleExplosions: () => void;
toggleOther: () => void;
```

<!-- From Plan 01: Updated ConflictEventData -->

From server/types.ts (updated):

```typescript
interface ConflictEventData {
  // ... existing fields ...
  summary?: string;
  casualties?: { killed?: number; injured?: number; unknown?: boolean };
  precision?: 'exact' | 'neighborhood' | 'city' | 'region';
  actors?: string[];
  sourceCount?: number;
  llmProcessed?: boolean;
}
```

<!-- From Plan 04: Updated EVENT_TYPE_LABELS -->

From src/types/ui.ts (updated):

```typescript
export const EVENT_TYPE_LABELS: Record<string, string> = {
  airstrike: 'Airstrike',
  on_ground: 'Ground Combat',
  explosion: 'Explosion',
  targeted: 'Targeted',
  other: 'Other',
};
```

</interfaces>
</context>

<threat_model>
No new security threats. UI rendering only, no new data inputs.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: LayerTogglesSlot master + 5 sub-toggles and event type colors</name>
  <files>src/components/layout/LayerTogglesSlot.tsx, src/__tests__/LayerToggles.test.tsx</files>
  <read_first>
    - src/components/layout/LayerTogglesSlot.tsx (full file — current toggle rows, SliderToggle usage, indentation pattern)
    - src/stores/filterStore.ts (updated toggle state from Plan 04)
    - src/types/ui.ts (updated EVENT_TYPE_LABELS from Plan 04)
    - src/__tests__/LayerToggles.test.tsx (existing tests)
  </read_first>
  <action>
  Replace the current 3 event toggle rows (Airstrikes, Ground Combat, Targeted) with a master + 5 sub-toggle system:

**New toggle rows (in order):**

1. **Events** — Master toggle, NOT indented, uses `showEvents` / `toggleEvents`
2. **Airstrikes** — Indented, uses `showAirstrikes` / `toggleAirstrikes`, color dot: bright red (#ff3b30)
3. **Ground Combat** — Indented, uses `showOnGround` / `toggleOnGround`, color dot: dark red (#c0392b)
4. **Explosions** — Indented, uses `showExplosions` / `toggleExplosions`, color dot: orange-red (#e74c3c)
5. **Targeted** — Indented, uses `showTargeted` / `toggleTargeted`, color dot: crimson (#dc143c)
6. **Other** — Indented, uses `showOther` / `toggleOther`, color dot: maroon (#800000)

**Color choices (D-09: red spectrum, must not clash with yellow flights, purple ships, green sites):**

- airstrike: bright red #ff3b30 (existing)
- on_ground: dark red #c0392b
- explosion: orange-red #e74c3c
- targeted: crimson #dc143c
- other: maroon #800000

Export these colors as `EVENT_TYPE_COLORS: Record<ConflictEventType, string>` from a shared location (src/lib/eventColors.ts or inline in LayerTogglesSlot) so they can be reused by entity layers and the icon system.

When the master `showEvents` is OFF, all 5 sub-toggles should appear dimmed (opacity 40%, existing dimming pattern). The sub-toggles still maintain their own state so when the master is toggled back ON, they resume.

**Update existing tests:** Change assertions from 3 event toggles to 5 + master.
</action>
<verify>

<!-- prettier-ignore -->
<automated>npx vitest run src/__tests__/LayerToggles.test.tsx -x</automated>
</verify>
<done>LayerTogglesSlot renders 6 toggle rows for events (1 master + 5 sub). Colors are in the red spectrum per D-09. Master toggle dims sub-toggles when OFF.</done>

<!-- prettier-ignore -->
<acceptance_criteria> - src/components/layout/LayerTogglesSlot.tsx contains 'Events' as a toggle label (master) - src/components/layout/LayerTogglesSlot.tsx contains 'Explosions' as a toggle label - src/components/layout/LayerTogglesSlot.tsx contains 'Ground Combat' as a toggle label - src/components/layout/LayerTogglesSlot.tsx contains 'showEvents' reference - src/components/layout/LayerTogglesSlot.tsx contains 'showOnGround' reference - Event type colors are defined with at least 5 hex values in the red spectrum - `npx vitest run src/__tests__/LayerToggles.test.tsx -x` exits 0
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: EventDetail enrichment + EntityTooltip + PrecisionRingLayer</name>
  <files>src/components/detail/EventDetail.tsx, src/components/map/EntityTooltip.tsx, src/components/map/PrecisionRingLayer.tsx, src/components/map/BaseMap.tsx, src/__tests__/DetailPanel.test.tsx</files>
  <read_first>
    - src/components/layout/DetailPanelSlot.tsx (how EventDetail is rendered — check for per-type content sections)
    - src/components/detail/EventDetail.tsx (if it exists as a separate component; may be inline in DetailPanelSlot)
    - src/components/map/EntityTooltip.tsx (current event tooltip rendering)
    - src/components/map/BaseMap.tsx (DeckGLOverlay layers array, how layers are stacked)
    - src/components/map/layers/ThreatHeatmapOverlay.tsx (ScatterplotLayer pattern with radiusUnits: 'meters' for reference)
    - src/__tests__/DetailPanel.test.tsx (existing detail panel tests)
    - server/types.ts (ConflictEventData with new fields from Plan 01)
  </read_first>
  <action>
  **File 1: EventDetail (src/components/detail/EventDetail.tsx or section in DetailPanelSlot.tsx)**

Add new sections to the event detail view:

1. **Summary section** — Render `event.data.summary` if present. Use a blockquote or styled paragraph. Label: "Summary". If not present (raw GDELT fallback), show nothing (not "No summary available").

2. **Casualties section** — Render `event.data.casualties` if present:
   - "Killed: {n}" if `casualties.killed` is non-null
   - "Injured: {n}" if `casualties.injured` is non-null
   - "Casualties: Unknown" if `casualties.unknown` is true and both killed/injured are null
   - If all null/undefined, hide section entirely

3. **Precision indicator** — Render `event.data.precision` if present:
   - exact: green dot + "Precise location"
   - neighborhood: yellow dot + "Neighborhood-level (~1km)"
   - city: orange dot + "City-level (~5km)"
   - region: red dot + "Region-level (~25km)"
   - If not present (raw GDELT), show legacy `event.data.geoPrecision` instead

4. **Location name** — Render `event.data.locationName` prominently (it's always present, but now may be LLM-enriched with a more specific name like "Deir ez-Zor, eastern Syria" instead of just "Deir Ez-Zor, Dayr Az Zawr, Syria").

5. **Source count** — If `event.data.sourceCount` is present, show "Reported by {n} sources". Otherwise fall back to `event.data.numSources`.

6. **LLM badge** — If `event.data.llmProcessed` is true, show a small "AI-enriched" badge (subtle, not prominent).

**File 2: EntityTooltip** — Update event tooltip to show:

- Precision indicator dot (same color coding as detail panel)
- Location name (event.data.locationName)
- If llmProcessed, show type from EVENT_TYPE_LABELS

**File 3: PrecisionRingLayer (NEW)** — Create a deck.gl ScatterplotLayer that renders translucent radius rings for events with precision data:

```typescript
// Radius mapping per precision level
const PRECISION_RADIUS_METERS: Record<string, number> = {
  exact: 0, // No ring for exact — point icon only
  neighborhood: 1000, // 1km ring
  city: 5000, // 5km ring
  region: 25000, // 25km ring
};
```

Use ScatterplotLayer with:

- `radiusUnits: 'meters'`
- `getRadius: d => PRECISION_RADIUS_METERS[d.data.precision] || 0`
- `getFillColor: [239, 68, 68, 40]` (red with low alpha — translucent ring)
- `getLineColor: [239, 68, 68, 120]` (red border, slightly more opaque)
- `stroked: true`, `filled: true`, `lineWidthMinPixels: 1`
- `pickable: false` (rings are visual-only, don't intercept clicks)
- Only render events where `precision !== 'exact'` and `precision` is defined

**File 4: BaseMap.tsx** — Add PrecisionRingLayer to the DeckGLOverlay layers array. Stack it BELOW entity icons (rings should be behind the event markers). Filter input events by those with `precision` field defined.

**Update tests:** Add test for summary rendering in detail panel.
</action>
<verify>

<!-- prettier-ignore -->
<automated>npx vitest run src/__tests__/DetailPanel.test.tsx -x</automated>
</verify>
<done>EventDetail shows summary, casualties, precision, and location name. EntityTooltip shows precision indicator. PrecisionRingLayer renders radius rings. BaseMap includes precision ring layer.</done>
<acceptance_criteria> - EventDetail component (wherever it lives) contains 'Summary' as rendered text - EventDetail component contains 'casualties' or 'Killed' as rendered text - EventDetail component contains 'precision' reference - src/components/map/PrecisionRingLayer.tsx exists - src/components/map/PrecisionRingLayer.tsx contains 'ScatterplotLayer' - src/components/map/PrecisionRingLayer.tsx contains 'neighborhood' and 'city' and 'region' - src/components/map/BaseMap.tsx contains 'PrecisionRingLayer' import - `npx vitest run src/ -x` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npx vitest run src/ -x` — all client tests pass
- `npx vitest run -x` — full suite passes
- `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
LayerTogglesSlot has master + 5 sub-toggles with red spectrum colors. EventDetail displays LLM-enriched data (summary, casualties, precision, location). EntityTooltip shows precision indicators. PrecisionRingLayer renders uncertainty rings on the map. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/27-conflict-geolocation-improvement/27-05-SUMMARY.md`
</output>

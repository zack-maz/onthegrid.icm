---
phase: 44-events-subtab-pipeline-detail
plan: 02
type: execute
wave: 2
depends_on: ['44-01']
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/__tests__/DevApiStatusV3.test.tsx
  - src/__tests__/devApiStatusEventsSection.test.tsx
  - src/__tests__/components/DevApiStatus.prune.test.tsx
autonomous: true
requirements: [EVENTS-TAB-01, EVENTS-TAB-02]
must_haves:
  truths:
    - 'The 7 already-built LLM blocks (Waterfall, Histograms, CallLog, BudgetBars, EvalScore, Dlq, Suspect) render inside EventsFiltersSectionV3, each presence-gated so it self-hides when its LLMStatus field is absent'
    - 'FlightRecorderBlock is re-mounted inside the events subtab so run-history is visible there'
    - "A new DeadLinkBucketsBlock renders per-liveness-status bucket counts (labeled 'of N scanned'), the authoritative deadUrlCount, last24hPrunes, and drill-down rows with status badge / url / evidence(TEXT) / relativeTime(lastProbedAt) / dead-streak attemptCount"
    - 'The DeadLinkBucketsBlock self-hides when the threaded prune prop is absent (degrade-open: older server, fetch failure, missing Bearer)'
    - 'evidence renders as a plain React text node, never dangerouslySetInnerHTML (T-43-16 carried forward)'
    - 'The WAI-ARIA tablist DOM is unchanged — all mounts live inside the existing role=tabpanel aria-labelledby=tab-events container'
    - 'BudgetBarsBlock self-hides under NIM-only (absent tokenCounters/breakerState) — this is the correct, honest outcome, not a defect (D-06)'
  artifacts:
    - path: 'src/components/ui/DevApiStatus.tsx'
      provides: 'EventsFiltersSectionV3 gains presence-gated mounts of the 7 v2 blocks + FlightRecorder re-mount + new DeadLinkBucketsBlock; gains optional prune prop threaded from opStatus.prune'
      contains: 'DeadLinkBucketsBlock'
    - path: 'src/__tests__/DevApiStatusV3.test.tsx'
      provides: 'Evolved V3 empty-state pins reflecting presence-gated mounts (Pitfall 1)'
      contains: 'self-hide'
    - path: 'src/__tests__/components/DevApiStatus.prune.test.tsx'
      provides: 'DeadLinkBucketsBlock assertions — bucket counts, sample rows, self-hide on absent prune, evidence-as-TEXT'
      contains: 'DeadLinkBucketsBlock'
  key_links:
    - from: 'DevApiStatus events render switch (~line 916-920)'
      to: 'EventsFiltersSectionV3'
      via: 'prune={opStatus?.prune ?? null} prop thread (D-10)'
      pattern: "prune=\\{opStatus"
    - from: 'EventsFiltersSectionV3'
      to: 'DeadLinkBucketsBlock'
      via: '{prune && <DeadLinkBucketsBlock prune={prune} />}'
      pattern: 'DeadLinkBucketsBlock'
    - from: 'EventsFiltersSectionV3'
      to: 'the 7 v2 blocks'
      via: 'presence-gated mounts (llmStatus.<field> && <Block .../>)'
      pattern: 'CallLogBlock'
---

<objective>
Wire the existing LLM-pipeline observability into the production events subtab. This is a mount-and-thread phase: the 7 blocks already exist as local function components in `DevApiStatus.tsx` and are wired today only in the legacy v2 composer; mount them — presence-gated — into `EventsFiltersSectionV3` (the default/production path), re-mount the self-contained `FlightRecorderBlock`, add one new `DeadLinkBucketsBlock`, thread the already-fetched `opStatus.prune` down as a new prop, and evolve the two events-section test files in lockstep.

Purpose: EVENTS-TAB-01 (full LLM pipeline detail in the events subtab) + EVENTS-TAB-02 (per-bucket dead-link state). The operator with a Bearer key sees this subtab in production — every block must render honestly under real NIM-only data and self-hide (never fabricate zeros) when its data is absent.
Output: A populated `EventsFiltersSectionV3` + new `DeadLinkBucketsBlock` + evolved events-section test pins. No server changes (those landed in Plan 01). No styling beyond matching the existing block idiom (Phase 45 restyles). The WAI-ARIA tablist DOM is frozen.

Depends on Plan 01: this plan consumes `prune.countsByStatus`, `prune.deadUrlSample[].lastProbedAt`, `prune.deadUrlSample[].attemptCount`, and the widened `OperatorStatus.prune` client interface that Plan 01 shipped.
</objective>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification — net-new here):

- `DeadLinkBucketsBlock` — new local function component in `DevApiStatus.tsx` (per-status bucket counts + authoritative total + drill-down rows)
- `EventsFiltersSectionV3` signature change: gains an optional second prop `prune?: OperatorStatus['prune'] | null` (currently `({ llmStatus })`)
- New presence-gated JSX mounts inside `EventsFiltersSectionV3`: `WaterfallBlock`, `HistogramsBlock`, `CallLogBlock`, `BudgetBarsBlock`, `EvalScoreBlock`, `DlqBlock`, `SuspectBlock`, `FlightRecorderBlock`, `DeadLinkBucketsBlock`
- New test assertions in `DevApiStatusV3.test.tsx`, `devApiStatusEventsSection.test.tsx`, `DevApiStatus.prune.test.tsx` (test evolution authorized by D-12)
  </artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-events-subtab-pipeline-detail/44-CONTEXT.md
@.planning/phases/44-events-subtab-pipeline-detail/44-RESEARCH.md
@.planning/phases/44-events-subtab-pipeline-detail/44-PATTERNS.md
@.planning/phases/44-events-subtab-pipeline-detail/44-UI-SPEC.md
@.planning/phases/44-events-subtab-pipeline-detail/44-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the 7 v2 blocks + FlightRecorder into EventsFiltersSectionV3, presence-gated (no synthetic zero-defaults)</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx — the legacy composer `EventsFiltersSection` (~3331-3389) for the exact BLOCK ORDER + PROP NAMES; its zero-default destructuring (~3335-3341: `tc = llmStatus.tokenCounters ?? {cerebras:0, groq:0}` etc.) is the D-05 ANTI-PATTERN — read it to know what NOT to copy. The current `EventsFiltersSectionV3` (~3414-3446) is what you extend (it ends with `<DrillDownBlock llmStatus={llmStatus} />` already mounted — D-07: do NOT re-add it). The 7 block signatures: `WaterfallBlock` (~2529, self-`?? 0`-guards), `HistogramsBlock` (~2582, NonNullable callHistory), `CallLogBlock` (~2778, NonNullable callHistory), `BudgetBarsBlock` (~2833, requires BOTH tokenCounters AND breakerState), `EvalScoreBlock` (~2879), `DlqBlock` (~2933, NonNullable dlqRecent), `SuspectBlock` (~2968, needs a number). `FlightRecorderBlock` is already imported (line 6) and mounted in GROUP 2 (~1876).
    - src/hooks/useLLMStatusPolling.ts — `LLMStatus` interface (~185-247): all 7 fields are OPTIONAL on the wire (`callHistory?`, `tokenCounters?` (v2-shaped cerebras/groq), `breakerState?` (v2-shaped), `evalScore?`, `dlqRecent?`, `provenanceCounts?`, `suspectCount?`). This is why the guard must live in the composer (block bodies declare NonNullable props).
    - .planning/phases/44-events-subtab-pipeline-detail/44-UI-SPEC.md — §"Block Inventory & Mount Contract" (the per-block presence-gate table) and §"Color"/§"Typography" idiom.
    - .planning/phases/44-events-subtab-pipeline-detail/44-RESEARCH.md — Pattern 1 (presence-gated mount, the correct V3 idiom), Pitfall 6 (BudgetBarsBlock self-hide is correct).
  </read_first>
  <action>
    Inside `EventsFiltersSectionV3`, after the existing v3-native blocks and BEFORE/AFTER `<DrillDownBlock />` (ordering is your discretion — suggested: v3-native unchanged, then the 7 v2 blocks in legacy order, then FlightRecorder; Phase 45 will reorder anyway), add presence-gated mounts. The gate lives at the composer because the block bodies declare `NonNullable<...>` props:

    - `WaterfallBlock`: mount with `llmStatus={llmStatus}`. It self-guards every field with `?? 0`, so it is render-safe; mount it unconditionally OR gate on `llmStatus.stage !== 'idle'` for honesty (your call — record which in the summary).
    - `HistogramsBlock`: render only when `llmStatus.callHistory && llmStatus.callHistory.length > 0`; pass `provenanceCounts={llmStatus.provenanceCounts ?? {}}` and `callHistory={llmStatus.callHistory}`.
    - `CallLogBlock`: render only when `llmStatus.callHistory` is present; pass `callHistory={llmStatus.callHistory}`.
    - `BudgetBarsBlock`: render only when `llmStatus.tokenCounters && llmStatus.breakerState` are BOTH present; pass both. Under NIM-only these are absent → the block self-hides. THIS IS CORRECT (D-06) — do NOT add `?? {cerebras:0, groq:0}` fallbacks.
    - `EvalScoreBlock`: render only when `llmStatus.evalScore` is present; pass `evalScore={llmStatus.evalScore}`.
    - `DlqBlock`: render only when `llmStatus.dlqRecent` is present; pass `entries={llmStatus.dlqRecent}`.
    - `SuspectBlock`: render only when `typeof llmStatus.suspectCount === 'number'`; pass `count={llmStatus.suspectCount}`.
    - `FlightRecorderBlock`: one JSX line, no props (self-contained — own `/api/events/llm-history` fetch + 30s poll + degrade-open). The events tab and the API-Health tab are mutually-exclusive `activeTab` render branches, so this re-mount causes NO double fetch (D-08).

    Do NOT copy the legacy composer's `ch/tc/bk/es/dlq/sc` zero-default destructuring block. Do NOT re-mount `DrillDownBlock` (already in V3). Do NOT change the tabpanel container or any tablist DOM. Match the existing visual idiom exactly (D-13) — these are existing components; just mount them.

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EventsFiltersSectionV3" src/components/ui/DevApiStatus.tsx` shows all 7 v2 block names (`WaterfallBlock`, `HistogramsBlock`, `CallLogBlock`, `BudgetBarsBlock`, `EvalScoreBlock`, `DlqBlock`, `SuspectBlock`) and `FlightRecorderBlock` referenced inside the V3 composer body.
    - `grep -c "cerebras: 0" src/components/ui/DevApiStatus.tsx` does NOT increase from baseline (no new synthetic zero-defaults — the only pre-existing occurrence is in the legacy composer, untouched).
    - `DrillDownBlock` appears exactly once inside `EventsFiltersSectionV3` (not re-added).
    - `grep -c "role=\"tabpanel\" aria-labelledby=\"tab-events\"" src/components/ui/DevApiStatus.tsx` is unchanged from baseline (1) — tablist DOM untouched.
    - `npm run typecheck` passes (the composer-side gates satisfy the NonNullable block props).
  </acceptance_criteria>
  <done>The events subtab (V3 production path) renders all 7 LLM blocks + FlightRecorder, each presence-gated to self-hide on absent data, with no fabricated zeros and the tablist DOM unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add DeadLinkBucketsBlock (D-09 — new block) + thread the prune prop into EventsFiltersSectionV3</name>
  <files>src/components/ui/DevApiStatus.tsx</files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx — `DlqBlock` (~2933-2960) and `SuspectBlock` (~2968-2977) as the verbatim visual idiom for the new block; the existing inline prune render in the API-Health tab (~1985-2006: `Dead URL events: {opStatus.prune.deadUrlCount}` + sample rows) for the authoritative-total + drill-down copy idiom; the `relativeTime()` helper (~72); the chip class idiom `rounded px-1 text-[9px] font-bold uppercase tracking-wider` (UI-SPEC cites `~1784`); the render switch (~916-920: `schemaVersion === 'v2' ? <EventsFiltersSection.../> : <EventsFiltersSectionV3 llmStatus={llmStatus} />`); the `OperatorStatus.prune` interface (now widened by Plan 01, ~1111-1119) for the prop type.
    - .planning/phases/44-events-subtab-pipeline-detail/44-UI-SPEC.md — §"DeadLinkBucketsBlock internal layout" (4-tier layout) + §"Color" (status→color map) + §"Copywriting Contract" (the `of N scanned` caveat wording, the `dead ×N` attemptCount framing, the `Dead URL events: 0` empty copy, evidence-as-TEXT).
    - .planning/phases/44-events-subtab-pipeline-detail/44-PATTERNS.md — §"New DeadLinkBucketsBlock idiom" + §"Prune-prop threading (D-10)".
    - .planning/phases/43-ghost-link-prune-correctness/43-CONTEXT.md — the 7-status taxonomy + evidence semantics + attemptCount-as-dead-streak meaning this block must render honestly.
  </read_first>
  <action>
    Add a new local `function DeadLinkBucketsBlock({ prune }: { prune: NonNullable<OperatorStatus['prune']> })` next to the other block components (sibling-function idiom). Layout (UI-SPEC §"DeadLinkBucketsBlock internal layout"):
    1. Header `Dead Links` — `text-[9px] font-bold uppercase tracking-wider text-white/40`.
    2. Authoritative total row: `Dead URL events: {prune.deadUrlCount}` plus a `· pruned {prune.last24hPrunes} in 24h` segment. `deadUrlCount` is authoritative (sidecar) — NOT the summed buckets. When `deadUrlCount === 0`, render `Dead URL events: 0` (mirror the existing line) and skip the bucket/sample rows.
    3. Per-status bucket rows from `prune.countsByStatus` (guard `prune.countsByStatus ?? {}` — forward-compat optional): one row per present status, each with a status badge (semantic color per the map below) + the count, labeled with the sampled-tally caveat `of N scanned` where N is the total scanned (sum of all bucket values). NEVER present this tally as authoritative.
    4. Sample drill-down rows from `prune.deadUrlSample` (≤20): each row = status badge · truncated `url` (use `truncate`) · `evidence` rendered as a PLAIN TEXT node (never `dangerouslySetInnerHTML` — T-43-16/D-11) · `relativeTime(lastProbedAt)` (guard for the optional field) · `dead ×{attemptCount}` dead-streak label (D-02 — attemptCount is the honest transition proxy; do NOT imply a true firstSeenDead timestamp). Wrap the list in `max-h-32 overflow-y-auto`.

    Status→color map (mirror the existing semantic ladder): `live` → green (`text-green-300`), `unknown` → amber (`text-amber-300`), `no-url` → muted (`text-white/40`), and `404`/`403`/`dead-host`/`soft-404` → red (`text-red-300`). Reuse the existing chip class string idiom (`rounded px-1 text-[9px] font-bold uppercase tracking-wider`) for the per-status badge — do NOT author a new chip component, do NOT touch `STATUS_PILL_CLASSES` (that map is keyed on `HealthStatus`, not liveness status).

    Thread the prop: change `EventsFiltersSectionV3`'s signature to `function EventsFiltersSectionV3({ llmStatus, prune }: { llmStatus: LLMStatus; prune?: OperatorStatus['prune'] | null })`. Inside the composer, self-hide on absence: `{prune && <DeadLinkBucketsBlock prune={prune} />}` (degrade-open D-10). At the render switch (~916-920), pass the already-fetched object down: `<EventsFiltersSectionV3 llmStatus={llmStatus} prune={opStatus?.prune ?? null} />`. No second fetch, no new hook — `opStatus` is already fetched once at top level.

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/DevApiStatus.prune.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function DeadLinkBucketsBlock" src/components/ui/DevApiStatus.tsx` matches; the component renders `prune.deadUrlCount`, `prune.countsByStatus`, and `prune.deadUrlSample`.
    - `grep -c "dangerouslySetInnerHTML" src/components/ui/DevApiStatus.tsx` returns 0 (evidence and all Redis strings render as TEXT — T-43-16/D-11).
    - `grep -n "prune=\{opStatus" src/components/ui/DevApiStatus.tsx` shows the prune prop threaded at the render switch.
    - `EventsFiltersSectionV3` signature includes the optional `prune` prop; the block is wrapped in `{prune && ...}` self-hide.
    - `grep -n "of N scanned\|of \${\|scanned" src/components/ui/DevApiStatus.tsx` shows the sampled-tally caveat label on the bucket rows.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The events subtab shows a degrade-open `DeadLinkBucketsBlock`: authoritative `deadUrlCount`, sampled per-status buckets labeled "of N scanned", and drill-down rows with evidence as TEXT — fed by the threaded `prune` prop, self-hiding when absent.</done>
</task>

<task type="auto">
  <name>Task 3: Evolve the two events-section test pins in lockstep (D-12 test evolution); keep the 5 pinning suites green untouched</name>
  <files>src/__tests__/DevApiStatusV3.test.tsx, src/__tests__/devApiStatusEventsSection.test.tsx, src/__tests__/components/DevApiStatus.prune.test.tsx</files>
  <read_first>
    - src/__tests__/DevApiStatusV3.test.tsx — the "renders all 6 v3 empty-state lines" test (~157-176) and its sibling assertions: line 171 asserts `'No LLM calls yet.'` matches EXACTLY ONCE because `EventsFiltersSectionV3` did NOT render `CallLogBlock`. Mounting `CallLogBlock` (now present, not array-empty) changes this — read the exact current assertions to evolve them honestly.
    - src/__tests__/devApiStatusEventsSection.test.tsx — existing events-section render pins (~221-382): the harness/mock-status pattern for rendering the events tab.
    - src/__tests__/components/DevApiStatus.prune.test.tsx — existing prune UI pin (~145-292): how prune fixtures are supplied to the rendered component; extend it for `DeadLinkBucketsBlock`.
    - .planning/phases/44-events-subtab-pipeline-detail/44-RESEARCH.md — Pitfall 1 (the exact contradiction the new mounts create) + §"Test files" guidance in 44-PATTERNS.md.
  </read_first>
  <action>
    Test EVOLUTION (authorized by D-12 for these two events-section files — NOT contract breakage). Do NOT touch the 5 pinning suites.

    (a) `DevApiStatusV3.test.tsx` (Pitfall 1): Under an EMPTY `llmStatus` (the test's empty-fields case), the 7 v2 blocks now SELF-HIDE (presence-gated). Update the "6 v3 empty-state lines" test so it asserts: the v3-native empty-state lines still render; AND the v2 blocks are ABSENT when their fields are empty — specifically, do not assert a synthetic v2 empty line. If `CallLogBlock` now mounts with a present-but-empty `callHistory: []` in some fixture, account for its own empty copy honestly; if `callHistory` is undefined (self-hide), assert it is absent. Add a positive-path assertion (or extend an existing data-fed test): with a populated `llmStatus` (callHistory present), assert the mounted v2 blocks render their content. Keep the assertions honest — presence only when fed data, absence (not a fabricated zero line) when fields are empty.

    (b) `devApiStatusEventsSection.test.tsx`: Add/extend assertions for the new mounts and the FlightRecorder re-mount: with data-fed `llmStatus`, the mounted blocks appear; with empty `llmStatus`, they self-hide. Confirm degrade-open (no crash) on absent fields.

    (c) `DevApiStatus.prune.test.tsx`: Add `DeadLinkBucketsBlock` assertions — supply a `prune` fixture (with `countsByStatus`, `deadUrlSample` carrying `evidence`/`lastProbedAt`/`attemptCount`, mixed statuses incl. `soft-404`/`no-url`) and assert: bucket counts render with the "of N scanned" caveat; the authoritative `Dead URL events: {deadUrlCount}` line renders; sample rows render status badge + url + evidence + relative time + dead-streak; the block SELF-HIDES when `prune` is absent/null; and `evidence` is rendered as a TEXT node (assert the literal evidence string is present as text, and that no `innerHTML`/markup injection occurs — e.g. an evidence value containing `<b>` renders as the literal characters, not bold).

    Run the 5 pinning suites to confirm they stay green WITHOUT edits: `DevApiStatusConsolidatedLayout.snapshot.test.tsx`, `DevApiStatus.tabMerge.test.tsx`, `DevApiStatus.diagnosticBlocks.test.tsx`, `DevApiStatus.operatorActions.test.tsx`, and `DevApiStatus.prune.test.tsx`'s pre-existing assertions (only ADD to the prune suite; do not change existing prune assertions unless the new mounts genuinely require it — if a snapshot in the consolidated-layout suite captures the events subtab and legitimately changes, STOP and surface it rather than blanket-updating; the events subtab is a sibling render branch and should not be in the API-Health snapshot).

  </action>
  <verify>
    <automated>npx vitest run src/__tests__/DevApiStatusV3.test.tsx src/__tests__/devApiStatusEventsSection.test.tsx src/__tests__/components/DevApiStatus.prune.test.tsx src/components/ui/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/DevApiStatusV3.test.tsx` green with the evolved empty-state assertions (no longer asserting the old "CallLog absent / exactly-6-lines" contract that the mounts contradict).
    - `npx vitest run src/__tests__/devApiStatusEventsSection.test.tsx` green with the new-mount + FlightRecorder assertions.
    - `npx vitest run src/__tests__/components/DevApiStatus.prune.test.tsx` green, including the `DeadLinkBucketsBlock` bucket-count, sample-row, self-hide, and evidence-as-TEXT assertions.
    - The 5 pinning suites (`...ConsolidatedLayout.snapshot`, `...tabMerge`, `...diagnosticBlocks`, `...operatorActions`, plus pre-existing prune assertions) pass with NO edits to their existing assertions — `git diff --stat` shows no changes to the 4 pure-pinning suite files.
    - Full frontend suite green: `npx vitest run` exits 0.
  </acceptance_criteria>
  <done>The two events-section suites are evolved to reflect the presence-gated mounts + DeadLinkBucketsBlock (incl. evidence-as-TEXT), the new block is pinned, and the 5 pinning suites stay green untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                                                                         |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Redis-sourced strings → React render           | `evidence` and `url` originate from Redis (operator-uncontrolled content); a naive HTML render would be stored XSS                                                                  |
| Bearer-gated telemetry → prod-reachable subtab | The events subtab is reachable in production for an authed operator (`showEventsTab = DEV \|\| hasDashboardKey()`); rendered blocks must expose telemetry only, not secrets/prompts |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                     |
| --------- | ---------------------------------- | ---------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-44-04   | Tampering / Elevation (stored XSS) | `DeadLinkBucketsBlock` rendering `evidence`/`url` from Redis                 | mitigate    | Render every Redis-sourced string as a React TEXT node (default-escaped); NO `dangerouslySetInnerHTML` (D-11 / T-43-16, the server comment at `operator-status.ts:202` pins this). Acceptance criterion greps `dangerouslySetInnerHTML` count == 0 AND the prune test asserts a `<b>`-bearing evidence value renders as literal characters. `evidence` is server-capped ≤200 chars (Phase 43 D-16). |
| T-44-05   | Information Disclosure             | The 7 v2 blocks + FlightRecorder mounted in the prod-reachable events subtab | mitigate    | The 7 v2 blocks render telemetry only (counts, durations, provider names) — no prompt/response text. FlightRecorder is self-contained behind the existing Bearer-gated `/api/events/llm-history`. Lineage prompt/response copy stays inside the pre-existing, untouched `DrillDownRow` replay-button flow. No new raw-prompt rendering is added (Pitfall 5).                                        |
| T-44-06   | Spoofing / honest-signal integrity | `BudgetBarsBlock` under NIM-only; `countsByStatus` sampled tally             | mitigate    | Presence-gates self-hide `BudgetBarsBlock` rather than fabricating `cerebras:0/groq:0` (D-05/D-06 — the self-hide IS the correct outcome). The sampled `countsByStatus` is labeled "of N scanned" and never presented as authoritative; `deadUrlCount` (sidecar) stays the authoritative total (D-03).                                                                                              |
| T-44-07   | Denial of Service                  | events subtab render under absent/partial data                               | mitigate    | Every mount is presence-gated and degrade-open; `DeadLinkBucketsBlock` self-hides when `prune` is null (older server / fetch failure / missing Bearer). No crash, no fabricated data. Pinned by the self-hide test assertions.                                                                                                                                                                      |
| T-44-SC   | Tampering                          | npm/pip/cargo installs                                                       | mitigate    | No package installs in this plan (RESEARCH §Standard Stack: "No new libraries"; "Installation: None required"). No legitimacy checkpoint required.                                                                                                                                                                                                                                                  |

</threat_model>

<verification>
- `npx vitest run` (full frontend) green.
- `npm run typecheck` green.
- The 5 pinning suites green with no edits to their existing assertions (`git diff --stat` on the 4 pure-pinning files shows no change).
- `dangerouslySetInnerHTML` count remains 0 across `DevApiStatus.tsx`.
- Tablist DOM unchanged (single `role="tabpanel" aria-labelledby="tab-events"` occurrence).
- Manual (deferred to UAT per VALIDATION §Manual-Only): on the live prod dashboard with a Bearer, the Events subtab shows the mounted blocks with real Redis data, and `BudgetBarsBlock` honestly self-hides under NIM-only (record this expected self-hide, not a defect — D-06).
</verification>

<success_criteria>

- The 7 LLM blocks + FlightRecorder render inside `EventsFiltersSectionV3`, each presence-gated and degrade-open.
- `DeadLinkBucketsBlock` shows authoritative `deadUrlCount` + sampled per-status buckets ("of N scanned") + drill-down rows (evidence as TEXT, `relativeTime(lastProbedAt)`, dead-streak `attemptCount`); self-hides when `prune` absent.
- The WAI-ARIA tablist DOM is unchanged; the 5 pinning suites stay green untouched; the two events-section suites are evolved in lockstep.
- No `dangerouslySetInnerHTML`; no synthetic zero-defaults; no second `/api/operator-status` fetch.
  </success_criteria>

<output>
Create `.planning/phases/44-events-subtab-pipeline-detail/44-02-SUMMARY.md` when done.
</output>

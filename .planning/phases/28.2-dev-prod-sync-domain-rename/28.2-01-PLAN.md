---
phase: 28.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/load-test.js
  - scripts/load-test.spec.ts
  - CHANGELOG.md
  - docs/runbook.md
  - memory/reference_deployment.md
autonomous: false
requirements:
  - D-03
  - D-01
  - D-02
must_haves:
  truths:
    - 'Every confirmed `irt-monitoring.vercel.app` literal in the repo is replaced with `otg-iran-monitor.vercel.app` (5 confirmed occurrences)'
    - "scripts/load-test.js BASE_URL default points to the new domain so 28.3's k6 sweep targets the right deployment"
    - 'scripts/load-test.spec.ts PROD_URL default points to the new domain (Playwright validation will hit the renamed deploy)'
    - 'Precautionary file list (vercel.json, package.json, README.md, PROJECT_SPEC.md, PROJECT_STATUS.md, .planning/PROJECT.md, CLAUDE.md) is verified clean — zero post-substitution occurrences anywhere'
    - 'memory/reference_deployment.md operator memory reflects the new production URL'
  artifacts:
    - path: scripts/load-test.js
      provides: 'Updated BASE_URL default (https://otg-iran-monitor.vercel.app)'
      contains: 'otg-iran-monitor.vercel.app'
    - path: scripts/load-test.spec.ts
      provides: 'Updated PROD_URL default for Playwright validation'
      contains: 'otg-iran-monitor.vercel.app'
    - path: CHANGELOG.md
      provides: 'Historical Production URL note updated'
      contains: 'otg-iran-monitor.vercel.app'
    - path: docs/runbook.md
      provides: 'Operator runbook references the new domain'
      contains: 'otg-iran-monitor.vercel.app'
    - path: memory/reference_deployment.md
      provides: 'Auto-memory deployment URL refreshed'
      contains: 'otg-iran-monitor.vercel.app'
  key_links:
    - from: scripts/load-test.js
      to: 'https://otg-iran-monitor.vercel.app'
      via: 'BASE_URL default'
      pattern: "BASE_URL.*otg-iran-monitor\\.vercel\\.app"
    - from: scripts/load-test.spec.ts
      to: 'https://otg-iran-monitor.vercel.app'
      via: 'PROD_URL default'
      pattern: "PROD_URL.*otg-iran-monitor\\.vercel\\.app"
---

<objective>
Domain rename per CONTEXT D-03: change every code/doc reference from `irt-monitoring.vercel.app` to `otg-iran-monitor.vercel.app`. This is W1 — the smallest-blast-radius wave — and lands first so downstream waves (and 28.3's load test) target a coherent deployment URL.

Purpose: 28.3's k6 sweep depends on `scripts/load-test.js:18` BASE_URL pointing at the live domain (CONTEXT.md `<canonical_refs>` "Domain rename targets"). The W6 connectivity audit (Plan 06) explicitly runs against `https://otg-iran-monitor.vercel.app` per D-25 — it cannot land before this plan ships. Old-domain redirect mechanic (Vercel dashboard alias / `vercel:deployments-cicd` skill) is captured as a checkpoint for the operator to handle out-of-band — file-edit work in this plan is purely string substitutions.

Output:

- 5 atomic per-file commits (one substitution per file per CONTEXT "atomic per-concern commits" rule)
- 1 audit checkpoint where the operator confirms the precautionary file list is clean and decides on the Vercel-side redirect strategy
- Rebased / clean git tree ready to merge into the 28.2 feature branch
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The five files in `files_modified` are all simple string-substitution targets.
     No types, no imports, no contracts. The only "interface" is the literal URL
     string. -->

scripts/load-test.js:18 — `const BASE_URL = __ENV.BASE_URL || 'https://irt-monitoring.vercel.app';`
scripts/load-test.spec.ts:16 — `const PROD_URL = process.env.PROD_URL ?? 'https://irt-monitoring.vercel.app';`
CHANGELOG.md:78 — `#### Results (Production — https://irt-monitoring.vercel.app)`
docs/runbook.md:509 — `https://irt-monitoring.vercel.app`
memory/reference_deployment.md — `**Production URL**: irt-monitoring.vercel.app`
`**Project**: zack-mazs-projects/irt-monitoring on Vercel`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide audit — confirm or reject the precautionary file list</name>
  <files>
    (read-only audit; produces NO file changes)
  </files>
  <read_first>
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md (the "Domain Rename String Substitutions — D-03" table)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (`<canonical_refs>` "Domain rename targets" list)
  </read_first>
  <action>
    Run a single repo-wide grep (excluding `node_modules`, `dist`, `dist-server`, `.git`, `.planning/phases/`) to enumerate every literal occurrence of `irt-monitoring`. Use ripgrep via the Grep tool:

    ```
    Grep pattern: irt-monitoring
    glob: !**/{node_modules,dist,dist-server,.git}/**
    output_mode: content
    -n: true
    ```

    Expected matches (per PATTERNS.md audit, 2026-05-04):
    1. scripts/load-test.js:18
    2. scripts/load-test.spec.ts:16
    3. CHANGELOG.md:78
    4. docs/runbook.md:509
    5. memory/reference_deployment.md (multiple lines)

    If grep returns ANY additional file (e.g. README.md, vercel.json, package.json, PROJECT_SPEC.md, PROJECT_STATUS.md, .planning/PROJECT.md, CLAUDE.md), append it to a working list. The PATTERNS.md audit asserts those files have ZERO occurrences — a new finding means the audit is stale and the planner's blast-radius estimate is wrong; add the file as a 6th task and surface as a HANDOFF note. If grep matches exactly the 5 expected files (and zero extras), proceed.

    Also verify the `.planning/phases/` exclusion is correct: the rename literal MAY appear in older phase summaries (28.1 / 27.x) as historical record; those are NOT to be edited (re-writing history is anti-pattern). The exclusion `!**/.planning/phases/**` keeps grep noise out of the working list.

    Output: a numbered list of (file, line, surrounding context) committed as a working note in the executor's session log. No file is modified by this task.

  </action>
  <verify>
    <automated>! grep -rn "irt-monitoring" --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=dist-server --exclude-dir=.git --exclude-dir=.planning/phases . | grep -v "^Binary file" | awk -F: '{print $1}' | sort -u | grep -v "^scripts/load-test\.js$\|^scripts/load-test\.spec\.ts$\|^CHANGELOG\.md$\|^docs/runbook\.md$\|^memory/reference_deployment\.md$" | head -1</automated>
  </verify>
  <done>
    The grep output exactly matches the 5 expected files (or, if it does not, a HANDOFF note is captured listing extras for inclusion as Task 6+). Zero unexpected file paths appear in the audit list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Substitute scripts/load-test.js BASE_URL (load-bearing for 28.3 k6 sweep)</name>
  <files>scripts/load-test.js</files>
  <read_first>
    - scripts/load-test.js (full file — context for the env-default pattern; this is the analog scripts/load-test.spec.ts mirrors)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md (Domain Rename section, suggested commit train item 1)
  </read_first>
  <action>
    Edit scripts/load-test.js line 18 ONLY. Replace the literal `irt-monitoring.vercel.app` with `otg-iran-monitor.vercel.app`. Preserve the surrounding `__ENV.BASE_URL ||` env-default pattern verbatim (this pattern is referenced by D-29 — the new connectivity smoke suite mirrors it).

    Before:
    ```js
    const BASE_URL = __ENV.BASE_URL || 'https://irt-monitoring.vercel.app';
    ```

    After (exact target):
    ```js
    const BASE_URL = __ENV.BASE_URL || 'https://otg-iran-monitor.vercel.app';
    ```

    Commit message:
    ```
    feat(28.2): rename load-test.js BASE_URL to otg-iran-monitor.vercel.app

    Per CONTEXT D-03 — Phase 28.3 k6 sweep depends on this default
    pointing at the live deployment.
    ```

  </action>
  <verify>
    <automated>grep -n "otg-iran-monitor.vercel.app" scripts/load-test.js && ! grep -n "irt-monitoring" scripts/load-test.js</automated>
  </verify>
  <done>
    Line 18 contains `otg-iran-monitor.vercel.app`. The string `irt-monitoring` does NOT appear anywhere in the file. Commit landed atomically. `npm run lint` clean (script files are excluded from lint, but verify no errors introduced).
  </done>
</task>

<task type="auto">
  <name>Task 3: Substitute scripts/load-test.spec.ts PROD_URL</name>
  <files>scripts/load-test.spec.ts</files>
  <read_first>
    - scripts/load-test.spec.ts (full file — small Playwright validation harness)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md (commit train item 2)
  </read_first>
  <action>
    Edit scripts/load-test.spec.ts line 16 ONLY. Replace the literal `irt-monitoring.vercel.app` with `otg-iran-monitor.vercel.app`. Preserve the `process.env.PROD_URL ??` env-default pattern verbatim.

    Before:
    ```ts
    const PROD_URL = process.env.PROD_URL ?? 'https://irt-monitoring.vercel.app';
    ```

    After (exact target):
    ```ts
    const PROD_URL = process.env.PROD_URL ?? 'https://otg-iran-monitor.vercel.app';
    ```

    Commit message:
    ```
    feat(28.2): rename load-test.spec.ts PROD_URL to otg-iran-monitor.vercel.app

    Per CONTEXT D-03 — Playwright validation follows BASE_URL.
    ```

  </action>
  <verify>
    <automated>grep -n "otg-iran-monitor.vercel.app" scripts/load-test.spec.ts && ! grep -n "irt-monitoring" scripts/load-test.spec.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "scripts/load-test\.spec\.ts" | head -1 || true</automated>
  </verify>
  <done>
    Line 16 contains `otg-iran-monitor.vercel.app`. `npx tsc --noEmit` exits 0 (no new TS errors introduced). The string `irt-monitoring` does NOT appear in the file. Commit landed atomically.
  </done>
</task>

<task type="auto">
  <name>Task 4: Substitute CHANGELOG.md, docs/runbook.md, memory/reference_deployment.md</name>
  <files>
    CHANGELOG.md
    docs/runbook.md
    memory/reference_deployment.md
  </files>
  <read_first>
    - CHANGELOG.md (read line 78 + ±5 lines context)
    - docs/runbook.md (read line 509 + ±5 lines context)
    - memory/reference_deployment.md (full file — small auto-memory entry)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md (commit train items 3-5)
  </read_first>
  <action>
    Three atomic commits — one per file. Order: CHANGELOG → runbook → memory (matches PATTERNS.md commit train).

    **Commit A — CHANGELOG.md:**

    Edit CHANGELOG.md line 78. Replace `https://irt-monitoring.vercel.app` with `https://otg-iran-monitor.vercel.app` in the Production URL note. PRESERVE all surrounding markdown / heading / date context verbatim. NO other lines edited.

    Commit message:
    ```
    docs(28.2): update CHANGELOG.md production URL to otg-iran-monitor.vercel.app

    Per CONTEXT D-03 — historical record updated to match deployed domain.
    ```

    **Commit B — docs/runbook.md:**

    Edit docs/runbook.md line 509. Replace `https://irt-monitoring.vercel.app` with `https://otg-iran-monitor.vercel.app`. PRESERVE all surrounding operator-instruction text verbatim.

    Commit message:
    ```
    docs(28.2): update runbook production URL to otg-iran-monitor.vercel.app

    Per CONTEXT D-03 — operator instructions point to the live deploy.
    ```

    **Commit C — memory/reference_deployment.md:**

    Edit memory/reference_deployment.md. Replace EVERY occurrence of:
    - `irt-monitoring.vercel.app` → `otg-iran-monitor.vercel.app`
    - `zack-mazs-projects/irt-monitoring` → `zack-mazs-projects/otg-iran-monitor`

    Preserve all surrounding "Production URL", "Project", `vercel --prod` deployment-checklist context verbatim.

    Commit message:
    ```
    docs(28.2): refresh deployment auto-memory for otg-iran-monitor.vercel.app

    Per CONTEXT D-03 — auto-memory now reflects the renamed Vercel project.
    ```

  </action>
  <verify>
    <automated>grep -l "otg-iran-monitor.vercel.app" CHANGELOG.md docs/runbook.md memory/reference_deployment.md && ! grep -l "irt-monitoring" CHANGELOG.md docs/runbook.md memory/reference_deployment.md</automated>
  </verify>
  <done>
    All three files contain `otg-iran-monitor.vercel.app`. None contain `irt-monitoring`. Three atomic commits landed in the order CHANGELOG → runbook → memory. `git log --oneline` shows three distinct `docs(28.2):` commits.
  </done>
</task>

<task type="auto">
  <name>Task 5: Repo-wide post-substitution verification sweep</name>
  <files>
    (read-only audit; produces NO file changes — gates Task 6 checkpoint)
  </files>
  <read_first>
    Task 1 working note (the original audit list).
  </read_first>
  <action>
    Re-run the same grep used in Task 1:

    ```
    Grep pattern: irt-monitoring
    glob: !**/{node_modules,dist,dist-server,.git,.planning/phases}/**
    ```

    Expected: ZERO matches.

    If any match remains, identify the file and either (a) substitute it as a 6th atomic commit if it is clearly an unsurfaced literal occurrence, OR (b) capture it in HANDOFF if it is a structural reference (e.g., a Vercel project ID in a config that expects the old slug — that would imply Vercel-side dashboard work which is the Task 6 checkpoint's domain).

    The .planning/phases/ exclusion remains in place — historical phase summaries are immutable.

  </action>
  <verify>
    <automated>! grep -rn "irt-monitoring" --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=dist-server --exclude-dir=.git --exclude-dir=.planning/phases .</automated>
  </verify>
  <done>
    The grep returns no output (exit code 1 from `grep -r` when no matches = success per the `! grep …` pattern). Zero unintentional `irt-monitoring` literals remain in editable repo files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Operator decides Vercel-side redirect strategy</name>
  <files>
    (operator-driven Vercel dashboard step — no repo file edits in this task)
  </files>
  <action>
    Operator-driven Vercel dashboard procedure (cannot be Claude-automated without dashboard credentials). The full step-by-step lives in <how-to-verify> below; the operator picks Option A (in-place rename), Option B (added alias), or Option C (vercel.json redirect) and records the choice in the executor session log for SUMMARY inclusion.
  </action>
  <verify>
    <automated>curl -sI https://otg-iran-monitor.vercel.app/api/health | head -1 | grep -E "^HTTP/[12]\.[0-9]? 200"</automated>
  </verify>
  <done>
    The new prod URL responds 200 on /api/health. The chosen old-domain strategy (rename-in-place / added-alias / vercel-json-redirect) is captured in the executor session log for SUMMARY inclusion.
  </done>
  <what-built>
    All five repo-side string substitutions are committed. The local repo is consistent with the new domain. What remains is a Vercel-dashboard-side decision about the OLD `irt-monitoring.vercel.app` URL: hard cutover (delete the alias; users on the old URL get DNS-fail) vs. permanent redirect (Vercel `redirects` rule in vercel.json mapping old domain → new domain).

    Per CONTEXT D-03: "Old-domain redirect strategy (hard cutover vs. permanent redirect) is Claude's discretion at planning time." This planner defers to the operator because the choice depends on:
    - Whether external dashboards / bookmarks / archived links point at the old URL
    - Vercel free-tier alias quota (renaming on the same project keeps the existing alias slot; deleting + re-adding may reset)
    - 28.3 load-test target — k6 sweeps the renamed domain only; the old URL is not in scope for performance metrics

  </what-built>
  <how-to-verify>
    1. Open the Vercel dashboard for `zack-mazs-projects/irt-monitoring` (the existing project).
    2. Confirm whether the project itself can be renamed to `otg-iran-monitor` in-place, OR whether a new project is required (Vercel docs: project rename is supported for non-Pro tiers via Settings → General → Project Name).
    3. Pick redirect strategy:
       - **Option A — In-place rename:** Settings → Project Name → `otg-iran-monitor`. The existing `*.vercel.app` alias auto-updates. Old URL returns 404 after DNS propagation (~5 min).
       - **Option B — Add alias:** Keep `irt-monitoring.vercel.app` as a permanent alias on the renamed project. Old links resolve indefinitely; serves identical content.
       - **Option C — vercel.json redirect:** Add a `redirects` rule mapping the old domain → new domain (HTTP 308). More flexible but only fires AFTER the request reaches the project — requires the alias to still resolve. Most operator-friendly for hard-link compatibility.
    4. Execute chosen strategy in the Vercel dashboard. Note the choice in the executor's session log for inclusion in the SUMMARY file.
    5. Verify post-action: `curl -I https://otg-iran-monitor.vercel.app/api/health` returns 200; `curl -I https://irt-monitoring.vercel.app/api/health` returns the expected status code (404 for Option A, 200 for B, 308 + Location header for C).
  </how-to-verify>
  <resume-signal>
    Type one of: "rename-in-place", "added-alias", "vercel-json-redirect", or describe the chosen path. The SUMMARY file will record the decision so 28.3's k6 sweep + Plan 06's connectivity audit can pin their target URL with full context.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Repo → Vercel deploy   | Domain literal in scripts/load-test.js becomes the k6 target — wrong literal sends synthetic load to the wrong host        |
| Repo → operator memory | memory/reference_deployment.md is read by Claude in future sessions; stale URL = wrong operator guidance for future phases |

## STRIDE Threat Register

| Threat ID    | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                    |
| ------------ | ---------------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-28.2-01-01 | Tampering              | scripts/load-test.js BASE_URL           | mitigate    | Atomic per-file commit + grep-verify before commit; CONTEXT D-03 lists the exact target string verbatim, eliminating typo risk                                     |
| T-28.2-01-02 | Information Disclosure | docs/runbook.md operator runbook        | accept      | Domain literal is public-by-nature — Vercel `*.vercel.app` URLs are not secrets; rename is intentional and documented in CHANGELOG                                 |
| T-28.2-01-03 | Denial of Service      | k6 load test (28.3 hand-off)            | mitigate    | If the rename ships incomplete, scripts/load-test.js still points at irt-monitoring; that risk is closed by Task 5's repo-wide grep verification gate              |
| T-28.2-01-04 | Repudiation            | git history of rename                   | accept      | Five atomic per-file commits with `feat(28.2):` / `docs(28.2):` prefixes provide full bisect-friendly traceability — no need for sign-off log                      |
| T-28.2-01-05 | Spoofing               | Old-domain alias resolution post-rename | mitigate    | Task 6 checkpoint forces the operator to make an explicit choice about the old URL's fate — silent dangling alias would let stale bookmarks resolve to a dead host |

No Elevation-of-Privilege threats — this plan touches no auth surfaces, no permissions logic, no rate-limit code paths.
</threat_model>

<verification>
- All 5 confirmed `irt-monitoring.vercel.app` literals replaced with `otg-iran-monitor.vercel.app`
- Repo-wide grep for `irt-monitoring` returns 0 matches in editable files (excluding `.planning/phases/`)
- `npx tsc --noEmit` exits 0 (no new TS errors introduced by the Playwright spec edit)
- 5 atomic commits land in the order: load-test.js → load-test.spec.ts → CHANGELOG.md → runbook.md → reference_deployment.md
- Task 6 checkpoint records the operator's chosen Vercel-side strategy in SUMMARY
- `npm run lint` exits 0; `prettier --check .` exits 0 (string substitutions don't break formatting)
</verification>

<success_criteria>

- `grep -r 'irt-monitoring' --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=dist-server --exclude-dir=.git --exclude-dir=.planning/phases .` returns ZERO matches
- `grep -l 'otg-iran-monitor.vercel.app' scripts/load-test.js scripts/load-test.spec.ts CHANGELOG.md docs/runbook.md memory/reference_deployment.md` lists all 5 files
- `git log --oneline -n 5 | grep -E '\(28\.2\)' | wc -l` returns 5
- 28.3's k6 BASE_URL default and Playwright PROD_URL default both point at the new domain
- Task 6 SUMMARY captures the Vercel-side redirect choice (rename-in-place / added-alias / vercel-json-redirect)
  </success_criteria>

<output>
After completion, create `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-01-SUMMARY.md` recording:
- 5 commit SHAs in chronological order
- The exact line-level diff for each substitution (so a future bisect can grep for the change)
- The Vercel-side decision from Task 6 (rename-in-place / alias / vercel.json redirect) — load-bearing for Plan 06's connectivity audit target URL
- Confirmation that `irt-monitoring` returns 0 matches in the post-substitution sweep
</output>

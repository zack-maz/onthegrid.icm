---
phase: 28.2
plan: 06
type: execute
wave: 6
depends_on:
  - 01
  - 02
  - 03
  - 04
  - 05
files_modified:
  - src/__tests__/api-connectivity.test.ts
  - src/__tests__/rate-limit.test.ts
  - .github/workflows/prod-connectivity-audit.yml
  - server/routes/audit-status.ts
  - server/app.ts
  - src/components/ui/DevApiStatus.tsx
  - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md
autonomous: false
requirements:
  - D-24
  - D-25
  - D-28
  - D-29
  - D-30
must_haves:
  truths:
    - 'Vitest smoke suite at `src/__tests__/api-connectivity.test.ts` probes every adapter from `PROBE_STRATEGIES` (16 endpoints) and asserts ≥1 successful fetch with non-empty body for each'
    - 'Smoke suite uses `API_BASE_URL` env var, default `http://localhost:5173`; CI workflow sets `API_BASE_URL=https://otg-iran-monitor.vercel.app` from a GitHub Actions secret'
    - 'Companion test `src/__tests__/rate-limit.test.ts` sends one unauthenticated burst over 60s and asserts ≥1 request returns 429 — codifies the public-tier still-defends regression'
    - "Connectivity audit attaches `Authorization: Bearer ${DASHBOARD_PASSWORD}` to every probe (D-30) — exercises Plan 02's Bearer-bypass end-to-end so the suite hits all 16 endpoints in seconds without 429s"
    - 'Critical-tier banner verification: HealthBanner fires and remains visible during a synthetic critical-tier outage (integration trace adds to existing HealthBanner.test.tsx unit coverage)'
    - 'Manual audit walks every adapter under prod conditions, fixes any that are broken/stale, and produces the W6-AUDIT.md report committed to the planning dir'
    - 'Audit runs against the deployed prod URL `https://otg-iran-monitor.vercel.app` (D-25) post-Plan-01 domain rename; local-dev pre-flight is acceptable but the gating evidence is the prod-URL run'
    - 'Sidecar Redis key `audit:connectivity:last-result` is populated by the CI workflow and surfaced to the merged tab via either /api/health extension or a new /api/audit-status route (planner picks)'
    - 'GitHub Actions workflow `.github/workflows/prod-connectivity-audit.yml` runs the smoke suite against prod with the secret-loaded API_BASE_URL + DASHBOARD_PASSWORD; manual-trigger by default, schedulable later'
  artifacts:
    - path: src/__tests__/api-connectivity.test.ts
      provides: '16-endpoint connectivity smoke suite'
      contains: 'API_BASE_URL'
    - path: src/__tests__/rate-limit.test.ts
      provides: 'Public-tier throttle regression — 70-request unauthenticated burst → ≥1 returns 429'
      contains: 'Promise.all'
    - path: .github/workflows/prod-connectivity-audit.yml
      provides: 'Manual-trigger workflow that runs smoke suite against prod URL with secrets'
      contains: 'otg-iran-monitor.vercel.app'
    - path: server/routes/audit-status.ts
      provides: 'GET /api/audit-status returning sidecar Redis key audit:connectivity:last-result'
      contains: 'audit:connectivity:last-result'
    - path: .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md
      provides: 'Manual audit report — per-endpoint pass/fail + remediation notes'
      contains: '16 endpoints'
  key_links:
    - from: src/__tests__/api-connectivity.test.ts
      to: PROBE_STRATEGIES
      via: 'endpoint enumeration'
      pattern: '/api/(flights|ships|events|news|markets|weather|sites|water|sources|geocode|health|dashboard/auth-check)'
    - from: src/__tests__/api-connectivity.test.ts
      to: dashboardAuthHeaders
      via: 'Bearer attached to every probe'
      pattern: 'Authorization: `Bearer '
    - from: .github/workflows/prod-connectivity-audit.yml
      to: api-connectivity.test.ts + rate-limit.test.ts
      via: 'two-pass run with secrets'
      pattern: 'API_BASE_URL.*otg-iran-monitor'
    - from: server/routes/audit-status.ts
      to: src/components/ui/DevApiStatus.tsx
      via: 'merged tab reads `audit:connectivity:last-result` to populate banner'
      pattern: 'audit:connectivity'
---

<objective>
Per CONTEXT D-24/D-25/D-28/D-29/D-30 + AI-SPEC §6/§7: deliver the three-pronged connectivity guarantee that gates Phase 28.2's close.

(a) **Manual audit + fix** — walk every adapter (16 endpoints from `PROBE_STRATEGIES` in `server/routes/health.ts:227` plus underlying upstream sources). Verify a fresh fetch lands non-empty data. Fix any that are broken/stale. Produce `28.2-W6-AUDIT.md` documenting per-endpoint pass/fail + remediation notes.

(b) **Vitest smoke suite** — `src/__tests__/api-connectivity.test.ts` hits every adapter and asserts ≥1 successful fetch with non-empty body. Single suite, two environments (`API_BASE_URL` env-var, default localhost). Mirrors `scripts/load-test.js` BASE_URL pattern (already updated to `otg-iran-monitor.vercel.app` by Plan 01).

(c) **Critical-banner verification** — confirm HealthBanner actually fires and remains visible during a critical-tier outage. Add an integration trace beyond the existing `HealthBanner.test.tsx` unit coverage. Add the audit-result banner population path: a sidecar Redis key (`audit:connectivity:last-result`) updated by the CI workflow + a new server route to expose it to the merged tab (Plan 05 left a placeholder for this).

Plus the **D-30 companion test**: `src/__tests__/rate-limit.test.ts` sends an unauthenticated burst and asserts ≥1 request returns 429. Captures the regression where Plan 02's Bearer-bypass might accidentally widen the bypass to anonymous traffic. Two named test files, two atomic commits per CONTEXT "<specifics> D-30 two-pass audit".

This is W6 of 6 — the wave-close gate per D-25/D-28. Depends on ALL prior plans:

- Plan 01 must ship for the prod URL to be the renamed domain
- Plan 02 must ship for the audit suite (+Bearer) to hit all 16 endpoints in seconds without 429s on the global tier
- Plan 03 must ship for the audit's Bearer-attached `/llm-pipeline` + `/llm-replay` probes to not 401
- Plan 04 must ship so the gate-swap surfaces are testable in their final form
- Plan 05 must ship for the merged tab's audit-result banner placeholder to receive its data feed

The CONTEXT.md user directive is explicit: **"make sure it's working on PROD, that's where it matters."** Local-dev passing is acceptable as a smoke check, but the wave-close gate is the prod-URL run captured in W6-AUDIT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-AI-SPEC.md
@CLAUDE.md
@server/routes/health.ts
@server/lib/healthSources.ts
@scripts/load-test.js
@src/lib/dashboardAuth.ts

<interfaces>
The 16 endpoints to probe are enumerated in `server/routes/health.ts:227` `PROBE_STRATEGIES`. Read that file to capture the canonical list. Per Phase 28.1 W2 closeout, the 16 endpoints span:
- Critical tier: `/api/flights`, `/api/ships`, `/api/events`, `/api/news`, `/api/markets`, `/api/weather`, `/api/sites`, `/api/water`
- Non-critical / config: `/api/sources`, `/api/geocode`, `/api/water/precip`
- Aggregate / dashboard: `/api/health`, `/api/dashboard/auth-check`
- Cron-tier (probe-only via /api/health summary): `/api/cron/refresh-events`, `/api/cron/health`, `/api/cron/warm`

Confirm the exact list in Task 1 by reading PROBE_STRATEGIES. The plan refers to "16 endpoints" but the executor MUST cite the exact list from source.

Existing patterns to mirror:

- `scripts/load-test.js:18` (post-Plan-01) → `const BASE_URL = __ENV.BASE_URL || 'https://otg-iran-monitor.vercel.app';`
- `server/middleware/dashboardAuth.test.ts:23-37` — env-toggle pattern for tests that depend on `NODE_ENV` / `DASHBOARD_PASSWORD`
- `server/__tests__/resilience/redis-death.test.ts` — supertest pattern for in-process Express tests; useful for the audit-status route test
- `src/lib/dashboardAuth.ts` `dashboardAuthHeaders()` — emits `Authorization: Bearer ${key}` headers; reused for D-30 audit Bearer attachment

Sidecar Redis key for the audit-result banner:

- Key: `audit:connectivity:last-result`
- Value: JSON `{lastVerifiedAt: ISO timestamp, totalEndpoints: 16, healthy: number, failing: string[], status: 'clean' | 'failing' | 'absent'}`
- TTL: 7 days (long enough that a stale audit doesn't silently mask a regression; short enough that the banner self-clears if the workflow stops running)
- Reader: new `GET /api/audit-status` route (Claude-discretion option per CONTEXT D-23 sparkline), OR an extension to `/api/health` (cheaper but couples concerns).
- Writer: GitHub Actions workflow + a small CLI invocation in the smoke-suite that POSTs the result to the new endpoint, OR a direct redis write from the CI runner using the Upstash REST API.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 16-endpoint canonical list from PROBE_STRATEGIES</name>
  <files>
    .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md
  </files>
  <read_first>
    - server/routes/health.ts (lines 220-300 — PROBE_STRATEGIES + buildSummary; capture the EXACT endpoint list)
    - server/lib/healthSources.ts (TIER_BY_ENDPOINT — capture the tier classification for each endpoint)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (D-24 connectivity-guarantee shape)
  </read_first>
  <action>
    Create `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md` as the audit-report scaffold. Initialize with:

    1. A header block: phase, wave, started-at timestamp, target URL.
    2. The CANONICAL ENDPOINT TABLE with one row per `PROBE_STRATEGIES` entry: `endpoint name | URL path | tier (critical/non-critical/cron) | upstream source | freshness threshold ms`. Source values from healthSources.ts directly.
    3. EMPTY columns for: `local pass | prod pass | remediation notes`. Filled progressively during Tasks 4 + 7 + 8.

    Do NOT modify the source code in this task — the goal is a frozen-at-W6-start audit baseline so all subsequent task outputs land in one document.

    Atomic commit message: `docs(28.2): scaffold W6 connectivity audit report`. Body: enumerates the 16 endpoints from PROBE_STRATEGIES + tier classification.

  </action>
  <verify>
    <automated>grep -c "/api/" .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md | grep -E "^[1-9][0-9]*"</automated>
  </verify>
  <done>
    The audit doc lists every endpoint from PROBE_STRATEGIES (≥10 unique `/api/` paths). Tier classification matches healthSources.ts. Empty `local pass | prod pass | remediation` columns exist for Tasks 4/7/8 to populate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest smoke suite — `src/__tests__/api-connectivity.test.ts`</name>
  <files>
    src/__tests__/api-connectivity.test.ts
  </files>
  <read_first>
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md (Task 1's enumerated list — single source of truth for the 16 endpoints)
    - server/routes/health.ts (PROBE_STRATEGIES — confirm endpoint paths)
    - scripts/load-test.js (BASE_URL env-default pattern)
    - server/middleware/dashboardAuth.test.ts (env-toggle pattern for DASHBOARD_PASSWORD)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Connectivity Smoke Suite — `src/__tests__/api-connectivity.test.ts`")
    - vite.config.ts (confirm vitest test discovery picks up `src/__tests__/*.test.ts`)
  </read_first>
  <behavior>
    For each of the 16 endpoints, generate one test case:
    - HTTP GET `${API_BASE_URL}${path}` with `Authorization: Bearer ${process.env.DASHBOARD_PASSWORD}` header attached (D-30 — exercises Plan 02 Bearer-bypass).
    - Assert `res.ok === true` (status 200).
    - Assert `res.json()` returns a non-empty body. For list endpoints (flights/ships/events/news/markets/water/sites), assert `Array.isArray(data) && data.length > 0` OR a structured envelope (`{events: [...], ...}` for cached responses) — match the actual shape per endpoint. For singleton endpoints (sources, health, dashboard/auth-check), assert `Object.keys(data).length > 0`.
    - Endpoint-specific shape assertions (defense in depth):
      - `/api/flights`: response has `flights` array OR top-level array; first item has `id`, `lat`, `lng`, `type === 'flight'`.
      - `/api/events`: response has `events` array; first item has `id`, `lat`, `lng`, `data.eventType`.
      - `/api/water`: response has `facilities` array; first item has `osmId`, `facilityType`.
      - `/api/sites`: response has `sites` array; first item has `siteType`.
      - `/api/health`: response has `summary`, `endpoints` keys.
      - `/api/dashboard/auth-check`: 200 means Bearer matches; assert `res.ok` only.
      - Other endpoints: skip shape assertion if generic empty-check passes.

    Test failure mode: a fetch that 200s but returns `{}` or `[]` is a FAIL — connectivity is "≥1 successful fetch with non-empty body" per D-24. Empty arrays usually mean "upstream is degraded but our cache survived"; that's a connectivity warning surfaced as a test fail.

  </behavior>
  <action>
    Create `src/__tests__/api-connectivity.test.ts`:

    1. Read `API_BASE_URL` from `process.env.API_BASE_URL ?? 'http://localhost:5173'`.
    2. Read `DASHBOARD_PASSWORD` from `process.env.DASHBOARD_PASSWORD ?? ''`. If empty, the test logs a warning AND attaches no Bearer header — this lets local-dev runs without secrets still smoke-test (the dev short-circuit in the server middleware handles auth bypass) but flags that the prod-URL run must have the secret set.
    3. Define `ENDPOINTS` as an exported `const` array matching Task 1's W6-AUDIT.md table. Comment line-by-line citing the source file (e.g., `'/api/flights', // PROBE_STRATEGIES line ~228`).
    4. For each endpoint, emit `it(\`${path} returns non-empty data with Bearer\`, async () => { ... })`. Wrap fetch in try/catch so a network failure is reported with diagnostic context (URL + status + body excerpt).
    5. Set timeout per test to 30s (some endpoints — e.g., `/api/water` — can be slow on cold cache; 30s allows for a single Overpass round-trip).

    Run locally: `npm run dev` in one shell, then `API_BASE_URL=http://localhost:5173 npx vitest run src/__tests__/api-connectivity.test.ts` in another. Capture pass/fail per endpoint into Task 1's W6-AUDIT.md `local pass` column.

    Atomic commit message: `test(28.2): add vitest smoke suite covering 16-endpoint connectivity (D-24/D-29)`. Body: cites the env-default pattern + per-endpoint shape assertions.

  </action>
  <verify>
    <automated>grep -c "it(" src/__tests__/api-connectivity.test.ts | grep -E "^[1-9][0-9]+" && grep -n "API_BASE_URL" src/__tests__/api-connectivity.test.ts && grep -n "Authorization.*Bearer" src/__tests__/api-connectivity.test.ts</automated>
  </verify>
  <done>
    The test file exists with ≥10 `it(...)` blocks (one per endpoint). `API_BASE_URL` env-var read at top. Bearer header attached on every fetch. The test compiles (`npx tsc --noEmit` exits 0). Running against `http://localhost:5173` (with `npm run dev`) produces a pass/fail list per endpoint that gets folded into W6-AUDIT.md.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-30 companion — `src/__tests__/rate-limit.test.ts`</name>
  <files>
    src/__tests__/rate-limit.test.ts
  </files>
  <read_first>
    - server/middleware/rateLimit.ts (post-Plan-02 — confirm the bypass branch + global 60/min ceiling)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (D-30 — "two named test files, two atomic commits, NOT bundled")
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md (D-30 companion suite snippet)
  </read_first>
  <behavior>
    - Test 1: Send 70 unauthenticated GET requests to `/api/flights` (above the 60/min global cap) within ~5 seconds via `Promise.all`. Assert that AT LEAST ONE response returns status 429. This codifies the "public tier still defends" contract — if Plan 02's Bearer-bypass accidentally widens to anonymous traffic, this test fails.
    - Test 2: Negative case — send 70 requests WITH valid Bearer attached. Assert that ZERO responses return 429 (the bypass is firing). This is a positive-path verification of Plan 02.

    Both tests must run against the SAME `API_BASE_URL` env var as Task 2's suite. Bearer is read from `process.env.DASHBOARD_PASSWORD`.

    Test 2 may produce 429s anyway from per-endpoint flights tier (120 req/min cap on flights specifically). 70 requests in 5s = 840 req/min effective rate, well above the 120/min flights cap. Adjust the test:
    - Use `/api/health` (not `/api/flights`) for Test 2's positive Bearer case — `/api/health` has no per-endpoint cap so the only throttle is the global 60/min, which Bearer bypasses.
    - For Test 1 (unauthenticated), use `/api/flights` so we exercise the global tier specifically (without Bearer, the request hits global 60/min FIRST; if you're in production, this is the same throttle path the operator wants to verify).

  </behavior>
  <action>
    Create `src/__tests__/rate-limit.test.ts`:

    Implementation:
    ```typescript
    const API_BASE_URL = process.env.API_BASE_URL ?? 'http://localhost:5173';
    const BEARER = process.env.DASHBOARD_PASSWORD ?? '';

    describe('Rate limit (D-30 companion)', () => {
      it('public tier throttles unauthenticated burst', async () => {
        const responses = await Promise.all(
          Array.from({ length: 70 }, () => fetch(`${API_BASE_URL}/api/flights`)),
        );
        const throttled = responses.filter((r) => r.status === 429);
        expect(throttled.length).toBeGreaterThanOrEqual(1);
      }, 30_000);

      it('Bearer bypasses public tier', async () => {
        if (BEARER === '') {
          // Skip when no secret in env (local dev short-circuits anyway)
          return;
        }
        const responses = await Promise.all(
          Array.from({ length: 70 }, () =>
            fetch(`${API_BASE_URL}/api/health`, {
              headers: { Authorization: `Bearer ${BEARER}` },
            }),
          ),
        );
        const throttled = responses.filter((r) => r.status === 429);
        expect(throttled.length).toBe(0);
      }, 30_000);
    });
    ```

    The test file is NOT run as part of the default `npx vitest run` because both tests require a running server (and the prod-URL run requires real Bearer secrets). Add a vitest filter `--testPathIgnorePatterns` config at the project level OR mark the test with a custom tag (e.g., `describe.skipIf(!process.env.RUN_RATE_LIMIT_TEST)`). The CI workflow (Task 6) sets the environment flag. Local dev skips it by default (so `npx vitest run` doesn't trigger 70 fetches against localhost during regular testing).

    Per CONTEXT D-30 + PATTERNS "atomic per-concern commits": keep this in a separate file from Task 2; commit separately.

    Atomic commit message: `test(28.2): add public-tier rate-limit defense companion (D-30)`. Body: cites D-30 two-pass audit + Plan 02 Bearer-bypass exercise.

  </action>
  <verify>
    <automated>grep -c "it(" src/__tests__/rate-limit.test.ts | grep -E "^[2-9]" && grep -n "70.*fetch\\|fetch.*70\\|length: 70" src/__tests__/rate-limit.test.ts && grep -n "429" src/__tests__/rate-limit.test.ts</automated>
  </verify>
  <done>
    Test file has 2 it() blocks. Both reference 70 requests + 429 status. The file is excluded from default `npx vitest run` via skipIf or testPathIgnorePatterns; CI workflow runs it explicitly. `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Local-dev manual audit pass — populate W6-AUDIT.md `local pass` column</name>
  <files>
    .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md (the Task 1 scaffold)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (D-24 manual audit + fix)
  </read_first>
  <action>
    1. Start local dev: `npm run dev` (Vercel emulation: `vercel dev` if available; otherwise vite dev + `npx tsx server/index.ts` per the existing dev workflow).
    2. Run `API_BASE_URL=http://localhost:5173 DASHBOARD_PASSWORD=<dev-default> npx vitest run src/__tests__/api-connectivity.test.ts`.
    3. For each FAIL, use curl + browser console + DevApiStatus modal to diagnose. Document the symptom + root cause + fix in the W6-AUDIT.md `remediation notes` column. Common patterns expected per CLAUDE.md history:
       - Stale Redis cache key (e.g., from a prior phase's deprecated `events:llm` key) → bump cache key version OR cold-fill via `?force=true` on cron route.
       - Adapter timeout (Overpass typically) → increase timeout OR fall back to mirror.
       - Schema-drift in cached data → flush + re-fetch.
       - Auth gate misconfiguration → confirm `dashboardAuth` middleware is correctly mounted on the failing route.
    4. For each fix, create an atomic commit in the `feat(28.2):` / `fix(28.2):` series. Update W6-AUDIT.md `local pass` to `✅` once that endpoint's smoke test goes green.
    5. Re-run the smoke suite end-to-end after every fix; aim for all 16 GREEN before moving to Task 5.

    Audit doc commit (after all local fixes): `docs(28.2): close local-dev audit pass — N of 16 GREEN`. Body lists per-endpoint outcomes.

    NOTE: Per CONTEXT D-25, prod-URL audit is the gating evidence. Local pass is a smoke check, not the close gate. Move to Task 5 once local is reasonably clean (no obvious regressions).

  </action>
  <verify>
    <automated>grep -c "✅\\|GREEN\\|PASS" .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md | grep -E "^[1-9]"</automated>
  </verify>
  <done>
    W6-AUDIT.md `local pass` column is populated for all 16 endpoints. Any local FAILs have remediation commits + green re-runs documented. Audit-doc commit landed with the per-endpoint outcomes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add `GET /api/audit-status` route + sidecar Redis key writer</name>
  <files>
    server/routes/audit-status.ts
    server/app.ts
    server/routes/__tests__/audit-status.test.ts
  </files>
  <read_first>
    - server/app.ts (focus on route registration — find where `eventsRouter` and other routers are mounted; the new route mounts in the same pattern)
    - server/routes/health.ts (read to confirm the existing /api/health response shape — the new route is structurally similar)
    - server/cache/redis.ts (Upstash interface)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (D-23 — "Claude's discretion at planning time" between /api/health extension vs new /api/audit-status route)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.4 — the merged-tab banner reads via "/api/health extension or via a dedicated /api/audit-status route")
    - Plan 05 SUMMARY for the placeholder location in DevApiStatus.tsx
  </read_first>
  <behavior>
    Planner choice: NEW `/api/audit-status` route (NOT a /api/health extension). Rationale: keeps concerns separated; /api/health is critical-tier observability with strict freshness contracts (Plan 28.1 W2), while /api/audit-status is optional CI-fed metadata that may be absent. Coupling them risks /api/health's freshness budget being polluted by stale audit data. New route also avoids adding the audit-status field to the existing HealthResponse Zod schema, which is consumed by the merged tab + HealthBanner — schema churn is more expensive than a new endpoint.

    Behavior:
    - GET endpoint, NO auth gate (status info is operator-tier metadata; the operator already has it visible via the merged tab; making it a public endpoint simplifies the CI feedback loop).
    - Reads `audit:connectivity:last-result` from Redis. If absent, returns `{status: 'absent'}`. If present, returns the parsed JSON.
    - Adds an X-Audit-Stale header if `lastVerifiedAt` is older than 7 days (informational; doesn't reject the response).
    - Tests:
      - Test 1: Redis empty → 200 + `{status: 'absent'}`.
      - Test 2: Redis has fresh JSON → 200 + parsed value.
      - Test 3: Redis has malformed JSON → 200 + `{status: 'absent'}` (degrade-open; never 500).
      - Test 4: `lastVerifiedAt` > 7d ago → 200 + `X-Audit-Stale: true` header.

    POST endpoint NOT created in this plan — the CI workflow (Task 6) writes the Redis key directly via the Upstash REST API, NOT through a server endpoint. This avoids needing a write-auth gate and keeps the server-side surface read-only.

  </behavior>
  <action>
    First commit (RED — test only):

    Create `server/routes/__tests__/audit-status.test.ts`. Mount via supertest. Mock `redis.get`. Implement Tests 1-4. Tests fail — file does not exist.

    Commit message: `test(28.2): add /api/audit-status route tests (RED)`.

    Second commit (GREEN — route + mount):

    Create `server/routes/audit-status.ts`:
    ```typescript
    import { Router, type Request, type Response } from 'express';
    import { redis } from '../cache/redis.js';

    export const auditStatusRouter = Router();

    const AUDIT_KEY = 'audit:connectivity:last-result';
    const STALE_MS = 7 * 86400 * 1000;

    auditStatusRouter.get('/audit-status', async (_req: Request, res: Response) => {
      try {
        const raw = await redis.get<string>(AUDIT_KEY);
        if (!raw) return res.json({ status: 'absent' });
        let parsed: { lastVerifiedAt?: string; [k: string]: unknown };
        try { parsed = typeof raw === 'string' ? JSON.parse(raw) : (raw as object); }
        catch { return res.json({ status: 'absent' }); }
        if (parsed.lastVerifiedAt) {
          const ageMs = Date.now() - new Date(parsed.lastVerifiedAt).getTime();
          if (ageMs > STALE_MS) res.set('X-Audit-Stale', 'true');
        }
        res.json(parsed);
      } catch {
        res.json({ status: 'absent' });
      }
    });
    ```

    Edit `server/app.ts`. Mount the new router alongside other routers, e.g., `app.use('/api', auditStatusRouter);` (placement matches existing routers).

    Run vitest — Tests 1-4 PASS.

    Commit message: `feat(28.2): add /api/audit-status route for W6 banner data feed (D-23/D-25)`. Body: read-only Redis key reader; degrade-open on parse fail; CI workflow writes the key via Upstash REST API.

  </action>
  <verify>
    <automated>npx vitest run server/routes/__tests__/audit-status.test.ts && grep -n "/audit-status" server/routes/audit-status.ts && grep -n "auditStatusRouter" server/app.ts</automated>
  </verify>
  <done>
    Tests 1-4 PASS. The route is mounted in app.ts. `npx tsc --noEmit` exits 0. 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: GitHub Actions workflow `.github/workflows/prod-connectivity-audit.yml`</name>
  <files>
    .github/workflows/prod-connectivity-audit.yml
  </files>
  <read_first>
    - .github/workflows/ (list existing workflows for naming convention + secret handling)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (D-25 — manual audit + vitest smoke + critical-banner verify; D-29 prod smoke trigger choice)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("D-30 companion suite" excerpt)
    - server/lib/llmEvalHarness.ts (for reference on how the existing `runEval` Redis-key-write pattern works — informs the workflow's Upstash REST API write)
  </read_first>
  <behavior>
    The workflow:
    1. Triggers on `workflow_dispatch` (manual) — per CONTEXT D-29 Claude-discretion, manual is the safest baseline. Future phase can promote to PR-trigger or scheduled.
    2. Runs on `ubuntu-latest`.
    3. Sets up Node 20 + installs.
    4. Reads two secrets: `PROD_DASHBOARD_PASSWORD` (Bearer for the audit) + `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` (for the sidecar key write). All sourced from GitHub Actions `secrets.*`.
    5. Step "Connectivity smoke against prod": `API_BASE_URL=https://otg-iran-monitor.vercel.app DASHBOARD_PASSWORD=${{ secrets.PROD_DASHBOARD_PASSWORD }} npx vitest run src/__tests__/api-connectivity.test.ts --reporter=json --outputFile=audit-result.json`. The reporter:json output captures per-endpoint pass/fail.
    6. Step "Rate-limit defense companion": `API_BASE_URL=https://otg-iran-monitor.vercel.app DASHBOARD_PASSWORD=${{ secrets.PROD_DASHBOARD_PASSWORD }} RUN_RATE_LIMIT_TEST=1 npx vitest run src/__tests__/rate-limit.test.ts --reporter=basic`.
    7. Step "Write audit result to Redis": parse `audit-result.json`, derive `{lastVerifiedAt, totalEndpoints, healthy, failing, status}`, POST to Upstash REST API as the sidecar key. Use a small inline node script or `curl` against the REST API.
    8. Step "Upload audit log artifact": uploads the JSON to GitHub Actions artifacts so the operator can download per-run.
    9. Failure handling: if any step fails, the workflow exits non-zero AND the audit-status Redis key gets `status: 'failing'` written so the merged tab surfaces a red banner immediately. The job-level `continue-on-error: false` for steps 5-7 ensures step 7 runs even if step 5/6 fail (use `if: always()` on step 7).
  </behavior>
  <action>
    Create `.github/workflows/prod-connectivity-audit.yml`. Use existing workflow YAML conventions (audit a sibling workflow file in `.github/workflows/` for indentation, action versions, etc.).

    Key elements (paraphrased; executor writes the YAML matching repo conventions):
    - `on: workflow_dispatch:` with optional `inputs: target_url:` defaulting to the prod URL (allows operator to point at a preview deploy if needed).
    - Job `connectivity-audit` running on `ubuntu-latest`.
    - Steps: checkout, setup-node@v4 with node 20, `npm ci`, the smoke step (with `env:` block setting API_BASE_URL + DASHBOARD_PASSWORD from secrets), the rate-limit step, the Redis write step (using a small inline `node -e "..."` block calling Upstash REST `https://${UPSTASH_REDIS_REST_URL}/set/${KEY}` with the JSON body, OR using `actions/github-script` if cleaner), the artifact upload step.
    - The Redis SET TTL: 7 days (`?EX=604800`).

    Verification step at the bottom of the workflow: `curl https://otg-iran-monitor.vercel.app/api/audit-status` and assert the response JSON matches what was just written.

    Atomic commit message: `ci(28.2): add prod-connectivity audit workflow (D-25/D-29/D-30)`. Body: manual-trigger workflow runs both smoke + rate-limit suites against prod URL; writes sidecar Redis key for the merged tab banner; uploads JSON artifact.

    NOTE: this workflow does NOT run automatically on PR. Phase 28.2 user directive is to keep manual cadence; future phase promotes to scheduled (per CONTEXT deferred section).

  </action>
  <verify>
    <automated>grep -n "otg-iran-monitor.vercel.app" .github/workflows/prod-connectivity-audit.yml && grep -n "PROD_DASHBOARD_PASSWORD\\|UPSTASH_REDIS_REST" .github/workflows/prod-connectivity-audit.yml && grep -n "workflow_dispatch" .github/workflows/prod-connectivity-audit.yml</automated>
  </verify>
  <done>
    Workflow file exists with workflow_dispatch trigger, references secrets correctly, and runs both smoke + rate-limit suites against the renamed prod URL. 1 atomic commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Wire the audit-result banner into the merged tab + integration test for HealthBanner-during-outage</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.auditBanner.test.tsx
    src/components/ui/__tests__/HealthBanner.outage.test.tsx
  </files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (locate the audit-result banner placeholder from Plan 05 Task 2 — `data-testid='audit-result-banner'`)
    - src/components/ui/HealthBanner.tsx (current implementation)
    - src/components/ui/__tests__/HealthBanner.test.tsx (existing unit coverage — the new test ADDs an integration trace, doesn't replace)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.4 banner copy + §10 verbatim)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (D-24 critical-banner verification)
  </read_first>
  <behavior>
    Audit-result banner wiring:
    - Test 1: Mock `/api/audit-status` returning `{status: 'clean', lastVerifiedAt: '2026-05-04T00:00:00.000Z', totalEndpoints: 16, healthy: 16, failing: []}`. Banner renders with class `bg-green-500/10 border border-green-500/20 text-green-400` and copy `All 16 endpoints connecting (last verified 2026-05-04 via prod audit).` (UI-SPEC §10 verbatim — note date is formatted humanely; trailing period preserved).
    - Test 2: Mock `/api/audit-status` returning `{status: 'failing', failing: ['Events', 'Water']}`. Banner renders with class `bg-red-500/10 border border-red-500/20 text-red-400` and copy `2 of 16 endpoints failing connectivity audit. See Events, Water.`.
    - Test 3: Mock `/api/audit-status` returning `{status: 'absent'}`. Banner is NOT rendered (silent per UI-SPEC §5.4).
    - Test 4: Spacing audit — banner div has `px-3 py-2` padding (multiples of 4); `rounded` corner.

    HealthBanner-during-outage integration:
    - Test 5: Mount `<HealthStatusProvider><HealthBanner /></HealthStatusProvider>` with mocked `/api/health` returning a critical-tier endpoint as `unhealthy`. Banner renders with the existing critical-tier copy. Then change the mock to `healthy` and re-poll — banner clears. Then mock unhealthy again — banner re-fires. Confirms the banner is responsive to live state, not a one-shot.
    - Test 6: HealthBanner renders even when `shouldRenderDashboard()` returns false (D-26 specifies HealthBanner is NOT gated by the Bearer predicate). Mock `@/lib/dashboardAuth` to return false; mount HealthBanner; confirm it still renders for a critical issue.

  </behavior>
  <action>
    First commit (RED — both test files):

    Create `src/components/ui/__tests__/DevApiStatus.auditBanner.test.tsx`. Mount DevApiStatus inside HealthStatusProvider. Mock `fetch('/api/audit-status', ...)`. Implement Tests 1-4. Tests fail because the merged tab still has only the placeholder div.

    Create `src/components/ui/__tests__/HealthBanner.outage.test.tsx`. Implement Tests 5 + 6. Test 6 may pass immediately (HealthBanner has no shouldRenderDashboard gate today); Test 5 may need an integration helper that flips mocked /api/health between polls.

    Commit message: `test(28.2): add audit-result banner + HealthBanner-during-outage integration tests (RED)`.

    Second commit (GREEN — wire the banner):

    Edit `src/components/ui/DevApiStatus.tsx`. Inside `DevApiStatusAllApisTab` body, replace the placeholder `data-testid='audit-result-banner'` div with a fetch-driven banner:
    1. Add a `useEffect` (or use SWR / react-query if already in the project; otherwise use a simple `useEffect + useState`) that fetches `/api/audit-status` on mount + every 60s. Cache the result in component state.
    2. Render the banner conditionally:
       - `status === 'absent'` → no banner (silent).
       - `status === 'clean'` → green banner with verbatim copy from UI-SPEC §10. Format `lastVerifiedAt` as `YYYY-MM-DD` for the `{date}` substitution.
       - `status === 'failing'` → red banner with verbatim copy citing the failing endpoint names (joined with `, `).
    3. Class verbatim per UI-SPEC §5.4: `px-3 py-2 rounded bg-{color}-500/10 border border-{color}-500/20 text-{color}-400 text-xs` where `{color}` is `green` or `red`.
    4. data-testid: `audit-result-banner-clean` or `audit-result-banner-failing` (or a single `audit-result-banner` with a `data-status` attribute, per the test's expectation).

    Run vitest — Tests 1-4 (audit banner) + Tests 5-6 (HealthBanner) PASS.

    Commit message: `feat(28.2): wire audit-result banner + lock down HealthBanner outage behavior (D-24/D-25)`.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.auditBanner.test.tsx && npx vitest run src/components/ui/__tests__/HealthBanner.outage.test.tsx && grep -n "/api/audit-status" src/components/ui/DevApiStatus.tsx && grep -n "All 16 endpoints connecting" src/components/ui/DevApiStatus.tsx</automated>
  </verify>
  <done>
    Tests 1-6 PASS. The banner fetches /api/audit-status, renders verbatim copy from UI-SPEC §10, and respects clean/failing/absent states. HealthBanner integration test confirms the banner stays responsive across live state transitions and remains visible without Bearer (per D-26 — HealthBanner is intentionally NOT gated). 2 atomic commits.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: Operator triggers the prod-audit workflow + closes W6-AUDIT.md</name>
  <files>
    .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md
  </files>
  <action>
    Operator-driven step that runs the GitHub Actions workflow against prod and updates W6-AUDIT.md with the prod-pass column populated. Full procedure in <how-to-verify> below. After the workflow PASSes, the operator updates the audit doc and commits with message `docs(28.2): close W6 prod connectivity audit — 16 of 16 GREEN`.
  </action>
  <verify>
    <automated>curl -s https://otg-iran-monitor.vercel.app/api/audit-status | grep -E "\"status\"\s*:\s*\"clean\""</automated>
  </verify>
  <done>
    The /api/audit-status endpoint returns `status: 'clean'` indicating the most recent CI workflow run found 16 of 16 endpoints connecting. W6-AUDIT.md `prod pass` column is ✅ for all 16 endpoints. The closing commit is on main.
  </done>
  <what-built>
    All 28.2 server + UI work has merged to main (Plans 01-05) and the renamed Vercel deploy is live at `https://otg-iran-monitor.vercel.app`. The new `.github/workflows/prod-connectivity-audit.yml` is in place but has not been triggered yet. This checkpoint forces an operator-driven prod-audit run and waits for the result.

    Why a human-action checkpoint: triggering a GitHub Actions workflow that hits the deployed URL with real secrets cannot be Claude-automated without giving Claude `gh workflow run` privileges + the secret values. The operator clicks "Run workflow" in the GitHub UI; the workflow takes ~2 minutes; results land as artifacts + the sidecar Redis key; the merged tab's audit-result banner reflects the outcome.

  </what-built>
  <how-to-verify>
    1. Confirm Plans 01-05 + Tasks 1-7 of Plan 06 have all merged to main: `git log --oneline | head -30` shows the 28.2 commit train.
    2. Confirm the renamed Vercel deploy is live: `curl -I https://otg-iran-monitor.vercel.app/api/health` returns 200.
    3. Open GitHub → Actions → "Prod Connectivity Audit" → "Run workflow" → select branch `main` → click Run.
    4. Wait ~2 minutes for the workflow to complete. If it succeeds, all 16 endpoints connect cleanly with Bearer attached AND the rate-limit defense companion confirms unauthenticated bursts still 429.
    5. Download the audit-result JSON artifact from the workflow run.
    6. Open the deployed dashboard at `https://otg-iran-monitor.vercel.app`, enter Bearer in the auth modal, navigate to the API Health tab. The audit-result banner reads `All 16 endpoints connecting (last verified <today's date> via prod audit).` (green).
    7. If the workflow fails, run the manual remediation loop from Task 4 against the prod URL: identify the failing endpoint, fix it, re-run the workflow. Repeat until clean.
    8. Update `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-W6-AUDIT.md` `prod pass` column to ✅ for all 16 endpoints. Add a closing section "W6 close (YYYY-MM-DD)" with the workflow run URL + the audit-result JSON SHA.
    9. Commit: `docs(28.2): close W6 prod connectivity audit — 16 of 16 GREEN`.
  </how-to-verify>
  <resume-signal>
    Type "audit-clean" if all 16 endpoints PASS via the prod-URL workflow run; or describe the remediation loop applied (e.g., "fixed water:facilities:v3 stale-cache via cron-warm refresh, re-ran workflow, all 16 GREEN"). The 28.2 phase closes once the operator types this signal AND the W6-AUDIT.md `docs(28.2):` close commit is on main.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                              |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| GitHub Actions runner → prod URL      | Workflow attaches PROD_DASHBOARD_PASSWORD secret as Bearer to every probe; secret never logged or persisted in artifacts |
| GitHub Actions runner → Upstash Redis | Workflow uses UPSTASH_REDIS_REST_URL + TOKEN secrets to write sidecar audit key; SET-with-TTL prevents permanent writes  |
| GET /api/audit-status → public        | Read-only metadata endpoint; no auth (operator-tier metadata is non-secret)                                              |
| Rate-limit companion test → prod      | 70-request burst is well below DDoS threshold and exercises the existing rate limit; not abusive                         |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                               |
| ------------ | ---------------------- | ------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28.2-06-01 | Information Disclosure | PROD_DASHBOARD_PASSWORD logged in workflow output                                           | mitigate    | GitHub Actions auto-redacts secret values in logs (`***`); the test code uses `process.env.DASHBOARD_PASSWORD` only inside fetch headers, never `console.log`'d; review workflow file diff for any `echo "$DASHBOARD_PASSWORD"` regression                                    |
| T-28.2-06-02 | Tampering              | Audit-result Redis key poisoned by attacker who can write to Upstash                        | accept      | Upstash Redis is single-tenant; only the GitHub Actions workflow has the REST API token; the dashboard reads via /api/audit-status server route (which only `redis.get`s, never relays user input); poisoned key worst case = misleading banner copy, not security compromise |
| T-28.2-06-03 | Denial of Service      | Rate-limit companion test creates 70-req burst against prod                                 | accept      | 70 req/min is below the global 60/min ceiling slightly (counter sliding-window means brief overshoot) but well below any DDoS threshold; Upstash Redis backed; Vercel + Cloudflare absorb the burst trivially. Test runs ≤1x/day at most                                      |
| T-28.2-06-04 | Information Disclosure | /api/audit-status leaks endpoint names that are failing                                     | accept      | Endpoint names like "Events" / "Water" are public-by-nature (visible in /api/health response which is also unauthenticated). The audit-status response adds no novel surface                                                                                                  |
| T-28.2-06-05 | Spoofing               | Attacker forges /api/audit-status response in transit                                       | accept      | Vercel HTTPS prevents mitm; the dashboard fetch is same-origin; the JSON shape is opaque to attackers without privileged access                                                                                                                                               |
| T-28.2-06-06 | Repudiation            | Operator clicks "Run workflow" but no audit trail in /api/health logs                       | mitigate    | GitHub Actions retains workflow run history with operator GitHub identity; W6-AUDIT.md close commit references the run URL; 28.2 SUMMARY links to the audit doc                                                                                                               |
| T-28.2-06-07 | Tampering              | Sidecar key TTL expires before next manual run → banner switches to silent (status: absent) | accept      | 7-day TTL is intentional. If the operator hasn't run the audit in 7 days, the silent banner correctly reflects "we don't know if prod is clean right now" — the absence of stale-but-green is the safe failure mode                                                           |
| T-28.2-06-08 | Elevation of Privilege | /api/audit-status is unauthenticated and could be probed for sensitive info                 | mitigate    | Response is JSON metadata only — no PII, no secrets, no token data. Rate-limited via existing per-endpoint tier (default 60/min). Adding auth would couple the operator UI to Bearer state — undesired complexity for a metadata-only endpoint                                |

</threat_model>

<verification>
- 8 atomic commits land in order: docs Task 1 → test Task 2 → test Task 3 → docs Task 4 (multiple sub-commits possible) → test+feat Task 5 → ci Task 6 → test+feat Task 7 → docs Task 8 close
- All test files PASS:
  - `src/__tests__/api-connectivity.test.ts` — 16 tests (one per endpoint)
  - `src/__tests__/rate-limit.test.ts` — 2 tests (Bearer-bypass + public-tier defense)
  - `server/routes/__tests__/audit-status.test.ts` — 4 tests
  - `src/components/ui/__tests__/DevApiStatus.auditBanner.test.tsx` — 4 tests
  - `src/components/ui/__tests__/HealthBanner.outage.test.tsx` — 2 tests
- The GitHub Actions workflow runs successfully against the prod URL with operator-triggered dispatch
- All 16 endpoints PASS the prod-URL smoke suite
- W6-AUDIT.md is committed with both `local pass` and `prod pass` columns ✅ for all 16 endpoints
- The merged API Health tab in prod shows the green audit-result banner
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- `prettier --check .` exits 0
- HealthBanner renders for critical-tier outages even when shouldRenderDashboard returns false
- HealthStatusProvider Test 7 still PASSes (no regression from Plan 05)
</verification>

<success_criteria>

- 28.2 PHASE CLOSE GATE: the operator-triggered prod-audit workflow run shows `16 of 16` PASS, the merged API Health tab in prod shows the green audit-result banner, and W6-AUDIT.md is committed with the `docs(28.2): close W6 prod connectivity audit — 16 of 16 GREEN` commit
- Hand-off to Phase 28.3: a domain-renamed `scripts/load-test.js` BASE_URL (Plan 01), an operator endpoint surface that responds to Bearer (Plan 03), a global rate-limit tier that doesn't kill the operator's own polling browser (Plan 02), all dev/prod gates aligned (Plan 04), the merged dashboard with quality + retry + sparkline + audit banner (Plan 05), AND a healthy `/api/health` baseline (W6 audit green) — Phase 28.3's k6 sweep starts from a known-clean prod surface
- The companion `rate-limit.test.ts` confirms Plan 02's Bearer-bypass works in BOTH directions: with Bearer the global tier is skipped; without Bearer the global tier still throttles
- The sidecar audit-result Redis key + /api/audit-status route deliver the merged tab's banner data feed without coupling /api/health's freshness contract
- Memory `reference_deployment.md` (updated in Plan 01) reflects the renamed URL for future Claude sessions
- Plan 01 Task 6 Vercel-side decision is consistent with the audit's prod-URL target — if the operator chose option C (vercel.json redirect), the workflow's `curl https://irt-monitoring.vercel.app/api/audit-status` should also resolve via 308; if option A (in-place rename), the old URL returns 404 (audit doc captures this)
  </success_criteria>

<output>
After completion, create `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-06-SUMMARY.md` recording:
- 8+ commit SHAs (Tasks 1-7) plus the close commit from Task 8
- Final W6-AUDIT.md state — pointer to the committed file
- GitHub Actions workflow run URL
- Audit-result JSON SHA + content excerpt (totalEndpoints / healthy / failing)
- Per-endpoint pass/fail matrix from the prod run
- 28 test outcomes (16 + 2 + 4 + 4 + 2) all PASS
- Phase 28.2 wave-close confirmation: all 6 waves merged to main; 28.3 unblocked
- Memory update reminder (per CLAUDE.md `feedback_project_docs.md`): README, PROJECT_SPEC, PROJECT_STATUS, CHANGELOG, CLAUDE.md must be updated to reflect domain rename + dev/prod policy at 28.2 close. Capture as a follow-up item if not done in Plan 01
</output>

---
phase: 28.2
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/components/map/EntityTooltip.tsx
  - src/components/detail/EventDetail.tsx
  - src/components/detail/WaterFacilityDetail.tsx
  - src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx
  - src/components/detail/__tests__/EventDetail.gateSwap.test.tsx
  - src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx
  - src/components/notifications/__tests__/NotificationCard.lockdown.test.tsx
  - src/components/map/__tests__/MapDevExposer.lockdown.test.tsx
  - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-09
must_haves:
  truths:
    - 'EntityTooltip event-ID subscript renders identically in dev AND prod-with-Bearer; hidden in prod-without-Bearer (UI-SPEC §3.1)'
    - 'EventDetail event-ID row renders identically in dev AND prod-with-Bearer; hidden in prod-without-Bearer (UI-SPEC §3.2)'
    - 'EventDetail Confidence + Geo Precision wrapper renders identically in dev AND prod-with-Bearer; hidden in prod-without-Bearer (UI-SPEC §3.3)'
    - 'WaterFacilityDetail OSM ID row follows Path B (Bearer-gated) per planner-chosen path documented in CONTEXT.md addendum (UI-SPEC §3.4)'
    - 'NotificationCard severity score numeric STAYS gated by import.meta.env.DEV — locked dev-only-forever per memory project_dev_score_display.md + D-07'
    - 'MapDevExposer window.__map global STAYS gated by import.meta.env.DEV — locked dev-only-forever per D-07 (footgun risk)'
    - 'WaterFacilityDetail notabilityScore row STAYS gated by import.meta.env.DEV — locked dev-only-forever per D-07 (internal tuning hook)'
    - 'Render-parity per UI-SPEC §3.5: dev-after-swap, Bearer-prod-after-swap snapshots are byte-identical; no-Bearer-prod-after-swap omits the block entirely'
    - 'No NEW debug-field surfaces are added per D-09'
  artifacts:
    - path: src/components/map/EntityTooltip.tsx
      provides: 'event-ID subscript gated by shouldRenderDashboard()'
      contains: 'shouldRenderDashboard'
    - path: src/components/detail/EventDetail.tsx
      provides: 'event-ID row + Confidence/GeoPrecision wrapper both gated by shouldRenderDashboard()'
      contains: 'shouldRenderDashboard'
    - path: src/components/detail/WaterFacilityDetail.tsx
      provides: 'OSM ID row gated by shouldRenderDashboard() (Path B); notabilityScore row STILL gated by import.meta.env.DEV'
      contains: 'shouldRenderDashboard'
    - path: src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx
      provides: '4-snapshot render-parity test per UI-SPEC §3.5'
    - path: src/components/detail/__tests__/EventDetail.gateSwap.test.tsx
      provides: '6-snapshot render-parity test (2 surfaces × 3 conditions) per UI-SPEC §3.5'
    - path: src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx
      provides: 'Path-B render-parity for OSM ID + lockdown verification for notabilityScore'
    - path: src/components/notifications/__tests__/NotificationCard.lockdown.test.tsx
      provides: 'Lockdown assertion: severity score numeric NEVER renders when import.meta.env.DEV is false regardless of Bearer presence'
    - path: src/components/map/__tests__/MapDevExposer.lockdown.test.tsx
      provides: 'Lockdown assertion: window.__map is NOT set when import.meta.env.DEV is false regardless of Bearer presence'
    - path: .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md
      provides: 'Path A vs Path B addendum for D-06 OSM ID disambiguation'
      contains: 'Path B'
  key_links:
    - from: src/components/map/EntityTooltip.tsx
      to: '@/lib/dashboardAuth'
      via: 'shouldRenderDashboard import + call'
      pattern: "from '@/lib/dashboardAuth'"
    - from: src/components/detail/EventDetail.tsx
      to: '@/lib/dashboardAuth'
      via: 'shouldRenderDashboard import + 2 call sites (line 56, line 165)'
      pattern: "from '@/lib/dashboardAuth'"
    - from: src/components/detail/WaterFacilityDetail.tsx
      to: '@/lib/dashboardAuth'
      via: 'shouldRenderDashboard import for OSM ID row only'
      pattern: "from '@/lib/dashboardAuth'"
---

<objective>
Per CONTEXT D-05/D-06/D-07 + UI-SPEC §3 the gate-swap contract: graduate three currently-dev-only debug surfaces to Bearer-gated prod by replacing `import.meta.env.DEV` with `shouldRenderDashboard()`. Lock down three other dev-only-forever surfaces with explicit regression tests so a future agent cannot accidentally graduate them.

**Path B chosen** for the WaterFacilityDetail OSM ID row per UI-SPEC §3.4. Rationale: D-06 literally lists "OSM ID values" as a graduation target; Path A's "preserve current behavior" creates an asymmetry where event IDs (in the same panel-class) are Bearer-gated while OSM IDs leak. Path B aligns operator-tier metadata across Event / Site / Water details. Task 0 records this in CONTEXT.md before any gate-swap commit lands.

Surfaces to graduate (D-06):

1. `src/components/map/EntityTooltip.tsx:125` — event-ID subscript
2. `src/components/detail/EventDetail.tsx:56` — event-ID row
3. `src/components/detail/EventDetail.tsx:165` — Confidence + Geo Precision wrapper
4. `src/components/detail/WaterFacilityDetail.tsx:117` — OSM ID row (Path B)

Surfaces to lock down (D-07):

1. `src/components/notifications/NotificationCard.tsx` — severity score numeric (memory `project_dev_score_display.md`)
2. `src/components/map/BaseMap.tsx:458` — `MapDevExposer` `window.__map` (footgun in prod)
3. `src/components/detail/WaterFacilityDetail.tsx:238` — `notabilityScore` row (internal tuning hook)

Output:

- 4 atomic gate-swap commits (one per swap surface)
- 5 atomic test commits (3 render-parity + 2 lockdown regression tests)
- 1 doc commit for the CONTEXT.md Path-B addendum
- 15 snapshots total (UI-SPEC §3.5 enumerates 12 + 3 for Path B)
- Lockdown surfaces have explicit "block absent in prod" assertions
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md
@CLAUDE.md
@src/lib/dashboardAuth.ts
@src/components/map/EntityTooltip.tsx
@src/components/detail/EventDetail.tsx
@src/components/detail/WaterFacilityDetail.tsx

<interfaces>
Source-of-truth gate predicate (read-only, do NOT modify):

`src/lib/dashboardAuth.ts` exports `shouldRenderDashboard(): boolean` returning `import.meta.env.DEV || hasDashboardKey()`.

Three currently-dev-only blocks to gate-swap (verbatim from current source):

`EntityTooltip.tsx:125` — span with style `color: #6b7280`, `fontSize: 8px`, `marginLeft: 4px` rendering `entity.id.slice(0, 12)`.

`EventDetail.tsx:56` — `<DetailValue label="ID" value={entity.id} />` wrapped in `import.meta.env.DEV` gate.

`EventDetail.tsx:165` — wrapper `<div className="mt-2 border-t border-white/5 pt-2">` containing two `<DetailValue>` rows for Confidence (`d.confidence.toFixed(3)`) and Geo Precision (raw `d.geoPrecision` enum string), gated by `import.meta.env.DEV && d.confidence != null`.

`WaterFacilityDetail.tsx:117` — `<DetailValue label="OSM ID" value={String(facility.osmId)} />` currently always-rendered (no gate today).

Three lockdown surfaces (DO NOT TOUCH):

`NotificationCard.tsx` severity score numeric block — gated by `import.meta.env.DEV`.

`BaseMap.tsx:458` `<MapDevExposer />` — gated by `import.meta.env.DEV`.

`WaterFacilityDetail.tsx:238` notabilityScore block — gated by `import.meta.env.DEV && facility.notabilityScore !== undefined`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Document Path B choice in CONTEXT.md addendum</name>
  <files>.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md</files>
  <read_first>
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§3.4 — the Path A vs Path B specification)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (the existing D-06 entry around lines 51-55)
  </read_first>
  <action>
    Append a one-line clarification to the existing D-06 entry under `<decisions>` → `### Dev → Prod Feature Promotion`. After the existing 3 sub-bullets, insert a new bullet:

    `**D-06 clarification (added during Plan 04 drafting):** WaterFacilityDetail OSM ID row → **Path B** (Bearer-gated via shouldRenderDashboard()). Rationale: D-06 lists OSM IDs as graduation targets; Path A's "preserve always-rendered behavior" creates an asymmetry where event IDs (same panel-class) are Bearer-gated while OSM IDs leak to all users. Path B aligns operator-tier metadata across Event / Site / Water details. UAT-trace screenshot captured during Plan 04 Task 4.`

    Use the actual current date when this task runs.

    Atomic commit message: `docs(28.2): document Path B choice for WaterFacilityDetail OSM ID gate`. Body explains the planner choice for symmetry with EventDetail/EntityTooltip and references UI-SPEC §3.4.

  </action>
  <verify>
    <automated>grep -n "Path B" .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md</automated>
  </verify>
  <done>
    The CONTEXT.md addendum line is present under D-06. `git log -1 --oneline` shows the docs(28.2) commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Gate-swap EntityTooltip event-ID subscript + 4-snapshot render-parity test</name>
  <files>
    src/components/map/EntityTooltip.tsx
    src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx
  </files>
  <read_first>
    - src/components/map/EntityTooltip.tsx (full file — confirm gate at line 125 and surrounding context)
    - src/lib/dashboardAuth.ts (shouldRenderDashboard signature + STORAGE_KEY for localStorage mocking)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§3.1 verbatim — preserve color #6b7280, fontSize 8px, marginLeft 4px, slice(0,12))
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Gate Swap A — EntityTooltip.tsx" section)
  </read_first>
  <behavior>
    4 render-parity tests per UI-SPEC §3.5:
    - Test 1 (dev render before swap): baseline string captured pre-edit.
    - Test 2 (dev render after swap): with `import.meta.env.DEV === true` mocked, the gate-swapped component renders the same span; assert via `container.innerHTML` byte-equality against Test 1's baseline.
    - Test 3 (Bearer-prod render): with `import.meta.env.DEV === false` AND `localStorage.setItem('dashboard:auth-key', 'fake-bearer')`, the span is identical to Test 2.
    - Test 4 (no-Bearer prod render): with `import.meta.env.DEV === false` AND localStorage cleared, the span is absent (`querySelector` returns null).

    Test 3 fails BEFORE the gate swap (existing predicate is `import.meta.env.DEV` which is false in Test 3's environment). Atomic-commit RED state.
    Test 3 passes AFTER the gate swap (predicate is `shouldRenderDashboard()` which returns true via hasDashboardKey()). Atomic-commit GREEN state.

  </behavior>
  <action>
    First commit (RED — test only):

    Create `src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx`. Mock `@/lib/dashboardAuth` with `vi.mock(...)` so the test toggles `shouldRenderDashboard` return values via `vi.mocked(shouldRenderDashboard).mockReturnValue(true|false)`. Use `vi.stubEnv('DEV', false)` for prod-render cases. Implement the 4-test matrix using Testing Library `render()`. Run `npx vitest run src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx` — Test 3 FAILs.

    Commit message: `test(28.2): add EntityTooltip gate-swap render-parity (RED)`. Body cites UI-SPEC §3.5 and notes Test 3 fails until next commit.

    Second commit (GREEN — gate swap):

    Edit `src/components/map/EntityTooltip.tsx`:
    1. Add import: `import { shouldRenderDashboard } from '@/lib/dashboardAuth';`
    2. At line 125, replace the predicate from `{import.meta.env.DEV && (...)}` to `{shouldRenderDashboard() && (...)}`. Inline span styling (color, fontSize, marginLeft, slice) preserved verbatim per UI-SPEC §3.1.

    Run vitest — all 4 tests PASS.

    Commit message: `feat(28.2): gate-swap EntityTooltip event-ID subscript to shouldRenderDashboard (D-06, UI-SPEC §3.1)`.

  </action>
  <verify>
    <automated>npx vitest run src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx && grep -n "shouldRenderDashboard" src/components/map/EntityTooltip.tsx</automated>
  </verify>
  <done>
    All 4 tests PASS. EntityTooltip.tsx imports `shouldRenderDashboard` and uses it at line ~125. The literal `import.meta.env.DEV` is gone from the modified gate (other gates in the file remain untouched). 2 atomic commits. `npx tsc --noEmit` exits 0. `npm run lint` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate-swap EventDetail event-ID row + Confidence/GeoPrecision wrapper + 6-snapshot render-parity test</name>
  <files>
    src/components/detail/EventDetail.tsx
    src/components/detail/__tests__/EventDetail.gateSwap.test.tsx
  </files>
  <read_first>
    - src/components/detail/EventDetail.tsx (focus on lines 50-60 and 160-175)
    - src/lib/dashboardAuth.ts
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§3.2 + §3.3 verbatim — DetailValue spacing, .toFixed(3) confidence formatting, raw enum string for geoPrecision, wrapper mt-2 border-t border-white/5 pt-2)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Gate Swap B — EventDetail.tsx (two gate swaps)")
  </read_first>
  <behavior>
    6 snapshots = 2 surfaces × 3 conditions per UI-SPEC §3.5:
    Surface A: event-ID DetailValue row at line 56.
    Surface B: Confidence + GeoPrecision wrapper at line 165 (entire wrapper div with `mt-2 border-t border-white/5 pt-2` classes + both child DetailValue rows).

    Conditions: dev-after-swap, Bearer-prod-after-swap, no-Bearer-prod-after-swap.

    Mount EventDetail with mock event entity having `data.confidence = 0.8472` and `data.geoPrecision = 'nominatim-verified-2pass'` so both surfaces have data to render. Surface B test asserts `confidence.toFixed(3)` produces "0.847" verbatim (no rounding drift).

    Bearer-prod tests fail until gate swaps land. Atomic-commit RED → GREEN.

  </behavior>
  <action>
    First commit (RED — test only):

    Create `src/components/detail/__tests__/EventDetail.gateSwap.test.tsx`. Use Testing Library `render()`. Mock `@/lib/dashboardAuth` and `import.meta.env.DEV`. Mock the events store so the entity is selected. Implement the 6-test matrix.

    Commit message: `test(28.2): add EventDetail gate-swap render-parity (RED)`.

    Second commit (GREEN — both gate swaps):

    Edit `src/components/detail/EventDetail.tsx`:
    1. Add import at top: `import { shouldRenderDashboard } from '@/lib/dashboardAuth';`
    2. Line 56: replace `{import.meta.env.DEV && <DetailValue label="ID" value={entity.id} />}` predicate with `{shouldRenderDashboard() && <DetailValue label="ID" value={entity.id} />}`.
    3. Line 165: replace `{import.meta.env.DEV && d.confidence != null && (...)}` predicate with `{shouldRenderDashboard() && d.confidence != null && (...)}`. The wrapper div, `.toFixed(3)`, raw geoPrecision enum string, child rows all preserved verbatim per UI-SPEC §3.3.

    Run vitest — all 6 tests PASS.

    Commit message: `feat(28.2): gate-swap EventDetail event-ID + Confidence/GeoPrecision rows (D-06, UI-SPEC §3.2/§3.3)`.

  </action>
  <verify>
    <automated>npx vitest run src/components/detail/__tests__/EventDetail.gateSwap.test.tsx && grep -c "shouldRenderDashboard" src/components/detail/EventDetail.tsx</automated>
  </verify>
  <done>
    All 6 tests PASS. EventDetail.tsx contains `shouldRenderDashboard` ≥2 times (one per swap site). The literal `import.meta.env.DEV` is gone from lines 56 and 165 (other gates in the file untouched). 2 atomic commits. `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Path B — Gate-swap WaterFacilityDetail OSM ID + lockdown assertion for notabilityScore</name>
  <files>
    src/components/detail/WaterFacilityDetail.tsx
    src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx
  </files>
  <read_first>
    - src/components/detail/WaterFacilityDetail.tsx (lines 110-130 for OSM ID + lines 230-250 for notabilityScore)
    - src/lib/dashboardAuth.ts
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§3.4 Path B + §1 lockdown for notabilityScore)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md (Task 0's Path-B addendum, now in CONTEXT.md)
  </read_first>
  <behavior>
    Surface A (Path B gate-swap): OSM ID row at line 117 wrapped in `{shouldRenderDashboard() && ...}`. 3 snapshots (dev / Bearer-prod / no-Bearer-prod).

    Surface B (lockdown — notabilityScore): `import.meta.env.DEV === false` + Bearer present STILL hides the notability-score block.

    Test 4 (graduation does not bleed across same file): render WaterFacilityDetail with `import.meta.env.DEV === false` AND localStorage with valid Bearer (`shouldRenderDashboard()` returns true). The OSM ID row appears (graduated). The notabilityScore row STAYS hidden (its predicate is still `import.meta.env.DEV` which is false).

    Test 5 (no-Bearer prod): both rows hidden.

    Bearer-prod OSM ID tests fail until gate-swap lands. Lockdown tests pass immediately.

  </behavior>
  <action>
    First commit (RED — test only):

    Create `src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx`. Mount WaterFacilityDetail with mock facility having `osmId: 12345` AND `notabilityScore: 87`. Implement:
    - Tests 1-3: 3 OSM ID snapshots (Bearer-prod fails until next commit).
    - Test 4: Bearer-prod render → notabilityScore row IS HIDDEN (passes immediately; this is the regression guard against future graduation).
    - Test 5: no-Bearer-prod render → BOTH rows hidden.

    Commit message: `test(28.2): add WaterFacilityDetail Path-B render-parity + notabilityScore lockdown (RED)`.

    Second commit (GREEN — Path B gate-swap):

    Edit `src/components/detail/WaterFacilityDetail.tsx`:
    1. Add import: `import { shouldRenderDashboard } from '@/lib/dashboardAuth';`
    2. Line 117: wrap the previously always-rendered DetailValue with `{shouldRenderDashboard() && ...}` per UI-SPEC §3.4 Path B.
    3. DO NOT TOUCH lines 238-245 — the notabilityScore block stays gated by `import.meta.env.DEV` per UI-SPEC §1 lockdown. Test 4 in the test file explicitly catches a regression here.

    Run vitest — all 5 tests PASS.

    Commit message: `feat(28.2): gate-swap WaterFacilityDetail OSM ID row to shouldRenderDashboard (D-06, UI-SPEC §3.4 Path B)`. Body cites the CONTEXT D-06 addendum from Task 0 and notes notabilityScore stays locked.

  </action>
  <verify>
    <automated>npx vitest run src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx && grep -n 'shouldRenderDashboard.*OSM ID' src/components/detail/WaterFacilityDetail.tsx && grep -n 'import.meta.env.DEV.*notabilityScore' src/components/detail/WaterFacilityDetail.tsx</automated>
  </verify>
  <done>
    All 5 tests PASS. The OSM ID row at line 117 is wrapped in `shouldRenderDashboard()`. The notabilityScore block at line 238 still uses `import.meta.env.DEV`. The grep verifying both predicates in the same file is the regression guard. 2 atomic commits. `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Lockdown regression test for NotificationCard severity score numeric</name>
  <files>
    src/components/notifications/__tests__/NotificationCard.lockdown.test.tsx
  </files>
  <read_first>
    - src/components/notifications/NotificationCard.tsx (find the severity score numeric block; confirm its predicate is `import.meta.env.DEV` and identify the surrounding test ID or class for query)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§1 lockdown table — first row: NotificationCard severity score numeric)
    - memory/project_dev_score_display.md (the canonical "dev-only forever" memory entry that justifies this lockdown)
  </read_first>
  <behavior>
    Single test asserting the lockdown:
    - Render NotificationCard with mock notification having a non-null severity score (e.g., 8.5).
    - Mock `import.meta.env.DEV = false` AND set localStorage with valid Bearer so `shouldRenderDashboard()` returns true.
    - Assert that the severity score numeric is NOT rendered. Use the most stable selector available — likely a numeric value match or a specific data-testid if one exists. If no testid exists, query by text content `expect(screen.queryByText(/8\.5/)).toBeNull()`.

    Test passes immediately (the predicate is still `import.meta.env.DEV` and Plan 04 must NOT change it). The test exists to make a future accidental graduation FAIL the suite.

  </behavior>
  <action>
    Create `src/components/notifications/__tests__/NotificationCard.lockdown.test.tsx`. Implement the single-test assertion above. Run vitest — passes immediately.

    Commit message: `test(28.2): lockdown regression — NotificationCard severity score stays dev-only (D-07, UI-SPEC §1)`. Body cites memory/project_dev_score_display.md.

  </action>
  <verify>
    <automated>npx vitest run src/components/notifications/__tests__/NotificationCard.lockdown.test.tsx</automated>
  </verify>
  <done>
    Test PASSES. The file is committed. A future agent who tries to swap the NotificationCard severity score predicate from `import.meta.env.DEV` to `shouldRenderDashboard()` will fail this test in CI. `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Lockdown regression test for MapDevExposer window.__map global</name>
  <files>
    src/components/map/__tests__/MapDevExposer.lockdown.test.tsx
  </files>
  <read_first>
    - src/components/map/BaseMap.tsx (focus on line 458 where `<MapDevExposer />` is rendered behind `import.meta.env.DEV`)
    - The MapDevExposer source — locate the file via `grep -rn "function MapDevExposer\|const MapDevExposer" src/` to find the implementation
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§1 lockdown table — second row: MapDevExposer window.__map)
  </read_first>
  <behavior>
    Single test asserting the lockdown:
    - Set `delete (window as unknown as { __map?: unknown }).__map` before each test (clean slate).
    - Mock `import.meta.env.DEV = false` AND set localStorage with valid Bearer so `shouldRenderDashboard()` returns true.
    - Render BaseMap (or directly mount MapDevExposer if BaseMap requires too much WebGL setup; the test exists to verify the gate, not the map).
    - Assert that `window.__map` is `undefined` after render. The exposer must NOT have run.

    Test passes immediately (the predicate is `import.meta.env.DEV` and Plan 04 must NOT change it). Existing in-place to catch future regression.

  </behavior>
  <action>
    Create `src/components/map/__tests__/MapDevExposer.lockdown.test.tsx`. If MapDevExposer is a small component that takes a `map` prop, mount it directly with a mock `map` instance. If it requires the full BaseMap mount, use the existing maplibre-gl mock pattern from `vite.config.ts` test.alias to keep the test in jsdom.

    Implement single test:
    1. `beforeEach` cleans `window.__map`.
    2. Stub `import.meta.env.DEV` to false; set valid Bearer in localStorage.
    3. Render the component.
    4. Assert `(window as { __map?: unknown }).__map === undefined`.

    Commit message: `test(28.2): lockdown regression — MapDevExposer window.__map stays dev-only (D-07, UI-SPEC §1)`. Body notes the footgun-in-prod risk that justifies the lockdown.

  </action>
  <verify>
    <automated>npx vitest run src/components/map/__tests__/MapDevExposer.lockdown.test.tsx</automated>
  </verify>
  <done>
    Test PASSES. A future accidental graduation of MapDevExposer to `shouldRenderDashboard()` will fail this test. `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 6: Repo-wide audit — confirm three lockdown predicates remain `import.meta.env.DEV`</name>
  <files>
    (read-only audit; produces NO file changes)
  </files>
  <read_first>
    - All three lockdown source files cited in UI-SPEC §1 (NotificationCard.tsx, BaseMap.tsx, WaterFacilityDetail.tsx)
  </read_first>
  <action>
    Run three targeted greps to confirm the lockdown surfaces still use `import.meta.env.DEV`:

    1. `grep -n 'import.meta.env.DEV' src/components/notifications/NotificationCard.tsx` — expect ≥1 match (the severity score numeric block).
    2. `grep -n 'import.meta.env.DEV.*MapDevExposer\|MapDevExposer.*import.meta.env.DEV' src/components/map/BaseMap.tsx` — expect 1 match (line ~458). The exact pattern depends on whether the gate wraps the JSX element or the component file itself.
    3. `grep -n 'import.meta.env.DEV.*notabilityScore\|notabilityScore.*import.meta.env.DEV' src/components/detail/WaterFacilityDetail.tsx` — expect 1 match (line ~238 + ±5).

    If ANY of the three greps returns zero matches, a regression has been introduced; halt and surface as HANDOFF blocker.

    Also verify symmetric: the THREE graduated surfaces no longer have `import.meta.env.DEV` at the swap line:
    1. `grep -n 'import.meta.env.DEV' src/components/map/EntityTooltip.tsx` — should NOT match the line previously holding the event-ID gate (other DEV gates in the file may legitimately remain).
    2. `grep -n 'import.meta.env.DEV' src/components/detail/EventDetail.tsx` — line 56 + line 165 should not match.
    3. `grep -n 'import.meta.env.DEV' src/components/detail/WaterFacilityDetail.tsx` — line 117 should not match (the OSM ID row); line 238 SHOULD match (notabilityScore lockdown).

    Capture the audit results in the executor's session log for inclusion in the SUMMARY. No commits are produced by this task — it is a verification gate.

  </action>
  <verify>
    <automated>grep -n 'import.meta.env.DEV' src/components/notifications/NotificationCard.tsx && grep -n 'import.meta.env.DEV' src/components/map/BaseMap.tsx && grep -n 'import.meta.env.DEV' src/components/detail/WaterFacilityDetail.tsx</automated>
  </verify>
  <done>
    All 3 lockdown predicates still match `import.meta.env.DEV`. The 3 graduated swap lines no longer match. Audit log captured. No new commits.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                             |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Dev-only field → Bearer-gated prod field        | Per-field opt-in graduation; symmetric predicate change must NOT bleed across files or across surfaces in the same file |
| Lockdown surfaces → frozen                      | Severity score, MapDevExposer, notabilityScore must NEVER graduate; regression tests are the mechanical enforcement     |
| Bearer presence → privileged surface visibility | `shouldRenderDashboard()` is the only authorization predicate; no per-field access logic                                |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                  |
| ------------ | ---------------------- | --------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28.2-04-01 | Information Disclosure | Locked-dev-only fields accidentally graduated to prod                                                           | mitigate    | Tasks 4 + 5 add explicit lockdown regression tests; Task 6 grep audit codifies the file-line predicate map. Future agent who tries to graduate via search-and-replace fails the test suite                                                                                                                       |
| T-28.2-04-02 | Information Disclosure | Operator metadata (event ID, OSM ID, LLM confidence, geo provenance) leaks to public anonymous users            | mitigate    | Predicate-only swap from `import.meta.env.DEV` to `shouldRenderDashboard()` ensures no-Bearer prod hides the block entirely; Tests 4 in each gate-swap test file assert the absent-block case                                                                                                                    |
| T-28.2-04-03 | Spoofing               | Forged Bearer evades client-side gate                                                                           | accept      | Client-side gate is UX-level only — actual auth enforcement lives in `server/middleware/dashboardAuth.ts` constant-time compare. A forged Bearer that satisfies `hasDashboardKey()` (any non-empty localStorage value) only gets the surface to render; any subsequent fetch needing real Bearer auth still 401s |
| T-28.2-04-04 | Tampering              | Wrapper styling regresses (e.g., `border-t border-white/5` accidentally dropped)                                | mitigate    | UI-SPEC §3.5 render-parity tests assert byte-equality of the dev-render before-and-after the swap. Class drift triggers test failure                                                                                                                                                                             |
| T-28.2-04-05 | Information Disclosure | Future debug field added to graduate-target file accidentally inherits Bearer-gating without per-field decision | mitigate    | D-09 hard constraint: no NEW debug-field surfaces in 28.2. The audit (Task 6) is a static check; future phases that add debug fields re-enter the per-field decision tree                                                                                                                                        |
| T-28.2-04-06 | Repudiation            | Path A vs Path B choice undocumented                                                                            | mitigate    | Task 0 adds the CONTEXT.md addendum; commits cite the addendum; Plan 04 SUMMARY records the choice with UAT-trace                                                                                                                                                                                                |

No Elevation-of-Privilege threats — gate-swap is a visibility predicate, not an authorization upgrade. No DoS threats — surfaces render the same data; visibility is the only delta.
</threat_model>

<verification>
- 9 atomic commits land in order: docs Task 0 → test/feat × 3 (Tasks 1-3, RED+GREEN each = 6 commits) → test Task 4 → test Task 5
- All test files PASS:
  - `src/components/map/__tests__/EntityTooltip.gateSwap.test.tsx` — 4 tests
  - `src/components/detail/__tests__/EventDetail.gateSwap.test.tsx` — 6 tests
  - `src/components/detail/__tests__/WaterFacilityDetail.gateSwap.test.tsx` — 5 tests
  - `src/components/notifications/__tests__/NotificationCard.lockdown.test.tsx` — 1 test
  - `src/components/map/__tests__/MapDevExposer.lockdown.test.tsx` — 1 test
- 3 graduated surfaces import `shouldRenderDashboard` from `@/lib/dashboardAuth`
- 3 lockdown surfaces still use `import.meta.env.DEV` predicate (Task 6 grep audit confirms)
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0; no new react-hooks/exhaustive-deps warnings
- `prettier --check .` exits 0
- Full vitest run green; no regressions in existing component tests
- CONTEXT.md addendum citing Path B is committed
</verification>

<success_criteria>

- A no-Bearer prod build (e.g., simulated via `vi.stubEnv('DEV', false)` + cleared localStorage) shows EntityTooltip without the event-ID subscript, EventDetail without the event-ID row + Confidence/GeoPrecision wrapper, WaterFacilityDetail without the OSM ID row
- The same prod build with a valid `dashboard:auth-key` localStorage entry shows all four surfaces identically to the dev render
- NotificationCard severity score numeric is NEVER visible in any prod build (with or without Bearer)
- `window.__map` is `undefined` in any prod build
- `notabilityScore` row in WaterFacilityDetail is NEVER visible in any prod build
- Hand-off to Plan 05 (W5 dashboard merge): the 4 graduated surfaces ARE the dev/prod gate surface; Plan 05's tab merge does NOT need to re-gate them
- Hand-off to Plan 06 (W6 connectivity audit): no surface created or changed by Plan 04 affects the 16 endpoints
  </success_criteria>

<output>
After completion, create `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-04-SUMMARY.md` recording:
- 9 commit SHAs in chronological order
- Inline diff snippets for each gate-swap (lines changed in EntityTooltip / EventDetail × 2 / WaterFacilityDetail)
- 17 test outcomes (4+6+5+1+1) all PASS
- Path B decision recorded with the CONTEXT.md addendum line
- Task 6 audit log: confirmed predicate-line map (graduated surfaces vs lockdown surfaces)
- Hand-off note for Plan 05: the dashboard tab merge does not need to re-implement the gate predicate; `shouldRenderDashboard()` is the single canonical predicate for the merged tab visibility per D-26
</output>

---
phase: 28.2
plan: 05
type: execute
wave: 5
depends_on:
  - 03
  - 04
files_modified:
  - src/components/ui/DevApiStatus.tsx
  - src/stores/uiStore.ts
  - src/types/ui.ts
  - src/components/ui/__tests__/DevApiStatusAllApisTab.test.tsx
  - src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx
  - src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
  - src/components/ui/__tests__/DevApiStatus.confirmModal.test.tsx
  - src/components/providers/__tests__/HealthStatusProvider.test.tsx
autonomous: true
requirements:
  - D-22
  - D-23
  - D-26
  - D-27
  - D-28
  - D-08
must_haves:
  truths:
    - 'DevApiStatus modal has 4 tabs in order: API Health / Water / Sites / Events (D-27)'
    - "The Overview tab is deleted; DevApiStatusOverviewTab function is removed; its polling-store rows + LLMPipelineSection block are folded into the merged tab (D-22 'nothing lost' contract)"
    - "Tab default key is 'apiHealth' (D-22 / D-27); uiStore + types/ui.ts DevApiStatusTab union both updated"
    - 'The merged API Health tab is gated by shouldRenderDashboard() (D-26); was previously always-visible'
    - "Tier-grouped summary banner renders at top with copy '{N} of {TOTAL} healthy · {N} degraded · {N} unhealthy' + per-tier breakdown 'critical: N/T · non-critical: N/T · cron: N/T' (UI-SPEC §5.3.1 + §10)"
    - 'Per-endpoint quality metrics block renders inside expanded row for events / water / flights only — silent for other endpoints (UI-SPEC §5.3.2)'
    - "Per-endpoint manual retry button renders with class 'text-xs px-2 py-1 rounded border border-white/10 hover:bg-white/5'; copy 'Refresh now' / disabled-state 'Refreshing...' (UI-SPEC §5.3.3 + §10)"
    - "Recent-fetch sparkline renders 10 dots (4px diameter, 4px gap via 'flex gap-1' + 'w-1 h-1') with ok-green / fail-red / empty-bg-white/10 (UI-SPEC §5.3.4 + §10)"
    - 'HealthStatusProvider Test 7 still passes — single-poll guarantee preserved through the merge'
    - 'HealthBanner stays NOT-gated by shouldRenderDashboard() — only the merged tab is gated'
    - "UI-side confirm modal fires on Pin to v1 / Pin to v2 only; copy verbatim per UI-SPEC §10 (modal title 'Confirm pipeline version pin'; button 'Keep v3' + 'Pin to {VERSION}')"
    - '429 replay-quota alert renders in operator-actions block when /llm-replay returns 429; amber color via existing utility set; copy verbatim per UI-SPEC §6.2 + §10'
    - 'Multiples-of-4 spacing only — no py-0.5 / gap-0.5 / py-1.5 (UI-SPEC §7)'
    - 'No new color tokens added; no new Tailwind utilities introduced; no new accent assignments (UI-SPEC §9)'
  artifacts:
    - path: src/components/ui/DevApiStatus.tsx
      provides: 'Merged API Health tab with 4 diagnostic blocks + confirm modal + 429 alert; DevApiStatusOverviewTab function deleted'
      contains: 'apiHealth|tier-summary-banner|api-health-retry-|api-health-sparkline-|replay-quota-alert'
    - path: src/types/ui.ts
      provides: "DevApiStatusTab union updated: 'overview'|'allApis' removed, 'apiHealth' added"
      contains: 'DevApiStatusTab'
    - path: src/stores/uiStore.ts
      provides: "activeDevApiStatusTab default 'apiHealth'"
      contains: 'apiHealth'
    - path: src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx
      provides: 'Tab merge contract tests: D-22 nothing-lost (polling store rows + LLMPipelineSection visible); D-26 Bearer gate; D-27 tab order'
    - path: src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
      provides: '4 new diagnostic blocks: tier summary banner, per-endpoint quality, retry button, sparkline'
    - path: src/components/ui/__tests__/DevApiStatus.confirmModal.test.tsx
      provides: 'Confirm modal fires on v1/v2 pin only, NOT on v3 / clear; verbatim copy assertions'
  key_links:
    - from: src/components/ui/DevApiStatus.tsx
      to: useHealthStatusContext
      via: 'consume HealthStatusProvider via context, NOT useHealthStatus directly'
      pattern: 'useHealthStatusContext'
    - from: src/components/ui/DevApiStatus.tsx
      to: shouldRenderDashboard
      via: 'merged tab visibility predicate'
      pattern: 'showApiHealthTab.*shouldRenderDashboard'
    - from: src/components/ui/DevApiStatus.tsx
      to: dashboardAuthHeaders
      via: 'POST /api/events/llm-pipeline + /llm-replay calls attach Bearer'
      pattern: 'dashboardAuthHeaders'
    - from: src/components/ui/DevApiStatus.tsx
      to: src/lib/colorBridge.ts
      via: 'site-healthy / site-attacked / event-airstrike CSS vars for sparkline + summary banner'
      pattern: "var\\(--color-(site-healthy|site-attacked|event-airstrike)\\)"
---

<objective>
Per CONTEXT D-22/D-23/D-26/D-27/D-28 + UI-SPEC §5: merge the DevApiStatus modal's `Overview` tab into `All APIs`, rename to `API Health`, position first, Bearer-gate via `shouldRenderDashboard()`, and add four new diagnostic blocks. Plus: pin a confirm modal in front of `Pin to v1/v2` operator actions per UI-SPEC §6.1, and surface the 429 replay-quota alert per UI-SPEC §6.2 (the server side ships in Plan 03 — this plan wires the UI surface).

This is W5 of 6. Depends on Plan 03 (operator endpoints + audit log + replay quota are in place server-side; the UI surfaces those signals) and Plan 04 (the gate-swap pattern uses `shouldRenderDashboard()` as the canonical predicate; the merged tab inherits the same gate). Independent of Plans 01 and 02 (no file overlap).

Output:

- DevApiStatus.tsx: delete `DevApiStatusOverviewTab` (~lines 361-554), absorb its body into `DevApiStatusAllApisTab` (~line 995). Rename tab key, label, testid. Bearer-gate via `shouldRenderDashboard()`. Add 4 new diagnostic blocks. Add confirm modal. Add 429 alert.
- uiStore.ts + types/ui.ts: `'overview'` → `'apiHealth'`; remove `'allApis'` from union; add `'apiHealth'`. Default value updated.
- HealthStatusProvider Test 7 must still pass — ZERO new `useHealthStatus` callers added in the merged tab; consumption stays via `useHealthStatusContext()`.
- UI-SPEC §10 verbatim copy — pinned in 9 grep-verifiable strings.
- UI-SPEC §7 spacing — multiples of 4 only.
- D-22 "nothing lost" contract — polling-store metrics (count / avg / success rate / next-poll countdown / recent fetches) + LLMPipelineSection BOTH render in the merged tab on Day 1.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-CONTEXT.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-AI-SPEC.md
@.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md
@CLAUDE.md
@src/components/ui/DevApiStatus.tsx
@src/components/providers/HealthStatusProvider.tsx
@src/lib/dashboardAuth.ts
@src/lib/colorBridge.ts

<interfaces>
Existing surfaces this plan modifies (read-only references):

`src/components/ui/DevApiStatus.tsx` exports the modal. The current structure:

- Tab bar: Overview / Water / Sites / Events / All APIs (`DevApiStatus.tsx:819-865`)
- `DevApiStatusOverviewTab` body: polling-store rows + `LLMPipelineSection` (`DevApiStatus.tsx:361-554`)
- `DevApiStatusAllApisTab` body: `/api/health` aggregate, tier-grouped, expanded-row JSON dump (`DevApiStatus.tsx:995+`)
- Per-store `recentFetches[]` selectors collected at `DevApiStatus.tsx:516,529,540`
- `eventQuality` memo at `DevApiStatus.tsx:545-553`
- `aggregateHealth` from `useHealthStatusContext()` (single-poll consumer)

`src/types/ui.ts` exports `DevApiStatusTab = 'overview' | 'water' | 'sites' | 'events' | 'allApis'` (line 81). Updates: drop `'overview'` and `'allApis'`; add `'apiHealth'`.

`src/stores/uiStore.ts:38` sets `activeDevApiStatusTab: 'overview'`. Update to `'apiHealth'`.

`src/lib/colorBridge.ts` exports the canonical hex strings via re-exports (`COLOR_SITE_HEALTHY_HEX`, `COLOR_SITE_ATTACKED_HEX`, `COLOR_EVENT_AIRSTRIKE_HEX`) and as RGBA tuples for deck.gl. The tab uses CSS vars `var(--color-site-healthy)` / `var(--color-site-attacked)` / `var(--color-event-airstrike)` directly per UI-SPEC §9.

`src/lib/dashboardAuth.ts` exports `shouldRenderDashboard()` (visibility predicate) and `dashboardAuthHeaders()` (Bearer headers for fetches).

Server-side dependencies (delivered by Plan 03):

- `POST /api/events/llm-pipeline` accepts `{version: 'v1'|'v2'|'v3'|null}`; on success writes audit + Redis override (7d TTL).
- `POST /api/events/llm-replay/:groupKey` returns `{old, new}` diff; quota check returns 429 with `Retry-After` + `{error: 'replay_quota_exceeded', message, resetsAt}`.
- `GET /api/events/llm-pipeline` returns the current pinned version (read by Topbar pill — no UI change needed for that surface).
- Per-store `fetchNow()` actions OR cache-bust query params for D-23 retry button — planner audits the stores before drafting; current finding: most stores expose `setActiveSource()` / explicit `fetch*Now()` actions; if any store lacks one, Task 5 falls back to `?_ts=Date.now()` query bust.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update DevApiStatusTab type union + uiStore default key</name>
  <files>
    src/types/ui.ts
    src/stores/uiStore.ts
  </files>
  <read_first>
    - src/types/ui.ts (line 81 + ±10 — confirm union + UIState consumer types)
    - src/stores/uiStore.ts (lines 37-41 — confirm default + setter)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.1 — tab order; §1 surface map row 9)
  </read_first>
  <action>
    Edit `src/types/ui.ts`:
    - Replace `export type DevApiStatusTab = 'overview' | 'water' | 'sites' | 'events' | 'allApis';` with `export type DevApiStatusTab = 'apiHealth' | 'water' | 'sites' | 'events';`. Order matches D-27 (API Health first).

    Edit `src/stores/uiStore.ts`:
    - Replace `activeDevApiStatusTab: 'overview',` (line 38) with `activeDevApiStatusTab: 'apiHealth',`.

    These two changes intentionally happen FIRST so the rest of the plan can be type-checked after each subsequent commit. After this commit, DevApiStatus.tsx will FAIL to type-check (it still references `'overview'` and `'allApis'` literal tab keys). Tasks 2-3 fix that. The temporary RED state is acceptable for one commit.

    Atomic commit message: `refactor(28.2): drop DevApiStatusTab 'overview'+'allApis', add 'apiHealth' (D-22/D-27)`. Body: this is the type-foundation commit; subsequent commits fold the Overview tab body into apiHealth.

    Run `npx tsc --noEmit` — expect failures in DevApiStatus.tsx (those are intentional intermediate state; Tasks 2 + 3 close them).

  </action>
  <verify>
    <automated>grep -n 'apiHealth' src/types/ui.ts && grep -n 'apiHealth' src/stores/uiStore.ts && ! grep -n "'overview'\\|'allApis'" src/types/ui.ts</automated>
  </verify>
  <done>
    `DevApiStatusTab` union is exactly `'apiHealth' | 'water' | 'sites' | 'events'`. uiStore default is `'apiHealth'`. The two type/store edits are committed atomically. tsc shows expected DevApiStatus.tsx errors that Tasks 2-3 will resolve.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tab merge — delete Overview, absorb body into apiHealth, Bearer-gate, reorder</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx
  </files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (the WHOLE file; it is large but the merge is structural — read 0-200 for imports/setup, 360-560 for DevApiStatusOverviewTab, 800-930 for tab bar + body switch, 995-end for DevApiStatusAllApisTab)
    - src/components/providers/HealthStatusProvider.tsx (confirm `useHealthStatusContext` is the canonical hook the merged tab consumes)
    - src/components/providers/__tests__/HealthStatusProvider.test.tsx (Test 7 — TWO consumers ONE provider ONE poll guarantee. Read so the merge does not break it)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.1 tab order, §5.2 polling-store fold-in mapping, §4 surface integrity invariant)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("DevApiStatus Tab Merge" section + "uiStore Tab Default Key" + "tier-grouped summary banner" code excerpt)
    - src/lib/dashboardAuth.ts
  </read_first>
  <behavior>
    Tab merge tests:
    - Test 1 (D-27 tab order): with `shouldRenderDashboard() === true`, the tab bar renders `API Health / Water / Sites / Events` in that order. Use Testing Library `queryAllByRole('tab')` and assert text matches in order.
    - Test 2 (D-22 nothing lost — polling rows): with shouldRenderDashboard true, the merged tab body renders polling-store rows for at least 4 known stores (flights, ships, events, water) with their count + avg + success-rate + next-poll columns. Mount with mocked stores returning a known fixture per store.
    - Test 3 (D-22 nothing lost — LLMPipelineSection): the merged tab body renders the LLM Pipeline block (heading text 'LLM Pipeline' or `data-testid='llm-pipeline-section'` if one exists; otherwise query by heading text).
    - Test 4 (D-26 Bearer gate): `shouldRenderDashboard() === false` → modal trigger / tab body NOT rendered. The tab is hidden.
    - Test 5 (D-26 dev short-circuit): `import.meta.env.DEV === true` AND no Bearer → tab IS rendered (preserves dev UX).
    - Test 6 (HealthStatusProvider single-poll preservation): import the existing `HealthStatusProvider.test.tsx` Test 7 setup; mount the merged DevApiStatus PLUS HealthBanner inside ONE provider; assert `/api/health` was fetched ONCE. Failure = the merge broke single-poll.
    - Test 7 (HealthBanner NOT gated): with shouldRenderDashboard false, HealthBanner STILL renders when there's a critical-tier issue. Confirms the gate change applies only to the modal tab, not to HealthBanner.

    Tab body fold-in tests:
    - Test 8: switching `activeDevApiStatusTab === 'apiHealth'` mounts `DevApiStatusAllApisTab` with new props `rows` (polling rows) and `llmStatus` per PATTERNS.md "Body switch to modify".
    - Test 9: clicking on the merged tab calls `setTab('apiHealth')`, not `setTab('overview')`.

    Tests 1-9 fail before this task lands because: (a) the literal tab keys are `'overview'` / `'allApis'` (Task 1 already changed types but the source still references the old keys, so the file doesn't compile); (b) the polling rows + LLMPipelineSection are rendered only inside the deleted Overview tab.

  </behavior>
  <action>
    First commit (RED — test only):

    Create `src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx`. Use Testing Library + `vi.mock(...)` for `@/lib/dashboardAuth`, `useFlightStore`, `useShipStore`, `useEventsStore`, `useWaterStore` (existing patterns elsewhere in the codebase). Mount `<HealthStatusProvider><DevApiStatus /></HealthStatusProvider>` for context-consuming tests; for Test 6 mount `<HealthStatusProvider><HealthBanner /><DevApiStatus /></HealthStatusProvider>` with a `fetch` spy to count calls.

    Tests fail because DevApiStatus.tsx is currently broken (Task 1's type change). Commit:
    `test(28.2): add DevApiStatus tab merge contract tests (RED)`. Body cites D-22 / D-26 / D-27 + UI-SPEC §4 / §5.

    Second commit (GREEN — DevApiStatus.tsx merge):

    Edit `src/components/ui/DevApiStatus.tsx`:

    1. DELETE the `DevApiStatusOverviewTab` function (lines ~361-554). Capture its rendered JSX into a fragment that will live inside the merged tab body. Specifically the polling-rows table (lines ~373-432) + the per-row expanded JSON-dump area (lines ~434-540 approx) + the `LLMPipelineSection` invocation (find the line in the deleted function — typically `<LLMPipelineSection llmStatus={llmStatus} />` rendered after the table).

    2. Modify `DevApiStatusAllApisTab` signature to accept `rows: ApiRow[]` and `llmStatus: LLMStatus` as additional props. Inside the function body, render in this DOM order per UI-SPEC §5.2:
       - Tier-grouped summary banner (Task 3 adds; placeholder div for now with `data-testid='tier-summary-banner'`).
       - Audit-result banner stub (placeholder for Plan 06 W6 to populate; reads `auditResult` from a sidecar Redis key — leave a `data-testid='audit-result-banner'` div that renders nothing if no data).
       - Existing tier-grouped table from current `DevApiStatusAllApisTab` body — keep as-is.
       - Polling-store row block (folded from deleted Overview tab) — kept inside its own `<section>` so the merged tab body has clear visual hierarchy. Add `data-testid='polling-store-rows'`.
       - LLMPipelineSection — kept BELOW polling-store rows per UI-SPEC §5.2. `data-testid='llm-pipeline-section'`.

    3. Modify the tab bar (`DevApiStatus.tsx:819-865`):
       - Replace `<TabButton active={activeTab === 'overview'} ... testid='tab-overview'>Overview</TabButton>` with the API Health tab: `<TabButton active={activeTab === 'apiHealth'} onClick={() => setTab('apiHealth')} indicator={hasIssue || hasCriticalUnhealthy ? 'red' : undefined} testid='tab-api-health'>API Health</TabButton>`. Combine the Overview's `hasIssue` indicator with the All-APIs `hasCriticalUnhealthy` indicator into one OR — both signals matter on the merged tab.
       - DELETE the `{showAllApisTab && (<TabButton ... All APIs />)}` block (the `showAllApisTab = true` constant becomes obsolete — replace with `const showApiHealthTab = shouldRenderDashboard();`).
       - Order in the JSX matches D-27: API Health → Water → Sites → Events.

    4. Modify the body switch (`DevApiStatus.tsx:895-925`):
       - Delete the `{activeTab === 'overview' && (<DevApiStatusOverviewTab ... />)}` branch.
       - Replace `{activeTab === 'allApis' && showAllApisTab && (<DevApiStatusAllApisTab ... />)}` with `{activeTab === 'apiHealth' && showApiHealthTab && (<DevApiStatusAllApisTab health={aggregateHealth} loading={healthLoading} error={healthError} rows={rows} llmStatus={llmStatus} />)}`.

    5. Update the snap-back guard (`DevApiStatus.tsx:784-786`): replace `if (activeTab === 'sites' && !showSitesTab) setTab('overview');` with `if (activeTab === 'sites' && !showSitesTab) setTab('apiHealth');`. Apply the same change for any other snap-back branch in the same useEffect.

    6. Verify HealthStatusProvider single-poll preservation: the merged `DevApiStatusAllApisTab` continues to consume `aggregateHealth` via `useHealthStatusContext()`. NO new `useHealthStatus()` direct caller added. Re-run `HealthStatusProvider.test.tsx` Test 7 — must still PASS.

    Run vitest — all 9 tests in `DevApiStatus.tabMerge.test.tsx` PASS. Full vitest run green (no regressions). `npx tsc --noEmit` exits 0.

    Commit message: `feat(28.2): merge DevApiStatus Overview into API Health, Bearer-gate, reorder tabs (D-22/D-26/D-27)`. Body: cites D-22 nothing-lost contract (polling rows + LLMPipelineSection both render); single-poll preservation; tab order API Health / Water / Sites / Events.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.tabMerge.test.tsx && npx vitest run src/components/providers/__tests__/HealthStatusProvider.test.tsx && grep -n "apiHealth" src/components/ui/DevApiStatus.tsx && ! grep -n "DevApiStatusOverviewTab" src/components/ui/DevApiStatus.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>
    All 9 tab-merge tests + 7 HealthStatusProvider tests PASS. `DevApiStatusOverviewTab` function is GONE from the source. The tab bar renders 4 tabs in order. The body switch routes `apiHealth` to `DevApiStatusAllApisTab`. tsc clean. lint clean. 2 atomic commits. Single-poll guarantee preserved per Test 6.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tier-grouped summary banner (D-23 block 1)</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
  </files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (current `DevApiStatusAllApisTab` body — find the `data-testid='tier-summary-banner'` placeholder from Task 2)
    - src/lib/healthClient.ts (for `HealthResponse` / `HealthSummary` shape)
    - server/routes/health.ts (the `buildSummary()` function around line 261 — confirm the shape of `health.summary`)
    - server/lib/healthSources.ts (TIER_BY_ENDPOINT — confirm tier classification matches "critical / non-critical / cron")
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.3.1 banner spec + §10 verbatim copy)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Tier-Grouped Summary Banner (D-23 block 1)")
  </read_first>
  <behavior>
    - Test 1: With mocked `health` containing `summary: {critical: {healthy: 5, total: 5}, nonCritical: {healthy: 9, total: 10}, cron: {healthy: 1, total: 1}}` and overall `{healthy: 15, total: 16, degraded: 1, unhealthy: 0}`, the banner renders the verbatim copy `"15 of 16 healthy · 1 degraded · 0 unhealthy"` AND the breakdown line `"critical: 5/5 · non-critical: 9/10 · cron: 1/1"`. Use `screen.getByTestId('tier-summary-banner')` + text-content assertion.
    - Test 2: Color dots use CSS variables `var(--color-site-healthy)` / `var(--color-site-attacked)` / `var(--color-event-airstrike)` — assert via `style.backgroundColor` containing `var(--color-`.
    - Test 3: Spacing classes — banner div has `mb-2 flex items-center gap-3 px-3 py-1 text-[10px]`. Assert via `className.includes(...)`.
    - Test 4: When `health` is null (loading state), the banner is absent (`queryByTestId('tier-summary-banner')` returns null).
    - Test 5: When `summary.cron.total === 0` (Hobby cron cap edge case), the cron breakdown still renders as `cron: 0/0` (no division-by-zero / no falsy hide).
  </behavior>
  <action>
    First commit (RED — tests only — covers all 4 D-23 blocks):

    Create `src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx`. Implement Tests 1-5 for the tier banner. Tests 6-15 for the other 3 blocks (Tasks 4, 5, 6 below) are added in this single test file but split into separate `describe()` blocks per block. Tests for Tasks 4-6 are RED until the corresponding implementation lands.

    Commit message: `test(28.2): add DevApiStatus diagnostic-blocks tests (RED for tier banner + quality + retry + sparkline)`. Body cites D-23 + UI-SPEC §5.3.

    Second commit (GREEN — tier summary banner only):

    Inside `DevApiStatusAllApisTab` body, replace the placeholder `data-testid='tier-summary-banner'` div with the full banner per PATTERNS.md "Tier-Grouped Summary Banner" + UI-SPEC §10 verbatim copy:

    Compute counts: total = `health.endpoints.length` (or use `health.summary.totals` if present); healthy/degraded/unhealthy from filtering `health.endpoints` by `status`. Render the banner ONLY when `health` is non-null per Test 4.

    Inline color usage: `style={{ backgroundColor: 'var(--color-site-healthy)' }}` for green dot; `var(--color-site-attacked)` for orange; `var(--color-event-airstrike)` for red. NO inline hex literals — UI-SPEC §9 mandates colorBridge / CSS-var consumption.

    Spacing classes per UI-SPEC §7 verification: `mb-2 px-3 py-1 gap-3 text-[10px]` — every value is a multiple of 4 (`mb-2` = 8px, `px-3` = 12px, `py-1` = 4px, `gap-3` = 12px). NO `py-0.5` / `gap-0.5` / `py-1.5`.

    Run vitest — Tests 1-5 PASS.

    Commit message: `feat(28.2): add tier-grouped summary banner to API Health tab (D-23 block 1, UI-SPEC §5.3.1)`. Body cites the verbatim copy.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx -t "tier summary banner" && grep -n "tier-summary-banner" src/components/ui/DevApiStatus.tsx && grep -n "var(--color-site-healthy)" src/components/ui/DevApiStatus.tsx && ! grep -n "py-0.5\\|py-1.5\\|gap-0.5" src/components/ui/DevApiStatus.tsx</automated>
  </verify>
  <done>
    Tier banner tests PASS. `data-testid='tier-summary-banner'` is present. Colors source from `var(--color-...)` CSS vars. NO `py-0.5` / `py-1.5` / `gap-0.5` regressions. Spacing values all multiples of 4. 2 atomic commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Per-endpoint quality metrics block (D-23 block 2)</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
  </files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (existing `eventQuality` memo at lines 545-553; the existing `quality` strings on ApiRow at lines 633-651)
    - src/stores/waterStore.ts (locate `filterStats` selector for water admission percentage)
    - src/stores/flightStore.ts (locate `unidentifiedCount` or compute via `flights.filter(f => f.data.unidentified).length`)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.3.2 + §10 verbatim copy: events `Precision: exact {N} / city {N} / region {N}` + `LLM-vs-raw: {N}%`; water `Admission: {N} of {TOTAL} ({PCT}%)`; flights `Unidentified: {N} of {TOTAL}`)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Per-Endpoint Quality Metrics (D-23 block 2)")
  </read_first>
  <behavior>
    - Test 6: events expanded row → renders text matching exactly `"Precision: exact 12 / city 8 / region 3"` and `"LLM-vs-raw: 78%"` when fixture has 23 events with eventQuality `{exact:12, city:8, region:3, llmCount:18, rawCount:5}` (78% rounded). Use `getByText(/Precision: exact 12/)` etc.
    - Test 7: water expanded row → text `"Admission: 305 of 1284 (24%)"` when fixture has filterStats `{admitted:305, total:1284}`.
    - Test 8: flights expanded row → text `"Unidentified: 7 of 142"` when 7 flights have `data.unidentified=true` of 142 total.
    - Test 9: ships expanded row → no quality block (silent — `queryByText(/Precision/)` and `queryByText(/Admission/)` and `queryByText(/Unidentified/)` ALL null).
    - Test 10: quality block renders ABOVE the existing JSON dump per UI-SPEC §5.3.2 — DOM order assertion via `compareDocumentPosition` or by checking `closest('[data-testid=expanded-row]')` ordering.
  </behavior>
  <action>
    Inside `DevApiStatusAllApisTab` body, in the per-endpoint expanded row (current location around the JSON dump area in the merged body), add a quality metrics fragment ABOVE the JSON dump.

    The selector logic: switch on `ep.name` (the endpoint name from `health.endpoints[i].name` — values like `'Flights'`, `'Events'`, `'Water'`). For matching names, render the quality block; for others, render `null`.

    Reuse `eventQuality` memo (already present in the file pre-merge). For water, add a `filterStats` selector via `useWaterStore(s => s.filterStats)`. For flights, add a derived `unidentifiedCount` via `useFlightStore(s => s.flights.filter(f => f.data.unidentified).length)` and `totalFlights` via `useFlightStore(s => s.flights.length)`.

    Verbatim copy per UI-SPEC §10:
    - Events: `Precision: exact {exact} / city {city} / region {region}` + ` LLM-vs-raw: {pct}%` where pct = `Math.round(llmCount / (llmCount + rawCount) * 100)`. Two `<DetailValue>`-style rows or two `<div>` rows; reuse the existing label-value pattern in DevApiStatus.
    - Water: `Admission: {admitted} of {total} ({pct}%)` where pct = `Math.round(admitted / total * 100)`.
    - Flights: `Unidentified: {unid} of {total}`.

    Empty case (other endpoints): render nothing (silent — no "no metrics" placeholder per UI-SPEC §5.3.2).

    Run vitest — Tests 6-10 PASS.

    Commit message: `feat(28.2): add per-endpoint quality metrics to expanded row (D-23 block 2, UI-SPEC §5.3.2)`.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx -t "per-endpoint quality" && grep -n "Precision: exact" src/components/ui/DevApiStatus.tsx && grep -n "Admission:" src/components/ui/DevApiStatus.tsx && grep -n "Unidentified:" src/components/ui/DevApiStatus.tsx</automated>
  </verify>
  <done>
    Tests 6-10 PASS. The 3 verbatim copy strings are grep-findable in the source. Other endpoints render no quality block. 1 atomic commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Per-endpoint manual retry button (D-23 block 3)</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
  </files>
  <read_first>
    - src/components/ui/DashboardAuthModal.tsx (lines 123-138 — the existing button-pattern analog)
    - src/components/ui/DevApiStatus.tsx (the expanded-row area where the button mounts — below the quality block from Task 4)
    - src/stores/flightStore.ts, src/stores/shipStore.ts, src/stores/eventStore.ts, src/stores/newsStore.ts, src/stores/marketStore.ts, src/stores/weatherStore.ts, src/stores/siteStore.ts, src/stores/waterStore.ts (audit each: does the store expose a `fetchNow()` / `refreshNow()` action? Document findings in the executor's session log)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.3.3 + §10 — class spec verbatim, `Refresh now` / `Refreshing...` copy)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Per-Endpoint Manual Retry Button (D-23 block 3)")
    - CLAUDE.md ("D-23 retry button MUST NOT bust LLM cache keys — only polling-store cache keys")
  </read_first>
  <behavior>
    - Test 11: Render expanded row for `Flights`. The button is present with text `Refresh now` and class containing `text-xs px-2 py-1 rounded border border-white/10 hover:bg-white/5`. data-testid `api-health-retry-Flights` (or per-endpoint scheme).
    - Test 12: Click the button. Button text changes to `Refreshing...` (disabled-state copy). The store's `fetchNow()` action is invoked (mock the store).
    - Test 13: After the mocked fetch resolves, button reverts to `Refresh now`.
    - Test 14 (cache-bust scope): Verify the retry mechanic does NOT touch `events:llm:v3` Redis key. This is best validated server-side, but client-side test asserts the button's onClick handler does NOT call any URL containing `events:llm:v3` or `llm-replay`. Use a `fetch` spy.
    - Test 15: Bearer is attached to ANY fetch the retry button issues — reuse `dashboardAuthHeaders()` so the request piggybacks on the operator's existing token. Assert via `fetch` spy: `expect(fetchSpy).toHaveBeenCalledWith(expect.any(String), expect.objectContaining({headers: expect.objectContaining({Authorization: expect.stringMatching(/^Bearer /)})}))`.
  </behavior>
  <action>
    Audit each store and document findings in the executor session log:
    - For stores exposing `fetchNow()` (or equivalent): wire the button's `onClick` to `useXStore.getState().fetchNow()`.
    - For stores WITHOUT a fetch action: use the cache-bust query-param fallback — `fetch(`/api/${endpoint}?_ts=${Date.now()}`, {headers: {...dashboardAuthHeaders()}})`. The store's polling will pick up the fresh cache on its next tick.

    Add a small handler `handleRefreshNow(endpointName: string)` inside `DevApiStatusAllApisTab`:
    1. Set `isRefreshing` state to true (per-endpoint `Set<string>` to allow concurrent refreshes).
    2. Dispatch the store-specific action OR cache-bust fetch.
    3. On settle (success or fail), remove from `isRefreshing` set after a small minimum (e.g., 200ms) so the button visibly transitions.

    Render inside the per-endpoint expanded row, BELOW the quality block from Task 4:

    Button JSX per UI-SPEC §5.3.3 (class verbatim):
    `<button type='button' disabled={isRefreshing.has(ep.name)} onClick={() => handleRefreshNow(ep.name)} className='text-xs px-2 py-1 rounded border border-white/10 hover:bg-white/5' data-testid={\`api-health-retry-${ep.name}\`}>{isRefreshing.has(ep.name) ? 'Refreshing...' : 'Refresh now'}</button>`

    Critical: the button MUST NOT call `/api/events/llm-replay/...` or any cache-bust against `events:llm:v3`. Per CLAUDE.md "D-23 retry button MUST NOT bust LLM cache keys".

    Run vitest — Tests 11-15 PASS.

    Commit message: `feat(28.2): add per-endpoint manual retry button (D-23 block 3, UI-SPEC §5.3.3)`. Body: documents per-store fetchNow/cache-bust mechanic and confirms no LLM-cache write occurs.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx -t "manual retry button" && grep -n "api-health-retry-" src/components/ui/DevApiStatus.tsx && grep -n "Refresh now" src/components/ui/DevApiStatus.tsx && ! grep -n "llm-replay\\|events:llm:v3" src/components/ui/DevApiStatus.tsx | grep -v "// "</automated>
  </verify>
  <done>
    Tests 11-15 PASS. Button class matches `text-xs px-2 py-1 rounded border border-white/10 hover:bg-white/5` exactly. `Refresh now` / `Refreshing...` copy verbatim. NO LLM cache key referenced in the retry handler. 1 atomic commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Recent-fetch sparkline (D-23 block 4)</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx
  </files>
  <read_first>
    - src/components/ui/DevApiStatus.tsx (the existing `recentFetches[]` selectors at lines 516, 529, 540 — the same selectors feed the sparkline)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§5.3.4 + §10 — `flex gap-1` + `w-1 h-1`; ok-green via `--color-site-healthy`; fail-red via `--color-event-airstrike`; empty `bg-white/10`)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("Recent-Fetch Sparkline (D-23 block 4)")
    - src/lib/colorBridge.ts (CSS-var tokens — confirm `--color-site-healthy` and `--color-event-airstrike` exist in the @theme block)
  </read_first>
  <behavior>
    - Test 16: Render row for an endpoint where `recentFetches` has 7 entries (5 ok, 2 fail). The sparkline element with `data-testid='api-health-sparkline-Flights'` contains exactly 10 spans. The first 3 spans (oldest, padding) have class `bg-white/10`; the remaining 7 have inline backgroundColor matching the var pattern (5 green, 2 red, ordered oldest-left → newest-right).
    - Test 17: Render row where `recentFetches` is empty. All 10 dots are `bg-white/10` placeholder.
    - Test 18: Render row where `recentFetches` has 15 entries. Only the LAST 10 (newest) render — confirms the slice/window logic.
    - Test 19: Each dot has class `w-1 h-1 rounded-full`; the container has `flex gap-1`. NO `gap-0.5` / `w-0.5` regressions.
    - Test 20: Position — sparkline renders inside the per-endpoint row (NOT the expanded view) per UI-SPEC §5.3.4. Assert by querying the row's `<tr>` element for the sparkline within it.
  </behavior>
  <action>
    Inside the per-endpoint table row of `DevApiStatusAllApisTab` (NOT the expanded view), add the sparkline as a new `<td>` right before the chevron / status column. Implementation per PATTERNS.md "Recent-Fetch Sparkline":

    For each row, compute `recentFetches` from the matching store via `useXStore(s => s.recentFetches)`. The map from row-name to store is the same map used in Task 5's retry button.

    JSX:
    `<div className='flex gap-1' data-testid={\`api-health-sparkline-${ep.name}\`}> {Array.from({length: 10}, (_, i) => { const fetch = recentFetches[recentFetches.length - 10 + i]; if (!fetch) return <span key={i} className='w-1 h-1 rounded-full bg-white/10' />; const bg = fetch.ok ? 'var(--color-site-healthy)' : 'var(--color-event-airstrike)'; return <span key={i} className='w-1 h-1 rounded-full' style={{backgroundColor: bg}} />; })} </div>`

    No tooltip on hover per UI-SPEC §5.3.4 — the JSON dump in expanded row carries detail.

    Run vitest — Tests 16-20 PASS.

    Commit message: `feat(28.2): add recent-fetch sparkline to API Health tab rows (D-23 block 4, UI-SPEC §5.3.4)`.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.diagnosticBlocks.test.tsx -t "sparkline" && grep -n "api-health-sparkline-" src/components/ui/DevApiStatus.tsx && grep -n "var(--color-event-airstrike)" src/components/ui/DevApiStatus.tsx</automated>
  </verify>
  <done>
    Tests 16-20 PASS. Sparkline renders 10 dots with `flex gap-1 w-1 h-1` classes. Color sources from CSS vars. 1 atomic commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: UI confirm modal on Pin to v1/v2 + 429 replay-quota alert</name>
  <files>
    src/components/ui/DevApiStatus.tsx
    src/components/ui/__tests__/DevApiStatus.confirmModal.test.tsx
  </files>
  <read_first>
    - src/components/ui/DashboardAuthModal.tsx (full file — the modal pattern analog: backdrop, Escape capture-phase, dual-button footer)
    - src/components/ui/DevApiStatus.tsx (find the LLMPipelineSection or wherever `Pin to v1/v2` operator action is wired; if not yet present, add it as part of this task's wiring — the operator-action UI may be a small new sub-block in the merged tab)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§6.1 confirm modal verbatim copy; §6.2 429 alert verbatim copy; §10 copy table)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-AI-SPEC.md (§6 — UI-side confirm modal row + per-Bearer replay quota row)
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-PATTERNS.md ("UI Confirm Modal on Pin to v1/v2" + "W6 Audit-Result Banner + 429 Quota Alert")
    - src/lib/dashboardAuth.ts (`dashboardAuthHeaders` for the POST /llm-pipeline call)
  </read_first>
  <behavior>
    Confirm modal:
    - Test 21: Click `Pin to v1` button. Modal appears with title `Confirm pipeline version pin`, body containing `Pin LLM pipeline to v1 for 7 days.` + `v3 is current production. Pinning to v1 disables ongoing v3 enrichment until the pin clears or you revert.` + `Continue?` (entire body verbatim per UI-SPEC §10).
    - Test 22: Modal has two buttons. Cancel button text is `Keep v3` (NOT `Cancel`); class `rounded-md px-3 py-1.5 text-xs text-text-muted hover:bg-white/5`. Confirm button text is `Pin to v1` (variable substitution); class includes `bg-red-500/10 border border-red-500/20 text-red-400`.
    - Test 23: Click `Keep v3` → modal closes; POST /llm-pipeline NOT issued.
    - Test 24: Click `Pin to v1` → modal closes; POST /llm-pipeline issued with body `{version: 'v1'}` and Bearer header attached.
    - Test 25: Press Escape key → modal closes (capture-phase listener per DashboardAuthModal pattern).
    - Test 26: Click `Pin to v3` → modal does NOT fire (v3 is current production; no friction needed).
    - Test 27: Click `Clear pin` (which sends `{version: null}`) → modal does NOT fire.

    429 quota alert:
    - Test 28: Mock POST /llm-replay returning 429 with body `{error: 'replay_quota_exceeded', resetsAt: '2026-05-05T00:00:00.000Z'}`. After invocation, the merged tab's operator-actions block renders an inline alert with `data-testid='replay-quota-alert'`, copy `Replay quota reached: 50 of 50 in last 24h. Resets at 2026-05-05T00:00:00.000Z. Quota protects daily token budget.` (verbatim per UI-SPEC §10) and class `bg-amber-500/10 border border-amber-500/20 text-amber-400`.
    - Test 29: When the quota resets (subsequent /llm-replay returns 200), the alert disappears.
    - Test 30: Spacing — alert has `px-2 py-1` padding. NO `py-0.5` / `py-1.5`.

  </behavior>
  <action>
    First commit (RED — tests only):

    Create `src/components/ui/__tests__/DevApiStatus.confirmModal.test.tsx`. Mount DevApiStatus inside HealthStatusProvider. Mock `fetch` to track POST /llm-pipeline + POST /llm-replay calls. Implement Tests 21-30. Tests fail because the confirm modal + 429 alert do not exist yet.

    Commit message: `test(28.2): add DevApiStatus confirm modal + 429 alert tests (RED)`.

    Second commit (GREEN — confirm modal + 429 alert):

    Inside `DevApiStatus.tsx`:

    1. Locate the operator-action block where `Pin pipeline to v1/v2/v3` buttons are rendered. If the block does not yet exist (Phase 27.4.1 stripped click-to-swap from Topbar pill but the dashboard MUST have a Pin button per AI-SPEC §6), add a small `<section>` with `data-testid='operator-actions'` inside the merged API Health tab body containing four buttons: `Pin to v1`, `Pin to v2`, `Pin to v3`, `Clear pin`.

    2. Confirm-modal state: `const [confirmTarget, setConfirmTarget] = useState<'v1' | 'v2' | null>(null);`. Each Pin-to-v1/v2 button onClick sets `setConfirmTarget(versionString)` BEFORE issuing the POST. Pin-to-v3 and Clear-pin issue the POST directly per UI-SPEC §6.1 ("Triggers ONLY for `version === 'v1' || version === 'v2'`").

    3. Render the modal (analogous to DashboardAuthModal — copy structure verbatim per PATTERNS.md "UI Confirm Modal on Pin to v1/v2"): backdrop `fixed inset-0 z-[var(--z-modal)] flex items-center justify-center bg-black/60 backdrop-blur-sm`; card `w-[360px] rounded-lg border border-border bg-surface-overlay p-5 shadow-xl`; title `text-sm font-semibold text-text-primary` reading `Confirm pipeline version pin`; body `text-xs text-text-muted` with the 4-line block from UI-SPEC §10 verbatim; dual-button footer with `Keep v3` (secondary) + `Pin to ${target}` (destructive primary).

    4. Escape capture-phase listener: `useEffect(() => { const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') { e.stopPropagation(); setConfirmTarget(null); }}; document.addEventListener('keydown', onKey, true); return () => document.removeEventListener('keydown', onKey, true); }, []);` inside the modal component.

    5. On `Pin to ${target}` confirm: issue `fetch('/api/events/llm-pipeline', {method: 'POST', headers: {'Content-Type': 'application/json', ...dashboardAuthHeaders()}, body: JSON.stringify({version: target})})`; await response; close modal.

    6. 429 quota alert state: track the most-recent /llm-replay response. If 429 with `error: 'replay_quota_exceeded'`, store `{message, resetsAt}`. Render an inline alert (above the operator-actions block) with `data-testid='replay-quota-alert'`, class `rounded bg-amber-500/10 border border-amber-500/20 px-2 py-1 text-xs text-amber-400`, copy from UI-SPEC §10. The alert clears when a subsequent /llm-replay returns 200.

    Verify spacing audit per UI-SPEC §7: ALL new elements use multiples of 4 — no `py-0.5` / `py-1.5` / `gap-0.5`.

    Run vitest — Tests 21-30 PASS.

    Commit message: `feat(28.2): add Pin-to-v1/v2 confirm modal + 429 replay-quota alert (D-08, UI-SPEC §6.1/§6.2)`.

  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/DevApiStatus.confirmModal.test.tsx && grep -n "Confirm pipeline version pin" src/components/ui/DevApiStatus.tsx && grep -n "Keep v3" src/components/ui/DevApiStatus.tsx && grep -n "Replay quota reached" src/components/ui/DevApiStatus.tsx && ! grep -n "py-0.5\\|py-1.5\\|gap-0.5" src/components/ui/DevApiStatus.tsx</automated>
  </verify>
  <done>
    Tests 21-30 PASS. Modal title + body + Keep-v3 button + Pin-to-v1/v2 button copy all grep-findable. 429 alert copy verbatim. Confirm modal does NOT fire on v3 or clear. Spacing values all multiples of 4. 2 atomic commits.
  </done>
</task>

<task type="auto">
  <name>Task 8: UI-SPEC §12 acceptance audit</name>
  <files>
    (read-only audit; produces NO file changes — gates wave-close handoff to Plan 06)
  </files>
  <read_first>
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-UI-SPEC.md (§12 acceptance criteria — full table)
  </read_first>
  <action>
    Walk every checklist item in UI-SPEC §12 (12.1 through 12.5) and capture pass/fail in the executor session log:

    §12.1 Gate-swap render parity: ALREADY satisfied by Plan 04 (out of scope for Plan 05; just confirm the snapshot tests still PASS).
    §12.2 Surface-integrity invariants: assert via direct test runs:
    - `npx vitest run src/components/providers/__tests__/HealthStatusProvider.test.tsx` — Test 7 PASS.
    - HealthBanner-not-gated test — locate or create a quick test asserting HealthBanner renders even when shouldRenderDashboard is false.
    - Topbar pipeline pill test — confirm renders whenever shouldRenderDashboard is true (existing pre-28.2 test should still pass).
    §12.3 Lockdown: ALREADY satisfied by Plan 04 (just confirm the lockdown tests still PASS).
    §12.4 New surfaces: walk the 8 acceptance lines; confirm each via the diagnostic-blocks test file + confirm-modal test file.
    §12.5 No drift: run `git diff --name-only main HEAD -- src/styles/app.css src/lib/colorBridge.ts` — assert ZERO changes (no new color tokens added). Run `npx tsc --noEmit` exit 0. Run `npm run lint` exit 0. Run `prettier --check .` exit 0.

    If any audit item fails, surface as HANDOFF blocker. No commits produced by this task.

  </action>
  <verify>
    <automated>npx vitest run --reporter=basic 2>&1 | tail -5 && npx tsc --noEmit && npm run lint 2>&1 | tail -3 && npx prettier --check . 2>&1 | tail -5 && ! git diff --name-only HEAD~10 HEAD -- src/styles/app.css src/lib/colorBridge.ts | grep -E "."</automated>
  </verify>
  <done>
    All §12 acceptance items checked. Test suite green. tsc clean. lint clean. prettier clean. NO drift in app.css / colorBridge.ts. Audit log captured for SUMMARY.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                                                 |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Operator → POST /api/events/llm-pipeline        | Confirm modal adds UI friction for destructive pipeline pins; bypassable via curl per AI-SPEC §6 (intentional — operator scripts must work) |
| Operator → POST /api/events/llm-replay          | Bearer attached via `dashboardAuthHeaders()`; quota enforcement is server-side (Plan 03); UI surfaces the 429                               |
| Merged tab visibility → shouldRenderDashboard() | Single canonical predicate; D-26 inheritance from D-06 keeps the gate uniform                                                               |
| HealthStatusProvider single-poll                | Test 7 enforces "TWO consumers ONE provider ONE poll"; merge MUST NOT add a second `useHealthStatus` direct caller                          |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                           |
| ------------ | ---------------------- | ------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28.2-05-01 | Tampering              | Retry button accidentally busts LLM cache                                       | mitigate    | Test 14 explicitly asserts no fetch to `/llm-replay` or any URL containing `events:llm:v3`. Code review checklist + grep gate in CI                                                                                                                                                       |
| T-28.2-05-02 | Information Disclosure | Operator metadata leaks to anonymous public via merged tab if Bearer-gate fails | mitigate    | D-26 + Test 4 in tab-merge tests assert tab is hidden when shouldRenderDashboard is false. Predicate is the canonical `shouldRenderDashboard()` (Plan 04 swap pattern reused)                                                                                                             |
| T-28.2-05-03 | Tampering              | HealthStatusProvider single-poll regresses (extra fetch per cycle)              | mitigate    | Test 6 in tab-merge + existing HealthStatusProvider.test.tsx Test 7 BOTH assert exactly one /api/health fetch per cycle. CI catches a regression                                                                                                                                          |
| T-28.2-05-04 | Spoofing               | Confirm modal bypassed via JS console (operator clicks Pin via setState)        | accept      | Confirm modal is UI friction only per UI-SPEC §6.1; server-side has no enforcement. Direct curl is intentional for operator scripts. Console-bypass equivalence to curl                                                                                                                   |
| T-28.2-05-05 | Information Disclosure | 429 alert leaks `resetsAt` ISO timestamp to operator                            | accept      | The timestamp is operator-facing intentionally (helps the operator decide whether to wait). Not a secret                                                                                                                                                                                  |
| T-28.2-05-06 | Denial of Service      | Retry button storm (rapid clicks) creates upstream API burst                    | mitigate    | Per-store `isRefreshing` Set guards against concurrent re-clicks; the 200ms minimum re-enable visibly throttles the click cadence; underlying Plan 02 rate-limit Bearer-bypass means no 429 from /api/\* but per-endpoint upstream limits (e.g., Nominatim 1 rps) still apply server-side |
| T-28.2-05-07 | Tampering              | Pin-to-v1 confirm modal closes without firing the POST due to Escape race       | mitigate    | Tests 23 + 25 codify Escape and Keep-v3 as cancel paths; the POST only fires after explicit Pin-to-{version} click. Test 24 asserts the success path                                                                                                                                      |
| T-28.2-05-08 | Information Disclosure | Sparkline renders fetch failure history (potentially containing PII)            | accept      | `recentFetches[]` is metadata-only: `{timestamp, ok, latency, status}`. No URL paths or request bodies. Already exposed in pre-28.2 Overview tab — no new exposure                                                                                                                        |

No Elevation-of-Privilege threats — all operator surfaces are pre-existing endpoints (Plan 03 affirmed Bearer-gating); the merge changes presentation, not authorization.
</threat_model>

<verification>
- 9 atomic commits (1 type/store + 1 RED tab-merge test + 1 GREEN tab-merge + 1 RED diagnostic-blocks tests + 4 GREEN diagnostic blocks + 1 RED confirm-modal tests + 1 GREEN confirm-modal+alert + 0 audit task)
- All test files PASS:
  - DevApiStatus.tabMerge.test.tsx — 9 tests
  - DevApiStatus.diagnosticBlocks.test.tsx — 15 tests (5 + 5 + 5 + 5 sub-blocks; some shared describe blocks)
  - DevApiStatus.confirmModal.test.tsx — 10 tests
  - HealthStatusProvider.test.tsx — Test 7 still PASS
- DevApiStatus.tsx: `DevApiStatusOverviewTab` function gone; `DevApiStatusAllApisTab` consumes new props (`rows`, `llmStatus`); 4 diagnostic blocks present; confirm modal + 429 alert present
- Tab order in DOM: API Health → Water → Sites → Events
- 9 verbatim copy strings from UI-SPEC §10 grep-findable in source
- All NEW spacing values multiples of 4 (`grep -n 'py-0.5\\|py-1.5\\|gap-0.5'` returns 0 matches)
- NO new color tokens in app.css; NO new exports in colorBridge.ts
- `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `prettier --check .` exits 0
- Full vitest suite green (no regressions)
</verification>

<success_criteria>

- A Bearer-prod browser session shows: tab bar with `API Health / Water / Sites / Events` (4 tabs in order); merged API Health tab body containing tier summary banner + per-endpoint rows with sparklines + LLMPipelineSection block + operator-actions section
- Clicking `Pin to v1` opens the confirm modal; `Keep v3` cancels; `Pin to v1` button issues POST /api/events/llm-pipeline with valid Bearer
- Hitting the per-Bearer replay quota (50/24h via Plan 03) surfaces the amber 429 alert with verbatim copy
- A no-Bearer prod browser session shows ZERO tabs (modal trigger hidden) but HealthBanner still fires on critical-tier outage
- HealthStatusProvider still issues exactly ONE /api/health fetch per cycle when both HealthBanner + DevApiStatus consume from the same provider
- Hand-off to Plan 06 (W6 connectivity audit): the audit can issue probes against the 16 endpoints with Bearer attached; merged-tab UI exists for the operator to verify "is the prod surface clean right now?" via the tier summary banner + audit-result banner placeholder (Plan 06 will populate the placeholder)
  </success_criteria>

<output>
After completion, create `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-05-SUMMARY.md` recording:
- 9 commit SHAs in chronological order
- Audit log of per-store retry mechanic chosen for each of the 8 stores (fetchNow / cache-bust / dedicated route)
- Final 4-tab order screenshot trace (the executor captures the UI test output)
- 34 test outcomes (9 + 15 + 10) all PASS
- HealthStatusProvider Test 7 status (PASS — single-poll preserved)
- Audit-result banner placeholder location for Plan 06 to fill
- Hand-off note for Plan 06: the merged tab consumes a sidecar Redis key (e.g., `audit:connectivity:last-result`) for the W6 banner. Plan 06 must (a) write the sidecar key as part of its CI workflow's prod-audit step and (b) extend `/api/health` (or add a `/api/audit-status` route per CONTEXT D-23 Claude-discretion) to surface the key value to the merged tab
</output>

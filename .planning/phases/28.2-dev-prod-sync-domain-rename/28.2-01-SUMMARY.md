---
phase: 28.2-dev-prod-sync-domain-rename
plan: 01
subsystem: infra
tags: [vercel, deployment, domain-rename, k6, playwright, runbook]

# Dependency graph
requires:
  - phase: 28.1-cleanup-sweep
    provides: "Stable baseline (all 7 waves shipped to main 2026-05-04 merge 71fc33b); rate-limiter source-of-truth at server/middleware/rateLimit.ts:131 = createRateLimiter(60, 60, 'ratelimit:public')"
provides:
  - 'scripts/load-test.js BASE_URL default points at https://otg-iran-monitor.vercel.app — 28.3 k6 sweep target locked'
  - 'scripts/load-test.spec.ts PROD_URL default points at https://otg-iran-monitor.vercel.app — Playwright validation follows the k6 BASE_URL'
  - 'CHANGELOG.md historical Production URL note updated to https://otg-iran-monitor.vercel.app'
  - 'docs/runbook.md operator instructions point at https://otg-iran-monitor.vercel.app'
  - 'memory/reference_deployment.md auto-memory entry refreshed (zack-mazs-projects/otg-iran-monitor + new URL)'
  - 'Repo-wide grep audit confirms zero irt-monitoring literals remain in editable code/docs'
affects: [28.2-02, 28.2-03, 28.2-04, 28.2-05, 28.2-06, 28.3]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Atomic per-file commit train for cross-cutting string substitutions (one logical concern = one commit, bisect-friendly)'
    - 'Verify-and-skip semantics for partially-pre-applied plan tasks (Task 0 CONTEXT.md doc-fix landed during plan revision; verifier confirmed via grep)'

key-files:
  created:
    - .planning/phases/28.2-dev-prod-sync-domain-rename/28.2-01-SUMMARY.md
  modified:
    - scripts/load-test.js
    - scripts/load-test.spec.ts
    - CHANGELOG.md
    - docs/runbook.md
    - '[auto-memory] ~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md'

key-decisions:
  - 'Task 0 (CONTEXT.md D-04 doc-fix 6-req/min → 60-req/min) was already applied during plan revision (commit 10e3178); verifier confirmed via grep `6-req/min` returns only the historical-correction addendum on line 44, no live occurrences. Skipped per plan instruction (verify-and-skip if grep returns 0).'
  - 'memory/reference_deployment.md is an auto-memory file at ~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md, OUTSIDE the repo working tree. It was edited in place but is NOT committable to the repo (auto-memory persists across sessions independent of git). Plan files_modified frontmatter listed memory/reference_deployment.md as if it were a repo path; treated as the auto-memory location.'
  - 'Task 6 (Vercel-side redirect strategy) — operator picked **Option A: in-place rename**. The Vercel project will be renamed `irt-monitoring` → `otg-iran-monitor` in the dashboard; old URL returns 404 after DNS propagation (~5 min). No code change required — vercel.json redirect rule and permanent alias both rejected (cleanest end-state preferred over hard-link compatibility). Operator confirms `curl -sI https://otg-iran-monitor.vercel.app/api/health` returns 200 post-rename before Plan 06 connectivity audit runs.'

patterns-established:
  - 'Atomic per-file string-substitution commits with `feat(28.2):` for code defaults and `docs(28.2):` for prose/historical/operator-doc updates — preserves the conventional-commits convention `<type>(<scope>): <subject>` where type signals what changed semantically (a code default that downstream behavior depends on = feat, a historical record update = docs)'

requirements-completed: [D-03, D-04]

# Metrics
duration: ~25 min (Tasks 0-5; Task 6 resolved by operator decision)
completed: 2026-05-05
---

# Phase 28.2 Plan 01: Domain Rename — Substitution Pass Summary

**Five-file string substitution from `irt-monitoring.vercel.app` → `otg-iran-monitor.vercel.app` plus the CONTEXT.md D-04 rate-limit doc-fix (6→60 req/min); 4 atomic commits landed, repo-wide grep clean, Vercel-side redirect strategy pending operator decision (Task 6 checkpoint).**

## Performance

- **Duration:** ~25 min (substitution + verify; Task 6 checkpoint not included)
- **Started:** 2026-05-05T16:05Z
- **Completed:** 2026-05-05T16:30Z (Tasks 0-5 done; Task 6 awaits operator)
- **Tasks:** 5 of 7 complete (Task 0 verified-and-skipped; Tasks 2-5 committed; Task 6 checkpoint blocking; Task 1 read-only audit folded into Tasks 2-5 verification)
- **Files modified:** 4 in repo + 1 auto-memory file outside repo

## Accomplishments

- `scripts/load-test.js` line 18 `BASE_URL` default updated to `https://otg-iran-monitor.vercel.app` — 28.3 k6 sweep now targets the live deployment by default
- `scripts/load-test.spec.ts` line 16 `PROD_URL` default updated — Playwright validation follows the k6 BASE_URL
- `CHANGELOG.md` line 78 Phase 21.3 Results header updated (historical record reflects the renamed domain)
- `docs/runbook.md` line 509 CORS_ORIGIN guidance updated to point operators at the new domain
- Auto-memory `reference_deployment.md` refreshed (project name + Production URL)
- Repo-wide grep audit (excluding historical paths: `.planning/phases/`, `.planning/milestones/`, `load-test-results/`, `.claude/worktrees/`, `.git/`) returns ZERO `irt-monitoring` matches in editable code/docs

## Task Commits

1. **Task 0: CONTEXT.md D-04 description (6→60 req/min) doc-fix** — already applied during plan revision (commit `10e3178`, plan revision rev1); verifier confirmed via `grep "6-req/min"` returning only the historical-correction addendum on line 44. No new commit needed (verify-and-skip per plan instruction).
2. **Task 1: Repo-wide audit** — read-only audit folded into Task 5 verification (no commit). Confirmed 5 expected editable files; surfaced 3 additional non-editable paths (recorded as deviations below).
3. **Task 2: scripts/load-test.js BASE_URL** — `5548b3a` (feat)
4. **Task 3: scripts/load-test.spec.ts PROD_URL** — `a229628` (feat)
5. **Task 4 Commit A: CHANGELOG.md Production URL** — `6927e9d` (docs)
6. **Task 4 Commit B: docs/runbook.md production URL** — `9d2da12` (docs)
7. **Task 4 Commit C: memory/reference_deployment.md** — auto-memory file edited in place; NOT a repo commit (file lives at `~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md` outside the working tree).
8. **Task 5: Post-substitution sweep** — verification only (no commit). Confirmed zero `irt-monitoring` literals in editable code/docs.

**Plan metadata commit:** This SUMMARY's Task-6-resolved version (operator picked Option A — in-place rename, no code change required). Lands as `docs(28.2): close plan 01 — Task 6 operator decision recorded (in-place rename)`.

## Files Created/Modified

- `scripts/load-test.js` (modified, line 18) — `BASE_URL` default now `https://otg-iran-monitor.vercel.app`
- `scripts/load-test.spec.ts` (modified, line 16) — `PROD_URL` default now `https://otg-iran-monitor.vercel.app`
- `CHANGELOG.md` (modified, line 78) — Phase 21.3 Results header updated
- `docs/runbook.md` (modified, line 509) — CORS_ORIGIN production URL example updated
- `~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md` (modified) — auto-memory file (lives outside repo); both `Project: zack-mazs-projects/irt-monitoring` and `Production URL: irt-monitoring.vercel.app` lines updated
- `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-01-SUMMARY.md` (created) — this file

## Line-Level Diffs

```diff
# scripts/load-test.js:18
-const BASE_URL = __ENV.BASE_URL || 'https://irt-monitoring.vercel.app';
+const BASE_URL = __ENV.BASE_URL || 'https://otg-iran-monitor.vercel.app';

# scripts/load-test.spec.ts:16
-const PROD_URL = process.env.PROD_URL ?? 'https://irt-monitoring.vercel.app';
+const PROD_URL = process.env.PROD_URL ?? 'https://otg-iran-monitor.vercel.app';

# CHANGELOG.md:78
-#### Results (Production — https://irt-monitoring.vercel.app)
+#### Results (Production — https://otg-iran-monitor.vercel.app)

# docs/runbook.md:509
-   `https://irt-monitoring.vercel.app`), save, and redeploy.
+   `https://otg-iran-monitor.vercel.app`), save, and redeploy.

# ~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md:13-15
-**Project**: `zack-mazs-projects/irt-monitoring` on Vercel
-
-**Production URL**: `irt-monitoring.vercel.app`
+**Project**: `zack-mazs-projects/otg-iran-monitor` on Vercel
+
+**Production URL**: `otg-iran-monitor.vercel.app`
```

## Decisions Made

- **Task 0 verify-and-skip:** Plan rev1 (commit `10e3178`) already corrected the three `6-req/min` → `60-req/min` occurrences and added the doc-fix addendum on CONTEXT.md line 44. Source-of-truth `server/middleware/rateLimit.ts:131` (`createRateLimiter(60, 60, 'ratelimit:public')`) was re-confirmed at session start. Per plan instruction "may already be applied if the planner pre-edited during revision; verify-and-skip if grep returns 0", Task 0 records as a decision rather than a commit.
- **memory/reference_deployment.md scope:** This is an auto-memory file at `~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md`, NOT a repo file. Plan frontmatter listed it under `files_modified` as if it were a repo path. Treated as the auto-memory location and edited in place; not staged or committed (auto-memory has its own persistence and is read by Claude across sessions independent of git).
- **Task 6 resolved — Option A (in-place rename):** Operator picked Option A via orchestrator AskUserQuestion 2026-05-05. The Vercel project will be renamed in the dashboard (`irt-monitoring` → `otg-iran-monitor`); old `irt-monitoring.vercel.app` URL returns 404 after DNS propagation (~5 min). Options B (permanent alias) and C (vercel.json 308 redirect) were rejected — cleanest end-state preferred over hard-link compatibility. **No code change required**; this is an operator-side dashboard action. Plan 06 (W6 connectivity audit) unblocks once the operator confirms `curl -sI https://otg-iran-monitor.vercel.app/api/health | head -1` returns HTTP 200/2xx post-rename. The dashboard rename itself is captured as a USER-SETUP step (see User Setup Required below).

## Deviations from Plan

### Discovered Issues

**1. [Rule 2 - Missing Critical] Task 5 verification grep exclusions incomplete vs. actual repo state**

- **Found during:** Task 5 (post-substitution sweep)
- **Issue:** Plan's Task 5 verification command excludes `node_modules`, `dist`, `dist-server`, `.git`, `.planning/phases/` — but the repo also contains:
  1. `.claude/worktrees/agent-aecdb7f0/` — a separate parallel-agent git worktree at HEAD `0cafae6` (Phase 28.1 closeout state, locked, controlled by another agent process). Per `<destructive_git_prohibition>` and worktree-isolation contract, this is read-only from my perspective.
  2. `.planning/milestones/v1.2-phases/21.3-multi-user-load-testing/` — historical Phase 21.3 records (CHANGELOG, plan, summary, verification). Same immutability rule as `.planning/phases/` per plan Task 1's "older phase summaries are NOT to be edited (re-writing history is anti-pattern)" guidance.
  3. `load-test-results/k6-output.txt` and `load-test-results/k6-results.json` — historical k6 test artifacts captured during Phase 21.3 production run. Immutable test evidence.
- **Fix:** Recorded the three additional non-editable paths as out-of-scope (re-writing history is anti-pattern per plan Task 1). Re-ran the verification grep with full historical exclusions (`--exclude-dir=phases --exclude-dir=milestones --exclude-dir=worktrees --exclude-dir=load-test-results`) — returns ZERO `irt-monitoring` matches in editable code/docs. Plan-level success criteria met; only the verification command literal needed broader exclusions to match the plan's stated intent.
- **Files modified:** None (verification-only deviation).
- **Verification:** `grep -rn "irt-monitoring" --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=dist-server --exclude-dir=.git --exclude-dir=phases --exclude-dir=milestones --exclude-dir=worktrees --exclude-dir=load-test-results .` returns no matches (excluding `.planning/STATE.md` line 31 which is inside a Phase 28.1 W2 historical narrative — same immutability rule).
- **Committed in:** N/A (deviation is a finding/decision, not a code change). Recorded here for future plan-template improvement.

**2. [Rule 1 - Bug] memory/reference_deployment.md path in plan frontmatter is repo-relative; actual file lives in user-level auto-memory directory**

- **Found during:** Task 4 Commit C (memory/reference_deployment.md substitution)
- **Issue:** Plan `files_modified` lists `memory/reference_deployment.md` as if it were a repo path. The repo has no `memory/` directory; the file is at `~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md` (Claude Code per-project auto-memory).
- **Fix:** Edited the auto-memory file in place. Treated the substitution as complete but NOT a repo commit. Documented the path divergence in this SUMMARY's `key-files.modified` and `Files Created/Modified` sections so future agents grep correctly.
- **Files modified:** `~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md` (auto-memory, not a repo commit).
- **Verification:** `grep -n "irt-monitoring\|otg-iran-monitor" ~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md` returns lines 13 (`zack-mazs-projects/otg-iran-monitor`) and 15 (`otg-iran-monitor.vercel.app`); zero `irt-monitoring` occurrences remain.
- **Committed in:** N/A — auto-memory persists across sessions independently of git. The file's freshness reflects the new domain immediately.

---

**Total deviations:** 2 (1 missing-critical verification-spec, 1 path-mismatch finding). Both are observations on the plan/spec rather than code-correction work; no remediation commits required.

**Impact on plan:** Zero regression risk. All 5 confirmed editable substitutions landed atomically. Plan-level success criteria met (`grep -rln "otg-iran-monitor.vercel.app" scripts/load-test.js scripts/load-test.spec.ts CHANGELOG.md docs/runbook.md` lists all 4 repo files; auto-memory file confirmed updated). Task 6 checkpoint remains the only outstanding gate.

## Issues Encountered

- **Bash CWD vs. agent worktree disambiguation:** This agent's reported `Working directory` is `/Users/zackmaz/Desktop/my_world/.claude/worktrees/agent-aecdb7f0` (a separate worktree at HEAD `0cafae6`), but every `cd /Users/zackmaz/Desktop/my_world && ...` bash invocation operated on the **main checkout** at branch `feature/28.2-dev-prod-sync-domain-rename`. All commits landed on the correct branch (verified via `git -C /Users/zackmaz/Desktop/my_world worktree list` showing `9d2da12 [feature/28.2-dev-prod-sync-domain-rename]` for the main checkout). The worktree shadow at `.claude/worktrees/agent-aecdb7f0/` is a parallel-agent system artifact — left untouched per `<destructive_git_prohibition>`. Resolution: documented disambiguation in this SUMMARY so continuation agents grep against the right path tree.

## User Setup Required

**Vercel dashboard project rename (Task 6, Option A):** Operator goes to Vercel Settings → General → Project Name and renames `irt-monitoring` → `otg-iran-monitor`. The `*.vercel.app` alias auto-updates; old `irt-monitoring.vercel.app` URL returns 404 after DNS propagation (~5 min). Verification:

```bash
curl -sI https://otg-iran-monitor.vercel.app/api/health | head -1 | grep -E "^HTTP/[12](\.[0-9])? 200"
```

This must return HTTP 200 before Plan 06 (W6 connectivity audit) runs.

## Next Phase Readiness

- **Substitution pass complete.** Plans 02–05 (rate-limit Bearer-bypass, dev/prod gates, dashboard merge) can proceed in parallel waves; none depend on the Vercel-side domain alias.
- **Plan 06 (W6 connectivity audit) gated on operator dashboard rename.** Per CONTEXT D-25, the audit runs against `https://otg-iran-monitor.vercel.app` post-Vercel-rename. Once the operator completes Option A and the new domain returns 200 on `/api/health`, Plan 06 unblocks.
- **28.3 k6 sweep target locked.** `scripts/load-test.js:18` BASE_URL default now points at the new domain; no override needed during 28.3 PLAN drafting.

## Task 6 Resolution

**Decision:** Option A — in-place Vercel project rename (operator chose via orchestrator AskUserQuestion 2026-05-05). Old URL returns 404 post-rename. No vercel.json redirect rule; no permanent alias. Cleanest end-state.

**Rationale (operator-stated context):** Hard-link compatibility for the old URL is not required; the project domain is operator-internal and any external references can be updated in tandem. Permanent alias was rejected to avoid two URLs serving identical content (drift hazard); 308 redirect was rejected because the alias would still need to be configured to resolve, defeating the simplicity gain.

**Code change required:** None. This is an operator-side dashboard action.

**Verification gate for Plan 06:** Operator confirms `curl -sI https://otg-iran-monitor.vercel.app/api/health` returns HTTP 200/2xx after dashboard rename completes (~5 min DNS propagation).

---

_Phase: 28.2-dev-prod-sync-domain-rename_
_Plan: 01 (substitution pass)_
_Completed: 2026-05-05 (Tasks 0-5; Task 6 checkpoint pending)_

## Self-Check: PASSED

All four task-commit hashes (`5548b3a`, `a229628`, `6927e9d`, `9d2da12`) verified present on `feature/28.2-dev-prod-sync-domain-rename`. All four repo files contain the new `otg-iran-monitor.vercel.app` literal and zero `irt-monitoring` literals. Auto-memory file `~/.claude/projects/-Users-zackmaz-Desktop-my-world/memory/reference_deployment.md` confirmed updated. SUMMARY.md created at `.planning/phases/28.2-dev-prod-sync-domain-rename/28.2-01-SUMMARY.md`. Self-check executed via direct grep + `git log` cross-check 2026-05-05.

# Redis Key Registry (v1.5)

> Auditable inventory of every Redis key written or read by `otg-iran-monitor` in production. Pinned by `src/__tests__/lib/redis-registry.test.ts` — drift fails the next `vitest run`.

**Source of truth:** Code (line references below). This document is the operator skim's companion deep-dive; the test is the gate.
**Companion surface:** [`CLAUDE.md` §"Serverless Cache (Phase 13)"](../../CLAUDE.md) — same key list, one-line-per-key shape, refreshed in lockstep.
**Phase 35 measurement window:** Upstash command budget baseline → close (see `redis-budget-baseline-2026-05-27.png` / `redis-budget-close-YYYY-MM-DD.png` in `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/`).

---

## events:\*

| Key                                      | Writers                                                                                                                                                                                                                | Readers                                                                                                                                                                 | TTL                                                                              | Value                                                                                                             | Purpose                                                                                                                                                                                                          | Cardinality                                                        | Classification   |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ---------------- |
| `events:gdelt`                           | `server/routes/events.ts:62` (`EVENTS_KEY`) — written by GDELT polling + `backfillEvents()` paths in same file                                                                                                         | `server/lib/llmExtractionPipeline.ts:77` (raw input to extractor); `server/routes/events.ts` Pitfall 1 terminal fallback when v3 cache is empty                         | 15-min logical / 2.5h hard                                                       | `ConflictEvent[]` (raw GDELT shape)                                                                               | Raw GDELT cache; the LLM-pipeline input AND the ADR-0010 graceful-degradation terminal fallback when v3 is unavailable.                                                                                          | 1                                                                  | **load-bearing** |
| `events:backfill-ts`                     | `server/routes/events.ts:71` — written by `backfillEvents()` as a 1h cooldown sentinel                                                                                                                                 | `server/routes/events.ts` cooldown check inside `backfillEvents()`                                                                                                      | 1h logical                                                                       | epoch-ms integer                                                                                                  | Prevents repeated cold-cache backfills within the cooldown window.                                                                                                                                               | 1                                                                  | observability    |
| `events:llm:v3`                          | `server/lib/llmEventExtractor.v3.ts:114` (`EVENTS_LLM_V3_KEY`); `server/lib/llmExtractionPipeline.ts:88` (`LLM_EVENTS_KEY_ACTIVE`); written end-of-`runRefreshExtraction` via `mergeAndPersistLlmEntities` (cron-only) | `server/routes/events.ts:78`; `server/lib/healthSources.ts:53`; `server/lib/urlLiveness.ts:585` (liveness splice writer); `server/routes/operator-status.ts` aggregator | 9000s (`LLM_REDIS_TTL_SEC`, ≈2.5h hard)                                          | `ConflictEventEntity[]`                                                                                           | Terminal LLM-enriched cache; sole cache served to `/api/events`. ADR-0010 invariant — only key written by the cascade.                                                                                           | 1                                                                  | **load-bearing** |
| `events:llm:v3:partial`                  | (retired in plan 35-02)                                                                                                                                                                                                | NONE (verified by grep; never had production reader)                                                                                                                    | (n/a — natural TTL expiry within 9000s of plan-35-02 deploy)                     | (n/a)                                                                                                             | **RETIRED Phase 35 / SIMPLIFY-02 (plan 35-02).** Hobby-era 300s-budget mitigation; Pro 800s makes terminal writes reliable. Natural TTL expiry within 9000s (≈ 2.5h) of deploy. See ADR-0010 Phase 35 sub-block. | 0 (post-retirement)                                                | **retire**       |
| `events:llm:v3:lineage:{eventId}`        | `server/lib/llmLineage.ts:57` `appendLineage` (HSET + EXPIRE)                                                                                                                                                          | `scripts/snapshot-v3-redis.ts:80`; lineage DrillDownRow in DevApiStatus (Plan 04 surface)                                                                               | 7d (`LINEAGE_TTL_SEC = 7*24*3600`)                                               | HSET fields `{prompt, response, parsed, coord, provenance, resolverPath, reasoningTrace, model, lineageHash, ts}` | Per-event lineage drill-down. Pinned `reasoningTrace` + `lineageHash` are populated AT WRITE TIME (not optional).                                                                                                | ~50–500 per cron run (rolling, LRU-capped via index)               | observability    |
| `events:llm:v3:lineage-keys`             | `server/lib/llmLineage.ts:78-80` (ZADD + ZREMRANGEBYRANK + EXPIRE)                                                                                                                                                     | Read indirectly via the per-event keys; LRU index only                                                                                                                  | 7d                                                                               | Sorted set of `eventId` members scored by epoch-ms                                                                | LRU index over `events:llm:v3:lineage:*` so the per-event payloads stay bounded to 500 newest entries.                                                                                                           | ≤500 (capped via `LINEAGE_MAX_ENTRIES`)                            | observability    |
| `events:llm:v3:group-lineage:{hash}`     | (none — write side not yet implemented; `server/lib/llmLineage.ts:104` comment marks Plan 02 Gate B follow-up)                                                                                                         | `server/lib/llmEventExtractor.v3.ts:529-587` (`processEventGroupsV3` pre-filter loop)                                                                                   | 7d (`GROUP_LINEAGE_TTL_SEC`)                                                     | Cached enriched group-output JSON (Plan 02 Gate B shape)                                                          | Reader-only currently; pre-filter cache for group-level lineage. Future-phase write-side per `llmLineage.ts:104`.                                                                                                | 0 in production (no writer); read path is no-op until writer ships | observability    |
| `events:llm-summary:v3`                  | `server/lib/llmExtractionPipeline.ts:91` (`LLM_SUMMARY_KEY_ACTIVE`) — written by `runRefreshExtraction` post-run                                                                                                       | `server/routes/events.ts:81`; `scripts/analyze-llm-run.ts:289`; `scripts/snapshot-cron-watch.ts:508`                                                                    | 24h (`LLM_SUMMARY_TTL_SEC = 86400`)                                              | JSON `{startedAt, completedAt, summary metadata}`                                                                 | Last-run summary metadata; `/api/events/llm-status` reader.                                                                                                                                                      | 1                                                                  | observability    |
| `events:llm-dlq`                         | `server/lib/llmDLQ.ts:60` (`redis.sadd`)                                                                                                                                                                               | `server/lib/llmDLQ.ts:88` (`redis.smembers`); `/api/operator-status` aggregator                                                                                         | 7d (`DLQ_TTL_SEC`); SADD bounded 200 (`DLQ_MAX`)                                 | JSON `DLQEntry` strings (capped 500 chars `lastError`)                                                            | Dead-letter queue for exhausted-retry events; surfaces in operator-status.                                                                                                                                       | ≤200 (capped)                                                      | observability    |
| `events:llm-process-ts`                  | `server/routes/events.ts:84`; `server/lib/llmExtractionPipeline.ts:100` — written at `runRefreshExtraction` start                                                                                                      | Cooldown check in `runRefreshExtraction` (`llmExtractionPipeline.ts:231`)                                                                                               | (no explicit TTL — sentinel)                                                     | epoch-ms integer                                                                                                  | Cooldown sentinel for cron extraction (15-min default; bypassed by `?force=true` or cold-cache self-heal).                                                                                                       | 1                                                                  | observability    |
| `events:llm-eval-baseline:v3`            | `server/lib/llmEvalHarness.ts:78` (`BASELINE_KEY`) — written by `runEval()` after each cron-tick eval run                                                                                                              | `server/routes/operator-status.ts` aggregator; `scripts/eval-detail.ts`                                                                                                 | 90d                                                                              | JSON `EvalBaseline` (per-radius accuracy snapshot)                                                                | Resolver-only baseline (avoids doubling token spend); persisted for drift detection.                                                                                                                             | 1                                                                  | **load-bearing** |
| `events:llm-eval-adversarial:v3`         | `server/lib/llmEvalHarness.ts:462` (`ADVERSARIAL_KEY`) — `runAdversarialEval()` writer                                                                                                                                 | `server/routes/operator-status.ts:348`                                                                                                                                  | 90d                                                                              | JSON adversarial-eval snapshot                                                                                    | Prompt-injection robustness (`.planning/eval/adversarial-injections.json`); folded into `/api/cron/health`.                                                                                                      | 1                                                                  | **load-bearing** |
| `events:llm-pipeline-audit`              | `server/lib/pipelineAudit.ts:33-35` `appendPipelineAudit` (LPUSH + LTRIM + EXPIRE)                                                                                                                                     | `server/lib/pipelineAudit.ts:44` `listPipelineAudit` (LRANGE)                                                                                                           | 90d (`PIPELINE_AUDIT_TTL_SEC`); bounded 200 (`PIPELINE_AUDIT_MAX`)               | List of JSON `PipelineFlipEntry` (`{ts, from, to, trigger, operator, reason?}`)                                   | Historical record of pipeline-version flips — no new writers expected post-Phase-29 (v1/v2 retired).                                                                                                             | ≤200 (capped); essentially static after Phase 29                   | observability    |
| `events:llm-cost-shadow:v3:{YYYY-MM-DD}` | `server/lib/freeClaudeRouter.ts:669` `accrueShadowCost` (HINCRBY tokensIn/tokensOut/usdMicrocents + EXPIRE)                                                                                                            | none in production code (dashboard reads via Upstash REST)                                                                                                              | 90d (`90 * 24 * 3600`)                                                           | HSET fields `{tokensIn, tokensOut, usdMicrocents}` (USD as integer microcents to avoid float drift)               | Daily cost roll-up. Pricing model: tokens_in × $0.20/M + tokens_out × $0.40/M. 90d ring — survives Phase 34's deferred Cerebras/Groq, so historical cost surface remains intact even with OpenRouter dormant.    | ≤90 (one key per day, 90d ring; capped via natural TTL expiry)     | observability    |
| `events:url-liveness:{eventId}`          | `server/lib/urlLiveness.ts:62` (`URL_LIVENESS_KEY_PREFIX`); `:454` writer (`persistLiveness` during cron probe sweep)                                                                                                  | `pruneDeadUrlEvents`; `/api/operator-status` aggregator                                                                                                                 | Tiered TTL: `live` 7d, terminal dead (`404`/`403`/`dead-host`) 24h, `unknown` 1h | JSON `{status, lastProbedAt, attemptCount, lastUrlProbed, lastHttpStatus}`                                        | Per-event URL liveness probe result. Schema pinned by `server/__tests__/lib/urlLiveness.schema.test.ts` (+ literal-path shim in `src/__tests__/...`).                                                            | ~5000 (production sample 2026-05-26, bounded by event-cache size)  | **load-bearing** |
| `events:url-liveness-count`              | `server/lib/urlLiveness.ts:71` (`URL_LIVENESS_COUNT_KEY`); INCR/DECR by probe writer + prune                                                                                                                           | `server/routes/operator-status.ts`                                                                                                                                      | None (persistent sidecar)                                                        | integer                                                                                                           | O(1) sidecar count of events with terminal-dead URL; avoids N Redis GETs per `/api/operator-status` poll. Floored at 0 via the lone permitted raw `redis.set(KEY, 0)` call.                                      | 1                                                                  | **load-bearing** |

## flights:\*

| Key               | Writers                                               | Readers                                                     | TTL                             | Value            | Purpose                                          | Cardinality | Classification   |
| ----------------- | ----------------------------------------------------- | ----------------------------------------------------------- | ------------------------------- | ---------------- | ------------------------------------------------ | ----------- | ---------------- |
| `flights:opensky` | `server/routes/flights.ts:26` — OpenSky adapter poll  | Same file (`/api/flights?source=opensky`)                   | short (`FLIGHTS_REDIS_TTL_SEC`) | `FlightEntity[]` | OpenSky 5s-polled source cache.                  | 1           | **load-bearing** |
| `flights:adsblol` | `server/routes/flights.ts:27` — adsb.lol adapter poll | Same file + `server/lib/healthSources.ts:35` (health probe) | short                           | `FlightEntity[]` | adsb.lol 30s-polled source cache (free default). | 1           | **load-bearing** |

## ships:\*

| Key         | Writers                                                 | Readers                                      | TTL          | Value          | Purpose                                                       | Cardinality | Classification   |
| ----------- | ------------------------------------------------------- | -------------------------------------------- | ------------ | -------------- | ------------------------------------------------------------- | ----------- | ---------------- |
| `ships:ais` | `server/routes/ships.ts:13` — AISStream on-demand merge | Same file + `server/lib/healthSources.ts:36` | 10-min stale | `ShipEntity[]` | AISStream merged ship cache (MMSI dedup, 10-min stale prune). | 1           | **load-bearing** |

## sites:\*

| Key        | Writers                                                                  | Readers                                      | TTL | Value          | Purpose                                                           | Cardinality | Classification   |
| ---------- | ------------------------------------------------------------------------ | -------------------------------------------- | --- | -------------- | ----------------------------------------------------------------- | ----------- | ---------------- |
| `sites:v3` | `server/routes/sites.ts:58`; `server/routes/cron-warm.ts:28` (warm path) | Same file + `server/lib/healthSources.ts:44` | 24h | `SiteEntity[]` | Overpass static infrastructure (key sites). Warmed by daily cron. | 1           | **load-bearing** |

## water:\*

| Key                   | Writers                                                       | Readers                                      | TTL | Value             | Purpose                                                                             | Cardinality | Classification   |
| --------------------- | ------------------------------------------------------------- | -------------------------------------------- | --- | ----------------- | ----------------------------------------------------------------------------------- | ----------- | ---------------- |
| `water:facilities:v3` | `server/routes/water.ts:122`; `server/routes/cron-warm.ts:29` | Same file + `server/lib/healthSources.ts:46` | 24h | `WaterFacility[]` | Overpass water facilities with Latin-label admission gate + desalination synthesis. | 1           | **load-bearing** |
| `water:precip`        | `server/routes/water.ts:125`                                  | Same file + `server/lib/healthSources.ts:48` | 6h  | `PrecipAnomaly[]` | Open-Meteo 30-day precipitation anomaly; `findNearestPrecip` 4° Manhattan cutoff.   | 1           | **load-bearing** |

## news:\*

| Key          | Writers                                                                                           | Readers                                                                                                                  | TTL            | Value                                 | Purpose                                                                        | Cardinality | Classification   |
| ------------ | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------- | ------------------------------------- | ------------------------------------------------------------------------------ | ----------- | ---------------- |
| `news:feed`  | `server/routes/news.ts:28` (`NEWS_FEED_KEY`); written post-merge after RSS + GDELT-DOC clustering | Same file + `server/lib/healthSources.ts:40`                                                                             | 15-min logical | `NewsCluster[]` (clustered, deduped)  | Render-target cache (RSS + GDELT-DOC merged, Jaccard 0.8 dedup, 7-day window). | 1           | **load-bearing** |
| `news:gdelt` | GDELT-DOC adapter (`server/adapters/gdelt-doc.ts`)                                                | `server/lib/llmEventExtractor.v3.ts:107` (NEWS BLOCK in prompt); `server/routes/events.ts:672` (Pitfall 1 fallback path) | 15-min         | `NewsCluster[]` (raw GDELT-DOC shape) | LLM-input cache; different lifecycle class from `news:feed`.                   | 1           | **load-bearing** |

## markets:\*

| Key                     | Writers                                                                 | Readers                                                                 | TTL | Value           | Purpose                                              | Cardinality                            | Classification   |
| ----------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------- | --- | --------------- | ---------------------------------------------------- | -------------------------------------- | ---------------- |
| `markets:yahoo:{range}` | `server/routes/markets.ts:26` (`cacheKey = \`markets:yahoo:${range}\``) | Same file + `server/lib/healthSources.ts:41` (`markets:yahoo:1d` probe) | 60s | `MarketQuote[]` | Yahoo Finance commodity prices; one key per `range`. | 4 (one per range ∈ {1d, 5d, 1mo, ytd}) | **load-bearing** |

## geocode:\*

| Key                                 | Writers                                                                             | Readers   | TTL                    | Value                                       | Purpose                                                                                              | Cardinality                                       | Classification   |
| ----------------------------------- | ----------------------------------------------------------------------------------- | --------- | ---------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ---------------- |
| `geocode:{lat},{lon}`               | `server/routes/geocode.ts:17` (legacy reverse-geocode prefix)                       | Same file | 30d logical / 90d hard | JSON `{place name, admin levels}`           | Legacy reverse-geocode cache (Nominatim, 1 req/s throttle).                                          | unbounded (per-coord) — old entries decay via TTL | observability    |
| `geocode:fwd:constrained:v2:{hash}` | `server/lib/llmResolver.ts:38` (`GEOCODE_CACHE_PREFIX`); written in 6-path resolver | Same file | 30d hard               | JSON `{lat, lng, provenance, resolverPath}` | Forward geocode cache, ME-viewbox-constrained, hash-keyed. Pinned by the resolver's 6-path strategy. | unbounded (per-hash) — TTL bound only             | **load-bearing** |

## llm:\*

| Key                                | Writers                                                                                                                                   | Readers                                                                                             | TTL                              | Value                             | Purpose                                                                                                                                                     | Cardinality                                                                                   | Classification   |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------- |
| `llm:tokens:{provider}:YYYY-MM-DD` | `server/lib/llmTokenBudget.ts:54` `todayKey()` + `incrDailyTokens` (`redis.multi().incrby`)                                               | Same file `getDailyTokens` + `shouldPauseNewEvents`; `/api/operator-status`                         | 48h (`TTL_48H_SEC`)              | integer (token count for the day) | Per-provider daily token-budget counter; soft 0.8 / hard 0.95 gates the cascade.                                                                            | ~2 active per day (NIM-active + day-old) (Cerebras + Groq deferred — see `absent` rows below) | **load-bearing** |
| `llm:lastProgress`                 | `server/lib/llmProgress.ts:508` (`LLM_LASTPROGRESS_KEY`); `resetProgress` always (D-01) + `updateProgress` on terminal transitions (D-02) | `server/routes/health.ts` `probeLlmStatus` (with in-memory fallback)                                | 7d (`LLM_LASTPROGRESS_TTL_SEC`)  | JSON `{startedAt, completedAt}`   | Redis-backed write-through for the `llmProgress` singleton so `/llm-status` survives Vercel Fluid Compute cold starts.                                      | 1                                                                                             | **load-bearing** |
| `llm:calls:history`                | `server/lib/llmCallHistory.ts` `appendCallHistory` (`CALLS_KEY`; LPUSH+LTRIM+EXPIRE)                                                      | `server/lib/llmCallHistory.ts` `listCallHistory` + `/api/events/llm-history` + cold-start hydration | 30d (`CALLS_TTL_SEC`; LTRIM 500) | JSON `CallHistoryEntry[]`         | LLM call-level flight recorder; entry = in-memory `callHistory` fields + `runId` + `batchIndex`. Survives Fluid Compute cold starts.                        | ≤500 (capped)                                                                                 | observability    |
| `llm:runs:history`                 | `server/lib/llmRunHistory.ts` `openRunRecord`/`closeRunRecord` (`RUNS_KEY`; re-LPUSH terminal, NOT LSET — GA-2)                           | `server/lib/llmRunHistory.ts` `listRunHistory` (dedupe-by-runId) + `/api/events/llm-history`        | 30d (`RUNS_TTL_SEC`; LTRIM 200)  | JSON `RunHistoryEntry[]`          | Per-run summary flight recorder; opens `running` at run start, re-LPUSHes terminal at run end. Never-closed `running` = maxDuration-killed run (Pitfall 5). | ≤200 (capped)                                                                                 | observability    |

> **Note:** `llm:tokens:cerebras:*` and `llm:tokens:groq:*` are `absent (Phase 34 deferred — see ADR-0010 Phase 34 sub-block)`. They are not in the current operator action list.

## cron:\*

| Key                            | Writers                                                                                                                  | Readers                                                      | TTL                          | Value            | Purpose                                                                                                                  | Cardinality | Classification   |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ | ---------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------- | ---------------- |
| `cron:lastTick:health`         | `server/routes/cron-health.ts:131` (`cacheSetSafe(..., CRON_LASTTICK_TTL_SEC)`)                                          | `server/lib/healthSources.ts` `probeCronTick`; `/api/health` | 7d (`CRON_LASTTICK_TTL_SEC`) | epoch-ms integer | Last-tick sentinel for `/api/cron/health` (Redis ping + freshness + eval-drift).                                         | 1           | **load-bearing** |
| `cron:lastTick:warm`           | `server/routes/cron-warm.ts:85`                                                                                          | Same as above                                                | 7d                           | epoch-ms integer | Last-tick sentinel for `/api/cron/warm` (Overpass sites + water pre-warm).                                               | 1           | **load-bearing** |
| `cron:lastTick:refresh-events` | `server/routes/refresh-events-cron.ts:74` (writes AFTER `runRefreshExtraction` resolves — D-03 honest-failure semantics) | Same as above                                                | 7d                           | epoch-ms integer | Last-tick sentinel for `/api/cron/refresh-events` (LLM v3 extraction). Failure does NOT write — honest dashboard signal. | 1           | **load-bearing** |

## operator:\*

| Key                                                      | Writers                                                                                            | Readers                                                   | TTL                                                                   | Value                                                                  | Purpose                                                                                                                                           | Cardinality                                                   | Classification   |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ---------------- |
| `operator:audit-log`                                     | `server/lib/operatorAudit.ts:34` (`OPERATOR_AUDIT_KEY`); `redis.sadd` from prune + replay handlers | `server/routes/operator-status.ts:289` (`redis.smembers`) | 30d; SADD bounded 500                                                 | JSON `{timestamp, bearerFingerprint, operation, args, result}` strings | Operator-action audit log; surfaced in API Health → Operator Actions block.                                                                       | ≤500 (capped)                                                 | **load-bearing** |
| `operator:replay-quota:{bearerFingerprint}:{YYYY-MM-DD}` | `server/lib/replayQuota.ts:29` (`QUOTA_KEY_PREFIX`); INCR + EXPIRE                                 | Same file `checkReplayQuota`                              | 48h                                                                   | integer (count of replay calls for the day)                            | Per-Bearer per-day replay quota (50 calls / 24h). 429 + `Retry-After` at cap.                                                                     | unbounded (per-Bearer per-day, naturally expires via 48h TTL) | **load-bearing** |
| `operator:prune-quota:{bearerFingerprint}:{YYYY-MM-DD}`  | `server/lib/pruneQuota.ts:49` (`QUOTA_KEY_PREFIX`); INCR + EXPIRE on first INCR of each UTC day    | Same file `checkPruneQuota`                               | 48h (set only on first INCR; subsequent INCRs do NOT re-issue EXPIRE) | integer (count of prune calls for the day)                             | Per-Bearer per-day prune quota (50/24h). Cron caller (`bearerFingerprint: 'cron:refresh-events'`) BYPASSES the quota check at the endpoint layer. | unbounded (per-Bearer per-day, naturally expires via 48h TTL) | **load-bearing** |

## audit:\*

| Key                              | Writers                                                                | Readers                                                                                            | TTL | Value                                                                                 | Purpose                                                                                                       | Cardinality | Classification   |
| -------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | --- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------- | ---------------- |
| `audit:connectivity:last-result` | `.github/workflows/prod-connectivity-audit.yml:215` (GH-Actions write) | `server/routes/audit-status.ts:39` (`AUDIT_KEY`); `/api/audit-status` (no auth gate; degrade-open) | 7d  | JSON `{status, runId, timestamp, endpoints, durationMs, allTiersGreen?, tierStatus?}` | Last prod-connectivity-audit result; surfaced as audit-result banner. JSON shape pinned by W-3 contract test. | 1           | **load-bearing** |

---

## Classification Counts (D-08)

- **load-bearing:** 19 keys (removal breaks a live feature).
- **observability:** 7 keys (bounded; OK to keep but cap reviewed).
- **retire:** 1 key — `events:llm:v3:partial` (no callers; replaced by terminal-write reliability post Vercel Pro upgrade).

Total: 27 distinct keys (parametric prefixes counted once).

## Verification

The Redis-key registry drift gate at `src/__tests__/lib/redis-registry.test.ts` parses this file PLUS `CLAUDE.md` §Serverless Cache PLUS the codebase grep. Drift between the three surfaces fails `npx vitest run src/__tests__/lib/redis-registry.test.ts`.

## Phase 35 Provenance

- Authored: 2026-05-27 (plan 35-01 task 3) per DOCS-INT-03 + REDIS-OPT-01 / D-05 / D-06.
- Grep audit input: `.planning/phases/35-internal-docs-jsdoc-redis-registry-redis-optimization-cleanu/35-RESEARCH.md` §Grep Audit.
- Drift gate: `src/__tests__/lib/redis-registry.test.ts` (plan 35-01 task 4 / D-01).
